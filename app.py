<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>WeatherValet · v1.5.1</title>
<meta name="description" content="WeatherValet — Hand us the keys. We'll handle the weather.">

<!-- Security meta tags (May 17, 2026).
     The backend sets the real HTTP security headers (HSTS, X-Frame-Options,
     etc.) on its own responses. But weathervalet.ai itself is served as
     static HTML from Render Static, which doesn't set those headers
     automatically. These meta-equiv tags get the browser to enforce the
     same protections for THIS page.

     NOT included as meta tags:
       - Strict-Transport-Security (must be a real HTTP header — meta
         is ignored by browsers. Will be set when Cloudflare is in front.)
       - X-Frame-Options (same — must be HTTP header for full enforcement)

     What IS effective as meta:
       - Content-Security-Policy (browsers honor meta tag)
       - Referrer-Policy (browsers honor meta tag)
     -->
<meta http-equiv="X-Content-Type-Options" content="nosniff">
<meta name="referrer" content="strict-origin-when-cross-origin">
<meta http-equiv="Permissions-Policy" content="interest-cohort=()">

<!-- NO external scripts. NO React. NO Babel. NO icon libraries. NO frameworks. -->
<!-- Just one CSS file and one inline script. Works in any iframe, any host, even file://. -->

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600;700&family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700&display=swap" rel="stylesheet">

<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  html, body { min-height: 100vh; background: #FCFCFD; }
  body {
    font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, sans-serif;
    color: #0E1116;
    -webkit-font-smoothing: antialiased;
    line-height: 1.5;
  }
  button { font-family: inherit; cursor: pointer; border: none; background: none; padding: 0; color: inherit; }
  input { font-family: inherit; }
  .font-display { font-family: 'Fraunces', Georgia, serif; }
  .font-mono { font-family: 'JetBrains Mono', ui-monospace, monospace; }

  /* ── Page shell ── */
  /* On mobile/portrait, page is constrained to 720px so the layout
     reads cleanly on phones and tablets. On larger screens, the
     wrapper expands to 1100px so a 24" monitor doesn't feel like
     a narrow column with empty margins on either side. Inner
     content (search bar at 640px, steps/two-paths grids with their
     own max-widths) keeps its readable proportions; only the outer
     breathing room expands. */
  .page { max-width: 720px; margin: 0 auto; padding: 32px 24px 80px; position: relative; }
  @media (min-width: 920px) {
    .page { max-width: 1100px; padding-top: 56px; padding-bottom: 96px; }
  }

  /* When a workspace screen is active (Met, Crew, Admin), the page
     wrapper goes edge-to-edge — those zones own their own layout
     and need full bleed. Without this, .page's 32px top padding +
     24px side padding leaves white margins around the dark workspace
     screens, which looks like a broken empty rectangle. */
  body.is-workspace-active .page {
    max-width: none;
    padding: 0;
  }

  /* ── Atmospheric depth system ──────────────────────────────────────
     The visual identity rests on three layered moves that compound
     across every customer-facing screen:

     1. Ambient orbs — soft, blurred radial gradients in Royal Blue and
        warm amber. Fixed-position so they shift relative to scrolling
        content (gentle parallax effect). The reader always feels the
        atmosphere, never a flat background.

     2. Weather grid — a faint isobaric-pressure-line texture sitting at
        ~2% opacity, signals "weather" without theatrics. The same
        tactile-rebellion move premium 2026 design is moving toward.

     3. Vignette wash — a soft radial light at the top center that
        creates a focal point and adds dimensionality without an
        obvious gradient. Subtle but present.

     All three layers sit behind .page-content (z-index 1) so content
     reads cleanly above them. */

  /* Layer 1: ambient orbs.
     One blue, top-right; one amber, bottom-left; one additional
     deeper-blue orb mid-page-left for depth on long-scroll surfaces.
     All fixed-position; blur radius is high so they read as light,
     not shapes. */
  .orb {
    position: fixed; border-radius: 999px;
    pointer-events: none;
    z-index: 0;
    will-change: transform;
  }
  .orb-blue {
    top: -180px; right: -160px;
    width: 560px; height: 560px;
    background: radial-gradient(circle at center, rgba(65,105,225,0.14), rgba(65,105,225,0) 70%);
    filter: blur(40px);
  }
  .orb-amber {
    bottom: -200px; left: -140px;
    width: 480px; height: 480px;
    background: radial-gradient(circle at center, rgba(245,165,36,0.10), rgba(245,165,36,0) 70%);
    filter: blur(40px);
  }
  .orb-deep {
    top: 40%; left: -200px;
    width: 420px; height: 420px;
    background: radial-gradient(circle at center, rgba(46,79,184,0.08), rgba(46,79,184,0) 70%);
    filter: blur(60px);
  }

  /* Layer 2: weather grid texture.
     A faint isobaric-line pattern, generated via repeating linear
     gradients so it ships as zero KB of image bytes. Sits at 2%
     opacity — present in peripheral vision, invisible in foveal
     reading. Two crossed patterns at offset angles create the
     "topographic chart" feel without being a grid. */
  .weather-grid {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background-image:
      repeating-linear-gradient(
        118deg,
        transparent 0px,
        transparent 96px,
        rgba(15, 17, 22, 0.014) 96px,
        rgba(15, 17, 22, 0.014) 97px
      ),
      repeating-linear-gradient(
        62deg,
        transparent 0px,
        transparent 128px,
        rgba(15, 17, 22, 0.010) 128px,
        rgba(15, 17, 22, 0.010) 129px
      );
    mask-image: radial-gradient(ellipse at center, black 20%, transparent 75%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 20%, transparent 75%);
    opacity: 0.7;
  }

  /* Layer 3: top vignette wash.
     Soft radial light at top-center, creates a "lit from above"
     dimensionality. Especially helpful on long-scroll pages where
     pure white can read clinical. */
  .vignette-wash {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background: radial-gradient(
      ellipse 1200px 600px at 50% -200px,
      rgba(65, 105, 225, 0.06),
      transparent 70%
    );
  }

  /* Workspace screens (Met/Crew-WS/Admin) have their own dark theme
     and shouldn't show these atmospheric layers — they'd read as
     bugs over the dark background. */
  body.is-workspace-active .orb,
  body.is-workspace-active .weather-grid,
  body.is-workspace-active .vignette-wash {
    display: none;
  }

  .page-content { position: relative; z-index: 1; }

  /* Hide screens not in active state */
  .screen { display: none; }
  .screen.active { display: block; }

  /* ── Header ── */
  .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 56px; }
  .brand {
    display: flex;
    align-items: center;
    gap: 7px;
    text-decoration: none;     /* in case it's an <a> */
    color: inherit;            /* don't inherit blue link color */
    cursor: pointer;
  }
  .brand:hover .brand-name {
    color: #2E4FB8;            /* subtle hover hint that it's clickable */
  }
  .brand:hover .brand-tld {
    color: rgba(46, 79, 184, 0.55);
  }
  .brand-mark {
    width: 16px; height: 22px;
    flex-shrink: 0;
  }
  .brand-name { font-size: 18px; font-weight: 800; letter-spacing: -0.4px; color: #0E1116; }
  .brand-tld { color: rgba(15,17,22,0.40); }

  .profile-chip {
    width: 36px; height: 36px; border-radius: 999px;
    background: rgba(255,255,255,0.65);
    border: 1px solid rgba(15,17,22,0.08);
    display: flex; align-items: center; justify-content: center;
  }
  .profile-avatar {
    width: 28px; height: 28px; border-radius: 999px;
    background: #4169E1; color: #fff;
    display: flex; align-items: center; justify-content: center;
    font-weight: 700; font-size: 12px;
  }

  /* ── Eyebrow ── */
  .eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 6px 12px; border-radius: 999px;
    background: rgba(65,105,225,0.06);
    border: 1px solid rgba(65,105,225,0.18);
    font-family: 'JetBrains Mono'; font-size: 10.5px; font-weight: 700;
    color: #4169E1; letter-spacing: 1.6px; text-transform: uppercase;
    margin-bottom: 24px;
  }
  .eyebrow-dot {
    width: 6px; height: 6px; border-radius: 999px;
    background: #4169E1;
    position: relative;
  }
  /* Signature motion: the accent dot has a gentle pulsing halo —
     a continuous soft outward ripple. This is the visual heartbeat
     of the brand, the same one that appears on every eyebrow across
     the site (welcome, pricing, lockscreen, crew). Subtle enough to
     not distract, present enough to feel alive. */
  .eyebrow-dot::after,
  .pricing-hero-eyebrow-dot::after,
  .lockscreen-eyebrow-dot::after {
    content: '';
    position: absolute;
    inset: -3px;
    border-radius: 999px;
    background: inherit;
    opacity: 0.35;
    animation: dotPulse 2.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    pointer-events: none;
  }
  .pricing-hero-eyebrow-dot,
  .lockscreen-eyebrow-dot {
    position: relative;
  }
  @keyframes dotPulse {
    0%, 100% { transform: scale(1); opacity: 0.35; }
    50% { transform: scale(2.4); opacity: 0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .eyebrow-dot::after,
    .pricing-hero-eyebrow-dot::after,
    .lockscreen-eyebrow-dot::after { animation: none; }
  }

  /* ── Hero ── */
  .hero { text-align: center; margin-top: 56px; }
  .headline {
    font-family: 'Fraunces';
    font-size: 48px; font-weight: 600;
    line-height: 1.05; letter-spacing: -1.5px;
    color: #0E1116;
  }
  .headline em {
    font-style: italic;
    font-weight: 500;
    /* Subtle gradient on the italic — Royal Blue with a hint of a
       slightly more saturated mid-tone. Adds dimensionality to the
       hero's signature word without screaming. */
    background: linear-gradient(135deg, #4169E1 0%, #2E4FB8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    /* Slightly tighter letter-spacing on the italic, optical
       refinement that distinguishes it as a stylistic accent. */
    letter-spacing: -1px;
  }
  /* ── Rotating-word inside the headline ──
     The word slides up and fades in/out every few seconds. We use
     inline-block so the span participates in inline layout while
     allowing transform (which doesn't work on inline elements).
     The width is fluid — the headline reflows when the word changes
     length ("party" vs "weekend getaway"). This is acceptable because
     reflows happen during the brief invisible moment when one word
     has faded out and the next hasn't faded in yet. To users it just
     looks like the new word arriving in place. */
  .rotating-word {
    display: inline-block;
    position: relative;
    will-change: transform, opacity;
    /* Re-apply the same gradient + text-clip as the parent <em>. When the
       span is transformed (translateY) during entering/leaving animations,
       WebKit re-renders the clipped child outside the parent's gradient
       bounding box, which makes the text appear white/transparent below
       the headline. Painting our own clipped gradient onto the span itself
       keeps the color stable through the slide. */
    background: linear-gradient(135deg, #4169E1 0%, #2E4FB8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  /* "Entering" state: word starts below + transparent, animates to
     in-place + visible. Used the moment we swap to the new word. */
  .rotating-word.is-entering {
    animation: rotatingWordIn 420ms cubic-bezier(0.16, 1, 0.3, 1) both;
  }
  /* "Leaving" state: word starts in-place + visible, animates up + out.
     Used in the moment just before we swap to the next word. */
  .rotating-word.is-leaving {
    animation: rotatingWordOut 280ms cubic-bezier(0.4, 0, 0.7, 0.2) both;
  }
  @keyframes rotatingWordIn {
    from {
      opacity: 0;
      transform: translateY(0.4em);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes rotatingWordOut {
    from {
      opacity: 1;
      transform: translateY(0);
    }
    to {
      opacity: 0;
      transform: translateY(-0.4em);
    }
  }
  /* Respect users who've asked to reduce motion — show the word
     without animation. The text still rotates, just doesn't slide. */
  @media (prefers-reduced-motion: reduce) {
    .rotating-word.is-entering,
    .rotating-word.is-leaving {
      animation: none;
    }
  }
  .subhead {
    font-family: 'Fraunces';
    font-size: 17px; line-height: 1.55;
    color: rgba(15,17,22,0.55);
    margin: 20px auto 0; max-width: 460px;
  }
  /* Desktop: scale up the hero so it owns the wider canvas. The
     headline grows ~40% larger; subhead and hero margin scale
     proportionally so the whole hero reads as one cohesive unit
     at a viewing distance of 2-3 feet from a monitor. */
  @media (min-width: 920px) {
    .hero { margin-top: 88px; }
    .headline {
      font-size: 68px;
      letter-spacing: -2.2px;
    }
    .subhead {
      font-size: 19px;
      max-width: 540px;
      margin-top: 24px;
    }
  }

  /* ── Search bar ── */
  .search-form { margin-top: 56px; }
  .search-bar {
    display: flex; align-items: center; gap: 8px;
    background: linear-gradient(135deg, #5B7BFF, #4169E1);
    padding: 6px 6px 6px 20px;
    border-radius: 999px;
    box-shadow: 0 12px 40px rgba(65,105,225,0.30), inset 0 1px 0 rgba(255,255,255,0.2);
  }
  .search-icon { width: 20px; height: 20px; color: rgba(255,255,255,0.85); flex-shrink: 0; }
  .search-input {
    flex: 1; min-width: 0;
    background: transparent; border: none; outline: none;
    color: #fff; font-size: 15px; font-weight: 500;
    padding: 14px 4px;
  }
  .search-input::placeholder { color: rgba(255,255,255,0.85); font-weight: 400; }
  .search-submit {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 12px 18px; border-radius: 999px;
    background: #fff; color: #4169E1;
    font-weight: 700; font-size: 13px;
    box-shadow: 0 4px 12px rgba(15,17,22,0.10);
    transition: transform 200ms;
    /* Defense: ensure the button itself is always the click target */
    position: relative;
    z-index: 1;
    -webkit-tap-highlight-color: rgba(65,105,225,0.20);
    touch-action: manipulation;
  }
  .search-submit:hover { transform: translateY(-1px); }
  .search-submit:active { transform: translateY(0) scale(0.98); }
  .search-submit svg {
    /* Defense: SVG never captures clicks — button always does */
    pointer-events: none;
  }
  /* Mobile — tighten the submit pill so the input placeholder has
     room to display fully. At 390px-wide iPhone the default padding
     was pushing "Hand off" so wide that the placeholder truncated
     mid-word ("What are you handir"). */
  @media (max-width: 480px) {
    .search-bar {
      padding: 6px 6px 6px 16px;
      gap: 6px;
    }
    .search-input { font-size: 14px; padding: 13px 2px; }
    .search-submit {
      padding: 10px 14px;
      font-size: 12.5px;
    }
  }

  .search-hint {
    text-align: center;
    font-family: 'JetBrains Mono'; font-size: 11px;
    color: rgba(15,17,22,0.40);
    margin-top: 12px;
  }

  /* Quiet secondary entry point: a small chip with a Royal Blue accent dot
     that points to the Crew side of the app. Sits centered below the
     search bar and the search hint, separated by a thin divider so it
     reads as "or — try this other thing." Designed to be discoverable
     without competing visually with the primary search action. */
  .crew-quick-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin: 16px auto 0;
    padding: 10px 18px 10px 16px;
    border-radius: 999px;
    background: rgba(65,105,225,0.06);
    border: 1px solid rgba(65,105,225,0.16);
    color: var(--rb, #4169E1);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    text-decoration: none;
    cursor: pointer;
    transition: all 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-quick-link:hover {
    background: rgba(65,105,225,0.10);
    border-color: rgba(65,105,225,0.30);
    transform: translateY(-1px);
  }
  .crew-quick-link:active { transform: translateY(0); }
  .crew-quick-link-dot {
    width: 8px; height: 8px; border-radius: 50%;
    background: var(--rb, #4169E1);
    box-shadow: 0 0 0 3px rgba(65,105,225,0.20);
    /* Soft pulse — signals "live activity behind this link" */
    animation: crewLinkPulse 2.4s ease-in-out infinite;
  }
  @keyframes crewLinkPulse {
    0%, 100% { box-shadow: 0 0 0 3px rgba(65,105,225,0.20); }
    50%      { box-shadow: 0 0 0 5px rgba(65,105,225,0.10); }
  }
  .crew-quick-link-arrow {
    color: rgba(65,105,225,0.65);
  }
  .crew-quick-link-wrap {
    text-align: center;
    margin-top: 4px;
  }

  /* ── Search wrap (positioning anchor for dropdown) ── */
  .search-wrap {
    position: relative;
    margin: 0 auto;
    max-width: 640px;
  }
  /* Desktop: search bar grows to 720px so it has more presence on
     a wider canvas without becoming oversized. */
  @media (min-width: 920px) {
    .search-wrap { max-width: 720px; }
  }

  /* ── Search dropdown — hidden by default, premium card on focus ── */
  .search-dropdown {
    position: absolute;
    top: calc(100% + 12px);
    left: 0; right: 0;
    z-index: 20;
    padding: 10px;
    background: rgba(255,255,255,0.96);
    backdrop-filter: blur(20px) saturate(1.4);
    -webkit-backdrop-filter: blur(20px) saturate(1.4);
    border: 1px solid rgba(15,17,22,0.08);
    border-radius: 18px;
    box-shadow:
      0 4px 12px rgba(15,17,22,0.04),
      0 24px 48px rgba(15,17,22,0.10);
    /* Hidden by default */
    opacity: 0;
    transform: translateY(-6px);
    pointer-events: none;
    transition: opacity 220ms cubic-bezier(0.16,1,0.3,1), transform 220ms cubic-bezier(0.16,1,0.3,1);
  }
  .search-wrap.is-focused .search-dropdown {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .search-dropdown-head {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: rgba(15,17,22,0.45);
    padding: 6px 14px 8px;
  }

  /* Chips inside the dropdown */
  .chip {
    display: flex; align-items: center; gap: 12px;
    width: 100%;
    text-align: left;
    padding: 10px 14px;
    border-radius: 12px;
    background: transparent;
    border: 1px solid transparent;
    font-size: 13.5px; font-weight: 500;
    color: rgba(15,17,22,0.78);
    transition: all 150ms;
    -webkit-tap-highlight-color: rgba(65,105,225,0.10);
    touch-action: manipulation;
  }
  .chip:hover, .chip:focus-visible {
    background: rgba(65,105,225,0.05);
    border-color: rgba(65,105,225,0.18);
    color: #0E1116;
    outline: none;
  }
  .chip-icon {
    flex-shrink: 0;
    width: 14px; height: 14px;
    color: rgba(65,105,225,0.55);
  }

  /* ── Three steps ── */
  .steps { margin-top: 140px; display: grid; grid-template-columns: 1fr; gap: 32px; }
  @media (min-width: 720px) { .steps { grid-template-columns: 1fr 1fr 1fr; gap: 24px; } }
  .step-head { display: flex; align-items: center; gap: 10px; margin-bottom: 16px; }
  .step-num {
    font-family: 'JetBrains Mono'; font-size: 11px; font-weight: 700;
    color: #4169E1; letter-spacing: 2.4px;
  }
  .step-rule {
    flex: 1; height: 1px;
    /* A gradient rule fades from the brand accent outward, more
       elegant than a flat line. Frequent design tell — Stripe and
       Linear both do this. */
    background: linear-gradient(90deg,
      rgba(65,105,225,0.35) 0%,
      rgba(65,105,225,0.10) 80%,
      transparent 100%);
  }
  .step-title {
    font-family: 'Fraunces'; font-size: 20px; font-weight: 600;
    color: #0E1116; letter-spacing: -0.4px; margin-bottom: 10px;
  }
  .step-body {
    font-family: 'Fraunces'; font-size: 14.5px; line-height: 1.6;
    color: rgba(15,17,22,0.60);
  }

  /* ── Two paths — front-door clarity for who WeatherValet is for ──
     Two side-by-side cards explaining the Subscriber and Valet Crew
     paths. A free walk-up user sees the search and uses it; the cards
     tell them what comes next if they want more. No upsell pressure,
     no modal — just present.

     Hover lifts the card and shifts the arrow forward to invite the
     click. Subtle, not bouncy. */
  .two-paths {
    margin-top: 96px;
    padding-top: 56px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
  }
  .two-paths-eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.85);
    margin-bottom: 14px;
  }
  .two-paths-eyebrow-dot {
    width: 6px; height: 6px; border-radius: 999px;
    background: #4169E1;
  }
  .two-paths-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 30px;
    font-weight: 600;
    letter-spacing: -0.5px;
    line-height: 1.2;
    margin: 0 0 12px;
    color: #0E1116;
  }
  .two-paths-sub {
    font-size: 15px;
    color: rgba(15, 17, 22, 0.65);
    margin: 0 0 32px;
    line-height: 1.5;
    max-width: 560px;
  }
  .two-paths-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 18px;
  }
  @media (min-width: 720px) {
    .two-paths-grid { grid-template-columns: 1fr 1fr; gap: 22px; }
  }

  /* The cards themselves. Two distinct visual identities: subscriber
     card leans Royal Blue (the brand), Crew card leans amber (the
     warm "join us" call). Both share the same structural language. */
  .two-paths-card {
    position: relative;
    padding: 28px 26px 24px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 18px;
    text-align: left;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: transform 220ms cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 220ms ease,
                border-color 220ms ease;
    box-shadow: 0 1px 2px rgba(15, 17, 22, 0.04);
    overflow: hidden;
    display: flex; flex-direction: column;
  }
  .two-paths-card:hover {
    transform: translateY(-3px);
    box-shadow:
      0 4px 12px rgba(15, 17, 22, 0.06),
      0 16px 36px rgba(15, 17, 22, 0.08);
  }
  /* A subtle accent stripe slides in from the left on hover —
     "you're heading somewhere" cue. */
  .two-paths-card::before {
    content: '';
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 4px;
    background: currentColor;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .two-paths-card:hover::before { transform: scaleX(1); }
  .two-paths-card.path-subscriber { color: #4169E1; }
  .two-paths-card.path-subscriber:hover { border-color: rgba(65, 105, 225, 0.30); }
  .two-paths-card.path-crew { color: #C28412; }
  .two-paths-card.path-crew:hover { border-color: rgba(245, 165, 36, 0.40); }

  .two-paths-card-icon {
    width: 38px; height: 38px;
    border-radius: 10px;
    background: currentColor;
    color: #fff;
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 16px;
  }
  .two-paths-card.path-subscriber .two-paths-card-icon {
    background: rgba(65, 105, 225, 0.10);
    color: #4169E1;
  }
  .two-paths-card.path-crew .two-paths-card-icon {
    background: rgba(245, 165, 36, 0.14);
    color: #C28412;
  }

  .two-paths-card-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: currentColor;
    margin-bottom: 6px;
  }
  .two-paths-card-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -0.3px;
    color: #0E1116;
    margin: 0 0 10px;
    line-height: 1.25;
  }
  .two-paths-card-body {
    font-size: 14px;
    color: rgba(15, 17, 22, 0.68);
    line-height: 1.55;
    margin: 0 0 18px;
    flex-grow: 1;
  }
  .two-paths-card-bullets {
    list-style: none;
    padding: 0; margin: 0 0 22px;
    display: flex; flex-direction: column; gap: 8px;
  }
  .two-paths-card-bullets li {
    display: flex; align-items: flex-start; gap: 10px;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.78);
    line-height: 1.5;
  }
  .two-paths-card-bullet-dot {
    flex-shrink: 0;
    width: 5px; height: 5px;
    border-radius: 50%;
    background: currentColor;
    margin-top: 8px;
  }
  .two-paths-card-cta {
    display: inline-flex; align-items: center; gap: 8px;
    font-size: 13.5px;
    font-weight: 700;
    color: currentColor;
    margin-top: auto;
  }
  .two-paths-card-cta-arrow {
    transition: transform 240ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .two-paths-card:hover .two-paths-card-cta-arrow {
    transform: translateX(4px);
  }
  .two-paths-card-pricing {
    font-size: 12px;
    color: rgba(15, 17, 22, 0.45);
    margin-top: 8px;
    font-style: italic;
  }

  /* ── Pricing page ─────────────────────────────────────────────────
       Public-facing pricing surface, shown when a prospect clicks
       "See pricing" from the Subscriber path. Five tiers laid out in
       two visual rows: individual (Free + AI) and business (Pro
       Single, Pro Multi, Pro Enterprise). Uses "starting at" language
       to keep numbers adjustable until the market study is complete. */
  .pricing-page {
    padding-top: 24px;
  }
  .pricing-back {
    display: inline-flex; align-items: center; gap: 6px;
    background: transparent; border: none;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px; font-weight: 600;
    color: var(--rb, #4169E1);
    cursor: pointer;
    padding: 8px 0;
    margin-bottom: 20px;
    -webkit-tap-highlight-color: transparent;
  }
  .pricing-back:hover { text-decoration: underline; }
  .pricing-hero {
    text-align: center;
    margin-bottom: 56px;
  }
  .pricing-hero-eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 6px 14px;
    background: rgba(65, 105, 225, 0.08);
    border-radius: 999px;
    margin-bottom: 18px;
  }
  .pricing-hero-eyebrow-dot {
    width: 6px; height: 6px; border-radius: 999px;
    background: var(--rb, #4169E1);
  }
  .pricing-hero-eyebrow-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; font-weight: 700;
    letter-spacing: 1.4px; text-transform: uppercase;
    color: var(--rb, #4169E1);
  }
  .pricing-hero-headline {
    font-family: 'Fraunces';
    font-size: 40px; font-weight: 600;
    line-height: 1.1; letter-spacing: -1.2px;
    color: #0E1116;
    margin: 0 0 16px;
  }
  @media (min-width: 920px) {
    .pricing-hero-headline { font-size: 52px; letter-spacing: -1.8px; }
  }
  .pricing-hero-sub {
    font-family: 'Fraunces';
    font-size: 17px; line-height: 1.55;
    color: rgba(15,17,22,0.60);
    margin: 0 auto;
    max-width: 540px;
  }

  /* Tier-group section labels (Individual / Business) */
  .pricing-section-label {
    display: flex; align-items: center; gap: 14px;
    margin: 0 0 22px;
  }
  .pricing-section-label-text {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px; font-weight: 700;
    letter-spacing: 1.6px; text-transform: uppercase;
    color: rgba(15,17,22,0.55);
    white-space: nowrap;
  }
  .pricing-section-label-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg,
      rgba(15,17,22,0.18) 0%,
      rgba(15,17,22,0.06) 80%,
      transparent 100%);
  }
  .pricing-section + .pricing-section { margin-top: 56px; }

  /* Tier card grid — responsive: 1 col mobile, 2 col tablet,
     up to 3 col desktop for the Pro row. */
  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
  }
  @media (min-width: 720px) {
    .pricing-grid-2 { grid-template-columns: 1fr 1fr; gap: 20px; }
    .pricing-grid-3 { grid-template-columns: 1fr 1fr 1fr; gap: 18px; }
  }

  /* Individual tier cards (Free + AI) — lighter visual weight */
  .pricing-card {
    background: #fff;
    /* Multi-stop shadow stack: a sharp inner edge highlight (1px white)
       plus a soft drop. Reads as "lit" rather than flat. */
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 1px 2px rgba(15, 17, 22, 0.04),
      0 8px 24px rgba(15, 17, 22, 0.03);
    border: 1px solid rgba(15,17,22,0.06);
    border-radius: 20px;
    padding: 30px 26px;
    display: flex; flex-direction: column;
    position: relative;
    transition: transform 240ms cubic-bezier(0.2, 0.8, 0.2, 1),
                box-shadow 240ms ease,
                border-color 240ms ease;
  }
  .pricing-card:hover {
    transform: translateY(-4px);
    border-color: rgba(65, 105, 225, 0.18);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 2px 4px rgba(15, 17, 22, 0.04),
      0 16px 40px rgba(15, 17, 22, 0.07),
      0 4px 16px rgba(65, 105, 225, 0.06);
  }

  /* Featured / "Most popular" treatment — Pro Single gets this.
     The card has a subtle Royal Blue gradient wash from top-right
     to bottom-left, plus a brighter border and stronger shadow.
     Reads as the "lit" card in the row without being garish. */
  .pricing-card.is-featured {
    border: 1px solid rgba(65, 105, 225, 0.40);
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.04) 0%,
        rgba(255, 255, 255, 1) 60%,
        rgba(245, 165, 36, 0.025) 100%),
      #fff;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 1px 2px rgba(65, 105, 225, 0.06),
      0 16px 48px rgba(65, 105, 225, 0.14),
      0 4px 16px rgba(15, 17, 22, 0.04);
  }
  .pricing-card.is-featured:hover {
    transform: translateY(-6px);
    border-color: var(--rb, #4169E1);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 2px 4px rgba(65, 105, 225, 0.08),
      0 24px 56px rgba(65, 105, 225, 0.18),
      0 6px 20px rgba(15, 17, 22, 0.06);
  }
  /* "Most Popular" pill — sits *inside* the card at the top now,
     anchored just below the top edge with a small floating offset.
     Reads as part of the card, not a sticker poking up out of it.
     Royal Blue background, white text, soft glow. */
  .pricing-card.is-featured::before {
    content: 'MOST POPULAR';
    position: absolute;
    top: -1px;
    left: 50%;
    transform: translate(-50%, -50%);
    padding: 6px 14px;
    background: var(--rb, #4169E1);
    color: #fff;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.6px;
    box-shadow: 0 4px 12px rgba(65, 105, 225, 0.35);
    /* Subtle inner highlight on the pill so it reads as a real
       physical object, not just a colored rectangle. */
    background-image: linear-gradient(180deg, rgba(255,255,255,0.18), rgba(255,255,255,0) 50%);
    background-color: var(--rb, #4169E1);
  }

  .pricing-card-name {
    font-family: 'Fraunces';
    font-size: 22px; font-weight: 600;
    letter-spacing: -0.4px;
    color: #0E1116;
    margin: 0 0 4px;
  }
  .pricing-card-tagline {
    font-size: 12.5px;
    color: rgba(15,17,22,0.60);
    line-height: 1.45;
    margin: 0 0 20px;
    min-height: 32px;
  }
  .pricing-card-price {
    display: flex; align-items: baseline; gap: 6px;
    margin-bottom: 4px;
  }
  .pricing-card-price-prefix {
    font-size: 12px;
    color: rgba(15,17,22,0.55);
    font-weight: 500;
  }
  .pricing-card-price-amount {
    font-family: 'Fraunces';
    font-size: 36px; font-weight: 600;
    letter-spacing: -1px;
    color: #0E1116;
    line-height: 1;
  }
  .pricing-card-price-unit {
    font-size: 13px;
    color: rgba(15,17,22,0.55);
  }
  .pricing-card-price-annual {
    font-size: 11.5px;
    color: rgba(15,17,22,0.45);
    font-style: italic;
    margin-bottom: 22px;
    min-height: 16px;
  }
  .pricing-card-features {
    list-style: none;
    padding: 0;
    margin: 0 0 24px;
    display: flex; flex-direction: column;
    gap: 10px;
    flex-grow: 1;
  }
  .pricing-card-features li {
    display: flex; align-items: flex-start; gap: 10px;
    font-size: 13px;
    line-height: 1.5;
    color: rgba(15,17,22,0.82);
  }
  .pricing-card-features li svg {
    flex-shrink: 0;
    color: var(--rb, #4169E1);
    margin-top: 3px;
  }
  .pricing-card-cta {
    display: inline-flex; align-items: center; justify-content: center;
    gap: 8px;
    padding: 14px 18px;
    /* Two-stop gradient gives the button physical depth. The light
       comes from the top, the bottom catches a slightly darker
       saturated tone. Reads as a real surface, not flat color. */
    background: linear-gradient(180deg, #5278EA 0%, #4169E1 50%, #3257CC 100%);
    color: #fff;
    border: none;
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px; font-weight: 600;
    letter-spacing: 0.1px;
    cursor: pointer;
    text-decoration: none;
    text-align: center;
    -webkit-tap-highlight-color: transparent;
    /* Shadow stack: inset top highlight (the "lit edge"), inset
       bottom shadow (the "shadow side"), drop shadow (resting glow). */
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.20),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 1px 2px rgba(65, 105, 225, 0.30),
      0 4px 12px rgba(65, 105, 225, 0.18);
    transition: transform 120ms cubic-bezier(0.2, 0.8, 0.2, 1),
                box-shadow 220ms ease,
                background 220ms ease;
  }
  .pricing-card-cta:hover {
    background: linear-gradient(180deg, #5C81EE 0%, #4972E5 50%, #355DD2 100%);
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.25),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 2px 4px rgba(65, 105, 225, 0.36),
      0 12px 24px rgba(65, 105, 225, 0.28);
  }
  .pricing-card-cta:active {
    transform: translateY(0);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.15),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 1px 2px rgba(65, 105, 225, 0.30);
  }
  /* Secondary CTA style — used for Free tier (no payment, just start).
     Outlined rather than filled; signals "low-commitment entry point." */
  .pricing-card-cta.is-secondary {
    background: transparent;
    color: var(--rb, #4169E1);
    border: 1.5px solid rgba(65, 105, 225, 0.30);
    box-shadow: none;
  }
  .pricing-card-cta.is-secondary:hover {
    background: rgba(65, 105, 225, 0.06);
    border-color: var(--rb, #4169E1);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(65, 105, 225, 0.08);
  }

  /* Below-the-button quieter link: "questions before you subscribe? Email us"
     Appears under Pro Single + Pro Multi CTAs to give buyers who need a
     conversation a path, without obscuring the self-serve primary action. */
  .pricing-card-cta-secondary {
    display: block;
    text-align: center;
    margin-top: 10px;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
    text-decoration: none;
    transition: color 0.15s ease;
  }
  .pricing-card-cta-secondary:hover {
    color: var(--rb, #4169E1);
    text-decoration: underline;
  }

  /* FAQ / footer block on pricing page */
  .pricing-footer {
    margin-top: 72px;
    padding-top: 40px;
    border-top: 1px solid rgba(15,17,22,0.08);
    text-align: center;
  }
  .pricing-footer-headline {
    font-family: 'Fraunces';
    font-size: 22px; font-weight: 600;
    margin: 0 0 10px;
  }
  .pricing-footer-sub {
    font-size: 14px;
    color: rgba(15,17,22,0.60);
    line-height: 1.55;
    max-width: 540px;
    margin: 0 auto 24px;
  }
  .pricing-footer-cta {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 12px 22px;
    background: #0E1116;
    color: #fff;
    border: none;
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px; font-weight: 600;
    text-decoration: none;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background 160ms ease;
  }
  .pricing-footer-cta:hover { background: #2C2F36; }

  /* ── Lock-screen preview ── */
  /* The Indy FAQ section that briefly lived here (May 10, 2026) was
     removed because it narrowed WeatherValet's brand to a single
     event. The accordion CSS pattern (details/summary with rotating
     chevron, gradient italic on headline) is captured in the
     subscriber portal and other surfaces. If a future generic FAQ
     surface is needed (e.g. for the help center), revive the pattern
     by referencing earlier git history or rebuilding from the
     existing component vocabulary. */

  .lockscreen-block {
    margin-top: 80px; padding-top: 48px;
    border-top: 1px solid rgba(15,17,22,0.06);
    text-align: center;
  }
  .lockscreen-eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    margin-bottom: 16px;
  }
  .lockscreen-eyebrow-dot {
    width: 6px; height: 6px; border-radius: 999px; background: #12A150;
  }
  .lockscreen-eyebrow-label {
    font-family: 'JetBrains Mono'; font-size: 10.5px; font-weight: 700;
    color: #0E7A3C; letter-spacing: 2.2px; text-transform: uppercase;
  }
  .lockscreen-title {
    font-family: 'Fraunces'; font-size: 24px; font-weight: 600;
    color: #0E1116; letter-spacing: -0.6px;
    margin-bottom: 12px;
  }
  .lockscreen-title em {
    font-style: italic;
    font-weight: 500;
    background: linear-gradient(135deg, #4169E1 0%, #2E4FB8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: -0.5px;
  }
  .lockscreen-prose {
    font-family: 'Fraunces'; font-size: 14.5px; line-height: 1.55;
    color: rgba(15,17,22,0.55);
    max-width: 380px; margin: 0 auto 32px;
  }

  /* The phone */
  .phone-frame {
    width: 280px; margin: 0 auto;
    background: linear-gradient(180deg, #1B2436 0%, #0D1320 100%);
    border-radius: 36px; padding: 12px;
    border: 1px solid rgba(255,255,255,0.10);
    box-shadow: 0 24px 48px rgba(15,17,22,0.20), 0 0 0 4px rgba(91,123,255,0.06);
  }
  .phone-screen {
    position: relative;
    aspect-ratio: 9 / 19.5;
    background: linear-gradient(180deg, #050810 0%, #0E1422 60%, #1A2238 100%);
    border-radius: 26px; overflow: hidden;
  }
  .phone-notch {
    position: absolute; top: 9px; left: 50%; transform: translateX(-50%);
    width: 86px; height: 22px; background: #000; border-radius: 999px;
  }
  .phone-status-bar {
    position: absolute; top: 12px; left: 0; right: 0;
    padding: 0 22px;
    display: flex; justify-content: space-between; align-items: center;
    color: rgba(255,255,255,0.95);
    z-index: 10;
  }
  .phone-time-bar { font-size: 12.5px; font-weight: 600; }
  .phone-status-icons { display: flex; align-items: center; gap: 5px; }
  .phone-status-icons svg { width: 12px; height: 12px; }
  .phone-glow {
    position: absolute; left: 50%; top: 38%;
    width: 220px; height: 220px;
    transform: translate(-50%, -50%);
    background: radial-gradient(closest-side, rgba(91,123,255,0.40), transparent 70%);
    pointer-events: none;
  }
  .phone-body {
    position: absolute; inset: 0;
    padding: 60px 16px 0;
    display: flex; flex-direction: column;
  }
  .phone-clock {
    text-align: center;
    font-family: 'Fraunces'; font-size: 54px; font-weight: 600;
    color: #fff; letter-spacing: -2px; line-height: 1;
  }
  .phone-date {
    text-align: center;
    font-size: 11px; font-weight: 500;
    color: rgba(255,255,255,0.75);
    margin-top: 4px;
  }
  .phone-lock-icon {
    width: 28px; height: 28px; border-radius: 999px;
    background: rgba(255,255,255,0.12);
    display: flex; align-items: center; justify-content: center;
    margin: 22px auto 0;
  }
  .phone-lock-icon svg { width: 12px; height: 12px; color: rgba(255,255,255,0.90); }

  .notif-card {
    margin-top: 28px;
    background: rgba(255,255,255,0.14);
    backdrop-filter: blur(20px) saturate(1.4);
    -webkit-backdrop-filter: blur(20px) saturate(1.4);
    border: 1px solid rgba(255,255,255,0.18);
    border-radius: 14px;
    padding: 11px;
    display: flex; gap: 10px;
    box-shadow: 0 12px 32px rgba(0,0,0,0.42);
  }
  .notif-icon {
    width: 28px; height: 28px;
    border-radius: 7px;
    background: linear-gradient(135deg, #5B7BFF, #4169E1);
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    box-shadow: 0 3px 8px rgba(0,0,0,0.30);
  }
  .notif-icon svg { width: 12px; height: auto; }
  .notif-body { flex: 1; min-width: 0; }
  .notif-head {
    display: flex; justify-content: space-between; align-items: baseline;
    margin-bottom: 2px;
  }
  .notif-app { color: #fff; font-size: 10.5px; font-weight: 600; }
  .notif-now { color: rgba(255,255,255,0.55); font-size: 9.5px; font-family: 'JetBrains Mono'; }
  .notif-text {
    color: #fff; font-size: 12.5px; font-weight: 600;
    line-height: 1.35;
  }
  .notif-text-em-clear { color: #16C77E; }
  .notif-text-em-caution { color: #F5A524; }
  .notif-text-em-risk { color: #E5484D; }
  .notif-sig {
    color: rgba(255,255,255,0.85);
    font-size: 11px;
    margin-top: 2px;
  }

  /* ── Crew footer ── */
  .crew-footer {
    margin-top: 80px; padding-top: 40px;
    border-top: 1px solid rgba(15,17,22,0.06);
    text-align: center;
  }
  .crew-eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    margin-bottom: 12px;
  }
  .crew-dot { width: 6px; height: 6px; border-radius: 999px; background: #12A150; }
  .crew-label {
    font-family: 'JetBrains Mono'; font-size: 10.5px; font-weight: 700;
    color: #0E7A3C; letter-spacing: 2.2px; text-transform: uppercase;
  }
  .crew-line {
    font-family: 'Fraunces'; font-size: 14px;
    color: rgba(15,17,22,0.55);
    max-width: 380px; margin: 0 auto 20px;
  }
  .join-crew {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 22px;
    border-radius: 999px;
    background: var(--rb, #4169E1);
    border: 1px solid var(--rb, #4169E1);
    color: #fff;
    font-size: 14px; font-weight: 600;
    cursor: pointer;
    box-shadow: 0 6px 16px rgba(65,105,225,0.20);
    transition: all 200ms;
    -webkit-tap-highlight-color: transparent;
  }
  .join-crew:hover {
    transform: translateY(-1px);
    box-shadow: 0 10px 22px rgba(65,105,225,0.30);
  }
  .join-crew:active { transform: translateY(0); }

  /* ── Handoff ── */
  .handoff {
    min-height: 60vh;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    text-align: center;
    animation: fadeUp 720ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .handoff-key {
    width: 72px; height: 72px;
    border-radius: 999px;
    background: rgba(65,105,225,0.06);
    border: 1.5px solid rgba(65,105,225,0.30);
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 36px;
    animation: keyEnter 920ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .handoff-key svg { width: 32px; height: 32px; color: #4169E1; }
  .handoff-headline {
    font-family: 'Fraunces'; font-size: 40px; font-weight: 600;
    letter-spacing: -1px; line-height: 1.05;
    color: #0E1116;
    animation: fadeUp 720ms 200ms cubic-bezier(0.16, 1, 0.3, 1) backwards;
  }
  .handoff-sub {
    font-family: 'Fraunces'; font-size: 18px; line-height: 1.5;
    color: rgba(15,17,22,0.60);
    margin-top: 16px; max-width: 380px;
    animation: fadeUp 720ms 480ms cubic-bezier(0.16, 1, 0.3, 1) backwards;
  }
  .handoff-sub em { font-style: italic; font-weight: 500; color: #4169E1; }
  .handoff-routing {
    margin-top: 36px;
    display: inline-flex; align-items: center; gap: 8px;
    padding: 10px 16px; border-radius: 999px;
    background: rgba(15,17,22,0.04);
    color: rgba(15,17,22,0.55);
    font-family: 'JetBrains Mono'; font-size: 11px;
    animation: fadeUp 720ms 720ms cubic-bezier(0.16, 1, 0.3, 1) backwards;
  }
  .handoff-routing-dot {
    width: 6px; height: 6px; border-radius: 999px;
    background: #4169E1;
    animation: pulse 1.4s infinite;
  }
  .handoff-routing-query { color: #0E1116; font-weight: 600; }

  /* ── Ticket ── */
  .back-btn {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 10px 18px;
    background: #fff;
    border: 1px solid rgba(15,17,22,0.10);
    border-radius: 999px;
    font-size: 13px; font-weight: 600;
    color: #0E1116;
    margin-bottom: 24px;
    box-shadow: 0 1px 2px rgba(15,17,22,0.04), 0 4px 12px rgba(15,17,22,0.04);
    transition: all 200ms;
  }
  .back-btn:hover {
    border-color: #4169E1;
    color: #4169E1;
    background: rgba(65,105,225,0.04);
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(65,105,225,0.16);
  }
  .back-btn svg { width: 14px; height: 14px; stroke-width: 2.5; }

  .ticket-card {
    background: #fff;
    border: 1px solid rgba(15,17,22,0.06);
    border-radius: 24px;
    padding: 32px;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.85),
      0 1px 3px rgba(15,17,22,0.04),
      0 8px 24px rgba(15,17,22,0.06),
      0 24px 48px rgba(15,17,22,0.04);
    animation: ticketIn 620ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .ticket-head {
    display: flex; justify-content: space-between; align-items: flex-start;
    gap: 16px; flex-wrap: wrap;
  }
  .status-pill {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 6px 12px; border-radius: 999px;
    font-size: 12px; font-weight: 700;
    letter-spacing: 0.6px;
  }
  .status-pill-dot {
    width: 8px; height: 8px; border-radius: 999px;
    animation: pulse 2.4s infinite;
  }
  .status-clear {
    background: rgba(18,161,80,0.08);
    border: 1px solid rgba(18,161,80,0.24);
    color: #0E7A3C;
  }
  .status-clear .status-pill-dot {
    background: #12A150;
    box-shadow: 0 0 0 4px rgba(18,161,80,0.16);
  }
  .status-caution {
    background: rgba(194,132,18,0.08);
    border: 1px solid rgba(194,132,18,0.28);
    color: #C28412;
  }
  .status-caution .status-pill-dot {
    background: #F5A524;
    box-shadow: 0 0 0 4px rgba(245,165,36,0.18);
  }
  .status-risk {
    background: rgba(194,52,43,0.08);
    border: 1px solid rgba(194,52,43,0.28);
    color: #C2342B;
  }
  .status-risk .status-pill-dot {
    background: #E5484D;
    box-shadow: 0 0 0 4px rgba(194,52,43,0.20);
  }

  .ticket-meta {
    display: flex; align-items: center; gap: 8px;
    font-family: 'JetBrains Mono'; font-size: 10.5px;
    color: rgba(15,17,22,0.45);
  }
  .ticket-meta-divider {
    width: 1px; height: 12px; background: rgba(15,17,22,0.10);
  }
  .ticket-plan {
    margin-top: 28px;
    display: flex; flex-direction: column; gap: 6px;
  }
  .ticket-plan-label {
    font-family: 'JetBrains Mono'; font-size: 10.5px; font-weight: 700;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: rgba(15,17,22,0.45);
  }
  .ticket-plan-text {
    font-family: 'Fraunces'; font-size: 17px; font-weight: 400;
    line-height: 1.4; letter-spacing: -0.2px;
    color: rgba(15,17,22,0.70);
    font-style: italic;
  }
  .ticket-verdict {
    font-family: 'Fraunces'; font-size: 30px; font-weight: 600;
    line-height: 1.15; letter-spacing: -0.8px;
    color: #0E1116;
    margin: 14px 0 0;
  }
  /* Status-specific verdict color accent — sits in the same color
     family as the border so the headline reads as part of the
     verdict object, not against it. Deep saturations, not pastels. */
  .ticket-card[data-status="clear"]   .ticket-verdict { color: #0a6b3a; }
  .ticket-card[data-status="caution"] .ticket-verdict { color: #8a5a0a; }
  .ticket-card[data-status="risk"]    .ticket-verdict { color: #9b2823; }

  /* Confidence band — sits right under the verdict. Communicates
     not just the raw confidence percentage but the recommended posture
     for the customer ("Bet on it" / "Plan as scheduled" / etc.) The
     percentage is demoted to the right side; the action label leads.
     This is the front-and-center version of the four-band system. */
  .ticket-confidence-band {
    display: inline-flex; align-items: baseline; gap: 8px;
    margin: 10px 0 0;
    padding: 6px 12px;
    background: rgba(15, 17, 22, 0.04);
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.78);
  }
  .ticket-confidence-band-label {
    font-weight: 700;
    letter-spacing: 0.2px;
  }
  /* Tint the label by tier so the eye lands on the right severity */
  .ticket-confidence-band[data-band-tier="high"] .ticket-confidence-band-label { color: #0a6b3a; }
  .ticket-confidence-band[data-band-tier="mid"]  .ticket-confidence-band-label { color: #0E1116; }
  .ticket-confidence-band[data-band-tier="low"]  .ticket-confidence-band-label { color: #8a5a0a; }
  .ticket-confidence-band[data-band-tier="flag"] .ticket-confidence-band-label { color: #9b2823; }
  .ticket-confidence-band-sep {
    color: rgba(15, 17, 22, 0.30);
  }
  .ticket-confidence-band-action {
    color: rgba(15, 17, 22, 0.65);
  }
  .ticket-confidence-band-pct {
    color: rgba(15, 17, 22, 0.45);
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    margin-left: 4px;
  }
  .ticket-rule {
    height: 1px;
    background: rgba(15,17,22,0.07);
    margin: 28px 0;
  }
  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 0;
  }
  .stat-cell {
    padding: 0 20px;
    border-left: 1px solid rgba(15,17,22,0.06);
  }
  .stat-cell:first-child { padding-left: 0; border-left: none; }
  .stat-cell:last-child { padding-right: 0; }
  .stat-label {
    font-size: 10px; font-weight: 600;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: #4169E1; margin-bottom: 12px;
  }
  .stat-value {
    font-family: 'Fraunces'; font-weight: 600;
    letter-spacing: -0.5px; color: #0E1116;
    line-height: 1;
  }
  .stat-value-big { font-size: 32px; }
  .stat-value-small { font-size: 20px; }
  .stat-verifier {
    display: flex; align-items: center; gap: 6px;
    font-family: 'JetBrains Mono'; font-size: 10.5px;
    color: rgba(15,17,22,0.45);
    margin-top: 12px;
  }
  .stat-verifier svg { width: 10px; height: 10px; color: #4169E1; }

  .ai-row {
    display: flex; align-items: center; gap: 10px;
    margin-top: 24px;
  }
  .ai-badge {
    width: 18px; height: 18px; border-radius: 999px;
    background: #4169E1;
    display: inline-flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  .ai-badge svg { width: 11px; height: 11px; color: #fff; }
  .ai-label { font-size: 12.5px; font-weight: 600; color: #4169E1; }
  .ai-dot { width: 4px; height: 4px; border-radius: 999px; background: rgba(65,105,225,0.40); }
  .ai-meta { font-size: 12.5px; color: rgba(65,105,225,0.75); }

  /* ── Source badge: Live | Cached — small indicator next to confidence ── */
  .source-badge {
    display: inline-flex; align-items: center; gap: 5px;
    margin-left: 6px;
    padding: 3px 9px;
    border-radius: 999px;
    font-size: 10px; font-weight: 700;
    font-family: 'JetBrains Mono';
    letter-spacing: 1.4px; text-transform: uppercase;
    cursor: help;
  }
  .source-badge-live {
    background: rgba(18,161,80,0.10);
    color: #0E7A3C;
    border: 1px solid rgba(18,161,80,0.24);
  }
  .source-badge-live .source-badge-dot {
    width: 5px; height: 5px; border-radius: 999px;
    background: #12A150;
    box-shadow: 0 0 0 3px rgba(18,161,80,0.18);
    animation: pulse 2.4s infinite;
  }
  .source-badge-fallback {
    background: rgba(15,17,22,0.05);
    color: rgba(15,17,22,0.55);
    border: 1px solid rgba(15,17,22,0.10);
  }

  /* ── Alternates hint: "Did you mean a different Lebanon?" ── */
  .alternates-hint {
    display: flex;
    align-items: center;
    gap: 12px;
    margin: 16px 0 4px;
    padding: 10px 12px;
    background: rgba(65,105,225,0.04);
    border-radius: 10px;
    border: 1px dashed rgba(65,105,225,0.22);
    flex-wrap: wrap;
  }
  .alternates-hint-label {
    font-family: 'Inter';
    font-size: 12.5px;
    font-weight: 600;
    color: rgba(15,17,22,0.65);
    letter-spacing: -0.1px;
  }
  .alternates-list {
    display: flex; flex-wrap: wrap; gap: 6px;
  }
  .alternate-chip {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 5px 10px;
    border-radius: 999px;
    background: #fff;
    border: 1px solid rgba(65,105,225,0.30);
    font-size: 12px;
    color: #4169E1;
    cursor: pointer;
    transition: all 150ms;
    font-weight: 600;
    -webkit-tap-highlight-color: rgba(65,105,225,0.10);
    touch-action: manipulation;
  }
  .alternate-chip:hover, .alternate-chip:focus-visible {
    background: rgba(65,105,225,0.08);
    border-color: rgba(65,105,225,0.55);
    outline: none;
  }
  .alternate-chip-label {
    font-weight: 600;
  }
  .alternate-chip-detail {
    font-family: 'JetBrains Mono';
    font-size: 10.5px;
    color: rgba(65,105,225,0.55);
  }

  /* ── NWS alert ribbon — sits attached to the verdict zone, not as a
       separate card. Reviewer feedback (May 2026): the alert read as a
       disconnected widget; should feel like part of the verdict context.
       The ribbon is a thin colored bar with severity-tinted background
       and a left accent stripe; expandable for full alert text. ── */
  .ticket-alert-ribbon {
    margin: 14px 0 0;
    border-radius: 8px;
    overflow: hidden;
    border-left: 4px solid;
    display: flex;
    flex-direction: column;
  }
  .ticket-alert-ribbon.alert-extreme,
  .ticket-alert-ribbon.alert-severe {
    background: rgba(194,52,43,0.06);
    border-left-color: #C2342B;
  }
  .ticket-alert-ribbon.alert-moderate {
    background: rgba(194,132,18,0.08);
    border-left-color: #C28412;
  }
  .ticket-alert-ribbon.alert-minor,
  .ticket-alert-ribbon.alert-unknown {
    background: rgba(15,17,22,0.04);
    border-left-color: rgba(15,17,22,0.30);
  }
  .ticket-alert-ribbon-head {
    display: flex; align-items: center; gap: 10px;
    padding: 10px 14px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    background: transparent;
    border: none;
    width: 100%;
    text-align: left;
    font: inherit;
    color: inherit;
  }
  .ticket-alert-ribbon-icon {
    flex-shrink: 0;
    width: 18px; height: 18px;
    color: rgba(15,17,22,0.65);
  }
  .ticket-alert-ribbon.alert-severe .ticket-alert-ribbon-icon,
  .ticket-alert-ribbon.alert-extreme .ticket-alert-ribbon-icon {
    color: #9b2823;
  }
  .ticket-alert-ribbon.alert-moderate .ticket-alert-ribbon-icon {
    color: #8a5a0a;
  }
  .ticket-alert-ribbon-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 1.6px;
    text-transform: uppercase;
  }
  .ticket-alert-ribbon.alert-severe .ticket-alert-ribbon-label,
  .ticket-alert-ribbon.alert-extreme .ticket-alert-ribbon-label {
    color: #9b2823;
  }
  .ticket-alert-ribbon.alert-moderate .ticket-alert-ribbon-label {
    color: #8a5a0a;
  }
  .ticket-alert-ribbon-event {
    flex: 1;
    font-size: 13.5px;
    color: rgba(15,17,22,0.85);
    font-weight: 500;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .ticket-alert-ribbon-chev {
    flex-shrink: 0;
    color: rgba(15,17,22,0.45);
    transition: transform 200ms ease;
  }
  .ticket-alert-ribbon.is-open .ticket-alert-ribbon-chev {
    transform: rotate(180deg);
  }
  .ticket-alert-ribbon-body {
    display: none;
    padding: 0 14px 12px;
    border-top: 1px solid rgba(15,17,22,0.06);
    background: rgba(255,255,255,0.40);
    margin-top: -1px;
  }
  .ticket-alert-ribbon.is-open .ticket-alert-ribbon-body {
    display: block;
  }
  .ticket-alert-ribbon-headline {
    font-size: 13px;
    color: rgba(15,17,22,0.78);
    line-height: 1.5;
    margin: 10px 0 6px;
  }
  .ticket-alert-ribbon-source {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15,17,22,0.42);
    letter-spacing: 0.6px;
  }
  /* Pro-tier badge inside the alert ribbon — signals this is your
     subscriber-context, not a generic free-tier display */
  .ticket-alert-ribbon-pro-pill {
    display: inline-flex; align-items: center;
    padding: 1px 6px;
    background: rgba(65, 105, 225, 0.12);
    color: #4169E1;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    margin-left: 4px;
  }

  /* ── NWS alert callout (LEGACY — kept for non-ticket surfaces;
       on the ticket the ribbon above replaces it) ── */
  .alert-callout {
    display: flex;
    gap: 14px;
    align-items: flex-start;
    margin: 20px 0 0;
    padding: 14px 16px;
    border-radius: 12px;
    border: 1px solid;
    background: #fff;
  }
  .alert-callout.alert-extreme,
  .alert-callout.alert-severe {
    background: rgba(194,52,43,0.04);
    border-color: rgba(194,52,43,0.30);
  }
  .alert-callout.alert-moderate {
    background: rgba(194,132,18,0.05);
    border-color: rgba(194,132,18,0.30);
  }
  .alert-callout.alert-minor,
  .alert-callout.alert-unknown {
    background: rgba(15,17,22,0.03);
    border-color: rgba(15,17,22,0.10);
  }
  .alert-callout-icon {
    flex-shrink: 0;
    width: 32px; height: 32px;
    border-radius: 999px;
    display: flex; align-items: center; justify-content: center;
  }
  .alert-callout.alert-severe .alert-callout-icon,
  .alert-callout.alert-extreme .alert-callout-icon {
    background: rgba(194,52,43,0.12); color: #C2342B;
  }
  .alert-callout.alert-moderate .alert-callout-icon {
    background: rgba(194,132,18,0.12); color: #C28412;
  }
  .alert-callout.alert-minor .alert-callout-icon,
  .alert-callout.alert-unknown .alert-callout-icon {
    background: rgba(15,17,22,0.06); color: rgba(15,17,22,0.55);
  }
  .alert-callout-icon svg { width: 16px; height: 16px; }
  .alert-callout-body { flex: 1; min-width: 0; }
  .alert-callout-head {
    display: flex; align-items: center; gap: 10px;
    margin-bottom: 4px;
    flex-wrap: wrap;
  }
  .alert-callout-event {
    font-family: 'Inter'; font-weight: 700; font-size: 13.5px;
    color: #0E1116; letter-spacing: -0.1px;
  }
  .alert-callout-sev {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    letter-spacing: 1.4px; text-transform: uppercase;
    padding: 2px 8px; border-radius: 999px;
  }
  .alert-callout.alert-severe .alert-callout-sev,
  .alert-callout.alert-extreme .alert-callout-sev {
    background: rgba(194,52,43,0.12); color: #C2342B;
  }
  .alert-callout.alert-moderate .alert-callout-sev {
    background: rgba(194,132,18,0.12); color: #C28412;
  }
  .alert-callout.alert-minor .alert-callout-sev,
  .alert-callout.alert-unknown .alert-callout-sev {
    background: rgba(15,17,22,0.08); color: rgba(15,17,22,0.55);
  }
  .alert-callout-headline {
    font-family: 'Fraunces'; font-size: 13.5px; line-height: 1.45;
    color: rgba(15,17,22,0.65);
    margin: 4px 0 6px;
  }
  .alert-callout-source {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 600;
    color: rgba(15,17,22,0.45);
    letter-spacing: 0.8px;
  }

  /* ── AI Paragraph (the hero of the ticket) ──────────────────────────
     The single most important piece of UI in the product. This is the
     friendly, plain-English translation of weather data into how the
     user will actually experience it for their specific plan.
     
     Visually: serif typeface (Fraunces) at a generous reading size,
     in a soft Royal Blue panel that visually frames it as "the answer."
     Sits between the verdict headline and the stats grid — verdict is
     the at-a-glance, paragraph is the lede, stats are the receipts.
     
     Loading state: a calm skeleton (three pulsing lines) while the
     AI generates. Typically 2-4 seconds. Never blocks the rest of
     the ticket from rendering — verdict and stats are already visible. */
  .ai-paragraph-block {
    margin: 24px 0 4px;
    padding: 20px 22px;
    background: linear-gradient(135deg, rgba(15, 17, 22, 0.025), rgba(15, 17, 22, 0.04));
    border-radius: 14px;
    border-left: 3px solid var(--rb, #4169E1);
    position: relative;
  }
  /* Verdict-aware accent: when the AI paragraph sits inside a ticket
     card, its eyebrow + dot + left border pick up the verdict color
     family so the block reads as part of the ticket, not as a
     separate Royal Blue UI panel. The body text stays charcoal so
     readability is unaffected. */
  .ticket-card[data-status="clear"] .ai-paragraph-block {
    border-left-color: #16C77E;
    background: linear-gradient(135deg, rgba(22, 199, 126, 0.04), rgba(22, 199, 126, 0.07));
  }
  .ticket-card[data-status="caution"] .ai-paragraph-block {
    border-left-color: #C28412;
    background: linear-gradient(135deg, rgba(245, 165, 36, 0.05), rgba(245, 165, 36, 0.09));
  }
  .ticket-card[data-status="risk"] .ai-paragraph-block {
    border-left-color: #C2342B;
    background: linear-gradient(135deg, rgba(194, 52, 43, 0.05), rgba(194, 52, 43, 0.08));
  }
  .ticket-card[data-status="clear"]   .ai-paragraph-eyebrow { color: rgba(10, 107, 58, 0.85); }
  .ticket-card[data-status="caution"] .ai-paragraph-eyebrow { color: rgba(138, 90, 10, 0.90); }
  .ticket-card[data-status="risk"]    .ai-paragraph-eyebrow { color: rgba(155, 40, 35, 0.90); }
  .ticket-card[data-status="clear"]   .ai-paragraph-eyebrow-dot { background: #16C77E; }
  .ticket-card[data-status="caution"] .ai-paragraph-eyebrow-dot { background: #F5A524; }
  .ticket-card[data-status="risk"]    .ai-paragraph-eyebrow-dot { background: #C2342B; }
  /* Skeleton shimmer also picks up the verdict family during load */
  .ticket-card[data-status="clear"]   .ai-paragraph-skeleton {
    background: linear-gradient(90deg,
      rgba(22, 199, 126, 0.10) 0%,
      rgba(22, 199, 126, 0.20) 50%,
      rgba(22, 199, 126, 0.10) 100%);
    background-size: 200% 100%;
  }
  .ticket-card[data-status="caution"] .ai-paragraph-skeleton {
    background: linear-gradient(90deg,
      rgba(245, 165, 36, 0.10) 0%,
      rgba(245, 165, 36, 0.22) 50%,
      rgba(245, 165, 36, 0.10) 100%);
    background-size: 200% 100%;
  }
  .ticket-card[data-status="risk"] .ai-paragraph-skeleton {
    background: linear-gradient(90deg,
      rgba(194, 52, 43, 0.10) 0%,
      rgba(194, 52, 43, 0.22) 50%,
      rgba(194, 52, 43, 0.10) 100%);
    background-size: 200% 100%;
  }
  .ai-paragraph-eyebrow {
    display: flex; align-items: center; gap: 7px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.85);
    margin-bottom: 10px;
  }
  .ai-paragraph-eyebrow-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    animation: aiEyebrowPulse 2s ease-in-out infinite;
  }
  @keyframes aiEyebrowPulse {
    0%, 100% { opacity: 0.5; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.2); }
  }
  .ai-paragraph-text {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 17px;
    line-height: 1.55;
    color: #0E1116;
    margin: 0;
    font-weight: 400;
    letter-spacing: -0.1px;
  }
  /* Loading state — three pulsing lines that look like text being drawn */
  .ai-paragraph-loading {
    display: flex; flex-direction: column; gap: 9px;
  }
  .ai-paragraph-skeleton {
    height: 14px;
    border-radius: 4px;
    background: linear-gradient(90deg,
      rgba(65, 105, 225, 0.10) 0%,
      rgba(65, 105, 225, 0.20) 50%,
      rgba(65, 105, 225, 0.10) 100%);
    background-size: 200% 100%;
    animation: aiSkeletonShimmer 1.6s ease-in-out infinite;
  }
  .ai-paragraph-skeleton:nth-child(1) { width: 92%; }
  .ai-paragraph-skeleton:nth-child(2) { width: 96%; }
  .ai-paragraph-skeleton:nth-child(3) { width: 78%; }
  @keyframes aiSkeletonShimmer {
    0%   { background-position: 200% 0; }
    100% { background-position: -200% 0; }
  }

  /* ── "How we got there" — collapsible AI reasons ─────────────────────
     The old always-visible reasons block, demoted. Now a small button
     the user can tap to see the AI's specific reasoning + the data
     sources that contributed. Most users won't open it; the few who
     want to verify the AI's work can. */
  .reasons-collapse {
    margin-top: 14px;
  }
  .reasons-collapse-toggle {
    display: inline-flex; align-items: center; gap: 6px;
    background: transparent;
    border: none;
    padding: 6px 0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 500;
    color: rgba(15, 17, 22, 0.55);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: color 120ms ease;
  }
  .reasons-collapse-toggle:hover {
    color: var(--rb, #4169E1);
  }
  .reasons-collapse-chevron {
    width: 12px; height: 12px;
    transition: transform 180ms ease;
  }
  .reasons-collapse.is-open .reasons-collapse-chevron {
    transform: rotate(90deg);
  }
  .reasons-collapse-panel {
    max-height: 0;
    overflow: hidden;
    transition: max-height 220ms ease;
  }
  .reasons-collapse.is-open .reasons-collapse-panel {
    max-height: 600px;
  }
  .reasons-collapse-inner {
    margin-top: 10px;
    padding: 14px 16px;
    background: rgba(15,17,22,0.03);
    border-radius: 12px;
    border: 1px solid rgba(15,17,22,0.06);
  }
  .reasons-collapse-ai-row {
    display: flex; align-items: center; flex-wrap: wrap;
    gap: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.65);
    margin-bottom: 10px;
    padding-bottom: 10px;
    border-bottom: 1px solid rgba(15,17,22,0.06);
  }

  /* ── Reasons block — "Why this call" ── */
  .reasons-block {
    margin-top: 20px;
    padding: 14px 16px;
    background: rgba(15,17,22,0.03);
    border-radius: 12px;
    border: 1px solid rgba(15,17,22,0.06);
  }
  .reasons-label {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: rgba(15,17,22,0.45);
    margin-bottom: 8px;
  }
  .reasons-list {
    list-style: none; padding: 0; margin: 0;
    display: flex; flex-direction: column; gap: 6px;
  }
  .reason-item {
    display: flex; align-items: flex-start; gap: 10px;
    font-family: 'Inter'; font-size: 13px; line-height: 1.5;
    color: rgba(15,17,22,0.72);
  }
  .reason-bullet {
    flex-shrink: 0;
    width: 4px; height: 4px;
    border-radius: 999px;
    background: #4169E1;
    margin-top: 8px;
  }
  .reasons-lookahead {
    margin-top: 10px;
    padding: 10px 12px;
    border-left: 2px solid rgba(65,105,225,0.40);
    background: rgba(65,105,225,0.04);
    border-radius: 0 8px 8px 0;
    font-family: 'Fraunces'; font-size: 13px; line-height: 1.5;
    color: rgba(15,17,22,0.68);
    font-style: italic;
  }

  /* ── Crew evidence (between AI reasons and the human-review upsell) ──
     Subtle Royal Blue framing — distinct from the neutral .reasons-block
     above, so the Crew section feels like its own voice. The label icon
     uses the same people-glyph as the Crew tab in the app to anchor the
     visual relationship. */
  .crew-evidence-block {
    margin-top: 16px;
    padding: 14px 16px;
    background: rgba(65, 105, 225, 0.04);
    border: 1px solid rgba(65, 105, 225, 0.14);
    border-radius: 12px;
  }
  .crew-evidence-empty {
    background: rgba(15, 17, 22, 0.02);
    border-color: rgba(15, 17, 22, 0.06);
  }
  .crew-evidence-label {
    display: flex; align-items: center; gap: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: rgba(65, 105, 225, 0.85);
    margin-bottom: 10px;
  }
  .crew-evidence-empty .crew-evidence-label {
    color: rgba(15, 17, 22, 0.45);
  }
  .crew-evidence-label svg {
    width: 14px; height: 14px;
    flex-shrink: 0;
  }
  .crew-evidence-summary {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px; line-height: 1.5;
    color: rgba(15, 17, 22, 0.78);
    margin: 0 0 12px;
    font-weight: 500;
  }
  .crew-evidence-empty-text {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px; line-height: 1.5;
    color: rgba(15, 17, 22, 0.55);
    margin: 0;
  }
  .crew-evidence-list {
    display: flex; flex-direction: column;
    gap: 6px;
    margin-bottom: 12px;
  }
  .crew-evidence-chip {
    display: flex; align-items: center; gap: 10px;
    padding: 8px 10px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    cursor: pointer;
    transition: border-color 120ms ease, transform 80ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-evidence-chip:hover {
    border-color: rgba(65, 105, 225, 0.35);
  }
  .crew-evidence-chip:active {
    transform: scale(0.99);
  }
  .crew-evidence-chip-type {
    flex-shrink: 0;
    padding: 3px 8px;
    border-radius: 999px;
    font-size: 10px; font-weight: 700;
    letter-spacing: 0.4px;
    text-transform: uppercase;
    background: rgba(15, 17, 22, 0.06);
    color: rgba(15, 17, 22, 0.70);
  }
  .crew-evidence-chip-type.type-storm { background: rgba(194, 52, 43, 0.10); color: #9b2823; }
  .crew-evidence-chip-type.type-hail  { background: rgba(91, 107, 214, 0.12); color: #3b4cb1; }
  .crew-evidence-chip-type.type-wind  { background: rgba(232, 164, 0, 0.14); color: #8a5a0a; }
  .crew-evidence-chip-type.type-rain  { background: rgba(37, 102, 214, 0.12); color: #1e4fa6; }
  .crew-evidence-chip-type.type-clear { background: rgba(18, 161, 80, 0.12); color: #0c7a3c; }
  .crew-evidence-chip-author {
    font-weight: 600;
    color: #0E1116;
    flex-shrink: 0;
  }
  .crew-evidence-chip-where {
    color: rgba(15, 17, 22, 0.55);
    flex: 1;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .crew-evidence-chip-when {
    color: rgba(15, 17, 22, 0.45);
    font-size: 11px;
    flex-shrink: 0;
  }
  .crew-evidence-chip-verified {
    flex-shrink: 0;
    width: 16px; height: 16px;
    border-radius: 50%;
    background: rgba(18, 161, 80, 0.16);
    color: #0c7a3c;
    display: inline-flex; align-items: center; justify-content: center;
  }
  .crew-evidence-chip-verified svg {
    width: 10px; height: 10px;
  }
  .crew-evidence-cta {
    display: inline-flex; align-items: center; gap: 6px;
    background: transparent;
    border: none;
    color: var(--rb, #4169E1);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    cursor: pointer;
    padding: 4px 0;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-evidence-cta:hover {
    text-decoration: underline;
  }

  /* ── Sources roster (below upsell, inside ticket card) ── */
  .sources-block {
    margin-top: 20px;
    padding-top: 16px;
    border-top: 1px solid rgba(15,17,22,0.06);
  }
  .sources-label {
    display: flex; align-items: center; gap: 6px;
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: rgba(15,17,22,0.45);
    margin-bottom: 10px;
  }
  .sources-label svg {
    width: 12px; height: 12px;
    color: rgba(65,105,225,0.55);
  }
  .sources-list {
    display: flex; flex-direction: column; gap: 4px;
  }
  .sources-item {
    display: flex; justify-content: space-between; align-items: baseline;
    gap: 12px;
    font-size: 12.5px;
    padding: 4px 0;
  }
  .sources-item-name {
    font-family: 'Inter'; font-weight: 700;
    color: #0E1116;
    flex-shrink: 0;
  }
  .sources-item-detail {
    font-family: 'JetBrains Mono'; font-size: 11px;
    color: rgba(15,17,22,0.50);
    text-align: right;
  }


  /* ════════════════════════════════════════════════════════════════════
     TICKET v3 — premium, sky-toned, thick verdict border
     The sky background shifts with verdict so the visual matches
     the meaning. Clear → soft sunny blue. Caution → golden-hour amber.
     Risk → moody slate-stormy. Print-cuttable: the actual ticket card
     contains all the weather a person needs at a glance.
     ════════════════════════════════════════════════════════════════════ */

  /* ════════════════════════════════════════════════════════════════════
     PREMIUM TICKET — paper-stock substrate, surgical verdict accents
     ════════════════════════════════════════════════════════════════════
     Design direction: think Tesla service appointment + high-end hotel
     valet stub + Concorde boarding pass. The card is a real object the
     customer wants to keep — warm cream paper with subtle fiber texture,
     a colored accent stripe at the top, a thick verdict-color border,
     and a faint watermark. The verdict color does not flood the card;
     it appears in three precise places (border, top stripe, verdict
     text) so the data has room to breathe.
     ════════════════════════════════════════════════════════════════════ */

  .ticket-card {
    /* The base substrate: warm ivory paper that fades to white at the
       bottom. Cream at the top where the verdict lives, near-white at
       the bottom where the conditions sit, so the data stays cleanly
       readable while the upper region carries the "ticket" character. */
    background:
      linear-gradient(180deg,
        #fbf7ed 0%,
        #fcfaf3 28%,
        #ffffff 70%);
    border: 5px solid #16C77E;
    border-radius: 20px;
    padding: 32px 36px;
    box-shadow:
      /* Inner highlight — top edge catches light like real card stock */
      inset 0 1px 0 rgba(255, 255, 255, 0.85),
      /* Inner shadow — the very subtle "this is a physical object" cue */
      inset 0 0 24px rgba(120, 95, 60, 0.04),
      /* Soft drop, ambient and key */
      0 1px 3px rgba(15, 17, 22, 0.05),
      0 12px 32px rgba(15, 17, 22, 0.08),
      0 32px 56px rgba(15, 17, 22, 0.05);
    animation: ticketIn 620ms cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    overflow: hidden;
  }

  /* The fiber texture overlay — a CSS-generated noise pattern that
     gives the card a real paper feel. Very subtle: the pattern is
     barely visible at full opacity, and we run it at 0.35 opacity
     so it reads as material rather than texture. Multiple radial
     gradients at random-looking positions create a non-repeating
     fiber-like noise without any image asset. */
  .ticket-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 17% 23%, rgba(140, 110, 70, 0.06) 0%, transparent 0.5%),
      radial-gradient(circle at 82% 14%, rgba(140, 110, 70, 0.05) 0%, transparent 0.5%),
      radial-gradient(circle at 47% 71%, rgba(140, 110, 70, 0.06) 0%, transparent 0.5%),
      radial-gradient(circle at 31% 43%, rgba(140, 110, 70, 0.04) 0%, transparent 0.4%),
      radial-gradient(circle at 91% 67%, rgba(140, 110, 70, 0.05) 0%, transparent 0.4%),
      radial-gradient(circle at 13% 88%, rgba(140, 110, 70, 0.05) 0%, transparent 0.4%),
      radial-gradient(circle at 67% 36%, rgba(140, 110, 70, 0.04) 0%, transparent 0.4%),
      /* The watermark: subtle vertical stripes at very low opacity,
         spanning the whole card. Reads as fine paper grain. */
      repeating-linear-gradient(
        90deg,
        transparent 0,
        transparent 3px,
        rgba(120, 95, 60, 0.018) 3px,
        rgba(120, 95, 60, 0.018) 4px);
    background-size: 100% 100%, 100% 100%, 100% 100%, 100% 100%, 100% 100%, 100% 100%, 100% 100%, 8px 100%;
    pointer-events: none;
    opacity: 0.55;
    border-radius: inherit;
    z-index: 0;
  }

  /* The colored top stripe — a thin accent band running across the top
     edge of the ticket. This is where the verdict color lives in its
     most concentrated form. Like the colored stripe on a Tesla service
     receipt or a high-end concert pass. The stripe sits flush with
     the inner border. */
  .ticket-card::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: #16C77E;
    z-index: 2;
    pointer-events: none;
  }
  .ticket-card[data-status="caution"]::after { background: #F5A524; }
  .ticket-card[data-status="risk"]::after    { background: #C2342B; }

  /* All inner content sits above the texture overlay */
  .ticket-card > * {
    position: relative;
    z-index: 1;
  }

  /* Verdict-color → border color. The substrate stays cream regardless
     of verdict; only the border (and the top stripe + verdict text +
     status pill) carry the verdict color. This is the Tesla discipline:
     color is structural, not decorative. */
  .ticket-card[data-status="clear"]   { border-color: #16C77E; }
  .ticket-card[data-status="caution"] { border-color: #F5A524; }
  .ticket-card[data-status="risk"]    { border-color: #C2342B; }

  /* A faint micro-perforation pattern down the left edge — suggests
     a tear-off stub without literally being one. Sits inside the
     border, very subtle. */
  .ticket-perforation {
    position: absolute;
    top: 18px;
    bottom: 18px;
    left: 14px;
    width: 1px;
    background-image: linear-gradient(
      180deg,
      rgba(120, 95, 60, 0.18) 50%,
      transparent 50%);
    background-size: 1px 6px;
    z-index: 1;
    pointer-events: none;
  }

  /* The watermark — a faint, large WeatherValet "WV" mark in the lower
     right of the ticket. Like the watermark on a cashier's check.
     Only visible if you're looking for it. */
  .ticket-watermark {
    position: absolute;
    right: 36px;
    bottom: 36px;
    font-family: 'Fraunces', Georgia, serif;
    font-weight: 600;
    font-size: 80px;
    line-height: 1;
    color: rgba(60, 50, 30, 0.04);
    letter-spacing: -3px;
    pointer-events: none;
    z-index: 0;
    user-select: none;
  }

  /* The plan number gets ticket-reference treatment — boxed in JetBrains
     Mono, slightly elevated, like a real ticket reference. */
  .ticket-meta {
    align-items: center;
  }

  /* Plan label tints to match each verdict family — keep the cream
     substrate readable but signal which color family is in play. */
  .ticket-card[data-status="clear"]   .ticket-plan-label { color: rgba(10, 80, 45, 0.70); }
  .ticket-card[data-status="caution"] .ticket-plan-label { color: rgba(120, 75, 5, 0.70); }
  .ticket-card[data-status="risk"]    .ticket-plan-label { color: rgba(120, 40, 35, 0.70); }

  /* ════════════════════════════════════════════════════════════════════
     PREMIUM TICKET v2 — masthead, stamp-style status, reference number
     ════════════════════════════════════════════════════════════════════
     The card now opens with a proper ticket masthead: WeatherValet
     wordmark, a category line, and the issue date. Below the masthead
     sits a thin rule, then the status (treated as a stamp/seal, not a
     chat-app pill), then the customer's plan. The plan number gets
     ticket-reference treatment in the upper-right — boxed, monospace,
     deliberate. */

  /* ── Masthead ────────────────────────────────────────── */
  .ticket-masthead {
    display: flex; align-items: flex-start; justify-content: space-between;
    gap: 24px;
    padding-bottom: 14px;
    border-bottom: 1px solid rgba(60, 50, 30, 0.12);
    margin-bottom: 18px;
  }
  .ticket-masthead-brand {
    display: flex; flex-direction: column; gap: 3px;
  }
  .ticket-masthead-wordmark {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 19px;
    font-weight: 600;
    line-height: 1;
    color: #0E1116;
    letter-spacing: -0.4px;
  }
  .ticket-masthead-wordmark-em {
    /* The .ai gets the verdict color so the brand visibly reflects status */
    color: #16C77E;
    font-weight: 700;
  }
  .ticket-card[data-status="caution"] .ticket-masthead-wordmark-em { color: #C28412; }
  .ticket-card[data-status="risk"]    .ticket-masthead-wordmark-em { color: #C2342B; }
  .ticket-masthead-tagline {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(60, 50, 30, 0.50);
    margin-top: 5px;
  }
  .ticket-masthead-issued {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(60, 50, 30, 0.40);
    text-align: right;
    line-height: 1.5;
  }
  .ticket-masthead-issued-value {
    color: rgba(15, 17, 22, 0.85);
    font-weight: 700;
    letter-spacing: 0.8px;
  }

  /* ── Reference number — boxed, JetBrains Mono, ticket character ──
     Replaces the old "Plan #X | City · Zip" inline metadata with a
     proper ticket reference: a small framed box in the upper-right
     of the masthead, like a reservation confirmation code. */
  .ticket-reference {
    display: flex; flex-direction: column; align-items: flex-end; gap: 3px;
  }
  .ticket-reference-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 8.5px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(60, 50, 30, 0.45);
  }
  .ticket-reference-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 1.2px;
    color: #0E1116;
    padding: 5px 10px;
    background: rgba(255, 255, 255, 0.7);
    border: 1px solid rgba(60, 50, 30, 0.18);
    border-radius: 4px;
  }
  .ticket-reference-location {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 500;
    letter-spacing: 0.6px;
    color: rgba(15, 17, 22, 0.55);
    margin-top: 2px;
    max-width: 220px;
    text-align: right;
    line-height: 1.4;
  }

  /* ── Status stamp — replaces the chat-pill ───────────
     A proper "STATUS" stamp in the upper-left of the body region.
     Uppercase letterforms, double-line border in the verdict color,
     subtly tilted like a real ink stamp. JetBrains Mono lockup. */
  .ticket-stamp {
    display: inline-flex; flex-direction: column; align-items: flex-start;
    padding: 7px 14px 8px;
    border: 2px solid currentColor;
    border-radius: 3px;
    /* Double-line border using box-shadow inset */
    box-shadow: inset 0 0 0 4px rgba(255, 255, 255, 0.5),
                inset 0 0 0 5px currentColor;
    color: #16C77E;
    font-family: 'JetBrains Mono', monospace;
    font-weight: 700;
    line-height: 1;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    transform: rotate(-1.5deg);
    transform-origin: top left;
    margin-top: 2px;
  }
  .ticket-stamp-label {
    font-size: 8px;
    letter-spacing: 2px;
    color: currentColor;
    opacity: 0.65;
  }
  .ticket-stamp-value {
    font-size: 16px;
    letter-spacing: 1.8px;
    margin-top: 2px;
    color: currentColor;
  }
  .ticket-card[data-status="caution"] .ticket-stamp { color: #C28412; }
  .ticket-card[data-status="risk"]    .ticket-stamp { color: #C2342B; }

  /* ── Plan section — gets a bit more breathing room now that the
     masthead/stamp consume the top region ── */
  .ticket-card .ticket-plan {
    margin-top: 18px;
  }

  /* ── Conditions Row 1 — ceremonial treatment ─────────
     Row 1 is the headline conditions. It now has a thicker double-rule
     above it instead of the simple thin rule, and the cells are slightly
     more elevated than the lower rows — a thicker bottom border in the
     verdict color, paper-stock background that lifts off the substrate. */
  .ticket-conditions .ticket-conditions-tier.ticket-conditions-top {
    margin-top: 4px;
  }
  .ticket-cond.ticket-cond-top {
    background: rgba(255, 255, 255, 0.85);
    border: 1px solid rgba(60, 50, 30, 0.10);
    border-bottom: 3px solid rgba(22, 199, 126, 0.55);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.95),
      0 1px 2px rgba(60, 50, 30, 0.04);
  }
  .ticket-card[data-status="caution"] .ticket-cond-top { border-bottom-color: rgba(245, 165, 36, 0.55); }
  .ticket-card[data-status="risk"]    .ticket-cond-top { border-bottom-color: rgba(194, 52, 43, 0.55); }

  /* Middle and bottom rows: lighter, more "fine print" energy */
  .ticket-cond.ticket-cond-mid,
  .ticket-cond.ticket-cond-sm {
    background: rgba(255, 255, 255, 0.55);
    border: 1px solid rgba(60, 50, 30, 0.08);
  }

  /* "The full reading" eyebrow gets a small embellishment — a thin
     verdict-color underline that ties it back to the verdict family
     without overpowering the cream substrate. */
  .ticket-conditions-eyebrow {
    display: inline-block;
    padding-bottom: 6px;
    border-bottom: 2px solid rgba(22, 199, 126, 0.45);
    color: rgba(15, 17, 22, 0.70);
  }
  .ticket-card[data-status="caution"] .ticket-conditions-eyebrow {
    border-bottom-color: rgba(245, 165, 36, 0.50);
  }
  .ticket-card[data-status="risk"] .ticket-conditions-eyebrow {
    border-bottom-color: rgba(194, 52, 43, 0.50);
  }

  /* ── Drop the perforation entirely. The pretend-stub was suggesting
     a feature we don't deliver. Cleaner without it. ── */
  .ticket-perforation { display: none; }

  /* Hide the old status-pill and ticket-meta — replaced by stamp + reference */
  .ticket-card .ticket-head { display: none; }

  /* ── Conditions grid v3 — tiered ──────────────────────
     Top tier (4 conditions, big):    Temp, Wind, Precipitation, Sky
     Middle tier (4 conditions, med): Visibility, UV, Humidity, Sunrise/Sunset
     Bottom tier (4 conditions, sm):  Pressure, Air Quality, Pollen, Wind Chill/Heat Index
     The "as-of" attribution (verifier) lives only on the top tier so
     the rest stay clean. */

  .ticket-conditions {
    margin-top: 28px;
  }
  .ticket-conditions-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 2.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin: 0 0 14px;
  }
  .ticket-conditions-tier {
    display: grid;
    gap: 10px;
    margin-bottom: 12px;
  }
  .ticket-conditions-tier:last-child { margin-bottom: 0; }
  .ticket-conditions-top    { grid-template-columns: repeat(4, 1fr); }
  .ticket-conditions-middle { grid-template-columns: repeat(4, 1fr); }
  .ticket-conditions-bottom { grid-template-columns: repeat(4, 1fr); }
  @media (max-width: 720px) {
    .ticket-conditions-top    { grid-template-columns: repeat(2, 1fr); }
    .ticket-conditions-middle { grid-template-columns: repeat(2, 1fr); }
    .ticket-conditions-bottom { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 480px) {
    .ticket-conditions-bottom { grid-template-columns: repeat(2, 1fr); }
  }

  .ticket-cond {
    background: rgba(255, 255, 255, 0.55);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 12px;
    padding: 12px 14px;
    transition: background-color 200ms ease;
  }
  .ticket-card[data-status="risk"] .ticket-cond {
    background: rgba(255, 255, 255, 0.75);
  }
  .ticket-cond-top {
    padding: 14px 16px 14px 16px;
  }
  .ticket-cond-mid {
    padding: 11px 13px;
  }
  .ticket-cond-sm {
    padding: 9px 12px;
  }
  .ticket-cond-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin: 0 0 4px;
  }
  .ticket-cond-top .ticket-cond-label { font-size: 10px; letter-spacing: 1.8px; }
  .ticket-cond-value {
    font-family: 'JetBrains Mono', monospace;
    font-weight: 600;
    color: #0E1116;
    line-height: 1;
    letter-spacing: -0.4px;
  }
  .ticket-cond-top .ticket-cond-value { font-size: 22px; }
  .ticket-cond-mid .ticket-cond-value { font-size: 16px; }
  .ticket-cond-sm  .ticket-cond-value { font-size: 13.5px; }
  .ticket-cond-sub {
    margin-top: 4px;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.3;
  }
  .ticket-cond-top .ticket-cond-sub { font-size: 11.5px; }

  /* Subscriber-continuity block, sits under conditions.
     This replaces the bare "Set alerts" placeholder with a richer
     answer to the question "the free search gives a lot, why pay?"
     Three feature lines (Daily brief, Threshold alerts, Severe
     weather) make ongoing subscription value visible inside the
     free experience. Calm visual treatment — same depth language
     as the rest of the ticket, no aggressive sales styling. */
  .ticket-continuity {
    margin-top: 14px;
    padding: 18px 18px 16px;
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.03) 0%,
        rgba(255, 255, 255, 1) 60%,
        rgba(245, 165, 36, 0.02) 100%),
      #fff;
    border: 1px solid rgba(15, 17, 22, 0.07);
    border-radius: 14px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
  }
  .ticket-continuity-head {
    margin-bottom: 6px;
  }
  .ticket-continuity-eyebrow {
    display: inline-flex; align-items: center; gap: 7px;
    padding: 3px 10px;
    background: rgba(65, 105, 225, 0.07);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
  }
  .ticket-continuity-eyebrow-dot {
    width: 5px; height: 5px;
    border-radius: 999px;
    background: var(--rb, #4169E1);
    position: relative;
  }
  .ticket-continuity-eyebrow-dot::after {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 999px;
    background: inherit;
    opacity: 0.35;
    animation: dotPulse 2.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    pointer-events: none;
  }
  .ticket-continuity-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px;
    font-weight: 600;
    line-height: 1.25;
    letter-spacing: -0.3px;
    color: #0E1116;
    margin: 4px 0 4px;
  }
  .ticket-continuity-body {
    font-size: 13px;
    color: rgba(15, 17, 22, 0.62);
    line-height: 1.5;
    margin: 0 0 14px;
  }
  .ticket-continuity-features {
    list-style: none;
    padding: 0;
    margin: 0 0 16px;
    display: flex; flex-direction: column;
    gap: 8px;
  }
  .ticket-continuity-features li {
    display: flex; align-items: flex-start; gap: 9px;
    font-size: 12.5px;
    line-height: 1.5;
    color: rgba(15, 17, 22, 0.78);
  }
  .ticket-continuity-features li svg {
    flex-shrink: 0;
    color: var(--rb, #4169E1);
    margin-top: 3px;
  }
  .ticket-continuity-features strong {
    color: #0E1116;
    font-weight: 600;
  }
  .ticket-continuity-cta-row {
    display: flex; align-items: center; gap: 14px;
    flex-wrap: wrap;
    margin-top: 4px;
  }
  .ticket-continuity-cta {
    display: inline-flex; align-items: center; gap: 7px;
    padding: 9px 16px;
    background: linear-gradient(180deg, #5278EA 0%, #4169E1 50%, #3257CC 100%);
    color: #fff;
    border: none;
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.1px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.20),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 1px 2px rgba(65, 105, 225, 0.30),
      0 4px 12px rgba(65, 105, 225, 0.18);
    transition: transform 120ms cubic-bezier(0.2, 0.8, 0.2, 1),
                box-shadow 220ms ease;
  }
  .ticket-continuity-cta:hover {
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.25),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 2px 4px rgba(65, 105, 225, 0.36),
      0 10px 22px rgba(65, 105, 225, 0.26);
  }
  .ticket-continuity-cta:active { transform: translateY(0); }
  .ticket-continuity-price {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.50);
    font-style: italic;
  }

  /* Thank-your-Met button — sits below the ticket card, not inside.
     Small, warm, single-action. Doesn't compete with the upsell. */
  .thank-met-row {
    margin: 18px 0 8px;
    display: flex; align-items: center; gap: 12px;
    padding: 14px 18px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 14px;
    box-shadow: 0 4px 14px rgba(15, 17, 22, 0.04);
  }
  .thank-met-eyebrow {
    flex: 1;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.70);
    line-height: 1.45;
  }
  .thank-met-eyebrow strong { color: #0E1116; font-weight: 600; }
  .thank-met-btn {
    flex-shrink: 0;
    display: inline-flex; align-items: center; gap: 7px;
    padding: 8px 14px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: transform 140ms ease, box-shadow 220ms ease;
  }
  .thank-met-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(65, 105, 225, 0.25);
  }
  .thank-met-btn.is-sent {
    background: #16C77E;
    pointer-events: none;
  }

  /* Share-forecast row — sibling of thank-met-row. Quieter visual
     treatment (no Royal Blue button; outlined secondary CTA) so it
     doesn't compete with the thank button. Sits below it as the
     "what to do next" affordance. */
  .share-forecast-row {
    margin: 8px 0 0;
    display: flex; align-items: center; gap: 12px;
    padding: 12px 18px;
    background: rgba(65, 105, 225, 0.03);
    border: 1px solid rgba(65, 105, 225, 0.10);
    border-radius: 14px;
  }
  .share-forecast-text {
    flex: 1;
    min-width: 0;
  }
  .share-forecast-title {
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.35;
  }
  .share-forecast-sub {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.4;
    margin-top: 2px;
  }
  .share-forecast-btn {
    flex-shrink: 0;
    display: inline-flex; align-items: center; gap: 7px;
    padding: 8px 14px;
    background: transparent;
    color: var(--rb, #4169E1);
    border: 1.5px solid rgba(65, 105, 225, 0.30);
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .share-forecast-btn:hover {
    background: rgba(65, 105, 225, 0.06);
    border-color: var(--rb, #4169E1);
    transform: translateY(-1px);
  }
  .share-forecast-btn.is-copied {
    background: rgba(18, 161, 80, 0.10);
    border-color: #16C77E;
    color: #0c7a3c;
  }

  /* ════════════════════════════════════════════════════════════════════
     WORKSPACE NAVIGATION — exit/back affordance for Met & Crew zones
     ════════════════════════════════════════════════════════════════════ */
  .ws-exit-btn {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 7px 12px;
    background: rgba(15, 17, 22, 0.04);
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 500;
    color: rgba(15, 17, 22, 0.65);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
    text-decoration: none;
  }
  .ws-exit-btn:hover {
    background: rgba(15, 17, 22, 0.08);
    color: #0E1116;
  }
  /* Dark variant for the Crew workspace */
  .crew-ws-screen .ws-exit-btn {
    background: rgba(255, 255, 255, 0.04);
    border-color: rgba(255, 255, 255, 0.10);
    color: rgba(232, 235, 240, 0.70);
  }
  .crew-ws-screen .ws-exit-btn:hover {
    background: rgba(255, 255, 255, 0.10);
    color: #F5F7FB;
  }

  /* ════════════════════════════════════════════════════════════════════
     CHECK-IN "OTHER" EXPANSION
     A small grid of spotter conditions that opens when "Other" is
     tapped. Two columns of 5 options on desktop; single column on
     mobile. Each option uses the same tap-target style as the four
     primary check-in tiles.
     ════════════════════════════════════════════════════════════════════ */
  .crew-checkin-other {
    margin-top: 10px;
    padding: 12px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 12px;
    display: none;
  }
  .crew-checkin-other.is-open {
    display: block;
    animation: crewOtherFadeIn 220ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  @keyframes crewOtherFadeIn {
    from { opacity: 0; transform: translateY(-4px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  .crew-checkin-other-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(232, 235, 240, 0.50);
    margin: 0 0 8px;
    display: flex; align-items: center; gap: 8px;
  }
  .crew-checkin-other-eyebrow span:last-child {
    color: rgba(65, 105, 225, 0.80);
    font-weight: 400;
    letter-spacing: 0.8px;
    text-transform: none;
    font-size: 10px;
  }
  .crew-checkin-other-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 5px;
  }
  @media (max-width: 900px) {
    .crew-checkin-other-grid { grid-template-columns: repeat(2, 1fr); }
  }
  .crew-checkin-other-btn {
    padding: 8px 10px;
    background:
      linear-gradient(180deg,
        rgba(255, 255, 255, 0.04) 0%,
        rgba(255, 255, 255, 0.01) 100%);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    color: #F5F7FB;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
    font-family: 'Inter', system-ui, sans-serif;
    text-align: center;
    display: flex; flex-direction: column; align-items: center; gap: 3px;
  }
  .crew-checkin-other-btn:hover {
    border-color: rgba(65, 105, 225, 0.35);
    background:
      linear-gradient(180deg,
        rgba(65, 105, 225, 0.08) 0%,
        rgba(65, 105, 225, 0.02) 100%);
  }
  .crew-checkin-other-btn.is-selected {
    border-color: rgba(65, 105, 225, 0.65);
    background:
      linear-gradient(180deg,
        rgba(65, 105, 225, 0.15) 0%,
        rgba(65, 105, 225, 0.05) 100%);
  }
  .crew-checkin-other-icon {
    font-size: 16px;
    line-height: 1;
  }
  .crew-checkin-other-label {
    font-size: 11px;
    font-weight: 500;
    color: rgba(232, 235, 240, 0.85);
  }

  /* ── Error card: graceful failure when geocode misses ── */
  .ticket-card-error {
    text-align: center;
  }
  .ticket-error-head {
    display: flex; flex-direction: column; align-items: center; gap: 14px;
    margin-bottom: 12px;
  }
  .ticket-error-icon {
    width: 56px; height: 56px; border-radius: 999px;
    background: rgba(194,132,18,0.08);
    border: 1px solid rgba(194,132,18,0.24);
    display: flex; align-items: center; justify-content: center;
    color: #C28412;
  }
  .ticket-error-icon svg { width: 26px; height: 26px; }
  .ticket-error-title {
    font-family: 'Fraunces'; font-size: 24px; font-weight: 600;
    color: #0E1116; letter-spacing: -0.4px;
    margin: 0;
  }
  .ticket-error-body {
    font-family: 'Fraunces'; font-size: 15.5px; line-height: 1.55;
    color: rgba(15,17,22,0.65);
    max-width: 380px; margin: 0 auto 24px;
  }
  .ticket-error-cta {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 12px 22px;
    border-radius: 999px;
    background: #4169E1; color: #fff;
    font-size: 14px; font-weight: 700;
    box-shadow: 0 6px 20px rgba(65,105,225,0.32);
    transition: all 200ms;
  }
  .ticket-error-cta:hover { transform: translateY(-1px); box-shadow: 0 10px 26px rgba(65,105,225,0.42); }
  .ticket-error-cta svg { width: 14px; height: 14px; }

  .ticket-mock { margin-top: 40px; }
  .ticket-mock-head { text-align: center; margin-bottom: 24px; }
  .ticket-mock-eyebrow {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    color: rgba(15,17,22,0.45);
    letter-spacing: 2.2px; text-transform: uppercase;
    margin-bottom: 10px;
  }
  .ticket-mock-title {
    font-family: 'Fraunces'; font-size: 22px; font-weight: 600;
    color: #0E1116; letter-spacing: -0.4px;
  }

  /* ── Assumption banner — when we had to default time or location ── */
  .assumption-banner {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    margin: 16px 0 4px;
    padding: 12px 14px;
    background: rgba(232, 164, 0, 0.06);
    border-left: 3px solid #E8A400;
    border-radius: 0 10px 10px 0;
  }
  .assumption-icon {
    flex-shrink: 0;
    width: 18px;
    height: 18px;
    color: #C28412;
    margin-top: 1px;
  }
  .assumption-text {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    line-height: 1.55;
    color: rgba(15, 17, 22, 0.75);
  }
  .assumption-text strong {
    color: #8a5a0a;
    font-weight: 700;
  }
  .assumption-text em {
    color: rgba(15, 17, 22, 0.55);
    font-style: italic;
    font-family: 'Fraunces', Georgia, serif;
  }

  /* ── Route stops block (multi-location tickets) ── */
  .route-block {
    margin: 16px 0 4px;
    padding: 12px 14px;
    background: rgba(15,17,22,0.03);
    border-radius: 12px;
    border: 1px solid rgba(15,17,22,0.06);
  }
  .route-block-label {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: rgba(15,17,22,0.45);
    margin-bottom: 8px;
  }
  .route-stops {
    display: flex; align-items: center; gap: 6px;
    flex-wrap: wrap;
  }
  .route-stop {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 4px 10px;
    border-radius: 999px;
    font-size: 12px; font-weight: 600;
    background: #fff;
    border: 1px solid rgba(15,17,22,0.10);
  }
  .route-stop-info {
    display: inline-flex; flex-direction: column;
    line-height: 1.15;
  }
  .route-stop-arrival {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 500;
    letter-spacing: 0.4px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.50);
    margin-top: 1px;
  }
  .route-stop.route-stop-caution .route-stop-arrival {
    color: rgba(138, 90, 10, 0.65);
  }
  .route-stop.route-stop-risk .route-stop-arrival {
    color: rgba(155, 40, 35, 0.70);
  }
  .route-stop-dot {
    width: 6px; height: 6px; border-radius: 999px;
    background: #12A150;
  }
  .route-stop.route-stop-caution {
    border-color: rgba(194,132,18,0.35);
    background: rgba(194,132,18,0.05);
    color: #8a5a0a;
  }
  .route-stop.route-stop-caution .route-stop-dot { background: #C28412; }
  .route-stop.route-stop-risk {
    border-color: rgba(194,52,43,0.35);
    background: rgba(194,52,43,0.05);
    color: #9b2823;
  }
  .route-stop.route-stop-risk .route-stop-dot { background: #C2342B; }
  .route-stop.route-stop-clear {
    color: rgba(15,17,22,0.78);
  }
  .route-arrow {
    width: 14px; height: 14px;
    color: rgba(15,17,22,0.30);
    flex-shrink: 0;
  }

  /* ── Upsell block inside the ticket card ── */
  .upsell-block {
    position: relative;
    margin-top: 28px;
    padding: 28px 24px 24px;
    background:
      radial-gradient(circle at 100% 0%, rgba(65,105,225,0.10), transparent 60%),
      linear-gradient(135deg, rgba(65,105,225,0.04), rgba(91,123,255,0.07));
    border: 1px solid rgba(65,105,225,0.20);
    border-radius: 18px;
    overflow: hidden;
    transition: all 220ms cubic-bezier(0.16,1,0.3,1);
  }
  .upsell-block::before {
    content: "";
    position: absolute;
    top: -1px; right: -1px; bottom: -1px;
    width: 4px;
    background: linear-gradient(180deg, #5B7BFF, #4169E1);
    border-radius: 0 18px 18px 0;
    opacity: 0.85;
  }

  /* Prominent variant — used when status is Caution or Risk */
  .upsell-block.upsell-prominent {
    background:
      radial-gradient(circle at 100% 0%, rgba(65,105,225,0.16), transparent 60%),
      linear-gradient(135deg, rgba(65,105,225,0.08), rgba(91,123,255,0.12));
    border: 1.5px solid rgba(65,105,225,0.40);
    box-shadow: 0 12px 32px rgba(65,105,225,0.14), 0 2px 8px rgba(15,17,22,0.04);
    margin-top: 24px;
  }
  .upsell-block.upsell-prominent::before {
    width: 5px;
    opacity: 1;
  }
  .upsell-block.upsell-prominent .upsell-title {
    font-size: 22px;
  }
  .upsell-block.upsell-prominent .upsell-eyebrow-label {
    color: #4169E1;
    font-weight: 800;
  }

  /* The "definitive answer" line — only renders for prominent upsells */
  .upsell-definitive {
    display: flex; align-items: flex-start; gap: 8px;
    margin: 0 0 16px;
    padding: 10px 12px;
    background: rgba(65,105,225,0.08);
    border-radius: 10px;
    border-left: 3px solid #4169E1;
    font-family: 'Fraunces';
    font-size: 13.5px; line-height: 1.5;
    color: #2A3F8F;
    font-weight: 500;
  }
  .upsell-definitive svg {
    flex-shrink: 0;
    width: 14px; height: 14px;
    margin-top: 2px;
    color: #4169E1;
  }
  .upsell-eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    margin-bottom: 12px;
  }
  .upsell-eyebrow-dot {
    width: 6px; height: 6px; border-radius: 999px; background: #4169E1;
  }
  .upsell-eyebrow-label {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    color: #4169E1; letter-spacing: 2.2px; text-transform: uppercase;
  }
  .upsell-title {
    font-family: 'Fraunces'; font-size: 22px; font-weight: 600;
    color: #0E1116; letter-spacing: -0.5px; margin-bottom: 10px;
  }
  .upsell-body {
    font-family: 'Fraunces'; font-size: 14.5px; line-height: 1.55;
    color: rgba(15,17,22,0.62); margin-bottom: 20px;
  }
  .upsell-cta {
    display: inline-flex; align-items: center; gap: 10px;
    width: 100%; justify-content: center;
    padding: 14px 20px; border-radius: 999px;
    background: #4169E1; color: #fff;
    font-size: 14px; font-weight: 700; letter-spacing: -0.1px;
    box-shadow: 0 6px 20px rgba(65,105,225,0.32);
    transition: all 200ms;
  }
  .upsell-cta:hover { transform: translateY(-1px); box-shadow: 0 10px 26px rgba(65,105,225,0.42); }
  .upsell-cta svg { width: 16px; height: 16px; }
  .upsell-pricing {
    text-align: center; margin-top: 12px;
    font-family: 'JetBrains Mono'; font-size: 11px;
    color: rgba(15,17,22,0.55);
  }
  .upsell-price { font-weight: 700; color: #0E1116; font-size: 13px; }
  .upsell-price-divider { margin: 0 8px; color: rgba(15,17,22,0.30); }
  .upsell-price-alt { font-size: 11px; }

  /* ════════════════════════════════════════════════════════════════════════
     VALET CREW — gamification screens (secondary surfaces)
     ════════════════════════════════════════════════════════════════════════
     Three screens: Profile/Member Card, Live Feed (map), Quick Report.
     Same brand language as the customer app — Royal Blue accents, Fraunces
     for display, Inter for body, JetBrains Mono for caps. No XP bars, no
     leaderboards-with-fireworks. Status communicated through typography
     hierarchy and color discipline. Designed for someone standing in
     weather wearing gloves: large taps, calm visuals, fast scanning. */

  /* ── Crew shell (shared header/back nav) ── */
  .crew-screen {
    max-width: 720px;
    margin: 0 auto;
    padding: 0 20px 80px;
  }
  /* Crew Detail card container — gives the single-report deep-dive
     a physical surface. Same depth language as pricing cards and
     the crew welcome card: rounded 20px corners, multi-stop shadow,
     subtle gradient backdrop. The report content lives inside this
     card so it reads as a primary object rather than loose markup
     against the page atmosphere. */
  .detail-card {
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.025) 0%,
        rgba(255, 255, 255, 1) 60%,
        rgba(245, 165, 36, 0.015) 100%),
      #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 22px;
    padding: 24px 22px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 1px 2px rgba(15, 17, 22, 0.04),
      0 12px 32px rgba(65, 105, 225, 0.06),
      0 4px 12px rgba(15, 17, 22, 0.03);
  }
  @media (min-width: 920px) {
    .detail-card { padding: 32px 28px; }
  }

  /* ── Portal screens (Subscriber + Crew Registration) ─────────────
     Shared chrome for the signed-in surfaces. Same depth language as
     the rest of the brand — multi-stop shadows, soft gradient
     backdrops, rounded 20px corners on cards, Royal Blue gradient
     CTAs. These screens are where subscribers and Crew members
     spend most of their time once signed in. */
  .portal-screen {
    max-width: 760px;
    margin: 0 auto;
    padding: 0 20px 80px;
  }
  .portal-nav {
    display: flex; align-items: center; justify-content: space-between;
    padding: 16px 0;
    margin-bottom: 24px;
  }
  .portal-back {
    color: var(--rb, #4169E1); text-decoration: none;
    font-size: 13px; font-weight: 600;
    display: inline-flex; align-items: center; gap: 4px;
    background: transparent; border: none;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .portal-nav-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
  }
  .portal-signout {
    background: transparent;
    border: none;
    color: rgba(15, 17, 22, 0.50);
    font-size: 12px;
    font-weight: 500;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: color 140ms ease;
  }
  .portal-signout:hover { color: var(--rb, #4169E1); }

  /* Identity card — top of subscriber portal, plan and renewal info */
  .portal-identity-card {
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.05) 0%,
        rgba(255, 255, 255, 1) 55%,
        rgba(245, 165, 36, 0.02) 100%),
      #fff;
    border: 1px solid rgba(65, 105, 225, 0.18);
    border-radius: 22px;
    padding: 26px 24px;
    margin-bottom: 28px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 1px 2px rgba(65, 105, 225, 0.06),
      0 12px 36px rgba(65, 105, 225, 0.10),
      0 4px 16px rgba(15, 17, 22, 0.03);
  }
  .portal-identity-eyebrow {
    display: inline-flex; align-items: center; gap: 7px;
    padding: 4px 12px;
    background: rgba(65, 105, 225, 0.10);
    border-radius: 999px;
    margin-bottom: 12px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
  }
  .portal-identity-dot {
    width: 5px; height: 5px;
    border-radius: 999px;
    background: var(--rb, #4169E1);
    position: relative;
  }
  .portal-identity-dot::after {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 999px;
    background: inherit;
    opacity: 0.35;
    animation: dotPulse 2.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    pointer-events: none;
  }
  .portal-identity-name {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 30px;
    font-weight: 600;
    line-height: 1.1;
    letter-spacing: -0.6px;
    color: #0E1116;
    margin: 0 0 4px;
  }
  .portal-identity-loc {
    font-size: 14px;
    color: rgba(15, 17, 22, 0.60);
    margin: 0 0 22px;
  }
  .portal-identity-meta {
    display: grid;
    grid-template-columns: 1fr;
    gap: 14px;
    padding-top: 18px;
    border-top: 1px solid rgba(65, 105, 225, 0.12);
  }
  @media (min-width: 640px) {
    .portal-identity-meta { grid-template-columns: repeat(3, 1fr); }
  }
  .portal-identity-meta-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin-bottom: 4px;
  }
  .portal-identity-meta-value {
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.3;
  }

  /* Section blocks */
  .portal-section {
    margin-bottom: 32px;
  }
  .portal-section-head {
    display: flex; align-items: baseline; justify-content: space-between;
    margin-bottom: 12px;
  }
  .portal-section-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 20px;
    font-weight: 600;
    letter-spacing: -0.3px;
    color: #0E1116;
    margin: 0;
  }
  .portal-section-edit,
  .portal-section-add {
    background: transparent;
    border: none;
    color: var(--rb, #4169E1);
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    padding: 4px 0;
  }
  .portal-section-edit:hover,
  .portal-section-add:hover {
    text-decoration: underline;
  }

  /* Saved location card */
  .portal-location-card {
    display: flex; align-items: center; gap: 14px;
    padding: 18px 20px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 16px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
  }
  .portal-location-icon {
    color: var(--rb, #4169E1);
    flex-shrink: 0;
  }
  .portal-location-name {
    font-size: 15px;
    font-weight: 600;
    color: #0E1116;
    margin-bottom: 2px;
  }
  .portal-location-coords {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
    letter-spacing: 0.2px;
  }

  /* Preferences grid */
  .portal-prefs-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
  }
  @media (min-width: 640px) {
    .portal-prefs-grid { grid-template-columns: 1fr 1fr; }
  }
  .portal-pref {
    padding: 16px 18px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 14px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
  }
  .portal-pref-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin-bottom: 6px;
  }
  .portal-pref-value {
    font-size: 16px;
    font-weight: 600;
    color: #0E1116;
    margin-bottom: 4px;
  }
  .portal-pref-value.portal-pref-muted {
    color: rgba(15, 17, 22, 0.35);
    font-weight: 500;
  }
  .portal-pref-detail {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.50);
    line-height: 1.4;
  }
  .portal-pref-channels {
    display: flex; gap: 6px;
    margin-bottom: 6px;
  }
  .portal-channel-chip {
    display: inline-flex; align-items: center;
    padding: 4px 10px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    background: rgba(15, 17, 22, 0.05);
    color: rgba(15, 17, 22, 0.40);
    border: 1px solid rgba(15, 17, 22, 0.06);
  }
  .portal-channel-chip.is-active {
    background: rgba(65, 105, 225, 0.10);
    color: var(--rb, #4169E1);
    border-color: rgba(65, 105, 225, 0.25);
  }

  /* Threshold alerts list */
  .portal-thresholds {
    display: flex; flex-direction: column;
    gap: 8px;
  }
  .portal-threshold {
    display: flex; align-items: center; gap: 14px;
    padding: 14px 16px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 12px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
    transition: border-color 160ms ease, box-shadow 160ms ease;
  }
  .portal-threshold:hover {
    border-color: rgba(65, 105, 225, 0.25);
  }
  .portal-threshold-icon {
    flex-shrink: 0;
    width: 32px; height: 32px;
    border-radius: 8px;
    background: rgba(65, 105, 225, 0.08);
    color: var(--rb, #4169E1);
    display: flex; align-items: center; justify-content: center;
  }
  .portal-threshold-text { flex: 1; }
  .portal-threshold-name {
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
    margin-bottom: 2px;
  }
  .portal-threshold-detail {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
  }
  .portal-threshold-action {
    background: transparent;
    border: none;
    color: var(--rb, #4169E1);
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    padding: 4px 8px;
    -webkit-tap-highlight-color: transparent;
  }
  .portal-threshold-action:hover {
    text-decoration: underline;
  }

  /* Brief history */
  .portal-history {
    display: flex; flex-direction: column;
    gap: 12px;
  }
  .portal-history-item {
    display: grid;
    grid-template-columns: 80px 1fr;
    gap: 18px;
    padding: 16px 18px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 14px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
    cursor: pointer;
    transition: border-color 160ms ease, transform 160ms ease, box-shadow 160ms ease;
  }
  .portal-history-item:hover {
    border-color: rgba(65, 105, 225, 0.25);
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 4px 14px rgba(15, 17, 22, 0.05);
  }
  .portal-history-date {
    border-right: 1px solid rgba(15, 17, 22, 0.08);
    padding-right: 18px;
  }
  .portal-history-day {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1;
  }
  .portal-history-time {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.50);
    margin-top: 4px;
    letter-spacing: 0.2px;
  }
  .portal-history-meta {
    display: flex; align-items: center; gap: 10px;
    margin-bottom: 6px;
  }
  .portal-history-type {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
  }
  .portal-history-verdict {
    padding: 2px 8px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
  }
  .portal-history-verdict.verdict-clear   { background: rgba(18, 161, 80, 0.10); color: #0c7a3c; }
  .portal-history-verdict.verdict-caution { background: rgba(232, 164, 0, 0.12); color: #8a5a0a; }
  .portal-history-verdict.verdict-risk    { background: rgba(194, 52, 43, 0.10); color: #9b2823; }
  .portal-history-snippet {
    font-size: 14px;
    color: rgba(15, 17, 22, 0.82);
    line-height: 1.5;
    margin-bottom: 6px;
  }
  .portal-history-signed {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15, 17, 22, 0.45);
    letter-spacing: 0.2px;
  }

  /* Billing card */
  .portal-billing-card {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 16px;
    padding: 22px 24px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
  }
  .portal-billing-row {
    display: flex; align-items: center; justify-content: space-between;
    padding: 10px 0;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
  }
  .portal-billing-row:last-of-type {
    border-bottom: none;
  }
  .portal-billing-label {
    font-size: 13px;
    color: rgba(15, 17, 22, 0.55);
  }
  .portal-billing-value {
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
  }
  .portal-billing-actions {
    display: flex; gap: 10px;
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid rgba(15, 17, 22, 0.08);
  }
  .portal-billing-action {
    padding: 9px 16px;
    background: transparent;
    border: 1.5px solid rgba(65, 105, 225, 0.30);
    color: var(--rb, #4169E1);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .portal-billing-action:hover {
    background: rgba(65, 105, 225, 0.06);
    border-color: var(--rb, #4169E1);
  }
  .portal-billing-action.portal-billing-cancel {
    border-color: rgba(15, 17, 22, 0.12);
    color: rgba(15, 17, 22, 0.55);
  }
  .portal-billing-action.portal-billing-cancel:hover {
    border-color: rgba(194, 52, 43, 0.40);
    color: #9b2823;
    background: rgba(194, 52, 43, 0.04);
  }

  /* ── Crew Registration page ─────────────────────────────────────── */
  .crew-register-hero {
    text-align: center;
    margin-bottom: 40px;
  }
  .crew-register-eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 5px 14px;
    background: rgba(232, 164, 0, 0.10);
    border-radius: 999px;
    margin-bottom: 18px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: #8a5a0a;
  }
  .crew-register-eyebrow-dot {
    width: 6px; height: 6px;
    border-radius: 999px;
    background: #E8A400;
  }
  .crew-register-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 32px;
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -0.8px;
    color: #0E1116;
    margin: 0 0 16px;
  }
  @media (min-width: 720px) {
    .crew-register-headline { font-size: 40px; letter-spacing: -1.2px; }
  }
  .crew-register-headline em {
    font-style: italic;
    font-weight: 500;
    background: linear-gradient(135deg, #C28412 0%, #8a5a0a 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: -0.8px;
  }
  .crew-register-sub {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 16px;
    line-height: 1.55;
    color: rgba(15, 17, 22, 0.60);
    max-width: 520px;
    margin: 0 auto;
  }

  /* Three-pillar value prop */
  .crew-register-pillars {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
    margin-bottom: 40px;
  }
  @media (min-width: 720px) {
    .crew-register-pillars { grid-template-columns: repeat(3, 1fr); }
  }
  .crew-register-pillar {
    padding: 22px 20px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 16px;
    text-align: left;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
  }
  .crew-register-pillar-icon {
    width: 36px; height: 36px;
    border-radius: 10px;
    background: rgba(232, 164, 0, 0.10);
    color: #C28412;
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 12px;
  }
  .crew-register-pillar-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 17px;
    font-weight: 600;
    letter-spacing: -0.2px;
    color: #0E1116;
    margin: 0 0 6px;
  }
  .crew-register-pillar-body {
    font-size: 13px;
    line-height: 1.5;
    color: rgba(15, 17, 22, 0.65);
    margin: 0;
  }

  /* Form */
  .crew-register-form {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 18px;
    padding: 24px 22px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.8),
      0 1px 2px rgba(15, 17, 22, 0.03);
  }
  .form-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 14px;
    margin-bottom: 14px;
  }
  @media (min-width: 600px) {
    .form-grid { grid-template-columns: 1fr 1fr; }
  }
  .form-field {
    margin-bottom: 18px;
  }
  .form-field:last-child { margin-bottom: 0; }
  .form-label {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin-bottom: 8px;
  }
  .form-input {
    width: 100%;
    padding: 11px 14px;
    background: #fff;
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    color: #0E1116;
    outline: none;
    transition: border-color 160ms ease, box-shadow 160ms ease;
    -webkit-appearance: none;
    appearance: none;
  }
  .form-input:focus {
    border-color: var(--rb, #4169E1);
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.15);
  }
  .form-detail {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.50);
    margin-top: 6px;
    line-height: 1.4;
  }
  .form-checkbox-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
  }
  @media (min-width: 600px) {
    .form-checkbox-grid { grid-template-columns: repeat(3, 1fr); }
  }
  .form-checkbox {
    display: flex; align-items: center; gap: 8px;
    padding: 10px 12px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 8px;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.78);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .form-checkbox:hover {
    border-color: rgba(65, 105, 225, 0.25);
  }
  .form-checkbox input[type="checkbox"] {
    accent-color: var(--rb, #4169E1);
  }
  .form-radio-stack {
    display: flex; flex-direction: column;
    gap: 6px;
  }
  .form-radio {
    display: flex; align-items: flex-start; gap: 10px;
    padding: 11px 14px;
    background: #fff;
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    border-radius: 10px;
    font-size: 13px;
    line-height: 1.5;
    color: rgba(15, 17, 22, 0.78);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .form-radio:hover { border-color: rgba(65, 105, 225, 0.30); }
  .form-radio input[type="radio"] {
    margin-top: 2px;
    accent-color: var(--rb, #4169E1);
  }
  .form-radio strong { color: #0E1116; font-weight: 600; }
  .form-agree {
    padding: 14px 16px;
    background: rgba(65, 105, 225, 0.04);
    border-radius: 10px;
  }
  .form-checkbox-large {
    display: flex; align-items: flex-start; gap: 12px;
    font-size: 13.5px;
    line-height: 1.5;
    color: rgba(15, 17, 22, 0.78);
    cursor: pointer;
  }
  .form-checkbox-large input[type="checkbox"] {
    margin-top: 3px;
    accent-color: var(--rb, #4169E1);
  }
  .form-link {
    color: var(--rb, #4169E1);
    text-decoration: underline;
  }
  .form-submit-row {
    margin-top: 18px;
    padding-top: 18px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
    text-align: center;
  }
  .form-submit-btn {
    display: inline-flex; align-items: center; justify-content: center;
    padding: 14px 28px;
    background: linear-gradient(180deg, #5278EA 0%, #4169E1 50%, #3257CC 100%);
    color: #fff;
    border: none;
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.1px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.20),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 1px 2px rgba(65, 105, 225, 0.30),
      0 4px 12px rgba(65, 105, 225, 0.20);
    transition: transform 120ms cubic-bezier(0.2, 0.8, 0.2, 1),
                box-shadow 220ms ease;
  }
  .form-submit-btn:hover {
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.25),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 2px 4px rgba(65, 105, 225, 0.36),
      0 12px 24px rgba(65, 105, 225, 0.28);
  }
  .form-submit-detail {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.50);
    margin: 10px 0 0;
  }

  .crew-nav {
    display: flex; align-items: center; justify-content: space-between;
    padding: 16px 0;
    margin-bottom: 8px;
  }
  .crew-back {
    color: var(--rb, #4169E1); text-decoration: none;
    font-size: 13px; font-weight: 600;
    display: inline-flex; align-items: center; gap: 4px;
    min-height: 44px; min-width: 44px;
    margin-left: -8px; padding: 0 8px;
    background: none; border: none; cursor: pointer;
    font-family: inherit;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-nav-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.55);
  }

  /* ── PROFILE / MEMBER CARD ── */
  .member-card {
    background: linear-gradient(135deg, #4169E1 0%, #5B7DEB 100%);
    border-radius: 24px;
    padding: 28px 24px 24px;
    color: #fff;
    margin-bottom: 24px;
    position: relative;
    overflow: hidden;
    box-shadow: 0 20px 50px rgba(65,105,225,0.25);
  }
  /* Subtle pattern overlay — like a passport security weave */
  .member-card::before {
    content: ''; position: absolute; inset: 0;
    background-image: radial-gradient(circle at 20% 30%, rgba(255,255,255,0.08) 0%, transparent 40%),
                      radial-gradient(circle at 80% 70%, rgba(255,255,255,0.06) 0%, transparent 50%);
    pointer-events: none;
  }
  .member-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 2.4px; text-transform: uppercase;
    color: rgba(255,255,255,0.75);
    display: inline-flex; align-items: center; gap: 6px;
    margin-bottom: 16px;
    position: relative;
    /* Reserve space for the absolutely-positioned tokens chip in the top-right */
    padding-right: 130px;
    min-height: 30px;
  }
  .member-eyebrow-dot {
    width: 6px; height: 6px; border-radius: 999px;
    background: rgba(255,255,255,0.95);
    box-shadow: 0 0 0 3px rgba(255,255,255,0.20);
  }
  .member-name {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 28px; font-weight: 600;
    letter-spacing: -0.4px; line-height: 1.15;
    margin: 0 0 4px;
    position: relative;
  }
  .member-handle {
    font-size: 13px;
    color: rgba(255,255,255,0.70);
    margin: 0 0 24px;
    position: relative;
  }
  .member-rank-row {
    display: flex; align-items: center;
    gap: 14px;
    padding: 16px 0 4px;
    border-top: 1px solid rgba(255,255,255,0.15);
    position: relative;
  }
  .member-rank-icon {
    width: 56px; height: 56px;
    border-radius: 50%;
    background: linear-gradient(135deg, #FFD56F 0%, #E8A400 100%);
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 4px 16px rgba(232,164,0,0.45);
    color: #6b3f00;
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px; font-weight: 700;
    flex-shrink: 0;
  }
  .member-rank-text {
    flex: 1;
  }
  .member-rank-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 1.4px; text-transform: uppercase;
    color: rgba(255,255,255,0.65);
    margin-bottom: 2px;
  }
  .member-rank-tier {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px; font-weight: 600;
  }
  .member-rank-progress {
    font-size: 12px;
    color: rgba(255,255,255,0.70);
    margin-top: 2px;
  }

  /* ── Bio strip (Chunk 5a — character card) ───────────
     Sits below the rank row inside the member card. Four
     identity facts — season, member-since, days active,
     specialty — arranged as a grid. Reads like the stat
     line on the back of a trading card. Quiet white-on-
     blue, monospace, low-contrast labels and crisp values. */
  .member-bio-strip {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 14px 18px;
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid rgba(255, 255, 255, 0.15);
    position: relative;
  }
  @media (min-width: 540px) {
    .member-bio-strip {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .member-bio-stat {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
  }
  .member-bio-stat-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.55);
  }
  .member-bio-stat-value {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    font-weight: 600;
    color: #fff;
    line-height: 1.25;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  /* Specialty spans 2 cols on narrow viewports so its longer text has room. */
  .member-bio-specialty {
    grid-column: span 2;
  }
  @media (min-width: 540px) {
    .member-bio-specialty {
      grid-column: span 1;
    }
  }

  /* Tokens chip — a small balance indicator in the top-right of the card.
     Tokens are the Crew's contribution currency: they accumulate from
     reports + clarity streaks and unlock badges at thresholds. They are
     NOT spendable — they're a quiet ledger of contributions, like miles
     flown rather than miles redeemable. The chip shows the running total
     and animates on increment when a report is submitted. */
  .tokens-chip {
    position: absolute;
    top: 20px; right: 20px;
    background: rgba(255,255,255,0.18);
    border: 1px solid rgba(255,255,255,0.30);
    border-radius: 999px;
    padding: 6px 12px 6px 10px;
    display: inline-flex; align-items: center; gap: 6px;
    backdrop-filter: blur(6px);
    z-index: 1;
  }
  .tokens-chip-icon {
    color: rgba(255,255,255,0.95);
    flex-shrink: 0;
  }
  .tokens-chip-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px; font-weight: 700;
    color: #fff;
    letter-spacing: 0.4px;
    /* Smooth transition for the increment animation */
    transition: transform 240ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .tokens-chip-value.bump {
    /* Triggered momentarily after a report submit — small scale-up that
       settles back, paired with the +N float-up text. Restrained: 120%
       max, not 150%. */
    transform: scale(1.18);
  }
  .tokens-chip-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: rgba(255,255,255,0.70);
  }

  /* ── Stats grid (Protected Hours / Clarity Streak) ── */
  .crew-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 24px;
  }
  .crew-stat {
    background: #fff;
    border: 1px solid rgba(15,17,22,0.08);
    border-radius: 16px;
    padding: 16px 18px;
  }
  .crew-stat-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.55);
    margin-bottom: 8px;
  }
  .crew-stat-value {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 30px; font-weight: 600;
    letter-spacing: -0.4px;
    line-height: 1;
    color: #0E1116;
    margin-bottom: 4px;
  }
  .crew-stat-unit {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px; font-weight: 500;
    color: rgba(15,17,22,0.55);
    letter-spacing: 0;
    margin-left: 4px;
  }
  .crew-stat-sub {
    font-size: 12px;
    color: rgba(15,17,22,0.55);
    line-height: 1.4;
  }
  .crew-stat-sub strong { color: #0E1116; font-weight: 600; }

  /* Tiny progress bar under each stat — shows the path to the next
     milestone without making it feel like a grind. Filled portion is
     Royal Blue for protected hours (steady accumulation) and a softer
     amber-red gradient for the streak (more energetic). */
  .crew-stat-progress {
    height: 4px;
    background: rgba(15,17,22,0.06);
    border-radius: 999px;
    overflow: hidden;
    margin-top: 10px;
  }
  .crew-stat-progress-fill {
    height: 100%;
    background: var(--rb, #4169E1);
    border-radius: 999px;
    transition: width 600ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .crew-stat.streak .crew-stat-progress-fill {
    background: linear-gradient(90deg, #E8A400 0%, #C2342B 100%);
  }
  .crew-stat-milestone {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    color: rgba(15,17,22,0.45);
    letter-spacing: 0.4px;
    text-transform: uppercase;
    margin-top: 6px;
  }

  /* Streak flame — small icon next to the streak number */
  .crew-stat.streak .crew-stat-value {
    display: inline-flex;
    align-items: baseline;
    gap: 4px;
  }
  .crew-stat-flame {
    font-size: 22px;
    line-height: 1;
    /* Subtle flicker — 4s cycle, low amplitude. Calmer than a real flame. */
    animation: flameFlicker 3.6s ease-in-out infinite;
    transform-origin: bottom center;
  }
  @keyframes flameFlicker {
    0%, 100% { transform: scale(1) rotate(0deg); opacity: 1; }
    33% { transform: scale(1.04) rotate(-1deg); opacity: 0.92; }
    66% { transform: scale(0.98) rotate(0.8deg); opacity: 0.96; }
  }

  /* Streak gets a subtle flame accent — restrained, not gamey */
  .crew-stat.streak .crew-stat-value { color: #C2342B; }

  /* ── Receipt row (Chunk 5b) ──────────────────────────
     Sits below the two journey tiles (Protected Hours +
     Clarity Streak). Three compact tiles, no progress bars,
     no milestone copy. Reads as a stat line: what your work
     has earned you (reports submitted, verifies your reports
     received from other Crew, citations from Mets).

     Visually quieter than the journey tiles above — flat
     paper background, single-row layout, monospaced labels
     for that "scoreboard" feel without going full sportsbook. */
  .crew-receipts-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
    margin: 0 0 24px;
    padding: 14px 4px;
    background: #FAFBFD;
    border: 1px solid rgba(15, 17, 22, 0.07);
    border-radius: 14px;
  }
  .crew-receipt-tile {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    padding: 4px 6px;
    text-align: center;
    /* Vertical separators between tiles — drawn as a left border on each
       tile except the first, so the row reads as a single grouped strip
       rather than three loose cards. */
    border-left: 1px solid rgba(15, 17, 22, 0.06);
  }
  .crew-receipt-tile:first-child {
    border-left: none;
  }
  .crew-receipt-value {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 26px;
    font-weight: 600;
    line-height: 1;
    color: #0E1116;
    letter-spacing: -0.4px;
  }
  .crew-receipt-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    text-align: center;
  }

  /* ── Season Highlights (Chunk 5c) ──────────────────────
     Curated moments below the receipt row. Each card is a
     specific event with a generated editorial headline.
     Cards in a 2-column grid on desktop, single column on
     mobile. Category-keyed accent stripe on the left edge
     gives a subtle visual differentiation between the three
     highlight kinds (verified / cited / photo). */
  .season-highlights {
    margin: 0 0 24px;
  }
  .season-highlights-sub {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 500;
    letter-spacing: 0.6px;
    color: rgba(15, 17, 22, 0.40);
    text-transform: none;
    margin-left: 10px;
  }
  .season-highlights-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
    margin-top: 10px;
  }
  @media (min-width: 720px) {
    .season-highlights-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .highlight-card {
    position: relative;
    padding: 16px 18px 16px 22px;
    background: #FAFBFD;
    border: 1px solid rgba(15, 17, 22, 0.07);
    border-radius: 14px;
    overflow: hidden;
  }
  .highlight-card-stripe {
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 4px;
    background: var(--rb, #4169E1);
  }
  .highlight-card[data-kind="verified"] .highlight-card-stripe { background: #16C77E; }
  .highlight-card[data-kind="cited"]    .highlight-card-stripe { background: #F5A524; }
  .highlight-card[data-kind="photo"]    .highlight-card-stripe { background: #4169E1; }

  .highlight-card-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin-bottom: 8px;
  }
  .highlight-card-eyebrow-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
  }
  .highlight-card[data-kind="verified"] .highlight-card-eyebrow-dot { background: #16C77E; }
  .highlight-card[data-kind="cited"]    .highlight-card-eyebrow-dot { background: #F5A524; }
  .highlight-card[data-kind="photo"]    .highlight-card-eyebrow-dot { background: #4169E1; }

  .highlight-card-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 18px;
    font-weight: 500;
    line-height: 1.3;
    letter-spacing: -0.2px;
    color: #0E1116;
    margin: 0 0 10px;
  }
  .highlight-card-meta {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.55);
    letter-spacing: 0.3px;
    margin-bottom: 10px;
  }
  .highlight-card-meta-sep {
    color: rgba(15, 17, 22, 0.30);
  }
  .highlight-card-quote {
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 13.5px;
    line-height: 1.5;
    color: rgba(15, 17, 22, 0.72);
    margin: 0;
    padding: 10px 12px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 9px;
    /* Two-line clamp so long reports don't blow out the card. */
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .highlight-card-quote::before {
    content: '\201C';  /* open curly quote */
    margin-right: 2px;
    color: rgba(15, 17, 22, 0.30);
  }
  .highlight-card-quote::after {
    content: '\201D';  /* close curly quote */
    margin-left: 2px;
    color: rgba(15, 17, 22, 0.30);
  }

  /* Fallback card — shown when no highlights qualify.
     Warmer than an empty state: a small nudge toward action,
     not a closed door. */
  .highlight-empty {
    grid-column: 1 / -1;
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 16px 18px;
    background: #FAFBFD;
    border: 1px dashed rgba(65, 105, 225, 0.25);
    border-radius: 14px;
  }
  .highlight-empty-icon {
    flex-shrink: 0;
    width: 38px;
    height: 38px;
    border-radius: 50%;
    background: rgba(65, 105, 225, 0.08);
    color: #4169E1;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .highlight-empty-text {
    flex: 1;
    min-width: 0;
  }
  .highlight-empty-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 15px;
    font-weight: 500;
    color: #0E1116;
    margin: 0 0 4px;
  }
  .highlight-empty-sub {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.60);
    margin: 0;
    line-height: 1.4;
  }

  /* ── Profile · Your recent reports (Chunk 5d) ───────────
     A scannable vertical list of this user's own reports.
     Each row: a colored type chip, the report text (clamped
     to 2 lines), and a meta line (city · time · verifies).
     Compact and quiet — this is reflective content, not
     headline content. */
  .profile-recent {
    margin: 0 0 24px;
  }
  .profile-recent-sub {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 500;
    letter-spacing: 0.6px;
    color: rgba(15, 17, 22, 0.40);
    text-transform: none;
    margin-left: 10px;
  }
  .profile-recent-list {
    list-style: none;
    margin: 10px 0 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .profile-recent-empty {
    padding: 16px 18px;
    background: #FAFBFD;
    border: 1px dashed rgba(15, 17, 22, 0.10);
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.55);
    text-align: center;
    line-height: 1.5;
  }
  .profile-recent-item {
    display: flex;
    gap: 10px;
    padding: 10px 12px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 10px;
    cursor: pointer;
    transition: border-color 140ms ease, transform 100ms ease;
  }
  .profile-recent-item:hover {
    border-color: rgba(65, 105, 225, 0.25);
  }
  .profile-recent-item:active {
    transform: scale(0.99);
  }
  .profile-recent-type {
    flex-shrink: 0;
    width: 8px;
    border-radius: 4px;
    align-self: stretch;
  }
  .profile-recent-item[data-type="storm"]    .profile-recent-type { background: #C2342B; }
  .profile-recent-item[data-type="hail"]     .profile-recent-type { background: #5B6BD6; }
  .profile-recent-item[data-type="wind"]     .profile-recent-type { background: #E8A400; }
  .profile-recent-item[data-type="rain"]     .profile-recent-type { background: #2566D6; }
  .profile-recent-item[data-type="snow"]     .profile-recent-type { background: #4A6BA8; }
  .profile-recent-item[data-type="sleet"]    .profile-recent-type { background: #7088B0; }
  .profile-recent-item[data-type="freezing"] .profile-recent-type { background: #5B7E9B; }
  .profile-recent-item[data-type="fog"]      .profile-recent-type { background: #94A3B8; }
  .profile-recent-item[data-type="sunny"]    .profile-recent-type { background: #F5A524; }
  .profile-recent-item[data-type="cloudy"]   .profile-recent-type { background: #9CA3AF; }
  .profile-recent-item[data-type="humid"]    .profile-recent-type { background: #8FA8B5; }
  .profile-recent-item[data-type="clearing"] .profile-recent-type { background: #5EE3A6; }
  .profile-recent-item[data-type="clear"]    .profile-recent-type { background: #12A150; }
  .profile-recent-body {
    flex: 1;
    min-width: 0;
  }
  .profile-recent-text {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    line-height: 1.45;
    color: rgba(15, 17, 22, 0.85);
    margin: 0 0 4px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .profile-recent-meta {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15, 17, 22, 0.50);
    letter-spacing: 0.3px;
  }
  .profile-recent-meta-sep {
    color: rgba(15, 17, 22, 0.25);
  }
  .profile-recent-verifies {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    color: #0a6e35;
  }
  .profile-recent-verifies svg {
    width: 10px;
    height: 10px;
  }

  /* ── Profile · Field Notebook (light-mode version, Chunk 5d) ──
     Light-mode counterpart to the dark notebook that used to
     live on the Workspace. Quote-card styling: each entry is
     a Fraunces-italic note with a date and a privacy flag. */
  .profile-notebook {
    margin: 0 0 24px;
  }
  .profile-notebook-sub {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 500;
    letter-spacing: 0.6px;
    color: rgba(15, 17, 22, 0.40);
    text-transform: none;
    margin-left: 10px;
  }
  .profile-notebook-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: 10px;
  }
  .profile-notebook-empty {
    padding: 20px 18px;
    background: #FAFBFD;
    border: 1px dashed rgba(15, 17, 22, 0.10);
    border-radius: 12px;
    text-align: center;
    color: rgba(15, 17, 22, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    line-height: 1.55;
    max-width: 580px;
    margin: 10px auto 0;
  }
  .profile-notebook-entry {
    padding: 14px 16px;
    background: #FAFBFD;
    border: 1px solid rgba(15, 17, 22, 0.07);
    border-radius: 12px;
  }
  .profile-notebook-entry-meta {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 8px;
    flex-wrap: wrap;
  }
  .profile-notebook-entry-date {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.8px;
    color: rgba(15, 17, 22, 0.50);
    text-transform: uppercase;
  }
  .profile-notebook-entry-mission {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.4px;
    color: rgba(65, 105, 225, 0.85);
  }
  .profile-notebook-entry-flag {
    display: inline-block;
    padding: 1px 7px;
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.3px;
    background: rgba(245, 165, 36, 0.15);
    color: #8a5a0a;
  }
  .profile-notebook-entry-flag.is-private {
    background: rgba(15, 17, 22, 0.06);
    color: rgba(15, 17, 22, 0.60);
  }
  .profile-notebook-entry-text {
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 14.5px;
    line-height: 1.55;
    color: rgba(15, 17, 22, 0.85);
    margin: 0;
    white-space: pre-wrap;
  }

  /* Prototype data indicator — quiet amber pill that signals to testers
     that the Crew reports are simulated. Not alarming (no warning icon,
     no shouty red), just honest: the work of building the data layer is
     in progress, and we're not pretending these are real-time human
     reports. Same calm restraint as the rest of the Crew layer. */
  .prototype-pill {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 7px 12px 6px;
    margin-bottom: 16px;
    background: rgba(232, 164, 0, 0.08);
    border: 1px solid rgba(232, 164, 0, 0.22);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.6px;
    color: rgba(120, 80, 0, 0.85);
    text-transform: uppercase;
  }
  .prototype-pill-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: rgb(232, 164, 0);
    flex-shrink: 0;
  }

  /* ── First-time Crew welcome card ─────────────────────────────────────
     Shown once per user-device when they first arrive at the Live Feed.
     Royal Blue framing makes it feel like a Crew artifact rather than
     a system notice. Calm, instructive, dismissable. */
  /* ── Live broadcast ticker (Chunk 4a) ──────────────────
     The "ON AIR" strip at the top of the Live Feed. Pulsing red
     dot, then a single-line slot-machine that fades through real
     events every ~3.5 seconds. Visual chassis: dark band with
     italic Fraunces for the event text, monospace label. */
  .feed-ticker {
    display: flex;
    align-items: center;
    gap: 14px;
    margin: 0 0 16px;
    padding: 9px 14px;
    background: linear-gradient(180deg, #14191F, #0F1419);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 10px;
    overflow: hidden;
    min-height: 38px;
  }
  .feed-ticker-rec {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    color: #FCA5A5;
  }
  .feed-ticker-rec-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #C2342B;
    box-shadow: 0 0 0 3px rgba(194, 52, 43, 0.20), 0 0 10px rgba(194, 52, 43, 0.65);
    animation: feedTickerRec 1.6s ease-in-out infinite;
  }
  @keyframes feedTickerRec {
    0%, 100% { opacity: 0.75; transform: scale(0.92); }
    50%      { opacity: 1;    transform: scale(1); }
  }
  .feed-ticker-content {
    flex: 1;
    min-width: 0;
    overflow: hidden;
    position: relative;
    height: 20px;
  }
  .feed-ticker-text {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 14px;
    color: rgba(232, 235, 240, 0.92);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    transition: opacity 280ms ease, transform 280ms ease;
  }
  .feed-ticker-text.is-leaving {
    opacity: 0;
    transform: translateY(-6px);
  }
  .feed-ticker-text.is-entering {
    opacity: 0;
    transform: translateY(6px);
  }
  .feed-ticker-text strong {
    font-style: normal;
    font-weight: 600;
    color: #fff;
  }
  /* Event-type accents — small color cue to differentiate report
     events from verification events from citation events. */
  .feed-ticker-text .ticker-tag-report   { color: #FCA5A5; }
  .feed-ticker-text .ticker-tag-verify   { color: #5EE3A6; }
  .feed-ticker-text .ticker-tag-cited    { color: #F5C57A; }
  .feed-ticker-text .ticker-tag-tag {
    font-family: 'JetBrains Mono', monospace;
    font-style: normal;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    margin-right: 8px;
  }
  @media (prefers-reduced-motion: reduce) {
    .feed-ticker-text {
      transition: opacity 100ms ease;
    }
    .feed-ticker-text.is-leaving,
    .feed-ticker-text.is-entering {
      transform: none;
    }
  }

  /* ── "Now on air" rail (Chunk 4b) ──────────────────────
     The cast for this episode. Sits between the ticker and
     the headline. Horizontal-scrolling row of small cards,
     each representing a Crew member who's reported recently.
     Light-mode chassis (the ticker is dark; the rail is paper
     so it doesn't look like one big broadcast bar that
     overwhelms the page). */
  .onair-rail {
    margin: 0 0 16px;
    padding: 0;
  }
  .onair-rail-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 8px;
    padding: 0 2px;
  }
  .onair-rail-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
  }
  .onair-rail-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.6px;
    color: rgba(15, 17, 22, 0.40);
  }
  .onair-rail-scroll {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    overflow-y: hidden;
    padding: 2px 14px 4px 2px;  /* trailing pad-right so fade edge has room */
    scrollbar-width: none;       /* hide native scrollbar on Firefox */
    -webkit-overflow-scrolling: touch;
    scroll-snap-type: x proximity;
    /* Fade right edge to hint that more cards exist offscreen */
    mask-image: linear-gradient(to right, #000 0, #000 calc(100% - 32px), transparent 100%);
    -webkit-mask-image: linear-gradient(to right, #000 0, #000 calc(100% - 32px), transparent 100%);
  }
  .onair-rail-scroll::-webkit-scrollbar { display: none; }

  .onair-card {
    flex: 0 0 auto;
    width: 220px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 12px;
    padding: 12px 14px;
    cursor: pointer;
    scroll-snap-align: start;
    text-align: left;
    font-family: inherit;
    color: inherit;
    transition: border-color 140ms ease, transform 100ms ease, box-shadow 140ms ease;
    -webkit-tap-highlight-color: transparent;
    position: relative;
    overflow: hidden;
  }
  .onair-card:hover {
    border-color: rgba(65, 105, 225, 0.30);
    box-shadow: 0 4px 14px rgba(15, 17, 22, 0.06);
  }
  .onair-card:active { transform: scale(0.98); }

  .onair-card-head {
    display: flex;
    align-items: center;
    gap: 9px;
    margin-bottom: 8px;
  }
  .onair-card-avatar {
    width: 30px;
    height: 30px;
    border-radius: 50%;
    background: linear-gradient(135deg, #4169E1, #2E4FB8);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 12px;
    flex-shrink: 0;
    position: relative;
  }
  .onair-card.is-active .onair-card-avatar::after {
    content: '';
    position: absolute;
    bottom: -1px; right: -1px;
    width: 10px; height: 10px;
    border-radius: 50%;
    background: #16C77E;
    border: 2px solid #fff;
    box-shadow: 0 0 0 2px rgba(22, 199, 126, 0.20);
    animation: onairActiveDot 1.8s ease-in-out infinite;
  }
  @keyframes onairActiveDot {
    0%, 100% { transform: scale(0.92); opacity: 0.85; }
    50%      { transform: scale(1);    opacity: 1; }
  }
  .onair-card-id {
    flex: 1;
    min-width: 0;
  }
  .onair-card-handle {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.2;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .onair-card-city {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 600;
    letter-spacing: 0.6px;
    color: rgba(15, 17, 22, 0.50);
    margin-top: 2px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    text-transform: uppercase;
  }
  .onair-card-summary {
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 13px;
    line-height: 1.35;
    color: rgba(15, 17, 22, 0.78);
    margin: 0 0 8px;
    /* Clamp to 2 lines */
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .onair-card-meta {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }
  .onair-card-time {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.50);
    letter-spacing: 0.3px;
  }
  .onair-card-verifies {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 1px 7px;
    background: rgba(18, 161, 80, 0.10);
    color: #0a6e35;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.4px;
  }
  .onair-card-verifies svg {
    width: 10px;
    height: 10px;
  }
  .onair-card-type-stripe {
    /* A thin colored stripe on the left edge of the card, keyed by the
       Crew member's most recent report type. Subtle visual cue without
       being a giant colored block. */
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 3px;
  }
  .onair-card[data-type="storm"]    .onair-card-type-stripe { background: #C2342B; }
  .onair-card[data-type="hail"]     .onair-card-type-stripe { background: #5B6BD6; }
  .onair-card[data-type="wind"]     .onair-card-type-stripe { background: #E8A400; }
  .onair-card[data-type="rain"]     .onair-card-type-stripe { background: #2566D6; }
  .onair-card[data-type="snow"]     .onair-card-type-stripe { background: #4A6BA8; }
  .onair-card[data-type="sleet"]    .onair-card-type-stripe { background: #7088B0; }
  .onair-card[data-type="freezing"] .onair-card-type-stripe { background: #5B7E9B; }
  .onair-card[data-type="fog"]      .onair-card-type-stripe { background: #94A3B8; }
  .onair-card[data-type="sunny"]    .onair-card-type-stripe { background: #F5A524; }
  .onair-card[data-type="cloudy"]   .onair-card-type-stripe { background: #9CA3AF; }
  .onair-card[data-type="humid"]    .onair-card-type-stripe { background: #8FA8B5; }
  .onair-card[data-type="clearing"] .onair-card-type-stripe { background: #5EE3A6; }
  .onair-card[data-type="clear"]    .onair-card-type-stripe { background: #12A150; }

  /* ── Tonight's story tagline (Chunk 4c) ───────────────
     A cinematic line stating the situation. Sits between the
     on-air rail and the page headline. Fraunces italic to feel
     like a chapter title; deliberately quieter color than the
     headline so it reads as framing, not as primary content.

     On severe-weather conditions, the line takes a red tint and
     a subtle pulse on the leading bullet to convey urgency. */
  .tonight-story {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 16px;
    margin: 8px 0 14px;
    padding: 0 2px;
    flex-wrap: wrap;
  }
  .tonight-story-line {
    margin: 0;
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-weight: 500;
    font-size: 18px;
    line-height: 1.35;
    letter-spacing: -0.2px;
    color: rgba(15, 17, 22, 0.65);
    flex: 1;
    min-width: 0;
    position: relative;
    padding-left: 18px;
  }
  @media (min-width: 920px) {
    .tonight-story-line {
      font-size: 22px;
    }
  }
  .tonight-story-line::before {
    /* Leading bullet that sits in the left padding gutter — a small
       blue dot indicating "this is the live situation." Pulses gently
       when severe weather is active (controlled via the parent's
       data-severity attribute below). */
    content: '';
    position: absolute;
    left: 0;
    top: 0.55em;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #4169E1;
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.15);
  }
  /* Severe-weather tint: the whole line shifts to a warm-red ink
     and the bullet pulses. Calm conditions stay royal-blue. */
  .tonight-story[data-severity="severe"] .tonight-story-line {
    color: rgba(140, 30, 24, 0.85);
  }
  .tonight-story[data-severity="severe"] .tonight-story-line::before {
    background: #C2342B;
    box-shadow: 0 0 0 3px rgba(194, 52, 43, 0.20);
    animation: tonightStoryPulseSevere 1.8s ease-in-out infinite;
  }
  .tonight-story[data-severity="active"] .tonight-story-line::before {
    background: #E8A400;
    box-shadow: 0 0 0 3px rgba(232, 164, 0, 0.20);
  }
  @keyframes tonightStoryPulseSevere {
    0%, 100% { transform: scale(0.92); opacity: 0.85; }
    50%      { transform: scale(1.05); opacity: 1; }
  }

  /* Watcher count — hidden by default. Markup-present so backend
     wiring is one attribute flip later. Position: top-right of the
     tagline strip, mono-cased like a broadcast viewership pill. */
  .tonight-story-watchers {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 4px 10px 4px 9px;
    background: rgba(15, 17, 22, 0.04);
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.4px;
    color: rgba(15, 17, 22, 0.65);
    flex-shrink: 0;
  }
  .tonight-story-watchers-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #C2342B;
    box-shadow: 0 0 0 2px rgba(194, 52, 43, 0.25);
    animation: tonightStoryWatchersDot 1.6s ease-in-out infinite;
  }
  @keyframes tonightStoryWatchersDot {
    0%, 100% { opacity: 0.65; transform: scale(0.9); }
    50%      { opacity: 1;    transform: scale(1); }
  }

  /* ── Up Next footer (Chunk 4d) ──────────────────────────
     Sits below the map and side list — a "looking ahead" coda.
     Newspaper-sidebar styling: muted, scannable, deliberately
     different from the top-of-page broadcast band. Each item
     is a compact row with category color stripe + headline +
     area + expiry time. */
  .upnext-footer {
    margin: 28px 0 8px;
    padding: 18px 18px 16px;
    background: #FAFBFD;
    border: 1px solid rgba(15, 17, 22, 0.07);
    border-radius: 14px;
  }
  .upnext-footer-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 14px;
    padding-bottom: 12px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    flex-wrap: wrap;
  }
  .upnext-footer-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.65);
  }
  .upnext-footer-eyebrow svg {
    color: rgba(15, 17, 22, 0.45);
  }
  .upnext-footer-source {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15, 17, 22, 0.45);
    letter-spacing: 0.4px;
  }
  .upnext-footer-body {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .upnext-loading {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-style: italic;
    color: rgba(15, 17, 22, 0.40);
    padding: 8px 0;
  }

  /* Individual upcoming-event row */
  .upnext-item {
    display: flex;
    gap: 12px;
    padding: 10px 12px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 10px;
    position: relative;
    overflow: hidden;
  }
  .upnext-item-stripe {
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 3px;
  }
  .upnext-item[data-category="warning"]  .upnext-item-stripe { background: #C2342B; }
  .upnext-item[data-category="watch"]    .upnext-item-stripe { background: #F5A524; }
  .upnext-item[data-category="advisory"] .upnext-item-stripe { background: #4169E1; }
  .upnext-item-body {
    flex: 1;
    min-width: 0;
    padding-left: 4px;
  }
  .upnext-item-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 4px;
    flex-wrap: wrap;
  }
  .upnext-item-headline {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13.5px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.35;
    margin: 0;
  }
  .upnext-item-category {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    padding: 2px 8px;
    border-radius: 999px;
  }
  .upnext-item[data-category="warning"]  .upnext-item-category {
    background: rgba(194, 52, 43, 0.10);
    color: #8a2823;
  }
  .upnext-item[data-category="watch"]    .upnext-item-category {
    background: rgba(245, 165, 36, 0.12);
    color: #8a5a0a;
  }
  .upnext-item[data-category="advisory"] .upnext-item-category {
    background: rgba(65, 105, 225, 0.10);
    color: #1d3a8a;
  }
  .upnext-item-meta {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 4px;
    flex-wrap: wrap;
  }
  .upnext-item-area {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.65);
    letter-spacing: 0.3px;
  }
  .upnext-item-time {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.50);
    letter-spacing: 0.3px;
  }
  .upnext-item-time::before {
    content: '\00b7';
    margin-right: 6px;
    color: rgba(15, 17, 22, 0.35);
  }

  /* Quiet-day fallback — no active alerts. Shows the next NWS
     forecast issuance time so the section still conveys that
     someone's watching. */
  .upnext-quiet {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 10px;
  }
  .upnext-quiet-icon {
    flex-shrink: 0;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: rgba(65, 105, 225, 0.08);
    color: #4169E1;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .upnext-quiet-text {
    flex: 1;
    min-width: 0;
  }
  .upnext-quiet-headline {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13.5px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 2px;
  }
  .upnext-quiet-sub {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.50);
    letter-spacing: 0.3px;
    margin: 0;
  }
  .upnext-error {
    padding: 12px 14px;
    background: rgba(194, 52, 43, 0.04);
    border: 1px solid rgba(194, 52, 43, 0.12);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    color: rgba(140, 30, 24, 0.85);
  }

  /* Crew Feed headline — same Fraunces serif as the welcome page,
     with the signature gradient italic on the verb. Brand voice
     consistent across surfaces. */
  .crew-feed-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 26px;
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -0.5px;
    color: #0E1116;
    margin: 0 0 8px;
  }
  @media (min-width: 920px) {
    .crew-feed-headline { font-size: 32px; letter-spacing: -0.8px; }
  }
  .crew-feed-headline em {
    font-style: italic;
    font-weight: 500;
    background: linear-gradient(135deg, #4169E1 0%, #2E4FB8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: -0.6px;
  }

  .crew-welcome-card {
    position: relative;
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.04) 0%,
        rgba(255, 255, 255, 1) 60%,
        rgba(245, 165, 36, 0.025) 100%),
      #fff;
    border: 1px solid rgba(65, 105, 225, 0.18);
    border-radius: 20px;
    padding: 22px 22px 20px;
    margin-bottom: 16px;
    /* Multi-stop shadow stack matching pricing cards — inset top
       highlight + soft drop, with a Royal Blue color cast in the
       larger shadow for atmospheric coherence. */
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 1px 2px rgba(65, 105, 225, 0.06),
      0 12px 32px rgba(65, 105, 225, 0.08),
      0 4px 12px rgba(15, 17, 22, 0.03);
  }
  .crew-welcome-dismiss {
    position: absolute;
    top: 10px; right: 10px;
    width: 28px; height: 28px;
    border-radius: 50%;
    background: rgba(15, 17, 22, 0.05);
    color: rgba(15, 17, 22, 0.55);
    border: none;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background 120ms ease, color 120ms ease;
  }
  .crew-welcome-dismiss:hover {
    background: rgba(15, 17, 22, 0.10);
    color: #0E1116;
  }
  .crew-welcome-eyebrow {
    display: inline-flex; align-items: center; gap: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
    margin-bottom: 8px;
  }
  .crew-welcome-eyebrow-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    position: relative;
  }
  /* Match the dotPulse motion from the welcome page so the brand's
     visual heartbeat continues across surfaces. */
  .crew-welcome-eyebrow-dot::after {
    content: '';
    position: absolute;
    inset: -3px;
    border-radius: 999px;
    background: inherit;
    opacity: 0.35;
    animation: dotPulse 2.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    pointer-events: none;
  }
  .crew-welcome-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    line-height: 1.2;
    color: #0E1116;
    margin: 0 0 8px;
  }
  .crew-welcome-body {
    font-size: 13.5px;
    color: rgba(15, 17, 22, 0.72);
    line-height: 1.55;
    margin: 0 0 14px;
  }
  .crew-welcome-steps {
    display: flex; flex-direction: column; gap: 8px;
    margin-bottom: 14px;
  }
  .crew-welcome-step {
    display: flex; align-items: center; gap: 10px;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.78);
  }
  .crew-welcome-step-num {
    flex-shrink: 0;
    width: 22px; height: 22px;
    border-radius: 50%;
    /* Gradient inset for depth — matches the pricing CTAs */
    background: linear-gradient(180deg, #5278EA 0%, #4169E1 50%, #3257CC 100%);
    color: #fff;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    display: flex; align-items: center; justify-content: center;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.20),
      0 1px 2px rgba(65, 105, 225, 0.25);
  }
  /* CTA matches the pricing-card-cta depth treatment so the brand's
     button language is one consistent family. */
  .crew-welcome-cta {
    width: 100%;
    padding: 13px 16px;
    background: linear-gradient(180deg, #5278EA 0%, #4169E1 50%, #3257CC 100%);
    color: #fff;
    border: none;
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 600;
    font-size: 14px;
    letter-spacing: 0.1px;
    cursor: pointer;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.20),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 1px 2px rgba(65, 105, 225, 0.30),
      0 4px 12px rgba(65, 105, 225, 0.18);
    transition: transform 120ms cubic-bezier(0.2, 0.8, 0.2, 1),
                box-shadow 220ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-welcome-cta:hover {
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.25),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 2px 4px rgba(65, 105, 225, 0.36),
      0 12px 24px rgba(65, 105, 225, 0.28);
  }
  .crew-welcome-cta:active {
    transform: translateY(0);
  }
  /* Welcome card actions row — two buttons (just-looking + apply).
     The "Apply" button uses the primary gradient treatment; the
     "Just looking" is a quiet ghost variant so the apply CTA wins
     visual hierarchy without overwhelming the welcome message. */
  .crew-welcome-actions {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  @media (min-width: 480px) {
    .crew-welcome-actions { flex-direction: row; }
    .crew-welcome-actions .crew-welcome-cta { flex: 1.5; }
    .crew-welcome-actions .crew-welcome-cta-secondary { flex: 1; }
  }
  .crew-welcome-cta-secondary {
    padding: 13px 16px;
    background: transparent;
    color: rgba(15, 17, 22, 0.62);
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 600;
    font-size: 13.5px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .crew-welcome-cta-secondary:hover {
    background: rgba(15, 17, 22, 0.03);
    border-color: rgba(15, 17, 22, 0.18);
    color: #0E1116;
  }

  /* ════════════════════════════════════════════════════════════════════════
     DEV MENU — only visible when ?dev=1 is in the URL
     ════════════════════════════════════════════════════════════════════════
     Floating chip in the bottom-left of the viewport. Tap it, three role
     buttons appear (Customer / Met / Admin). Tap one to switch the
     prototype into that role's view. The menu deliberately uses the same
     calm amber palette as the other prototype-only indicators so it's
     visually parsed as "dev tool, not real feature." */
  .dev-menu {
    position: fixed;
    bottom: 18px;
    left: 18px;
    z-index: 9999;
    font-family: 'JetBrains Mono', monospace;
    /* Hidden by default — the JS only un-hides this element when ?dev=1 is in the URL.
       That means a normal user (or anyone who shares a clean URL) will never see it. */
    display: none;
  }
  .dev-menu.is-open .dev-menu-options {
    display: flex;
  }
  .dev-menu-trigger {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 14px;
    background: rgba(232, 164, 0, 0.98);
    color: #1a1308;
    border: none;
    border-radius: 999px;
    font-family: inherit;
    font-size: 11.5px;
    font-weight: 700;
    letter-spacing: 1.0px;
    text-transform: uppercase;
    cursor: pointer;
    box-shadow: 0 8px 22px rgba(232, 164, 0, 0.42);
    -webkit-tap-highlight-color: transparent;
    transition: transform 80ms ease, box-shadow 140ms ease;
    position: relative;
  }
  /* Soft pulsing halo when the menu is closed — signals "I'm interactive"
     without being aggressive. Stops pulsing once the user opens the menu
     once (handled by the .is-open class on the parent). */
  .dev-menu-trigger::after {
    content: '';
    position: absolute;
    inset: -4px;
    border-radius: 999px;
    border: 2px solid rgba(232, 164, 0, 0.40);
    opacity: 0;
    pointer-events: none;
    animation: devPulse 2.4s ease-out infinite;
  }
  .dev-menu.is-open .dev-menu-trigger::after,
  .dev-menu.has-been-opened .dev-menu-trigger::after {
    display: none;
  }
  @keyframes devPulse {
    0%   { transform: scale(0.9); opacity: 0; }
    20%  { opacity: 0.7; }
    100% { transform: scale(1.4); opacity: 0; }
  }
  .dev-menu-trigger:hover {
    box-shadow: 0 12px 28px rgba(232, 164, 0, 0.55);
  }
  .dev-menu-trigger:active {
    transform: scale(0.97);
  }
  .dev-menu-trigger-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: #1a1308;
  }
  .dev-menu-options {
    display: none;
    flex-direction: column;
    gap: 6px;
    margin-top: 8px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 14px;
    padding: 8px;
    box-shadow: 0 12px 32px rgba(15, 17, 22, 0.18);
    min-width: 180px;
  }
  .dev-menu-label {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 1.4px;
    color: rgba(15, 17, 22, 0.45);
    padding: 6px 10px 4px;
  }
  .dev-menu-divider {
    height: 1px;
    background: rgba(15, 17, 22, 0.08);
    margin: 6px 8px;
  }
  .dev-menu-option {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 9px 10px;
    background: transparent;
    border: none;
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 500;
    color: #0E1116;
    text-align: left;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background 100ms ease;
  }
  .dev-menu-option:hover {
    background: rgba(65, 105, 225, 0.06);
  }
  .dev-menu-option.is-active {
    background: rgba(65, 105, 225, 0.08);
    color: var(--rb, #4169E1);
    font-weight: 600;
  }
  .dev-menu-option-icon {
    width: 18px; height: 18px;
    flex-shrink: 0;
    color: rgba(15, 17, 22, 0.55);
  }
  .dev-menu-option.is-active .dev-menu-option-icon {
    color: var(--rb, #4169E1);
  }

  /* ════════════════════════════════════════════════════════════════════════
     SIMULATION BANNER — the persistent stripe at the top of Met & Admin views
     ════════════════════════════════════════════════════════════════════════
     Honest about what these views are. Same amber palette as the prototype
     pill so testers can't miss it. */
  .sim-banner {
    background: rgba(232, 164, 0, 0.10);
    border-bottom: 1px solid rgba(232, 164, 0, 0.30);
    padding: 10px 18px;
    text-align: center;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.0px;
    text-transform: uppercase;
    color: rgba(120, 80, 0, 0.95);
    line-height: 1.45;
  }
  .sim-banner-strong {
    color: rgba(80, 50, 0, 1);
  }

  /* ════════════════════════════════════════════════════════════════════════
     METEOROLOGIST VIEW
     ════════════════════════════════════════════════════════════════════════
     Two tabs — Review Queue (paid customer requests) and Daily Brief (a
     once-a-day territory post). Review Queue is primary because it's the
     revenue-generating workflow. */
  .met-screen {
    background: #f7f8fb;
    min-height: 100vh;
    padding-bottom: 60px;
  }
  .met-header {
    background: #fff;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    padding: 18px 20px 0;
  }
  .met-header-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
  }
  .met-header-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    margin: 0;
    color: #0E1116;
  }
  .met-header-meta {
    display: flex; align-items: center; gap: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
  }
  .met-header-meta-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: #12A150;
    box-shadow: 0 0 0 3px rgba(18, 161, 80, 0.18);
  }
  .met-header-greeting {
    font-size: 14px;
    color: rgba(15, 17, 22, 0.65);
    margin: 0 0 16px;
  }
  .met-tabs {
    display: flex;
    gap: 6px;
    border-bottom: 1px solid transparent;
    margin-bottom: -1px;
  }
  .met-tab {
    padding: 12px 16px 14px;
    background: transparent;
    border: none;
    border-bottom: 2px solid transparent;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.55);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: color 120ms ease, border-color 120ms ease;
  }
  .met-tab:hover { color: #0E1116; }
  .met-tab.is-active {
    color: var(--rb, #4169E1);
    border-bottom-color: var(--rb, #4169E1);
  }
  .met-tab-count {
    display: inline-block;
    margin-left: 6px;
    padding: 2px 7px;
    background: rgba(65, 105, 225, 0.12);
    border-radius: 999px;
    font-size: 11px;
    font-weight: 700;
  }
  .met-tab.is-active .met-tab-count {
    background: var(--rb, #4169E1);
    color: #fff;
  }
  .met-body {
    padding: 20px;
    max-width: 720px;
    margin: 0 auto;
  }
  .met-panel { display: none; }
  .met-panel.is-active { display: block; }

  /* Review queue cards */
  .met-queue-summary {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    margin-bottom: 18px;
  }
  .met-summary-card {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 12px;
    padding: 12px 14px;
  }
  .met-summary-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin-bottom: 6px;
  }
  .met-summary-value {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 24px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.0;
  }
  .met-summary-sub {
    font-size: 11px;
    color: rgba(15, 17, 22, 0.50);
    margin-top: 4px;
  }

  .met-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin: 24px 0 10px;
  }
  .met-review-card {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 14px;
    padding: 16px;
    margin-bottom: 10px;
    transition: border-color 140ms ease, box-shadow 140ms ease;
  }
  .met-review-card.is-urgent {
    border-color: rgba(232, 164, 0, 0.45);
    background: rgba(232, 164, 0, 0.04);
  }
  .met-review-card.is-claimed {
    border-color: rgba(65, 105, 225, 0.35);
    background: rgba(65, 105, 225, 0.04);
  }
  .met-review-head {
    display: flex; align-items: flex-start; justify-content: space-between;
    gap: 12px;
    margin-bottom: 10px;
  }
  .met-review-tier {
    display: inline-block;
    padding: 3px 9px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.8px;
    text-transform: uppercase;
  }
  .met-review-tier.tier-single   { background: rgba(15, 17, 22, 0.08); color: #0E1116; }
  .met-review-tier.tier-day      { background: rgba(65, 105, 225, 0.12); color: #2E4FB8; }
  .met-review-tier.tier-pro      { background: rgba(18, 161, 80, 0.14); color: #0c7a3c; }
  .met-review-time {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15, 17, 22, 0.55);
    letter-spacing: 0.5px;
  }
  .met-review-time.is-urgent { color: #C2342B; font-weight: 700; }
  .met-review-plan {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 16px;
    color: #0E1116;
    margin: 0 0 4px;
    line-height: 1.4;
  }
  .met-review-area {
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.60);
    margin: 0 0 10px;
  }
  .met-review-ai {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 6px 10px;
    background: rgba(15, 17, 22, 0.04);
    border-radius: 8px;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.70);
    margin-bottom: 12px;
  }
  .met-review-ai-status {
    font-weight: 700;
  }
  .met-review-ai-status.status-clear   { color: #0c7a3c; }
  .met-review-ai-status.status-caution { color: #8a5a0a; }
  .met-review-ai-status.status-risk    { color: #9b2823; }
  .met-review-actions {
    display: flex;
    gap: 8px;
    align-items: center;
  }
  .met-claim-btn {
    flex: 1;
    padding: 10px 14px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 13px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: transform 80ms ease, box-shadow 140ms ease;
  }
  .met-claim-btn:hover {
    box-shadow: 0 6px 18px rgba(65, 105, 225, 0.28);
  }
  .met-claim-btn:active { transform: scale(0.99); }
  .met-claim-btn.secondary {
    background: rgba(15, 17, 22, 0.06);
    color: #0E1116;
  }
  .met-claim-btn.secondary:hover {
    background: rgba(15, 17, 22, 0.10);
    box-shadow: none;
  }

  /* Brief composer (when a Met has claimed a review) */
  .met-composer {
    background: #fff;
    border: 1px solid rgba(65, 105, 225, 0.30);
    border-radius: 16px;
    padding: 20px;
    margin-bottom: 20px;
    box-shadow: 0 8px 28px rgba(65, 105, 225, 0.10);
  }
  .met-composer-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
    margin: 0 0 6px;
  }
  .met-composer-customer {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px;
    margin: 0 0 4px;
    color: #0E1116;
  }
  .met-composer-area {
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.60);
    margin: 0 0 16px;
  }
  .met-form-row {
    margin-bottom: 14px;
  }
  .met-form-label {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.0px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin-bottom: 6px;
  }
  .met-verdict-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 6px;
  }
  .met-verdict-btn {
    padding: 10px 8px;
    background: #fff;
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 120ms ease;
  }
  .met-verdict-btn.is-active.verdict-clear   { border-color: #12A150; background: rgba(18, 161, 80, 0.08); color: #0c7a3c; }
  .met-verdict-btn.is-active.verdict-caution { border-color: #E8A400; background: rgba(232, 164, 0, 0.10); color: #8a5a0a; }
  .met-verdict-btn.is-active.verdict-risk    { border-color: #C2342B; background: rgba(194, 52, 43, 0.08); color: #9b2823; }
  .met-form-input,
  .met-form-textarea {
    width: 100%;
    padding: 10px 12px;
    background: #fff;
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13.5px;
    color: #0E1116;
    -webkit-appearance: none;
    appearance: none;
    transition: border-color 120ms ease;
  }
  .met-form-textarea {
    min-height: 80px;
    resize: vertical;
    line-height: 1.5;
  }
  .met-form-input:focus,
  .met-form-textarea:focus {
    outline: none;
    border-color: var(--rb, #4169E1);
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.12);
  }
  .met-confidence-row {
    display: flex; align-items: center; gap: 12px;
  }
  .met-confidence-input {
    flex: 1;
  }
  .met-confidence-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 16px;
    font-weight: 700;
    color: var(--rb, #4169E1);
    min-width: 50px;
    text-align: right;
  }
  .met-composer-actions {
    display: flex;
    gap: 8px;
    margin-top: 18px;
    padding-top: 16px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
  }
  .met-submit-btn {
    flex: 1;
    padding: 12px 18px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: transform 80ms ease, box-shadow 140ms ease;
  }
  .met-submit-btn:hover { box-shadow: 0 8px 22px rgba(65, 105, 225, 0.28); }
  .met-submit-btn:active { transform: scale(0.99); }
  .met-cancel-btn {
    padding: 12px 16px;
    background: transparent;
    color: rgba(15, 17, 22, 0.60);
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 500;
    font-size: 13px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .met-cancel-btn:hover { background: rgba(15, 17, 22, 0.04); }

  /* Daily brief panel — secondary tab */
  .met-daily-intro {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 14px;
    padding: 18px;
    margin-bottom: 16px;
  }
  .met-daily-intro-text {
    font-size: 13.5px;
    color: rgba(15, 17, 22, 0.70);
    line-height: 1.6;
    margin: 0;
  }


  /* ════════════════════════════════════════════════════════════════════
     METEOROLOGIST WORKSPACE (v2)
     ════════════════════════════════════════════════════════════════════
     Light-mode counterpart to the Command Center — same Royal Blue
     accents, same typography, same numeric language — but on a soft
     paper-white canvas so meteorologists can read paragraphs for
     hours without eye fatigue. Density and visual hierarchy are
     turned up; horizontal space is used purposefully on desktop
     instead of constraining everything to a phone-width column.

     The "team panel" near the top is the design north star: belonging
     through transparency, not competition through ranking. Mets see
     team metrics and their own metrics, with no comparative leaderboard.
     A "send recognition" affordance invites positive feedback between
     teammates without making it mandatory or scored. */

  .met-screen.workspace {
    background: #f7f8fb;
    min-height: 100vh;
    padding-bottom: 60px;
  }

  /* ── Workspace header ───────────────────────────────────── */
  .met-ws-header {
    background: #fff;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    padding: 16px 28px 0;
  }
  .met-ws-header-top {
    display: flex; align-items: center; justify-content: space-between;
    gap: 16px; flex-wrap: wrap;
    padding-bottom: 14px;
  }
  .met-ws-brand {
    display: flex; align-items: baseline; gap: 14px;
  }
  .met-ws-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.85);
  }
  .met-ws-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: 0.2px;
    color: #0E1116;
    margin: 0;
  }
  .met-ws-meta {
    display: flex; align-items: center; gap: 20px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
  }
  .met-ws-meta-item { display: flex; align-items: center; gap: 6px; }
  .met-ws-meta-label { color: rgba(15, 17, 22, 0.40); }
  .met-ws-meta-value { color: rgba(15, 17, 22, 0.85); }
  .met-ws-shift-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #16C77E;
    box-shadow: 0 0 0 3px rgba(22, 199, 126, 0.18);
    animation: metWsShiftPulse 2.5s ease-in-out infinite;
  }
  @keyframes metWsShiftPulse {
    0%, 100% { opacity: 0.7; }
    50%      { opacity: 1; }
  }

  /* Tabs */
  .met-ws-tabs {
    display: flex; align-items: center;
    gap: 4px;
    /* A subtle track that visually groups the tabs, so they read as
       a tab-bar component rather than three text headings. */
    background: rgba(15, 17, 22, 0.04);
    padding: 4px;
    border-radius: 10px;
    width: fit-content;
  }
  .met-ws-tab {
    padding: 8px 16px;
    background: transparent;
    border: none;
    border-radius: 7px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13.5px;
    font-weight: 500;
    color: rgba(15, 17, 22, 0.60);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    position: relative;
    display: inline-flex; align-items: center; gap: 8px;
    transition: background 160ms ease, color 160ms ease, box-shadow 160ms ease;
  }
  /* Hover on inactive tabs: subtle white background appears so the
     cursor lands on something that visibly responds — confirms it's
     a button, not a label. */
  .met-ws-tab:hover {
    color: rgba(15, 17, 22, 0.90);
    background: rgba(255, 255, 255, 0.55);
  }
  /* Active tab gets a white pill with a soft shadow — clearly the
     selected one of a set of options. */
  .met-ws-tab.is-active {
    color: #0E1116;
    font-weight: 600;
    background: #fff;
    box-shadow: 0 1px 3px rgba(15, 17, 22, 0.06),
                0 1px 1px rgba(15, 17, 22, 0.04);
  }
  /* Royal Blue accent on the active tab — small underline below the
     label, signals "this is the active section" in the brand color
     without overpowering the pill. */
  .met-ws-tab.is-active::after {
    content: '';
    position: absolute;
    left: 16px; right: 16px;
    bottom: 4px;
    height: 2px;
    background: var(--rb, #4169E1);
    border-radius: 2px;
  }
  .met-ws-tab-count {
    display: inline-flex; align-items: center; justify-content: center;
    min-width: 20px; height: 20px;
    padding: 0 6px;
    border-radius: 999px;
    background: rgba(15, 17, 22, 0.08);
    color: rgba(15, 17, 22, 0.65);
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
  }
  .met-ws-tab.is-active .met-ws-tab-count {
    background: rgba(65, 105, 225, 0.14);
    color: var(--rb, #4169E1);
  }

  /* ── Met portal: map tab ───────────────────────────────────
     The map container is Leaflet's host. Height is fixed so the
     map doesn't collapse to zero during load. Border + radius
     match the rest of the ws-tile aesthetic. */
  .met-map-container {
    width: 100%;
    height: 600px;
    border-radius: 10px;
    border: 1px solid rgba(15, 17, 22, 0.10);
    background: #f3f4f6;
    overflow: hidden;
    position: relative;
  }
  /* Loading placeholder — shown until met-map.js fires the ready event.
     Positioned to overlay the container, fades out when map is ready. */
  .met-map-loading {
    width: 100%;
    height: 600px;
    border-radius: 10px;
    border: 1px solid rgba(15, 17, 22, 0.10);
    background: #f8f9fb;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    color: rgba(15, 17, 22, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    margin-bottom: -601px; /* sit on top of the container during load */
    position: relative;
    z-index: 1;
    transition: opacity 240ms ease;
  }
  .met-map-loading.is-hidden {
    opacity: 0;
    pointer-events: none;
  }
  .met-map-loading-dot {
    width: 10px; height: 10px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    animation: metMapPulse 1.4s ease-in-out infinite;
  }
  @keyframes metMapPulse {
    0%, 100% { opacity: 0.35; transform: scale(0.85); }
    50%      { opacity: 1;    transform: scale(1); }
  }

  /* ── Crew full-map screen (Chunk 6b) ──────────────────────
     Mirrors the Met map's chassis but on a Crew-friendly tone:
     same Leaflet-based geographic map, mouse zoom, pan. The
     wrapper holds the map, a loading placeholder, and a legend
     pinned to bottom-left. Pulses get an animated SVG icon
     keyed to report type; NWS warnings/watches/advisories
     render as colored polygons behind the pulses. */
  .crew-map-wrap {
    position: relative;
    width: 100%;
    margin-bottom: 16px;
  }
  .crew-map-container {
    width: 100%;
    height: 540px;
    border-radius: 14px;
    border: 1px solid rgba(15, 17, 22, 0.08);
    background: #eef1f5;
    overflow: hidden;
    position: relative;
  }
  .crew-map-loading {
    position: absolute;
    inset: 0;
    border-radius: 14px;
    background: #f8f9fb;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    color: rgba(15, 17, 22, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    z-index: 5;
    transition: opacity 240ms ease;
  }
  .crew-map-loading.is-hidden {
    opacity: 0;
    pointer-events: none;
  }
  .crew-map-loading-dot {
    width: 10px; height: 10px;
    border-radius: 50%;
    background: #4169E1;
    animation: metMapPulse 1.4s ease-in-out infinite;
  }
  /* Legend pinned bottom-left inside the wrapper. Same paper card
     style as the Live Feed legend. */
  .crew-map-legend {
    position: absolute;
    bottom: 12px;
    left: 12px;
    z-index: 6;  /* above the Leaflet canvas */
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: 10px 12px;
    background: rgba(255, 255, 255, 0.94);
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 10px;
    box-shadow: 0 4px 14px rgba(15, 17, 22, 0.08);
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 0.6px;
    color: rgba(15, 17, 22, 0.70);
    text-transform: uppercase;
  }
  .crew-map-legend-row {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .crew-map-legend-dot {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  /* Small hint text in the filter row reminding the user where the
     advisory sub-types are managed (kept on the Live Feed to avoid
     duplicating the sub-menu state). */
  .crewmap-filter-hint {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    color: rgba(15, 17, 22, 0.40);
    letter-spacing: 0.4px;
    margin-left: auto;
    flex-shrink: 0;
    align-self: center;
  }
  /* Pulses on the Leaflet map are rendered as Leaflet divIcons. The
     CSS below styles the SVG inside each divIcon so they look like
     the stylized map's pulses (animated halo + colored dot). */
  .crew-leaflet-pulse {
    position: relative;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: auto;
  }
  .crew-leaflet-pulse-halo {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    opacity: 0.25;
    animation: crewLeafletPulseHalo 2.2s ease-out infinite;
  }
  @keyframes crewLeafletPulseHalo {
    0%   { transform: scale(0.6); opacity: 0.45; }
    100% { transform: scale(1.6); opacity: 0; }
  }
  .crew-leaflet-pulse-dot {
    position: relative;
    width: 14px;
    height: 14px;
    border-radius: 50%;
    border: 2px solid #fff;
    box-shadow: 0 2px 6px rgba(15, 17, 22, 0.30);
    z-index: 2;
  }
  /* Own-pulse ring (Chunk 6b, mirroring 6a) — Crew member sees
     their own reports with a royal-blue ring around the dot. */
  .crew-leaflet-pulse.is-mine .crew-leaflet-pulse-dot {
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.55),
                0 2px 8px rgba(15, 17, 22, 0.25);
  }
  /* Type colors keyed by data-type attribute on the pulse */
  .crew-leaflet-pulse[data-type="storm"]    .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="storm"]    .crew-leaflet-pulse-halo { background: #C2342B; }
  .crew-leaflet-pulse[data-type="hail"]     .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="hail"]     .crew-leaflet-pulse-halo { background: #5B6BD6; }
  .crew-leaflet-pulse[data-type="wind"]     .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="wind"]     .crew-leaflet-pulse-halo { background: #E8A400; }
  .crew-leaflet-pulse[data-type="rain"]     .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="rain"]     .crew-leaflet-pulse-halo { background: #2566D6; }
  .crew-leaflet-pulse[data-type="snow"]     .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="snow"]     .crew-leaflet-pulse-halo { background: #4A6BA8; }
  .crew-leaflet-pulse[data-type="sleet"]    .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="sleet"]    .crew-leaflet-pulse-halo { background: #7088B0; }
  .crew-leaflet-pulse[data-type="freezing"] .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="freezing"] .crew-leaflet-pulse-halo { background: #5B7E9B; }
  .crew-leaflet-pulse[data-type="fog"]      .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="fog"]      .crew-leaflet-pulse-halo { background: #94A3B8; }
  .crew-leaflet-pulse[data-type="sunny"]    .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="sunny"]    .crew-leaflet-pulse-halo { background: #F5A524; }
  .crew-leaflet-pulse[data-type="cloudy"]   .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="cloudy"]   .crew-leaflet-pulse-halo { background: #9CA3AF; }
  .crew-leaflet-pulse[data-type="humid"]    .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="humid"]    .crew-leaflet-pulse-halo { background: #8FA8B5; }
  .crew-leaflet-pulse[data-type="clearing"] .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="clearing"] .crew-leaflet-pulse-halo { background: #5EE3A6; }
  .crew-leaflet-pulse[data-type="clear"]    .crew-leaflet-pulse-dot,
  .crew-leaflet-pulse[data-type="clear"]    .crew-leaflet-pulse-halo { background: #12A150; }

  /* ── Met-post variant on the Leaflet crew map (Chunk 6c-1) ──
     Met posts use a square dot (vs Crew's round dot) so the two
     are visually distinguishable at a glance. Color is solid
     royal-blue regardless of weather type (Met posts aren't
     weather observations — they're meteorologist commentary).
     No animated halo: this isn't a "live observation," it's a
     deliberate broadcast. */
  .crew-leaflet-pulse.is-met-post {
    width: 28px;
    height: 28px;
  }
  .crew-leaflet-pulse.is-met-post .crew-leaflet-pulse-dot {
    background: #4169E1 !important;
    width: 16px;
    height: 16px;
    border-radius: 4px;
    border: 2px solid #fff;
    box-shadow: 0 0 0 2px rgba(65, 105, 225, 0.30),
                0 2px 8px rgba(15, 17, 22, 0.30);
  }
  .crew-leaflet-pulse.is-met-post .crew-leaflet-pulse-halo {
    display: none;
  }

  /* ── Met-post composer in the brief panel (Chunk 6c-1) ──
     Sits below the brief summary textarea. Compact card so it
     doesn't dominate the brief form. */
  .met-post-composer {
    background: rgba(65, 105, 225, 0.04);
    border: 1px solid rgba(65, 105, 225, 0.18);
    border-radius: 10px;
    padding: 14px 16px;
  }
  .ws-composer-section-label-detail {
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 500;
    text-transform: none;
    letter-spacing: normal;
    font-size: 11.5px;
    color: rgba(232, 235, 240, 0.55);
    margin-left: 6px;
  }
  .ws-textarea-short {
    min-height: 56px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    line-height: 1.5;
  }
  .met-post-composer-actions {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 10px;
    flex-wrap: wrap;
  }
  .met-post-send-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 14px;
    background: #4169E1;
    border: none;
    border-radius: 999px;
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    transition: background 140ms ease, transform 80ms ease,
                opacity 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .met-post-send-btn:hover:not(:disabled) {
    background: #2E4FB8;
  }
  .met-post-send-btn:active:not(:disabled) { transform: scale(0.97); }
  .met-post-send-btn:disabled {
    background: rgba(65, 105, 225, 0.35);
    cursor: not-allowed;
  }
  .met-post-composer-hint {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(232, 235, 240, 0.45);
    letter-spacing: 0.4px;
  }
  .met-post-composer-hint.is-success {
    color: #5EE3A6;
  }

  /* ── Met-post pin-drop modal (Chunk 6c-1) ──────────────
     Full-screen overlay with a centered card holding the
     message preview, a small Leaflet map for pin-drop, and
     the action buttons. */
  .met-post-modal {
    position: fixed;
    inset: 0;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }
  .met-post-modal[hidden] { display: none; }
  .met-post-modal-scrim {
    position: absolute;
    inset: 0;
    background: rgba(15, 17, 22, 0.55);
    backdrop-filter: blur(2px);
  }
  .met-post-modal-card {
    position: relative;
    width: 100%;
    max-width: 540px;
    max-height: calc(100vh - 40px);
    background: #fff;
    border-radius: 16px;
    box-shadow: 0 24px 60px rgba(15, 17, 22, 0.30);
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }
  .met-post-modal-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 20px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.08);
    flex-shrink: 0;
  }
  .met-post-modal-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px;
    font-weight: 600;
    color: #0E1116;
    margin: 0;
  }
  .met-post-modal-close {
    width: 32px;
    height: 32px;
    background: transparent;
    border: none;
    border-radius: 50%;
    color: rgba(15, 17, 22, 0.55);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .met-post-modal-close:hover {
    background: rgba(15, 17, 22, 0.06);
    color: #0E1116;
  }
  .met-post-modal-body {
    padding: 18px 20px;
    overflow-y: auto;
    flex: 1;
  }
  .met-post-modal-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin: 0 0 8px;
  }
  .met-post-modal-preview {
    margin: 0 0 18px;
    padding: 12px 14px;
    background: rgba(65, 105, 225, 0.06);
    border-left: 3px solid #4169E1;
    border-radius: 0 8px 8px 0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    line-height: 1.5;
    color: #0E1116;
    white-space: pre-wrap;
  }
  .met-post-modal-help {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.60);
    line-height: 1.5;
    margin: 0 0 10px;
  }
  /* Phase 7 — Message Again modal */
  .met-message-context {
    background: rgba(65, 105, 225, 0.04);
    border: 1px solid rgba(65, 105, 225, 0.14);
    border-radius: 8px;
    padding: 12px 14px;
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin: 0 0 4px;
  }
  .met-message-context-row {
    display: grid;
    grid-template-columns: 88px minmax(0, 1fr);
    gap: 10px;
    align-items: baseline;
  }
  .met-message-context-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.45);
  }
  .met-message-context-value {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: #0E1116;
    word-break: break-word;
  }
  .met-message-textarea {
    width: 100%;
    min-height: 110px;
    padding: 12px 14px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.18);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    line-height: 1.5;
    color: #0E1116;
    resize: vertical;
    box-sizing: border-box;
    transition: border-color 120ms ease, box-shadow 120ms ease;
  }
  .met-message-textarea:focus {
    outline: none;
    border-color: #4169E1;
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.14);
  }
  .met-message-textarea::placeholder {
    color: rgba(15, 17, 22, 0.35);
  }
  .met-message-count {
    margin: 6px 0 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.45);
    text-align: right;
  }
  /* Phase 8 — Mission Deploy modal */
  .mission-deploy-card {
    max-width: 640px;
  }
  .mission-deploy-summary {
    display: grid;
    grid-template-columns: 44px minmax(0, 1fr);
    gap: 12px;
    align-items: center;
    padding: 12px 14px;
    background: rgba(65, 105, 225, 0.04);
    border: 1px solid rgba(65, 105, 225, 0.14);
    border-radius: 8px;
    margin-bottom: 16px;
  }
  .mission-deploy-summary-icon {
    width: 44px;
    height: 44px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 14px;
    background: linear-gradient(135deg, #4169E1, #2E4FCC);
    letter-spacing: 0.5px;
  }
  .mission-deploy-summary-name {
    margin: 0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: #0E1116;
  }
  .mission-deploy-summary-meta {
    margin: 3px 0 0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.60);
    line-height: 1.4;
  }
  .mission-deploy-warning {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 10px 12px;
    background: rgba(194, 52, 43, 0.06);
    border: 1px solid rgba(194, 52, 43, 0.22);
    color: #C2342B;
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 500;
    margin-bottom: 16px;
  }
  .mission-deploy-warning svg { flex-shrink: 0; }
  .mission-deploy-audience {
    margin-top: 14px;
    padding: 10px 12px;
    background: rgba(15, 17, 22, 0.03);
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
  }
  .mission-deploy-audience-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
  }
  .mission-deploy-audience-value {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: #0E1116;
    font-weight: 500;
  }
  /* Mission template cards — replaces the inline-style cards in HTML */
  .mission-template-card {
    display: grid;
    grid-template-columns: 36px minmax(0, 1fr) auto auto;
    gap: 12px;
    align-items: center;
    padding: 12px;
    background: rgba(15, 17, 22, 0.03);
    border-radius: 8px;
    border: 1px solid rgba(15, 17, 22, 0.08);
    transition: background-color 120ms ease, border-color 120ms ease;
    cursor: pointer;
  }
  .mission-template-card:hover {
    background: rgba(15, 17, 22, 0.05);
    border-color: rgba(15, 17, 22, 0.15);
  }
  .mission-template-card.is-severe {
    background: rgba(194, 52, 43, 0.06);
    border-color: rgba(194, 52, 43, 0.25);
  }
  .mission-template-card.is-severe:hover {
    background: rgba(194, 52, 43, 0.10);
    border-color: rgba(194, 52, 43, 0.35);
  }
  .mission-template-icon {
    width: 32px;
    height: 32px;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 600;
    font-size: 13px;
  }
  .mission-template-info { min-width: 0; }
  .mission-template-name {
    margin: 0;
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
  }
  .mission-template-meta {
    margin: 2px 0 0;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
  }
  .mission-template-card.is-severe .mission-template-meta {
    color: rgba(155, 40, 35, 0.85);
  }
  .mission-template-count {
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
    white-space: nowrap;
  }
  .mission-template-card.is-severe .mission-template-count {
    color: rgba(255, 180, 180, 0.60);
  }
  .mission-template-deploy {
    font-size: 12px;
    padding: 6px 12px;
    background: rgba(65, 105, 225, 0.15);
    border: 1px solid rgba(65, 105, 225, 0.35);
    color: #6B8EFF;
    border-radius: 6px;
    cursor: pointer;
    font-weight: 500;
    transition: background-color 120ms ease, transform 80ms ease;
  }
  .mission-template-deploy:hover {
    background: rgba(65, 105, 225, 0.25);
  }
  .mission-template-deploy:active { transform: scale(0.97); }
  .mission-template-card.is-severe .mission-template-deploy {
    background: rgba(255, 92, 92, 0.15);
    border-color: rgba(255, 92, 92, 0.35);
    color: #FF9E9E;
  }
  .mission-template-card.is-severe .mission-template-deploy:hover {
    background: rgba(255, 92, 92, 0.25);
  }
  .met-post-modal-map-wrap {
    position: relative;
    width: 100%;
    height: 320px;
    border-radius: 10px;
    overflow: hidden;
    border: 1px solid rgba(15, 17, 22, 0.10);
  }
  .met-post-modal-map {
    width: 100%;
    height: 100%;
  }
  .met-post-modal-map-loading {
    position: absolute;
    inset: 0;
    background: #f8f9fb;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    color: rgba(15, 17, 22, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    transition: opacity 240ms ease;
  }
  .met-post-modal-map-loading.is-hidden {
    opacity: 0;
    pointer-events: none;
  }
  .met-post-modal-pin-status {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
    margin: 10px 0 0;
    letter-spacing: 0.3px;
  }
  .met-post-modal-pin-status.is-pinned {
    color: #0a6e35;
  }
  .met-post-modal-actions {
    display: flex;
    justify-content: flex-end;
    gap: 10px;
    padding: 14px 20px;
    border-top: 1px solid rgba(15, 17, 22, 0.08);
    background: #fafbfd;
    flex-shrink: 0;
  }
  .met-post-modal-cancel {
    padding: 9px 16px;
    background: transparent;
    border: 1px solid rgba(15, 17, 22, 0.15);
    border-radius: 999px;
    color: rgba(15, 17, 22, 0.75);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
  }
  .met-post-modal-cancel:hover {
    border-color: rgba(15, 17, 22, 0.30);
    color: #0E1116;
  }
  .met-post-modal-send {
    padding: 9px 18px;
    background: #4169E1;
    border: none;
    border-radius: 999px;
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    transition: background 140ms ease, opacity 140ms ease;
  }
  .met-post-modal-send:hover:not(:disabled) { background: #2E4FB8; }
  .met-post-modal-send:disabled {
    background: rgba(65, 105, 225, 0.35);
    cursor: not-allowed;
  }
  /* Delete button — subtle danger framing for destructive actions
     inside the threshold-alert modal. Always sits between Cancel
     and Save so a user can confirm intent. */
  .met-post-modal-delete {
    padding: 9px 16px;
    background: transparent;
    border: 1px solid rgba(194, 52, 43, 0.30);
    border-radius: 999px;
    color: #C2342B;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    margin-right: auto;
  }
  .met-post-modal-delete:hover {
    background: rgba(194, 52, 43, 0.06);
    border-color: rgba(194, 52, 43, 0.50);
  }

  /* ── Subscriber portal modal form controls (Phase 10 B1) ──
     Used by the brief preferences and threshold alert modals.
     Compact, two-column-friendly, native HTML5 inputs styled to
     match the rest of the modal surfaces. */
  .prefs-section {
    margin-bottom: 18px;
    padding-bottom: 18px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
  }
  .prefs-section:last-of-type {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }
  .prefs-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin-bottom: 10px;
  }
  .prefs-section-detail {
    font-weight: 500;
    text-transform: none;
    letter-spacing: 0;
    opacity: 0.75;
    font-size: 10px;
  }
  .prefs-toggle-row {
    display: flex;
    align-items: center;
    gap: 10px;
    cursor: pointer;
    user-select: none;
    margin-bottom: 8px;
  }
  .prefs-toggle-input {
    width: 16px;
    height: 16px;
    cursor: pointer;
    accent-color: #4169E1;
  }
  .prefs-toggle-label {
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
  }
  .prefs-time-row {
    display: flex;
    gap: 12px;
    margin-top: 4px;
  }
  .prefs-time-field {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .prefs-time-label {
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
    font-weight: 500;
  }
  .prefs-time-input {
    padding: 8px 10px;
    border: 1px solid rgba(15, 17, 22, 0.15);
    border-radius: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    background: #fff;
    color: #0E1116;
  }
  .prefs-time-input:focus {
    outline: none;
    border-color: #4169E1;
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.15);
  }
  .prefs-channels-row {
    display: flex;
    gap: 18px;
    flex-wrap: wrap;
  }
  .prefs-channel-check {
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    user-select: none;
    font-size: 13px;
    color: #0E1116;
  }
  .prefs-channel-check input {
    width: 16px;
    height: 16px;
    cursor: pointer;
    accent-color: #4169E1;
  }
  .prefs-error {
    color: #C2342B;
    font-size: 12px;
    margin: 12px 0 0;
    font-weight: 500;
  }
  /* Threshold form row — metric, comparator, value, units in one line */
  .threshold-form-row {
    display: flex;
    gap: 6px;
    align-items: stretch;
    flex-wrap: wrap;
  }
  .threshold-select {
    padding: 8px 10px;
    border: 1px solid rgba(15, 17, 22, 0.15);
    border-radius: 6px;
    background: #fff;
    color: #0E1116;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 500;
    cursor: pointer;
    min-width: 0;
  }
  .threshold-select:focus {
    outline: none;
    border-color: #4169E1;
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.15);
  }
  .threshold-units-select {
    flex-shrink: 0;
    width: auto;
  }
  .threshold-value-input {
    padding: 8px 10px;
    border: 1px solid rgba(15, 17, 22, 0.15);
    border-radius: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    background: #fff;
    color: #0E1116;
    width: 80px;
    flex-shrink: 0;
  }
  .threshold-value-input:focus {
    outline: none;
    border-color: #4169E1;
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.15);
  }

  /* ── Saved location modal styles (Phase 10 B2) ──
     Address input + lookup button on one row; map below.
     The map needs explicit pixel height because Leaflet relies on
     a sized container to render tiles. */
  .saved-location-input {
    width: 100%;
    padding: 10px 12px;
    border: 1px solid rgba(15, 17, 22, 0.15);
    border-radius: 6px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    color: #0E1116;
    background: #fff;
    box-sizing: border-box;
  }
  .saved-location-input:focus {
    outline: none;
    border-color: #4169E1;
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.15);
  }
  .saved-location-lookup-row {
    display: flex;
    gap: 8px;
    align-items: stretch;
  }
  .saved-location-lookup-row .saved-location-input {
    flex: 1;
  }
  .saved-location-lookup-btn {
    padding: 10px 18px;
    background: #0E1116;
    border: none;
    border-radius: 6px;
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    white-space: nowrap;
    transition: background 140ms ease;
  }
  .saved-location-lookup-btn:hover:not(:disabled) { background: #2a2d33; }
  .saved-location-lookup-btn:disabled {
    background: rgba(15, 17, 22, 0.35);
    cursor: not-allowed;
  }
  .saved-location-help {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    margin: 8px 0 0;
    line-height: 1.4;
  }
  .saved-location-map-wrap {
    position: relative;
    margin: 18px 0 8px;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid rgba(15, 17, 22, 0.08);
  }
  .saved-location-map {
    width: 100%;
    height: 260px;
    background: #f4f6fa;
  }
  .saved-location-map-loading {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f4f6fa;
    color: rgba(15, 17, 22, 0.55);
    font-size: 13px;
    z-index: 5;
    pointer-events: none;
  }
  .saved-location-map-loading.is-hidden { display: none; }
  .saved-location-coords-readout {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    margin: 4px 0 0;
  }

  /* ── Schedule toggle in post modal (Chunk 6c-2) ────────
     Segmented two-button control. Active state filled royal-blue. */
  .met-post-schedule-toggle {
    display: inline-flex;
    align-items: stretch;
    background: rgba(15, 17, 22, 0.04);
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 999px;
    padding: 3px;
    gap: 2px;
    margin-bottom: 14px;
  }
  .met-post-schedule-toggle-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 7px 14px;
    background: transparent;
    border: none;
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.60);
    cursor: pointer;
    transition: background 140ms ease, color 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .met-post-schedule-toggle-btn:hover:not(.is-active) {
    color: #0E1116;
  }
  .met-post-schedule-toggle-btn.is-active {
    background: #4169E1;
    color: #fff;
  }
  .met-post-schedule-fields {
    margin-bottom: 14px;
  }
  .met-post-schedule-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }
  .met-post-schedule-field {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .met-post-schedule-field-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
  }
  .met-post-schedule-input {
    width: 100%;
    padding: 8px 10px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.12);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: #0E1116;
  }
  .met-post-schedule-input:focus {
    outline: none;
    border-color: #4169E1;
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.15);
  }
  .met-post-schedule-zone {
    margin: 8px 0 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.55);
    letter-spacing: 0.3px;
  }
  .met-post-schedule-error {
    margin: 6px 0 0;
    padding: 6px 10px;
    background: rgba(194, 52, 43, 0.06);
    border: 1px solid rgba(194, 52, 43, 0.20);
    border-radius: 6px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    color: #8a2823;
  }

  /* ── Scheduled-queue panel below the composer (6c-2) ──── */
  .met-post-queue {
    margin-top: 14px;
    padding-top: 12px;
    border-top: 1px solid rgba(65, 105, 225, 0.15);
  }
  .met-post-queue-label {
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(232, 235, 240, 0.65);
    margin: 0 0 8px;
  }
  .met-post-queue-count {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 18px;
    height: 16px;
    padding: 0 6px;
    background: rgba(245, 165, 36, 0.20);
    color: #F5A524;
    border-radius: 999px;
    font-size: 9.5px;
    letter-spacing: 0.4px;
  }
  .met-post-queue-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .met-post-queue-item {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 10px 12px;
    background: rgba(232, 235, 240, 0.04);
    border: 1px solid rgba(232, 235, 240, 0.10);
    border-radius: 8px;
  }
  .met-post-queue-icon {
    flex-shrink: 0;
    width: 26px;
    height: 26px;
    border-radius: 50%;
    background: rgba(245, 165, 36, 0.18);
    color: #F5A524;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 1px;
  }
  .met-post-queue-body {
    flex: 1;
    min-width: 0;
  }
  .met-post-queue-text {
    margin: 0 0 4px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    line-height: 1.4;
    color: rgba(232, 235, 240, 0.92);
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .met-post-queue-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(232, 235, 240, 0.55);
    letter-spacing: 0.4px;
  }
  .met-post-queue-meta-overdue {
    color: #F5A524;
  }
  .met-post-queue-actions {
    display: flex;
    gap: 4px;
    flex-shrink: 0;
  }
  .met-post-queue-action-btn {
    width: 28px;
    height: 28px;
    background: transparent;
    border: 1px solid rgba(232, 235, 240, 0.12);
    border-radius: 6px;
    color: rgba(232, 235, 240, 0.70);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 140ms ease, color 140ms ease, border-color 140ms ease;
  }
  .met-post-queue-action-btn:hover {
    border-color: rgba(232, 235, 240, 0.30);
    color: #fff;
  }
  .met-post-queue-action-btn.is-cancel:hover {
    background: rgba(194, 52, 43, 0.20);
    border-color: rgba(194, 52, 43, 0.50);
    color: #fff;
  }

  /* ── Cancel-confirm dialog (6c-2) ────────────────────── */
  .met-queue-cancel-modal {
    position: fixed;
    inset: 0;
    z-index: 1100;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }
  .met-queue-cancel-modal[hidden] { display: none; }
  .met-queue-cancel-scrim {
    position: absolute;
    inset: 0;
    background: rgba(15, 17, 22, 0.55);
  }
  .met-queue-cancel-card {
    position: relative;
    width: 100%;
    max-width: 420px;
    background: #fff;
    border-radius: 14px;
    padding: 20px 22px;
    box-shadow: 0 24px 60px rgba(15, 17, 22, 0.30);
  }
  .met-queue-cancel-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 17px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 10px;
  }
  .met-queue-cancel-preview {
    margin: 0 0 8px;
    padding: 10px 12px;
    background: rgba(65, 105, 225, 0.05);
    border-left: 3px solid #4169E1;
    border-radius: 0 8px 8px 0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    line-height: 1.45;
    color: rgba(15, 17, 22, 0.85);
  }
  .met-queue-cancel-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.55);
    letter-spacing: 0.3px;
    margin: 0 0 14px;
  }
  .met-queue-cancel-actions {
    display: flex;
    justify-content: flex-end;
    gap: 10px;
  }
  .met-queue-cancel-keep {
    padding: 8px 14px;
    background: transparent;
    border: 1px solid rgba(15, 17, 22, 0.15);
    border-radius: 999px;
    color: rgba(15, 17, 22, 0.75);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
  }
  .met-queue-cancel-keep:hover {
    border-color: rgba(15, 17, 22, 0.30);
    color: #0E1116;
  }
  .met-queue-cancel-confirm {
    padding: 8px 14px;
    background: #C2342B;
    border: none;
    border-radius: 999px;
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
  }
  .met-queue-cancel-confirm:hover { background: #8a2823; }

  /* ── Met-post marked indicator in the Live Feed side list (6c-1) ──
     The story-list item shape stays the same; we add a royal-blue
     left stripe and a "MET" eyebrow chip so it's identifiable at a
     glance without looking like a different surface. */
  .story-list .story.is-met-post {
    border-left: 3px solid #4169E1;
    padding-left: 12px;
  }
  .met-post-eyebrow-chip {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 1px 7px;
    background: rgba(65, 105, 225, 0.12);
    color: #1d3a8a;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    margin-right: 6px;
    vertical-align: 1px;
  }

  /* ── Met-post variant on the stylized Live Feed map (6c-1) ──
     Square pulse, royal-blue, no halo animation — same visual
     vocabulary as the Leaflet map's Met-post variant for
     consistency between the two map surfaces. */
  .feed-pulse.is-met-post .feed-pulse-dot {
    background: #4169E1 !important;
    width: 14px;
    height: 14px;
    border-radius: 3px;
    border: 2px solid #fff;
    box-shadow: 0 0 0 2px rgba(65, 105, 225, 0.25),
                0 4px 10px rgba(15, 17, 22, 0.25);
  }
  .feed-pulse.is-met-post .feed-pulse-halo,
  .feed-pulse.is-met-post .feed-pulse-ring {
    display: none;
  }

  /* Map footer — legend and status hints below the map */
  .met-map-footer {
    display: flex;
    align-items: center;
    gap: 18px;
    padding: 10px 4px 0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.65);
    flex-wrap: wrap;
  }
  .met-map-footer-item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
  .met-map-footer-dot {
    width: 10px; height: 10px;
    border-radius: 2px;
  }
  .met-map-footer-spacer { flex: 1; }
  .met-map-footer-hint {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.40);
  }

  /* ── NWS alerts bar — second row of map footer ──
     Three category toggles with live counts, plus last-updated
     timestamp and a manual refresh button on the right. */
  .met-map-alerts-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    margin-top: 10px;
    padding: 10px 12px;
    background: #f8f9fb;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 10px;
    flex-wrap: wrap;
  }
  .met-map-alerts-bar-left {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-wrap: wrap;
  }
  .met-map-alerts-bar-right {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .met-map-alerts-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin-right: 4px;
  }
  /* Each toggle is a clickable label wrapping a hidden checkbox.
     When checked, dot + label are full color; when unchecked, they're
     dimmed. Click anywhere on the pill to toggle. */
  .met-map-toggle {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 5px 10px;
    border-radius: 999px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    cursor: pointer;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    color: #0E1116;
    transition: background 140ms ease, border-color 140ms ease, opacity 140ms ease;
    user-select: none;
  }
  .met-map-toggle input { display: none; }
  .met-map-toggle:hover { border-color: rgba(15, 17, 22, 0.25); }
  .met-map-toggle.is-off {
    opacity: 0.50;
    background: rgba(15, 17, 22, 0.03);
  }
  .met-map-toggle.is-off .met-map-toggle-dot {
    background: rgba(15, 17, 22, 0.25) !important;
  }
  .met-map-toggle-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  .met-map-toggle-label { font-weight: 500; }
  .met-map-toggle-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    color: rgba(15, 17, 22, 0.55);
    background: rgba(15, 17, 22, 0.06);
    padding: 1px 6px;
    border-radius: 999px;
    min-width: 18px;
    text-align: center;
  }
  .met-map-alerts-updated {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 600;
    letter-spacing: 0.8px;
    color: rgba(15, 17, 22, 0.50);
  }
  .met-map-refresh-btn {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 5px 10px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 7px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 500;
    color: #0E1116;
    cursor: pointer;
    transition: background 140ms ease, border-color 140ms ease;
  }
  .met-map-refresh-btn:hover {
    background: #f3f4f6;
    border-color: rgba(15, 17, 22, 0.20);
  }
  .met-map-refresh-btn.is-refreshing svg {
    animation: metMapRefreshSpin 700ms linear;
  }
  @keyframes metMapRefreshSpin {
    from { transform: rotate(0deg); }
    to   { transform: rotate(360deg); }
  }

  /* Leaflet overrides — match the brand. Leaflet's default attribution
     is fine; we just style its container to fit the page. */
  .met-map-container .leaflet-control-attribution {
    background: rgba(255, 255, 255, 0.85);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 10px;
    color: rgba(15, 17, 22, 0.65);
  }
  .met-map-container .leaflet-control-zoom a {
    color: rgba(15, 17, 22, 0.85);
    border-color: rgba(15, 17, 22, 0.10);
  }


  /* ── Workspace body grid ──────────────────────────────── */
  .met-ws-body {
    padding: 22px 28px 80px;
    max-width: 1480px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 14px;
  }
  @media (max-width: 900px) {
    .met-ws-body { padding: 16px; }
  }

  /* Workspace tile */
  .ws-tile {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 16px;
    padding: 16px 18px;
    transition: border-color 200ms ease, box-shadow 200ms ease;
  }
  .ws-tile:hover {
    border-color: rgba(65, 105, 225, 0.14);
    box-shadow: 0 1px 3px rgba(15, 17, 22, 0.04);
  }
  .ws-tile-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.75);
    margin: 0 0 10px;
    display: flex; align-items: center; gap: 8px;
  }
  .ws-tile-label-detail {
    color: rgba(15, 17, 22, 0.40);
    font-weight: 400;
    letter-spacing: 0.8px;
  }
  .ws-span-3  { grid-column: span 3; }
  .ws-span-4  { grid-column: span 4; }
  .ws-span-6  { grid-column: span 6; }
  .ws-span-8  { grid-column: span 8; }
  .ws-span-12 { grid-column: span 12; }
  @media (max-width: 1100px) {
    .ws-span-3, .ws-span-4 { grid-column: span 6; }
    .ws-span-8 { grid-column: span 12; }
  }
  @media (max-width: 640px) {
    .ws-span-3, .ws-span-4, .ws-span-6, .ws-span-8 { grid-column: span 12; }
  }

  /* Big numerics */
  .ws-metric-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 28px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1;
    letter-spacing: -0.4px;
    margin: 4px 0 6px;
  }
  .ws-metric-value-unit {
    font-size: 14px;
    color: rgba(15, 17, 22, 0.45);
    margin-left: 2px;
  }
  .ws-metric-context {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
  }

  /* ── Team panel ───────────────────────────────────────── */
  .ws-team-panel {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 18px;
    padding: 4px 0;
  }
  @media (max-width: 700px) {
    .ws-team-panel { grid-template-columns: repeat(2, 1fr); }
  }
  .ws-team-stat {
    border-right: 1px solid rgba(15, 17, 22, 0.06);
    padding-right: 18px;
  }
  .ws-team-stat:last-child { border-right: none; padding-right: 0; }
  @media (max-width: 700px) {
    .ws-team-stat { border-right: none; padding-right: 0; }
  }
  .ws-team-stat-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin-bottom: 6px;
  }
  .ws-team-stat-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 26px;
    font-weight: 600;
    color: #0E1116;
    letter-spacing: -0.3px;
    line-height: 1;
  }
  .ws-team-stat-value-unit {
    font-size: 13px;
    color: rgba(15, 17, 22, 0.45);
    margin-left: 2px;
  }
  .ws-team-stat-context {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    margin-top: 6px;
    line-height: 1.4;
  }

  /* My contribution */
  .ws-mine-row {
    display: flex; flex-direction: column; gap: 0;
  }
  .ws-mine-stat {
    display: flex; align-items: baseline; justify-content: space-between;
    padding: 8px 0;
    border-bottom: 1px solid rgba(15, 17, 22, 0.05);
  }
  .ws-mine-stat:last-child { border-bottom: none; }
  .ws-mine-label {
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.65);
  }
  .ws-mine-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
  }
  .ws-mine-value-em {
    color: var(--rb, #4169E1);
  }

  /* Recognition affordance */
  .ws-recognize {
    display: flex; flex-direction: column; gap: 10px;
  }
  .ws-recognize-suggestion {
    display: flex; align-items: center; gap: 12px;
    padding: 10px 12px;
    background: rgba(65, 105, 225, 0.04);
    border: 1px solid rgba(65, 105, 225, 0.10);
    border-radius: 12px;
  }
  .ws-recognize-suggestion-text {
    flex: 1;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.78);
    line-height: 1.45;
  }
  .ws-recognize-suggestion-text strong { color: #0E1116; font-weight: 600; }
  .ws-recognize-btn {
    flex-shrink: 0;
    padding: 7px 12px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: transform 140ms ease, box-shadow 220ms ease;
    display: inline-flex; align-items: center; gap: 5px;
  }
  .ws-recognize-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 14px rgba(65, 105, 225, 0.22);
  }
  .ws-recognize-btn:active { transform: translateY(0); }
  .ws-recognize-btn.is-sent {
    background: #16C77E;
    pointer-events: none;
  }

  /* ── Workspace queue ─────────────────────────────────── */
  .ws-queue-list {
    display: flex; flex-direction: column;
  }
  .ws-queue-row {
    display: grid;
    grid-template-columns: 90px minmax(0, 1fr) 130px 110px 180px;
    align-items: center;
    gap: 12px;
    padding: 12px 6px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.05);
    transition: background-color 140ms ease;
  }
  .ws-queue-row:last-child { border-bottom: none; }
  .ws-queue-row:hover { background: rgba(65, 105, 225, 0.025); }
  @media (max-width: 900px) {
    .ws-queue-row { grid-template-columns: 1fr; gap: 6px; }
  }
  .ws-queue-tier {
    justify-self: start;
    padding: 4px 9px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
  }
  .ws-queue-tier.tier-single { background: rgba(15, 17, 22, 0.06);  color: rgba(15, 17, 22, 0.70); }
  .ws-queue-tier.tier-day    { background: rgba(65, 105, 225, 0.12); color: #2E4FB8; }
  .ws-queue-tier.tier-pro    { background: rgba(18, 161, 80, 0.14);  color: #0c7a3c; }
  /* Real (paid) requests get a subtle LIVE marker so the Met can
     distinguish them from demo mock data at a glance. */
  .ws-queue-real-badge {
    display: inline-block;
    margin-left: 6px;
    padding: 1px 5px;
    background: rgba(194, 52, 43, 0.12);
    color: #C2342B;
    border-radius: 999px;
    font-size: 9px;
    font-weight: 800;
    letter-spacing: 0.8px;
    vertical-align: middle;
  }
  .ws-queue-row.is-real {
    background: linear-gradient(90deg, rgba(194, 52, 43, 0.03), transparent 30%);
  }
  .ws-queue-plan { min-width: 0; overflow: hidden; }
  .ws-queue-plan-headline {
    font-size: 14px;
    font-weight: 500;
    color: #0E1116;
    margin: 0 0 2px;
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .ws-queue-plan-meta {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.50);
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .ws-queue-ai {
    display: flex; align-items: center; gap: 6px;
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
  }
  .ws-queue-ai-pill {
    padding: 2px 7px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-transform: uppercase;
  }
  .ws-queue-ai-pill.verdict-clear   { background: rgba(18, 161, 80, 0.14); color: #0c7a3c; }
  .ws-queue-ai-pill.verdict-caution { background: rgba(232, 164, 0, 0.14); color: #8a5a0a; }
  .ws-queue-ai-pill.verdict-risk    { background: rgba(194, 52, 43, 0.10); color: #9b2823; }
  .ws-queue-age {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.45);
    text-align: right;
  }
  .ws-queue-actions {
    display: flex; gap: 6px; justify-content: flex-end;
  }
  .ws-queue-claim-btn {
    padding: 7px 14px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: transform 140ms ease, box-shadow 220ms ease;
  }
  .ws-queue-claim-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 5px 12px rgba(65, 105, 225, 0.22);
  }
  .ws-queue-skip-btn {
    padding: 7px 12px;
    background: transparent;
    color: rgba(15, 17, 22, 0.55);
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 8px;
    font-size: 12.5px;
    font-weight: 500;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .ws-queue-skip-btn:hover { background: rgba(15, 17, 22, 0.04); }

  /* ── Composer ─────────────────────────────────────────── */
  .ws-composer-shell {
    grid-column: span 12;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 18px;
    overflow: hidden;
    margin-bottom: 4px;
  }
  .ws-composer-strip {
    display: flex; align-items: center; justify-content: space-between;
    gap: 16px; flex-wrap: wrap;
    padding: 12px 18px;
    background: linear-gradient(90deg,
      rgba(65, 105, 225, 0.06),
      rgba(65, 105, 225, 0.02));
    border-bottom: 1px solid rgba(65, 105, 225, 0.10);
  }
  .ws-composer-strip-left {
    display: flex; align-items: center; gap: 14px;
  }
  .ws-composer-strip-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
  }
  .ws-composer-strip-customer {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 17px;
    font-weight: 600;
    color: #0E1116;
  }
  .ws-composer-timer {
    display: flex; align-items: center; gap: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    background: rgba(15, 17, 22, 0.04);
    padding: 6px 12px;
    border-radius: 999px;
  }
  .ws-composer-timer-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.16);
    animation: wsTimerPulse 2s ease-in-out infinite;
  }
  @keyframes wsTimerPulse {
    0%, 100% { opacity: 0.7; }
    50%      { opacity: 1; }
  }
  /* Helper line under the brief composer's status strip — reassures the
     Met that nothing is mandatory. Quiet, italic, mono-tone — reads as a
     standing note, not a UI element that needs interacting with. */
  .ws-composer-hint {
    margin: 0;
    padding: 10px 20px 0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-style: italic;
    line-height: 1.5;
    color: rgba(232, 235, 240, 0.55);
  }
  .ws-composer-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
  }
  @media (max-width: 1000px) {
    .ws-composer-grid { grid-template-columns: 1fr; }
  }
  .ws-composer-write {
    padding: 18px 20px 20px;
    border-right: 1px solid rgba(15, 17, 22, 0.06);
  }
  @media (max-width: 1000px) {
    .ws-composer-write { border-right: none; border-bottom: 1px solid rgba(15, 17, 22, 0.06); }
  }
  .ws-composer-section { margin-bottom: 16px; }
  .ws-composer-section:last-child { margin-bottom: 0; }
  .ws-composer-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin: 0 0 8px;
  }
  .ws-verdict-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
  }
  /* Brief Type selector — five pill buttons in a row, picks the
     forecast type that determines delivery rules. The Met picks
     explicitly at the top of the composer; the selection cascades
     to the delivery block at the bottom which updates its "Will
     deliver at..." line based on the type. Severe Alert is styled
     distinctly (red border + amber-on-active) so a Met choosing
     it knows they're picking the bypass-everything path. */
  .ws-brief-type-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 8px;
  }
  @media (min-width: 1100px) {
    .ws-brief-type-grid { grid-template-columns: repeat(5, 1fr); }
  }
  .ws-brief-type-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 8px 10px;
    border-radius: 10px;
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    background: transparent;
    color: rgba(15, 17, 22, 0.70);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
    text-align: center;
    line-height: 1.2;
  }
  .ws-brief-type-btn svg {
    flex-shrink: 0;
    opacity: 0.7;
  }
  .ws-brief-type-btn:hover {
    border-color: rgba(65, 105, 225, 0.30);
    color: #0E1116;
    transform: translateY(-1px);
  }
  .ws-brief-type-btn.is-active {
    background: rgba(65, 105, 225, 0.08);
    border-color: var(--rb, #4169E1);
    color: var(--rb, #4169E1);
  }
  .ws-brief-type-btn.is-active svg {
    opacity: 1;
  }
  /* Severe Alert button — red-tinted to signal "this is the
     bypass-everything path." When active, it switches to a more
     prominent red treatment so the Met can't miss what they've
     selected. */
  .ws-brief-type-severe {
    border-color: rgba(194, 52, 43, 0.18);
    color: rgba(155, 40, 35, 0.85);
  }
  .ws-brief-type-severe:hover {
    border-color: rgba(194, 52, 43, 0.40);
    color: #9b2823;
  }
  .ws-brief-type-severe.is-active {
    background: rgba(194, 52, 43, 0.08);
    border-color: #C2342B;
    color: #9b2823;
  }

  .ws-verdict-btn {
    padding: 10px 12px;
    border-radius: 12px;
    border: 1.5px solid;
    background: transparent;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
    text-align: center;
  }
  .ws-verdict-btn.verdict-clear   { border-color: rgba(18, 161, 80, 0.30); color: #0c7a3c; }
  .ws-verdict-btn.verdict-caution { border-color: rgba(232, 164, 0, 0.30); color: #8a5a0a; }
  .ws-verdict-btn.verdict-risk    { border-color: rgba(194, 52, 43, 0.30); color: #9b2823; }
  .ws-verdict-btn:hover { transform: translateY(-1px); }
  .ws-verdict-btn.verdict-clear.is-active   { background: rgba(18, 161, 80, 0.10); border-color: #16C77E; }
  .ws-verdict-btn.verdict-caution.is-active { background: rgba(232, 164, 0, 0.10); border-color: #F5A524; }
  .ws-verdict-btn.verdict-risk.is-active    { background: rgba(194, 52, 43, 0.10); border-color: #C2342B; }
  .ws-confidence-row {
    display: flex; align-items: center; gap: 12px;
  }
  .ws-confidence-input {
    flex: 1;
    accent-color: var(--rb, #4169E1);
  }
  .ws-confidence-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    font-weight: 700;
    color: var(--rb, #4169E1);
    min-width: 42px;
    text-align: right;
  }
  .ws-textarea, .ws-input {
    width: 100%;
    padding: 10px 12px;
    background: #f7f8fb;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    line-height: 1.5;
    color: #0E1116;
    box-sizing: border-box;
    resize: vertical;
    transition: border-color 160ms ease, background-color 160ms ease;
  }
  .ws-textarea { min-height: 80px; }
  .ws-textarea:focus, .ws-input:focus {
    outline: none;
    background: #fff;
    border-color: rgba(65, 105, 225, 0.40);
  }
  .ws-composer-actions {
    display: flex; gap: 8px; justify-content: flex-end;
    margin-top: 18px;
    padding-top: 14px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
  }
  /* Delivery block — addresses the Met question
     "if I work at 4 AM, does that send at 4 AM?" Always-visible
     "Will deliver at..." indicator plus explicit override controls.
     The smart default respects customer quiet hours; the override
     is for emergencies and time-critical responses (paid reviews,
     severe weather). */
  .ws-delivery-block {
    margin-top: 18px;
    padding-top: 14px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
  }
  .ws-delivery-card {
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.04) 0%,
        rgba(255, 255, 255, 1) 65%),
      #fff;
    border: 1px solid rgba(65, 105, 225, 0.15);
    border-radius: 12px;
    padding: 14px 16px;
  }
  .ws-delivery-summary {
    display: flex; align-items: flex-start; gap: 12px;
    padding-bottom: 12px;
    margin-bottom: 12px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
  }
  .ws-delivery-icon {
    flex-shrink: 0;
    width: 28px; height: 28px;
    border-radius: 8px;
    background: rgba(65, 105, 225, 0.10);
    color: var(--rb, #4169E1);
    display: flex; align-items: center; justify-content: center;
    margin-top: 1px;
  }
  .ws-delivery-summary-text { flex: 1; min-width: 0; }
  .ws-delivery-when {
    font-size: 14px;
    color: #0E1116;
    margin-bottom: 3px;
    line-height: 1.4;
  }
  .ws-delivery-when strong {
    font-weight: 700;
  }
  .ws-delivery-relative {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(65, 105, 225, 0.85);
    margin-left: 4px;
    padding: 2px 7px;
    background: rgba(65, 105, 225, 0.08);
    border-radius: 999px;
    letter-spacing: 0.2px;
  }
  .ws-delivery-why {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.4;
  }
  /* Override toggle — sits at the bottom of the delivery card,
     quietly. Click expands the override panel. The Met composes,
     glances at "will deliver at X," hits send, walks away.
     Override is for the rare case. */
  .ws-delivery-override-toggle {
    display: inline-flex; align-items: center; gap: 6px;
    margin-top: 8px;
    padding: 4px 0;
    background: transparent;
    border: none;
    color: rgba(15, 17, 22, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 500;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: color 140ms ease;
  }
  .ws-delivery-override-toggle:hover {
    color: var(--rb, #4169E1);
  }
  .ws-delivery-override-toggle svg {
    transition: transform 200ms ease;
  }
  .ws-delivery-override-toggle[aria-expanded="true"] svg {
    transform: rotate(180deg);
  }
  .ws-delivery-override-toggle[aria-expanded="true"] {
    color: var(--rb, #4169E1);
  }
  /* Expandable override panel — three explicit options when the
     Met wants to break the default rule. */
  .ws-delivery-override-panel {
    margin-top: 8px;
    padding-top: 12px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
    display: flex; flex-direction: column;
    gap: 4px;
  }
  .ws-delivery-override-panel[hidden] { display: none; }
  .ws-delivery-override-option {
    display: grid;
    grid-template-columns: 18px 1fr;
    column-gap: 10px;
    align-items: start;
    padding: 8px 10px;
    border-radius: 8px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background-color 140ms ease;
  }
  .ws-delivery-override-option:hover {
    background: rgba(65, 105, 225, 0.04);
  }
  .ws-delivery-override-option input[type="radio"] {
    grid-row: 1 / 3;
    margin-top: 2px;
    accent-color: var(--rb, #4169E1);
  }
  .ws-delivery-override-label {
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.4;
  }
  .ws-delivery-override-detail {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.4;
    grid-column: 2;
  }
  /* The bypass-quiet-hours option gets a danger treatment so the
     Met can't accidentally pick it. Red border on the radio dot
     when this option is hovered or selected. */
  .ws-delivery-override-danger:hover {
    background: rgba(194, 52, 43, 0.04);
  }
  .ws-delivery-override-danger input[type="radio"] {
    accent-color: #C2342B;
  }
  .ws-delivery-override-danger .ws-delivery-override-detail {
    color: rgba(155, 40, 35, 0.75);
    font-weight: 500;
  }
  .ws-composer-cancel {
    padding: 10px 16px;
    background: transparent;
    color: rgba(15, 17, 22, 0.60);
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 10px;
    font-size: 13px; font-weight: 500;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .ws-composer-cancel:hover { background: rgba(15, 17, 22, 0.04); }
  .ws-composer-submit {
    padding: 10px 18px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 10px;
    font-size: 13px; font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: transform 140ms ease, box-shadow 220ms ease;
  }
  .ws-composer-submit:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(65, 105, 225, 0.25);
  }

  /* Right column: context panel */
  .ws-composer-context {
    padding: 18px 20px 20px;
    background: #fafbff;
  }
  .ws-context-block { margin-bottom: 18px; }
  .ws-context-block:last-child { margin-bottom: 0; }
  .ws-context-block-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.75);
    margin: 0 0 8px;
  }
  .ws-context-customer-plan {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 15px;
    font-style: italic;
    color: #0E1116;
    line-height: 1.5;
    margin: 0;
    padding: 12px 14px;
    background: #fff;
    border-left: 3px solid var(--rb, #4169E1);
    border-radius: 0 10px 10px 0;
  }
  .ws-context-customer-meta {
    margin-top: 8px;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
  }
  .ws-context-ai {
    background: #fff;
    border: 1px solid rgba(65, 105, 225, 0.15);
    border-radius: 12px;
    padding: 12px 14px;
  }
  .ws-context-ai-head {
    display: flex; align-items: center; justify-content: space-between;
    gap: 10px;
    margin-bottom: 8px;
  }
  .ws-context-ai-verdict {
    display: inline-flex; align-items: center; gap: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    padding: 3px 8px;
    border-radius: 999px;
  }
  .ws-context-ai-verdict.verdict-clear   { background: rgba(18, 161, 80, 0.12);  color: #0c7a3c; }
  .ws-context-ai-verdict.verdict-caution { background: rgba(232, 164, 0, 0.14);  color: #8a5a0a; }
  .ws-context-ai-verdict.verdict-risk    { background: rgba(194, 52, 43, 0.10);  color: #9b2823; }
  .ws-context-ai-confidence {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
  }
  .ws-context-ai-paragraph {
    font-size: 13px;
    line-height: 1.55;
    color: rgba(15, 17, 22, 0.85);
    margin: 0;
  }
  .ws-context-weather-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 8px;
  }
  .ws-context-weather-cell {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 10px;
    padding: 10px 12px;
  }
  .ws-context-weather-cell-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.45);
    margin-bottom: 3px;
  }
  .ws-context-weather-cell-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 15px;
    font-weight: 600;
    color: #0E1116;
  }
  .ws-context-reasons {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .ws-context-reasons li {
    display: flex; align-items: flex-start; gap: 8px;
    padding: 6px 0;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.78);
    line-height: 1.45;
    border-bottom: 1px dashed rgba(15, 17, 22, 0.06);
  }
  .ws-context-reasons li:last-child { border-bottom: none; }
  .ws-context-reasons li::before {
    content: '';
    flex-shrink: 0;
    width: 5px; height: 5px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    margin-top: 8px;
  }

  /* ════════════════════════════════════════════════════════════════════
     METEOROLOGIST WORKSPACE v3 — boxy command-center treatment.
     Layered on top of v2 styles. Where v2 had a sprawling team panel
     and a single-column Daily Brief, v3 breaks the workspace into
     a proper grid of focused tiles, adds depth (subtle outer shadow,
     gradient backgrounds, inner highlight at top edge), and treats
     the Daily Brief and History tabs as full-width grid panels too.
     ════════════════════════════════════════════════════════════════════ */

  /* Quiet luxury depth pass — applies to all .ws-tile elements */
  .ws-tile {
    background:
      linear-gradient(180deg, #ffffff 0%, #fafbff 100%);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.95) inset,
      0 1px 2px rgba(15, 17, 22, 0.04),
      0 4px 12px rgba(15, 17, 22, 0.02);
    position: relative;
    overflow: visible;
  }
  .ws-tile::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: linear-gradient(180deg,
      rgba(65, 105, 225, 0.025) 0%,
      transparent 28%);
    pointer-events: none;
  }
  .ws-tile:hover {
    border-color: rgba(65, 105, 225, 0.18);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.95) inset,
      0 2px 4px rgba(15, 17, 22, 0.05),
      0 8px 24px rgba(65, 105, 225, 0.06);
  }

  /* Personal vitals tile — small, dense, dollars prominent */
  .ws-vital-tile {
    padding: 14px 16px;
  }
  .ws-vital-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.75);
    margin: 0 0 6px;
  }
  .ws-vital-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 28px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1;
    letter-spacing: -0.5px;
    margin-bottom: 4px;
  }
  .ws-vital-value-unit {
    font-size: 14px;
    color: rgba(15, 17, 22, 0.45);
    margin-left: 2px;
  }
  .ws-vital-value.is-money {
    color: #0c7a3c;
  }
  .ws-vital-context {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.50);
    line-height: 1.3;
    margin: 0;
  }

  .ws-span-2 { grid-column: span 2; }
  @media (max-width: 1100px) {
    .ws-span-2 { grid-column: span 4; }
  }
  @media (max-width: 640px) {
    .ws-span-2 { grid-column: span 12; }
  }

  /* Section eyebrow — tiny header above a row of related tiles */
  .ws-section-eyebrow {
    grid-column: span 12;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.40);
    margin: 8px 4px 0;
    display: flex; align-items: center; gap: 12px;
  }
  .ws-section-eyebrow::after {
    content: '';
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, rgba(15, 17, 22, 0.08), transparent);
  }
  .ws-section-eyebrow-detail {
    color: rgba(15, 17, 22, 0.30);
    font-weight: 400;
    letter-spacing: 0.8px;
  }

  /* Recognition column (sits beside queue) */
  .ws-recognize-col {
    display: flex; flex-direction: column; gap: 10px;
  }

  /* Shift dot states — auto-detected, no manual toggle */
  .met-ws-shift-dot.is-idle {
    background: #F5A524;
    box-shadow: 0 0 0 3px rgba(245, 165, 36, 0.18);
    animation: none;
  }
  .met-ws-shift-dot.is-off {
    background: rgba(15, 17, 22, 0.30);
    box-shadow: 0 0 0 3px rgba(15, 17, 22, 0.10);
    animation: none;
  }

  /* History table — wide format */
  .ws-history-table {
    display: flex; flex-direction: column;
  }
  .ws-history-header,
  .ws-history-row {
    display: grid;
    grid-template-columns: 100px minmax(0, 1.3fr) minmax(0, 1.4fr) 90px 84px 84px 110px;
    gap: 12px;
    align-items: center;
    padding: 10px 6px;
  }
  .ws-history-header {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.45);
    border-bottom: 1px solid rgba(15, 17, 22, 0.08);
    padding-bottom: 8px;
  }
  .ws-history-row {
    border-bottom: 1px solid rgba(15, 17, 22, 0.05);
    transition: background-color 120ms ease;
    font-size: 13px;
  }
  .ws-history-row:last-child { border-bottom: none; }
  .ws-history-row:hover { background: rgba(65, 105, 225, 0.025); }
  @media (max-width: 900px) {
    .ws-history-header { display: none; }
    .ws-history-row {
      grid-template-columns: 1fr;
      gap: 4px;
      padding: 12px 6px;
    }
  }
  .ws-history-when { color: rgba(15, 17, 22, 0.55); font-family: 'JetBrains Mono', monospace; font-size: 11.5px; }
  .ws-history-customer { color: #0E1116; font-weight: 500; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .ws-history-plan { color: rgba(15, 17, 22, 0.65); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

  /* Phase 7 — Message again button */
  .ws-history-action { display: flex; justify-content: center; }
  .ws-history-message-btn {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 5px 10px;
    background: rgba(65, 105, 225, 0.08);
    border: 1px solid rgba(65, 105, 225, 0.22);
    color: #4169E1;
    border-radius: 6px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 11.5px;
    font-weight: 500;
    cursor: pointer;
    transition: background-color 120ms ease, border-color 120ms ease;
    white-space: nowrap;
  }
  .ws-history-message-btn:hover {
    background: rgba(65, 105, 225, 0.14);
    border-color: rgba(65, 105, 225, 0.40);
  }
  .ws-history-message-btn:active { transform: scale(0.97); }
  .ws-history-verdict {
    justify-self: start;
    padding: 2px 8px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-transform: uppercase;
  }
  .ws-history-verdict.verdict-clear   { background: rgba(18, 161, 80, 0.14); color: #0c7a3c; }
  .ws-history-verdict.verdict-caution { background: rgba(232, 164, 0, 0.14); color: #8a5a0a; }
  .ws-history-verdict.verdict-risk    { background: rgba(194, 52, 43, 0.10); color: #9b2823; }
  .ws-history-helpful { font-family: 'JetBrains Mono', monospace; font-size: 12px; }
  .ws-history-helpful.is-up   { color: #0c7a3c; }
  .ws-history-helpful.is-down { color: #9b2823; }
  .ws-history-helpful.is-pending { color: rgba(15, 17, 22, 0.40); }
  .ws-history-amount {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    font-weight: 600;
    color: #0c7a3c;
    text-align: right;
  }

  /* Forecasting tools placeholder */
  .ws-tools-placeholder {
    text-align: center;
    padding: 22px 16px;
    background: linear-gradient(180deg,
      rgba(65, 105, 225, 0.025),
      rgba(65, 105, 225, 0.005));
    border-radius: 12px;
  }
  .ws-tools-icon {
    width: 36px; height: 36px;
    color: rgba(65, 105, 225, 0.55);
    margin-bottom: 10px;
  }
  .ws-tools-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 16px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 6px;
  }
  .ws-tools-detail {
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.5;
    max-width: 480px;
    margin: 0 auto;
  }
  .ws-tools-list {
    display: flex; flex-wrap: wrap;
    justify-content: center;
    gap: 6px;
    margin-top: 10px;
  }
  .ws-tools-chip {
    padding: 4px 10px;
    background: #fff;
    border: 1px solid rgba(65, 105, 225, 0.20);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.85);
  }


  /* ════════════════════════════════════════════════════════════════════
     METEOROLOGIST WORKSPACE v3 — boxy command-center treatment,
     quiet luxury depth, severe alert mode, county multi-select.
     ════════════════════════════════════════════════════════════════════
     Layered on top of the v2 styles. Where v2 used flat tiles, v3
     adds subtle depth (outer shadow + gradient backgrounds + inner
     highlight at top edge) so tiles feel like they're floating just
     above the surface — Tesla command-center territory. Same color
     palette and typography; just more dimensional. */

  /* ── Tile depth pass ──────────────────────────────────── */
  .ws-tile {
    background:
      linear-gradient(180deg, #ffffff 0%, #fafbff 100%);
    border: 1px solid rgba(15, 17, 22, 0.05);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.95) inset,
      0 1px 2px rgba(15, 17, 22, 0.04),
      0 4px 12px rgba(15, 17, 22, 0.02);
    position: relative;
    overflow: visible;
  }
  .ws-tile::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: linear-gradient(180deg,
      rgba(65, 105, 225, 0.025) 0%,
      transparent 25%);
    pointer-events: none;
  }
  .ws-tile:hover {
    border-color: rgba(65, 105, 225, 0.18);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.95) inset,
      0 2px 4px rgba(15, 17, 22, 0.05),
      0 8px 24px rgba(65, 105, 225, 0.06);
  }

  /* ── Personal vitals strip (top row of small tiles) ─────
     Mirrors the Command Center's vitals strip: 4-6 small tiles
     across the top showing the operator's personal stats. Earnings
     are deliberately prominent — Mets are professionals being paid,
     and the dollars should be visible. */
  .ws-vital-tile {
    padding: 14px 16px;
  }
  .ws-vital-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.75);
    margin: 0 0 6px;
  }
  .ws-vital-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 28px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1;
    letter-spacing: -0.5px;
    margin-bottom: 4px;
  }
  .ws-vital-value-unit {
    font-size: 14px;
    color: rgba(15, 17, 22, 0.45);
    margin-left: 2px;
  }
  .ws-vital-value.is-money {
    color: #0c7a3c;
  }
  .ws-vital-context {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.50);
    line-height: 1.3;
  }

  .ws-span-2 { grid-column: span 2; }
  @media (max-width: 1100px) {
    .ws-span-2 { grid-column: span 4; }
  }
  @media (max-width: 640px) {
    .ws-span-2 { grid-column: span 12; }
  }

  /* ── Section divider — tiny eyebrow above a row of tiles ─ */
  .ws-section-eyebrow {
    grid-column: span 12;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.40);
    margin: 8px 4px 0;
    display: flex; align-items: center; gap: 12px;
  }
  .ws-section-eyebrow::after {
    content: '';
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, rgba(15, 17, 22, 0.08), transparent);
  }
  .ws-section-eyebrow-detail {
    color: rgba(15, 17, 22, 0.30);
    font-weight: 400;
    letter-spacing: 0.8px;
  }

  /* ── Recognition column (sits beside queue) ──────────── */
  .ws-recognize-col {
    display: flex; flex-direction: column; gap: 10px;
  }

  /* ── Shift status (idle vs on-shift) ─────────────────── */
  .met-ws-shift-dot.is-idle {
    background: #F5A524;
    box-shadow: 0 0 0 3px rgba(245, 165, 36, 0.18);
    animation: none;
  }
  .met-ws-shift-dot.is-off {
    background: rgba(15, 17, 22, 0.30);
    box-shadow: 0 0 0 3px rgba(15, 17, 22, 0.10);
    animation: none;
  }

  /* ── Forecasting tools placeholder ───────────────────── */
  .ws-tools-placeholder {
    text-align: center;
    padding: 22px 16px;
    background: linear-gradient(180deg,
      rgba(65, 105, 225, 0.025),
      rgba(65, 105, 225, 0.005));
    border-radius: 12px;
  }
  .ws-tools-icon {
    width: 36px; height: 36px;
    color: rgba(65, 105, 225, 0.55);
    margin-bottom: 10px;
  }
  .ws-tools-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 16px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 6px;
  }
  .ws-tools-detail {
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.5;
    max-width: 480px;
    margin: 0 auto;
  }
  .ws-tools-list {
    display: flex; flex-wrap: wrap;
    justify-content: center;
    gap: 6px;
    margin-top: 10px;
  }
  .ws-tools-chip {
    padding: 4px 10px;
    background: #fff;
    border: 1px solid rgba(65, 105, 225, 0.20);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.85);
  }

  /* ════════════════════════════════════════════════════════════════════
     DAILY BRIEF v3 — wide layout, county multi-select, severe alert mode
     ════════════════════════════════════════════════════════════════════ */

  /* The Daily Brief composer mode-switcher (Routine vs Severe Alert) */
  .ws-brief-mode-row {
    display: flex; gap: 8px;
    margin-bottom: 16px;
  }
  .ws-brief-mode-btn {
    flex: 1;
    display: flex; align-items: flex-start; gap: 10px;
    padding: 12px 14px;
    background: #fff;
    border: 1.5px solid rgba(15, 17, 22, 0.08);
    border-radius: 12px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: border-color 160ms ease, background-color 160ms ease;
    text-align: left;
  }
  .ws-brief-mode-btn:hover {
    border-color: rgba(65, 105, 225, 0.30);
  }
  .ws-brief-mode-btn.is-active {
    border-color: var(--rb, #4169E1);
    background: rgba(65, 105, 225, 0.04);
  }
  .ws-brief-mode-btn.is-active.mode-severe {
    border-color: #C2342B;
    background: rgba(194, 52, 43, 0.04);
  }
  .ws-brief-mode-icon {
    flex-shrink: 0;
    width: 32px; height: 32px;
    border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    background: rgba(65, 105, 225, 0.10);
    color: var(--rb, #4169E1);
  }
  .ws-brief-mode-btn.mode-severe .ws-brief-mode-icon {
    background: rgba(194, 52, 43, 0.10);
    color: #C2342B;
  }
  .ws-brief-mode-info { flex: 1; }
  .ws-brief-mode-title {
    font-size: 13.5px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 2px;
  }
  .ws-brief-mode-desc {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.4;
    margin: 0;
  }

  /* Severe alert mode visual treatment when active */
  .ws-composer-shell.is-severe-mode .ws-composer-strip {
    background: linear-gradient(90deg,
      rgba(194, 52, 43, 0.08),
      rgba(194, 52, 43, 0.02));
    border-bottom-color: rgba(194, 52, 43, 0.18);
  }
  .ws-composer-shell.is-severe-mode .ws-composer-strip-eyebrow {
    color: #C2342B;
  }

  /* County selector */
  .ws-county-shell {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 12px;
    overflow: hidden;
  }
  .ws-county-controls {
    display: flex; align-items: center; gap: 10px;
    padding: 10px 12px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    background: #fafbff;
  }
  .ws-county-state {
    flex-shrink: 0;
    padding: 7px 10px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: #0E1116;
    cursor: pointer;
    min-width: 180px;
  }
  .ws-county-search {
    flex: 1;
    padding: 7px 12px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: #0E1116;
  }
  .ws-county-search:focus, .ws-county-state:focus {
    outline: none;
    border-color: rgba(65, 105, 225, 0.40);
  }
  .ws-county-select-all-btn {
    flex-shrink: 0;
    padding: 7px 10px;
    background: rgba(65, 105, 225, 0.06);
    border: 1px solid rgba(65, 105, 225, 0.18);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 600;
    color: var(--rb, #4169E1);
    cursor: pointer;
  }
  .ws-county-select-all-btn:hover {
    background: rgba(65, 105, 225, 0.10);
  }
  .ws-county-list {
    max-height: 220px;
    overflow-y: auto;
    padding: 8px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 4px;
  }
  @media (max-width: 700px) {
    .ws-county-list { grid-template-columns: repeat(2, 1fr); }
  }
  .ws-county-item {
    display: flex; align-items: center; gap: 7px;
    padding: 6px 8px;
    border-radius: 6px;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.78);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background-color 120ms ease;
  }
  .ws-county-item:hover {
    background: rgba(65, 105, 225, 0.05);
  }
  .ws-county-item.is-selected {
    background: rgba(65, 105, 225, 0.08);
    color: #0E1116;
    font-weight: 500;
  }
  .ws-county-item input[type="checkbox"] {
    accent-color: var(--rb, #4169E1);
    cursor: pointer;
  }
  .ws-county-empty {
    grid-column: 1 / -1;
    padding: 20px;
    text-align: center;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.45);
  }

  /* Selected counties chips bar */
  .ws-county-chips {
    display: flex; flex-wrap: wrap; gap: 5px;
    padding: 10px 12px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
    background: #fafbff;
    min-height: 38px;
  }
  .ws-county-chip {
    display: inline-flex; align-items: center; gap: 5px;
    padding: 3px 6px 3px 10px;
    background: rgba(65, 105, 225, 0.10);
    color: var(--rb, #4169E1);
    border-radius: 999px;
    font-size: 11.5px;
    font-weight: 600;
  }
  .ws-county-chip-x {
    width: 18px; height: 18px;
    display: flex; align-items: center; justify-content: center;
    background: transparent;
    border: none;
    color: var(--rb, #4169E1);
    cursor: pointer;
    border-radius: 50%;
    -webkit-tap-highlight-color: transparent;
    font-size: 14px;
    line-height: 1;
  }
  .ws-county-chip-x:hover {
    background: rgba(65, 105, 225, 0.18);
  }
  .ws-county-chips-empty {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.45);
    font-style: italic;
  }

  /* Audience preview */
  .ws-audience-preview {
    display: flex; align-items: center; gap: 12px;
    padding: 12px 14px;
    background: linear-gradient(90deg,
      rgba(65, 105, 225, 0.06),
      rgba(65, 105, 225, 0.02));
    border: 1px solid rgba(65, 105, 225, 0.15);
    border-radius: 12px;
    margin-top: 10px;
  }
  .ws-audience-icon {
    flex-shrink: 0;
    width: 32px; height: 32px;
    background: rgba(65, 105, 225, 0.12);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: var(--rb, #4169E1);
  }
  .ws-audience-text {
    flex: 1;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.78);
    line-height: 1.4;
  }
  .ws-audience-text strong {
    color: var(--rb, #4169E1);
    font-weight: 700;
  }
  .ws-composer-shell.is-severe-mode .ws-audience-preview {
    background: linear-gradient(90deg,
      rgba(194, 52, 43, 0.08),
      rgba(194, 52, 43, 0.02));
    border-color: rgba(194, 52, 43, 0.18);
  }
  .ws-composer-shell.is-severe-mode .ws-audience-icon {
    background: rgba(194, 52, 43, 0.12);
    color: #C2342B;
  }
  .ws-composer-shell.is-severe-mode .ws-audience-text strong {
    color: #C2342B;
  }

  /* Future map placeholder */
  .ws-map-placeholder {
    background: linear-gradient(135deg, #fafbff, #f0f4ff);
    border: 1px dashed rgba(65, 105, 225, 0.25);
    border-radius: 10px;
    padding: 14px;
    display: flex; align-items: center; gap: 12px;
    margin-top: 8px;
  }
  .ws-map-placeholder-icon {
    flex-shrink: 0;
    color: rgba(65, 105, 225, 0.60);
  }
  .ws-map-placeholder-text {
    flex: 1;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.65);
    line-height: 1.45;
  }
  .ws-map-placeholder-text strong {
    color: #0E1116;
    font-weight: 600;
  }

  /* ── Polygon-drawing map (Chunk D) ─────────────────────────────
     Reusable polygon selector used in the Daily Brief tab and the
     Mission tab to pick a target area. */
  .ws-polygon-map-section {
    margin-top: 12px;
    padding: 14px;
    background: rgba(15, 17, 22, 0.02);
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 10px;
  }
  .ws-polygon-map-label {
    margin: 0 0 4px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(232, 235, 240, 0.65);
  }
  .ws-polygon-map-label-detail {
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 500;
    text-transform: none;
    letter-spacing: normal;
    font-size: 11.5px;
    color: rgba(232, 235, 240, 0.50);
    margin-left: 6px;
  }
  .ws-polygon-map-help {
    margin: 0 0 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    line-height: 1.5;
    color: rgba(232, 235, 240, 0.55);
  }
  .ws-polygon-map-wrap {
    position: relative;
    width: 100%;
    height: 360px;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid rgba(15, 17, 22, 0.10);
  }
  .ws-polygon-map { width: 100%; height: 100%; }
  .ws-polygon-map-loading {
    position: absolute;
    inset: 0;
    background: #f8f9fb;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    color: rgba(15, 17, 22, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    transition: opacity 240ms ease;
  }
  .ws-polygon-map-loading.is-hidden {
    opacity: 0;
    pointer-events: none;
  }
  .ws-polygon-map-status {
    margin-top: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(232, 235, 240, 0.55);
    letter-spacing: 0.3px;
  }
  .ws-polygon-map-status.is-active { color: #5EE3A6; }

  /* Severe alert publish button — distinct color + shake-y nudge */
  .ws-composer-submit.is-severe {
    background: linear-gradient(135deg, #C2342B, #FF5E68);
    box-shadow: 0 4px 12px rgba(194, 52, 43, 0.30);
  }
  .ws-composer-submit.is-severe:hover {
    box-shadow: 0 6px 16px rgba(194, 52, 43, 0.40);
  }

  /* ════════════════════════════════════════════════════════════════════
     HISTORY v3 — wide table layout
     ════════════════════════════════════════════════════════════════════ */
  .ws-history-table {
    display: flex; flex-direction: column;
  }
  .ws-history-header,
  .ws-history-row {
    display: grid;
    grid-template-columns: 100px minmax(0, 1.3fr) minmax(0, 1.4fr) 90px 84px 84px 110px;
    gap: 12px;
    align-items: center;
    padding: 10px 6px;
  }
  .ws-history-header {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.45);
    border-bottom: 1px solid rgba(15, 17, 22, 0.08);
    padding-bottom: 8px;
  }
  .ws-history-row {
    border-bottom: 1px solid rgba(15, 17, 22, 0.05);
    transition: background-color 120ms ease;
    font-size: 13px;
  }
  .ws-history-row:last-child { border-bottom: none; }
  .ws-history-row:hover { background: rgba(65, 105, 225, 0.025); }
  @media (max-width: 900px) {
    .ws-history-header { display: none; }
    .ws-history-row {
      grid-template-columns: 1fr;
      gap: 4px;
      padding: 12px 6px;
    }
  }
  .ws-history-when { color: rgba(15, 17, 22, 0.55); font-family: 'JetBrains Mono', monospace; font-size: 11.5px; }
  .ws-history-customer { color: #0E1116; font-weight: 500; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .ws-history-plan { color: rgba(15, 17, 22, 0.65); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .ws-history-verdict {
    justify-self: start;
    padding: 2px 8px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-transform: uppercase;
  }
  .ws-history-verdict.verdict-clear   { background: rgba(18, 161, 80, 0.14); color: #0c7a3c; }
  .ws-history-verdict.verdict-caution { background: rgba(232, 164, 0, 0.14); color: #8a5a0a; }
  .ws-history-verdict.verdict-risk    { background: rgba(194, 52, 43, 0.10); color: #9b2823; }
  .ws-history-helpful { font-family: 'JetBrains Mono', monospace; font-size: 12px; }
  .ws-history-helpful.is-up   { color: #0c7a3c; }
  .ws-history-helpful.is-down { color: #9b2823; }
  .ws-history-helpful.is-pending { color: rgba(15, 17, 22, 0.40); }
  .ws-history-amount {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    font-weight: 600;
    color: #0c7a3c;
    text-align: right;
  }



  /* ════════════════════════════════════════════════════════════════════
     METEOROLOGIST WORKSPACE v3 — Pass 2
     Full 50-state county multi-select, severe alert mode, audience preview.
     ════════════════════════════════════════════════════════════════════ */

  /* Mode switcher: Routine brief vs Severe Alert */
  .ws-brief-mode-row {
    display: flex; gap: 10px;
    margin-top: 4px;
  }
  .ws-brief-mode-btn {
    flex: 1;
    display: flex; align-items: flex-start; gap: 12px;
    padding: 14px 16px;
    background: linear-gradient(180deg, #ffffff 0%, #fafbff 100%);
    border: 1.5px solid rgba(15, 17, 22, 0.08);
    border-radius: 14px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: border-color 180ms ease, background 180ms ease, box-shadow 220ms ease;
    text-align: left;
    position: relative;
  }
  .ws-brief-mode-btn:hover {
    border-color: rgba(65, 105, 225, 0.30);
    box-shadow: 0 4px 12px rgba(65, 105, 225, 0.06);
  }
  .ws-brief-mode-btn.is-active {
    border-color: var(--rb, #4169E1);
    background: linear-gradient(180deg, rgba(65, 105, 225, 0.04), rgba(65, 105, 225, 0.01));
  }
  .ws-brief-mode-btn.is-active.mode-severe {
    border-color: #C2342B;
    background: linear-gradient(180deg, rgba(194, 52, 43, 0.04), rgba(194, 52, 43, 0.01));
  }
  .ws-brief-mode-icon {
    flex-shrink: 0;
    width: 36px; height: 36px;
    border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    background: rgba(65, 105, 225, 0.10);
    color: var(--rb, #4169E1);
  }
  .ws-brief-mode-btn.mode-severe .ws-brief-mode-icon {
    background: rgba(194, 52, 43, 0.10);
    color: #C2342B;
  }
  .ws-brief-mode-info { flex: 1; min-width: 0; }
  .ws-brief-mode-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 15px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 3px;
  }
  .ws-brief-mode-desc {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.4;
    margin: 0;
  }

  /* Severe-mode visual treatment ripples through the composer */
  .ws-composer-shell.is-severe-mode .ws-composer-strip {
    background: linear-gradient(90deg,
      rgba(194, 52, 43, 0.08),
      rgba(194, 52, 43, 0.02));
    border-bottom-color: rgba(194, 52, 43, 0.18);
  }
  .ws-composer-shell.is-severe-mode .ws-composer-strip-eyebrow {
    color: #C2342B;
  }
  .ws-composer-shell.is-severe-mode .ws-composer-timer {
    background: rgba(194, 52, 43, 0.08);
    color: #9b2823;
  }
  .ws-composer-shell.is-severe-mode .ws-composer-timer-dot {
    background: #C2342B;
    box-shadow: 0 0 0 3px rgba(194, 52, 43, 0.16);
  }

  /* County selector */
  .ws-county-shell {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 12px;
    overflow: hidden;
  }
  .ws-county-controls {
    display: flex; align-items: center; gap: 8px;
    padding: 10px 12px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    background: linear-gradient(180deg, #fafbff, #f5f7ff);
  }
  .ws-county-state {
    flex-shrink: 0;
    padding: 8px 10px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: #0E1116;
    cursor: pointer;
    min-width: 170px;
  }
  .ws-county-search {
    flex: 1;
    padding: 8px 12px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: #0E1116;
    box-sizing: border-box;
  }
  .ws-county-search:focus, .ws-county-state:focus {
    outline: none;
    border-color: rgba(65, 105, 225, 0.40);
  }
  .ws-county-select-all-btn,
  .ws-county-clear-btn {
    flex-shrink: 0;
    padding: 8px 12px;
    background: rgba(65, 105, 225, 0.06);
    border: 1px solid rgba(65, 105, 225, 0.18);
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 600;
    color: var(--rb, #4169E1);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background-color 120ms ease;
    white-space: nowrap;
  }
  .ws-county-select-all-btn:hover,
  .ws-county-clear-btn:hover {
    background: rgba(65, 105, 225, 0.10);
  }
  .ws-county-clear-btn {
    background: rgba(15, 17, 22, 0.04);
    border-color: rgba(15, 17, 22, 0.10);
    color: rgba(15, 17, 22, 0.60);
  }
  .ws-county-clear-btn:hover {
    background: rgba(15, 17, 22, 0.07);
  }
  .ws-county-list {
    max-height: 240px;
    overflow-y: auto;
    padding: 8px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
    background: #fff;
  }
  @media (max-width: 700px) {
    .ws-county-list { grid-template-columns: repeat(2, 1fr); max-height: 200px; }
  }
  .ws-county-item {
    display: flex; align-items: center; gap: 7px;
    padding: 6px 8px;
    border-radius: 6px;
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.78);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background-color 100ms ease;
    user-select: none;
  }
  .ws-county-item:hover {
    background: rgba(65, 105, 225, 0.05);
  }
  .ws-county-item.is-selected {
    background: rgba(65, 105, 225, 0.08);
    color: #0E1116;
    font-weight: 500;
  }
  .ws-county-item input[type="checkbox"] {
    accent-color: var(--rb, #4169E1);
    cursor: pointer;
    flex-shrink: 0;
  }
  .ws-county-item-label {
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
    flex: 1;
  }
  .ws-county-empty {
    grid-column: 1 / -1;
    padding: 24px 16px;
    text-align: center;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.45);
    line-height: 1.5;
  }

  /* Selected counties chips bar */
  .ws-county-chips {
    display: flex; flex-wrap: wrap; gap: 6px;
    padding: 10px 12px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
    background: #fafbff;
    min-height: 42px;
    align-items: center;
  }
  .ws-county-chip {
    display: inline-flex; align-items: center; gap: 4px;
    padding: 4px 4px 4px 10px;
    background: rgba(65, 105, 225, 0.10);
    color: var(--rb, #4169E1);
    border-radius: 999px;
    font-size: 11.5px;
    font-weight: 600;
  }
  .ws-county-chip-state {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.5px;
    background: rgba(65, 105, 225, 0.18);
    padding: 1px 5px;
    border-radius: 4px;
    margin-right: 4px;
  }
  .ws-county-chip-x {
    width: 18px; height: 18px;
    display: flex; align-items: center; justify-content: center;
    background: transparent;
    border: none;
    color: var(--rb, #4169E1);
    cursor: pointer;
    border-radius: 50%;
    -webkit-tap-highlight-color: transparent;
    font-size: 14px;
    line-height: 1;
    padding: 0;
  }
  .ws-county-chip-x:hover {
    background: rgba(65, 105, 225, 0.18);
  }
  .ws-county-chips-empty {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.45);
    font-style: italic;
  }

  /* Audience preview — the "who's about to receive this" panel */
  .ws-audience-preview {
    display: flex; align-items: center; gap: 14px;
    padding: 14px 16px;
    background: linear-gradient(135deg,
      rgba(65, 105, 225, 0.08),
      rgba(65, 105, 225, 0.02));
    border: 1px solid rgba(65, 105, 225, 0.18);
    border-radius: 14px;
  }
  .ws-audience-icon {
    flex-shrink: 0;
    width: 42px; height: 42px;
    background: rgba(65, 105, 225, 0.14);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: var(--rb, #4169E1);
  }
  .ws-audience-text {
    flex: 1;
    line-height: 1.45;
  }
  .ws-audience-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 16px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 2px;
  }
  .ws-audience-headline-num {
    color: var(--rb, #4169E1);
    font-family: 'JetBrains Mono', monospace;
    font-size: 18px;
    letter-spacing: -0.4px;
  }
  .ws-audience-detail {
    font-size: 12px;
    color: rgba(15, 17, 22, 0.60);
    margin: 0;
  }
  .ws-composer-shell.is-severe-mode .ws-audience-preview {
    background: linear-gradient(135deg,
      rgba(194, 52, 43, 0.08),
      rgba(194, 52, 43, 0.02));
    border-color: rgba(194, 52, 43, 0.20);
  }
  .ws-composer-shell.is-severe-mode .ws-audience-icon {
    background: rgba(194, 52, 43, 0.14);
    color: #C2342B;
  }
  .ws-composer-shell.is-severe-mode .ws-audience-headline-num {
    color: #C2342B;
  }

  /* Map placeholder — coming soon affordance */
  .ws-map-placeholder {
    display: flex; align-items: center; gap: 12px;
    padding: 12px 14px;
    margin-top: 10px;
    background: linear-gradient(135deg, #f5f7ff, #eef2ff);
    border: 1px dashed rgba(65, 105, 225, 0.30);
    border-radius: 12px;
  }
  .ws-map-placeholder-icon {
    flex-shrink: 0;
    color: rgba(65, 105, 225, 0.65);
  }
  .ws-map-placeholder-text {
    flex: 1;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.65);
    line-height: 1.45;
    margin: 0;
  }
  .ws-map-placeholder-text strong {
    color: #0E1116;
    font-weight: 600;
  }

  /* Severe alert publish button */
  .ws-composer-submit.is-severe {
    background: linear-gradient(135deg, #C2342B, #FF5E68);
    box-shadow: 0 4px 14px rgba(194, 52, 43, 0.30);
  }
  .ws-composer-submit.is-severe:hover {
    box-shadow: 0 6px 18px rgba(194, 52, 43, 0.42);
  }

  /* Severe-mode field label color shifts to red so labels match the mode */
  .ws-composer-shell.is-severe-mode .ws-composer-section-label {
    color: rgba(194, 52, 43, 0.65);
  }

  /* ════════════════════════════════════════════════════════════════════
     VALET CREW WORKSPACE
     ════════════════════════════════════════════════════════════════════
     The Crew zone is its own world. Different from the Met workspace
     (light, executive) and the Admin Command Center (deep navy, ops
     bunker). The Crew zone is a "field tablet" — dark gray-green
     baseline that hints at military/SAR equipment without literally
     using it. The zone breathes: a subtle ambient gradient pulses
     gently in the background, signalling that the system is alive
     and watching. When a severe-weather mission activates, the whole
     zone shifts into urgent mode (red accents, faster animations,
     denser layout).

     Critical design principle from Michael's brief:
       "We move away from playing a game to operating a mission.
        Adults don't want to play weather; they want to be essential."
     So: no badges, no points, no levels-as-currency. Status comes
     from real-world impact (a Met used your photo; a roofer got the
     lead from your hail report). The contribution receipts are the
     reward.
     ════════════════════════════════════════════════════════════════════ */

  .crew-ws-screen {
    background:
      radial-gradient(ellipse at top right,
        rgba(65, 105, 225, 0.06) 0%,
        transparent 55%),
      radial-gradient(ellipse at bottom left,
        rgba(22, 199, 126, 0.04) 0%,
        transparent 60%),
      #0F1419;
    color: #E8EBF0;
    min-height: 100vh;
    padding-bottom: 60px;
    position: relative;
    overflow: hidden;
  }

  /* The zone breathes — a faint ambient sweep that pulses across the
     background. This is the "system is alive" signal. Subtle: 6%
     opacity peak, 14-second cycle, behind everything. */
  .crew-ws-screen::before {
    content: '';
    position: absolute;
    inset: -50%;
    background: radial-gradient(circle at 30% 50%,
      rgba(65, 105, 225, 0.08) 0%,
      transparent 35%);
    opacity: 0.5;
    animation: crewAmbient 14s ease-in-out infinite;
    pointer-events: none;
    z-index: 0;
  }
  @keyframes crewAmbient {
    0%, 100% { transform: translate(0, 0); opacity: 0.4; }
    33%      { transform: translate(8%, -4%); opacity: 0.7; }
    66%      { transform: translate(-4%, 6%); opacity: 0.5; }
  }
  /* Severe mode: ambient sweep turns red and pulses faster */
  .crew-ws-screen.is-severe::before {
    background: radial-gradient(circle at 50% 30%,
      rgba(194, 52, 43, 0.16) 0%,
      transparent 45%);
    animation-duration: 4s;
    animation-timing-function: ease-in-out;
  }

  /* The whole content sits above the ambient gradient */
  .crew-ws-screen > * { position: relative; z-index: 1; }

  /* ── Episode strip ─────────────────────────────────────
     The thin, dramatic band above the workspace header. Frames
     the day as a Survivor episode. Pulsing red "LIVE" dot on
     the left, episode title + tagline center, "X watching"
     counter on the right. The watchers count is real: it's
     subscribers currently viewing the Live Feed in this region.
     In severe mode the strip turns red. */
  .crew-ws-episode {
    background: linear-gradient(180deg,
      rgba(8, 12, 18, 0.98),
      rgba(11, 16, 23, 0.92));
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    padding: 9px 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
    font-family: 'JetBrains Mono', monospace;
    color: rgba(232, 235, 240, 0.75);
    font-size: 11.5px;
    letter-spacing: 0.5px;
    position: relative;
    z-index: 6;
    flex-wrap: wrap;
  }
  .crew-ws-episode-rec {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-weight: 700;
    font-size: 10px;
    letter-spacing: 1.6px;
    color: #FCA5A5;
    text-transform: uppercase;
  }
  .crew-ws-episode-rec-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #C2342B;
    box-shadow: 0 0 0 3px rgba(194, 52, 43, 0.20), 0 0 10px rgba(194, 52, 43, 0.65);
    animation: crewEpisodeRec 1.6s ease-in-out infinite;
  }
  @keyframes crewEpisodeRec {
    0%, 100% { opacity: 0.75; transform: scale(0.92); }
    50%      { opacity: 1;    transform: scale(1); }
  }
  .crew-ws-episode-label {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    flex: 1;
    justify-content: center;
    min-width: 0;
  }
  .crew-ws-episode-label strong {
    font-weight: 700;
    color: rgba(232, 235, 240, 0.95);
    letter-spacing: 1.1px;
    text-transform: uppercase;
    font-size: 10.5px;
  }
  .crew-ws-episode-sep {
    color: rgba(232, 235, 240, 0.35);
  }
  .crew-ws-episode-tagline {
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 13px;
    letter-spacing: 0;
    color: rgba(232, 235, 240, 0.85);
  }
  #crew-ws-episode-tagline {
    font-family: 'Fraunces', Georgia, serif;
    font-style: italic;
    font-size: 13px;
    letter-spacing: 0;
    color: rgba(232, 235, 240, 0.85);
  }
  .crew-ws-episode-watchers {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: rgba(232, 235, 240, 0.55);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.8px;
  }
  .crew-ws-episode-watchers svg {
    opacity: 0.7;
  }
  .crew-ws-episode-watchers-label {
    color: rgba(232, 235, 240, 0.40);
    text-transform: uppercase;
    letter-spacing: 1.2px;
    font-size: 10px;
  }
  /* Severe mode: the whole strip turns red-tinged */
  .crew-ws-screen.is-severe .crew-ws-episode {
    background: linear-gradient(180deg,
      rgba(70, 14, 12, 0.98),
      rgba(45, 10, 8, 0.92));
    border-bottom-color: rgba(194, 52, 43, 0.30);
  }
  .crew-ws-screen.is-severe .crew-ws-episode-rec {
    color: #FECACA;
  }
  .crew-ws-screen.is-severe #crew-ws-episode-tagline {
    color: #FECACA;
  }

  /* ── Header strip ─────────────────────────────────────── */
  /* Header is NOT sticky — the parent .crew-ws-screen has overflow:hidden
     to contain the ambient ::before gradient, which kills sticky behavior
     in any descendant. The header scrolls away with the rest of the
     content; the dev menu and "Exit to search" stay reachable via the
     viewport-fixed dev pill. */
  .crew-ws-header {
    background: linear-gradient(180deg,
      rgba(15, 20, 25, 0.92),
      rgba(15, 20, 25, 0.78));
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
    padding: 14px 24px 0;
    position: relative;
    z-index: 5;
  }
  .crew-ws-header-top {
    display: flex; align-items: center; justify-content: space-between;
    gap: 16px; flex-wrap: wrap;
    padding-bottom: 12px;
  }
  .crew-ws-brand {
    display: flex; align-items: baseline; gap: 14px;
  }
  .crew-ws-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.95);
  }
  .crew-ws-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: 0.2px;
    color: #F5F7FB;
    margin: 0;
  }
  .crew-ws-meta {
    display: flex; align-items: center; gap: 18px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: rgba(232, 235, 240, 0.55);
  }
  .crew-ws-meta-item { display: flex; align-items: center; gap: 6px; }
  .crew-ws-meta-label { color: rgba(232, 235, 240, 0.40); }
  .crew-ws-meta-value { color: rgba(232, 235, 240, 0.92); }

  /* Operator status dot — green pulse for active */
  .crew-ws-operator-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #16C77E;
    box-shadow: 0 0 0 3px rgba(22, 199, 126, 0.20);
    animation: crewOperatorPulse 2.4s ease-in-out infinite;
  }
  @keyframes crewOperatorPulse {
    0%, 100% { opacity: 0.65; }
    50%      { opacity: 1; }
  }
  .crew-ws-screen.is-severe .crew-ws-operator-dot {
    background: #C2342B;
    box-shadow: 0 0 0 3px rgba(194, 52, 43, 0.25);
    animation-duration: 1.2s;
  }

  /* ── Body grid ──────────────────────────────────────── */
  .crew-ws-body {
    padding: 22px 24px 80px;
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 14px;
  }
  @media (max-width: 900px) {
    .crew-ws-body { padding: 16px; }
  }

  /* ── Crew tile ─────────────────────────────────────── */
  .crew-tile {
    background:
      linear-gradient(180deg,
        rgba(255, 255, 255, 0.04) 0%,
        rgba(255, 255, 255, 0.015) 100%);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    padding: 18px 20px;
    transition: border-color 220ms ease, box-shadow 220ms ease;
    position: relative;
    overflow: hidden;
  }
  .crew-tile::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: linear-gradient(180deg,
      rgba(255, 255, 255, 0.04) 0%,
      transparent 30%);
    pointer-events: none;
  }
  .crew-tile:hover {
    border-color: rgba(65, 105, 225, 0.26);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.20);
  }
  .crew-tile-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 0.95);
    margin: 0 0 12px;
    display: flex; align-items: center; gap: 8px;
    position: relative; z-index: 1;
  }
  .crew-tile-label-detail {
    color: rgba(232, 235, 240, 0.40);
    font-weight: 400;
    letter-spacing: 0.8px;
  }
  .crew-span-3  { grid-column: span 3; }
  .crew-span-4  { grid-column: span 4; }
  .crew-span-6  { grid-column: span 6; }
  .crew-span-8  { grid-column: span 8; }
  .crew-span-12 { grid-column: span 12; }
  @media (max-width: 1100px) {
    .crew-span-3, .crew-span-4 { grid-column: span 6; }
    .crew-span-8 { grid-column: span 12; }
  }
  @media (max-width: 640px) {
    .crew-span-3, .crew-span-4, .crew-span-6, .crew-span-8 { grid-column: span 12; }
  }

  /* ── Mission card — the centerpiece ──────────────────
     This is the single biggest thing on the screen. When a mission is
     active, it pulses gently around the edge. The interior is a
     two-column layout: status + briefing on left, action on right. */
  .crew-mission {
    grid-column: span 12;
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.10) 0%,
        rgba(65, 105, 225, 0.02) 50%,
        rgba(15, 20, 25, 0.70) 100%);
    border: 1.5px solid rgba(65, 105, 225, 0.30);
    border-radius: 20px;
    padding: 0;
    overflow: hidden;
    position: relative;
    box-shadow:
      0 0 0 1px rgba(65, 105, 225, 0.08) inset,
      0 8px 32px rgba(65, 105, 225, 0.10);
  }

  /* Pulse ring for active missions — Royal Blue glow */
  .crew-mission.is-active {
    animation: crewMissionPulse 3s ease-in-out infinite;
  }
  @keyframes crewMissionPulse {
    0%, 100% {
      box-shadow:
        0 0 0 1px rgba(65, 105, 225, 0.08) inset,
        0 8px 32px rgba(65, 105, 225, 0.10);
    }
    50% {
      box-shadow:
        0 0 0 1px rgba(65, 105, 225, 0.18) inset,
        0 12px 40px rgba(65, 105, 225, 0.20);
    }
  }

  /* Severe mission: red gradient instead of blue */
  .crew-mission.is-severe {
    background:
      linear-gradient(135deg,
        rgba(194, 52, 43, 0.16) 0%,
        rgba(194, 52, 43, 0.04) 50%,
        rgba(15, 20, 25, 0.70) 100%);
    border-color: rgba(194, 52, 43, 0.45);
    box-shadow:
      0 0 0 1px rgba(194, 52, 43, 0.14) inset,
      0 8px 32px rgba(194, 52, 43, 0.18);
    animation: crewMissionUrgent 1.4s ease-in-out infinite;
  }
  @keyframes crewMissionUrgent {
    0%, 100% {
      box-shadow:
        0 0 0 1px rgba(194, 52, 43, 0.14) inset,
        0 8px 32px rgba(194, 52, 43, 0.18);
    }
    50% {
      box-shadow:
        0 0 0 1px rgba(194, 52, 43, 0.28) inset,
        0 12px 48px rgba(194, 52, 43, 0.32);
    }
  }

  /* Mission strip header */
  .crew-mission-strip {
    display: flex; align-items: center; justify-content: space-between;
    gap: 14px;
    padding: 12px 20px;
    background: linear-gradient(90deg,
      rgba(65, 105, 225, 0.18),
      rgba(65, 105, 225, 0.04));
    border-bottom: 1px solid rgba(65, 105, 225, 0.20);
  }
  .crew-mission.is-severe .crew-mission-strip {
    background: linear-gradient(90deg,
      rgba(194, 52, 43, 0.24),
      rgba(194, 52, 43, 0.06));
    border-bottom-color: rgba(194, 52, 43, 0.30);
  }
  .crew-mission-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 1);
    display: flex; align-items: center; gap: 8px;
  }
  .crew-mission.is-severe .crew-mission-eyebrow {
    color: #FF8E76;
  }
  .crew-mission-eyebrow-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    box-shadow: 0 0 8px rgba(65, 105, 225, 0.7);
    animation: crewMissionDot 2s ease-in-out infinite;
  }
  .crew-mission.is-severe .crew-mission-eyebrow-dot {
    background: #C2342B;
    box-shadow: 0 0 8px rgba(194, 52, 43, 0.8);
    animation-duration: 0.8s;
  }
  @keyframes crewMissionDot {
    0%, 100% { transform: scale(1);   opacity: 0.7; }
    50%      { transform: scale(1.3); opacity: 1; }
  }
  .crew-mission-status {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(232, 235, 240, 0.65);
    letter-spacing: 0.5px;
  }

  /* Mission body */
  .crew-mission-body {
    padding: 22px 24px 24px;
  }
  .crew-mission-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 28px;
    font-weight: 600;
    color: #F5F7FB;
    line-height: 1.15;
    letter-spacing: -0.3px;
    margin: 0 0 8px;
  }
  .crew-mission-context {
    font-size: 14px;
    color: rgba(232, 235, 240, 0.78);
    line-height: 1.5;
    margin: 0 0 18px;
    max-width: 720px;
  }
  .crew-mission-purpose {
    display: flex; align-items: flex-start; gap: 10px;
    padding: 12px 14px;
    background: rgba(65, 105, 225, 0.08);
    border-left: 3px solid var(--rb, #4169E1);
    border-radius: 0 10px 10px 0;
    margin-bottom: 18px;
  }
  .crew-mission.is-severe .crew-mission-purpose {
    background: rgba(194, 52, 43, 0.10);
    border-left-color: #C2342B;
  }
  .crew-mission-purpose-icon {
    flex-shrink: 0;
    color: rgba(65, 105, 225, 0.95);
    margin-top: 1px;
  }
  .crew-mission.is-severe .crew-mission-purpose-icon {
    color: #FF8E76;
  }
  .crew-mission-purpose-text {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: rgba(232, 235, 240, 0.85);
    line-height: 1.5;
    margin: 0;
  }
  .crew-mission-purpose-text strong {
    color: #F5F7FB;
    font-weight: 600;
  }

  /* Mission action: large tap targets */
  .crew-mission-actions {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
  }
  @media (max-width: 600px) {
    .crew-mission-actions { grid-template-columns: 1fr; }
  }
  .crew-mission-tap {
    padding: 18px 14px;
    background:
      linear-gradient(180deg,
        rgba(255, 255, 255, 0.07) 0%,
        rgba(255, 255, 255, 0.02) 100%);
    border: 1.5px solid rgba(255, 255, 255, 0.12);
    border-radius: 14px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    text-align: center;
    transition: all 180ms ease;
    color: #F5F7FB;
    font-family: 'Inter', system-ui, sans-serif;
  }
  .crew-mission-tap:hover {
    border-color: rgba(65, 105, 225, 0.55);
    background:
      linear-gradient(180deg,
        rgba(65, 105, 225, 0.10) 0%,
        rgba(65, 105, 225, 0.02) 100%);
    transform: translateY(-1px);
  }
  .crew-mission.is-severe .crew-mission-tap:hover {
    border-color: rgba(194, 52, 43, 0.55);
    background:
      linear-gradient(180deg,
        rgba(194, 52, 43, 0.10) 0%,
        rgba(194, 52, 43, 0.02) 100%);
  }
  .crew-mission-tap-icon {
    font-size: 22px;
    margin-bottom: 6px;
    display: block;
  }
  .crew-mission-tap-label {
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 2px;
  }
  .crew-mission-tap-detail {
    font-size: 11px;
    color: rgba(232, 235, 240, 0.55);
  }

  /* Mission secondary actions: photo capture, decline */
  .crew-mission-secondary {
    display: flex; gap: 8px; justify-content: flex-end;
    margin-top: 16px;
    padding-top: 14px;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }
  .crew-mission-decline,
  .crew-mission-capture {
    padding: 9px 14px;
    border-radius: 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 500;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .crew-mission-decline {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: rgba(232, 235, 240, 0.60);
  }
  .crew-mission-decline:hover {
    background: rgba(255, 255, 255, 0.04);
    color: rgba(232, 235, 240, 0.80);
  }
  .crew-mission-capture {
    background: rgba(65, 105, 225, 0.18);
    border: 1px solid rgba(65, 105, 225, 0.40);
    color: #B5C8FF;
    display: inline-flex; align-items: center; gap: 6px;
  }
  .crew-mission-capture:hover {
    background: rgba(65, 105, 225, 0.28);
  }

  /* ── Personal status panel ─────────────────────────── */
  .crew-status-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 14px;
    padding: 4px 0;
  }
  .crew-status-stat-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: rgba(232, 235, 240, 0.45);
    margin-bottom: 6px;
  }
  .crew-status-stat-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 22px;
    font-weight: 600;
    color: #F5F7FB;
    line-height: 1;
    letter-spacing: -0.3px;
  }
  .crew-status-stat-value-unit {
    font-size: 12px;
    color: rgba(232, 235, 240, 0.45);
    margin-left: 2px;
  }
  .crew-status-stat-context {
    font-size: 11px;
    color: rgba(232, 235, 240, 0.55);
    margin-top: 4px;
    line-height: 1.4;
  }
  .crew-status-rank {
    grid-column: span 2;
    padding: 12px 14px;
    background: linear-gradient(135deg,
      rgba(245, 165, 36, 0.14),
      rgba(245, 165, 36, 0.02));
    border: 1px solid rgba(245, 165, 36, 0.25);
    border-radius: 12px;
    display: flex; align-items: center; gap: 12px;
  }
  .crew-status-rank-badge {
    flex-shrink: 0;
    width: 44px; height: 44px;
    border-radius: 50%;
    background: linear-gradient(135deg, #F5A524, #E89518);
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 4px 14px rgba(245, 165, 36, 0.35);
    color: #0F1419;
  }
  .crew-status-rank-info { flex: 1; min-width: 0; }
  .crew-status-rank-name {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 16px;
    font-weight: 600;
    color: #F5F7FB;
    margin: 0 0 2px;
  }
  .crew-status-rank-detail {
    font-size: 11.5px;
    color: rgba(232, 235, 240, 0.60);
    margin: 0;
  }
  .crew-status-specialty {
    grid-column: span 2;
    display: flex; align-items: center; gap: 8px;
    padding: 8px 12px;
    background: rgba(65, 105, 225, 0.10);
    border: 1px solid rgba(65, 105, 225, 0.22);
    border-radius: 999px;
    font-size: 11.5px;
    color: rgba(232, 235, 240, 0.85);
  }
  .crew-status-specialty-icon {
    color: rgba(65, 105, 225, 1);
  }
  .crew-status-specialty strong { color: #F5F7FB; }

  /* ── Daily check-in (always available) ─────────────── */
  .crew-checkin {
    display: flex; flex-direction: column; gap: 12px;
  }
  .crew-checkin-options {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 6px;
  }
  @media (max-width: 600px) {
    .crew-checkin-options { grid-template-columns: repeat(2, 1fr); }
  }
  .crew-checkin-btn {
    padding: 10px 6px;
    background:
      linear-gradient(180deg,
        rgba(255, 255, 255, 0.04) 0%,
        rgba(255, 255, 255, 0.01) 100%);
    border: 1px solid rgba(255, 255, 255, 0.10);
    border-radius: 10px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
    color: #F5F7FB;
    text-align: center;
  }
  .crew-checkin-btn:hover {
    border-color: rgba(65, 105, 225, 0.45);
    background:
      linear-gradient(180deg,
        rgba(65, 105, 225, 0.08) 0%,
        rgba(65, 105, 225, 0.02) 100%);
    transform: translateY(-1px);
  }
  .crew-checkin-btn.is-selected {
    border-color: rgba(65, 105, 225, 0.65);
    background:
      linear-gradient(180deg,
        rgba(65, 105, 225, 0.15) 0%,
        rgba(65, 105, 225, 0.05) 100%);
  }
  .crew-checkin-icon {
    font-size: 20px;
    margin-bottom: 4px;
    display: block;
  }
  .crew-checkin-label {
    font-size: 11.5px;
    font-weight: 500;
    color: rgba(232, 235, 240, 0.85);
  }
  .crew-checkin-streak {
    display: flex; align-items: center; justify-content: space-between;
    padding-top: 4px;
    font-size: 11.5px;
  }
  .crew-checkin-streak-label {
    color: rgba(232, 235, 240, 0.55);
  }
  .crew-checkin-streak-value {
    font-family: 'JetBrains Mono', monospace;
    color: #F5A524;
    font-weight: 700;
    letter-spacing: 0.5px;
  }

  /* ── Contribution receipts ─────────────────────────── */
  .crew-receipts {
    display: flex; flex-direction: column;
    gap: 0;
  }
  .crew-receipt {
    display: flex; align-items: flex-start; gap: 12px;
    padding: 12px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }
  .crew-receipt:last-child { border-bottom: none; }
  .crew-receipt-icon {
    flex-shrink: 0;
    width: 32px; height: 32px;
    background: rgba(22, 199, 126, 0.14);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: #16C77E;
  }
  .crew-receipt-icon.is-pending {
    background: rgba(245, 165, 36, 0.14);
    color: #F5A524;
  }
  .crew-receipt-body { flex: 1; min-width: 0; }
  .crew-receipt-headline {
    font-size: 13px;
    color: #F5F7FB;
    line-height: 1.4;
    margin: 0 0 3px;
  }
  .crew-receipt-headline strong {
    color: #B5C8FF;
    font-weight: 600;
  }
  .crew-receipt-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(232, 235, 240, 0.45);
    letter-spacing: 0.4px;
  }

  /* ── Crew nearby ───────────────────────────────────── */
  .crew-nearby-list {
    display: flex; flex-direction: column;
    gap: 0;
  }
  .crew-nearby-row {
    display: flex; align-items: center; gap: 12px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }
  .crew-nearby-row:last-child { border-bottom: none; }
  .crew-nearby-avatar {
    flex-shrink: 0;
    width: 32px; height: 32px;
    border-radius: 50%;
    background: linear-gradient(135deg,
      rgba(65, 105, 225, 0.55),
      rgba(65, 105, 225, 0.25));
    display: flex; align-items: center; justify-content: center;
    color: #fff;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.5px;
  }
  .crew-nearby-info { flex: 1; min-width: 0; }
  .crew-nearby-name {
    font-size: 13px;
    color: #F5F7FB;
    font-weight: 500;
    margin: 0 0 1px;
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .crew-nearby-detail {
    font-size: 11px;
    color: rgba(232, 235, 240, 0.50);
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .crew-nearby-status {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    padding: 3px 7px;
    border-radius: 999px;
  }
  .crew-nearby-status.is-on-mission {
    background: rgba(65, 105, 225, 0.18);
    color: #B5C8FF;
  }
  .crew-nearby-status.is-active {
    background: rgba(22, 199, 126, 0.16);
    color: #5EE3A6;
  }
  .crew-nearby-status.is-idle {
    background: rgba(232, 235, 240, 0.08);
    color: rgba(232, 235, 240, 0.50);
  }

  /* ── Conditions strip (current local conditions) ───── */
  .crew-conditions {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
  }
  @media (max-width: 600px) {
    .crew-conditions { grid-template-columns: repeat(2, 1fr); }
  }
  .crew-condition-cell {
    padding: 8px 10px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 10px;
  }
  .crew-condition-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 8.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(232, 235, 240, 0.40);
    margin-bottom: 3px;
  }
  .crew-condition-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    font-weight: 600;
    color: #F5F7FB;
    letter-spacing: -0.2px;
  }

  /* ── Mission complete confirmation ─────────────────── */
  .crew-mission-complete {
    text-align: center;
    padding: 32px 20px;
  }
  .crew-mission-complete-icon {
    width: 56px; height: 56px;
    border-radius: 50%;
    background: linear-gradient(135deg, #16C77E, #0EA468);
    display: inline-flex; align-items: center; justify-content: center;
    color: #fff;
    margin-bottom: 16px;
    box-shadow: 0 8px 24px rgba(22, 199, 126, 0.30);
  }
  .crew-mission-complete-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    color: #F5F7FB;
    margin: 0 0 8px;
  }
  .crew-mission-complete-detail {
    font-size: 14px;
    color: rgba(232, 235, 240, 0.75);
    line-height: 1.5;
    max-width: 480px;
    margin: 0 auto 18px;
  }
  .crew-mission-complete-receipt {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 10px 16px;
    background: rgba(22, 199, 126, 0.10);
    border: 1px solid rgba(22, 199, 126, 0.25);
    border-radius: 999px;
    font-size: 12px;
    color: #5EE3A6;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 0.5px;
  }

  /* ── Post-submit confessional ──────────────────────────
     Sits inside the mission-complete card. Optional. Asks the
     Crew member if they want to add a note while the memory is
     fresh. Stays quiet — soft contrast, no urgency. Save / skip
     buttons end the moment. */
  .crew-confessional {
    margin-top: 24px;
    padding: 16px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.10);
    border-radius: 12px;
    text-align: left;
  }
  .crew-confessional-label {
    display: block;
    font-family: 'Fraunces', Georgia, serif;
    font-size: 15px;
    font-weight: 600;
    color: #F5F7FB;
    margin-bottom: 4px;
  }
  .crew-confessional-sublabel {
    display: block;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 400;
    color: rgba(232, 235, 240, 0.55);
    margin-top: 4px;
    margin-bottom: 10px;
    letter-spacing: 0;
  }
  .crew-confessional-input {
    width: 100%;
    box-sizing: border-box;
    background: rgba(0, 0, 0, 0.32);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 9px;
    padding: 10px 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    line-height: 1.5;
    color: #F5F7FB;
    resize: vertical;
    min-height: 70px;
    transition: border-color 140ms ease;
  }
  .crew-confessional-input:focus {
    outline: none;
    border-color: rgba(95, 135, 245, 0.65);
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.18);
  }
  .crew-confessional-input::placeholder {
    color: rgba(232, 235, 240, 0.35);
    font-style: italic;
  }
  .crew-confessional-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-top: 10px;
    flex-wrap: wrap;
  }
  .crew-confessional-quote {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    cursor: pointer;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    color: rgba(232, 235, 240, 0.70);
  }
  .crew-confessional-quote input {
    width: 15px; height: 15px;
    accent-color: #5F87F5;
    cursor: pointer;
    margin: 0;
  }
  .crew-confessional-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(232, 235, 240, 0.45);
  }
  .crew-confessional-count.is-near-limit { color: #F5A524; }
  .crew-confessional-actions {
    display: flex;
    gap: 8px;
    justify-content: flex-end;
    margin-top: 12px;
  }
  .crew-confessional-skip {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.15);
    color: rgba(232, 235, 240, 0.65);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    padding: 8px 14px;
    border-radius: 8px;
    cursor: pointer;
    transition: background 140ms ease;
  }
  .crew-confessional-skip:hover { background: rgba(255, 255, 255, 0.06); }
  .crew-confessional-save {
    background: #4169E1;
    border: 1px solid rgba(255, 255, 255, 0.10);
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    padding: 8px 16px;
    border-radius: 8px;
    cursor: pointer;
    transition: background 140ms ease;
  }
  .crew-confessional-save:hover { background: #5277e8; }
  .crew-confessional-save:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }
  .crew-confessional-saved {
    margin-top: 16px;
    display: inline-flex;
    align-items: center;
    gap: 7px;
    padding: 7px 12px;
    background: rgba(22, 199, 126, 0.10);
    border: 1px solid rgba(22, 199, 126, 0.25);
    border-radius: 999px;
    color: #5EE3A6;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11.5px;
    letter-spacing: 0.4px;
  }

  /* ── Field notebook ──────────────────────────────────
     The Crew member's private log of confessional notes.
     Each entry shows date/time, the mission it came from
     (if applicable), a privacy tag, and the note text. */
  .crew-notebook-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .crew-notebook-empty {
    padding: 32px 16px;
    text-align: center;
    color: rgba(232, 235, 240, 0.50);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13.5px;
    line-height: 1.6;
    max-width: 580px;
    margin: 0 auto;
  }
  .crew-notebook-entry {
    padding: 14px 16px;
    background: rgba(255, 255, 255, 0.025);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 10px;
    transition: border-color 200ms ease;
  }
  .crew-notebook-entry:hover {
    border-color: rgba(255, 255, 255, 0.15);
  }
  .crew-notebook-entry-meta {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 8px;
    flex-wrap: wrap;
  }
  .crew-notebook-entry-date {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 600;
    letter-spacing: 0.8px;
    color: rgba(232, 235, 240, 0.55);
    text-transform: uppercase;
  }
  .crew-notebook-entry-mission {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 600;
    letter-spacing: 0.5px;
    color: rgba(95, 135, 245, 0.85);
  }
  .crew-notebook-entry-flag {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 10.5px;
    font-weight: 600;
    letter-spacing: 0.3px;
    background: rgba(245, 165, 36, 0.16);
    color: #F5C57A;
  }
  .crew-notebook-entry-flag.is-private {
    background: rgba(255, 255, 255, 0.08);
    color: rgba(232, 235, 240, 0.65);
  }
  .crew-notebook-entry-text {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 15px;
    line-height: 1.55;
    color: rgba(245, 247, 251, 0.92);
    margin: 0;
    white-space: pre-wrap;
  }


  /* ════════════════════════════════════════════════════════════════════
     CREW WORKSPACE PASS 2
     Collapsed mission buttons, citation badges, Second-this affordance,
     real-time Met-citation notification.
     ════════════════════════════════════════════════════════════════════ */

  /* ── Collapsed mission button (default state) ──────────
     The Mission Impossible briefing pattern: not open by default;
     tap to deploy. Each available mission sits as a compact button
     with eyebrow, headline, and a "tap to deploy" cue. The whole
     button has the same Royal Blue glow as the expanded card so the
     visual language is consistent. */
  .crew-mission-list {
    grid-column: span 12;
    display: flex; flex-direction: column;
    gap: 10px;
  }
  .crew-mission-btn {
    width: 100%;
    text-align: left;
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.10) 0%,
        rgba(65, 105, 225, 0.02) 50%,
        rgba(15, 20, 25, 0.70) 100%);
    border: 1.5px solid rgba(65, 105, 225, 0.30);
    border-radius: 16px;
    padding: 16px 20px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    color: #F5F7FB;
    font-family: 'Inter', system-ui, sans-serif;
    transition: all 200ms ease;
    position: relative;
    overflow: hidden;
    display: flex; align-items: center; gap: 14px;
    animation: crewMissionPulse 3s ease-in-out infinite;
  }
  .crew-mission-btn:hover {
    border-color: rgba(65, 105, 225, 0.55);
    transform: translateY(-1px);
  }
  .crew-mission-btn-icon {
    flex-shrink: 0;
    width: 40px; height: 40px;
    border-radius: 10px;
    background: rgba(65, 105, 225, 0.18);
    color: rgba(65, 105, 225, 1);
    display: flex; align-items: center; justify-content: center;
  }
  .crew-mission-btn.is-severe .crew-mission-btn-icon {
    background: rgba(194, 52, 43, 0.22);
    color: #FF8E76;
  }
  .crew-mission-btn-info {
    flex: 1; min-width: 0;
  }
  .crew-mission-btn-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 1);
    display: flex; align-items: center; gap: 6px;
    margin-bottom: 4px;
  }
  .crew-mission-btn.is-severe .crew-mission-btn-eyebrow {
    color: #FF8E76;
  }
  .crew-mission-btn-eyebrow-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    box-shadow: 0 0 6px rgba(65, 105, 225, 0.7);
    animation: crewMissionDot 2s ease-in-out infinite;
  }
  .crew-mission-btn.is-severe .crew-mission-btn-eyebrow-dot {
    background: #C2342B;
    box-shadow: 0 0 6px rgba(194, 52, 43, 0.8);
    animation-duration: 0.8s;
  }
  .crew-mission-btn-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 17px;
    font-weight: 600;
    color: #F5F7FB;
    line-height: 1.25;
    margin: 0;
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .crew-mission-btn-deploy {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: rgba(65, 105, 225, 1);
    display: flex; align-items: center; gap: 6px;
    padding: 8px 14px;
    background: rgba(65, 105, 225, 0.10);
    border: 1px solid rgba(65, 105, 225, 0.28);
    border-radius: 999px;
    transition: background-color 160ms ease;
  }
  .crew-mission-btn:hover .crew-mission-btn-deploy {
    background: rgba(65, 105, 225, 0.20);
  }
  .crew-mission-btn.is-severe .crew-mission-btn-deploy {
    color: #FF8E76;
    background: rgba(194, 52, 43, 0.14);
    border-color: rgba(194, 52, 43, 0.38);
  }
  .crew-mission-btn.is-severe:hover .crew-mission-btn-deploy {
    background: rgba(194, 52, 43, 0.24);
  }

  /* Severe missions visually distinct: red border, red gradient, faster pulse */
  .crew-mission-btn.is-severe {
    background:
      linear-gradient(135deg,
        rgba(194, 52, 43, 0.16) 0%,
        rgba(194, 52, 43, 0.04) 50%,
        rgba(15, 20, 25, 0.70) 100%);
    border-color: rgba(194, 52, 43, 0.45);
    animation: crewMissionUrgent 1.4s ease-in-out infinite;
  }
  .crew-mission-btn.is-severe:hover {
    border-color: rgba(194, 52, 43, 0.70);
  }

  /* Empty state when no missions are available */
  .crew-mission-empty {
    grid-column: span 12;
    padding: 22px 24px;
    background: rgba(255, 255, 255, 0.02);
    border: 1px dashed rgba(255, 255, 255, 0.08);
    border-radius: 14px;
    text-align: center;
    color: rgba(232, 235, 240, 0.55);
    font-size: 13px;
    line-height: 1.5;
  }
  .crew-mission-empty strong { color: rgba(232, 235, 240, 0.85); }

  /* ── Citation badge on receipts ────────────────────────
     When a Met cites a Crew member's report, the receipt gets a
     citation badge — small pill with the citing Met's initials.
     This is the verifiable usefulness flex. */
  .crew-receipt-citation {
    display: inline-flex; align-items: center; gap: 6px;
    margin-top: 6px;
    padding: 3px 9px;
    background: rgba(65, 105, 225, 0.14);
    border: 1px solid rgba(65, 105, 225, 0.30);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    color: #B5C8FF;
  }
  .crew-receipt-citation-icon {
    color: rgba(65, 105, 225, 1);
  }

  /* ── Second-this-report affordance ────────────────────
     Other Crew members confirming a teammate's report. Not a "like"
     — a "second." Means "I can confirm this from where I am."
     Counts shown on the receipt. */
  .crew-receipt-actions {
    display: flex; align-items: center; gap: 12px;
    margin-top: 8px;
  }
  .crew-second-btn {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 5px 11px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.10);
    border-radius: 999px;
    color: rgba(232, 235, 240, 0.75);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 11.5px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
  }
  .crew-second-btn:hover {
    background: rgba(22, 199, 126, 0.10);
    border-color: rgba(22, 199, 126, 0.30);
    color: #5EE3A6;
  }
  .crew-second-btn.is-seconded {
    background: rgba(22, 199, 126, 0.14);
    border-color: rgba(22, 199, 126, 0.40);
    color: #5EE3A6;
    pointer-events: none;
  }
  .crew-second-btn-icon {
    color: currentColor;
  }
  .crew-second-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(232, 235, 240, 0.55);
  }

  /* ── Real-time Met-citation notification ───────────────
     When a Met uses your report, a notification slides in from the
     top-right with the Met's name and what they used it for. Lives
     on top of everything else; auto-dismisses after 7 seconds.
     Production: this is a Twilio SMS too. */
  .crew-citation-toast {
    position: fixed;
    top: 80px; right: 24px;
    width: 360px;
    max-width: calc(100vw - 32px);
    background:
      linear-gradient(135deg,
        rgba(15, 30, 60, 0.96) 0%,
        rgba(20, 40, 80, 0.92) 100%);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    border: 1.5px solid rgba(65, 105, 225, 0.45);
    border-radius: 16px;
    padding: 16px 18px;
    color: #F5F7FB;
    box-shadow:
      0 0 0 1px rgba(65, 105, 225, 0.20) inset,
      0 16px 48px rgba(0, 0, 0, 0.40),
      0 0 64px rgba(65, 105, 225, 0.20);
    transform: translateX(110%);
    opacity: 0;
    pointer-events: none;
    transition: transform 420ms cubic-bezier(0.2, 0.8, 0.2, 1),
                opacity 280ms ease;
    z-index: 9000;
  }
  .crew-citation-toast.is-visible {
    transform: translateX(0);
    opacity: 1;
    pointer-events: auto;
  }
  .crew-citation-toast-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(181, 200, 255, 1);
    display: flex; align-items: center; gap: 8px;
    margin-bottom: 8px;
  }
  .crew-citation-toast-eyebrow-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: #16C77E;
    box-shadow: 0 0 8px rgba(22, 199, 126, 0.7);
    animation: crewMissionDot 1.4s ease-in-out infinite;
  }
  .crew-citation-toast-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 17px;
    font-weight: 600;
    line-height: 1.3;
    margin: 0 0 6px;
  }
  .crew-citation-toast-headline strong {
    color: #B5C8FF;
    font-weight: 700;
  }
  .crew-citation-toast-detail {
    font-size: 12.5px;
    color: rgba(232, 235, 240, 0.78);
    line-height: 1.45;
    margin: 0;
  }
  .crew-citation-toast-meta {
    margin-top: 10px;
    padding-top: 8px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.5px;
    color: rgba(181, 200, 255, 0.60);
  }
  .crew-citation-toast-close {
    position: absolute;
    top: 10px; right: 10px;
    width: 24px; height: 24px;
    background: transparent;
    border: none;
    color: rgba(232, 235, 240, 0.45);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 16px;
    line-height: 1;
    padding: 0;
    transition: background-color 140ms ease, color 140ms ease;
  }
  .crew-citation-toast-close:hover {
    background: rgba(255, 255, 255, 0.08);
    color: rgba(232, 235, 240, 0.85);
  }

  /* ── Top stats strip — compact summary above check-in ─
     Just the meaningful numbers, no big tile. Sits between
     the header and the check-in row. */
  .crew-top-strip {
    grid-column: span 12;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
    margin-top: -4px;
  }
  @media (max-width: 700px) {
    .crew-top-strip { grid-template-columns: repeat(2, 1fr); }
  }
  .crew-top-stat {
    padding: 10px 14px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 12px;
    display: flex; align-items: center; gap: 10px;
  }
  .crew-top-stat-icon {
    flex-shrink: 0;
    color: rgba(65, 105, 225, 0.85);
    width: 18px; height: 18px;
  }
  .crew-top-stat-text {
    flex: 1; min-width: 0;
  }
  .crew-top-stat-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(232, 235, 240, 0.45);
    margin-bottom: 1px;
  }
  .crew-top-stat-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    font-weight: 600;
    color: #F5F7FB;
  }
  .crew-top-stat-value-em { color: #F5A524; }

  /* ── Check-in row layout ─────────────────────────────
     Check-in becomes the first big row. Four condition tiles + a
     consecutive-days streak panel. Wider, more present than before. */
  .crew-checkin-row {
    grid-column: span 12;
    display: grid;
    grid-template-columns: minmax(0, 2fr) minmax(0, 1fr);
    gap: 12px;
  }
  @media (max-width: 900px) {
    .crew-checkin-row { grid-template-columns: 1fr; }
  }
  .crew-checkin-streak-tile {
    background:
      linear-gradient(135deg,
        rgba(245, 165, 36, 0.14) 0%,
        rgba(245, 165, 36, 0.02) 100%);
    border: 1px solid rgba(245, 165, 36, 0.28);
    border-radius: 16px;
    padding: 18px 20px;
    display: flex; align-items: center; gap: 14px;
  }
  .crew-checkin-streak-flame {
    flex-shrink: 0;
    width: 48px; height: 48px;
    border-radius: 50%;
    background: linear-gradient(135deg, #F5A524, #E89518);
    display: flex; align-items: center; justify-content: center;
    color: #0F1419;
    box-shadow: 0 4px 14px rgba(245, 165, 36, 0.30);
  }
  .crew-checkin-streak-info { flex: 1; min-width: 0; }
  .crew-checkin-streak-num {
    font-family: 'JetBrains Mono', monospace;
    font-size: 28px;
    font-weight: 600;
    color: #F5F7FB;
    line-height: 1;
    letter-spacing: -0.4px;
  }
  .crew-checkin-streak-num-unit {
    font-size: 14px;
    color: rgba(232, 235, 240, 0.50);
    margin-left: 3px;
  }
  .crew-checkin-streak-label {
    font-size: 11.5px;
    color: rgba(232, 235, 240, 0.65);
    margin-top: 4px;
  }

  /* ════════════════════════════════════════════════════════════════════
     STORM SHELTER v2 — customer-side severe weather surface
     ════════════════════════════════════════════════════════════════════
     This is the inverse of the Crew workspace severe mode. Crew sees
     urgency because they're being asked to ACT. Customers see severity
     because they need to TAKE SHELTER. Same storm, different posture,
     same color family as the rest of the product.

     Three states, mapped to the existing verdict colors:
       - WATCH   → amber  (#F5A524)  conditions favor a storm
       - WARNING → red    (#C2342B)  storm imminent or occurring
       - ALL CLEAR → green (#16C77E) risk has passed (auto-fade)

     Activation triggers (production):
       - SPC categorical risk Enhanced+ → Watch
       - NWS Watch issued in saved location → Watch
       - NWS Warning issued in saved location → Warning
       - All NWS alerts cleared → All Clear (auto-fade after 8 min)

     Met override: a Met can replace the auto-generated content for
     a specific region. The override locks for 60 minutes (visible
     indicator) so the next NWS poll doesn't silently overwrite the
     Met's voice mid-storm. The Met can extend or release the lock.

     The shelter takes over the top of the page during active alerts.
     Search and the rest of the welcome content remain accessible
     below the shelter — additive, not modal — but the shelter is
     unmissably the headline. ════════════════════════════════════ */

  /* The shelter sits as the page's first element when active.
     Hidden by default. Color-tone follows the alert tier. */
  .storm-shelter {
    display: none;
    position: relative;
    margin-bottom: 24px;
    border-radius: 6px;  /* tight corners — closer to alert-bar than card */
    overflow: hidden;
    isolation: isolate;
    box-shadow: 0 1px 3px rgba(15, 17, 22, 0.08), 0 4px 14px rgba(15, 17, 22, 0.06);
  }
  .storm-shelter.is-active { display: block; }
  /* Body backgrounds (when expanded). The colored head sits flush on top. */
  .storm-shelter[data-tier="watch"]     { background: #fff; }
  .storm-shelter[data-tier="warning"]   { background: #fff; }
  .storm-shelter[data-tier="all-clear"] { background: #fff; }

  /* Marching-stripe overlay for warning state — sits IN the head bar
     itself now, not as a separate strip. Subtle diagonal motion that
     signals "this is live broadcast" not "this is decoration". */
  .storm-shelter[data-tier="warning"] .storm-shelter-head::before {
    content: '';
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(45deg,
      rgba(255, 255, 255, 0) 0px,
      rgba(255, 255, 255, 0) 14px,
      rgba(255, 255, 255, 0.10) 14px,
      rgba(255, 255, 255, 0.10) 24px);
    animation: shelterStripeMove 1.4s linear infinite;
    pointer-events: none;
  }
  .storm-shelter-head { position: relative; }
  .storm-shelter-head > * { position: relative; z-index: 1; }
  @keyframes shelterStripeMove {
    from { transform: translateX(0); }
    to   { transform: translateX(28px); }
  }
  /* Hide the standalone stripe element — the head IS the stripe now */
  .storm-shelter-stripe { display: none; }

  /* Banner header — always visible when shelter is active. Tier label,
     headline, and an "Open shelter / Close shelter" toggle. */
  /* The shelter head is now a single colored bar holding the alert
     title only. The whole bar is clickable; clicking it expands the
     body below. Sharp corners and dense monospace metadata give it
     EAS-banner DNA so it doesn't read as marketing — closer to the
     red bar that interrupts a TV broadcast than to a card.

     Compressed by design: title fits in one row at desktop sizes so
     the search bar stays above the fold even on a 720p laptop. */
  .storm-shelter-head {
    background: currentColor;
    color: #C2342B; /* fallback; overridden per tier below */
    padding: 12px 18px;
    display: flex; align-items: center; gap: 12px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    border: none;
    width: 100%;
    text-align: left;
    font-family: inherit;
    transition: filter 160ms ease;
  }
  .storm-shelter[data-tier="watch"]     .storm-shelter-head { color: #F5A524; }
  .storm-shelter[data-tier="warning"]   .storm-shelter-head { color: #C2342B; }
  .storm-shelter[data-tier="all-clear"] .storm-shelter-head { color: #16C77E; }
  .storm-shelter-head:hover { filter: brightness(0.92); }
  .storm-shelter-head:focus-visible {
    outline: 3px solid rgba(255, 255, 255, 0.85);
    outline-offset: -3px;
  }

  /* Icon — small, white-on-color, the EAS triangle visual */
  .storm-shelter-head-icon {
    flex-shrink: 0;
    width: 26px; height: 26px;
    display: flex; align-items: center; justify-content: center;
    color: #fff;
  }
  /* Pulsing icon during warning state — same urgency cue as before
     but now on the colored bar background, not a separate badge. */
  .storm-shelter[data-tier="warning"] .storm-shelter-head-icon {
    animation: shelterIconBeat 1.2s ease-in-out infinite;
  }
  @keyframes shelterIconBeat {
    0%, 100% { transform: scale(1); }
    50%      { transform: scale(1.18); }
  }

  /* The label cluster — flexes between desktop (one row) and mobile
     (wraps gracefully). Tier name + location + time, all monospace. */
  .storm-shelter-head-text {
    flex: 1; min-width: 0;
    display: flex; align-items: baseline; gap: 14px;
    flex-wrap: wrap;
    color: #fff;
  }
  .storm-shelter-tier-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: #fff;
    display: inline-flex; align-items: center; gap: 8px;
    white-space: nowrap;
  }
  .storm-shelter-tier-pulse {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.95);
    animation: shelterDotPulse 1.4s ease-in-out infinite;
  }
  @keyframes shelterDotPulse {
    0%, 100% { opacity: 0.55; transform: scale(0.85); }
    50%      { opacity: 1; transform: scale(1.2); }
  }
  /* Separator between the label and location/time — a thin vertical
     bar like an EAS broadcast banner */
  .storm-shelter-head-sep {
    width: 1px;
    align-self: stretch;
    background: rgba(255, 255, 255, 0.30);
  }
  .storm-shelter-head-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.6px;
    color: rgba(255, 255, 255, 0.92);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* Right-side: the chevron only (no text label — keeps the bar
     compact and doesn't compete with the alert title) */
  .storm-shelter-toggle-chev {
    flex-shrink: 0;
    color: rgba(255, 255, 255, 0.85);
    transition: transform 220ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .storm-shelter.is-open .storm-shelter-toggle-chev {
    transform: rotate(180deg);
  }

  /* Headline + sub now live inside the body, shown only when
     expanded. Larger and warmer than they were before since they
     have more space here. */
  .storm-shelter-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 24px;
    font-weight: 600;
    line-height: 1.2;
    color: #0E1116;
    margin: 0 0 6px;
    letter-spacing: -0.3px;
  }
  .storm-shelter-sub {
    font-size: 14px;
    line-height: 1.5;
    color: rgba(15, 17, 22, 0.72);
    margin: 0 0 22px;
  }

  /* Body — only visible when shelter is open */
  .storm-shelter-body {
    display: none;
    border-top: 1px solid rgba(15, 17, 22, 0.08);
    padding: 22px 24px 28px;
    background: rgba(255, 255, 255, 0.55);
  }
  .storm-shelter.is-open .storm-shelter-body { display: block; }

  /* Met-watching attribution row — sits at top of body */
  .storm-shelter-met-row {
    display: flex; align-items: center; gap: 12px;
    padding: 12px 16px;
    background: rgba(15, 17, 22, 0.04);
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 10px;
    margin-bottom: 18px;
  }
  .storm-shelter-met-avatar {
    flex-shrink: 0;
    width: 36px; height: 36px;
    border-radius: 50%;
    background: linear-gradient(135deg, #4169E1, #2447b0);
    color: #fff;
    display: flex; align-items: center; justify-content: center;
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.4px;
  }
  .storm-shelter-met-info { flex: 1; }
  .storm-shelter-met-name {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 1px;
    line-height: 1.3;
  }
  .storm-shelter-met-status {
    font-size: 12px;
    color: rgba(15, 17, 22, 0.60);
    margin: 0;
    line-height: 1.35;
  }
  .storm-shelter-met-presence {
    flex-shrink: 0;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #16C77E;
    box-shadow: 0 0 8px rgba(22, 199, 126, 0.6);
    animation: shelterDotPulse 1.6s ease-in-out infinite;
  }

  /* Met-override-active indicator (shown when a Met has edited the
     content). Lives just below the met row when active. */
  .storm-shelter-override-pill {
    display: none;
    align-items: center; gap: 8px;
    padding: 8px 14px;
    background: rgba(65, 105, 225, 0.08);
    border: 1px solid rgba(65, 105, 225, 0.25);
    border-radius: 999px;
    font-size: 11.5px;
    font-weight: 600;
    color: #2447b0;
    margin-bottom: 16px;
  }
  .storm-shelter-override-pill.is-visible { display: inline-flex; }
  .storm-shelter-override-pill-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: #4169E1;
  }

  /* Phase tabs — clearly clickable now. Three pills with active-state
     underline, hover state, raised. */
  .storm-shelter-phases {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 6px;
    background: rgba(15, 17, 22, 0.04);
    padding: 4px;
    border-radius: 12px;
    margin-bottom: 22px;
  }
  .storm-shelter-phase-tab {
    padding: 10px 8px;
    background: transparent;
    border: none;
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.55);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
    text-align: center;
    line-height: 1.3;
    display: flex; flex-direction: column; align-items: center; gap: 2px;
  }
  .storm-shelter-phase-tab-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 8.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.45);
  }
  .storm-shelter-phase-tab:hover {
    background: rgba(255, 255, 255, 0.55);
    color: rgba(15, 17, 22, 0.85);
  }
  .storm-shelter-phase-tab.is-active {
    background: #fff;
    color: #0E1116;
    box-shadow: 0 1px 3px rgba(15, 17, 22, 0.08);
  }
  .storm-shelter-phase-tab.is-active .storm-shelter-phase-tab-eyebrow {
    color: rgba(15, 17, 22, 0.65);
  }
  /* Active tab gets a colored underline matching the alert tier */
  .storm-shelter-phase-tab.is-active::after {
    content: '';
    display: block;
    width: 28px; height: 2px;
    margin-top: 4px;
    border-radius: 2px;
    background: currentColor;
  }
  .storm-shelter[data-tier="watch"]     .storm-shelter-phase-tab.is-active { color: #8a5a0a; }
  .storm-shelter[data-tier="warning"]   .storm-shelter-phase-tab.is-active { color: #9b2823; }
  .storm-shelter[data-tier="all-clear"] .storm-shelter-phase-tab.is-active { color: #0a6b3a; }

  /* Phase content panels */
  .storm-shelter-phase-content { display: none; }
  .storm-shelter-phase-content.is-active {
    display: block;
    animation: shelterPhaseFade 280ms ease;
  }
  @keyframes shelterPhaseFade {
    from { opacity: 0; transform: translateY(4px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .storm-shelter-section { margin-bottom: 22px; }
  .storm-shelter-section:last-child { margin-bottom: 0; }
  .storm-shelter-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin: 0 0 8px;
  }
  .storm-shelter-section-text {
    font-size: 14px;
    line-height: 1.55;
    color: #1c2026;
    margin: 0;
  }
  .storm-shelter-section-text strong {
    color: #0E1116;
    font-weight: 600;
  }

  /* Live update slot — for radar loops, livestream, or Met video.
     During phase only. Currently shows a placeholder. */
  .storm-shelter-live-slot {
    background: rgba(15, 17, 22, 0.04);
    border: 1px dashed rgba(15, 17, 22, 0.18);
    border-radius: 10px;
    padding: 28px 16px;
    text-align: center;
    margin-bottom: 18px;
  }
  .storm-shelter-live-slot-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.45);
    margin: 0 0 4px;
  }
  .storm-shelter-live-slot-text {
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.55);
    margin: 0;
  }

  /* Crew nearby strip */
  .storm-shelter-crew-strip {
    display: flex; flex-wrap: wrap; gap: 8px;
    padding: 12px 14px;
    background: rgba(15, 17, 22, 0.03);
    border-radius: 10px;
  }
  .storm-shelter-crew-pill {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 4px 10px 4px 4px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 999px;
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.80);
  }
  .storm-shelter-crew-avatar {
    width: 22px; height: 22px;
    border-radius: 50%;
    background: linear-gradient(135deg, #4169E1, #2447b0);
    color: #fff;
    display: flex; align-items: center; justify-content: center;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
  }

  /* Prep checklist */
  .storm-shelter-checklist {
    list-style: none;
    padding: 0; margin: 0;
    display: flex; flex-direction: column; gap: 8px;
  }
  .storm-shelter-checklist li {
    display: flex; align-items: flex-start; gap: 10px;
    padding: 10px 14px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 10px;
    font-size: 13px;
    line-height: 1.45;
    color: #1c2026;
  }
  .storm-shelter-checklist-icon {
    flex-shrink: 0;
    margin-top: 2px;
  }
  .storm-shelter[data-tier="watch"]     .storm-shelter-checklist-icon { color: #c2870e; }
  .storm-shelter[data-tier="warning"]   .storm-shelter-checklist-icon { color: #9b2823; }
  .storm-shelter[data-tier="all-clear"] .storm-shelter-checklist-icon { color: #0a6b3a; }

  /* Damage check (After phase) */
  .storm-shelter-damage-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
    margin-bottom: 12px;
  }
  @media (max-width: 480px) {
    .storm-shelter-damage-row { grid-template-columns: 1fr; }
  }
  .storm-shelter-damage-btn {
    padding: 14px 12px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 10px;
    color: #0E1116;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 500;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
    text-align: center;
  }
  .storm-shelter-damage-btn:hover {
    border-color: rgba(15, 17, 22, 0.30);
    background: rgba(15, 17, 22, 0.02);
  }
  .storm-shelter-damage-btn.is-selected {
    border-color: #16C77E;
    background: rgba(22, 199, 126, 0.08);
    color: #0a6b3a;
  }
  .storm-shelter-damage-icon {
    display: block;
    font-size: 22px;
    margin-bottom: 4px;
  }

  /* All-Clear celebration: two suns gently swaying.
     Pure CSS — opposite-direction sway animations on the sun emoji.
     Only runs in the all-clear tier; subtle, dignified, "you're safe
     to go do your thing." */
  .storm-shelter-flags {
    display: none;
    align-items: center; justify-content: center;
    gap: 22px;
    padding: 18px 0 6px;
  }
  .storm-shelter[data-tier="all-clear"].is-open .storm-shelter-flags { display: flex; }
  .storm-shelter-flag {
    font-size: 32px;
    line-height: 1;
    display: inline-block;
    transform-origin: bottom center;
  }
  .storm-shelter-flag-left  { animation: flagWaveLeft  2.4s ease-in-out infinite; }
  .storm-shelter-flag-right { animation: flagWaveRight 2.4s ease-in-out infinite; }
  @keyframes flagWaveLeft {
    0%, 100% { transform: rotate(-8deg); }
    50%      { transform: rotate(12deg); }
  }
  @keyframes flagWaveRight {
    0%, 100% { transform: rotate(8deg); }
    50%      { transform: rotate(-12deg); }
  }
  .storm-shelter-flags-text {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px;
    font-weight: 600;
    color: #0a6b3a;
    text-align: center;
    letter-spacing: -0.2px;
  }

  /* Acknowledge button (All Clear) */
  .storm-shelter-ack-btn {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 11px 22px;
    background: #16C77E;
    border: none;
    border-radius: 999px;
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    margin: 16px auto 0;
    box-shadow: 0 2px 8px rgba(22, 199, 126, 0.30);
    transition: all 160ms ease;
  }
  .storm-shelter-ack-btn:hover {
    background: #14b372;
    box-shadow: 0 4px 14px rgba(22, 199, 126, 0.40);
    transform: translateY(-1px);
  }
  .storm-shelter-ack-wrap {
    display: flex; justify-content: center;
  }

  /* ════════════════════════════════════════════════════════════════════════
     ADMIN VIEW
     ════════════════════════════════════════════════════════════════════════ */
  .admin-screen {
    background: #f7f8fb;
    min-height: 100vh;
    padding-bottom: 60px;
  }
  .admin-header {
    background: #fff;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    padding: 18px 20px;
  }
  .admin-header-top {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 4px;
  }
  .admin-header-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    margin: 0;
  }
  .admin-header-sub {
    font-size: 13px;
    color: rgba(15, 17, 22, 0.55);
    margin: 0;
  }
  .admin-body {
    padding: 20px;
    max-width: 760px;
    margin: 0 auto;
  }

  .admin-summary-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
    margin-bottom: 24px;
  }
  @media (min-width: 540px) {
    .admin-summary-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  /* ── Personal welcome panel ────────────────────────────────────────
     A small but meaningful nod that this dashboard belongs to a person,
     not just to "an admin." Sits at the very top of the body. The
     greeting line is time-of-day-aware (good morning / afternoon /
     evening); the subtitle gives a quick sense of the day's shape. */
  .admin-welcome {
    margin: 4px 0 22px;
    padding: 18px 22px;
    background: linear-gradient(135deg,
      rgba(65, 105, 225, 0.04),
      rgba(65, 105, 225, 0.07));
    border-radius: 14px;
    border-left: 3px solid var(--rb, #4169E1);
  }
  .admin-welcome-greeting {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 4px;
    letter-spacing: -0.2px;
  }
  .admin-welcome-subtitle {
    font-size: 13.5px;
    color: rgba(15, 17, 22, 0.65);
    margin: 0;
    line-height: 1.5;
  }

  /* ── Met activity panel ────────────────────────────────────────────
     Who's online, who's working, who's idle. Real-time-feeling status
     pills with avatar/initial chips. Two-line rows for density. */
  .admin-met-list {
    display: flex; flex-direction: column;
    gap: 10px;
  }
  .admin-met-row {
    display: flex; align-items: center; gap: 12px;
    padding: 10px 4px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.05);
  }
  .admin-met-row:last-child { border-bottom: none; }
  .admin-met-avatar {
    flex-shrink: 0;
    width: 36px; height: 36px;
    border-radius: 50%;
    background: linear-gradient(135deg, #4169E1, #6c8aff);
    color: #fff;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.3px;
  }
  .admin-met-info {
    flex: 1;
    min-width: 0;
  }
  .admin-met-name {
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
    margin: 0;
  }
  .admin-met-detail {
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
    margin: 2px 0 0;
  }
  .admin-met-status {
    flex-shrink: 0;
    display: flex; align-items: center; gap: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
  }
  .admin-met-status-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
  }
  .admin-met-status.status-active   { color: #0c7a3c; }
  .admin-met-status.status-active   .admin-met-status-dot { background: #16C77E; box-shadow: 0 0 0 3px rgba(22, 199, 126, 0.18); }
  .admin-met-status.status-working  { color: #2E4FB8; }
  .admin-met-status.status-working  .admin-met-status-dot { background: #4169E1; box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.18); }
  .admin-met-status.status-idle     { color: rgba(15, 17, 22, 0.45); }
  .admin-met-status.status-idle     .admin-met-status-dot { background: rgba(15, 17, 22, 0.30); }
  .admin-met-status.status-offline  { color: rgba(15, 17, 22, 0.40); }
  .admin-met-status.status-offline  .admin-met-status-dot { background: rgba(15, 17, 22, 0.20); border: 1px solid rgba(15, 17, 22, 0.30); }

  /* ── User feedback panel ──────────────────────────────────────────
     Aggregate thumbs up/down rate plus a sample of recent comments.
     The big number is the lede; the comments are the proof. */
  .admin-feedback-summary {
    display: flex; align-items: center; gap: 18px;
    padding: 4px 0 14px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    margin-bottom: 14px;
  }
  .admin-feedback-pct {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 32px;
    font-weight: 600;
    color: #0c7a3c;
    line-height: 1;
  }
  .admin-feedback-pct-label {
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
    margin-top: 4px;
  }
  .admin-feedback-meta {
    flex: 1;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.70);
    line-height: 1.5;
  }
  .admin-feedback-comment {
    padding: 10px 0;
    border-bottom: 1px dashed rgba(15, 17, 22, 0.06);
    font-size: 13px;
    color: rgba(15, 17, 22, 0.78);
    line-height: 1.5;
  }
  .admin-feedback-comment:last-child { border-bottom: none; }
  .admin-feedback-comment-meta {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.5px;
    color: rgba(15, 17, 22, 0.45);
    margin-top: 4px;
    text-transform: uppercase;
  }
  .admin-feedback-thumb-up {
    color: #0c7a3c;
    font-weight: 600;
  }
  .admin-feedback-thumb-down {
    color: #9b2823;
    font-weight: 600;
  }

  /* ── Crew leaderboard ────────────────────────────────────────────
     Top contributors this week. Rank, handle, region, contributions.
     Worth styling to feel like recognition, not just data. */
  .admin-crew-list {
    display: flex; flex-direction: column;
  }
  .admin-crew-row {
    display: flex; align-items: center; gap: 12px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(15, 17, 22, 0.05);
  }
  .admin-crew-row:last-child { border-bottom: none; }
  .admin-crew-rank {
    flex-shrink: 0;
    width: 28px; height: 28px;
    border-radius: 50%;
    background: rgba(15, 17, 22, 0.06);
    color: rgba(15, 17, 22, 0.65);
    display: flex; align-items: center; justify-content: center;
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    font-weight: 700;
  }
  .admin-crew-rank.rank-1 { background: linear-gradient(135deg, #FFD53E, #FFB30A); color: #6b4400; }
  .admin-crew-rank.rank-2 { background: linear-gradient(135deg, #d6dde5, #b6c0cb); color: #2c3a4a; }
  .admin-crew-rank.rank-3 { background: linear-gradient(135deg, #d8a47f, #b87a4f); color: #4a2810; }
  .admin-crew-info {
    flex: 1;
    min-width: 0;
  }
  .admin-crew-handle {
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
    margin: 0;
  }
  .admin-crew-region {
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
    margin: 2px 0 0;
  }
  .admin-crew-count {
    flex-shrink: 0;
    text-align: right;
  }
  .admin-crew-count-num {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px;
    font-weight: 600;
    color: #0E1116;
  }
  .admin-crew-count-label {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin-top: 1px;
  }

  /* ── Reports placeholder ────────────────────────────────────────
     A friendly "this section will be richer once we have real data"
     placeholder. Better to acknowledge the gap than show fake charts
     that imply more than they should. */
  .admin-reports-placeholder {
    text-align: center;
    padding: 28px 20px;
  }
  .admin-reports-icon {
    width: 44px; height: 44px;
    margin: 0 auto 12px;
    color: rgba(65, 105, 225, 0.50);
  }
  .admin-reports-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 17px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 6px;
  }
  .admin-reports-detail {
    font-size: 13px;
    line-height: 1.5;
    color: rgba(15, 17, 22, 0.65);
    max-width: 420px;
    margin: 0 auto;
  }

  /* ════════════════════════════════════════════════════════════════════
     COMMAND CENTER (Admin view, v2)
     ════════════════════════════════════════════════════════════════════
     A dark-theme dashboard for the operator. This is intentionally
     distinct from the customer-facing prototype — same fonts, same
     Royal Blue accent, but inverted to a deep navy canvas with
     softly-glowing tiles. The goal is "Tesla command center," not
     "1980s submarine bridge": rounded edges, breathing room, soft
     glow rather than hard outlines.

     Tiles use class .cmd-tile and are arranged in a CSS grid that
     reflows on smaller screens. Numerics use JetBrains Mono;
     headlines use Fraunces; everything else is Inter. */

  /* The screen shell — replaces the light .admin-screen treatment */
  .admin-screen.cmd-center {
    background: radial-gradient(ellipse at top, #142036 0%, #0B1424 50%, #070C16 100%);
    color: rgba(255, 255, 255, 0.92);
    min-height: 100vh;
    padding-bottom: 60px;
    /* Subtle scanline effect — like a real ops display, but very faint
       so it doesn't fatigue the eye. Disabled if the user prefers
       reduced motion. */
    position: relative;
  }
  .admin-screen.cmd-center::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: repeating-linear-gradient(
      to bottom,
      rgba(255, 255, 255, 0.012) 0px,
      rgba(255, 255, 255, 0.012) 1px,
      transparent 1px,
      transparent 3px
    );
    pointer-events: none;
    z-index: 1;
  }
  .admin-screen.cmd-center > * { position: relative; z-index: 2; }
  @media (prefers-reduced-motion: reduce) {
    .admin-screen.cmd-center::before { display: none; }
  }

  /* The top header strip — operator name, timestamp, live indicator */
  .cmd-header {
    background: linear-gradient(180deg, rgba(20, 32, 54, 0.85), rgba(11, 20, 36, 0.6));
    border-bottom: 1px solid rgba(90, 130, 255, 0.15);
    padding: 18px 28px 16px;
    display: flex; align-items: center; justify-content: space-between;
    gap: 16px;
    flex-wrap: wrap;
    backdrop-filter: blur(6px);
  }
  .cmd-header-brand {
    display: flex; align-items: baseline; gap: 14px;
  }
  .cmd-header-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: 0.4px;
    color: #fff;
    margin: 0;
  }
  .cmd-header-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: rgba(90, 130, 255, 0.85);
  }
  .cmd-header-meta {
    display: flex; align-items: center; gap: 18px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.55);
  }
  .cmd-header-meta-item {
    display: flex; align-items: center; gap: 6px;
  }
  .cmd-header-meta-label {
    color: rgba(255, 255, 255, 0.40);
  }
  .cmd-header-meta-value {
    color: rgba(255, 255, 255, 0.85);
  }
  .cmd-live-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #16C77E;
    box-shadow: 0 0 0 3px rgba(22, 199, 126, 0.18), 0 0 8px rgba(22, 199, 126, 0.6);
    animation: cmdLivePulse 2.5s ease-in-out infinite;
  }
  @keyframes cmdLivePulse {
    0%, 100% { opacity: 0.7; }
    50%      { opacity: 1; }
  }
  /* Exit button — small ghost-style button on the right of the command
     center header. Lets the admin return to the homepage. Other workspace
     screens have their own exit affordances; this is admin-specific. */
  .cmd-exit-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 12px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 999px;
    color: rgba(255, 255, 255, 0.80);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: background 140ms ease, color 140ms ease, border-color 140ms ease;
  }
  .cmd-exit-btn:hover {
    background: rgba(255, 255, 255, 0.12);
    color: #fff;
    border-color: rgba(255, 255, 255, 0.28);
  }

  /* The body grid — the actual command-center canvas */
  .cmd-body {
    padding: 22px 28px 100px;
    max-width: 1480px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 14px;
  }

  /* Base tile — the visual primitive everything builds on */
  .cmd-tile {
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.045), rgba(255, 255, 255, 0.012)),
      rgba(20, 28, 48, 0.55);
    border: 1px solid rgba(90, 130, 255, 0.10);
    border-radius: 18px;
    padding: 16px 18px;
    position: relative;
    overflow: hidden;
    transition: border-color 220ms ease, transform 220ms ease;
    backdrop-filter: blur(4px);
  }
  .cmd-tile:hover {
    border-color: rgba(90, 130, 255, 0.22);
  }
  /* Soft inner highlight at the top edge for depth */
  .cmd-tile::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.05), transparent 40%);
    pointer-events: none;
  }
  .cmd-tile-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: rgba(90, 130, 255, 0.75);
    margin: 0 0 10px;
    display: flex; align-items: center; gap: 8px;
  }
  .cmd-tile-label-detail {
    color: rgba(255, 255, 255, 0.35);
    font-weight: 400;
    letter-spacing: 0.8px;
  }

  /* Grid spans for flexibility */
  .span-2  { grid-column: span 2; }
  .span-3  { grid-column: span 3; }
  .span-4  { grid-column: span 4; }
  .span-5  { grid-column: span 5; }
  .span-6  { grid-column: span 6; }
  .span-7  { grid-column: span 7; }
  .span-8  { grid-column: span 8; }
  .span-12 { grid-column: span 12; }
  @media (max-width: 1100px) {
    .span-2, .span-3 { grid-column: span 6; }
    .span-4, .span-5 { grid-column: span 6; }
    .span-7, .span-8 { grid-column: span 12; }
  }
  @media (max-width: 640px) {
    .cmd-body { padding: 14px; gap: 10px; }
    .span-2, .span-3, .span-4, .span-5, .span-6, .span-7, .span-8 {
      grid-column: span 12;
    }
  }

  /* ── Metric tiles (the top "vitals" row) ─────────────────────── */
  .cmd-metric-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 30px;
    font-weight: 600;
    color: #fff;
    line-height: 1;
    letter-spacing: -0.5px;
    margin: 4px 0 6px;
  }
  .cmd-metric-value-unit {
    font-size: 16px;
    color: rgba(255, 255, 255, 0.45);
    margin-left: 2px;
  }
  .cmd-metric-delta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    letter-spacing: 0.6px;
    color: rgba(255, 255, 255, 0.55);
  }
  .cmd-metric-delta.delta-up   { color: #5DECA8; }
  .cmd-metric-delta.delta-down { color: #FF7A82; }
  .cmd-metric-context {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 11.5px;
    color: rgba(255, 255, 255, 0.50);
    margin-top: 6px;
  }

  /* ── Review queue inside a tile ──────────────────────────────── */
  .cmd-queue-list {
    display: flex; flex-direction: column;
  }
  .cmd-queue-row {
    display: flex; align-items: center; gap: 10px;
    padding: 9px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    font-size: 12.5px;
  }
  .cmd-queue-row:last-child { border-bottom: none; }
  .cmd-queue-tier {
    flex-shrink: 0;
    padding: 3px 7px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
  }
  .cmd-queue-tier.tier-single { background: rgba(255, 255, 255, 0.08); color: rgba(255,255,255,0.7); }
  .cmd-queue-tier.tier-day    { background: rgba(90, 130, 255, 0.15);  color: #94B0FF; }
  .cmd-queue-tier.tier-pro    { background: rgba(22, 199, 126, 0.15);  color: #5DECA8; }
  .cmd-queue-plan {
    flex: 1; min-width: 0;
    color: rgba(255, 255, 255, 0.85);
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .cmd-queue-met {
    flex-shrink: 0;
    color: rgba(255, 255, 255, 0.45);
    font-size: 11.5px;
  }
  .cmd-queue-status {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    padding: 3px 7px;
    border-radius: 6px;
  }
  .cmd-queue-status.status-pending   { background: rgba(245, 165, 36, 0.16); color: #FFC366; }
  .cmd-queue-status.status-claimed   { background: rgba(90, 130, 255, 0.15); color: #94B0FF; }
  .cmd-queue-status.status-delivered { background: rgba(22, 199, 126, 0.15); color: #5DECA8; }

  /* ── Met activity inside a tile ──────────────────────────────── */
  .cmd-met-list {
    display: flex; flex-direction: column; gap: 8px;
  }
  .cmd-met-row {
    display: flex; align-items: center; gap: 10px;
    padding: 8px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
  }
  .cmd-met-row:last-child { border-bottom: none; }
  .cmd-met-avatar {
    flex-shrink: 0;
    width: 32px; height: 32px;
    border-radius: 50%;
    background: linear-gradient(135deg, #5A82FF, #9CB0FF);
    color: #0B1424;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Inter', sans-serif;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.3px;
  }
  .cmd-met-info { flex: 1; min-width: 0; }
  .cmd-met-name {
    font-size: 13px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.92);
    margin: 0;
  }
  .cmd-met-detail {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.45);
    margin: 1px 0 0;
  }
  .cmd-met-fatigue {
    flex-shrink: 0;
    width: 8px; height: 8px;
    border-radius: 50%;
    margin-right: 4px;
  }
  .cmd-met-fatigue.fatigue-green  { background: #5DECA8; box-shadow: 0 0 6px rgba(93, 236, 168, 0.5); }
  .cmd-met-fatigue.fatigue-yellow { background: #FFC366; box-shadow: 0 0 6px rgba(255, 195, 102, 0.5); }
  .cmd-met-fatigue.fatigue-red    { background: #FF7A82; box-shadow: 0 0 6px rgba(255, 122, 130, 0.6); animation: cmdFatigueFlash 1.4s ease-in-out infinite; }
  @keyframes cmdFatigueFlash {
    0%, 100% { opacity: 0.7; }
    50%      { opacity: 1; }
  }
  .cmd-met-status-pill {
    flex-shrink: 0;
    display: flex; align-items: center; gap: 5px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
  }
  .cmd-met-status-pill.is-reviewing { color: #94B0FF; }
  .cmd-met-status-pill.is-online    { color: #5DECA8; }
  .cmd-met-status-pill.is-offline   { color: rgba(255, 255, 255, 0.35); }

  /* ── Shadow Mode delta tile ──────────────────────────────────── */
  .cmd-shadow-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 20px;
    font-weight: 600;
    color: #fff;
    line-height: 1.25;
    margin: 4px 0 8px;
  }
  .cmd-shadow-headline em {
    color: #5DECA8;
    font-style: normal;
    font-family: 'JetBrains Mono', monospace;
    font-size: 18px;
    letter-spacing: -0.3px;
  }
  .cmd-shadow-detail {
    font-size: 12px;
    color: rgba(255, 255, 255, 0.55);
    line-height: 1.5;
    margin: 0;
  }
  .cmd-shadow-bars {
    display: flex; gap: 3px; height: 6px;
    margin: 14px 0 6px;
    border-radius: 999px; overflow: hidden;
  }
  .cmd-shadow-bar {
    flex: 1;
    background: rgba(255, 255, 255, 0.06);
  }
  .cmd-shadow-bar.is-saved { background: linear-gradient(90deg, #16C77E, #5DECA8); }
  .cmd-shadow-bar.is-upgrade { background: linear-gradient(90deg, #F5A524, #FFC366); }

  /* ── Challenges tile (formerly "Dispute Desk") ───────────────── */
  .cmd-challenges-empty {
    display: flex; align-items: center; gap: 10px;
    padding: 14px 4px;
  }
  .cmd-challenges-check {
    width: 28px; height: 28px;
    border-radius: 50%;
    background: rgba(22, 199, 126, 0.12);
    display: flex; align-items: center; justify-content: center;
    color: #5DECA8;
  }
  .cmd-challenges-empty-text {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.70);
  }
  .cmd-challenges-empty-text strong {
    color: rgba(255, 255, 255, 0.92);
    font-weight: 600;
  }

  /* ── Hot leads tile ──────────────────────────────────────────── */
  .cmd-leads-list {
    display: flex; flex-direction: column;
  }
  .cmd-leads-row {
    display: flex; align-items: center; gap: 10px;
    padding: 8px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    font-size: 12.5px;
  }
  .cmd-leads-row:last-child { border-bottom: none; }
  .cmd-leads-pulse {
    flex-shrink: 0;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #FFC366;
    box-shadow: 0 0 6px rgba(255, 195, 102, 0.6);
  }
  .cmd-leads-query {
    flex: 1; min-width: 0;
    color: rgba(255, 255, 255, 0.85);
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }
  .cmd-leads-meta {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(255, 255, 255, 0.40);
  }

  /* ── Wins (the Nod) feed ─────────────────────────────────────── */
  .cmd-wins-list {
    display: flex; flex-direction: column;
  }
  .cmd-wins-row {
    padding: 10px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
  }
  .cmd-wins-row:last-child { border-bottom: none; }
  .cmd-wins-quote {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 13.5px;
    font-style: italic;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.45;
    margin: 0 0 4px;
  }
  .cmd-wins-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.40);
  }

  /* ── Coverage gap (24h bar) ─────────────────────────────────── */
  .cmd-coverage-bars {
    display: grid;
    grid-template-columns: repeat(24, 1fr);
    gap: 2px;
    height: 38px;
    margin: 8px 0 10px;
  }
  .cmd-coverage-cell {
    border-radius: 3px;
    background: rgba(255, 255, 255, 0.05);
  }
  .cmd-coverage-cell.staff-3 { background: linear-gradient(180deg, #5DECA8, #16C77E); }
  .cmd-coverage-cell.staff-2 { background: linear-gradient(180deg, #94B0FF, #5A82FF); }
  .cmd-coverage-cell.staff-1 { background: linear-gradient(180deg, #FFC366, #F5A524); }
  .cmd-coverage-cell.staff-0 { background: linear-gradient(180deg, #FF7A82, #C2342B); }
  .cmd-coverage-axis {
    display: flex; justify-content: space-between;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    color: rgba(255, 255, 255, 0.40);
    margin-bottom: 4px;
  }
  .cmd-coverage-warn {
    display: flex; align-items: flex-start; gap: 8px;
    margin-top: 10px;
    padding: 9px 10px;
    background: rgba(255, 122, 130, 0.08);
    border: 1px solid rgba(255, 122, 130, 0.18);
    border-radius: 8px;
    font-size: 11.5px;
    color: rgba(255, 200, 205, 0.92);
    line-height: 1.4;
  }
  .cmd-coverage-warn-icon { flex-shrink: 0; color: #FF7A82; margin-top: 1px; }

  /* ── Search heatmap (placeholder visualization) ──────────────── */
  .cmd-heatmap {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 3px;
    margin-top: 4px;
  }
  .cmd-heatmap-cell {
    aspect-ratio: 1;
    border-radius: 4px;
    background: rgba(90, 130, 255, 0.06);
  }
  .cmd-heatmap-cell.heat-1 { background: rgba(90, 130, 255, 0.18); }
  .cmd-heatmap-cell.heat-2 { background: rgba(90, 130, 255, 0.32); }
  .cmd-heatmap-cell.heat-3 { background: rgba(90, 130, 255, 0.50); }
  .cmd-heatmap-cell.heat-4 { background: linear-gradient(135deg, #5A82FF, #94B0FF); box-shadow: 0 0 8px rgba(90, 130, 255, 0.4); }
  .cmd-heatmap-legend {
    display: flex; justify-content: space-between;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    color: rgba(255, 255, 255, 0.40);
    margin-top: 8px;
  }

  /* ── Missed opportunity tile ─────────────────────────────────── */
  .cmd-missed-list {
    display: flex; flex-direction: column;
  }
  .cmd-missed-row {
    display: flex; align-items: center; gap: 10px;
    padding: 7px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    font-size: 12.5px;
  }
  .cmd-missed-row:last-child { border-bottom: none; }
  .cmd-missed-location {
    flex: 1;
    color: rgba(255, 255, 255, 0.80);
  }
  .cmd-missed-count {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    color: rgba(90, 130, 255, 0.85);
  }

  /* ── Wins/recent activity tighter row format for dark ────────── */
  .cmd-activity-list {
    display: flex; flex-direction: column;
  }
  .cmd-activity-row {
    display: flex; align-items: center; gap: 10px;
    padding: 8px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    font-size: 12px;
  }
  .cmd-activity-row:last-child { border-bottom: none; }
  .cmd-activity-time {
    flex-shrink: 0;
    width: 46px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(255, 255, 255, 0.40);
  }
  .cmd-activity-icon {
    flex-shrink: 0;
    width: 22px; height: 22px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    background: rgba(90, 130, 255, 0.12);
    color: #94B0FF;
  }
  .cmd-activity-icon.kind-payment { background: rgba(22, 199, 126, 0.14); color: #5DECA8; }
  .cmd-activity-icon.kind-signup  { background: rgba(245, 165, 36, 0.14); color: #FFC366; }
  .cmd-activity-icon svg { width: 11px; height: 11px; }
  .cmd-activity-text {
    flex: 1;
    color: rgba(255, 255, 255, 0.78);
    overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  }

  /* ── Reports placeholder for dark ────────────────────────────── */
  .cmd-reports-empty {
    display: flex; flex-direction: column; align-items: center;
    text-align: center;
    padding: 22px 16px;
  }
  .cmd-reports-icon {
    width: 36px; height: 36px;
    color: rgba(90, 130, 255, 0.6);
    margin-bottom: 10px;
  }
  .cmd-reports-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 15px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.92);
    margin: 0 0 4px;
  }
  .cmd-reports-detail {
    font-size: 11.5px;
    color: rgba(255, 255, 255, 0.50);
    line-height: 1.5;
    max-width: 320px;
    margin: 0;
  }

  /* ── Force Alert button (fixed bottom-right) ─────────────────── */
  .cmd-force-alert {
    position: fixed;
    bottom: 24px;
    right: 28px;
    z-index: 50;
    background: linear-gradient(135deg, #C2342B, #FF5E68);
    color: #fff;
    border: 1px solid rgba(255, 122, 130, 0.4);
    border-radius: 999px;
    padding: 13px 22px;
    font-family: 'Inter', sans-serif;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.4px;
    cursor: pointer;
    display: flex; align-items: center; gap: 8px;
    box-shadow:
      0 4px 14px rgba(194, 52, 43, 0.45),
      0 0 0 4px rgba(194, 52, 43, 0.10),
      inset 0 1px 0 rgba(255, 255, 255, 0.18);
    transition: transform 160ms ease, box-shadow 220ms ease;
  }
  .cmd-force-alert:hover {
    transform: translateY(-1px);
    box-shadow:
      0 6px 18px rgba(194, 52, 43, 0.55),
      0 0 0 6px rgba(194, 52, 43, 0.10),
      inset 0 1px 0 rgba(255, 255, 255, 0.18);
  }
  .cmd-force-alert:active { transform: translateY(0); }
  .cmd-force-alert-icon {
    width: 14px; height: 14px;
  }

  /* Hide the Force Alert when not on the admin screen */
  body:not(.dev-view-admin) .cmd-force-alert { display: none; }

  /* Hide the dev-mode chip's overlap on the command center bottom-right */
  body.dev-view-admin .cmd-force-alert { display: flex; }

  /* ── Admin tab strip — sits between header and body ──
     Two tabs ("Dashboard", "Team"). Active tab gets Royal Blue underline.
     Visually consistent with cmd-header's deep navy palette. */
  .cmd-tabs {
    background: rgba(11, 20, 36, 0.6);
    border-bottom: 1px solid rgba(90, 130, 255, 0.10);
    padding: 0 28px;
    display: flex;
    gap: 4px;
  }
  .cmd-tab {
    background: transparent;
    border: none;
    color: rgba(255, 255, 255, 0.55);
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    padding: 14px 16px 12px;
    cursor: pointer;
    position: relative;
    transition: color 160ms ease;
  }
  .cmd-tab:hover { color: rgba(255, 255, 255, 0.85); }
  .cmd-tab.is-active { color: #fff; }
  .cmd-tab.is-active::after {
    content: '';
    position: absolute;
    left: 12px; right: 12px; bottom: -1px;
    height: 2px;
    background: #5A82FF;
    border-radius: 1px;
  }

  /* Tab panel visibility — only the active panel shows */
  .cmd-tab-panel { display: none; }
  .cmd-tab-panel.is-active { display: block; }

  /* ── Team panel — Met account management ──
     Sits inside cmd-body when the Team tab is active. Lives in the same
     deep-navy command-center palette as the Dashboard panel. */
  .team-panel {
    padding: 22px 28px 100px;
    max-width: 1480px;
    margin: 0 auto;
  }
  .team-panel-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 18px;
    gap: 14px; flex-wrap: wrap;
  }
  .team-panel-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    color: #fff;
    margin: 0;
  }
  .team-panel-sub {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.55);
    margin: 4px 0 0;
  }
  .team-add-btn {
    background: #4169E1;
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.10);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    padding: 9px 16px;
    border-radius: 10px;
    cursor: pointer;
    display: inline-flex; align-items: center; gap: 6px;
    transition: background 160ms ease, transform 100ms ease;
  }
  .team-add-btn:hover { background: #5277e8; }
  .team-add-btn:active { transform: translateY(1px); }

  /* The user list — table-like but using rows for responsive control */
  .team-list {
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.045), rgba(255, 255, 255, 0.012)),
      rgba(20, 28, 48, 0.55);
    border: 1px solid rgba(90, 130, 255, 0.10);
    border-radius: 14px;
    overflow: hidden;
  }
  .team-list-header,
  .team-list-row {
    display: grid;
    grid-template-columns: 1.6fr 2fr 1fr 1.2fr 1fr 120px;
    gap: 14px;
    padding: 12px 18px;
    align-items: center;
  }
  .team-list-header {
    background: rgba(90, 130, 255, 0.06);
    border-bottom: 1px solid rgba(90, 130, 255, 0.12);
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.55);
  }
  .team-list-row {
    border-bottom: 1px solid rgba(90, 130, 255, 0.06);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    color: rgba(255, 255, 255, 0.92);
  }
  .team-list-row:last-child { border-bottom: none; }
  .team-list-row.is-inactive { opacity: 0.45; }
  .team-list-name { font-weight: 500; }
  .team-list-email {
    font-family: 'JetBrains Mono', monospace;
    font-size: 12.5px;
    color: rgba(255, 255, 255, 0.65);
  }
  .team-list-role {
    display: inline-block;
    padding: 3px 9px;
    border-radius: 999px;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.3px;
  }
  .team-list-role.role-meteorologist { background: rgba(90, 130, 255, 0.16); color: #99B4FF; }
  .team-list-role.role-admin { background: rgba(245, 165, 36, 0.16); color: #F5C57A; }
  .team-list-role.role-crew { background: rgba(22, 199, 126, 0.16); color: #6FE0A8; }
  .team-list-meta {
    font-size: 12.5px;
    color: rgba(255, 255, 255, 0.55);
  }
  .team-list-status {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.6px;
    text-transform: uppercase;
  }
  .team-list-status.is-active { color: #6FE0A8; }
  .team-list-status.is-inactive { color: rgba(255, 255, 255, 0.40); }
  .team-list-actions {
    display: flex; gap: 6px; justify-content: flex-end;
  }
  .team-action-btn {
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.10);
    color: rgba(255, 255, 255, 0.85);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 500;
    padding: 5px 10px;
    border-radius: 7px;
    cursor: pointer;
    transition: background 140ms ease;
  }
  .team-action-btn:hover { background: rgba(255, 255, 255, 0.12); }
  .team-action-btn.is-danger { color: #FCA5A5; }
  .team-action-btn.is-danger:hover { background: rgba(229, 72, 77, 0.18); }

  .team-empty {
    padding: 48px 24px;
    text-align: center;
    color: rgba(255, 255, 255, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
  }

  /* ── Add/Edit Met modal ──
     Centered, dimmed background. Reuses the page's modal patterns but
     restyled for the command center's deep-navy aesthetic. */
  .team-modal-scrim {
    position: fixed; inset: 0;
    background: rgba(8, 14, 26, 0.72);
    backdrop-filter: blur(4px);
    z-index: 200;
    display: flex; align-items: center; justify-content: center;
    padding: 20px;
  }
  .team-modal-scrim[hidden] { display: none; }
  .team-modal {
    background: #14213A;
    border: 1px solid rgba(90, 130, 255, 0.20);
    border-radius: 16px;
    width: 100%;
    max-width: 460px;
    padding: 24px 26px;
    box-shadow: 0 24px 56px rgba(0, 0, 0, 0.50);
  }
  .team-modal-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 20px;
    color: #fff;
    margin: 0 0 4px;
  }
  .team-modal-sub {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.55);
    margin: 0 0 18px;
  }
  .team-modal-field { margin-bottom: 14px; }
  .team-modal-label {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.55);
    margin-bottom: 6px;
  }
  .team-modal-input,
  .team-modal-select {
    width: 100%;
    background: rgba(0, 0, 0, 0.25);
    border: 1px solid rgba(90, 130, 255, 0.18);
    border-radius: 9px;
    padding: 9px 12px;
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    transition: border-color 140ms ease;
  }
  .team-modal-input:focus,
  .team-modal-select:focus {
    outline: none;
    border-color: rgba(90, 130, 255, 0.50);
  }
  .team-modal-hint {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 11.5px;
    color: rgba(255, 255, 255, 0.45);
    margin-top: 5px;
  }
  .team-modal-error {
    background: rgba(229, 72, 77, 0.12);
    border: 1px solid rgba(229, 72, 77, 0.30);
    border-radius: 8px;
    padding: 8px 12px;
    color: #FCA5A5;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    margin-bottom: 14px;
  }
  .team-modal-error[hidden] { display: none; }
  .team-modal-actions {
    display: flex; gap: 8px; justify-content: flex-end;
    margin-top: 18px;
  }
  .team-modal-cancel {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.18);
    color: rgba(255, 255, 255, 0.75);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 500;
    padding: 9px 16px;
    border-radius: 9px;
    cursor: pointer;
  }
  .team-modal-cancel:hover { background: rgba(255, 255, 255, 0.06); }
  .team-modal-submit {
    background: #4169E1;
    border: 1px solid rgba(255, 255, 255, 0.10);
    color: #fff;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    padding: 9px 16px;
    border-radius: 9px;
    cursor: pointer;
    transition: background 140ms ease;
  }
  .team-modal-submit:hover { background: #5277e8; }
  .team-modal-submit:disabled {
    opacity: 0.55;
    cursor: not-allowed;
  }

  /* Temp password success card — shown after add or password reset */
  .team-temp-pw {
    background: rgba(22, 199, 126, 0.10);
    border: 1px solid rgba(22, 199, 126, 0.30);
    border-radius: 10px;
    padding: 12px 14px;
    margin-top: 12px;
  }
  .team-temp-pw-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: #6FE0A8;
    margin-bottom: 6px;
  }
  .team-temp-pw-value {
    font-family: 'JetBrains Mono', monospace;
    font-size: 17px;
    font-weight: 600;
    color: #fff;
    letter-spacing: 0.3px;
    word-break: break-all;
    user-select: all;
  }
  .team-temp-pw-hint {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    color: rgba(255, 255, 255, 0.65);
    margin-top: 8px;
    line-height: 1.5;
  }

  /* Responsive — collapse the user-list grid on narrow viewports */
  @media (max-width: 880px) {
    .team-list-header { display: none; }
    .team-list-row {
      grid-template-columns: 1fr;
      gap: 4px;
      padding: 14px 16px;
    }
    .team-list-actions { justify-content: flex-start; margin-top: 6px; }
  }

  .admin-summary-card {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 14px;
    padding: 14px;
    position: relative;
  }
  .admin-summary-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin-bottom: 6px;
  }
  .admin-summary-value {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 26px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.0;
  }
  .admin-summary-value-unit {
    font-size: 13px;
    color: rgba(15, 17, 22, 0.55);
    font-weight: 500;
    margin-left: 2px;
  }
  .admin-summary-sub {
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
    margin-top: 6px;
  }
  .admin-summary-sub.delta-up   { color: #0c7a3c; }
  .admin-summary-sub.delta-down { color: #9b2823; }

  .admin-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin: 28px 0 10px;
  }
  .admin-card {
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.06);
    border-radius: 14px;
    padding: 16px;
    margin-bottom: 16px;
  }

  /* Admin queue rows */
  .admin-queue-list {
    display: flex; flex-direction: column;
  }
  .admin-queue-row {
    display: flex; align-items: center; gap: 10px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    font-size: 13px;
  }
  .admin-queue-row:last-child { border-bottom: none; }
  .admin-queue-tier {
    flex-shrink: 0;
    padding: 3px 8px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
  }
  .admin-queue-tier.tier-single { background: rgba(15, 17, 22, 0.08); color: #0E1116; }
  .admin-queue-tier.tier-day    { background: rgba(65, 105, 225, 0.12); color: #2E4FB8; }
  .admin-queue-tier.tier-pro    { background: rgba(18, 161, 80, 0.14); color: #0c7a3c; }
  .admin-queue-plan {
    flex: 1;
    min-width: 0;
    font-size: 13px;
    color: #0E1116;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .admin-queue-met {
    flex-shrink: 0;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
  }
  .admin-queue-status {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    padding: 3px 7px;
    border-radius: 6px;
  }
  .admin-queue-status.status-pending  { background: rgba(232, 164, 0, 0.14); color: #8a5a0a; }
  .admin-queue-status.status-claimed  { background: rgba(65, 105, 225, 0.12); color: #2E4FB8; }
  .admin-queue-status.status-delivered { background: rgba(18, 161, 80, 0.14); color: #0c7a3c; }
  .admin-queue-status.status-stuck    { background: rgba(194, 52, 43, 0.10); color: #9b2823; }

  /* Disagreement rate card */
  .admin-disagreement {
    display: flex; align-items: center; gap: 16px;
  }
  .admin-disagreement-circle {
    width: 70px; height: 70px;
    border-radius: 50%;
    background: conic-gradient(var(--rb, #4169E1) 0%, var(--rb, #4169E1) 18%, rgba(15, 17, 22, 0.06) 18%, rgba(15, 17, 22, 0.06) 100%);
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    position: relative;
  }
  .admin-disagreement-circle::before {
    content: '';
    position: absolute;
    inset: 6px;
    background: #fff;
    border-radius: 50%;
  }
  .admin-disagreement-pct {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px;
    font-weight: 600;
    color: #0E1116;
    position: relative;
    z-index: 1;
  }
  .admin-disagreement-text {
    flex: 1;
  }
  .admin-disagreement-headline {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 15px;
    color: #0E1116;
    margin: 0 0 4px;
  }
  .admin-disagreement-detail {
    font-size: 12.5px;
    color: rgba(15, 17, 22, 0.60);
    line-height: 1.5;
    margin: 0;
  }

  /* Activity feed */
  .admin-activity-item {
    display: flex; align-items: flex-start; gap: 10px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(15, 17, 22, 0.04);
    font-size: 12.5px;
  }
  .admin-activity-item:last-child { border-bottom: none; }
  .admin-activity-time {
    flex-shrink: 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15, 17, 22, 0.45);
    width: 50px;
    padding-top: 2px;
  }
  .admin-activity-icon {
    flex-shrink: 0;
    width: 24px; height: 24px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: #fff;
  }
  .admin-activity-icon.kind-payment   { background: #12A150; }
  .admin-activity-icon.kind-signup    { background: var(--rb, #4169E1); }
  .admin-activity-icon.kind-brief     { background: #E8A400; }
  .admin-activity-icon.kind-report    { background: rgba(15, 17, 22, 0.55); }
  .admin-activity-icon svg {
    width: 12px; height: 12px;
  }
  .admin-activity-text {
    flex: 1;
    line-height: 1.45;
    color: #0E1116;
  }
  .admin-activity-text strong {
    font-weight: 600;
  }
  .admin-activity-meta {
    color: rgba(15, 17, 22, 0.55);
    font-size: 11.5px;
  }
  /* When admin-activity-meta is rendered inside the dark command
     center (.cmd-activity-row), override to a light color so the
     text remains readable. The original 0.55 dark-gray was the
     correct treatment on the legacy light-theme admin view, but
     disappears against the dark navy background. */
  .cmd-activity-row .admin-activity-meta {
    color: rgba(255, 255, 255, 0.50);
  }

  /* ── Badges ── */
  .crew-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; font-weight: 700;
    letter-spacing: 1.6px; text-transform: uppercase;
    color: rgba(15,17,22,0.55);
    margin: 32px 0 12px;
  }
  .badge-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(96px, 1fr));
    gap: 10px;
  }
  .badge {
    background: #fff;
    border: 1px solid rgba(15,17,22,0.08);
    border-radius: 14px;
    padding: 16px 12px 14px;
    text-align: center;
    transition: transform 160ms ease, box-shadow 160ms ease;
    position: relative;
    overflow: hidden;
  }
  /* Earned badges get a subtle radial highlight in the upper-left and a
     warmer shadow — communicates "this was earned" without being loud.
     Like the embossed seal on a paper certificate. */
  .badge:not(.locked)::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 30% 25%, rgba(255,255,255,0.85) 0%, transparent 55%);
    pointer-events: none;
    opacity: 0.9;
  }
  .badge:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 24px rgba(15,17,22,0.08);
  }
  .badge.locked {
    opacity: 0.45;
    background: #f5f6f8;
    border-style: dashed;
  }
  .badge.locked:hover {
    transform: none;  /* Don't tease — locked badges shouldn't lift on hover */
    box-shadow: none;
  }
  /* Newly-earned badge gets a small "NEW" ribbon and a soft glow.
     Used selectively — e.g. the Finish Saver badge in the demo data. */
  .badge.recent::after {
    content: 'NEW';
    position: absolute;
    top: 8px; right: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.8px;
    background: var(--rb, #4169E1);
    color: #fff;
    padding: 2px 5px;
    border-radius: 4px;
    box-shadow: 0 2px 6px rgba(65,105,225,0.30);
  }
  .badge.recent {
    box-shadow: 0 0 0 2px rgba(65,105,225,0.18), 0 8px 20px rgba(65,105,225,0.10);
  }
  .badge-icon {
    width: 40px; height: 40px;
    margin: 0 auto 8px;
    display: flex; align-items: center; justify-content: center;
    border-radius: 999px;
    position: relative;
    z-index: 1;
  }
  /* Icon backgrounds slightly richer than before — gradient + shadow */
  .badge-icon-storm   { background: linear-gradient(135deg, rgba(194,52,43,0.18) 0%, rgba(194,52,43,0.08) 100%); color: #9b2823; box-shadow: inset 0 0 0 1px rgba(194,52,43,0.20); }
  .badge-icon-eye     { background: linear-gradient(135deg, rgba(65,105,225,0.18) 0%, rgba(65,105,225,0.08) 100%); color: #1d4d8c; box-shadow: inset 0 0 0 1px rgba(65,105,225,0.20); }
  .badge-icon-streak  { background: linear-gradient(135deg, rgba(232,164,0,0.22) 0%, rgba(232,164,0,0.10) 100%); color: #8a5a0a; box-shadow: inset 0 0 0 1px rgba(232,164,0,0.22); }
  .badge-icon-volume  { background: linear-gradient(135deg, rgba(18,161,80,0.18) 0%, rgba(18,161,80,0.08) 100%); color: #0a6e35; box-shadow: inset 0 0 0 1px rgba(18,161,80,0.20); }
  .badge-icon-bird    { background: linear-gradient(135deg, rgba(255,210,140,0.55) 0%, rgba(232,164,0,0.20) 100%); color: #8a5a0a; box-shadow: inset 0 0 0 1px rgba(232,164,0,0.30); }
  .badge-icon-save    { background: linear-gradient(135deg, rgba(91,107,214,0.20) 0%, rgba(65,105,225,0.10) 100%); color: #2d4499; box-shadow: inset 0 0 0 1px rgba(91,107,214,0.25); }
  .badge-icon-locked  { background: rgba(15,17,22,0.06);   color: rgba(15,17,22,0.35); }

  .badge-name {
    font-size: 11.5px; font-weight: 700;
    color: #0E1116;
    letter-spacing: 0.2px;
    line-height: 1.25;
    position: relative;
    z-index: 1;
  }
  .badge.locked .badge-name { color: rgba(15,17,22,0.55); }
  .badge-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9px;
    color: rgba(15,17,22,0.45);
    margin-top: 3px;
    letter-spacing: 0.4px;
  }

  /* ── Crew action buttons ── */
  .crew-actions {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: 28px;
  }
  .crew-action {
    display: flex; align-items: center; gap: 14px;
    width: 100%;
    padding: 16px 18px;
    background: #fff;
    border: 1px solid rgba(15,17,22,0.08);
    border-radius: 14px;
    text-align: left;
    cursor: pointer;
    color: inherit;
    font: inherit;
    transition: border-color 120ms ease, transform 80ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-action:hover {
    border-color: rgba(65,105,225,0.30);
  }
  .crew-action:active { transform: scale(0.99); }
  .crew-action-icon {
    width: 40px; height: 40px;
    border-radius: 12px;
    background: rgba(65,105,225,0.10);
    color: var(--rb, #4169E1);
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  .crew-action-text { flex: 1; }
  .crew-action-title {
    font-size: 15px; font-weight: 600;
    color: #0E1116;
    margin-bottom: 2px;
  }
  .crew-action-sub {
    font-size: 12px;
    color: rgba(15,17,22,0.55);
  }
  .crew-action-arrow {
    color: rgba(15,17,22,0.35);
    flex-shrink: 0;
  }
  .crew-action-primary {
    background: var(--rb, #4169E1);
    border-color: var(--rb, #4169E1);
    color: #fff;
    box-shadow: 0 8px 24px rgba(65,105,225,0.22);
  }
  .crew-action-primary .crew-action-icon {
    background: rgba(255,255,255,0.18);
    color: #fff;
  }
  .crew-action-primary .crew-action-title { color: #fff; }
  .crew-action-primary .crew-action-sub { color: rgba(255,255,255,0.78); }
  .crew-action-primary .crew-action-arrow { color: rgba(255,255,255,0.65); }

  /* ── NWS overlay filter row (Chunk 6a) ──────────────────
     Sits above the Live Feed map. Three pills + a sub-menu
     for Advisories. Same pill chassis as other filter UI on
     the site — paper background, mono uppercase labels,
     active state = filled tint.

     The whole row scrolls horizontally on narrow viewports
     so the pills stay one line, not stacking awkwardly. */
  .nws-filter-row {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 6px 2px 14px;
    overflow-x: auto;
    overflow-y: visible;     /* let sub-menu pop above the row */
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
    position: relative;       /* anchor for sub-menu pop */
  }
  .nws-filter-row::-webkit-scrollbar { display: none; }
  .nws-filter-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    flex-shrink: 0;
    padding-right: 4px;
  }
  .nws-filter-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 11px 6px 10px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.60);
    cursor: pointer;
    flex-shrink: 0;
    transition: background 140ms ease, border-color 140ms ease,
                color 140ms ease, transform 80ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .nws-filter-pill:hover {
    border-color: rgba(15, 17, 22, 0.20);
  }
  .nws-filter-pill:active { transform: scale(0.97); }
  .nws-filter-pill[aria-pressed="true"] {
    background: #0E1116;
    border-color: #0E1116;
    color: #fff;
  }
  .nws-filter-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  .nws-filter-pill[aria-pressed="false"] .nws-filter-dot {
    opacity: 0.55;
  }
  .nws-filter-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    padding: 1px 6px;
    border-radius: 999px;
    background: rgba(15, 17, 22, 0.06);
    color: rgba(15, 17, 22, 0.50);
    min-width: 14px;
    text-align: center;
  }
  .nws-filter-pill[aria-pressed="true"] .nws-filter-count {
    background: rgba(255, 255, 255, 0.18);
    color: rgba(255, 255, 255, 0.85);
  }

  /* Sub-menu group — the Advisories pill plus its dropdown trigger.
     Position:relative so the dropdown can anchor to the group, not
     to the page. */
  .nws-filter-pill-group {
    display: inline-flex;
    align-items: stretch;
    position: relative;
    flex-shrink: 0;
  }
  .nws-filter-pill-group .nws-filter-pill {
    border-top-right-radius: 0;
    border-bottom-right-radius: 0;
    border-right: none;
  }
  .nws-filter-submenu-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0 9px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.10);
    border-radius: 0 999px 999px 0;
    color: rgba(15, 17, 22, 0.50);
    cursor: pointer;
    transition: background 140ms ease, border-color 140ms ease,
                color 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .nws-filter-submenu-btn:hover {
    border-color: rgba(15, 17, 22, 0.20);
    color: rgba(15, 17, 22, 0.85);
  }
  .nws-filter-pill-group .nws-filter-pill[aria-pressed="true"] + .nws-filter-submenu-btn {
    background: #0E1116;
    border-color: #0E1116;
    color: rgba(255, 255, 255, 0.85);
  }
  .nws-filter-submenu-btn[aria-expanded="true"] svg {
    transform: rotate(180deg);
  }
  .nws-filter-submenu-btn svg {
    transition: transform 200ms ease;
  }
  .nws-filter-submenu {
    position: absolute;
    top: calc(100% + 6px);
    right: 0;
    z-index: 20;
    min-width: 280px;
    padding: 12px 14px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.12);
    border-radius: 12px;
    box-shadow: 0 12px 28px rgba(15, 17, 22, 0.12);
  }
  .nws-filter-submenu-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.55);
    margin-bottom: 10px;
  }
  .nws-filter-submenu-option {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 6px 0;
    cursor: pointer;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    color: rgba(15, 17, 22, 0.85);
  }
  .nws-filter-submenu-option input {
    width: 15px;
    height: 15px;
    accent-color: #4169E1;
    flex-shrink: 0;
    cursor: pointer;
    margin: 0;
  }

  /* ── NWS overlay zones on the Live Feed map (Chunk 6a) ──
     Each warning/watch/advisory renders as a stylized rectangle
     positioned via projected percent coordinates on the SVG map.
     Behind the pulses, above the SVG background. Click → popup
     with the alert headline + expiry. */
  .feed-nws-overlay {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 2;  /* SVG bg = 1, pulses = 3 */
  }
  .feed-nws-zone {
    position: absolute;
    border-radius: 6px;
    pointer-events: auto;
    cursor: pointer;
    transition: transform 100ms ease, box-shadow 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .feed-nws-zone:hover {
    transform: scale(1.02);
    z-index: 3;
  }
  .feed-nws-zone[data-category="warning"] {
    background: rgba(194, 52, 43, 0.20);
    border: 1.5px solid rgba(194, 52, 43, 0.65);
    box-shadow: 0 0 12px rgba(194, 52, 43, 0.30);
    /* Subtle pulse so warnings draw the eye */
    animation: feedNwsWarningPulse 2.4s ease-in-out infinite;
  }
  .feed-nws-zone[data-category="watch"] {
    background: rgba(245, 165, 36, 0.18);
    border: 1.5px solid rgba(245, 165, 36, 0.65);
  }
  .feed-nws-zone[data-category="advisory"] {
    background: rgba(65, 105, 225, 0.15);
    border: 1.5px solid rgba(65, 105, 225, 0.55);
  }
  @keyframes feedNwsWarningPulse {
    0%, 100% { box-shadow: 0 0 12px rgba(194, 52, 43, 0.30); }
    50%      { box-shadow: 0 0 22px rgba(194, 52, 43, 0.55); }
  }

  /* Popup that floats above a tapped zone */
  .feed-nws-popup {
    position: absolute;
    z-index: 30;
    min-width: 220px;
    max-width: 300px;
    padding: 12px 14px;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.12);
    border-radius: 12px;
    box-shadow: 0 8px 24px rgba(15, 17, 22, 0.18);
    pointer-events: auto;
  }
  .feed-nws-popup-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    margin-bottom: 6px;
  }
  .feed-nws-popup-eyebrow[data-category="warning"]  { color: #8a2823; }
  .feed-nws-popup-eyebrow[data-category="watch"]    { color: #8a5a0a; }
  .feed-nws-popup-eyebrow[data-category="advisory"] { color: #1d3a8a; }
  .feed-nws-popup-headline {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13.5px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1.35;
    margin: 0 0 4px;
  }
  .feed-nws-popup-meta {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    color: rgba(15, 17, 22, 0.55);
    letter-spacing: 0.3px;
  }
  .feed-nws-popup-close {
    position: absolute;
    top: 6px;
    right: 6px;
    width: 24px;
    height: 24px;
    background: transparent;
    border: none;
    border-radius: 50%;
    color: rgba(15, 17, 22, 0.55);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .feed-nws-popup-close:hover {
    background: rgba(15, 17, 22, 0.06);
    color: #0E1116;
  }

  /* "Your own pulse" subtle distinguishing ring (Chunk 6a).
     Crew members see their own report pulses with a white-on-dark
     ring around the dot, so they can spot their work without it
     feeling gamey. */
  .feed-pulse.is-mine .feed-pulse-dot {
    box-shadow: 0 0 0 3px #fff,
                0 0 0 5px rgba(65, 105, 225, 0.55),
                0 0 0 7px rgba(15, 17, 22, 0.06),
                0 6px 16px rgba(15, 17, 22, 0.22);
  }

  /* Entry-point button for the full geographic map (Chunk 6b).
     Sits below the filter row on the Live Feed. Link-style — visible
     but doesn't compete with the stylized map below. */
  .crewmap-entry-row {
    display: flex;
    justify-content: flex-end;
    margin: 0 0 10px;
  }
  .crewmap-entry-btn {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    padding: 7px 12px;
    background: transparent;
    border: 1px solid rgba(15, 17, 22, 0.12);
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.70);
    cursor: pointer;
    transition: background 140ms ease, border-color 140ms ease,
                color 140ms ease, transform 80ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .crewmap-entry-btn:hover {
    background: #fff;
    border-color: rgba(65, 105, 225, 0.45);
    color: #4169E1;
  }
  .crewmap-entry-btn:active { transform: scale(0.97); }

  /* ── LIVE FEED — map mockup ── */
  .feed-map {
    position: relative;
    height: 480px;
    border-radius: 20px;
    overflow: hidden;
    background: #e8eef5;
    margin-bottom: 16px;
    box-shadow: inset 0 0 0 1px rgba(15,17,22,0.06);
  }
  /* Phase 9 — Leaflet container fills the feed-map */
  .feed-map-leaflet {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    background: #e8eef5;
  }
  /* Loading shim — covers the map until Leaflet tiles render */
  .feed-map-loading {
    position: absolute;
    inset: 0;
    z-index: 25;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    background: linear-gradient(135deg, #e8eef5, #f0f4f9);
    color: rgba(15, 17, 22, 0.55);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13.5px;
    transition: opacity 420ms ease;
  }
  .feed-map-loading.is-ready {
    opacity: 0;
    pointer-events: none;
  }
  .feed-map-loading-dot {
    width: 9px; height: 9px;
    border-radius: 50%;
    background: #4169E1;
    animation: feedMapLoadingPulse 1.4s ease-in-out infinite;
  }
  @keyframes feedMapLoadingPulse {
    0%, 100% { opacity: 0.35; transform: scale(0.85); }
    50%      { opacity: 1; transform: scale(1.1); }
  }
  /* "Recenter on me" button — appears bottom-right when user has
     geolocation enabled and has panned away. */
  .feed-map-locate {
    position: absolute;
    bottom: 50px;
    right: 12px;
    z-index: 20;
    width: 34px;
    height: 34px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.96);
    border: 1px solid rgba(15, 17, 22, 0.14);
    border-radius: 50%;
    cursor: pointer;
    color: #4169E1;
    box-shadow: 0 2px 6px rgba(15, 17, 22, 0.10);
    transition: background-color 120ms ease, transform 80ms ease;
  }
  .feed-map-locate:hover {
    background: #fff;
    transform: translateY(-1px);
  }
  .feed-map-locate:active { transform: translateY(0) scale(0.97); }
  /* Soft topographic-feel SVG background — kept for any residual legacy
     references; the .feed-map-leaflet container now sits above it. */
  .feed-map-bg { position: absolute; inset: 0; width: 100%; height: 100%; }

  /* Wrapper that holds two stacked SVG backgrounds during zoom transitions:
     one fading out, one fading in. Each child SVG fills the wrap absolutely. */
  .feed-map-bg-wrap {
    position: absolute;
    inset: 0;
  }
  .feed-map-bg-wrap svg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    transition: opacity 420ms cubic-bezier(0.5, 0, 0.2, 1);
  }
  /* Old background fading out has the .fading-out class; new one fades in
     by default (it starts at opacity:0 in JS and is animated to 1). */
  .feed-map-bg-wrap svg.fading-out {
    opacity: 0;
  }

  /* Pulsing report dots — upgraded to be more noticeable on the busy
     mockup map. Larger central dot with a small icon inside, soft outer
     halo so it pops against the topographic background, and three pulse
     rings instead of two for a richer rhythm. The icon makes the type
     identifiable at a glance without needing the legend. */
  .feed-pulse {
    position: absolute;
    width: 28px; height: 28px;
    transform: translate(-50%, -50%);
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    /* Smooth tween between zoom-tier positions. Cubic-bezier matches the
       map crossfade duration so they finish together. Default short
       transition is fine for re-positioning when reports update too. */
    transition: top 420ms cubic-bezier(0.5, 0, 0.2, 1),
                left 420ms cubic-bezier(0.5, 0, 0.2, 1);
  }
  /* Phase 9 Push B — when a feed-pulse is rendered as a Leaflet divIcon,
     Leaflet already positions the marker container via iconAnchor, so
     the pulse's own absolute positioning + transform offset are wrong.
     The .leaflet-marker-icon wrapper provides the positioning frame. */
  .leaflet-marker-icon .feed-pulse {
    position: relative;
    transform: none;
    top: auto;
    left: auto;
    width: 32px;
    height: 32px;
  }
  .feed-pulse-dot {
    position: absolute; inset: 0;
    border-radius: 50%;
    z-index: 3;
    box-shadow: 0 0 0 3px #fff, 0 0 0 5px rgba(15,17,22,0.06), 0 6px 16px rgba(15,17,22,0.22);
    display: flex; align-items: center; justify-content: center;
    color: #fff;
  }
  /* Soft halo behind the dot — same color as the type, low opacity, makes
     the dot read as glowing rather than just floating. */
  .feed-pulse-halo {
    position: absolute;
    inset: -8px;
    border-radius: 50%;
    background: radial-gradient(circle, currentColor 0%, transparent 65%);
    opacity: 0.35;
    z-index: 1;
  }
  .feed-pulse-ring {
    position: absolute; inset: 0;
    border-radius: 50%;
    border: 2px solid currentColor;
    opacity: 0;
    animation: feedPulse 2.4s cubic-bezier(0.16, 1, 0.3, 1) infinite;
    z-index: 2;
  }
  .feed-pulse-ring.ring-2 { animation-delay: 0.8s; }
  .feed-pulse-ring.ring-3 { animation-delay: 1.6s; }
  @keyframes feedPulse {
    0%   { transform: scale(1);   opacity: 0.6; }
    100% { transform: scale(3.2); opacity: 0; }
  }
  /* Aging stages: fading dims the pulse; stale removes the rings entirely
     and desaturates so it reads as "still here but historical." */
  .feed-pulse[data-stage="fading"] { opacity: 0.65; }
  .feed-pulse[data-stage="stale"]  {
    opacity: 0.45;
    filter: grayscale(0.5);
  }
  .feed-pulse[data-stage="stale"] .feed-pulse-ring { display: none; }
  .feed-pulse[data-stage="stale"] .feed-pulse-halo { opacity: 0.15; }
  /* Pulse colors keyed by report type (matches report-button colors) */
  .feed-pulse[data-type="storm"]    { color: #C2342B; }
  .feed-pulse[data-type="storm"]    .feed-pulse-dot { background: #C2342B; }
  .feed-pulse[data-type="hail"]     { color: #5B6BD6; }
  .feed-pulse[data-type="hail"]     .feed-pulse-dot { background: #5B6BD6; }
  .feed-pulse[data-type="wind"]     { color: #E8A400; }
  .feed-pulse[data-type="wind"]     .feed-pulse-dot { background: #E8A400; }
  .feed-pulse[data-type="rain"]     { color: #2566D6; }
  .feed-pulse[data-type="rain"]     .feed-pulse-dot { background: #2566D6; }
  .feed-pulse[data-type="snow"]     { color: #6B7B8C; }
  .feed-pulse[data-type="snow"]     .feed-pulse-dot { background: #6B7B8C; }
  .feed-pulse[data-type="sleet"]    { color: #4A6478; }
  .feed-pulse[data-type="sleet"]    .feed-pulse-dot { background: #4A6478; }
  .feed-pulse[data-type="freezing"] { color: #2F4858; }
  .feed-pulse[data-type="freezing"] .feed-pulse-dot { background: #2F4858; }
  .feed-pulse[data-type="fog"]      { color: #8A9099; }
  .feed-pulse[data-type="fog"]      .feed-pulse-dot { background: #8A9099; }
  .feed-pulse[data-type="sunny"]    { color: #F2A03D; }
  .feed-pulse[data-type="sunny"]    .feed-pulse-dot { background: #F2A03D; }
  .feed-pulse[data-type="cloudy"]   { color: #9AA3AB; }
  .feed-pulse[data-type="cloudy"]   .feed-pulse-dot { background: #9AA3AB; }
  .feed-pulse[data-type="humid"]    { color: #6BA47A; }
  .feed-pulse[data-type="humid"]    .feed-pulse-dot { background: #6BA47A; }
  .feed-pulse[data-type="clearing"] { color: #79B58E; }
  .feed-pulse[data-type="clearing"] .feed-pulse-dot { background: #79B58E; }
  .feed-pulse[data-type="clear"]    { color: #12A150; }
  .feed-pulse[data-type="clear"]    .feed-pulse-dot { background: #12A150; }

  /* Map legend */
  /* ── Zoom controls — top-right corner of the feed map ── */
  .zoom-controls {
    position: absolute;
    top: 16px;
    right: 16px;
    display: flex;
    flex-direction: column;
    gap: 1px;
    background: rgba(255,255,255,0.95);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 14px rgba(15,17,22,0.12);
    z-index: 6;
  }
  .zoom-btn {
    width: 38px;
    height: 38px;
    background: #fff;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #0E1116;
    transition: background 120ms ease, color 120ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .zoom-btn:hover:not(:disabled) {
    background: rgba(65,105,225,0.08);
    color: var(--rb, #4169E1);
  }
  .zoom-btn:active:not(:disabled) {
    background: rgba(65,105,225,0.15);
  }
  .zoom-btn:disabled {
    cursor: not-allowed;
    color: rgba(15,17,22,0.30);
    background: #f5f6f8;
  }
  .zoom-btn svg {
    width: 18px;
    height: 18px;
  }
  /* Zoom label — bottom-right of the map, above the stats strip */
  .zoom-label {
    position: absolute;
    top: 16px;
    right: 64px;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(8px);
    border-radius: 999px;
    padding: 7px 14px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.65);
    box-shadow: 0 4px 14px rgba(15,17,22,0.10);
    z-index: 6;
    pointer-events: none;
  }
  /* On narrow viewports, drop the zoom label below the controls so the
     map background isn't crowded. */
  @media (max-width: 480px) {
    .zoom-label {
      top: auto;
      bottom: 60px;
      right: 16px;
      font-size: 9.5px;
      padding: 6px 12px;
    }
  }
  /* Empty-state for the story list when zoom is too tight */
  .story-empty {
    list-style: none;
    text-align: center;
    padding: 28px 20px;
    color: rgba(15,17,22,0.55);
    font-size: 13px;
    background: rgba(15,17,22,0.02);
    border-radius: 14px;
    border: 1px dashed rgba(15,17,22,0.10);
  }

  .feed-legend {
    position: absolute; top: 16px; left: 16px;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(8px);
    border-radius: 10px;
    padding: 8px 12px;
    font-size: 11px;
    color: rgba(15,17,22,0.65);
    box-shadow: 0 4px 14px rgba(15,17,22,0.10);
    z-index: 5;
  }
  .feed-legend-line {
    display: flex; align-items: center; gap: 6px;
    margin: 2px 0;
  }
  .feed-legend-swatch {
    width: 8px; height: 8px; border-radius: 50%;
  }
  .feed-stats-strip {
    position: absolute; bottom: 12px; left: 12px; right: 12px;
    background: rgba(15,17,22,0.85);
    backdrop-filter: blur(8px);
    border-radius: 12px;
    padding: 10px 14px;
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 12px;
    z-index: 5;
  }
  .feed-stats-count {
    font-family: 'JetBrains Mono', monospace;
    font-weight: 700;
    letter-spacing: 0.6px;
  }
  .feed-stats-area {
    color: rgba(255,255,255,0.70);
    font-family: 'Inter', system-ui, sans-serif;
  }

  /* Story feed (the list below the map) */
  .story-list {
    list-style: none; padding: 0; margin: 0;
    display: flex; flex-direction: column; gap: 10px;
  }
  .story {
    background: #fff;
    border: 1px solid rgba(15,17,22,0.08);
    border-radius: 14px;
    padding: 14px 16px;
    display: flex; gap: 12px;
  }
  .story-icon {
    width: 36px; height: 36px;
    border-radius: 50%;
    flex-shrink: 0;
    display: flex; align-items: center; justify-content: center;
    color: #fff;
    font-weight: 700;
    font-size: 13px;
  }
  .story-icon.story-storm { background: #C2342B; }
  .story-icon.story-hail  { background: #5B6BD6; }
  .story-icon.story-wind  { background: #E8A400; }
  .story-icon.story-rain  { background: #2566D6; }
  .story-icon.story-clear { background: #12A150; }
  .story-body { flex: 1; min-width: 0; }
  .story-head {
    display: flex; align-items: baseline; justify-content: space-between;
    gap: 8px;
    margin-bottom: 2px;
  }
  .story-author {
    font-size: 14px; font-weight: 600;
    color: #0E1116;
  }
  .story-time {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15,17,22,0.55);
    flex-shrink: 0;
    letter-spacing: 0.3px;
  }
  .story-text {
    font-size: 13px;
    line-height: 1.5;
    color: rgba(15,17,22,0.78);
    margin: 0;
  }
  .story-meta {
    margin-top: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15,17,22,0.45);
    letter-spacing: 0.4px;
    text-transform: uppercase;
  }

  /* Story photo — a small mockup thumbnail rendered as an SVG sky scene
     (clouds, sun, rain, etc.). Pure visual stand-in, no real images.
     Sits to the right of the text on wide cards, top of the card on
     narrow ones. */
  .story-photo {
    width: 80px; height: 80px;
    border-radius: 12px;
    overflow: hidden;
    flex-shrink: 0;
    background: #d6e0ec;
    box-shadow: inset 0 0 0 1px rgba(15,17,22,0.06);
    position: relative;
  }
  .story-photo svg { width: 100%; height: 100%; display: block; }
  /* Verified-count chip — appears in the meta row when other Crew members
     have confirmed a similar observation in the same area within ~30min.
     Communicates social proof: this report has been corroborated. */
  .story-verified {
    display: inline-flex; align-items: center; gap: 4px;
    background: rgba(18,161,80,0.10);
    color: #0a6e35;
    padding: 2px 8px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.4px;
    text-transform: uppercase;
    margin-left: 6px;
  }
  .story-verified svg { width: 11px; height: 11px; }

  /* ── Inline verify action row (Chunk 3) ────────────────────
     Sits at the bottom of every story card. One of three states:
       (a) "Verify" button — open to verify (default for not-yet-verified reports)
       (b) "✓ You verified" — confirmation badge after you've tapped (with optional Undo)
       (c) "Your report" — placeholder for your own reports (no action)
     Plus an optional "Things are different here" button when the report is stale. */
  .story-actions {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 10px;
    flex-wrap: wrap;
  }
  .story-action-verify {
    display: inline-flex; align-items: center; gap: 5px;
    background: rgba(65, 105, 225, 0.10);
    color: var(--rb, #4169E1);
    border: 1px solid rgba(65, 105, 225, 0.20);
    border-radius: 8px;
    padding: 5px 12px 5px 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    cursor: pointer;
    transition: background 140ms ease, border-color 140ms ease, transform 80ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .story-action-verify:hover {
    background: rgba(65, 105, 225, 0.18);
    border-color: rgba(65, 105, 225, 0.40);
  }
  .story-action-verify:active { transform: scale(0.97); }
  .story-action-verify svg { width: 14px; height: 14px; }
  .story-action-verified {
    display: inline-flex; align-items: center; gap: 5px;
    background: rgba(18, 161, 80, 0.10);
    color: #0a6e35;
    border: 1px solid rgba(18, 161, 80, 0.22);
    border-radius: 8px;
    padding: 5px 12px 5px 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
  }
  .story-action-verified svg { width: 13px; height: 13px; }
  .story-action-undo {
    background: transparent;
    border: none;
    color: rgba(10, 110, 53, 0.65);
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 11px;
    font-weight: 500;
    text-decoration: underline;
    text-decoration-style: dotted;
    padding: 0;
    margin-left: 6px;
    cursor: pointer;
    transition: color 140ms ease;
  }
  .story-action-undo:hover { color: #0a6e35; }
  .story-action-mine {
    display: inline-flex; align-items: center;
    background: rgba(15, 17, 22, 0.04);
    color: rgba(15, 17, 22, 0.55);
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 8px;
    padding: 5px 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 500;
  }
  .story-action-refresh {
    display: inline-flex; align-items: center; gap: 5px;
    background: rgba(232, 164, 0, 0.10);
    color: #8a5a0a;
    border: 1px solid rgba(232, 164, 0, 0.25);
    border-radius: 8px;
    padding: 5px 12px 5px 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12.5px;
    font-weight: 600;
    cursor: pointer;
    transition: background 140ms ease, border-color 140ms ease, transform 80ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .story-action-refresh:hover {
    background: rgba(232, 164, 0, 0.18);
    border-color: rgba(232, 164, 0, 0.45);
  }
  .story-action-refresh:active { transform: scale(0.97); }
  .story-action-refresh svg { width: 13px; height: 13px; }

  /* ── Aging labels in the meta row ──────────────────────── */
  .story-fading-label {
    display: inline-block;
    background: rgba(232, 164, 0, 0.10);
    color: #8a5a0a;
    padding: 1px 7px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    margin-left: 6px;
  }
  .story-stale-label {
    display: inline-block;
    background: rgba(194, 52, 43, 0.10);
    color: #8a2823;
    padding: 1px 7px;
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    margin-left: 6px;
  }

  /* ── Aging visual stages on the story card itself ──────── */
  .story.is-fading {
    /* Slightly muted — the report is mid-life. Visible but not loudest. */
    opacity: 0.78;
  }
  .story.is-stale {
    /* Clearly aged. Background tints toward amber-gray; needs fresh eyes. */
    opacity: 0.62;
    background: linear-gradient(180deg, rgba(232, 164, 0, 0.04), rgba(255, 255, 255, 0.0));
  }
  .story.is-stale .story-icon {
    /* Desaturate the type icon — visual signal that this isn't current. */
    filter: grayscale(0.5);
  }

  /* Stack story photo above text on narrow viewports — keep the card
     readable on a 320px phone. Wide cards put it inline at the right. */
  .story.has-photo .story-body { min-width: 0; }
  @media (min-width: 481px) {
    .story.has-photo {
      align-items: flex-start;
    }
  }
  @media (max-width: 480px) {
    .story.has-photo {
      flex-direction: column;
      gap: 10px;
    }
    .story.has-photo .story-photo {
      width: 100%;
      height: 140px;
    }
    .story.has-photo .story-icon {
      position: absolute;
      top: 22px; left: 22px;
      box-shadow: 0 0 0 2px #fff;
    }
  }

  /* ── QUICK REPORT — bottom sheet ── */
  .report-fab {
    position: fixed;
    right: 24px; bottom: 24px;
    width: 60px; height: 60px;
    border-radius: 50%;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 12px 28px rgba(65,105,225,0.40);
    z-index: 50;
    transition: transform 120ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .report-fab:hover { transform: scale(1.05); }
  .report-fab:active { transform: scale(0.95); }
  .report-fab svg { width: 26px; height: 26px; }

  .report-sheet-scrim {
    position: fixed; inset: 0;
    background: rgba(15,17,22,0.55);
    backdrop-filter: blur(2px);
    z-index: 100;
    opacity: 0;
    pointer-events: none;
    transition: opacity 200ms ease;
  }
  .report-sheet-scrim.active {
    opacity: 1;
    pointer-events: auto;
  }
  .report-sheet {
    position: fixed; left: 0; right: 0; bottom: 0;
    background: #fff;
    border-radius: 24px 24px 0 0;
    padding: 12px 20px 28px;
    box-shadow: 0 -20px 60px rgba(15,17,22,0.20);
    z-index: 101;
    transform: translateY(100%);
    transition: transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    max-width: 720px;
    margin: 0 auto;
    /* Cap the sheet at most of the viewport so the expanded category list
       (now 13 buttons across 3 sections) doesn't push the sheet taller
       than the screen. Inside content scrolls if needed; the handle stays
       at the top so users can still close the sheet. */
    max-height: 88vh;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
  }
  .report-sheet.active { transform: translateY(0); }
  /* The Crew Quick Report sheet, scrim, and FAB only make sense from the
     customer-facing Crew screens. When the dev menu is set to Met or Admin
     view, hide them entirely so they don't bleed into screenshots or appear
     as orphaned UI on tall viewports. */
  body.dev-view-met .report-sheet,
  body.dev-view-met .report-sheet-scrim,
  body.dev-view-met .report-fab,
  body.dev-view-admin .report-sheet,
  body.dev-view-admin .report-sheet-scrim,
  body.dev-view-admin .report-fab,
  body.dev-view-crew-ws .report-sheet,
  body.dev-view-crew-ws .report-sheet-scrim,
  body.dev-view-crew-ws .report-fab {
    display: none !important;
  }
  .report-sheet-handle {
    width: 36px; height: 4px;
    background: rgba(15,17,22,0.18);
    border-radius: 999px;
    margin: 0 auto 18px;
  }
  .report-sheet-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 2.4px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
    margin-bottom: 6px;
  }
  .report-sheet-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px; font-weight: 600;
    margin: 0 0 4px;
    letter-spacing: -0.3px;
  }
  .report-sheet-sub {
    font-size: 13px;
    color: rgba(15,17,22,0.55);
    margin: 0 0 20px;
  }
  .report-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }
  .report-option {
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    gap: 8px;
    min-height: 100px;
    padding: 18px 12px;
    background: #fafbfc;
    border: 1.5px solid rgba(15,17,22,0.06);
    border-radius: 16px;
    cursor: pointer;
    text-align: center;
    transition: all 140ms ease;
    color: inherit;
    font: inherit;
    -webkit-tap-highlight-color: transparent;
  }
  .report-option:hover {
    background: #fff;
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(15,17,22,0.05);
  }
  .report-option:active { transform: translateY(0) scale(0.98); }
  .report-option-icon {
    width: 40px; height: 40px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    color: #fff;
  }
  .report-option[data-kind="storm"]    .report-option-icon { background: #C2342B; }
  .report-option[data-kind="hail"]     .report-option-icon { background: #5B6BD6; }
  .report-option[data-kind="wind"]     .report-option-icon { background: #E8A400; }
  .report-option[data-kind="rain"]     .report-option-icon { background: #2566D6; }
  .report-option[data-kind="snow"]     .report-option-icon { background: #6B7B8C; }
  .report-option[data-kind="sleet"]    .report-option-icon { background: #4A6478; }
  .report-option[data-kind="freezing"] .report-option-icon { background: #2F4858; }
  .report-option[data-kind="fog"]      .report-option-icon { background: #8A9099; }
  .report-option[data-kind="sunny"]    .report-option-icon { background: #F2A03D; }
  .report-option[data-kind="cloudy"]   .report-option-icon { background: #9AA3AB; }
  .report-option[data-kind="humid"]    .report-option-icon { background: #6BA47A; }
  .report-option[data-kind="clearing"] .report-option-icon { background: #79B58E; }
  .report-option[data-kind="photo"]    .report-option-icon { background: #5B6770; }

  /* Photo button — when it sits in its own row (no sibling), span full width */
  .report-option-photo {
    grid-column: 1 / -1;
    flex-direction: row;
    justify-content: flex-start;
    text-align: left;
    gap: 14px;
    padding: 14px 18px;
    min-height: 72px;
  }
  .report-option-photo .report-option-icon {
    width: 44px; height: 44px;
    flex-shrink: 0;
  }
  .report-option-photo .report-option-label {
    font-size: 15px;
  }

  /* Section label inside the report sheet — small mono caps that group
     the categories into Active weather / Current conditions / Photo. */
  .report-section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15, 17, 22, 0.50);
    margin: 18px 0 10px;
  }
  .report-section-label:first-of-type {
    margin-top: 4px;
  }

  .report-option-label {
    font-size: 14px; font-weight: 700;
    color: #0E1116;
  }
  .report-option-sub {
    font-size: 11px;
    color: rgba(15,17,22,0.55);
    line-height: 1.3;
  }

  /* ── Photo-capture state ─────────────────────────────────────────────
     Shown when the user taps "Photo" in the Quick Report grid. Two
     visual phases:
       (A) photo-picker — large tap target, centered icon + label.
           Replaces the four-button grid entirely.
       (B) photo-classify — preview thumbnail + retake X + type chips
           + submit button.
     The preview and chips share the calm Royal Blue / amber palette
     used elsewhere in the report sheet. The submit button is disabled
     until the user picks a weather type, since a photo with no
     classification is less useful for the rest of the Crew. */

  /* ── Optional confessional note ────────────────────────
     Collapsible "+ Add a note" expand at the bottom of the
     Quick Report category grid. Closed by default — preserves
     the 2-tap-submit promise for users who skip. */
  .report-note-details {
    margin-top: 18px;
    padding: 0;
    border: 1px solid rgba(15, 17, 22, 0.08);
    border-radius: 12px;
    background: rgba(245, 247, 252, 0.6);
  }
  .report-note-details[open] {
    background: #fff;
    border-color: rgba(15, 17, 22, 0.15);
  }
  .report-note-summary {
    display: flex; align-items: center; gap: 8px;
    padding: 12px 14px;
    cursor: pointer;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 500;
    color: rgba(15, 17, 22, 0.65);
    list-style: none;
    user-select: none;
  }
  .report-note-summary::-webkit-details-marker { display: none; }
  .report-note-summary svg {
    transition: transform 200ms ease;
    color: rgba(65, 105, 225, 0.85);
  }
  .report-note-details[open] .report-note-summary svg {
    transform: rotate(45deg);
  }
  .report-note-details[open] .report-note-summary {
    color: #0E1116;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
  }
  .report-note-body {
    padding: 14px;
  }
  .report-note-label {
    display: block;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 500;
    color: #0E1116;
    margin-bottom: 8px;
    line-height: 1.4;
  }
  .report-note-input {
    width: 100%;
    box-sizing: border-box;
    background: #fff;
    border: 1px solid rgba(15, 17, 22, 0.15);
    border-radius: 9px;
    padding: 10px 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    line-height: 1.5;
    color: #0E1116;
    resize: vertical;
    min-height: 70px;
    transition: border-color 140ms ease;
  }
  .report-note-input:focus {
    outline: none;
    border-color: rgba(65, 105, 225, 0.55);
    box-shadow: 0 0 0 3px rgba(65, 105, 225, 0.12);
  }
  .report-note-input::placeholder {
    color: rgba(15, 17, 22, 0.40);
    font-style: italic;
  }
  .report-note-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-top: 10px;
    flex-wrap: wrap;
  }
  .report-note-quote {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    cursor: pointer;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    color: rgba(15, 17, 22, 0.65);
    line-height: 1.4;
  }
  .report-note-quote input {
    width: 15px; height: 15px;
    accent-color: #4169E1;
    cursor: pointer;
    margin: 0;
  }
  .report-note-count {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.45);
    flex-shrink: 0;
  }
  .report-note-count.is-near-limit {
    color: #B8770A;
  }

  .photo-picker {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 36px 20px;
    margin-top: 12px;
    background: rgba(65, 105, 225, 0.05);
    border: 2px dashed rgba(65, 105, 225, 0.30);
    border-radius: 16px;
    cursor: pointer;
    text-align: center;
    transition: background 140ms ease, border-color 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .photo-picker:hover, .photo-picker:active {
    background: rgba(65, 105, 225, 0.09);
    border-color: rgba(65, 105, 225, 0.50);
  }
  .photo-picker-icon {
    color: var(--rb, #4169E1);
    opacity: 0.85;
  }
  .photo-picker-label {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 15px; font-weight: 600;
    color: #0E1116;
  }
  .photo-picker-sub {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.5px;
    color: rgba(15, 17, 22, 0.50);
  }

  /* Preview wrapper — shows the picked image at fixed aspect ratio
     so wide and tall photos display the same. Object-fit:cover
     crops to fill cleanly; object-position center keeps subjects
     anchored. */
  .photo-preview-wrap {
    position: relative;
    margin-top: 12px;
    width: 100%;
    aspect-ratio: 16 / 10;
    background: #0E1116;
    border-radius: 14px;
    overflow: hidden;
  }
  #photo-preview {
    width: 100%; height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }
  .photo-retake {
    position: absolute;
    top: 8px; right: 8px;
    width: 28px; height: 28px;
    border-radius: 50%;
    background: rgba(15, 17, 22, 0.65);
    color: #fff;
    border: none;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background 120ms ease;
  }
  .photo-retake:hover { background: rgba(15, 17, 22, 0.85); }

  .photo-classify-prompt {
    margin: 16px 0 10px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
  }

  /* Type chips — five horizontally-flowing pills. Single-select; the
     active one fills with Royal Blue. */
  .photo-type-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 18px;
  }
  .photo-type-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 9px 14px;
    background: #fff;
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    border-radius: 999px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px; font-weight: 500;
    color: #0E1116;
    cursor: pointer;
    transition: all 120ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .photo-type-chip:hover {
    border-color: rgba(15, 17, 22, 0.25);
  }
  .photo-type-chip.is-active {
    background: var(--rb, #4169E1);
    color: #fff;
    border-color: var(--rb, #4169E1);
  }
  .photo-type-chip.is-active .photo-type-dot {
    box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.55);
  }
  .photo-type-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .photo-submit {
    width: 100%;
    min-height: 50px;
    padding: 0 20px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 14px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 14px;
    display: flex; align-items: center; justify-content: center; gap: 8px;
    cursor: pointer;
    box-shadow: 0 8px 22px rgba(65, 105, 225, 0.26);
    transition: transform 80ms ease, box-shadow 140ms ease, opacity 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .photo-submit:hover:not(:disabled) {
    box-shadow: 0 12px 28px rgba(65, 105, 225, 0.32);
  }
  .photo-submit:active:not(:disabled) {
    transform: scale(0.99);
  }
  .photo-submit:disabled {
    opacity: 0.45;
    cursor: not-allowed;
    box-shadow: none;
  }

  .report-thanks {
    text-align: center;
    padding: 20px 0 4px;
    position: relative;
  }

  /* Animated checkmark — the SVG has a path-length stroke-dasharray that
     animates from invisible to fully drawn. Pairs with a soft pulse on
     the green halo behind it. The whole thing lasts ~700ms — short
     enough not to feel sluggish, long enough to feel like a moment. */
  .report-thanks-icon {
    width: 64px; height: 64px;
    border-radius: 50%;
    background: rgba(18,161,80,0.12);
    color: #0a6e35;
    margin: 0 auto 14px;
    display: flex; align-items: center; justify-content: center;
    position: relative;
    /* Pop-in on appearance */
    animation: thanksPop 480ms cubic-bezier(0.16, 1.2, 0.3, 1) both;
  }
  @keyframes thanksPop {
    0%   { transform: scale(0.4); opacity: 0; }
    60%  { transform: scale(1.08); opacity: 1; }
    100% { transform: scale(1); opacity: 1; }
  }
  /* Outer halo that pulses once when the thanks state appears */
  .report-thanks-icon::before {
    content: '';
    position: absolute;
    inset: -8px;
    border-radius: 50%;
    border: 2px solid rgba(18,161,80,0.45);
    animation: thanksHalo 1100ms cubic-bezier(0.16, 1, 0.3, 1) both;
    animation-delay: 200ms;
  }
  @keyframes thanksHalo {
    0%   { transform: scale(0.7); opacity: 0; }
    40%  { opacity: 0.6; }
    100% { transform: scale(1.6); opacity: 0; }
  }
  /* The checkmark stroke draws in from left to right */
  .report-thanks-check {
    stroke-dasharray: 24;
    stroke-dashoffset: 24;
    animation: drawCheck 480ms cubic-bezier(0.4, 0, 0.2, 1) forwards;
    animation-delay: 240ms;
  }
  @keyframes drawCheck {
    to { stroke-dashoffset: 0; }
  }

  .report-thanks-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px; font-weight: 600;
    color: #0E1116;
    margin: 0 0 6px;
    /* Slight slide-up + fade-in, slightly delayed after the checkmark */
    animation: thanksRise 480ms cubic-bezier(0.16, 1, 0.3, 1) both;
    animation-delay: 280ms;
  }
  @keyframes thanksRise {
    from { transform: translateY(8px); opacity: 0; }
    to   { transform: translateY(0); opacity: 1; }
  }

  .report-thanks-text {
    font-size: 13px;
    color: rgba(15,17,22,0.65);
    margin: 0 0 14px;
    animation: thanksRise 480ms cubic-bezier(0.16, 1, 0.3, 1) both;
    animation-delay: 360ms;
  }

  /* Tokens reward badge — the +10 tokens line. Stays on screen for the
     full duration, doesn't fade until the sheet auto-closes. The "+10"
     number animates as a separate float-up next to the chip. */
  .report-thanks-tokens {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px 8px 14px;
    background: rgba(65,105,225,0.10);
    border: 1px solid rgba(65,105,225,0.22);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    font-weight: 700;
    color: var(--rb, #4169E1);
    letter-spacing: 0.6px;
    text-transform: uppercase;
    animation: thanksRise 520ms cubic-bezier(0.16, 1, 0.3, 1) both;
    animation-delay: 440ms;
    position: relative;
  }
  .report-thanks-tokens-icon {
    color: var(--rb, #4169E1);
  }
  .report-thanks-tokens-amount {
    font-size: 14px;
    font-weight: 700;
  }

  /* The "+10" floating burst — appears, drifts up, fades out.
     A tiny, satisfying flourish that doesn't repeat. Triggered once
     per submission. */
  .tokens-burst {
    position: absolute;
    top: 18px; left: 50%;
    transform: translateX(-50%);
    font-family: 'JetBrains Mono', monospace;
    font-size: 24px;
    font-weight: 700;
    color: var(--rb, #4169E1);
    text-shadow: 0 2px 8px rgba(65,105,225,0.20);
    pointer-events: none;
    opacity: 0;
    animation: tokensBurst 1400ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
    animation-delay: 280ms;
  }
  @keyframes tokensBurst {
    0%   { transform: translate(-50%, 8px) scale(0.8); opacity: 0; }
    25%  { transform: translate(-50%, -12px) scale(1.1); opacity: 1; }
    100% { transform: translate(-50%, -52px) scale(1); opacity: 0; }
  }

  /* Confetti dots — three small dots that spread outward from the
     checkmark on completion. Calm, not raining-down. Three is plenty. */
  .thanks-confetti {
    position: absolute;
    width: 6px; height: 6px;
    border-radius: 50%;
    pointer-events: none;
    opacity: 0;
    animation: confettiFloat 900ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
  }
  .thanks-confetti.c1 {
    background: #4169E1;
    top: 20px; left: 38%;
    animation-delay: 320ms;
    --tx: -28px; --ty: -36px;
  }
  .thanks-confetti.c2 {
    background: #E8A400;
    top: 24px; left: 62%;
    animation-delay: 360ms;
    --tx: 32px; --ty: -28px;
  }
  .thanks-confetti.c3 {
    background: #12A150;
    top: 32px; left: 50%;
    animation-delay: 400ms;
    --tx: 4px; --ty: -44px;
  }
  @keyframes confettiFloat {
    0%   { transform: translate(0, 0) scale(0.4); opacity: 0; }
    20%  { opacity: 1; }
    100% { transform: translate(var(--tx), var(--ty)) scale(1); opacity: 0; }
  }

  /* ════════════════════════════════════════════════════════════════════════
     VALET CREW · REPORT DETAIL SCREEN
     ════════════════════════════════════════════════════════════════════════
     Full-screen view of a single report. Reachable from:
       - Tap a story card on Live Feed
       - Tap a pulse on the map
     Layout: large photo at top, then attribution row, then report text,
     then Verified button + verified count, then tipping menu. Same calm
     restraint as the rest of the Crew layer — no celebratory overload. */

  .detail-photo {
    width: 100%;
    height: 280px;
    border-radius: 16px;
    overflow: hidden;
    background: #d6e0ec;
    margin-bottom: 18px;
    position: relative;
    box-shadow: 0 8px 24px rgba(15,17,22,0.08);
  }
  .detail-photo svg { width: 100%; height: 100%; display: block; }
  /* When the report has no photo, show a quieter placeholder card with
     a subtle gradient and the type icon — keeps the visual hierarchy
     consistent across reports with and without photos. */
  .detail-photo.no-photo {
    height: 160px;
    background: linear-gradient(135deg, rgba(65,105,225,0.06) 0%, rgba(15,17,22,0.04) 100%);
    box-shadow: inset 0 0 0 1px rgba(15,17,22,0.06);
    display: flex; align-items: center; justify-content: center;
  }
  .detail-no-photo-text {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px; font-weight: 700;
    color: rgba(15,17,22,0.45);
    letter-spacing: 1.6px;
    text-transform: uppercase;
  }

  /* Type-tag pill in the corner of the photo — at-a-glance category */
  .detail-type-tag {
    position: absolute;
    top: 14px; left: 14px;
    display: inline-flex; align-items: center; gap: 5px;
    padding: 5px 10px 5px 8px;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(6px);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    box-shadow: 0 2px 8px rgba(15,17,22,0.10);
  }
  .detail-type-dot {
    width: 8px; height: 8px; border-radius: 50%;
  }
  .detail-type-tag.type-storm  { color: #9b2823; }
  .detail-type-tag.type-storm  .detail-type-dot { background: #C2342B; }
  .detail-type-tag.type-hail   { color: #2d4499; }
  .detail-type-tag.type-hail   .detail-type-dot { background: #5B6BD6; }
  .detail-type-tag.type-wind   { color: #8a5a0a; }
  .detail-type-tag.type-wind   .detail-type-dot { background: #E8A400; }
  .detail-type-tag.type-rain   { color: #1d4d8c; }
  .detail-type-tag.type-rain   .detail-type-dot { background: #2566D6; }
  .detail-type-tag.type-clear  { color: #0a6e35; }
  .detail-type-tag.type-clear  .detail-type-dot { background: #12A150; }

  .detail-attr {
    display: flex; align-items: center; gap: 12px;
    margin-bottom: 14px;
  }
  .detail-avatar {
    width: 44px; height: 44px;
    border-radius: 50%;
    background: linear-gradient(135deg, #4169E1 0%, #5B7DEB 100%);
    color: #fff;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Fraunces', Georgia, serif;
    font-size: 18px; font-weight: 600;
    flex-shrink: 0;
  }
  .detail-attr-text { flex: 1; min-width: 0; }
  .detail-attr-name {
    font-size: 15px; font-weight: 600; color: #0E1116;
    margin: 0;
  }
  .detail-attr-meta {
    font-size: 12px; color: rgba(15,17,22,0.55);
    margin: 2px 0 0;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 0.3px;
  }

  .detail-text {
    font-size: 16px; line-height: 1.55;
    color: #0E1116;
    margin: 0 0 24px;
    /* Slightly more elegant prose treatment than the small story text —
       this is the focused single-report view, give the words room. */
    font-family: 'Fraunces', Georgia, serif;
  }

  /* ── Verify button + count row ── */
  .detail-verify-section {
    background: rgba(65,105,225,0.04);
    border: 1px solid rgba(65,105,225,0.12);
    border-radius: 16px;
    padding: 18px 18px 16px;
    margin-bottom: 20px;
  }
  .detail-verify-row {
    display: flex; align-items: center; gap: 14px;
  }
  .detail-verify-btn {
    flex: 1;
    min-height: 52px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 15px;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center; gap: 8px;
    transition: background 140ms ease, transform 80ms ease, box-shadow 140ms ease;
    box-shadow: 0 6px 18px rgba(65,105,225,0.20);
    -webkit-tap-highlight-color: transparent;
    position: relative;
    overflow: hidden;
  }
  .detail-verify-btn:hover {
    box-shadow: 0 8px 22px rgba(65,105,225,0.28);
  }
  .detail-verify-btn:active { transform: scale(0.98); }
  /* Verified state: green, disabled, checkmark */
  .detail-verify-btn.is-verified {
    background: #12A150;
    box-shadow: 0 4px 14px rgba(18,161,80,0.20);
    cursor: default;
  }
  .detail-verify-btn.is-verified:hover { transform: none; }
  /* Owner state: hidden (you can't verify your own report) */
  .detail-verify-btn.is-mine {
    background: rgba(15,17,22,0.06);
    color: rgba(15,17,22,0.45);
    cursor: default;
    box-shadow: none;
  }
  .detail-verify-btn.is-mine:hover { transform: none; }

  /* +2 token burst that floats up out of the verify button */
  .verify-burst {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
    font-family: 'JetBrains Mono', monospace;
    font-size: 16px;
    font-weight: 700;
    color: #fff;
    text-shadow: 0 2px 8px rgba(15,17,22,0.30);
    opacity: 0;
    animation: verifyBurst 1100ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
  }
  @keyframes verifyBurst {
    0%   { transform: translate(-50%, -50%) scale(0.8); opacity: 0; }
    20%  { transform: translate(-50%, -100%) scale(1.1); opacity: 1; }
    100% { transform: translate(-50%, -180%) scale(1); opacity: 0; }
  }

  .detail-verify-count {
    text-align: center;
    flex-shrink: 0;
    min-width: 56px;
  }
  .detail-verify-count-num {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 28px;
    font-weight: 600;
    color: #0E1116;
    line-height: 1;
    /* Animate the count change with a brief pop */
    transition: transform 280ms cubic-bezier(0.16, 1.3, 0.3, 1);
  }
  .detail-verify-count-num.bump {
    transform: scale(1.25);
  }
  .detail-verify-count-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.55);
    margin-top: 4px;
  }
  .detail-verify-hint {
    font-size: 12px;
    color: rgba(15,17,22,0.55);
    margin: 12px 0 0;
    line-height: 1.5;
  }

  /* ── Tipping menu ── */
  .detail-tip-section {
    margin-bottom: 24px;
  }
  .detail-tip-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.55);
    margin-bottom: 4px;
    display: flex; align-items: center; gap: 8px;
  }
  /* Small inline help button — the "what's this?" affordance the reviewer
     asked for. Tucks next to the section label, doesn't compete with it. */
  .detail-tip-help {
    width: 18px; height: 18px;
    padding: 0;
    background: transparent;
    border: 1px solid rgba(15,17,22,0.18);
    border-radius: 50%;
    color: rgba(15,17,22,0.50);
    display: inline-flex; align-items: center; justify-content: center;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .detail-tip-help:hover {
    color: #4169E1;
    border-color: #4169E1;
    background: rgba(65, 105, 225, 0.06);
  }
  /* Help popover — appears on click of the help button. Lightweight,
     inline expansion rather than a modal. Explains what the recognition
     gestures are and how real tipping will work. */
  .detail-tip-help-popover {
    display: none;
    background: rgba(65, 105, 225, 0.05);
    border: 1px solid rgba(65, 105, 225, 0.20);
    border-radius: 10px;
    padding: 12px 14px;
    margin: 0 0 12px;
    font-size: 12.5px;
    line-height: 1.55;
    color: rgba(15,17,22,0.78);
  }
  .detail-tip-help-popover.is-open { display: block; }
  .detail-tip-help-popover strong {
    color: #0E1116;
    font-weight: 600;
  }
  .detail-tip-help-popover p {
    margin: 0 0 6px;
  }
  .detail-tip-help-popover p:last-child {
    margin-bottom: 0;
  }
  .detail-tip-sub {
    font-size: 12px;
    color: rgba(15,17,22,0.55);
    margin: 0 0 12px;
    line-height: 1.5;
  }
  .detail-tip-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
  }
  .tip-btn {
    background: #fff;
    border: 1.5px solid rgba(15,17,22,0.08);
    border-radius: 14px;
    padding: 14px 10px 12px;
    cursor: pointer;
    text-align: center;
    transition: all 140ms ease;
    color: inherit;
    font: inherit;
    -webkit-tap-highlight-color: transparent;
    position: relative;
    overflow: hidden;
  }
  .tip-btn:hover:not(:disabled):not(.is-given) {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(15,17,22,0.06);
    border-color: rgba(65,105,225,0.30);
  }
  .tip-btn:active:not(:disabled):not(.is-given) { transform: translateY(0) scale(0.98); }
  .tip-btn:disabled {
    opacity: 0.42;
    cursor: not-allowed;
    background: #f5f6f8;
  }
  /* Already-given state — show as completed, not disabled */
  .tip-btn.is-given {
    cursor: default;
    background: rgba(18,161,80,0.06);
    border-color: rgba(18,161,80,0.30);
  }
  .tip-btn.is-given .tip-icon { color: #12A150; }
  .tip-btn.is-given::after {
    content: '';
    position: absolute;
    top: 8px; right: 8px;
    width: 14px; height: 14px;
    border-radius: 50%;
    background: #12A150;
    box-shadow: 0 1px 4px rgba(18,161,80,0.30);
  }
  .tip-btn.is-given::before {
    content: '';
    position: absolute;
    top: 11px; right: 11px;
    width: 8px; height: 4px;
    border-left: 2px solid #fff;
    border-bottom: 2px solid #fff;
    transform: rotate(-45deg);
    z-index: 1;
  }

  .tip-icon {
    font-size: 26px;
    line-height: 1;
    margin-bottom: 6px;
    color: var(--rb, #4169E1);
    display: block;
  }
  .tip-name {
    display: block;
    font-size: 12px;
    font-weight: 700;
    color: #0E1116;
    line-height: 1.2;
    margin-bottom: 4px;
  }
  .tip-cost {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    color: rgba(65,105,225,0.85);
    letter-spacing: 0.4px;
  }
  .tip-btn:disabled .tip-cost { color: rgba(15,17,22,0.35); }

  /* Tip-burst (-N token float) */
  .tip-burst {
    position: absolute;
    top: 14px; left: 50%;
    transform: translateX(-50%);
    pointer-events: none;
    font-family: 'JetBrains Mono', monospace;
    font-size: 16px;
    font-weight: 700;
    color: var(--rb, #4169E1);
    text-shadow: 0 2px 8px rgba(65,105,225,0.20);
    opacity: 0;
    animation: tipBurst 1100ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
  }
  @keyframes tipBurst {
    0%   { transform: translate(-50%, 4px) scale(0.8); opacity: 0; }
    25%  { transform: translate(-50%, -22px) scale(1.1); opacity: 1; }
    100% { transform: translate(-50%, -52px) scale(1); opacity: 0; }
  }

  /* "Send a real tip" affordance — sits below the free recognition
     gestures, visually separated. Currently disabled because the
     Stripe payment flow isn't wired yet; presented as "coming soon"
     so customers can see the intent without confusion about which
     thing is which. When Stripe is live, this becomes the entry point
     for real-money tips going directly to the contributor. */
  .detail-tip-real-wrap {
    margin-top: 14px;
    padding-top: 14px;
    border-top: 1px dashed rgba(15,17,22,0.10);
  }
  .detail-tip-real-btn {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 9px 14px;
    background: rgba(65, 105, 225, 0.06);
    border: 1.5px solid rgba(65, 105, 225, 0.18);
    border-radius: 10px;
    color: #4169E1;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .detail-tip-real-btn:hover:not(:disabled) {
    background: rgba(65, 105, 225, 0.12);
    border-color: #4169E1;
  }
  .detail-tip-real-btn:disabled {
    cursor: not-allowed;
    opacity: 0.75;
  }
  .detail-tip-real-soon {
    display: inline-flex; align-items: center;
    padding: 2px 8px;
    background: rgba(15,17,22,0.06);
    border-radius: 999px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.55);
    margin-left: 4px;
  }
  .detail-tip-real-sub {
    font-size: 11.5px;
    color: rgba(15,17,22,0.50);
    margin: 8px 0 0;
    line-height: 1.5;
  }

  /* ── Tip-thank-you toast (small, ephemeral) ── */
  .tip-toast {
    position: fixed;
    left: 50%;
    bottom: 24px;
    transform: translate(-50%, 80px);
    background: rgba(15,17,22,0.92);
    color: #fff;
    backdrop-filter: blur(8px);
    padding: 12px 18px 11px 16px;
    border-radius: 999px;
    font-size: 13px;
    font-weight: 500;
    box-shadow: 0 12px 32px rgba(15,17,22,0.30);
    z-index: 200;
    display: flex; align-items: center; gap: 10px;
    opacity: 0;
    transition: transform 320ms cubic-bezier(0.16, 1, 0.3, 1), opacity 240ms ease;
    pointer-events: none;
    max-width: 92vw;
  }
  .tip-toast.active {
    transform: translate(-50%, 0);
    opacity: 1;
  }
  .tip-toast-icon {
    color: #5B7DEB;
    flex-shrink: 0;
  }

  /* ── Pulses on the map are now tappable; subtle hover cue ── */
  .feed-pulse {
    cursor: pointer;
  }
  .feed-pulse:hover .feed-pulse-dot {
    transform: scale(1.1);
    transition: transform 140ms ease;
  }
  /* Story cards on the feed are also tappable now */
  .story {
    cursor: pointer;
    transition: border-color 140ms ease, transform 80ms ease;
  }
  .story:hover {
    border-color: rgba(65,105,225,0.25);
  }
  .story:active { transform: scale(0.997); }

  /* Mobile tweaks */
  @media (max-width: 480px) {
    .crew-screen { padding: 0 14px 80px; }
    .member-card { padding: 24px 20px 20px; }
    .member-name { font-size: 24px; }
    .crew-stats { grid-template-columns: 1fr 1fr; gap: 10px; }
    .crew-stat-value { font-size: 26px; }
    .badge-grid { grid-template-columns: repeat(3, 1fr); }
    .feed-map { height: 380px; }
  }

  /* ════════════════════════════════════════════════════════════════════════
     AUTH GATE — sign-in prompt for Crew participation actions
     ════════════════════════════════════════════════════════════════════════
     A small calm modal shown when a signed-out user taps verify/tip/report.
     Same visual language as the Crew screens (Royal Blue accent, Fraunces
     serif title) but with a centered card rather than the full upsell
     pricing layout. Slides up + fades. */
  .auth-gate-scrim {
    position: fixed; inset: 0;
    background: rgba(15,17,22,0.55);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    z-index: 199;
    opacity: 0;
    pointer-events: none;
    transition: opacity 240ms ease;
  }
  .auth-gate-scrim.active {
    opacity: 1; pointer-events: auto;
  }
  .auth-gate-card {
    position: fixed;
    left: 50%; top: 50%;
    width: calc(100vw - 32px);
    max-width: 420px;
    transform: translate(-50%, calc(-50% + 16px));
    background: #fff;
    border-radius: 22px;
    padding: 32px 24px 24px;
    box-shadow: 0 30px 80px rgba(15,17,22,0.30);
    z-index: 200;
    opacity: 0;
    pointer-events: none;
    transition: transform 320ms cubic-bezier(0.16, 1, 0.3, 1), opacity 240ms ease;
    text-align: center;
  }
  .auth-gate-card.active {
    transform: translate(-50%, -50%);
    opacity: 1;
    pointer-events: auto;
  }
  .auth-gate-close {
    position: absolute;
    top: 14px; right: 14px;
    width: 32px; height: 32px;
    border-radius: 50%;
    background: transparent;
    border: none;
    color: rgba(15,17,22,0.45);
    cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    -webkit-tap-highlight-color: transparent;
  }
  .auth-gate-close:hover { color: #0E1116; background: rgba(15,17,22,0.04); }
  .auth-gate-close svg { width: 18px; height: 18px; }

  .auth-gate-eyebrow {
    display: inline-flex; align-items: center; gap: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
    margin-bottom: 12px;
  }
  .auth-gate-eyebrow-dot {
    width: 6px; height: 6px; border-radius: 50%;
    background: var(--rb, #4169E1);
    box-shadow: 0 0 0 3px rgba(65,105,225,0.18);
  }
  .auth-gate-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    margin: 0 0 10px;
    letter-spacing: -0.3px;
    line-height: 1.25;
    color: #0E1116;
  }
  .auth-gate-body {
    font-size: 14px;
    color: rgba(15,17,22,0.65);
    line-height: 1.55;
    margin: 0 auto 20px;
    max-width: 340px;
  }
  .auth-gate-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
    min-height: 52px;
    padding: 0 22px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 14px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 15px;
    cursor: pointer;
    box-shadow: 0 8px 22px rgba(65,105,225,0.26);
    transition: transform 80ms ease, box-shadow 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .auth-gate-cta:hover {
    box-shadow: 0 12px 28px rgba(65,105,225,0.34);
  }
  .auth-gate-cta:active {
    transform: scale(0.99);
  }
  .auth-gate-secondary {
    display: block;
    margin: 14px auto 0;
    background: transparent;
    border: none;
    color: rgba(15,17,22,0.55);
    font-size: 13px;
    cursor: pointer;
    padding: 8px 14px;
    -webkit-tap-highlight-color: transparent;
  }
  .auth-gate-secondary:hover { color: #0E1116; }
  .auth-gate-foot {
    margin: 18px 0 0;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: rgba(15,17,22,0.45);
    letter-spacing: 0.4px;
  }

  /* Tab switcher between Sign in / Create account */
  .auth-tabs {
    display: flex;
    gap: 4px;
    background: rgba(15,17,22,0.04);
    border-radius: 999px;
    padding: 4px;
    margin: 4px 0 18px;
  }
  .auth-tab {
    flex: 1;
    background: transparent;
    border: none;
    padding: 9px 14px;
    font: inherit;
    font-size: 13px;
    font-weight: 600;
    color: rgba(15,17,22,0.55);
    border-radius: 999px;
    cursor: pointer;
    transition: all 160ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .auth-tab.is-active {
    background: #fff;
    color: #0E1116;
    box-shadow: 0 2px 8px rgba(15,17,22,0.08);
  }
  .auth-tab:hover:not(.is-active) {
    color: #0E1116;
  }

  /* Form panels — only one shown at a time */
  .auth-panel {
    display: none;
    text-align: left;
  }
  .auth-panel.is-active {
    display: block;
  }
  .auth-form {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .auth-field {
    display: block;
  }
  .auth-field-row {
    display: flex;
    gap: 10px;
  }
  .auth-field-half {
    flex: 1;
    min-width: 0;
  }
  /* ── Home-base location field — required for severe alert routing ── */
  .auth-location-wrap {
    display: flex; gap: 8px;
    align-items: stretch;
  }
  .auth-location-wrap .auth-input {
    flex: 1;
  }
  .auth-location-geo-btn {
    flex-shrink: 0;
    padding: 0 14px;
    background: rgba(65, 105, 225, 0.08);
    border: 1px solid rgba(65, 105, 225, 0.30);
    border-radius: 10px;
    color: #2447b0;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 160ms ease;
    display: flex; align-items: center; gap: 6px;
    white-space: nowrap;
  }
  .auth-location-geo-btn:hover {
    background: rgba(65, 105, 225, 0.14);
    border-color: rgba(65, 105, 225, 0.50);
  }
  .auth-location-geo-btn:disabled {
    opacity: 0.55;
    cursor: not-allowed;
  }
  .auth-location-geo-btn.is-loading {
    color: rgba(65, 105, 225, 0.65);
  }
  .auth-location-help {
    margin-top: 6px;
    font-size: 11px;
    color: rgba(15, 17, 22, 0.55);
    line-height: 1.4;
  }
  .auth-location-help-em { color: rgba(65, 105, 225, 0.85); font-weight: 500; }
  .auth-field-label {
    display: block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    color: rgba(15,17,22,0.55);
    margin-bottom: 6px;
  }
  .auth-input {
    width: 100%;
    padding: 12px 14px;
    background: #fff;
    border: 1.5px solid rgba(15,17,22,0.10);
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    color: #0E1116;
    transition: border-color 140ms ease, box-shadow 140ms ease;
    -webkit-appearance: none;
    appearance: none;
  }
  .auth-input:focus {
    outline: none;
    border-color: var(--rb, #4169E1);
    box-shadow: 0 0 0 3px rgba(65,105,225,0.14);
  }
  .auth-input::placeholder {
    color: rgba(15,17,22,0.35);
  }
  .auth-handle-wrap {
    position: relative;
    display: flex;
    align-items: center;
  }
  .auth-handle-prefix {
    position: absolute;
    left: 14px;
    color: rgba(15,17,22,0.45);
    font-size: 14px;
    font-weight: 600;
    pointer-events: none;
  }
  .auth-input-handle {
    padding-left: 28px;
  }

  .auth-error {
    color: #C2342B;
    background: rgba(194,52,43,0.06);
    border: 1px solid rgba(194,52,43,0.20);
    border-radius: 10px;
    padding: 10px 12px;
    font-size: 12.5px;
    line-height: 1.4;
    margin: 2px 0 0;
  }

  .auth-submit {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
    min-height: 50px;
    padding: 0 18px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 14px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    box-shadow: 0 8px 22px rgba(65,105,225,0.26);
    transition: transform 80ms ease, box-shadow 140ms ease;
    margin-top: 4px;
    -webkit-tap-highlight-color: transparent;
  }
  .auth-submit:hover {
    box-shadow: 0 12px 28px rgba(65,105,225,0.32);
  }
  .auth-submit:active {
    transform: scale(0.99);
  }
  .auth-submit:disabled {
    opacity: 0.55;
    cursor: not-allowed;
  }

  .auth-quick {
    text-align: center;
    margin: 4px 0 0;
  }
  .auth-quick-link {
    background: transparent;
    border: none;
    color: var(--rb, #4169E1);
    font-size: 12.5px;
    font-weight: 500;
    cursor: pointer;
    padding: 6px 12px;
    -webkit-tap-highlight-color: transparent;
  }
  .auth-quick-link:hover {
    text-decoration: underline;
  }

  /* ════════════════════════════════════════════════════════════════════════
     ONBOARDING — three-card explainer shown to brand-new accounts
     ════════════════════════════════════════════════════════════════════════
     Fires once after signup. Three steps: what the Crew is, how reports
     become data, what tokens unlock. Skippable. After completion, the
     user's `onboardingShown` flag is set so it never shows again.
     Built deliberately as a positive, light first impression — not as a
     gauntlet of marketing claims. */
  .onboarding-scrim {
    position: fixed; inset: 0;
    background: rgba(15,17,22,0.55);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    z-index: 199;
    opacity: 0;
    pointer-events: none;
    transition: opacity 240ms ease;
  }
  .onboarding-scrim.active {
    opacity: 1;
    pointer-events: auto;
  }
  .onboarding-card {
    position: fixed;
    left: 50%; top: 50%;
    width: calc(100vw - 32px);
    max-width: 440px;
    transform: translate(-50%, calc(-50% + 16px));
    background: #fff;
    border-radius: 22px;
    padding: 28px 24px 22px;
    box-shadow: 0 30px 80px rgba(15,17,22,0.30);
    z-index: 200;
    opacity: 0;
    pointer-events: none;
    transition: transform 320ms cubic-bezier(0.16, 1, 0.3, 1), opacity 240ms ease;
    text-align: center;
  }
  .onboarding-card.active {
    transform: translate(-50%, -50%);
    opacity: 1;
    pointer-events: auto;
  }
  .onboarding-skip {
    position: absolute;
    top: 14px; right: 16px;
    background: transparent;
    border: none;
    color: rgba(15,17,22,0.45);
    font-size: 12px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .onboarding-skip:hover { color: #0E1116; }

  .onboarding-illustration {
    width: 96px; height: 96px;
    margin: 8px auto 18px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(65,105,225,0.12) 0%, rgba(65,105,225,0.04) 100%);
    border: 1px solid rgba(65,105,225,0.18);
    display: flex; align-items: center; justify-content: center;
    color: var(--rb, #4169E1);
  }
  .onboarding-illustration svg { width: 44px; height: 44px; }

  .onboarding-eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px; font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
    margin: 0 0 6px;
  }
  .onboarding-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 24px;
    font-weight: 600;
    margin: 0 0 12px;
    letter-spacing: -0.3px;
    line-height: 1.2;
    color: #0E1116;
  }
  .onboarding-body {
    font-size: 14.5px;
    line-height: 1.6;
    color: rgba(15,17,22,0.70);
    margin: 0 auto 20px;
    max-width: 340px;
  }
  .onboarding-progress {
    display: flex;
    justify-content: center;
    gap: 6px;
    margin-bottom: 18px;
  }
  .onboarding-progress-dot {
    width: 6px; height: 6px;
    border-radius: 999px;
    background: rgba(15,17,22,0.16);
    transition: all 200ms ease;
  }
  .onboarding-progress-dot.is-active {
    background: var(--rb, #4169E1);
    width: 22px;
  }
  .onboarding-actions {
    display: flex;
    gap: 10px;
    align-items: center;
    justify-content: center;
  }
  .onboarding-cta {
    flex: 1;
    min-height: 48px;
    padding: 0 22px;
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
    border-radius: 14px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center; gap: 8px;
    box-shadow: 0 6px 18px rgba(65,105,225,0.22);
    transition: transform 80ms ease, box-shadow 140ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .onboarding-cta:hover {
    box-shadow: 0 10px 24px rgba(65,105,225,0.28);
  }
  .onboarding-cta:active {
    transform: scale(0.99);
  }
  .onboarding-back {
    background: transparent;
    border: none;
    color: rgba(15,17,22,0.55);
    font-size: 13px;
    font-weight: 500;
    cursor: pointer;
    padding: 8px 14px;
    -webkit-tap-highlight-color: transparent;
  }
  .onboarding-back:hover { color: #0E1116; }

  /* ── Profile signed-out empty state ───────────────────────────────────
     Calm explainer card that stands in for the member card when the user
     hasn't signed in yet. Shows what they get + lets them browse the
     Live Feed first without committing. */
  .profile-signed-out {
    text-align: center;
    padding: 40px 20px 28px;
    margin-top: 24px;
  }
  .profile-signed-out-icon {
    width: 64px; height: 64px;
    margin: 0 auto 16px;
    border-radius: 50%;
    background: rgba(65,105,225,0.08);
    color: var(--rb, #4169E1);
    display: flex; align-items: center; justify-content: center;
    border: 1px solid rgba(65,105,225,0.16);
  }
  .profile-signed-out-icon svg { width: 28px; height: 28px; }
  .profile-signed-out-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 24px;
    font-weight: 600;
    margin: 0 0 10px;
    letter-spacing: -0.3px;
    color: #0E1116;
  }
  .profile-signed-out-body {
    font-size: 14px;
    line-height: 1.55;
    color: rgba(15,17,22,0.65);
    max-width: 380px;
    margin: 0 auto 24px;
  }
  .profile-signed-out-actions {
    display: flex;
    flex-direction: column;
    gap: 10px;
    align-items: center;
  }
  .profile-signed-out-cta {
    display: inline-flex; align-items: center; justify-content: center;
    gap: 8px;
    min-height: 50px;
    padding: 0 28px;
    background: linear-gradient(180deg, #5278EA 0%, #4169E1 50%, #3257CC 100%);
    color: #fff;
    border: none;
    border-radius: 14px;
    font-family: 'Inter', system-ui, sans-serif;
    font-weight: 600;
    font-size: 14px;
    letter-spacing: 0.1px;
    cursor: pointer;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.20),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 1px 2px rgba(65, 105, 225, 0.30),
      0 8px 20px rgba(65, 105, 225, 0.22);
    transition: transform 120ms cubic-bezier(0.2, 0.8, 0.2, 1),
                box-shadow 220ms ease;
    -webkit-tap-highlight-color: transparent;
  }
  .profile-signed-out-cta:hover {
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.25),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 2px 4px rgba(65, 105, 225, 0.36),
      0 14px 28px rgba(65, 105, 225, 0.30);
  }
  .profile-signed-out-cta:active { transform: translateY(0); }
  .profile-signed-out-alt {
    background: transparent;
    border: none;
    color: var(--rb, #4169E1);
    font-size: 13px;
    font-weight: 500;
    cursor: pointer;
    padding: 8px 14px;
    -webkit-tap-highlight-color: transparent;
  }
  .profile-signed-out-alt:hover { text-decoration: underline; }

  /* When signed in, the signed-out block hides; when signed out, the
     signed-in block hides. Both states default to display:none so neither
     flashes; JS sets the right one on init. */
  .profile-signed-out, .profile-signed-in {
    display: none;
  }
  body.is-signed-in .profile-signed-in   { display: block; }
  body:not(.is-signed-in) .profile-signed-out { display: block; }

  /* Sign Out small text link in Profile */
  /* ── Crew home base card (Item #6) ──────────────────────────
     Mission targeting location. Lives on Crew Profile between actions
     and signout. Matches the calm, restrained Crew aesthetic. */
  .crew-homebase-card {
    margin-top: 28px;
    padding: 20px;
    background: #fff;
    border: 1px solid rgba(15,17,22,0.08);
    border-radius: 14px;
  }
  .crew-homebase-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
  }
  .crew-homebase-label {
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(15,17,22,0.55);
  }
  .crew-homebase-edit {
    background: transparent;
    border: 1px solid rgba(15,17,22,0.12);
    color: rgba(15,17,22,0.75);
    font-size: 12px;
    font-weight: 500;
    padding: 6px 14px;
    border-radius: 6px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-homebase-edit:hover {
    background: rgba(15,17,22,0.04);
    color: #0E1116;
  }
  .crew-homebase-body { margin-bottom: 14px; }
  .crew-homebase-placeholder-title {
    font-size: 14px;
    font-weight: 500;
    color: #0E1116;
    margin: 0 0 4px;
  }
  .crew-homebase-placeholder-sub {
    font-size: 12.5px;
    color: rgba(15,17,22,0.55);
    line-height: 1.5;
    margin: 0;
  }
  .crew-homebase-label-text {
    font-size: 15px;
    font-weight: 500;
    color: #0E1116;
    margin: 0 0 3px;
  }
  .crew-homebase-coords {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: rgba(15,17,22,0.45);
    margin: 0;
  }
  .crew-homebase-toggle {
    padding-top: 14px;
    border-top: 1px solid rgba(15,17,22,0.06);
  }
  .crew-homebase-toggle-row {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    cursor: pointer;
  }
  .crew-homebase-toggle-row input[type="checkbox"] {
    margin-top: 3px;
    flex-shrink: 0;
  }
  .crew-homebase-toggle-text {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .crew-homebase-toggle-text strong {
    font-size: 13px;
    font-weight: 500;
    color: #0E1116;
  }
  .crew-homebase-toggle-sub {
    font-size: 11.5px;
    color: rgba(15,17,22,0.50);
    line-height: 1.4;
  }

  /* ── Crew home base edit modal ──────────────────────────────
     Map-based pin-drop modal mirroring the subscriber saved-location
     modal pattern. Address lookup + draggable Leaflet marker. */
  .crew-homebase-modal {
    position: fixed; inset: 0; z-index: 200;
    display: none;
    align-items: center; justify-content: center;
    padding: 20px;
  }
  .crew-homebase-modal[hidden] { display: none; }
  .crew-homebase-modal.is-open { display: flex; }
  .crew-homebase-modal-scrim {
    position: absolute; inset: 0;
    background: rgba(15,17,22,0.55);
    backdrop-filter: blur(6px); -webkit-backdrop-filter: blur(6px);
  }
  .crew-homebase-modal-card {
    position: relative;
    width: 100%; max-width: 540px;
    background: #fff;
    border-radius: 14px;
    overflow: hidden;
    max-height: calc(100vh - 40px);
    display: flex; flex-direction: column;
  }
  .crew-homebase-modal-head {
    display: flex; align-items: center; justify-content: space-between;
    padding: 18px 22px;
    border-bottom: 1px solid rgba(15,17,22,0.08);
  }
  .crew-homebase-modal-title {
    font-size: 16px; font-weight: 600;
    color: #0E1116;
    margin: 0;
  }
  .crew-homebase-modal-close {
    background: transparent; border: none;
    color: rgba(15,17,22,0.55);
    font-size: 20px; cursor: pointer;
    padding: 4px 10px;
    line-height: 1;
  }
  .crew-homebase-modal-body {
    padding: 18px 22px;
    overflow-y: auto;
    flex: 1;
  }
  .crew-homebase-modal-field { margin-bottom: 14px; }
  .crew-homebase-modal-field-label {
    display: block;
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(15,17,22,0.55);
    margin-bottom: 6px;
  }
  .crew-homebase-modal-input {
    width: 100%;
    padding: 10px 12px;
    border: 1px solid rgba(15,17,22,0.15);
    border-radius: 8px;
    font-size: 14px;
    font-family: inherit;
    box-sizing: border-box;
  }
  .crew-homebase-modal-input:focus {
    outline: none;
    border-color: var(--rb, #4169E1);
  }
  .crew-homebase-modal-lookup {
    margin-top: 8px;
    background: transparent;
    border: 1px solid rgba(15,17,22,0.12);
    color: rgba(15,17,22,0.70);
    font-size: 12px;
    padding: 6px 12px;
    border-radius: 6px;
    cursor: pointer;
  }
  .crew-homebase-modal-lookup:hover {
    background: rgba(15,17,22,0.04);
  }
  .crew-homebase-modal-map {
    width: 100%;
    height: 280px;
    border-radius: 8px;
    overflow: hidden;
    margin-top: 6px;
  }
  .crew-homebase-modal-hint {
    font-size: 11.5px;
    color: rgba(15,17,22,0.50);
    margin: 6px 0 0;
    line-height: 1.4;
  }
  .crew-homebase-modal-actions {
    display: flex; justify-content: flex-end; gap: 10px;
    padding: 16px 22px;
    border-top: 1px solid rgba(15,17,22,0.08);
  }
  .crew-homebase-modal-cancel,
  .crew-homebase-modal-save {
    padding: 10px 20px;
    border-radius: 8px;
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }
  .crew-homebase-modal-cancel {
    background: transparent;
    border: 1px solid rgba(15,17,22,0.15);
    color: rgba(15,17,22,0.70);
  }
  .crew-homebase-modal-save {
    background: var(--rb, #4169E1);
    color: #fff;
    border: none;
  }
  .crew-homebase-modal-save:disabled {
    background: rgba(15,17,22,0.20);
    cursor: not-allowed;
  }

  .signout-row {
    text-align: center;
    margin-top: 28px;
    padding-top: 20px;
    border-top: 1px solid rgba(15,17,22,0.06);
  }
  .signout-link {
    background: transparent;
    border: none;
    color: rgba(15,17,22,0.45);
    font-size: 12px;
    cursor: pointer;
    padding: 8px 14px;
    -webkit-tap-highlight-color: transparent;
  }
  .signout-link:hover { color: var(--rb, #4169E1); }

  /* ── Modal ── */
  .modal-scrim {
    position: fixed; inset: 0; z-index: 100;
    background: rgba(15,17,22,0.55);
    backdrop-filter: blur(6px); -webkit-backdrop-filter: blur(6px);
    display: none;
    align-items: center; justify-content: center;
    padding: 20px;
  }
  .modal-scrim.active { display: flex; animation: scrimIn 280ms ease; }
  @keyframes scrimIn { from { opacity: 0; } to { opacity: 1; } }

  /* ── Email-capture modal ─────────────────────────────────────────
     For May 24 launch — replaces real subscribe/Crew signup until
     Stripe and Twilio are wired. Captures email + name + ZIP so we
     can pre-forecast their area when subscriptions open. */
  .email-capture-scrim {
    position: fixed; inset: 0; z-index: 200;
    background: rgba(15,17,22,0.55);
    backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px);
    display: flex;
    align-items: center; justify-content: center;
    padding: 20px;
    animation: scrimIn 280ms ease;
  }
  .email-capture-scrim[hidden] { display: none; }
  .email-capture-modal {
    position: relative;
    width: 100%; max-width: 480px; max-height: 92vh;
    overflow-y: auto;
    background:
      linear-gradient(135deg,
        rgba(65, 105, 225, 0.04) 0%,
        rgba(255, 255, 255, 1) 60%,
        rgba(245, 165, 36, 0.02) 100%),
      #fff;
    border-radius: 22px;
    padding: 36px 30px 30px;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 30px 80px rgba(15, 17, 22, 0.30),
      0 8px 24px rgba(65, 105, 225, 0.12);
    animation: modalIn 480ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .email-capture-close {
    position: absolute;
    top: 14px; right: 14px;
    width: 32px; height: 32px;
    border-radius: 50%;
    background: rgba(15, 17, 22, 0.05);
    color: rgba(15, 17, 22, 0.55);
    border: none;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background 140ms ease, color 140ms ease;
  }
  .email-capture-close:hover {
    background: rgba(15, 17, 22, 0.10);
    color: #0E1116;
  }
  .email-capture-eyebrow {
    display: inline-flex; align-items: center; gap: 7px;
    padding: 4px 12px;
    background: rgba(65, 105, 225, 0.08);
    border-radius: 999px;
    margin-bottom: 14px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 1.6px;
    text-transform: uppercase;
    color: var(--rb, #4169E1);
  }
  .email-capture-eyebrow.is-crew {
    background: rgba(232, 164, 0, 0.10);
    color: #8a5a0a;
  }
  .email-capture-eyebrow-dot {
    width: 5px; height: 5px;
    border-radius: 999px;
    background: currentColor;
    position: relative;
  }
  .email-capture-eyebrow-dot::after {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 999px;
    background: currentColor;
    opacity: 0.35;
    animation: dotPulse 2.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    pointer-events: none;
  }
  .email-capture-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 24px;
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: -0.5px;
    color: #0E1116;
    margin: 0 0 10px;
  }
  .email-capture-body {
    font-size: 14px;
    line-height: 1.55;
    color: rgba(15, 17, 22, 0.65);
    margin: 0 0 22px;
  }
  .email-capture-fields,
  .email-capture-meta {
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
    margin-bottom: 12px;
  }
  @media (min-width: 480px) {
    .email-capture-fields { grid-template-columns: 1fr 1fr; }
  }
  .email-capture-submit {
    width: 100%;
    padding: 14px 18px;
    background: linear-gradient(180deg, #5278EA 0%, #4169E1 50%, #3257CC 100%);
    color: #fff;
    border: none;
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.1px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.20),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 1px 2px rgba(65, 105, 225, 0.30),
      0 4px 12px rgba(65, 105, 225, 0.20);
    transition: transform 120ms cubic-bezier(0.2, 0.8, 0.2, 1),
                box-shadow 220ms ease;
    margin-top: 6px;
  }
  .email-capture-submit:hover {
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.25),
      inset 0 -1px 0 rgba(0,0,0,0.10),
      0 2px 4px rgba(65, 105, 225, 0.36),
      0 12px 24px rgba(65, 105, 225, 0.28);
  }
  .email-capture-fine {
    font-size: 11.5px;
    color: rgba(15, 17, 22, 0.45);
    text-align: center;
    line-height: 1.5;
    margin: 12px 0 0;
  }
  .email-capture-success {
    text-align: center;
    padding: 20px 0 6px;
  }
  .email-capture-success-icon {
    color: #12A150;
    margin-bottom: 14px;
  }
  .email-capture-success-title {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -0.4px;
    color: #0E1116;
    margin: 0 0 10px;
  }
  .email-capture-success-body {
    font-size: 14px;
    line-height: 1.55;
    color: rgba(15, 17, 22, 0.65);
    margin: 0 0 22px;
  }
  .email-capture-success-close {
    padding: 11px 22px;
    background: transparent;
    border: 1.5px solid rgba(65, 105, 225, 0.30);
    color: var(--rb, #4169E1);
    border-radius: 12px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: all 140ms ease;
  }
  .email-capture-success-close:hover {
    background: rgba(65, 105, 225, 0.06);
    border-color: var(--rb, #4169E1);
  }

  /* ════════════════════════════════════════════════════════════════
     SIGN-IN — sticky pill in top-right + workspace selector modal
     ════════════════════════════════════════════════════════════════
     The visible Sign In button gives users a way to reach their
     workspace from anywhere on the homepage. In v1 it routes to the
     existing prototype workspaces (subscriber, Crew, Met, admin).
     In v2 (post-backend) it gates each workspace behind real auth.
     Same button position, same modal — only what happens AFTER
     clicking a workspace changes between versions. */
  .signin-pill {
    position: fixed;
    top: 14px;
    right: 14px;
    z-index: 90;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 16px;
    background: var(--rb, #4169E1);
    color: white;
    border: none;
    border-radius: 100px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: -0.1px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    box-shadow: 0 4px 14px rgba(65, 105, 225, 0.25);
    transition: all 160ms ease;
  }
  .signin-pill:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 18px rgba(65, 105, 225, 0.35);
  }
  .signin-pill svg {
    width: 12px; height: 12px;
    stroke-width: 2.5;
  }
  /* When the storm shelter banner is showing, scoot the pill down so
     it doesn't overlap. */
  body.has-storm-shelter .signin-pill {
    top: 64px;
  }
  @media (max-width: 480px) {
    .signin-pill {
      top: 10px;
      right: 10px;
      padding: 7px 13px;
      font-size: 12px;
    }
  }

  /* ── Logged-in pill variant: shows the user's email/name and toggles
        a small dropdown with "Sign out". Pattern matches Stripe, Linear,
        Notion — pill becomes user-aware when authed. ── */
  .signin-pill.is-authed {
    background: rgba(15, 17, 22, 0.04);
    color: #0E1116;
    border: 1px solid rgba(15, 17, 22, 0.10);
    box-shadow: 0 2px 8px rgba(15, 17, 22, 0.05);
  }
  .signin-pill.is-authed:hover {
    background: rgba(15, 17, 22, 0.08);
    box-shadow: 0 4px 12px rgba(15, 17, 22, 0.08);
  }
  .signin-pill-avatar {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: linear-gradient(135deg, #4169E1, #2E4FCC);
    color: white;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 11px;
    font-weight: 700;
    flex-shrink: 0;
    margin-right: 2px;
  }
  /* Dropdown menu shown below the pill when the authed pill is clicked. */
  .signin-pill-menu {
    position: fixed;
    top: 56px;
    right: 16px;
    z-index: 110;
    min-width: 240px;
    background: white;
    border-radius: 14px;
    border: 1px solid rgba(15, 17, 22, 0.08);
    box-shadow: 0 20px 40px rgba(15, 17, 22, 0.16);
    padding: 8px;
    animation: menuIn 160ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .signin-pill-menu[hidden] { display: none; }
  @keyframes menuIn {
    from { opacity: 0; transform: translateY(-4px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  body.has-storm-shelter .signin-pill-menu {
    top: 100px;
  }
  @media (max-width: 480px) {
    .signin-pill-menu {
      top: 48px;
      right: 10px;
      min-width: 220px;
    }
  }
  .signin-pill-menu-header {
    padding: 10px 12px 12px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
    margin-bottom: 4px;
  }
  .signin-pill-menu-name {
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
    word-break: break-all;
    line-height: 1.3;
  }
  .signin-pill-menu-roles {
    font-size: 11px;
    font-weight: 500;
    color: rgba(15, 17, 22, 0.55);
    margin-top: 3px;
    text-transform: capitalize;
  }
  .signin-pill-menu-item {
    display: flex;
    align-items: center;
    gap: 10px;
    width: 100%;
    padding: 10px 12px;
    background: none;
    border: none;
    border-radius: 8px;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 13px;
    font-weight: 500;
    color: #0E1116;
    text-align: left;
    cursor: pointer;
    transition: background 120ms ease;
  }
  .signin-pill-menu-item:hover {
    background: rgba(15, 17, 22, 0.05);
  }
  .signin-pill-menu-item.is-signout {
    color: #D43838;
  }
  .signin-pill-menu-item.is-signout:hover {
    background: rgba(212, 56, 56, 0.06);
  }
  .signin-pill-menu-item svg {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
    opacity: 0.7;
  }
  /* Workspace nav slot — list of links to the user's workspaces.
     One item per role, rendered between the header and sign-out. */
  .signin-pill-menu-workspaces {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .signin-pill-menu-workspaces:not(:empty) {
    padding-bottom: 4px;
    margin-bottom: 4px;
    border-bottom: 1px solid rgba(15, 17, 22, 0.06);
  }
  .signin-pill-menu-workspace-icon {
    width: 22px;
    height: 22px;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(65, 105, 225, 0.10);
    color: #4169E1;
    font-size: 11px;
    font-weight: 700;
    flex-shrink: 0;
  }
  .signin-pill-menu-workspace-icon svg { opacity: 1; }

  /* Sign-in modal — workspace selector */
  .signin-scrim {
    position: fixed;
    inset: 0;
    background: rgba(15, 17, 22, 0.50);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    z-index: 200;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    animation: scrimIn 280ms ease;
  }
  .signin-scrim[hidden] { display: none; }
  @keyframes scrimIn { from { opacity: 0; } to { opacity: 1; } }
  .signin-modal {
    position: relative;
    width: 100%;
    max-width: 440px;
    background: white;
    border-radius: 22px;
    padding: 32px 28px;
    box-shadow: 0 30px 80px rgba(15, 17, 22, 0.30);
    animation: modalIn 480ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  .signin-modal-close {
    position: absolute;
    top: 14px;
    right: 14px;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: rgba(15, 17, 22, 0.05);
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: rgba(15, 17, 22, 0.5);
    transition: all 160ms ease;
  }
  .signin-modal-close:hover {
    background: rgba(15, 17, 22, 0.10);
    color: rgba(15, 17, 22, 0.8);
  }
  .signin-modal-title {
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -0.4px;
    color: #0E1116;
    margin: 0 0 6px;
  }
  .signin-modal-body {
    font-size: 13px;
    line-height: 1.55;
    color: rgba(15, 17, 22, 0.65);
    margin: 0 0 24px;
  }
  .signin-workspace-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .signin-workspace-btn {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    background: rgba(65, 105, 225, 0.04);
    border: 1px solid rgba(65, 105, 225, 0.12);
    border-radius: 12px;
    cursor: pointer;
    text-align: left;
    transition: all 160ms ease;
    font-family: 'Inter', system-ui, sans-serif;
  }
  .signin-workspace-btn:hover {
    background: rgba(65, 105, 225, 0.08);
    border-color: rgba(65, 105, 225, 0.30);
    transform: translateY(-1px);
  }
  .signin-workspace-icon {
    width: 38px;
    height: 38px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: white;
  }
  .signin-workspace-icon svg {
    width: 18px;
    height: 18px;
    stroke-width: 2.2;
  }
  .signin-workspace-icon.is-subscriber {
    background: linear-gradient(135deg, #4169E1, #2E4FCC);
  }
  .signin-workspace-icon.is-crew {
    background: linear-gradient(135deg, #16C77E, #0FA666);
  }
  .signin-workspace-icon.is-met {
    background: linear-gradient(135deg, #7E5BEF, #5836C9);
  }
  .signin-workspace-icon.is-admin {
    background: linear-gradient(135deg, #0E1116, #2A2F38);
  }
  .signin-workspace-info { flex: 1; min-width: 0; }
  .signin-workspace-name {
    font-size: 14px;
    font-weight: 600;
    color: #0E1116;
    margin: 0 0 2px;
    letter-spacing: -0.2px;
  }
  .signin-workspace-detail {
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
    margin: 0;
    line-height: 1.4;
  }
  .signin-workspace-arrow {
    color: rgba(15, 17, 22, 0.35);
    flex-shrink: 0;
  }
  .signin-workspace-btn:hover .signin-workspace-arrow {
    color: var(--rb, #4169E1);
    transform: translateX(2px);
  }
  .signin-modal-foot {
    margin-top: 22px;
    padding-top: 18px;
    border-top: 1px solid rgba(15, 17, 22, 0.06);
    font-size: 12px;
    color: rgba(15, 17, 22, 0.55);
    text-align: center;
  }
  .signin-modal-foot a {
    color: var(--rb, #4169E1);
    text-decoration: none;
    font-weight: 600;
  }
  .signin-modal-foot a:hover { text-decoration: underline; }
  /* The admin option is hidden by default; only revealed in dev mode. */
  .signin-workspace-btn.is-admin-option { display: none; }
  body.has-dev-mode .signin-workspace-btn.is-admin-option { display: flex; }

  /* ── Multi-step modal: each [data-signin-step] is one screen ──
     JS toggles the `hidden` attribute on these to switch between
     email entry, check-email, workspace picker, account-pending,
     and verify-error states. */
  .signin-step[hidden] { display: none; }

  /* ── Email input field ── */
  .signin-field-label {
    display: block;
    font-size: 12px;
    font-weight: 600;
    color: rgba(15, 17, 22, 0.65);
    margin: 0 0 6px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }
  .signin-email-input {
    display: block;
    width: 100%;
    padding: 13px 14px;
    font-size: 15px;
    font-family: 'Inter', system-ui, sans-serif;
    color: #0E1116;
    background: rgba(15, 17, 22, 0.03);
    border: 1.5px solid rgba(15, 17, 22, 0.10);
    border-radius: 12px;
    outline: none;
    transition: border-color 160ms ease, background 160ms ease;
    box-sizing: border-box;
    -webkit-tap-highlight-color: transparent;
  }
  .signin-email-input:focus {
    border-color: var(--rb, #4169E1);
    background: white;
  }
  .signin-email-input::placeholder {
    color: rgba(15, 17, 22, 0.35);
  }
  /* Visible-but-inert state while submission is in flight */
  .signin-email-input[aria-busy="true"] {
    opacity: 0.6;
    pointer-events: none;
  }

  .signin-error-text {
    margin: 8px 0 0;
    font-size: 12px;
    color: #D43838;
    font-weight: 500;
  }

  /* ── Submit button (filled, brand-blue) ── */
  .signin-submit-btn {
    display: block;
    width: 100%;
    margin: 16px 0 0;
    padding: 13px 22px;
    font-size: 14px;
    font-weight: 600;
    font-family: 'Inter', system-ui, sans-serif;
    color: white;
    background: var(--rb, #4169E1);
    border: none;
    border-radius: 12px;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
    transition: background 160ms ease, transform 160ms ease;
  }
  .signin-submit-btn:hover:not(:disabled) {
    background: #3556C8;
    transform: translateY(-1px);
  }
  .signin-submit-btn:disabled {
    background: rgba(65, 105, 225, 0.5);
    cursor: not-allowed;
    transform: none;
  }
  .signin-submit-spinner {
    display: inline-block;
  }
  /* Ensure [hidden] wins regardless of selector specificity ordering.
     Without this, the .signin-submit-spinner rule above overrides the
     browser default [hidden] { display: none } because of cascade order
     — same specificity, later rule wins. Adding an explicit hidden
     rule for both label and spinner makes the hidden attribute work. */
  .signin-submit-label[hidden],
  .signin-submit-spinner[hidden] {
    display: none;
  }

  /* ── Inline link button (for "try a different email" etc) ── */
  .signin-link-btn {
    background: none;
    border: none;
    color: var(--rb, #4169E1);
    font-weight: 600;
    font-family: 'Inter', system-ui, sans-serif;
    font-size: inherit;
    cursor: pointer;
    padding: 0;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 2px;
  }
  .signin-link-btn:hover { color: #3556C8; }

  /* ── Check-email step: large icon + centered text ── */
  .signin-check-email-icon {
    width: 64px;
    height: 64px;
    margin: 0 auto 16px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(65, 105, 225, 0.12), rgba(65, 105, 225, 0.04));
    color: var(--rb, #4169E1);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  [data-signin-step="check-email"] {
    text-align: center;
  }
  [data-signin-step="check-email"] .signin-modal-title,
  [data-signin-step="check-email"] .signin-modal-body {
    text-align: center;
  }

  /* ── Account-pending step: centered, friendly ── */
  [data-signin-step="account-pending"],
  [data-signin-step="verify-error"] {
    text-align: center;
  }
  [data-signin-step="account-pending"] .signin-modal-title,
  [data-signin-step="verify-error"] .signin-modal-title {
    margin-top: 8px;
  }

  .modal-card {
    position: relative;
    width: 100%; max-width: 480px; max-height: 90vh;
    overflow-y: auto;
    background: #fff;
    border-radius: 22px;
    padding: 32px 28px;
    box-shadow: 0 30px 80px rgba(15,17,22,0.30);
    animation: modalIn 480ms cubic-bezier(0.16, 1, 0.3, 1);
  }
  @keyframes modalIn {
    from { opacity: 0; transform: translateY(20px) scale(0.96); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }
  .modal-close {
    position: absolute; top: 14px; right: 14px;
    width: 32px; height: 32px; border-radius: 999px;
    display: flex; align-items: center; justify-content: center;
    color: rgba(15,17,22,0.55);
    transition: background 180ms;
  }
  .modal-close:hover { background: rgba(15,17,22,0.06); }
  .modal-close svg { width: 16px; height: 16px; }

  .modal-step { display: none; }
  .modal-step.active { display: block; }

  .modal-eyebrow {
    display: inline-flex; align-items: center; gap: 8px;
    margin-bottom: 14px;
  }
  .modal-eyebrow-dot {
    width: 6px; height: 6px; border-radius: 999px; background: #4169E1;
  }
  .modal-eyebrow-label {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    color: #4169E1; letter-spacing: 2.2px; text-transform: uppercase;
  }
  .modal-title {
    font-family: 'Fraunces'; font-size: 26px; font-weight: 600;
    color: #0E1116; letter-spacing: -0.6px; line-height: 1.2;
    margin-bottom: 12px;
  }
  .modal-body {
    font-family: 'Fraunces'; font-size: 15px; line-height: 1.55;
    color: rgba(15,17,22,0.60); margin-bottom: 24px;
  }

  /* ── Benefit list (premium feel) ── */
  .benefit-list {
    list-style: none; padding: 0; margin: 0 0 24px;
    display: flex; flex-direction: column; gap: 12px;
  }
  .benefit-item {
    display: flex; align-items: flex-start; gap: 12px;
    padding: 12px 14px;
    background: rgba(65,105,225,0.03);
    border-radius: 10px;
    border: 1px solid rgba(65,105,225,0.08);
  }
  .benefit-icon {
    flex-shrink: 0;
    width: 22px; height: 22px;
    border-radius: 999px;
    background: rgba(65,105,225,0.10);
    color: #4169E1;
    display: inline-flex; align-items: center; justify-content: center;
    margin-top: 1px;
  }
  .benefit-icon svg { width: 12px; height: 12px; }
  .benefit-text { display: flex; flex-direction: column; gap: 2px; min-width: 0; }
  .benefit-title {
    font-size: 13.5px; font-weight: 700;
    color: #0E1116; letter-spacing: -0.1px;
    line-height: 1.35;
  }
  .benefit-sub {
    font-size: 12.5px; line-height: 1.45;
    color: rgba(15,17,22,0.55);
  }

  /* ── Pricing divider ── */
  .pricing-divider {
    display: flex; align-items: center; gap: 12px;
    margin: 8px 0 16px;
  }
  .pricing-divider::before, .pricing-divider::after {
    content: ""; flex: 1;
    height: 1px;
    background: rgba(15,17,22,0.08);
  }
  .pricing-divider-label {
    font-family: 'JetBrains Mono'; font-size: 10px; font-weight: 700;
    letter-spacing: 2.2px; text-transform: uppercase;
    color: rgba(15,17,22,0.45);
  }

  /* Founder tier — slightly elevated */
  .pricing-option-founder {
    background: linear-gradient(135deg, rgba(65,105,225,0.02), rgba(91,123,255,0.04));
  }
  .founder-flag {
    position: absolute;
    top: -8px; left: 14px;
    padding: 2px 10px;
    border-radius: 999px;
    background: linear-gradient(135deg, #2848B0, #4169E1);
    color: #fff;
    font-size: 9.5px; font-weight: 700;
    font-family: 'JetBrains Mono';
    letter-spacing: 1.2px; text-transform: uppercase;
    box-shadow: 0 4px 10px rgba(40,72,176,0.30);
  }

  /* SMS label text */
  .sms-label-text {
    display: block;
    font-size: 11px; font-weight: 600;
    color: rgba(15,17,22,0.55);
    letter-spacing: 1.2px; text-transform: uppercase;
    font-family: 'JetBrains Mono';
    margin-bottom: 8px;
  }

  /* Trust footer in modal */
  .modal-trust {
    display: flex; align-items: center; gap: 8px;
    margin: 16px 0 0;
    padding: 12px 14px;
    background: rgba(15,17,22,0.025);
    border-radius: 10px;
    font-size: 11.5px; line-height: 1.5;
    color: rgba(15,17,22,0.55);
  }
  .modal-trust svg {
    flex-shrink: 0;
    width: 13px; height: 13px;
    color: rgba(65,105,225,0.70);
  }

  /* Standby reassurance line */
  .standby-reassurance {
    text-align: center;
    font-family: 'Fraunces';
    font-size: 13.5px; line-height: 1.55;
    color: rgba(15,17,22,0.55);
    font-style: italic;
    margin: -4px 0 20px;
  }

  /* Pricing options */
  .pricing-options { display: flex; flex-direction: column; gap: 10px; margin-bottom: 20px; }
  .pricing-option {
    position: relative;
    text-align: left;
    padding: 16px;
    border-radius: 14px;
    background: #fff;
    border: 1.5px solid rgba(15,17,22,0.10);
    transition: all 220ms cubic-bezier(0.16, 1, 0.3, 1);
    width: 100%;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }
  .pricing-option:hover {
    border-color: rgba(65,105,225,0.40);
    background: rgba(65,105,225,0.02);
    transform: translateY(-1px);
  }
  .pricing-option.is-selected {
    border-color: #4169E1;
    background: linear-gradient(135deg, rgba(65,105,225,0.05), rgba(91,123,255,0.08));
    box-shadow: 0 0 0 4px rgba(65,105,225,0.10), 0 8px 24px rgba(65,105,225,0.10);
  }
  .pricing-option.is-popular::before {
    content: "Most chosen";
    position: absolute;
    top: -8px; right: 14px;
    padding: 2px 10px;
    border-radius: 999px;
    background: #4169E1;
    color: #fff;
    font-size: 9.5px; font-weight: 700;
    font-family: 'JetBrains Mono';
    letter-spacing: 1.2px; text-transform: uppercase;
    box-shadow: 0 4px 10px rgba(65,105,225,0.30);
  }
  .pricing-option-head {
    display: flex; justify-content: space-between; align-items: baseline;
    margin-bottom: 4px;
  }
  .pricing-option-label {
    font-size: 14.5px; font-weight: 700; color: #0E1116;
    letter-spacing: -0.1px;
  }
  .pricing-option-price {
    font-family: 'Fraunces'; font-size: 24px; font-weight: 600;
    color: #0E1116; letter-spacing: -0.6px;
  }
  .pricing-option-price-unit {
    font-size: 12px; font-weight: 500;
    color: rgba(15,17,22,0.50);
    margin-left: 2px;
  }
  .pricing-option-sub {
    font-size: 12.5px; line-height: 1.5;
    color: rgba(15,17,22,0.55);
  }

  /* SMS input */
  .sms-block { margin-bottom: 24px; }
  .sms-label { display: block; }
  .sms-block input {
    width: 100%;
    padding: 12px 14px;
    border-radius: 10px;
    border: 1.5px solid rgba(15,17,22,0.10);
    font-size: 14px;
    background: #fff;
    color: #0E1116;
    outline: none;
    transition: border-color 180ms;
  }
  .sms-block input:focus { border-color: #4169E1; }
  .sms-hint {
    display: block;
    font-family: 'JetBrains Mono'; font-size: 11px;
    color: rgba(15,17,22,0.50);
    margin-top: 8px; padding-left: 4px;
  }

  /* Modal actions */
  .modal-actions {
    display: flex; gap: 10px;
    align-items: center;
  }
  .modal-cancel {
    flex: 0 0 auto;
    padding: 12px 18px;
    border-radius: 999px;
    font-size: 13px; font-weight: 600;
    color: rgba(15,17,22,0.55);
    transition: color 180ms;
  }
  .modal-cancel:hover { color: #0E1116; }
  .modal-confirm {
    flex: 1;
    display: inline-flex; align-items: center; justify-content: center;
    gap: 8px;
    padding: 14px 20px;
    border-radius: 999px;
    background: #4169E1; color: #fff;
    font-size: 14px; font-weight: 700;
    box-shadow: 0 6px 20px rgba(65,105,225,0.32);
    transition: all 200ms;
  }
  .modal-confirm:hover { transform: translateY(-1px); box-shadow: 0 10px 26px rgba(65,105,225,0.42); }
  .modal-confirm svg { width: 14px; height: 14px; }
  .modal-confirm-full { width: 100%; }

  /* Standby step */
  .standby-icon {
    width: 64px; height: 64px;
    border-radius: 999px;
    background: rgba(65,105,225,0.06);
    border: 1.5px solid rgba(65,105,225,0.30);
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 24px;
    color: #4169E1;
    animation: pulseSoft 2.4s ease-in-out infinite;
  }
  .standby-icon svg { width: 28px; height: 28px; }
  @keyframes pulseSoft {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }
  .standby-meta {
    background: rgba(15,17,22,0.03);
    border-radius: 12px;
    padding: 14px 16px;
    margin-bottom: 20px;
  }
  .standby-meta-row {
    display: flex; justify-content: space-between;
    padding: 6px 0;
    border-bottom: 1px solid rgba(15,17,22,0.06);
  }
  .standby-meta-row:last-child { border-bottom: none; }
  .standby-meta-label {
    font-family: 'JetBrains Mono'; font-size: 10.5px;
    color: rgba(15,17,22,0.50); letter-spacing: 1px; text-transform: uppercase;
  }
  .standby-meta-value {
    font-size: 13px; font-weight: 600; color: #0E1116;
  }
  .phone-display {
    font-family: 'JetBrains Mono';
    font-weight: 600;
    color: #4169E1;
  }

  /* Animations */
  @keyframes pulse {
    0%, 100% { opacity: 0.6; transform: scale(0.9); }
    50% { opacity: 1; transform: scale(1.05); }
  }
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(14px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes keyEnter {
    0% { opacity: 0; transform: scale(0.6) rotate(-12deg); }
    60% { opacity: 1; transform: scale(1.08) rotate(0); }
    100% { opacity: 1; transform: scale(1); }
  }
  @keyframes ticketIn {
    0% { opacity: 0; transform: translateY(20px) scale(0.96); }
    55% { opacity: 1; transform: translateY(0) scale(1.02); }
    100% { opacity: 1; transform: translateY(0) scale(1); }
  }
</style>
</head>
<body>

<!-- ============ STATIC HTML — works even if JS is blocked ============ -->

<!-- Atmospheric depth layers — sit behind all content (z-index 0).
     Three layers compound to create dimensional, weather-feeling space:
     orbs (ambient color light), weather-grid (faint topographic
     texture), vignette-wash (lit-from-above effect). Workspace screens
     hide these via body.is-workspace-active. -->
<div class="vignette-wash" aria-hidden="true"></div>
<div class="weather-grid" aria-hidden="true"></div>
<div class="orb orb-blue" aria-hidden="true"></div>
<div class="orb orb-deep" aria-hidden="true"></div>
<div class="orb orb-amber" aria-hidden="true"></div>

<div class="page">
<div class="page-content">

<!-- ============ WELCOME SCREEN ============ -->
<div id="screen-welcome" class="screen active">

  <!-- Sign in pill — floating top-right, gives any visitor a path to their
       workspace (subscriber / Crew / Met) or to dev-mode admin. In v1 this
       is navigation-only (no real auth); in v2 it gates each workspace
       behind email/magic-link auth. Same button position, same modal. -->
  <button type="button" class="signin-pill" id="signin-pill-trigger">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round">
      <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"/>
      <polyline points="10 17 15 12 10 7"/>
      <line x1="15" y1="12" x2="3" y2="12"/>
    </svg>
    <span class="signin-pill-icon-wrap" id="signin-pill-content">
      <span>Sign in</span>
    </span>
  </button>

  <!-- ── Logged-in user dropdown menu ──
       Hidden by default. Shown when the user clicks the pill while logged
       in. Built dynamically — name and roles filled in by JS after the
       session check. -->
  <div class="signin-pill-menu" id="signin-pill-menu" hidden>
    <div class="signin-pill-menu-header">
      <div class="signin-pill-menu-name" id="signin-pill-menu-name">user@example.com</div>
      <div class="signin-pill-menu-roles" id="signin-pill-menu-roles">no roles yet</div>
    </div>
    <!-- Workspace nav slot — JS injects one button per workspace the user has.
         Populated by renderMenu() after a successful session check. -->
    <div class="signin-pill-menu-workspaces" id="signin-pill-menu-workspaces"></div>
    <button type="button" class="signin-pill-menu-item is-signout" id="signin-pill-signout">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
        <polyline points="16 17 21 12 16 7"/>
        <line x1="21" y1="12" x2="9" y2="12"/>
      </svg>
      <span>Sign out</span>
    </button>
  </div>

  <!-- ════════════════════════════════════════════════════════════
       STORM SHELTER v2 — customer-side severe weather surface
       Hidden by default. Activates with one of three tiers:
         data-tier="watch"     — amber, conditions favorable
         data-tier="warning"   — red, storm imminent or occurring
         data-tier="all-clear" — green, risk has passed
       Console hooks for testing:
         _stormShelterToggle('watch')
         _stormShelterToggle('warning')
         _stormShelterToggle('all-clear')
         _stormShelterToggle(false)   // dismiss
       ════════════════════════════════════════════════════════════ -->
  <div class="storm-shelter" id="storm-shelter" data-tier="warning">

    <div class="storm-shelter-stripe"></div>

    <button type="button" class="storm-shelter-head" id="storm-shelter-toggle-btn" aria-expanded="false" aria-controls="storm-shelter-body">
      <span class="storm-shelter-head-icon">
        <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
          <path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
          <line x1="12" y1="9" x2="12" y2="13"/>
          <line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
      </span>
      <span class="storm-shelter-head-text">
        <span class="storm-shelter-tier-label">
          <span class="storm-shelter-tier-pulse"></span>
          <span id="storm-shelter-tier-name">Tornado Warning</span>
        </span>
        <span class="storm-shelter-head-sep" aria-hidden="true"></span>
        <span class="storm-shelter-head-meta" id="storm-shelter-tier-meta">Boone County, IN &middot; until 7:45 PM</span>
      </span>
      <svg class="storm-shelter-toggle-chev" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <polyline points="6 9 12 15 18 9"/>
      </svg>
    </button>

    <div class="storm-shelter-body" id="storm-shelter-body">

      <h2 class="storm-shelter-headline" id="storm-shelter-headline">Take shelter now. We&rsquo;re with you.</h2>
      <p class="storm-shelter-sub" id="storm-shelter-sub">A confirmed rotation is moving toward your area. Get to your safe room. We&rsquo;re tracking this for you in real time.</p>

      <!-- Met-watching attribution row — always visible -->
      <div class="storm-shelter-met-row">
        <div class="storm-shelter-met-avatar">TA</div>
        <div class="storm-shelter-met-info">
          <p class="storm-shelter-met-name" id="storm-shelter-met-name">M. Reynolds, Lead Meteorologist</p>
          <p class="storm-shelter-met-status" id="storm-shelter-met-status">On shift &middot; tracking this storm for Boone County</p>
        </div>
        <span class="storm-shelter-met-presence"></span>
      </div>

      <!-- Met override indicator (shown only when a Met has edited content) -->
      <span class="storm-shelter-override-pill" id="storm-shelter-override-pill">
        <span class="storm-shelter-override-pill-dot"></span>
        <span id="storm-shelter-override-pill-text">Met-edited &middot; auto-refresh paused for 47 min</span>
      </span>

      <!-- Phase tabs -->
      <div class="storm-shelter-phases" role="tablist">
        <button type="button" class="storm-shelter-phase-tab is-active" data-shelter-phase="before" role="tab">
          <span class="storm-shelter-phase-tab-eyebrow">Now</span>
          <span>Before it arrives</span>
        </button>
        <button type="button" class="storm-shelter-phase-tab" data-shelter-phase="during" role="tab">
          <span class="storm-shelter-phase-tab-eyebrow">When it&rsquo;s here</span>
          <span>During the storm</span>
        </button>
        <button type="button" class="storm-shelter-phase-tab" data-shelter-phase="after" role="tab">
          <span class="storm-shelter-phase-tab-eyebrow">After</span>
          <span>Damage check</span>
        </button>
      </div>

      <!-- BEFORE phase -->
      <div class="storm-shelter-phase-content is-active" data-shelter-phase-content="before">
        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">What&rsquo;s coming</p>
          <p class="storm-shelter-section-text" id="storm-shelter-before-coming">A line of severe thunderstorms is tracking E-NE at 32 mph and will reach your area <strong>in approximately 47 minutes</strong>. Primary threats: damaging winds (60-75 mph), isolated tornado risk, large hail. The line clears your area by 8:30 PM.</p>
        </div>

        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">A few things to do now</p>
          <ul class="storm-shelter-checklist" id="storm-shelter-before-checklist">
            <li>
              <svg class="storm-shelter-checklist-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              <span>Bring outdoor furniture, grills, and trash bins inside or secure them.</span>
            </li>
            <li>
              <svg class="storm-shelter-checklist-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              <span>Park vehicles in a garage or away from large trees if possible.</span>
            </li>
            <li>
              <svg class="storm-shelter-checklist-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              <span>Charge phones now in case of power loss. Keep a flashlight close.</span>
            </li>
            <li>
              <svg class="storm-shelter-checklist-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="20 6 9 17 4 12"/>
              </svg>
              <span>Identify your safe room (lowest interior room, no windows). Sit in there if a Warning is issued.</span>
            </li>
          </ul>
        </div>

        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">Crew nearby reporting from the path</p>
          <div class="storm-shelter-crew-strip">
            <span class="storm-shelter-crew-pill"><span class="storm-shelter-crew-avatar">MR</span><span>Maria R. &middot; 0.6 mi NW</span></span>
            <span class="storm-shelter-crew-pill"><span class="storm-shelter-crew-avatar">DT</span><span>Daniel T. &middot; 4.2 mi W</span></span>
            <span class="storm-shelter-crew-pill"><span class="storm-shelter-crew-avatar">SK</span><span>Sarah K. &middot; 5.8 mi SW</span></span>
            <span class="storm-shelter-crew-pill"><span class="storm-shelter-crew-avatar">JM</span><span>Jordan M. &middot; 1.1 mi N</span></span>
          </div>
        </div>
      </div>

      <!-- DURING phase — most important. Live update slot, current
           conditions, what to do right now. -->
      <div class="storm-shelter-phase-content" data-shelter-phase-content="during">

        <!-- Live update slot — eventually radar, livestream, or Met video.
             Today: a Met-written message OR an automated status. -->
        <div class="storm-shelter-live-slot">
          <p class="storm-shelter-live-slot-eyebrow">Live · radar &amp; updates</p>
          <p class="storm-shelter-live-slot-text">Radar loop or Met livestream will appear here when available.</p>
        </div>

        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">If you&rsquo;re sheltering right now</p>
          <p class="storm-shelter-section-text" id="storm-shelter-during-shelter"><strong>Stay where you are.</strong> The line passes through your area in about 18 minutes. Avoid windows. If you hear a freight-train sound or your phone alerts a Tornado Warning, move to your safe room immediately.</p>
        </div>

        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">What&rsquo;s happening right now</p>
          <p class="storm-shelter-section-text" id="storm-shelter-during-now"><strong>Maria R.</strong> reports 1.5&Prime; hail and 60 mph wind gusts at her location 0.6 miles NW of you (timestamped 6 minutes ago). <strong>M. Reynolds</strong> notes the cell weakening as it crosses the river but maintaining strong wind potential. The worst should pass your address by 7:48 PM.</p>
        </div>

        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">If your power goes out</p>
          <ul class="storm-shelter-checklist">
            <li>
              <svg class="storm-shelter-checklist-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/>
              </svg>
              <span>Don&rsquo;t open the fridge unnecessarily &mdash; food stays cold ~4 hours sealed.</span>
            </li>
            <li>
              <svg class="storm-shelter-checklist-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/>
              </svg>
              <span>Avoid candles. Use phone flashlight or battery lantern.</span>
            </li>
            <li>
              <svg class="storm-shelter-checklist-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/>
              </svg>
              <span>Duke Energy outage map: <strong>1,247 customers affected in Boone County right now</strong>.</span>
            </li>
          </ul>
        </div>
      </div>

      <!-- AFTER phase — only meaningful while still in warning state.
           When ALL CLEAR fires, the After phase is replaced by the
           celebration view inside .storm-shelter-flags. -->
      <div class="storm-shelter-phase-content" data-shelter-phase-content="after">
        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">The line has cleared your area</p>
          <p class="storm-shelter-section-text">The worst weather has passed your location. Lingering rain ends in about 40 minutes. <strong>Take a moment, then check around.</strong></p>
        </div>

        <div class="storm-shelter-section">
          <p class="storm-shelter-section-label">How did your address fare?</p>
          <div class="storm-shelter-damage-row">
            <button type="button" class="storm-shelter-damage-btn" data-damage="none">
              <span class="storm-shelter-damage-icon">✓</span>
              <span>No damage</span>
            </button>
            <button type="button" class="storm-shelter-damage-btn" data-damage="minor">
              <span class="storm-shelter-damage-icon">~</span>
              <span>Minor &mdash; debris, branches</span>
            </button>
            <button type="button" class="storm-shelter-damage-btn" data-damage="major">
              <span class="storm-shelter-damage-icon">!</span>
              <span>Major &mdash; structural</span>
            </button>
          </div>
          <p class="storm-shelter-section-text" style="font-size: 12px; color: rgba(15, 17, 22, 0.55); margin-top: 8px;">Your check-in helps our meteorologist team verify the storm&rsquo;s impact and routes restoration resources to neighborhoods that need them.</p>
        </div>
      </div>

      <!-- All-Clear celebration view — replaces all phase content
           when tier is "all-clear". Two gentle suns flanking the
           meteorologist's all-clear message + acknowledge button. -->
      <div class="storm-shelter-flags">
        <span class="storm-shelter-flag storm-shelter-flag-left">&#9728;&#65039;</span>
        <div class="storm-shelter-flags-text" id="storm-shelter-all-clear-msg">You&rsquo;re clear. Severe risk is over for the day.</div>
        <span class="storm-shelter-flag storm-shelter-flag-right">&#9728;&#65039;</span>
      </div>
      <div class="storm-shelter-ack-wrap" id="storm-shelter-ack-wrap" style="display: none;">
        <button type="button" class="storm-shelter-ack-btn" id="storm-shelter-ack-btn">
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
          <span>Got it &mdash; back to normal</span>
        </button>
      </div>

    </div>
  </div>


  <div class="header">
    <a href="/" class="brand" aria-label="WeatherValet home">
      <svg class="brand-mark" viewBox="33 6 34 46">
        <path fill="#0E1116" d="M65.48,24.52h-12.51l2.78-16.68L34.9,32.86h12.51l-2.78,16.68L65.48,24.52Z"/>
      </svg>
      <span class="brand-name">WeatherValet<span class="brand-tld">.ai</span></span>
    </a>
  </div>

  <div class="hero">
    <div class="eyebrow">
      <span class="eyebrow-dot"></span>
      Welcome to WeatherValet
    </div>
    <h1 class="headline">Precise weather for your <em>exact <span class="rotating-word" id="rotating-word">plans</span>.</em></h1>
    <p class="subhead">Hyperlocal. Timely. Built for what you’re actually doing.</p>
  </div>

  <div class="search-wrap" id="search-wrap">
    <form id="search-form" class="search-form">
      <div class="search-bar">
        <svg class="search-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"/>
          <path d="m21 21-4.3-4.3"/>
        </svg>
        <input id="search-input" class="search-input" type="text" placeholder="Type your location, date, and specific plan" autocomplete="off" autocapitalize="sentences" spellcheck="false" enterkeyhint="send">
        <button type="submit" class="search-submit">
          Get Forecast
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M7 7h10v10"/>
            <path d="M7 17 17 7"/>
          </svg>
        </button>
      </div>
    </form>

    <!-- Dropdown — appears on input focus, hidden by default.

         Generic mixed-use plans intentional: WeatherValet is the weather
         concierge for any plan, anywhere, any time of year. Don't theme
         these to current campaigns — the product's brand needs to read
         the same whether you're a Florida fisherman in July or an Indy
         tailgater in May. -->
    <div id="search-dropdown" class="search-dropdown" role="listbox" aria-label="Example plans">
      <div class="search-dropdown-head">Try one of these</div>
      <button type="button" class="chip" data-chip="Bike ride at 5 PM Friday in St. Louis, MO">
        <svg class="chip-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
        <span>Bike ride at 5 PM Friday in St. Louis, MO</span>
      </button>
      <button type="button" class="chip" data-chip="Roofing job tomorrow in Chicago">
        <svg class="chip-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
        <span>Roofing job tomorrow in Chicago</span>
      </button>
      <button type="button" class="chip" data-chip="Outdoor wedding at 4 PM in Charlotte, NC">
        <svg class="chip-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
        <span>Outdoor wedding at 4 PM in Charlotte, NC</span>
      </button>
      <!-- Indy 500 campaign variants (used briefly May 10, then rolled back
           because they narrowed WeatherValet's brand to a single event.
           Kept here for reference if a future campaign wants to A/B test
           campaign-themed chips, but the default should always be the
           generic mixed-use plans above.):
        Indy 500 tailgate Saturday morning in Indianapolis
        Watching the Indy 500 on Sunday in Indianapolis, IN
        Carb Day at the speedway in Indianapolis, IN this Friday
      -->
    </div>

    <div class="search-hint">Press Enter to submit · Cancel anytime</div>
  </div>

  <!-- Secondary entry point to the Crew sub-app. Quiet by design — sits
       below the search hint so it's discoverable to anyone scanning the
       area but doesn't compete with the primary search action above. -->
  <div class="crew-quick-link-wrap">
    <button type="button" class="crew-quick-link" data-show-screen="screen-crew-feed">
      <span class="crew-quick-link-dot"></span>
      <span>See live Valet Crew reports</span>
      <svg class="crew-quick-link-arrow" width="14" height="14" viewBox="0 0 24 24"
           fill="none" stroke="currentColor" stroke-width="2.4"
           stroke-linecap="round" stroke-linejoin="round">
        <path d="M9 18l6-6-6-6"/>
      </svg>
    </button>
  </div>

  <div class="steps">
    <div>
      <div class="step-head"><span class="step-num">01</span><span class="step-rule"></span></div>
      <h3 class="step-title">Tell us your plan</h3>
      <p class="step-body">Just tell us what you’re planning to do and when.</p>
    </div>
    <div>
      <div class="step-head"><span class="step-num">02</span><span class="step-rule"></span></div>
      <h3 class="step-title">We handle the weather</h3>
      <p class="step-body">We take care of the forecast for you, blending the best models with real-time reports from the Valet Crew in your area.</p>
    </div>
    <div>
      <div class="step-head"><span class="step-num">03</span><span class="step-rule"></span></div>
      <h3 class="step-title">You get the clarity you need</h3>
      <p class="step-body">We’ll give you a clear, hyperlocal brief so you can move forward with confidence.</p>
    </div>
  </div>

  <!-- ════════════════════════════════════════════════════════════
       TWO PATHS — front-door clarity
       Two cards explaining the Subscriber and Valet Crew populations.
       Pricing is intentionally vague ("starting at" placeholder) until
       Michael does a market study; the card structure stays the same
       once we lock in numbers.
       ════════════════════════════════════════════════════════════ -->
  <div class="two-paths">
    <div class="two-paths-eyebrow">
      <span class="two-paths-eyebrow-dot"></span>
      Two ways to get involved
    </div>
    <h2 class="two-paths-headline">Become a subscriber. Or join the Valet Crew.</h2>
    <p class="two-paths-sub">
      Free to search anytime &mdash; type your plan and we&rsquo;ll give you a forecast.
      For more, here are the two paths.
    </p>

    <div class="two-paths-grid">

      <!-- Subscriber path — routes to the dedicated pricing page where
           prospects can self-qualify across all five tiers (Free / AI /
           Pro Single / Pro Multi / Pro Enterprise). -->
      <button type="button" class="two-paths-card path-subscriber" data-show-screen="screen-pricing" id="path-card-subscriber">
        <div class="two-paths-card-icon">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2 4 7v6c0 5 3.5 9 8 10 4.5-1 8-5 8-10V7l-8-5z"/>
            <path d="M9 12l2 2 4-4"/>
          </svg>
        </div>
        <div class="two-paths-card-eyebrow">For your plans</div>
        <h3 class="two-paths-card-title">Subscribe to WeatherValet</h3>
        <p class="two-paths-card-body">
          For people whose plans depend on the weather. Individuals get saved
          locations and alerts. Businesses get a meteorologist on watch for
          their operation.
        </p>
        <ul class="two-paths-card-bullets">
          <li><span class="two-paths-card-bullet-dot"></span><span>Saved locations and severe-weather alerts</span></li>
          <li><span class="two-paths-card-bullet-dot"></span><span>Daily Brief, AI or Met-touched (Pro tier)</span></li>
          <li><span class="two-paths-card-bullet-dot"></span><span>Threshold alerts &mdash; wind, temp, precip</span></li>
          <li><span class="two-paths-card-bullet-dot"></span><span>Pro tier: direct messaging with our meteorologist team</span></li>
        </ul>
        <span class="two-paths-card-cta">
          <span>See pricing</span>
          <svg class="two-paths-card-cta-arrow" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </span>
        <p class="two-paths-card-pricing">Individuals from $30/mo &middot; Businesses from $400/mo</p>
      </button>

      <!-- Crew path -->
      <button type="button" class="two-paths-card path-crew" data-show-screen="screen-crew-feed" id="path-card-crew">
        <div class="two-paths-card-icon">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
            <circle cx="9" cy="7" r="4"/>
            <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
            <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
          </svg>
        </div>
        <div class="two-paths-card-eyebrow">For weather watchers</div>
        <h3 class="two-paths-card-title">Join the Valet Crew</h3>
        <p class="two-paths-card-body">
          For people who love being outside in weather. Report ground-truth observations,
          calibrate sensors, get cited by meteorologists. Real attribution, paid for serious work.
        </p>
        <ul class="two-paths-card-bullets">
          <li><span class="two-paths-card-bullet-dot"></span><span>Get cited by name when meteorologists use your reports</span></li>
          <li><span class="two-paths-card-bullet-dot"></span><span>Earn for verified mission contributions</span></li>
          <li><span class="two-paths-card-bullet-dot"></span><span>Train into specialties &mdash; storm spotting, mesonet calibration</span></li>
          <li><span class="two-paths-card-bullet-dot"></span><span>Be part of the network that makes the forecasts better</span></li>
        </ul>
        <span class="two-paths-card-cta">
          <span>Join the Valet Crew</span>
          <svg class="two-paths-card-cta-arrow" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </span>
        <p class="two-paths-card-pricing">Free to join &middot; paid for verified work</p>
      </button>

    </div>
  </div>

  <div class="lockscreen-block">
    <div class="lockscreen-eyebrow">
      <span class="lockscreen-eyebrow-dot"></span>
      <span class="lockscreen-eyebrow-label">What $19 buys you</span>
    </div>
    <h2 class="lockscreen-title">For $19, a certified meteorologist reviews your <em>exact plan.</em></h2>
    <p class="lockscreen-prose">A certified meteorologist reads your plan, pulls the latest models, radar, and Valet Crew reports, and texts you back &mdash; usually within 30 minutes &mdash; with a handcrafted, hyperlocal forecast for your exact window. You make the call. $19, one-time. No subscription.</p>

    <!-- Sample phone for the welcome page -->
    <div class="phone-frame">
      <div class="phone-screen">
        <div class="phone-notch"></div>
        <div class="phone-status-bar">
          <span class="phone-time-bar" id="welcome-phone-time">10:23</span>
          <div class="phone-status-icons">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 20h.01"/><path d="M7 20v-4"/><path d="M12 20v-8"/><path d="M17 20V8"/><path d="M22 4v16"/></svg>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 13a10 10 0 0 1 14 0"/><path d="M8.5 16.5a5 5 0 0 1 7 0"/><path d="M2 8.82a15 15 0 0 1 20 0"/><line x1="12" y1="20" x2="12.01" y2="20"/></svg>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="16" height="10" x="2" y="7" rx="2" ry="2"/><line x1="22" y1="11" x2="22" y2="13"/></svg>
          </div>
        </div>
        <div class="phone-glow"></div>
        <div class="phone-body">
          <div class="phone-clock" id="welcome-phone-clock">10:23</div>
          <div class="phone-date" id="welcome-phone-date">Tuesday, April 29</div>
          <div class="phone-lock-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="11" x="3" y="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          </div>
          <div class="notif-card">
            <div class="notif-icon">
              <svg viewBox="0 0 70 100"><path fill="#fff" d="M65.48,24.52h-12.51l2.78-16.68L34.9,32.86h12.51l-2.78,16.68L65.48,24.52Z" transform="scale(2)"/></svg>
            </div>
            <div class="notif-body">
              <div class="notif-head">
                <span class="notif-app">Valet Update</span>
                <span class="notif-now">now</span>
              </div>
              <div class="notif-text">Your 5 PM bike ride in St. Louis is <span class="notif-text-em-caution">Caution</span> &mdash; gust front arrives at 4:45 with 30+ mph winds. The window before is clean.</div>
              <div class="notif-sig">Amanda L. &middot; Meteorologist</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="crew-footer">
    <div class="crew-eyebrow">
      <span class="crew-dot"></span>
      <span class="crew-label">Powered by the Valet Crew</span>
    </div>
    <p class="crew-line">Real-time weather reports from people on the ground &mdash; not just models.</p>
    <button class="join-crew" type="button">
      Open the Valet Crew
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <path d="M9 18l6-6-6-6"/>
      </svg>
    </button>
  </div>

  <!-- Site footer is now rendered globally after the last screen.
       See the footer block right before the dev-menu, around line 23057. -->

</div>

<!-- ============ HANDOFF SCREEN ============ -->
<div id="screen-handoff" class="screen">
  <div class="handoff">
    <div class="handoff-key">
      <!-- Cloud-with-radar icon — "checking" rather than "keys received" -->
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
        <path d="M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"/>
        <line x1="12" y1="13" x2="12" y2="13.01"/>
      </svg>
    </div>
    <h1 class="handoff-headline">Checking&hellip;</h1>
    <p class="handoff-sub">Pulling the latest forecast for you.</p>
    <div class="handoff-routing">
      <span class="handoff-routing-dot"></span>
      Routing your Valet…
    </div>
  </div>
</div>

<!-- ============ PRICING SCREEN ============================================
     Public-facing pricing page. Reached from the Subscriber path on the
     welcome screen. Lays out all five tiers (Free / AI / Pro Single /
     Pro Multi / Pro Enterprise) so prospects can self-qualify before
     starting signup. Uses "starting at" language for the Pro tiers
     since the exact numbers are pre-market-study placeholders. -->
<div id="screen-pricing" class="screen pricing-page">
  <button type="button" class="pricing-back" data-show-screen="screen-welcome">
    <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
      <polyline points="15 18 9 12 15 6"/>
    </svg>
    <span>Back to home</span>
  </button>

  <div class="pricing-hero">
    <div class="pricing-hero-eyebrow">
      <span class="pricing-hero-eyebrow-dot"></span>
      <span class="pricing-hero-eyebrow-label">Pricing</span>
    </div>
    <h1 class="pricing-hero-headline">Pick the plan that matches your stakes.</h1>
    <p class="pricing-hero-sub">
      Free to search anytime. Subscribe for saved locations and alerts.
      Pro tier when your business depends on getting it right.
    </p>
  </div>

  <!-- ── Hobbyist tier row (Free + Hobbyist) ── -->
  <div class="pricing-section">
    <div class="pricing-section-label">
      <span class="pricing-section-label-text">For individuals</span>
      <span class="pricing-section-label-line"></span>
    </div>
    <div class="pricing-grid pricing-grid-2">
      <!-- Free tier -->
      <div class="pricing-card">
        <h3 class="pricing-card-name">Free</h3>
        <p class="pricing-card-tagline">Search any plan, anytime. No signup required.</p>
        <div class="pricing-card-price">
          <span class="pricing-card-price-amount">$0</span>
        </div>
        <p class="pricing-card-price-annual">Always free</p>
        <ul class="pricing-card-features">
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Unlimited plan-specific searches</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>AI-generated forecast tickets</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Storm Shelter visibility during severe weather</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Live Crew reports (read-only)</span></li>
        </ul>
        <button type="button" class="pricing-card-cta is-secondary" data-show-screen="screen-welcome">
          Start searching
        </button>
      </div>

      <!-- Hobbyist tier ($30/mo) -->
      <div class="pricing-card">
        <h3 class="pricing-card-name">Hobbyist</h3>
        <p class="pricing-card-tagline">For people whose plans depend on the weather — but not their livelihood.</p>
        <div class="pricing-card-price">
          <span class="pricing-card-price-prefix">From</span>
          <span class="pricing-card-price-amount">$30</span>
          <span class="pricing-card-price-unit">/month</span>
        </div>
        <p class="pricing-card-price-annual">Or $300/year &mdash; normally $360, save $60 (17%)</p>
        <ul class="pricing-card-features">
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Everything in Free</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Saved location with severe-weather alerts</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>AI Daily Brief for your area</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Threshold alerts (wind, temp, precip)</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Storm Shelter mode</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>$19 Met review available anytime</span></li>
        </ul>
        <button type="button" class="pricing-card-cta" data-subscribe-tier="hobbyist">
          Subscribe
        </button>
      </div>
    </div>
  </div>

  <!-- ── Business / Pro tier row ── -->
  <div class="pricing-section">
    <div class="pricing-section-label">
      <span class="pricing-section-label-text">For businesses</span>
      <span class="pricing-section-label-line"></span>
    </div>
    <!-- Starter Month callout — sales funnel entry point.
         Visually distinct from the tier cards (it's not a tier itself,
         just a discounted entry into Pro Single). -->
    <div style="background: linear-gradient(135deg, rgba(46,79,184,0.06) 0%, rgba(46,79,184,0.12) 100%); border: 1px solid rgba(46,79,184,0.20); border-radius: 12px; padding: 18px 24px; margin: 0 0 24px; display: flex; align-items: center; justify-content: space-between; gap: 20px; flex-wrap: wrap;">
      <div style="flex: 1; min-width: 260px;">
        <div style="font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: #2E4FB8; margin-bottom: 4px;">New to WeatherValet?</div>
        <div style="font-size: 15px; color: #0E1116; line-height: 1.5;"><strong>Try Pro Single for $99 your first month.</strong> Full service for 30 days, then $400/month. Cancel anytime.</div>
      </div>
      <a href="/starter" style="background: #2E4FB8; color: #fff; text-decoration: none; padding: 11px 22px; border-radius: 8px; font-size: 14px; font-weight: 600; white-space: nowrap;">Start my $99 month &rarr;</a>
    </div>
    <div class="pricing-grid pricing-grid-3">
      <!-- Pro Single -->
      <div class="pricing-card is-featured">
        <h3 class="pricing-card-name">Pro Single</h3>
        <p class="pricing-card-tagline">For one business location, one team. Most contractors and small operations start here.</p>
        <div class="pricing-card-price">
          <span class="pricing-card-price-prefix">From</span>
          <span class="pricing-card-price-amount">$400</span>
          <span class="pricing-card-price-unit">/month</span>
        </div>
        <p class="pricing-card-price-annual">Or $4,000/year &mdash; normally $4,800, save $800 (17%)</p>
        <ul class="pricing-card-features">
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>One business location, up to 3 users</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span><strong>Met-touched daily brief</strong>, signed by the meteorologist who wrote it</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span><strong>Severe alerts confirmed by a meteorologist</strong> before sending</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span><strong>Pro Threads</strong> &mdash; direct messaging with our meteorologist team</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>30-min response SLA business hours</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Monthly forecast accuracy scorecard</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span><strong>The WeatherValet Insider</strong> &mdash; weekly newsletter from our meteorologists</span></li>
        </ul>
        <button type="button" class="pricing-card-cta" data-subscribe-tier="pro_single">
          Subscribe &mdash; $400/month
        </button>
        <a class="pricing-card-cta-secondary" href="mailto:hello@weathervalet.ai?subject=Pro%20Single%20question">Questions before you subscribe? Email us &rarr;</a>
      </div>

      <!-- Pro Multi -->
      <div class="pricing-card">
        <h3 class="pricing-card-name">Pro Multi</h3>
        <p class="pricing-card-tagline">For multi-crew, multi-location operations. Regional contractors, school districts, event firms.</p>
        <div class="pricing-card-price">
          <span class="pricing-card-price-prefix">From</span>
          <span class="pricing-card-price-amount">$1,200</span>
          <span class="pricing-card-price-unit">/month</span>
        </div>
        <p class="pricing-card-price-annual">Or $12,000/year &mdash; normally $14,400, save $2,400 (17%)</p>
        <ul class="pricing-card-features">
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Up to 5 locations, 10 users</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Everything in Pro Single, plus:</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Multi-location forecasts in a single brief</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Custom alert thresholds for wind, rain, temperature, and more</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Monthly forecast accuracy scorecard</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Multi-user role permissions</span></li>
        </ul>
        <button type="button" class="pricing-card-cta" data-subscribe-tier="pro_multi">
          Subscribe &mdash; $1,200/month
        </button>
        <a class="pricing-card-cta-secondary" href="mailto:hello@weathervalet.ai?subject=Pro%20Multi%20question">Need help scoping? Email us &rarr;</a>
      </div>

      <!-- Pro Enterprise -->
      <div class="pricing-card">
        <h3 class="pricing-card-name">Pro Enterprise</h3>
        <p class="pricing-card-tagline">For operations where weather is a critical operational factor. Custom-scoped per business.</p>
        <div class="pricing-card-price">
          <span class="pricing-card-price-prefix">From</span>
          <span class="pricing-card-price-amount">$2,000</span>
          <span class="pricing-card-price-unit">/month</span>
        </div>
        <p class="pricing-card-price-annual">Custom-quoted</p>
        <ul class="pricing-card-features">
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Unlimited locations and users</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Everything in Pro Multi, plus:</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span><strong>Dedicated primary Met</strong> for your business</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Custom alert integrations</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Priority severe weather coverage</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Direct phone access during high-stakes events</span></li>
          <li><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span>Direct line to leadership for operational planning</span></li>
        </ul>
        <a class="pricing-card-cta" style="text-decoration:none;display:inline-flex;align-items:center;justify-content:center;" href="mailto:hello@weathervalet.ai?subject=Pro%20Enterprise%20inquiry">
          Talk to us
        </a>
      </div>
    </div>
  </div>

  <!-- ── Footer / FAQ ── -->
  <div class="pricing-footer">
    <h2 class="pricing-footer-headline">Not sure which plan fits?</h2>
    <p class="pricing-footer-sub">
      Most businesses start with a 15-minute call. We talk about your operation,
      your weather pain, and which tier is the right fit. No pressure sale.
      If we&rsquo;re not a fit, we&rsquo;ll tell you.
    </p>
    <button type="button" class="pricing-footer-cta" data-email-capture="subscriber">
      <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.37 1.9.72 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.9.35 1.85.59 2.81.72A2 2 0 0 1 22 16.92z"/>
      </svg>
      <span>Schedule a 15-minute call</span>
    </button>
  </div>
</div>

<!-- ============ TICKET SCREEN — content built dynamically by JS ============ -->
<div id="screen-ticket" class="screen">
  <button id="back-button" class="back-btn" type="button">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
      <path d="m12 19-7-7 7-7"/>
      <path d="M19 12H5"/>
    </svg>
    Back to Search
  </button>
  <div id="ticket-content"></div>
</div>

</div>
</div>

<!-- ============ VALET CREW · PROFILE / MEMBER CARD ============ -->
<!-- Landing screen for the Crew sub-app. Shows rank, badges, protected
     hours, clarity streak — and entry points to Live Feed + Quick Report. -->
<div id="screen-crew-profile" class="screen">
  <div class="crew-screen">

    <nav class="crew-nav">
      <button type="button" class="crew-back" data-back-to="screen-welcome">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M15 18l-6-6 6-6"/>
        </svg>
        Back
      </button>
      <span class="crew-nav-title">Valet Crew</span>
      <span style="min-width:44px"></span>
    </nav>

    <!-- Signed-out state: shown to anyone visiting Profile without auth.
         Calm, inviting — explains what they get when they sign up, and
         lets them dip into Live Feed first to see what the Crew is about
         before committing. The signed-in DOM tree below this is hidden
         (display:none) until auth flips. -->
    <div id="profile-signed-out" class="profile-signed-out">
      <div class="profile-signed-out-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"
             stroke-linecap="round" stroke-linejoin="round">
          <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
          <circle cx="9" cy="7" r="4"/>
          <path d="M22 11h-6"/><path d="M19 8v6"/>
        </svg>
      </div>
      <h1 class="profile-signed-out-title">Join the Valet Crew</h1>
      <p class="profile-signed-out-body">
        Your member card lives here once you&rsquo;re part of the Crew. You&rsquo;ll earn tokens for
        reporting weather, get badges for streaks, and rise through the ranks as you contribute.
      </p>
      <div class="profile-signed-out-actions">
        <button type="button" class="profile-signed-out-cta" data-auth-trigger="profile">
          Sign in or create account
        </button>
        <button type="button" class="profile-signed-out-alt" data-show-screen="screen-crew-feed">
          Browse the Live Feed first &rarr;
        </button>
      </div>
    </div>

    <!-- Signed-in content — hidden until auth flips signedIn=true. -->
    <div id="profile-signed-in" class="profile-signed-in">

    <!-- Member card — the prestige object -->
    <div class="member-card">
      <!-- Tokens balance chip — top-right corner of the card. Updates on
           report submit (incremented in JS). The data attribute holds the
           numeric balance so the increment animation can target it. -->
      <div class="tokens-chip" id="tokens-chip">
        <svg class="tokens-chip-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="9"/>
          <path d="M8 12h8M12 8v8"/>
        </svg>
        <span class="tokens-chip-value" id="tokens-chip-value" data-tokens="1240">1,240</span>
        <span class="tokens-chip-label">tokens</span>
      </div>
      <div class="member-eyebrow">
        <span class="member-eyebrow-dot"></span>
        <span id="member-eyebrow-text">Valet Crew &middot; Member</span>
      </div>
      <h1 class="member-name">Michael Reynolds</h1>
      <p class="member-handle">@mreynolds · Boone County, IN</p>
      <div class="member-rank-row">
        <div class="member-rank-icon">II</div>
        <div class="member-rank-text">
          <div class="member-rank-label">Current rank</div>
          <div class="member-rank-tier">Senior Valet</div>
          <div class="member-rank-progress">42 protected hours to Lead Valet</div>
        </div>
      </div>

      <!-- ── Cast-page strip (Chunk 5a) ──────────────────────
           Season, member-since, days active, specialty. Treats the
           card like a sports trading card or a Survivor castaway
           bio: identity-anchoring facts that give the page weight
           without changing the existing card structure. -->
      <div class="member-bio-strip">
        <div class="member-bio-stat">
          <span class="member-bio-stat-label">Season</span>
          <span class="member-bio-stat-value" id="member-bio-season">1</span>
        </div>
        <div class="member-bio-stat">
          <span class="member-bio-stat-label">Member since</span>
          <span class="member-bio-stat-value" id="member-bio-since">May 2026</span>
        </div>
        <div class="member-bio-stat">
          <span class="member-bio-stat-label">Days active</span>
          <span class="member-bio-stat-value" id="member-bio-days">&mdash;</span>
        </div>
        <div class="member-bio-stat member-bio-specialty">
          <span class="member-bio-stat-label">Specialty</span>
          <span class="member-bio-stat-value" id="member-bio-specialty">Hail &amp; severe wind</span>
        </div>
      </div>
    </div>

    <!-- Stats — protected hours + clarity streak, now with progress bars
         showing path to next milestone. Restrained: 4px tall, no labels
         on the bar itself, just a tiny mono-caps milestone string below. -->
    <div class="crew-stats">
      <div class="crew-stat">
        <div class="crew-stat-label">Protected Hours</div>
        <div class="crew-stat-value">128<span class="crew-stat-unit">hrs</span></div>
        <div class="crew-stat-sub">Across <strong>47 reports</strong> this season.</div>
        <div class="crew-stat-progress">
          <div class="crew-stat-progress-fill" style="width:75%"></div>
        </div>
        <div class="crew-stat-milestone">22 hrs to Lead Valet</div>
      </div>
      <div class="crew-stat streak">
        <div class="crew-stat-label">Clarity Streak</div>
        <div class="crew-stat-value">
          <span>9</span>
          <span class="crew-stat-flame" aria-hidden="true">🔥</span>
          <span class="crew-stat-unit">in a row</span>
        </div>
        <div class="crew-stat-sub">Reports that matched the meteorologist's call.</div>
        <div class="crew-stat-progress">
          <div class="crew-stat-progress-fill" style="width:60%"></div>
        </div>
        <div class="crew-stat-milestone">1 more for Clarity 10 badge</div>
      </div>
    </div>

    <!-- ── Receipt row (Chunk 5b) ──────────────────────────
         Three compact tiles below the journey stats. Reads
         like a baseball-card stat line: factual, scannable,
         no progress bars (those would compete with the big
         tiles above). The receipts are what others gave back
         to you — total reports submitted, verifications your
         reports earned from other Crew, and Met citations.
         Populated by applySignedInProfile(). -->
    <div class="crew-receipts-row" id="crew-receipts-row">
      <div class="crew-receipt-tile">
        <span class="crew-receipt-value" id="receipt-reports">&mdash;</span>
        <span class="crew-receipt-label">Reports</span>
      </div>
      <div class="crew-receipt-tile">
        <span class="crew-receipt-value" id="receipt-verifies">&mdash;</span>
        <span class="crew-receipt-label">Verifies received</span>
      </div>
      <div class="crew-receipt-tile">
        <span class="crew-receipt-value" id="receipt-cited">&mdash;</span>
        <span class="crew-receipt-label">Cited by Mets</span>
      </div>
    </div>

    <!-- ── Season Highlights (Chunk 5c) ──────────────────────
         Curated moments derived from the Crew member's actual
         work. Each card has a generated headline, a date, the
         relevant context, and (when applicable) the report text
         that earned the moment.

         Quality gates: cards only render when the underlying
         numbers are strong enough to justify the framing. A
         "most-verified report" needs >= 3 verifications. A
         "first citation" needs a citation receipt with a Met
         name. A "photo highlight" needs a photo report with
         >= 2 verifications. Cards that don't pass don't render.

         If zero cards qualify, a friendly fallback card appears:
         "Your first highlight is one report away." -->
    <section class="season-highlights" id="season-highlights" hidden>
      <div class="crew-section-label">
        Season highlights
        <span class="season-highlights-sub">Moments worth remembering</span>
      </div>
      <div class="season-highlights-grid" id="season-highlights-grid">
        <!-- Cards rendered by renderSeasonHighlights() -->
      </div>
    </section>

    <!-- ── Your recent reports (Chunk 5d) ────────────────────
         A vertically scrolling list of this user's own most
         recent reports (last 10). Mirror of the Live Feed
         side list, but filtered to just their work. Lets a
         Crew member scroll back and see what they've
         contributed. Hidden when they have zero reports;
         the Season Highlights section above handles the
         "you have nothing yet" framing. -->
    <section class="profile-recent" id="profile-recent" hidden>
      <div class="crew-section-label">
        Your recent reports
        <span class="profile-recent-sub" id="profile-recent-sub"></span>
      </div>
      <ul class="profile-recent-list" id="profile-recent-list">
        <!-- Populated by renderProfileRecent() -->
      </ul>
    </section>

    <!-- ── Your field notebook (Chunk 5d, moved from Workspace) ──
         The Crew member's private log of confessional notes.
         Moved from the Workspace to the Profile because the
         notebook is reflective writing — it belongs on the
         identity page, not the active-work page. Workspace
         keeps its mission-focused energy. The save flow on
         mission complete still writes to the same source of
         truth; only the display surface changed. -->
    <section class="profile-notebook" id="profile-notebook" hidden>
      <div class="crew-section-label">
        Your field notebook
        <span class="profile-notebook-sub">Private &middot; only you see this list</span>
      </div>
      <div class="profile-notebook-list" id="profile-notebook-list">
        <!-- Populated by renderProfileNotebook() -->
      </div>
    </section>

    <!-- Badges — populated dynamically based on the current user's
         earned-badge IDs. Default is all locked for a brand-new user;
         badges unlock as the user hits their criteria (verifying, streaks,
         report counts, etc). Wrapper section is hidden by renderBadges()
         when there's no signed-in user state, so the orphaned "Badges
         earned" label doesn't appear above empty space. -->
    <section class="crew-badges-section" id="crew-badges-section">
      <div class="crew-section-label">Badges earned</div>
      <div class="badge-grid" id="badge-grid"></div>
    </section>

    <!-- Actions — Live Feed + Quick Report entry points -->
    <div class="crew-actions">
      <button type="button" class="crew-action crew-action-primary"
              data-show-screen="screen-crew-feed">
        <span class="crew-action-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <polygon points="1 6 1 22 8 18 16 22 23 18 23 2 16 6 8 2 1 6"/>
            <line x1="8" y1="2" x2="8" y2="18"/><line x1="16" y1="6" x2="16" y2="22"/>
          </svg>
        </span>
        <span class="crew-action-text">
          <span class="crew-action-title">Live Feed</span>
          <span class="crew-action-sub">See what the Crew is reporting near you</span>
        </span>
        <svg class="crew-action-arrow" width="16" height="16" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2.2"
             stroke-linecap="round" stroke-linejoin="round">
          <path d="M9 18l6-6-6-6"/>
        </svg>
      </button>
      <!-- The "Full map" button was removed in Phase 9 — the Live Feed
           above is now the full map. Kept the Live Feed entry as the
           single map destination. -->
      <button type="button" class="crew-action" data-open-report="1">
        <span class="crew-action-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="12" y1="5" x2="12" y2="19"/>
            <line x1="5" y1="12" x2="19" y2="12"/>
          </svg>
        </span>
        <span class="crew-action-text">
          <span class="crew-action-title">Submit a Quick Report</span>
          <span class="crew-action-sub">Hail · Wind · Rain · Photo · 2-tap submit</span>
        </span>
        <svg class="crew-action-arrow" width="16" height="16" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2.2"
             stroke-linecap="round" stroke-linejoin="round">
          <path d="M9 18l6-6-6-6"/>
        </svg>
      </button>
    </div>

    <!-- ── Home base — Crew location for mission targeting (Item #6) ──
         Surface the Crew member's saved home location, with an "Edit"
         button that opens a pin-drop map. This is what determines
         which polygon-targeted missions reach this Crew member.
         The card is hidden until backend confirms this user has the
         crew role (otherwise it's just empty noise on the page). -->
    <div class="crew-homebase-card" id="crew-homebase-card" hidden>
      <div class="crew-homebase-head">
        <div class="crew-homebase-label">Mission home base</div>
        <button type="button" class="crew-homebase-edit" id="crew-homebase-edit">Edit</button>
      </div>
      <div class="crew-homebase-body" id="crew-homebase-body">
        <div class="crew-homebase-placeholder" id="crew-homebase-placeholder" hidden>
          <p class="crew-homebase-placeholder-title">No home base set yet</p>
          <p class="crew-homebase-placeholder-sub">
            Mets target missions to a geographic area. Setting your home base lets us
            send you the right pages.
          </p>
        </div>
        <div class="crew-homebase-data" id="crew-homebase-data" hidden>
          <p class="crew-homebase-label-text" id="crew-homebase-label-text">&mdash;</p>
          <p class="crew-homebase-coords" id="crew-homebase-coords">&mdash;</p>
        </div>
      </div>
      <div class="crew-homebase-toggle">
        <label class="crew-homebase-toggle-row">
          <input type="checkbox" id="crew-homebase-active" checked>
          <span class="crew-homebase-toggle-text">
            <strong>Available for missions</strong>
            <span class="crew-homebase-toggle-sub">Uncheck if you&rsquo;re out of town and want to hold your pages.</span>
          </span>
        </label>
      </div>
    </div>

    <!-- Sign Out — small text link below the actions. Only visible when
         signed in. Resets verifiedByMe and tipsByMe so re-signing-in
         starts clean. Useful for testing the locked-state flow. -->
    <div class="signout-row">
      <button type="button" class="signout-link" data-auth-action="signout">
        Sign out
      </button>
    </div>

    </div>  <!-- /#profile-signed-in -->

  </div>
</div>


<!-- ============ SUBSCRIBER PORTAL =====================================
     Signed-in surface for paying subscribers (Hobbyist or Pro tier).
     Shows their saved location, preferences, threshold alerts, brief
     history, plan management. Pre-Stripe, this is mockup-only — real
     subscribers don't land here until June. For the May 24 launch,
     the portal exists for Met preview but isn't reachable from
     public navigation. -->
<div id="screen-subscriber-portal" class="screen subscriber-portal">
  <div class="portal-screen">

    <nav class="portal-nav">
      <button type="button" class="portal-back" data-back-to="screen-welcome">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M15 18l-6-6 6-6"/>
        </svg>
        Home
      </button>
      <span class="portal-nav-title">Your account</span>
      <button type="button" class="portal-signout" data-auth-action="signout">Sign out</button>
    </nav>

    <!-- Starter Month banner (hidden by default, shown by JS for active starter users) -->
    <div id="portal-starter-banner" hidden style="background: linear-gradient(135deg, rgba(46,79,184,0.10) 0%, rgba(46,79,184,0.18) 100%); border: 1px solid rgba(46,79,184,0.30); border-radius: 12px; padding: 16px 22px; margin: 0 0 20px; display: flex; align-items: center; gap: 14px;">
      <div style="font-size: 22px;">🎉</div>
      <div style="flex: 1; min-width: 0;">
        <div style="font-size: 14.5px; font-weight: 600; color: #0E1116; line-height: 1.35;">You&rsquo;re on the WeatherValet Starter Month.</div>
        <div style="font-size: 12.5px; color: rgba(15,17,22,0.65); margin-top: 3px; line-height: 1.45;">Renews at $400/month on <span id="portal-starter-renews-date">your renewal date</span>. Cancel anytime &mdash; no questions.</div>
      </div>
    </div>

    <!-- Identity card — plan, location, renewal -->
    <div class="portal-identity-card">
      <div class="portal-identity-eyebrow">
        <span class="portal-identity-dot"></span>
        <span>WeatherValet &middot; <span id="portal-plan-name">Hobbyist</span></span>
      </div>
      <h1 class="portal-identity-name" id="portal-user-name">Welcome back</h1>
      <p class="portal-identity-loc"><span id="portal-user-location">Lebanon, IN</span></p>
      <div class="portal-identity-meta">
        <div class="portal-identity-meta-item">
          <div class="portal-identity-meta-label">Plan</div>
          <div class="portal-identity-meta-value" id="portal-plan-display">WeatherValet &mdash; $30/mo</div>
        </div>
        <div class="portal-identity-meta-item">
          <div class="portal-identity-meta-label">Renews</div>
          <div class="portal-identity-meta-value" id="portal-renews">June 10, 2026</div>
        </div>
        <div class="portal-identity-meta-item">
          <div class="portal-identity-meta-label">Member since</div>
          <div class="portal-identity-meta-value" id="portal-since">May 10, 2026</div>
        </div>
      </div>
    </div>

    <!-- Saved location -->
    <section class="portal-section">
      <div class="portal-section-head">
        <h2 class="portal-section-title">Saved location</h2>
        <button type="button" class="portal-section-edit">Edit</button>
      </div>
      <div class="portal-location-card">
        <svg class="portal-location-icon" viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/>
        </svg>
        <div class="portal-location-text">
          <div class="portal-location-name">Home &mdash; Lebanon, IN</div>
          <div class="portal-location-coords">40.0481&deg;N, 86.4694&deg;W &middot; Boone County</div>
        </div>
      </div>
    </section>

    <!-- Brief preferences -->
    <section class="portal-section">
      <div class="portal-section-head">
        <h2 class="portal-section-title">Brief delivery</h2>
        <button type="button" class="portal-section-edit" id="portal-brief-delivery-edit">Edit</button>
      </div>
      <div class="portal-prefs-grid">
        <div class="portal-pref">
          <div class="portal-pref-label">Daily brief delivery time</div>
          <div class="portal-pref-value" id="portal-brief-delivery-display">7:00 AM</div>
          <div class="portal-pref-detail" id="portal-brief-delivery-tz">America/New_York &middot; your local time</div>
        </div>
        <div class="portal-pref">
          <div class="portal-pref-label">Morning brief</div>
          <div class="portal-pref-value">5:30&ndash;7:00 AM</div>
          <div class="portal-pref-detail">Customer-local &middot; weekdays + weekends</div>
        </div>
        <div class="portal-pref">
          <div class="portal-pref-label">Quiet hours</div>
          <div class="portal-pref-value">9:00 PM &ndash; 5:00 AM</div>
          <div class="portal-pref-detail">Bypassed only for severe weather</div>
        </div>
        <div class="portal-pref">
          <div class="portal-pref-label">Notification channels</div>
          <div class="portal-pref-channels">
            <span class="portal-channel-chip is-active">SMS</span>
            <span class="portal-channel-chip is-active">Email</span>
          </div>
          <div class="portal-pref-detail">Tap Edit above to change</div>
        </div>
      </div>
    </section>

    <!-- Brief delivery time edit modal -->
    <div id="portal-brief-delivery-modal" class="met-post-modal" hidden>
      <div class="met-post-modal-scrim" id="portal-brief-delivery-scrim"></div>
      <div class="met-post-modal-card" role="dialog" aria-modal="true" style="max-width: 440px;">
        <div class="met-post-modal-head">
          <h2 class="met-post-modal-title">When do you want your daily brief?</h2>
          <button type="button" class="met-post-modal-close" id="portal-brief-delivery-close" aria-label="Close">
            <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
          </button>
        </div>
        <div class="met-post-modal-body">
          <p style="font-size: 12.5px; color: rgba(15,17,22,0.65); margin: 0 0 16px; line-height: 1.55;">
            Your daily brief will be delivered by this time, in your local timezone. Your assigned meteorologist will write it before this deadline.
          </p>
          <div style="margin-bottom: 14px;">
            <label style="display: block; font-size: 11.5px; font-weight: 700; color: rgba(15,17,22,0.55); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 6px;">Delivery time *</label>
            <input type="time" id="portal-brief-delivery-time" value="07:00" style="width: 100%; padding: 9px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; font-size: 14px; font-family: inherit;">
          </div>
          <div style="margin-bottom: 14px;">
            <label style="display: block; font-size: 11.5px; font-weight: 700; color: rgba(15,17,22,0.55); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 6px;">Timezone *</label>
            <select id="portal-brief-delivery-timezone" style="width: 100%; padding: 9px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; font-size: 14px; font-family: inherit;">
              <option value="America/New_York">Eastern (New York)</option>
              <option value="America/Chicago">Central (Chicago)</option>
              <option value="America/Denver">Mountain (Denver)</option>
              <option value="America/Phoenix">Mountain (Phoenix, no DST)</option>
              <option value="America/Los_Angeles">Pacific (Los Angeles)</option>
              <option value="America/Anchorage">Alaska (Anchorage)</option>
              <option value="Pacific/Honolulu">Hawaii (Honolulu)</option>
            </select>
          </div>
          <p id="portal-brief-delivery-error" hidden style="background: rgba(194,52,43,0.08); border: 1px solid rgba(194,52,43,0.20); border-radius: 8px; padding: 10px 12px; font-size: 12.5px; color: #9b2823; margin: 0 0 14px;"></p>
          <div style="display: flex; justify-content: flex-end; gap: 10px;">
            <button type="button" id="portal-brief-delivery-cancel"
                    style="padding: 11px 18px; background: transparent; border: 1px solid rgba(15,17,22,0.15); color: rgba(15,17,22,0.65); border-radius: 8px; font-size: 13px; cursor: pointer;">Cancel</button>
            <button type="button" id="portal-brief-delivery-save"
                    style="padding: 11px 22px; background: #2E4FB8; border: none; color: #fff; border-radius: 8px; font-size: 13px; font-weight: 600; cursor: pointer;">Save</button>
          </div>
        </div>
      </div>
    </div>

    <!-- Threshold alerts -->
    <section class="portal-section">
      <div class="portal-section-head">
        <h2 class="portal-section-title">Threshold alerts</h2>
        <button type="button" class="portal-section-add">+ Add alert</button>
      </div>
      <div class="portal-thresholds">
        <div class="portal-threshold">
          <div class="portal-threshold-icon">
            <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9.59 4.59A2 2 0 1 1 11 8H2m10.59 11.41A2 2 0 1 0 14 16H2m15.73-8.27A2.5 2.5 0 1 1 19.5 12H2"/>
            </svg>
          </div>
          <div class="portal-threshold-text">
            <div class="portal-threshold-name">Wind exceeds 25 mph</div>
            <div class="portal-threshold-detail">Notify via SMS + Email &middot; active</div>
          </div>
          <button type="button" class="portal-threshold-action">Edit</button>
        </div>
        <div class="portal-threshold">
          <div class="portal-threshold-icon">
            <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 14.76V3.5a2.5 2.5 0 0 0-5 0v11.26a4.5 4.5 0 1 0 5 0z"/>
            </svg>
          </div>
          <div class="portal-threshold-text">
            <div class="portal-threshold-name">Temperature drops below 32&deg;F</div>
            <div class="portal-threshold-detail">Notify via SMS &middot; active</div>
          </div>
          <button type="button" class="portal-threshold-action">Edit</button>
        </div>
        <div class="portal-threshold">
          <div class="portal-threshold-icon">
            <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
              <path d="M20 16.2A4.5 4.5 0 0 0 17.5 8h-1.8A7 7 0 1 0 4 14.9"/>
            </svg>
          </div>
          <div class="portal-threshold-text">
            <div class="portal-threshold-name">Precipitation chance over 70%</div>
            <div class="portal-threshold-detail">Notify via Email &middot; active</div>
          </div>
          <button type="button" class="portal-threshold-action">Edit</button>
        </div>
      </div>
    </section>

    <!-- ══════════════════════════════════════════════════════════════
         Pro Thread — direct messaging with the Met team (PT-B)
         Two rendered states:
           (a) Pro subscriber: shows conversation view with composer
           (b) Hobbyist: shows feature teaser + upgrade CTA
         Empty state for Pro tier (no messages yet) shows a friendly
         "start the conversation" prompt.
         ══════════════════════════════════════════════════════════════ -->
    <section class="portal-section" id="portal-pro-thread-section" hidden>
      <div class="portal-section-head">
        <h2 class="portal-section-title">
          Pro Thread
          <span id="portal-pro-thread-unread" hidden
                style="background:#C2342B;color:#fff;padding:2px 8px;border-radius:10px;font-size:11px;font-weight:700;margin-left:8px;vertical-align:middle;">0</span>
        </h2>
        <button type="button" id="portal-pro-thread-refresh" class="portal-section-edit">Refresh</button>
      </div>

      <!-- Pro tier — conversation surface -->
      <div id="portal-pro-thread-conversation" hidden>
        <div id="portal-pro-thread-messages"
             style="background:#fff;border:1px solid rgba(15,17,22,0.08);border-radius:10px;padding:16px;max-height:420px;overflow-y:auto;margin-bottom:14px;display:flex;flex-direction:column;gap:10px;">
          <!-- messages rendered by JS -->
        </div>
        <div style="display:flex;gap:10px;align-items:flex-start;">
          <textarea id="portal-pro-thread-input" rows="2" maxlength="4000"
                    placeholder="Send a message to your meteorologist team..."
                    style="flex:1;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;resize:vertical;line-height:1.5;"></textarea>
          <button type="button" id="portal-pro-thread-send"
                  style="background:#2E4FB8;color:#fff;border:none;padding:10px 18px;border-radius:8px;font-size:13px;font-weight:600;cursor:pointer;align-self:flex-start;">
            Send
          </button>
        </div>
        <p style="font-size:11px;color:rgba(15,17,22,0.50);margin:8px 0 0;line-height:1.4;">
          Business-hours response within 30 min. After-hours messages get a reply by next morning's brief.
        </p>
      </div>

      <!-- Hobbyist tier — upgrade teaser -->
      <div id="portal-pro-thread-teaser" hidden
           style="background:linear-gradient(135deg,rgba(46,79,184,0.04) 0%,rgba(46,79,184,0.08) 100%);border:1px solid rgba(46,79,184,0.15);border-radius:10px;padding:20px;">
        <div style="display:flex;align-items:flex-start;gap:14px;">
          <div style="flex-shrink:0;width:36px;height:36px;background:rgba(46,79,184,0.12);border-radius:50%;display:flex;align-items:center;justify-content:center;">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#2E4FB8" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <div style="flex:1;">
            <h3 style="font-size:15px;font-weight:600;color:#0E1116;margin:0 0 6px;">Direct messaging is a Pro feature</h3>
            <p style="font-size:13px;color:rgba(15,17,22,0.65);margin:0 0 14px;line-height:1.5;">
              Pro subscribers can message our meteorologist team directly &mdash; 30-minute response during business hours, answers tailored to your operation.
            </p>
            <button type="button" id="portal-pro-thread-upgrade"
                    style="background:#2E4FB8;color:#fff;border:none;padding:8px 18px;border-radius:6px;font-size:13px;font-weight:500;cursor:pointer;">
              See Pro plans
            </button>
          </div>
        </div>
      </div>
    </section>

    <!-- Brief history -->
    <section class="portal-section">
      <div class="portal-section-head">
        <h2 class="portal-section-title">Brief history</h2>
        <button type="button" class="portal-section-edit">See all</button>
      </div>
      <div class="portal-history">
        <div class="portal-history-item">
          <div class="portal-history-date">
            <div class="portal-history-day">Today</div>
            <div class="portal-history-time">5:32 AM</div>
          </div>
          <div class="portal-history-content">
            <div class="portal-history-meta">
              <span class="portal-history-type">Morning brief</span>
              <span class="portal-history-verdict verdict-clear">Clear</span>
            </div>
            <div class="portal-history-snippet">Light winds through the morning. Cloud cover thickens by 2 PM but no precip in the work window.</div>
            <div class="portal-history-signed">AI brief &middot; tap to read</div>
          </div>
        </div>
        <div class="portal-history-item">
          <div class="portal-history-date">
            <div class="portal-history-day">Yesterday</div>
            <div class="portal-history-time">5:28 AM</div>
          </div>
          <div class="portal-history-content">
            <div class="portal-history-meta">
              <span class="portal-history-type">Morning brief</span>
              <span class="portal-history-verdict verdict-caution">Caution</span>
            </div>
            <div class="portal-history-snippet">Wind picking up by midday &mdash; 22-28 mph from the southwest. Watch for gusts at site.</div>
            <div class="portal-history-signed">AI brief &middot; tap to read</div>
          </div>
        </div>
        <div class="portal-history-item">
          <div class="portal-history-date">
            <div class="portal-history-day">Thu</div>
            <div class="portal-history-time">5:30 AM</div>
          </div>
          <div class="portal-history-content">
            <div class="portal-history-meta">
              <span class="portal-history-type">Morning brief</span>
              <span class="portal-history-verdict verdict-clear">Clear</span>
            </div>
            <div class="portal-history-snippet">Quiet weather pattern continues. Light easterly breeze, mid-60s by afternoon, no precip threat.</div>
            <div class="portal-history-signed">AI brief &middot; tap to read</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Subscription management -->
    <section class="portal-section portal-section-billing">
      <div class="portal-section-head">
        <h2 class="portal-section-title">Subscription</h2>
      </div>
      <div class="portal-billing-card">
        <div class="portal-billing-row">
          <div class="portal-billing-label">Plan</div>
          <div class="portal-billing-value">WeatherValet &mdash; Hobbyist</div>
        </div>
        <div class="portal-billing-row">
          <div class="portal-billing-label">Price</div>
          <div class="portal-billing-value">$30 / month</div>
        </div>
        <div class="portal-billing-row">
          <div class="portal-billing-label">Next billing date</div>
          <div class="portal-billing-value">June 10, 2026</div>
        </div>
        <div class="portal-billing-row">
          <div class="portal-billing-label">Payment method</div>
          <div class="portal-billing-value">Visa ending in 4242</div>
        </div>
        <div class="portal-billing-actions">
          <button type="button" class="portal-billing-action" data-billing-action="portal">Update payment method</button>
          <button type="button" class="portal-billing-action" data-billing-action="portal">Change plan</button>
          <button type="button" class="portal-billing-action portal-billing-cancel" data-billing-action="portal">Cancel subscription</button>
        </div>
      </div>
    </section>

  </div>
</div>


<!-- ============ CREW REGISTRATION ====================================
     Signup form for becoming a Valet Crew member. Lands here when a
     free user clicks "Join the Valet Crew" on the Two Paths card.
     Pre-Twilio, this is a captured-but-not-yet-active state — the
     form submits to a waitlist. For May 24, the CTA on Two Paths
     routes to email-capture instead of this screen; this screen is
     reachable for Met preview only. -->
<div id="screen-crew-register" class="screen crew-register">
  <div class="portal-screen">

    <nav class="portal-nav">
      <button type="button" class="portal-back" data-back-to="screen-welcome">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M15 18l-6-6 6-6"/>
        </svg>
        Home
      </button>
      <span class="portal-nav-title">Join the Crew</span>
      <span style="min-width: 60px;"></span>
    </nav>

    <!-- Hero / explainer -->
    <div class="crew-register-hero">
      <div class="crew-register-eyebrow">
        <span class="crew-register-eyebrow-dot"></span>
        <span>Valet Crew &middot; membership</span>
      </div>
      <h1 class="crew-register-headline">Be part of the network that makes <em>forecasts</em> better.</h1>
      <p class="crew-register-sub">Crew members observe weather where they live and contribute ground-truth reports that sharpen forecasts for everyone. Real attribution. Real impact. Real people on the ground.</p>
    </div>

    <!-- What you get / what's expected -->
    <div class="crew-register-pillars">
      <div class="crew-register-pillar">
        <div class="crew-register-pillar-icon">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2 4 6v6c0 5 3.5 9.5 8 10 4.5-.5 8-5 8-10V6l-8-4z"/>
            <path d="m9 12 2 2 4-4"/>
          </svg>
        </div>
        <h3 class="crew-register-pillar-title">Public attribution</h3>
        <p class="crew-register-pillar-body">When meteorologists cite your reports, your name appears on the customer's forecast. Real credit, real signal.</p>
      </div>
      <div class="crew-register-pillar">
        <div class="crew-register-pillar-icon">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
          </svg>
        </div>
        <h3 class="crew-register-pillar-title">Daily missions</h3>
        <p class="crew-register-pillar-body">A short text each morning &mdash; what to watch for in your county. Reply with a keyword to check in. Two-second daily habit.</p>
      </div>
      <div class="crew-register-pillar">
        <div class="crew-register-pillar-icon">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="12" y1="1" x2="12" y2="23"/>
            <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
          </svg>
        </div>
        <h3 class="crew-register-pillar-title">Real-money tips</h3>
        <p class="crew-register-pillar-body">When a subscriber's forecast cites your work, they can tip you directly. Money flows to you, not to WeatherValet.</p>
      </div>
    </div>

    <!-- Registration form -->
    <section class="portal-section">
      <h2 class="portal-section-title">Tell us about yourself</h2>
      <form class="crew-register-form" id="crew-register-form">
        <div class="form-grid">
          <div class="form-field">
            <label class="form-label">Your name</label>
            <input type="text" class="form-input" name="name" placeholder="Sarah Indianapolis">
          </div>
          <div class="form-field">
            <label class="form-label">Preferred handle</label>
            <input type="text" class="form-input" name="handle" placeholder="@sarah_indy">
          </div>
        </div>
        <div class="form-grid">
          <div class="form-field">
            <label class="form-label">Email</label>
            <input type="email" class="form-input" name="email" placeholder="sarah@example.com">
          </div>
          <div class="form-field">
            <label class="form-label">Phone (for daily mission texts)</label>
            <input type="tel" class="form-input" name="phone" placeholder="(317) 555-0123">
          </div>
        </div>
        <div class="form-field">
          <label class="form-label">Home county / region</label>
          <input type="text" class="form-input" name="county" placeholder="Marion County, IN">
          <div class="form-detail">Where you spend most of your time &mdash; we'll tap you for missions in this area.</div>
        </div>
        <div class="form-field">
          <label class="form-label">What kinds of weather interest you?</label>
          <div class="form-checkbox-grid">
            <label class="form-checkbox"><input type="checkbox" name="mission-storms" checked> Severe storms</label>
            <label class="form-checkbox"><input type="checkbox" name="mission-hail" checked> Hail events</label>
            <label class="form-checkbox"><input type="checkbox" name="mission-wind" checked> Wind</label>
            <label class="form-checkbox"><input type="checkbox" name="mission-rain"> Rain / flooding</label>
            <label class="form-checkbox"><input type="checkbox" name="mission-winter"> Winter weather</label>
            <label class="form-checkbox"><input type="checkbox" name="mission-general"> General observations</label>
          </div>
        </div>
        <div class="form-field">
          <label class="form-label">Available hours</label>
          <select class="form-input" name="hours">
            <option value="all">All day (default)</option>
            <option value="weekdays-day">Weekdays, daytime</option>
            <option value="weekdays-evening">Weekdays, evening</option>
            <option value="weekends">Weekends only</option>
            <option value="custom">Custom &mdash; we'll ask in onboarding</option>
          </select>
        </div>
        <div class="form-field">
          <label class="form-label">Notification preference</label>
          <div class="form-radio-stack">
            <label class="form-radio"><input type="radio" name="notify" value="sms" checked> <span><strong>SMS</strong> &mdash; daily mission text, reply to check in (recommended)</span></label>
            <label class="form-radio"><input type="radio" name="notify" value="email"> <span><strong>Email</strong> &mdash; daily mission email, no text replies</span></label>
            <label class="form-radio"><input type="radio" name="notify" value="both"> <span><strong>Both</strong> &mdash; SMS for missions, email for recognition feed</span></label>
          </div>
        </div>
        <div class="form-field form-agree">
          <label class="form-checkbox-large">
            <input type="checkbox" name="agree" required>
            <span>I agree to contribute honest weather observations and to follow the <a href="#" class="form-link">Valet Crew Code of Conduct</a> when reporting.</span>
          </label>
        </div>
        <div class="form-submit-row">
          <button type="button" class="form-submit-btn" data-crew-register-submit="1">Become a Valet Crew member</button>
          <p class="form-submit-detail">Free to join &middot; paid for verified work &middot; we&rsquo;ll be in touch within 48 hours with next steps.</p>
        </div>
      </form>
    </section>

  </div>
</div>


<!-- ============ VALET CREW · LIVE FEED (MAP) ============ -->
<div id="screen-crew-feed" class="screen">
  <div class="crew-screen">

    <nav class="crew-nav">
      <button type="button" class="crew-back" data-back-to="screen-crew-profile">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M15 18l-6-6 6-6"/>
        </svg>
        Back
      </button>
      <span class="crew-nav-title">Live Feed</span>
      <span style="min-width:44px"></span>
    </nav>

    <!-- ── Live broadcast ticker (Chunk 4a) ────────────────
         A single-line "slot machine" that fades through real events
         every ~3.5 seconds. Pulls from CREW_REPORTS, CREW_RECEIPTS,
         and live verification activity. Designed to make the page
         feel like a live channel — something is always happening,
         even on quiet weather days. Hidden on the smallest viewports
         to save space. -->
    <div class="feed-ticker" id="feed-ticker" aria-live="polite" aria-atomic="true">
      <span class="feed-ticker-rec">
        <span class="feed-ticker-rec-dot"></span>
        ON AIR
      </span>
      <span class="feed-ticker-content" id="feed-ticker-content">
        <span class="feed-ticker-text" id="feed-ticker-text">Loading the feed…</span>
      </span>
    </div>

    <!-- ── "Now on air" rail (Chunk 4b) ─────────────────────
         The cast for this episode. Crew members who've reported
         in the last 30 min, shown as small horizontal cards.
         Each card has avatar, handle, city, last-report summary,
         active dot, and verification count. Tappable → opens
         that Crew member's most recent report.

         Hidden when there are no recent reports (quiet days look
         empty rather than showing a stale cast). The "active"
         indicator only shows for the freshest reports. -->
    <div class="onair-rail" id="onair-rail" aria-label="Crew on air right now" hidden>
      <div class="onair-rail-header">
        <span class="onair-rail-eyebrow">Now on air</span>
        <span class="onair-rail-count" id="onair-rail-count">&mdash;</span>
      </div>
      <div class="onair-rail-scroll" id="onair-rail-scroll">
        <!-- Cards rendered by renderOnAirRail() -->
      </div>
    </div>

    <!-- ── Tonight's story tagline (Chunk 4c) ───────────────
         A single cinematic line stating the situation — what's
         happening across the territory right now. Auto-generates
         from the report mix (severe / active / current categories)
         + the current zoom level (for the geographic name).

         The watcher count is markup-ready but hidden by default —
         when backend ships real "currently viewing" numbers, just
         flip the hidden attribute and feed it values. Zero visual
         change today; trivial enablement later. -->
    <div class="tonight-story" id="tonight-story">
      <p class="tonight-story-line" id="tonight-story-line">Watching the Crew</p>
      <span class="tonight-story-watchers" id="tonight-story-watchers" hidden>
        <span class="tonight-story-watchers-dot"></span>
        <span id="tonight-story-watchers-num">0</span> watching
      </span>
    </div>

    <h1 class="crew-feed-headline">What the Crew is <em>seeing.</em></h1>
    <p style="font-size:13px;color:rgba(15,17,22,0.55);margin:0 0 12px">
      Live Crew reports across Indiana. Tap a pulse to see the report &middot; pinch or scroll to zoom &middot; drag to pan.
    </p>
    <div class="prototype-pill">
      <span class="prototype-pill-dot"></span>
      <span>Prototype data &middot; reports are simulated for testing</span>
    </div>

    <!-- First-time Crew welcome card. Shown once per user-device (tracked
         in localStorage). Reviewers reported the original Crew landing felt
         "abrupt" — you hit the map with no context. This card answers the
         three questions a brand-new visitor has: what is this, what can I
         do here, why should I care. Dismissable; doesn't reappear after
         the user closes it. -->
    <div class="crew-welcome-card" id="crew-welcome-card" hidden>
      <button type="button" class="crew-welcome-dismiss" id="crew-welcome-dismiss"
              aria-label="Dismiss welcome">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 6 6 18"/><path d="m6 6 12 12"/>
        </svg>
      </button>
      <div class="crew-welcome-eyebrow">
        <span class="crew-welcome-eyebrow-dot"></span>
        <span>First time here?</span>
      </div>
      <h2 class="crew-welcome-title">This is the Valet Crew.</h2>
      <p class="crew-welcome-body">
        Real people on the ground reporting weather as it&rsquo;s happening &mdash;
        wind, hail, rain, photos. Tap any pulse on the map to read what
        someone&rsquo;s seeing. Verify a report you can confirm. Tip Crew
        members whose reports help you. Use the <strong>+</strong> button
        in the corner to add your own.
      </p>
      <div class="crew-welcome-steps">
        <div class="crew-welcome-step">
          <span class="crew-welcome-step-num">1</span>
          <span>Tap a pulse to see the report</span>
        </div>
        <div class="crew-welcome-step">
          <span class="crew-welcome-step-num">2</span>
          <span>Pinch or scroll to zoom &middot; drag to pan around</span>
        </div>
        <div class="crew-welcome-step">
          <span class="crew-welcome-step-num">3</span>
          <span>Tap <strong>+</strong> to submit your own report</span>
        </div>
      </div>
      <div class="crew-welcome-actions">
        <button type="button" class="crew-welcome-cta-secondary" id="crew-welcome-cta">Just looking around</button>
        <button type="button" class="crew-welcome-cta" data-email-capture="crew">Become a Valet Crew member</button>
      </div>
    </div>

    <!-- ── NWS overlay filter row (Chunk 6a) ───────────────
         Three pills control what NWS overlays the Crew sees
         on top of the Live Feed map: Warnings, Watches, and
         Advisories. The Advisories pill has a small sub-menu
         button that opens a checklist of advisory event types
         (winter weather, wind, heat, fog, etc.) so a user can
         tune which advisories they want.

         Defaults: Warnings ON, Watches ON, Advisories OFF.
         State persists in localStorage (key: wv_nws_filters_v1)
         so the user's choice survives a refresh. -->
    <div class="nws-filter-row" id="nws-filter-row">
      <span class="nws-filter-label">Weather overlays:</span>
      <div style="position: relative; display: inline-flex; align-items: center;">
        <button type="button" class="nws-filter-pill" data-filter="warning" aria-pressed="true">
          <span class="nws-filter-dot" style="background:#C2342B"></span>
          <span>Warnings</span>
          <span class="nws-filter-count" id="nws-filter-count-warning">0</span>
        </button>
        <button type="button" class="nws-event-filter-caret" data-event-filter-cat="warning"
                title="Filter which warnings to show" aria-label="Filter warnings"
                style="background: transparent; border: 0; padding: 4px 6px; cursor: pointer; color: rgba(15,17,22,0.55); display: inline-flex; align-items: center; margin-left: 2px;">
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
        </button>
      </div>
      <div style="position: relative; display: inline-flex; align-items: center;">
        <button type="button" class="nws-filter-pill" data-filter="watch" aria-pressed="true">
          <span class="nws-filter-dot" style="background:#F5A524"></span>
          <span>Watches</span>
          <span class="nws-filter-count" id="nws-filter-count-watch">0</span>
        </button>
        <button type="button" class="nws-event-filter-caret" data-event-filter-cat="watch"
                title="Filter which watches to show" aria-label="Filter watches"
                style="background: transparent; border: 0; padding: 4px 6px; cursor: pointer; color: rgba(15,17,22,0.55); display: inline-flex; align-items: center; margin-left: 2px;">
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
        </button>
      </div>
      <div class="nws-filter-pill-group">
        <button type="button" class="nws-filter-pill" data-filter="advisory" aria-pressed="false">
          <span class="nws-filter-dot" style="background:#4169E1"></span>
          <span>Advisories</span>
          <span class="nws-filter-count" id="nws-filter-count-advisory">0</span>
        </button>
        <button type="button" class="nws-event-filter-caret" data-event-filter-cat="advisory"
                title="Filter which advisories to show" aria-label="Filter advisories"
                style="background: transparent; border: 0; padding: 4px 6px; cursor: pointer; color: rgba(15,17,22,0.55); display: inline-flex; align-items: center; margin-left: 2px;">
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
        </button>
        <button type="button" class="nws-filter-submenu-btn" id="nws-filter-submenu-btn"
                aria-label="Choose advisory categories" aria-expanded="false"
                title="Choose advisory categories">
          <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor"
               stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="6 9 12 15 18 9"/>
          </svg>
        </button>
        <div class="nws-filter-submenu" id="nws-filter-submenu" hidden>
          <div class="nws-filter-submenu-title">Show these advisory types</div>
          <label class="nws-filter-submenu-option">
            <input type="checkbox" data-advisory-type="winter" checked>
            <span>Winter weather (snow, ice, sleet, freezing rain)</span>
          </label>
          <label class="nws-filter-submenu-option">
            <input type="checkbox" data-advisory-type="wind">
            <span>Wind, dust, fog</span>
          </label>
          <label class="nws-filter-submenu-option">
            <input type="checkbox" data-advisory-type="heat">
            <span>Heat, cold, freeze</span>
          </label>
          <label class="nws-filter-submenu-option">
            <input type="checkbox" data-advisory-type="flood">
            <span>Flood, hydrology</span>
          </label>
          <label class="nws-filter-submenu-option">
            <input type="checkbox" data-advisory-type="other">
            <span>Other (statements, marine, fire)</span>
          </label>
        </div>
      </div>

      <!-- Radar toggle (Phase 4, May 17) — NEXRAD overlay on/off -->
      <button type="button" class="nws-filter-pill" id="crew-map-radar-pill" data-radar-toggle aria-pressed="false" style="margin-left: 6px;">
        <span class="nws-filter-dot" style="background: linear-gradient(90deg, #00ff00, #ffff00, #ff0000); border-radius: 2px;"></span>
        <span>Radar</span>
      </button>
    </div>

    <!-- Per-event-type filter dropdown for the Live Feed map.
         Anchored under whichever filter caret was clicked. -->
    <div id="feed-event-filter-dropdown" hidden
         style="position: absolute; z-index: 1000; background: #fff; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; box-shadow: 0 8px 24px rgba(15,17,22,0.12); min-width: 260px; max-width: 320px; max-height: 360px; overflow-y: auto; padding: 8px 0;">
      <div id="feed-event-filter-header" style="padding: 8px 14px 6px; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); border-bottom: 1px solid rgba(15,17,22,0.06); margin-bottom: 4px;">
        Filter warnings
      </div>
      <div id="feed-event-filter-list">
        <!-- Populated by JS -->
      </div>
    </div>

    <!-- The "Open full map" entry point was removed in Phase 9 —
         the Live Feed below IS the full map now. -->

    <!-- ── Live Feed map (Phase 9, May 14) ────────────────────────
         Real Leaflet map replacing the stylized SVG version.
         CartoDB Positron tiles, drag/scroll/zoom, viewport-driven
         story list filtering. NWS overlays render as real polygon
         layers. Reports render as Leaflet divIcons preserving the
         multi-ring pulse animations. Loaded lazily on first view —
         the file-level Leaflet libraries get pulled in by the
         shared initPolygonMap / met-map.js pipelines.

         Default center: user's geolocation (if permitted), with
         a 4-second timeout fallback to Indianapolis.

         Loading shim sits above the map until Leaflet renders.
    -->
    <div class="feed-map" id="feed-map">
      <!-- Loading state — visible while Leaflet + tiles initialize.
           Hidden via CSS class once the map is ready. -->
      <div class="feed-map-loading" id="feed-map-loading">
        <div class="feed-map-loading-dot"></div>
        <span>Centering the map on your area&hellip;</span>
      </div>

      <!-- The Leaflet container itself. JS attaches the map here. -->
      <div class="feed-map-leaflet" id="feed-map-leaflet"></div>

      <!-- Legend (top-left, sits ABOVE the map via z-index) -->
      <div class="feed-legend">
        <div class="feed-legend-line"><span class="feed-legend-swatch" style="background:#C2342B"></span> Storm</div>
        <div class="feed-legend-line"><span class="feed-legend-swatch" style="background:#5B6BD6"></span> Hail</div>
        <div class="feed-legend-line"><span class="feed-legend-swatch" style="background:#E8A400"></span> Wind</div>
        <div class="feed-legend-line"><span class="feed-legend-swatch" style="background:#2566D6"></span> Rain</div>
        <div class="feed-legend-line"><span class="feed-legend-swatch" style="background:#12A150"></span> Clear</div>
      </div>

      <!-- "Recenter on me" button — appears when geolocation worked
           and the user has panned away. Bottom-right corner. -->
      <button type="button" class="feed-map-locate" id="feed-map-locate" hidden
              aria-label="Recenter on my location">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="3"/>
          <path d="M12 1v6"/><path d="M12 17v6"/>
          <path d="M1 12h6"/><path d="M17 12h6"/>
        </svg>
      </button>

      <!-- Bottom stats strip — count + viewport label -->
      <div class="feed-stats-strip">
        <span class="feed-stats-count" id="feed-stats-count">&mdash;</span>
        <span class="feed-stats-area" id="feed-stats-area">&mdash;</span>
      </div>
    </div>

    <!-- Story list - populated dynamically, sorted newest-first, max 10. -->
    <div class="crew-section-label">Recent reports in this area</div>
    <ul class="story-list" id="feed-story-list"></ul>

    <!-- ── Up Next footer (Chunk 4d) ────────────────────────
         "Looking ahead" — what's coming geographically. Pulls
         NWS active watches/warnings/advisories and filters by
         the current map zoom (county / region / state). Shows
         all active items in scope — weather rarely has more
         than 5 in one area at once.

         Quiet-day fallback: instead of "nothing on the horizon,"
         shows the next scheduled NWS forecast issuance time, so
         the section still conveys "the Mets are watching." -->
    <section class="upnext-footer" id="upnext-footer">
      <header class="upnext-footer-header">
        <span class="upnext-footer-eyebrow">
          <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor"
               stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="12 6 12 12 16 14"/>
            <circle cx="12" cy="12" r="10"/>
          </svg>
          <span>Looking ahead</span>
        </span>
        <span class="upnext-footer-source">National Weather Service &middot; <span id="upnext-footer-updated">just now</span></span>
      </header>
      <div class="upnext-footer-body" id="upnext-footer-body">
        <div class="upnext-loading">Checking the horizon&hellip;</div>
      </div>
    </section>

  </div>

  <!-- Floating Quick Report button on the feed screen -->
  <button type="button" class="report-fab" data-open-report="1" aria-label="Submit Quick Report">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6"
         stroke-linecap="round" stroke-linejoin="round">
      <line x1="12" y1="5" x2="12" y2="19"/>
      <line x1="5" y1="12" x2="19" y2="12"/>
    </svg>
  </button>
</div>


<!-- ============ VALET CREW · REPORT DETAIL ============ -->
<!-- A focused single-report view. Reachable from a tap on any pulse on
     the map or any story card on the feed. The body of this screen is
     populated dynamically by renderReportDetail(reportId) — kept empty
     here so the same scaffold can host any of the six demo reports. -->
<div id="screen-crew-detail" class="screen">
  <div class="crew-screen">
    <nav class="crew-nav">
      <button type="button" class="crew-back" data-back-to="screen-crew-feed">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M15 18l-6-6 6-6"/>
        </svg>
        Feed
      </button>
      <span class="crew-nav-title">Report</span>
      <span style="min-width:44px"></span>
    </nav>
    <div class="detail-card">
      <div id="detail-body"></div>
    </div>
  </div>
</div>

<!-- ============ VALET CREW · FULL MAP SCREEN (Chunk 6b) ============
     A real geographic Leaflet map with mouse zoom + pan, showing
     Crew pulses, NWS warnings/watches/advisories, and the user's
     own pulses with a distinguishing ring. Lazy-loaded — Leaflet
     and crew-map.js only fetched the first time the user opens
     this screen. -->
<!-- The screen-crew-map screen was removed in Phase 9. Its functionality
     (full Leaflet map with NWS overlays and Crew pulses) moved into the
     Live Feed screen above (#screen-crew-feed), which is now a real map. -->

<!-- ============ MET-POST PIN-DROP MODAL (Chunk 6c-1) ===================
     Opens when a Met clicks "Send to Crew feed" on the brief panel's
     quick-post composer. Shows the message preview, a small Leaflet
     map for picking the coverage centroid, and a Send button that
     publishes the post to the Crew feed once a point is dropped. -->
<div id="met-post-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="met-post-modal-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="met-post-modal-title">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="met-post-modal-title">Send to Crew feed</h2>
      <button type="button" class="met-post-modal-close" id="met-post-modal-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 6 6 18"/><path d="m6 6 12 12"/>
        </svg>
      </button>
    </div>

    <div class="met-post-modal-body">
      <p class="met-post-modal-eyebrow">Your message</p>
      <blockquote class="met-post-modal-preview" id="met-post-modal-preview"></blockquote>

      <p class="met-post-modal-eyebrow">Where does this apply?</p>
      <p class="met-post-modal-help">
        Click on the map to drop a pin at the center of the area this message applies to.
        The pin shows up on the Crew map so they know which part of the territory you mean.
      </p>
      <div class="met-post-modal-map-wrap">
        <div class="met-post-modal-map-loading" id="met-post-modal-map-loading">
          <div class="crew-map-loading-dot"></div>
          <span>Loading the map&hellip;</span>
        </div>
        <div class="met-post-modal-map" id="met-post-modal-map"></div>
      </div>
      <p class="met-post-modal-pin-status" id="met-post-modal-pin-status">
        No pin dropped yet &middot; click on the map to set one.
      </p>

      <!-- ── Schedule toggle (Chunk 6c-2) ──────────────────
           Two-button segmented control: "Send now" / "Schedule
           for later." When "Schedule for later" is selected,
           the date + time fields below become visible and the
           Send button label changes to "Schedule." -->
      <p class="met-post-modal-eyebrow" style="margin-top:18px">When should this go out?</p>
      <div class="met-post-schedule-toggle" role="group" aria-label="Send time">
        <button type="button" class="met-post-schedule-toggle-btn is-active"
                data-schedule-mode="now" aria-pressed="true">
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor"
               stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <path d="M22 2 11 13"/><path d="M22 2 15 22l-4-9-9-4 20-7z"/>
          </svg>
          Send now
        </button>
        <button type="button" class="met-post-schedule-toggle-btn"
                data-schedule-mode="later" aria-pressed="false">
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor"
               stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
          Schedule for later
        </button>
      </div>

      <!-- Date + time inputs, hidden by default (only shown when "later"
           is active). Two side-by-side fields plus a timezone label. -->
      <div class="met-post-schedule-fields" id="met-post-schedule-fields" hidden>
        <div class="met-post-schedule-row">
          <label class="met-post-schedule-field">
            <span class="met-post-schedule-field-label">Date</span>
            <input type="date" id="met-post-schedule-date" class="met-post-schedule-input">
          </label>
          <label class="met-post-schedule-field">
            <span class="met-post-schedule-field-label">Time</span>
            <input type="time" id="met-post-schedule-time" class="met-post-schedule-input">
          </label>
        </div>
        <p class="met-post-schedule-zone" id="met-post-schedule-zone">
          Timezone: <span id="met-post-schedule-tz">&mdash;</span>
        </p>
        <p class="met-post-schedule-error" id="met-post-schedule-error" hidden></p>
      </div>
    </div>

    <div class="met-post-modal-actions">
      <button type="button" class="met-post-modal-cancel" id="met-post-modal-cancel">Cancel</button>
      <button type="button" class="met-post-modal-send" id="met-post-modal-send" disabled>
        Send to Crew feed
      </button>
    </div>
  </div>
</div>

<!-- ── Cancel confirmation modal (Chunk 6c-2) ─────────────
     Small confirm dialog that appears when a Met clicks "Cancel"
     on a scheduled post. Small to avoid being annoying; just
     enough to prevent fat-finger deletes. -->
<div id="met-queue-cancel-modal" class="met-queue-cancel-modal" hidden>
  <div class="met-queue-cancel-scrim" id="met-queue-cancel-scrim"></div>
  <div class="met-queue-cancel-card" role="dialog" aria-modal="true" aria-labelledby="met-queue-cancel-title">
    <h3 class="met-queue-cancel-title" id="met-queue-cancel-title">Cancel this scheduled post?</h3>
    <blockquote class="met-queue-cancel-preview" id="met-queue-cancel-preview"></blockquote>
    <p class="met-queue-cancel-meta" id="met-queue-cancel-meta">&mdash;</p>
    <div class="met-queue-cancel-actions">
      <button type="button" class="met-queue-cancel-keep" id="met-queue-cancel-keep">Keep it scheduled</button>
      <button type="button" class="met-queue-cancel-confirm" id="met-queue-cancel-confirm">Cancel post</button>
    </div>
  </div>
</div>

<!-- ── Subscriber portal: Saved location edit modal (Phase 10 B2) ──
     Opens when "Edit" tapped on the Saved location section (or for "Add"
     when the user has no saved location yet).
     Two-pane: address input on the left, Leaflet pin-drop map on the right.
     Type → "Look up" → backend geocodes → pin drops on map.
     User can drag the pin to fine-tune the exact coords. -->
<div id="saved-location-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="saved-location-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="saved-location-title" style="max-width: 680px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="saved-location-title">Saved location</h2>
      <button type="button" class="met-post-modal-close" id="saved-location-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body">

      <!-- Label -->
      <div class="prefs-section">
        <div class="prefs-section-label">Label</div>
        <input type="text" id="saved-location-label" class="saved-location-input"
               placeholder="Home, Job site, etc." maxlength="120">
      </div>

      <!-- Address lookup -->
      <div class="prefs-section">
        <div class="prefs-section-label">Address</div>
        <div class="saved-location-lookup-row">
          <input type="text" id="saved-location-address" class="saved-location-input"
                 placeholder="Lebanon, IN" autocomplete="off">
          <button type="button" class="saved-location-lookup-btn" id="saved-location-lookup-btn">
            Look up
          </button>
        </div>
        <p class="saved-location-help" id="saved-location-help">
          Type an address and tap Look up &mdash; or drag the pin on the map to fine-tune.
        </p>
      </div>

      <!-- Map pin-drop -->
      <div class="saved-location-map-wrap">
        <div class="saved-location-map-loading" id="saved-location-map-loading">
          Loading map&hellip;
        </div>
        <div class="saved-location-map" id="saved-location-map"></div>
      </div>

      <!-- Coords readout (auto-updated after lookup or pin drag) -->
      <p class="saved-location-coords-readout" id="saved-location-coords-readout"></p>

      <p class="prefs-error" id="saved-location-error" hidden></p>
    </div>
    <div class="met-post-modal-foot">
      <button type="button" class="met-post-modal-cancel" id="saved-location-cancel">Cancel</button>
      <button type="button" class="met-post-modal-delete" id="saved-location-delete" hidden>Delete</button>
      <button type="button" class="met-post-modal-send" id="saved-location-save">Save location</button>
    </div>
  </div>
</div>


<!-- ── Crew Home Base modal (Phase 10 Item #6) ──────────────────────
     Lets a Crew member set their home base for mission targeting.
     Same map-based pin-drop pattern as the subscriber saved-location
     modal — type address → look up → draggable Leaflet marker. -->
<div id="crew-homebase-modal" class="crew-homebase-modal" hidden>
  <div class="crew-homebase-modal-scrim" id="crew-homebase-modal-scrim"></div>
  <div class="crew-homebase-modal-card" role="dialog" aria-modal="true" aria-labelledby="crew-homebase-modal-title">
    <div class="crew-homebase-modal-head">
      <h2 class="crew-homebase-modal-title" id="crew-homebase-modal-title">Set your home base</h2>
      <button type="button" class="crew-homebase-modal-close" id="crew-homebase-modal-close" aria-label="Close">&times;</button>
    </div>
    <div class="crew-homebase-modal-body">
      <div class="crew-homebase-modal-field">
        <label class="crew-homebase-modal-field-label" for="crew-homebase-label-input">Label</label>
        <input type="text" class="crew-homebase-modal-input" id="crew-homebase-label-input"
               placeholder="e.g. Lebanon, IN" maxlength="200">
      </div>
      <div class="crew-homebase-modal-field">
        <label class="crew-homebase-modal-field-label" for="crew-homebase-address-input">Address (we&rsquo;ll find the location)</label>
        <input type="text" class="crew-homebase-modal-input" id="crew-homebase-address-input"
               placeholder="123 Main St, Lebanon, IN">
        <button type="button" class="crew-homebase-modal-lookup" id="crew-homebase-lookup-btn">Look up address</button>
      </div>
      <div class="crew-homebase-modal-field">
        <label class="crew-homebase-modal-field-label">Pin location (drag to fine-tune)</label>
        <div id="crew-homebase-modal-map" class="crew-homebase-modal-map"></div>
        <p class="crew-homebase-modal-hint">
          Drag the pin to your exact home base. Mets will target missions to your area
          based on this location.
        </p>
      </div>
    </div>
    <div class="crew-homebase-modal-actions">
      <button type="button" class="crew-homebase-modal-cancel" id="crew-homebase-modal-cancel">Cancel</button>
      <button type="button" class="crew-homebase-modal-save" id="crew-homebase-modal-save">Save</button>
    </div>
  </div>
</div>


<!-- ── Mission response page (Phase 10 Item #5) ──────────────────────
     Shown when the user lands on /?mission=<token> — typically a Crew
     member tapping the SMS link they got from a mission dispatch.
     Renders the mission prompt + textarea + submit. -->
<div id="screen-mission-response" class="screen">
  <div style="max-width: 580px; margin: 0 auto; padding: 40px 24px 80px;">
    <div id="mission-response-loading" style="text-align: center; padding: 40px 0; color: rgba(15,17,22,0.55); font-size: 14px;">
      Loading mission&hellip;
    </div>
    <div id="mission-response-error" hidden style="text-align: center; padding: 40px 0;">
      <p style="font-size: 16px; color: #0E1116; margin: 0 0 8px;">Mission not found</p>
      <p style="font-size: 13px; color: rgba(15,17,22,0.55); margin: 0;" id="mission-response-error-text">
        This mission link may have expired or already been used.
      </p>
    </div>
    <div id="mission-response-form" hidden>
      <div style="display: inline-block; background: rgba(65, 105, 225, 0.10); color: #2E4FB8; padding: 4px 10px; border-radius: 999px; font-size: 10px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 14px;">
        Mission from <span id="mission-response-from">your meteorologist</span>
      </div>
      <h1 style="font-size: 22px; font-weight: 600; color: #0E1116; margin: 0 0 6px;" id="mission-response-template">&mdash;</h1>
      <p style="font-size: 15px; color: rgba(15,17,22,0.78); line-height: 1.55; margin: 0 0 24px; padding: 16px; background: rgba(15,17,22,0.03); border-radius: 10px;" id="mission-response-prompt">&mdash;</p>

      <div style="margin-bottom: 16px;">
        <label style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: rgba(15,17,22,0.55); margin-bottom: 8px;" for="mission-response-textarea">Your observation</label>
        <textarea id="mission-response-textarea"
                  style="width: 100%; min-height: 140px; padding: 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 10px; font-size: 14px; font-family: inherit; box-sizing: border-box; resize: vertical;"
                  placeholder="What are you seeing right now? Be specific — wind direction, intensity, what objects are doing, sounds, anything that helps the meteorologist."
                  maxlength="4000"></textarea>
        <p style="font-size: 11.5px; color: rgba(15,17,22,0.50); margin: 6px 0 0;">
          The Met will use this to confirm what&rsquo;s actually happening on the ground.
        </p>
      </div>

      <button type="button" id="mission-response-submit"
              style="width: 100%; padding: 14px; background: var(--rb, #4169E1); color: #fff; border: none; border-radius: 10px; font-size: 15px; font-weight: 500; cursor: pointer; -webkit-tap-highlight-color: transparent;">
        Submit observation
      </button>
    </div>
    <div id="mission-response-success" hidden style="text-align: center; padding: 40px 0;">
      <div style="width: 56px; height: 56px; background: rgba(18, 161, 80, 0.12); border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; margin-bottom: 18px;">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#0c7a3c" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20 6 9 17 4 12"/>
        </svg>
      </div>
      <p style="font-size: 18px; font-weight: 600; color: #0E1116; margin: 0 0 6px;">Thanks for the eyes-on</p>
      <p style="font-size: 13px; color: rgba(15,17,22,0.60); margin: 0 0 24px; line-height: 1.5;">
        Your observation went straight to the meteorologist. They may follow up if they need more detail.
      </p>
      <a href="/" style="display: inline-block; padding: 10px 24px; background: transparent; border: 1px solid rgba(15,17,22,0.15); color: rgba(15,17,22,0.75); text-decoration: none; border-radius: 8px; font-size: 13px;">
        Back to WeatherValet
      </a>
    </div>
  </div>
</div>


<!-- ══════════════════════════════════════════════════════════════
     NWS Met severe-alert page (Phase 10 Item #7 Chunk 7B)
     Shown when Met taps the SMS link landing on /?nws-page=<token>.
     Single-purpose page: review alert, write custom message, send to
     subscribers (Confirm) or mark false alarm (Dismiss).
     ══════════════════════════════════════════════════════════════ -->
<div id="screen-nws-page" class="screen">
  <div style="max-width: 640px; margin: 0 auto; padding: 32px 20px 80px;">

    <!-- Loading state -->
    <div id="nws-page-loading" style="text-align:center;padding:40px 0;color:rgba(15,17,22,0.55);font-size:14px;">
      Loading alert&hellip;
    </div>

    <!-- Error state — invalid token, expired, etc. -->
    <div id="nws-page-error" hidden style="text-align:center;padding:40px 0;">
      <p style="font-size:16px;color:#0E1116;margin:0 0 8px;">Alert not found</p>
      <p style="font-size:13px;color:rgba(15,17,22,0.55);margin:0;" id="nws-page-error-text">
        This page link may have expired or already been handled.
      </p>
    </div>

    <!-- Active review state — pending review -->
    <div id="nws-page-review" hidden>
      <!-- Urgent banner -->
      <div style="display:flex;align-items:center;gap:10px;background:rgba(194,52,43,0.10);border:1px solid rgba(194,52,43,0.30);padding:14px 18px;border-radius:10px;margin-bottom:24px;">
        <div style="width:10px;height:10px;border-radius:50%;background:#C2342B;animation:nwsPulse 1.6s ease-in-out infinite;"></div>
        <div>
          <div style="font-size:11px;font-weight:700;letter-spacing:0.08em;text-transform:uppercase;color:#C2342B;">Severe alert page</div>
          <div style="font-size:12px;color:rgba(15,17,22,0.65);margin-top:2px;">An NWS alert affects WeatherValet subscribers in this area. Review and act.</div>
        </div>
      </div>

      <h1 style="font-size:24px;font-weight:600;color:#0E1116;margin:0 0 4px;" id="nws-page-event">&mdash;</h1>
      <p style="font-size:13px;color:rgba(15,17,22,0.55);margin:0 0 18px;font-family:'JetBrains Mono',monospace;">
        <span id="nws-page-severity">&mdash;</span> &middot;
        expires <span id="nws-page-expires">&mdash;</span>
      </p>

      <!-- NWS context -->
      <div style="background:rgba(15,17,22,0.03);padding:16px;border-radius:10px;margin-bottom:20px;">
        <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(15,17,22,0.55);margin-bottom:8px;">NWS headline</div>
        <p style="font-size:14px;color:#0E1116;line-height:1.5;margin:0 0 14px;" id="nws-page-headline">&mdash;</p>

        <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(15,17,22,0.55);margin-bottom:8px;">Affected area</div>
        <p style="font-size:13px;color:rgba(15,17,22,0.75);line-height:1.5;margin:0 0 14px;" id="nws-page-area">&mdash;</p>

        <details style="font-size:13px;color:rgba(15,17,22,0.65);">
          <summary style="cursor:pointer;font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(15,17,22,0.55);">Full NWS description</summary>
          <div style="margin-top:8px;line-height:1.55;white-space:pre-wrap;" id="nws-page-description">&mdash;</div>
          <div style="margin-top:14px;padding-top:14px;border-top:1px solid rgba(15,17,22,0.08);">
            <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(15,17,22,0.55);margin-bottom:6px;">NWS instructions</div>
            <div style="line-height:1.55;white-space:pre-wrap;" id="nws-page-instruction">&mdash;</div>
          </div>
        </details>
      </div>

      <!-- Affected subscribers -->
      <div style="margin-bottom:20px;">
        <div style="display:flex;align-items:baseline;justify-content:space-between;margin-bottom:10px;">
          <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(15,17,22,0.55);">Affected subscribers</div>
          <div style="font-size:11px;color:rgba(15,17,22,0.50);"><span id="nws-page-sub-count">0</span> affected</div>
        </div>
        <div id="nws-page-sub-list" style="background:#fff;border:1px solid rgba(15,17,22,0.08);border-radius:8px;max-height:180px;overflow-y:auto;">
          <!-- subscriber rows -->
        </div>
      </div>

      <!-- Message composer -->
      <div style="margin-bottom:18px;">
        <label style="display:block;font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(15,17,22,0.55);margin-bottom:8px;" for="nws-page-message">
          Your message to subscribers
        </label>
        <textarea id="nws-page-message" rows="5" maxlength="1000"
                  style="width:100%;padding:12px;border:1px solid rgba(15,17,22,0.15);border-radius:10px;font-size:14px;font-family:inherit;box-sizing:border-box;resize:vertical;line-height:1.55;"
                  placeholder="What do they need to do RIGHT NOW? Be specific — interior shelter, leave the field, etc. Signed automatically with your name."></textarea>
        <p style="font-size:11px;color:rgba(15,17,22,0.50);margin:6px 0 0;line-height:1.4;">
          Sent via SMS + email to every affected subscriber. Keep it tight &mdash; SMS is best under 200 characters.
        </p>
      </div>

      <!-- Met name (optional override) -->
      <div style="margin-bottom:24px;">
        <label style="display:block;font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(15,17,22,0.55);margin-bottom:8px;" for="nws-page-met-name">
          Sign as
        </label>
        <input type="text" id="nws-page-met-name" maxlength="100"
               style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;box-sizing:border-box;"
               placeholder="Your name (defaults to your account name if signed in)">
      </div>

      <!-- Action buttons -->
      <div style="display:flex;gap:10px;">
        <button type="button" id="nws-page-dismiss"
                style="flex:1;padding:14px;background:#fff;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.70);border-radius:10px;font-size:14px;font-weight:500;cursor:pointer;">
          Dismiss as false alarm
        </button>
        <button type="button" id="nws-page-confirm"
                style="flex:2;padding:14px;background:#C2342B;color:#fff;border:none;border-radius:10px;font-size:14px;font-weight:600;cursor:pointer;">
          Send alert to subscribers
        </button>
      </div>
    </div>

    <!-- Already handled state -->
    <div id="nws-page-handled" hidden style="text-align:center;padding:40px 0;">
      <div style="width:56px;height:56px;background:rgba(15,17,22,0.06);border-radius:50%;display:inline-flex;align-items:center;justify-content:center;margin-bottom:18px;">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="rgba(15,17,22,0.55)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20 6 9 17 4 12"/>
        </svg>
      </div>
      <p style="font-size:18px;font-weight:600;color:#0E1116;margin:0 0 6px;" id="nws-page-handled-title">Already handled</p>
      <p style="font-size:13px;color:rgba(15,17,22,0.60);margin:0 0 24px;line-height:1.5;" id="nws-page-handled-sub">This alert was already reviewed.</p>
      <a href="/" style="display:inline-block;padding:10px 24px;background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.75);text-decoration:none;border-radius:8px;font-size:13px;">
        Back to WeatherValet
      </a>
    </div>

    <!-- Confirmed success state -->
    <div id="nws-page-success" hidden style="text-align:center;padding:40px 0;">
      <div style="width:56px;height:56px;background:rgba(18,161,80,0.12);border-radius:50%;display:inline-flex;align-items:center;justify-content:center;margin-bottom:18px;">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#0c7a3c" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20 6 9 17 4 12"/>
        </svg>
      </div>
      <p style="font-size:18px;font-weight:600;color:#0E1116;margin:0 0 6px;">Alert sent</p>
      <p style="font-size:13px;color:rgba(15,17,22,0.60);margin:0 0 24px;line-height:1.5;">
        <span id="nws-page-success-count">0</span> subscribers notified via SMS and email.
      </p>
      <a href="/" style="display:inline-block;padding:10px 24px;background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.75);text-decoration:none;border-radius:8px;font-size:13px;">
        Back to WeatherValet
      </a>
    </div>

    <!-- Dismissed state -->
    <div id="nws-page-dismissed" hidden style="text-align:center;padding:40px 0;">
      <div style="width:56px;height:56px;background:rgba(15,17,22,0.06);border-radius:50%;display:inline-flex;align-items:center;justify-content:center;margin-bottom:18px;">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="rgba(15,17,22,0.55)" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </div>
      <p style="font-size:18px;font-weight:600;color:#0E1116;margin:0 0 6px;">Dismissed</p>
      <p style="font-size:13px;color:rgba(15,17,22,0.60);margin:0 0 24px;line-height:1.5;">
        Subscribers were not notified.
      </p>
      <a href="/" style="display:inline-block;padding:10px 24px;background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.75);text-decoration:none;border-radius:8px;font-size:13px;">
        Back to WeatherValet
      </a>
    </div>

  </div>
</div>

<style>
  @keyframes nwsPulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.4); opacity: 0.6; }
  }
</style>


<!-- ══════════════════════════════════════════════════════════════
     Customer review + tip page (Phase 10 — Met tips, TIP-B)
     Shown when URL has ?review=<token>. Customer arrives via SMS link
     after their $19 review delivers. Shows verdict + tip buttons.
     ══════════════════════════════════════════════════════════════ -->
<div id="screen-customer-review" class="screen">
  <div style="max-width: 640px; margin: 0 auto; padding: 32px 20px 80px;">

    <!-- Loading -->
    <div id="cust-review-loading" style="text-align:center;padding:40px 0;color:rgba(15,17,22,0.55);font-size:14px;">
      Loading your review&hellip;
    </div>

    <!-- Error (invalid/expired token) -->
    <div id="cust-review-error" hidden style="text-align:center;padding:40px 0;">
      <p style="font-size:16px;color:#0E1116;margin:0 0 8px;">We can&rsquo;t find that review.</p>
      <p style="font-size:13px;color:rgba(15,17,22,0.55);margin:0;">
        The link may have expired or been mistyped.
      </p>
    </div>

    <!-- Main review state -->
    <div id="cust-review-main" hidden>

      <!-- Header strip -->
      <div style="display:flex;align-items:center;gap:10px;background:rgba(15,17,22,0.03);padding:12px 16px;border-radius:10px;margin-bottom:22px;">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="rgba(15,17,22,0.55)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/>
        </svg>
        <div style="font-size:11.5px;color:rgba(15,17,22,0.65);">Your meteorologist&rsquo;s review is ready</div>
      </div>

      <!-- Plan summary -->
      <h1 style="font-size:22px;font-weight:600;color:#0E1116;margin:0 0 4px;line-height:1.3;" id="cust-review-plan">&mdash;</h1>
      <p style="font-size:13px;color:rgba(15,17,22,0.55);margin:0 0 24px;" id="cust-review-meta">&mdash;</p>

      <!-- Verdict block -->
      <div style="background:#fff;border:1px solid rgba(15,17,22,0.10);border-radius:12px;padding:20px 22px;margin-bottom:24px;">
        <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:rgba(15,17,22,0.55);margin-bottom:10px;">The call</div>
        <p style="font-size:15px;color:#0E1116;line-height:1.6;margin:0;white-space:pre-wrap;" id="cust-review-verdict">&mdash;</p>
        <div id="cust-review-notes-wrap" hidden style="margin-top:18px;padding-top:16px;border-top:1px solid rgba(15,17,22,0.08);">
          <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:rgba(15,17,22,0.55);margin-bottom:8px;">Why</div>
          <p style="font-size:13.5px;color:rgba(15,17,22,0.75);line-height:1.6;margin:0;white-space:pre-wrap;" id="cust-review-notes">&mdash;</p>
        </div>
        <div style="margin-top:18px;padding-top:14px;border-top:1px solid rgba(15,17,22,0.08);font-size:12px;color:rgba(15,17,22,0.55);">
          Reviewed by <strong style="color:#0E1116;font-weight:600;" id="cust-review-met-name">your meteorologist</strong>
          <span id="cust-review-completed-at"></span>
        </div>
      </div>

      <!-- Tip section -->
      <div id="cust-tip-section" hidden style="background:linear-gradient(135deg,rgba(46,79,184,0.04) 0%,rgba(46,79,184,0.08) 100%);border:1px solid rgba(46,79,184,0.15);border-radius:12px;padding:24px;margin-bottom:24px;">
        <div style="display:flex;align-items:flex-start;gap:14px;margin-bottom:18px;">
          <div style="flex-shrink:0;width:40px;height:40px;background:rgba(46,79,184,0.12);border-radius:50%;display:flex;align-items:center;justify-content:center;">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#2E4FB8" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 9V5a3 3 0 0 0-3-3l-4 9v11h11.28a2 2 0 0 0 2-1.7l1.38-9a2 2 0 0 0-2-2.3zM7 22H4a2 2 0 0 1-2-2v-7a2 2 0 0 1 2-2h3"/>
            </svg>
          </div>
          <div>
            <h3 style="font-size:15px;font-weight:600;color:#0E1116;margin:0 0 4px;">Thank <span id="cust-tip-met-name">your meteorologist</span></h3>
            <p style="font-size:13px;color:rgba(15,17,22,0.65);margin:0;line-height:1.5;">
              A quick thank-you helps the team keep showing up. 100% goes to them.
            </p>
          </div>
        </div>
        <div style="display:grid;grid-template-columns:repeat(4, 1fr);gap:8px;margin-bottom:8px;" id="cust-tip-amounts">
          <button type="button" class="cust-tip-amount-btn" data-tip-amount="300">$3</button>
          <button type="button" class="cust-tip-amount-btn" data-tip-amount="500">$5</button>
          <button type="button" class="cust-tip-amount-btn" data-tip-amount="1000">$10</button>
          <button type="button" class="cust-tip-amount-btn" data-tip-amount="2500">$25</button>
        </div>
        <button type="button" id="cust-tip-custom-toggle"
                style="background:transparent;border:none;color:#2E4FB8;font-size:12px;cursor:pointer;padding:6px 0;text-decoration:underline;">
          Custom amount
        </button>
        <div id="cust-tip-custom-wrap" hidden style="margin-top:8px;display:flex;gap:8px;align-items:center;">
          <span style="font-size:18px;color:#0E1116;font-weight:600;">$</span>
          <input type="number" id="cust-tip-custom-input" min="1" max="100" step="1" placeholder="10"
                 style="flex:1;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:15px;font-family:inherit;">
          <button type="button" id="cust-tip-custom-submit"
                  style="background:#2E4FB8;color:#fff;border:none;padding:10px 18px;border-radius:8px;font-size:13px;font-weight:600;cursor:pointer;">
            Send
          </button>
        </div>
      </div>

      <!-- Footer -->
      <p style="text-align:center;font-size:11.5px;color:rgba(15,17,22,0.50);margin:24px 0 0;line-height:1.5;">
        Want more? <a href="/" style="color:#2E4FB8;text-decoration:none;">Try a subscription plan</a> for daily briefs and direct messaging.
      </p>
    </div>

    <!-- Processing tip state -->
    <div id="cust-tip-processing" hidden style="text-align:center;padding:40px 0;">
      <p style="font-size:14px;color:rgba(15,17,22,0.65);margin:0;">Processing your thank-you&hellip;</p>
    </div>

    <!-- Thank-you state (post-tip success) -->
    <div id="cust-tip-thanks" hidden style="text-align:center;padding:40px 0;">
      <div style="width:64px;height:64px;background:rgba(18,161,80,0.12);border-radius:50%;display:inline-flex;align-items:center;justify-content:center;margin-bottom:18px;">
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#0c7a3c" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20 6 9 17 4 12"/>
        </svg>
      </div>
      <h2 style="font-size:22px;font-weight:600;color:#0E1116;margin:0 0 6px;">Thank you</h2>
      <p style="font-size:13.5px;color:rgba(15,17,22,0.60);margin:0 0 24px;line-height:1.55;max-width:380px;margin-left:auto;margin-right:auto;">
        <span id="cust-tip-thanks-met-name">Your meteorologist</span> will see your tip in their next paycheck. Means a lot.
      </p>
      <button type="button" id="cust-tip-back-to-review"
              style="background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.75);padding:10px 24px;border-radius:8px;font-size:13px;cursor:pointer;">
        Back to your review
      </button>
    </div>

  </div>
</div>

<style>
  .cust-tip-amount-btn {
    padding: 14px 0;
    background: #fff;
    border: 1.5px solid rgba(46,79,184,0.20);
    border-radius: 10px;
    cursor: pointer;
    font-size: 16px;
    font-weight: 600;
    color: #2E4FB8;
    transition: all 0.15s ease;
  }
  .cust-tip-amount-btn:hover {
    background: rgba(46,79,184,0.06);
    border-color: rgba(46,79,184,0.40);
  }
  .cust-tip-amount-btn:active {
    transform: scale(0.97);
  }
  .cust-tip-amount-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }
</style>


<!-- ══════════════════════════════════════════════════════════════
     Legal pages — Privacy + Terms (Phase 10 marketing — MARK-A)
     Triggered via /?legal=privacy or /?legal=terms. Standard
     SaaS-style policies; have a lawyer review before scaling.
     ══════════════════════════════════════════════════════════════ -->
<style>
  .legal-screen {
    max-width: 760px;
    margin: 0 auto;
    padding: 40px 24px 80px;
    color: #0E1116;
    line-height: 1.65;
    font-size: 15px;
  }
  .legal-screen h1 {
    font-size: 32px;
    font-weight: 700;
    margin: 0 0 8px;
    letter-spacing: -0.02em;
  }
  .legal-screen .legal-effective {
    font-size: 12px;
    color: rgba(15,17,22,0.55);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-weight: 600;
    margin: 0 0 32px;
  }
  .legal-screen h2 {
    font-size: 18px;
    font-weight: 600;
    margin: 36px 0 12px;
    color: #0E1116;
  }
  .legal-screen p {
    margin: 0 0 14px;
    color: rgba(15,17,22,0.80);
  }
  .legal-screen ul {
    margin: 0 0 14px;
    padding-left: 22px;
    color: rgba(15,17,22,0.80);
  }
  .legal-screen ul li {
    margin-bottom: 6px;
  }
  .legal-screen .legal-back {
    display: inline-block;
    margin-bottom: 24px;
    padding: 6px 14px;
    background: transparent;
    border: 1px solid rgba(15,17,22,0.15);
    color: rgba(15,17,22,0.65);
    text-decoration: none;
    border-radius: 6px;
    font-size: 12.5px;
  }
  .legal-screen .legal-back:hover {
    background: rgba(15,17,22,0.04);
    color: #0E1116;
  }
  .legal-screen strong { color: #0E1116; }
  .legal-screen a { color: #2E4FB8; }

  /* Site footer (MARK-A) — bottom of welcome screen */
  .site-footer {
    border-top: 1px solid rgba(15,17,22,0.08);
    background: rgba(244,245,247,0.6);
    padding: 32px 24px;
    margin-top: 80px;
  }
  .site-footer-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 16px;
  }
  .site-footer-brand {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    font-weight: 600;
    color: #0E1116;
    letter-spacing: -0.01em;
  }
  .site-footer-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #2E4FB8;
  }
  .site-footer-links {
    display: flex;
    gap: 20px;
  }
  .site-footer-links a {
    color: rgba(15,17,22,0.65);
    text-decoration: none;
    font-size: 13px;
  }
  .site-footer-links a:hover {
    color: #2E4FB8;
  }
  .site-footer-meta {
    font-size: 12px;
    color: rgba(15,17,22,0.50);
  }
  @media (max-width: 600px) {
    .site-footer-inner {
      flex-direction: column;
      gap: 12px;
      text-align: center;
    }
  }
  /* Hide the global footer on immersive / transient screens
     (handoff, mission response, NWS page, customer review).
     Toggled by JS in showScreen() via body.is-footer-hidden. */
  body.is-footer-hidden .site-footer { display: none; }
</style>

<div id="screen-legal-privacy" class="screen">
  <div class="legal-screen">
    <a href="/" class="legal-back">&larr; Back to WeatherValet</a>
    <h1>Privacy Policy</h1>
    <p class="legal-effective">Effective May 14, 2026</p>

    <p>
      WeatherValet (&ldquo;we,&rdquo; &ldquo;us&rdquo;) provides decision-grade weather verification
      services. This policy explains what we collect, why, and what choices you have.
      We wrote it in plain English. If anything is unclear, email
      <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a> and a person
      will answer.
    </p>

    <h2>What we collect</h2>
    <ul>
      <li><strong>Account information</strong> &mdash; your email, name, and (if you choose) phone number.</li>
      <li><strong>Locations you save</strong> &mdash; addresses and coordinates of places you want forecasts for.</li>
      <li><strong>Payment information</strong> &mdash; processed entirely by <a href="https://stripe.com">Stripe</a>. We don&rsquo;t store credit card numbers on our servers. We retain a Stripe customer ID linking you to your billing.</li>
      <li><strong>Service usage</strong> &mdash; which features you use, when you sign in, and which briefs you receive. Used for product improvement and accountability.</li>
      <li><strong>Crew location</strong> &mdash; if you&rsquo;re a Valet Crew member, the static &ldquo;home base&rdquo; you set so we can route mission notifications to you.</li>
      <li><strong>Communications</strong> &mdash; messages you send via Pro Threads or replies to mission SMS. We keep these so meteorologists can reference your conversation history.</li>
    </ul>

    <h2>How we use it</h2>
    <ul>
      <li>To deliver the service you signed up for (forecasts, briefs, verifications, mission coordination).</li>
      <li>To notify you when severe weather threatens a saved location.</li>
      <li>To process your payments and provide receipts.</li>
      <li>To improve our forecast accuracy and meteorologist workflows.</li>
      <li>To respond to support requests and resolve account issues.</li>
    </ul>

    <h2>Who we share it with</h2>
    <p>
      We only share data with service providers necessary to operate WeatherValet:
    </p>
    <ul>
      <li><strong>Stripe</strong> &mdash; payment processing</li>
      <li><strong>Resend</strong> &mdash; email delivery</li>
      <li><strong>Twilio</strong> &mdash; SMS delivery</li>
      <li><strong>Render</strong> &mdash; web hosting and database</li>
      <li><strong>National Weather Service (NWS)</strong> &mdash; weather data source (public API; no personal data flows to NWS)</li>
    </ul>
    <p>
      We do not sell your personal information. We do not share it for advertising
      purposes. If law enforcement requests data with a valid legal order, we will
      comply as required by law, but we will challenge overly broad requests where
      we can.
    </p>

    <h2>Your rights</h2>
    <ul>
      <li><strong>See your data.</strong> Email us at <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a> and we&rsquo;ll send you a copy of everything we have linked to your account.</li>
      <li><strong>Correct your data.</strong> Most fields are editable in your subscriber portal. For others, email us.</li>
      <li><strong>Delete your account.</strong> Email us with the subject &ldquo;Delete my account.&rdquo; We will deactivate immediately and purge personal data within 30 days, except where retention is required by law (e.g., financial records for tax purposes).</li>
      <li><strong>Opt out of marketing.</strong> All marketing emails include an unsubscribe link. Transactional emails (account verification, payment receipts) cannot be opted out of while your account is active.</li>
    </ul>

    <h2>Cookies and tracking</h2>
    <p>
      We use a session cookie to keep you signed in. We do not use third-party
      tracking cookies. We do not run advertising trackers. We do not embed
      social media trackers.
    </p>

    <h2>Children</h2>
    <p>
      WeatherValet is not directed at children under 13. We do not knowingly
      collect personal information from children under 13. If you believe a
      child has provided us information, email us and we will delete it.
    </p>

    <h2>Changes to this policy</h2>
    <p>
      If we make material changes, we&rsquo;ll email account holders and post a
      banner on the homepage at least 14 days before the change takes effect.
      Minor edits (typo fixes, clarifications) may happen without notice but
      will be reflected in the &ldquo;Effective&rdquo; date above.
    </p>

    <h2>Contact</h2>
    <p>
      <strong>WeatherValet</strong><br>
      Indianapolis, IN<br>
      <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a>
    </p>
  </div>
</div>


<div id="screen-legal-terms" class="screen">
  <div class="legal-screen">
    <a href="/" class="legal-back">&larr; Back to WeatherValet</a>
    <h1>Terms of Service</h1>
    <p class="legal-effective">Effective May 14, 2026</p>

    <p>
      By using WeatherValet, you agree to these terms. We&rsquo;ve kept them plain
      and short. If anything seems off, email
      <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a>.
    </p>

    <h2>What WeatherValet is</h2>
    <p>
      WeatherValet provides weather forecasting, daily briefs, meteorologist
      consultations, and severe weather notifications for businesses and
      individuals. We combine National Weather Service data with our own
      decision rules and human meteorologist review.
    </p>

    <h2>Your account</h2>
    <ul>
      <li>You are responsible for keeping your password secure.</li>
      <li>You agree to provide accurate contact information so we can deliver the service.</li>
      <li>One account per person or business. Don&rsquo;t share credentials.</li>
      <li>If you suspect unauthorized access, email us immediately.</li>
    </ul>

    <h2>Payment and refunds</h2>
    <ul>
      <li><strong>$19 reviews</strong> are charged at the time of purchase. If your meteorologist does not deliver the review within the stated service window, contact us within 7 days for a refund.</li>
      <li><strong>Subscription plans</strong> (Hobbyist, Pro Single, Pro Multi, Pro Enterprise) are billed monthly. You can cancel any time via the Stripe portal in your subscriber settings. Cancellation stops future billing; no partial-month refunds are issued for the current period unless required by law.</li>
      <li><strong>Tips</strong> to meteorologists are non-refundable except in cases of clear customer error (e.g., extra zero on the amount). Email us within 24 hours.</li>
      <li>All payments are processed by Stripe. We do not store credit card numbers.</li>
    </ul>

    <h2>What we promise</h2>
    <ul>
      <li>We will make our best effort to deliver accurate forecasts and timely briefs.</li>
      <li>We will respond to support requests within 1 business day.</li>
      <li>Pro tier customers get 30-minute meteorologist response during business hours.</li>
      <li>We will protect your data according to our <a href="?legal=privacy">Privacy Policy</a>.</li>
    </ul>

    <h2>What we cannot promise</h2>
    <p>
      <strong>Weather forecasts are inherently uncertain.</strong> No service can guarantee
      what the atmosphere will do. We provide our best professional judgment, but
      you are the one making the operational decision. WeatherValet is not
      responsible for outcomes resulting from weather events &mdash; including but not
      limited to property damage, business losses, event cancellations, or
      personal injury &mdash; whether or not those events were correctly forecasted.
    </p>
    <p>
      <strong>Life-safety decisions.</strong> WeatherValet is not a replacement for official
      National Weather Service warnings or local emergency management. If the NWS
      issues a tornado warning, take shelter regardless of what our briefs say.
      We are a decision-support service, not a sole-source authority.
    </p>

    <h2>Acceptable use</h2>
    <ul>
      <li>Don&rsquo;t use WeatherValet for illegal purposes.</li>
      <li>Don&rsquo;t resell our forecasts as your own without a partnership agreement.</li>
      <li>Don&rsquo;t attempt to access other users&rsquo; data or circumvent our security.</li>
      <li>Be respectful in Pro Threads and Crew responses. Abuse will result in account suspension.</li>
    </ul>

    <h2>Termination</h2>
    <p>
      You can cancel anytime via your subscriber portal or by emailing us. We
      can terminate accounts for violation of these terms, abuse of meteorologists
      or Crew members, fraudulent payments, or extended non-payment. We will
      typically give notice and an opportunity to correct except in cases of
      serious abuse.
    </p>

    <h2>Limitation of liability</h2>
    <p>
      To the maximum extent allowed by law, WeatherValet&rsquo;s liability for any
      claim related to the service is limited to the amount you paid us in the
      12 months prior to the claim. We are not liable for indirect, consequential,
      or incidental damages. Some jurisdictions don&rsquo;t allow these limits; in
      those jurisdictions, the limit is the smallest amount allowed by law.
    </p>

    <h2>Governing law</h2>
    <p>
      These terms are governed by the laws of the State of Indiana, United
      States. Any disputes will be resolved in the state or federal courts
      located in Marion County, Indiana, unless you have a right under
      consumer protection law to litigate in your own jurisdiction.
    </p>

    <h2>Changes to these terms</h2>
    <p>
      If we make material changes, we&rsquo;ll email account holders at least 14
      days before the change takes effect. Continued use after that constitutes
      acceptance.
    </p>

    <h2>Contact</h2>
    <p>
      <strong>WeatherValet</strong><br>
      Indianapolis, IN<br>
      <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a>
    </p>
  </div>
</div>


<!-- ══════════════════════════════════════════════════════════════
     FAQ page (MARK-B) — conversion-focused Q&A.
     Triggered via /?page=faq. Each Q is collapsible — answer hidden
     until clicked, so the page scans at a glance.
     ══════════════════════════════════════════════════════════════ -->
<div id="screen-faq" class="screen">
  <div class="legal-screen">
    <a href="/" class="legal-back">&larr; Back to WeatherValet</a>
    <h1>Questions, answered</h1>
    <p class="legal-effective">If you don&rsquo;t see your question, email us</p>

    <p style="font-size:16px;margin-bottom:32px;">
      The most common things people ask before signing up. Tap any question to
      expand the answer.
    </p>

    <details class="faq-item">
      <summary>What does WeatherValet actually do?</summary>
      <div class="faq-answer">
        <p>
          We turn weather forecasts into decisions you can act on. Instead of
          telling you &ldquo;40% chance of rain,&rdquo; we tell you whether your concrete
          pour is safe to start at 7 AM, or whether your outdoor wedding at 4 PM
          should move under cover.
        </p>
        <p>
          Three ways to use us:
        </p>
        <ul>
          <li><strong>$19 one-time review</strong> &mdash; for a specific event or window. A real meteorologist reviews the AI&rsquo;s call and confirms or corrects it within 30 minutes.</li>
          <li><strong>Hobbyist subscription ($30/month)</strong> &mdash; daily AI brief tailored to one location, threshold alerts.</li>
          <li><strong>Pro subscription ($400+/month)</strong> &mdash; meteorologist-reviewed daily briefs, direct messaging with the Met team, severe weather confirmation paging.</li>
        </ul>
      </div>
    </details>

    <details class="faq-item">
      <summary>How is this different from a free weather app?</summary>
      <div class="faq-answer">
        <p>
          Free apps tell you what the weather might be. We tell you what to do
          about it.
        </p>
        <p>
          Beyond that:
        </p>
        <ul>
          <li><strong>Decision rules.</strong> We don&rsquo;t just show data &mdash; we apply industry-specific thresholds (e.g., concrete shouldn&rsquo;t be poured below 40&deg;F).</li>
          <li><strong>Human review.</strong> For paid tiers, a credentialed meteorologist looks at the AI&rsquo;s output before it goes to you.</li>
          <li><strong>Ground truth.</strong> Our Valet Crew sends real-time observations from where weather actually is, which we feed into severe-weather decisions.</li>
          <li><strong>Direct line.</strong> Pro subscribers can text our meteorologist team and get a response within 30 minutes during business hours.</li>
        </ul>
      </div>
    </details>

    <details class="faq-item">
      <summary>Who are your meteorologists?</summary>
      <div class="faq-answer">
        <p>
          Our launch team is led by working broadcast meteorologists with degrees
          in atmospheric science and years of on-air forecasting experience. We&rsquo;ll
          name names on our <a href="?page=about">About page</a> as the team grows.
        </p>
        <p>
          Every paid review is signed by the meteorologist who delivered it. You
          know who&rsquo;s on the other end.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>What&rsquo;s the difference between the subscription tiers?</summary>
      <div class="faq-answer">
        <p>Each tier builds on the one below it.</p>
        <ul>
          <li><strong>Hobbyist ($30/month).</strong> AI-generated daily brief for one saved location, delivered by SMS and email in your chosen morning window. Threshold alerts (wind, temperature, rain probability). Live homepage AI brief.</li>
          <li><strong>Pro Single ($400/month).</strong> Everything in Hobbyist, but every daily brief is reviewed and signed by a meteorologist before it&rsquo;s sent. Direct messaging with the Met team (30-min response in business hours). Severe weather alerts are confirmed by a meteorologist before going out.</li>
          <li><strong>Pro Multi ($1,200/month).</strong> Pro Single, but for up to 5 business locations.</li>
          <li><strong>Pro Enterprise (custom quote).</strong> Multi-location, dedicated meteorologist assignment, custom integrations. <a href="mailto:hello@weathervalet.ai">Contact us</a>.</li>
        </ul>
      </div>
    </details>

    <details class="faq-item">
      <summary>How does the $19 review work?</summary>
      <div class="faq-answer">
        <p>
          You describe your event (what, where, when), we charge $19, an AI runs
          its first pass, and a meteorologist reviews and delivers the final call
          within 30 minutes during business hours. You get a text message with
          the verdict and can tap a link to read the full reasoning.
        </p>
        <p>
          If we miss the 30-minute window, contact us within 7 days and we&rsquo;ll
          refund the charge in full.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>What happens during severe weather?</summary>
      <div class="faq-answer">
        <p>
          For Pro subscribers, when the National Weather Service issues a severe
          alert (Tornado Warning, Severe Thunderstorm Warning, Flash Flood) that
          intersects your saved location, our on-duty meteorologist gets paged.
          They review the alert and either confirm it to you with custom guidance
          for your operation, or dismiss it if they judge it&rsquo;s a false alarm or
          outside your real risk area.
        </p>
        <p>
          Important: WeatherValet is not a replacement for official NWS warnings
          or local emergency management. If the NWS issues a tornado warning,
          take shelter regardless of what we say. We&rsquo;re a decision-support
          layer on top of official sources, not a substitute for them.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>Can I cancel?</summary>
      <div class="faq-answer">
        <p>
          Yes, anytime. Subscriptions are managed via Stripe through your
          subscriber portal. Cancellation stops future billing immediately;
          you keep access for the current billing period.
        </p>
        <p>
          No partial-month refunds for subscriptions. For one-time $19 reviews,
          see our refund policy in <a href="?legal=terms">Terms of Service</a>.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>What about my data?</summary>
      <div class="faq-answer">
        <p>
          We collect what we need to deliver the service: your email, name,
          phone (if you give it), and saved locations. Payments are handled
          entirely by Stripe &mdash; we never see your card number.
        </p>
        <p>
          We don&rsquo;t sell your data, we don&rsquo;t share it for advertising, and we
          don&rsquo;t use third-party trackers on our site. Full details in our
          <a href="?legal=privacy">Privacy Policy</a>.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>What is the Valet Crew?</summary>
      <div class="faq-answer">
        <p>
          Volunteer weather observers across our coverage area who send
          ground-truth reports during active weather events. When a storm is
          rolling through and our models say one thing but our Crew on the
          ground says another, we trust the people who can see out the window.
        </p>
        <p>
          Crew members get recognition (their observations cited by name in our
          reviews) but not direct monetary compensation. Tips from customers go
          to meteorologists, not Crew &mdash; we wanted to keep Crew participation
          free of financial pressure.
        </p>
        <p>
          Interested in joining? <a href="mailto:hello@weathervalet.ai">Email us</a>.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>Where do you operate?</summary>
      <div class="faq-answer">
        <p>
          We&rsquo;re based in Indianapolis, IN, and our launch coverage focuses on
          Indiana and surrounding states. The $19 review and subscription products
          work anywhere in the continental US &mdash; we pull from National Weather
          Service data, which covers the whole country. Our Valet Crew ground-
          truth network is densest in the Midwest at launch.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>What if I need a quick answer between briefs?</summary>
      <div class="faq-answer">
        <p>
          Pro subscribers can message the Met team directly from their portal &mdash;
          we call it Pro Threads. Average response is well under 30 minutes
          during business hours.
        </p>
        <p>
          Hobbyist and one-time customers don&rsquo;t get direct messaging, but you
          can always request another $19 review for any new question.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>How do you decide whether to tell me to cancel an event?</summary>
      <div class="faq-answer">
        <p>
          We don&rsquo;t. We give you the call &mdash; clear, caution, or risk &mdash; and the
          reasoning behind it. The cancellation decision is yours. We&rsquo;re a
          decision-support service, not a decision-maker.
        </p>
        <p>
          That said: when we say &ldquo;risk,&rdquo; we mean it. We don&rsquo;t use the word
          loosely. If your meteorologist tells you to move your wedding under
          cover, take that seriously.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>I&rsquo;m a meteorologist. Can I join?</summary>
      <div class="faq-answer">
        <p>
          We&rsquo;re hiring credentialed meteorologists as we grow. If you have a
          degree in atmospheric science (or equivalent operational experience),
          email us at <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a>
          with your resume and forecasting samples.
        </p>
      </div>
    </details>

    <details class="faq-item">
      <summary>Are you working with radio stations?</summary>
      <div class="faq-answer">
        <p>
          Yes &mdash; several broadcast meteorologists in our network produce
          regular radio weather hits that go to their affiliated stations. If
          you&rsquo;re a station programmer interested in a custom feed,
          <a href="mailto:hello@weathervalet.ai">contact us</a>.
        </p>
      </div>
    </details>

    <p style="margin-top:48px;padding-top:24px;border-top:1px solid rgba(15,17,22,0.10);">
      Still have questions? Email
      <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a>.
      A real person will read it.
    </p>
  </div>
</div>


<!-- ══════════════════════════════════════════════════════════════
     About page (MARK-B) — trust-builder and origin story.
     Triggered via /?page=about.
     ══════════════════════════════════════════════════════════════ -->
<div id="screen-about" class="screen">
  <div class="legal-screen">
    <a href="/" class="legal-back">&larr; Back to WeatherValet</a>
    <h1>About WeatherValet</h1>
    <p class="legal-effective">Decision-grade weather, by people who do this for a living</p>

    <p style="font-size:17px;margin-bottom:28px;line-height:1.6;">
      Weather apps got bigger. They didn&rsquo;t get better at telling you what to do.
    </p>

    <p>
      You can pull out your phone right now and find ten apps that&rsquo;ll show you
      a radar map and a 10-day forecast. None of them will tell you whether to
      pour concrete at 7 AM. None of them will text you when the storm cell
      that&rsquo;s sliding toward your wedding venue weakens. None of them have a
      meteorologist on the other end of a message.
    </p>

    <p>
      We built WeatherValet because the people we know who actually have to make
      weather-driven decisions &mdash; concrete crews, event planners, public works
      directors, farmers, roofers, broadcast meteorologists &mdash; deserve better
      than &ldquo;40% chance of rain.&rdquo;
    </p>

    <h2>What we&rsquo;re doing differently</h2>

    <p>
      <strong>We start with the decision.</strong> Every WeatherValet output is
      framed around what you&rsquo;re trying to accomplish, not what the atmosphere
      is doing in the abstract. &ldquo;Your 4 PM ceremony has a 15-minute window of
      heavy rain at 4:23 PM &mdash; consider starting at 4:30 or moving under the
      tent&rdquo; is more useful than &ldquo;PoP 60%.&rdquo;
    </p>

    <p>
      <strong>We pair AI with human judgment.</strong> The AI runs first &mdash; it&rsquo;s
      fast, it&rsquo;s consistent, and it never has an off day. Then a meteorologist
      reviews the call before it goes to you on our paid tiers. The AI catches
      patterns; the meteorologist catches context.
    </p>

    <p>
      <strong>We use the people who can see out the window.</strong> Our Valet
      Crew sends ground-truth observations during active weather. When the
      models say one thing and the radar shows another, the Crew is who we
      trust.
    </p>

    <p>
      <strong>We&rsquo;re a small, accountable team.</strong> Every paid review is
      signed by the meteorologist who delivered it. If something&rsquo;s wrong, you
      know who to talk to. There&rsquo;s no faceless brand &mdash; just people who care
      about getting this right.
    </p>

    <h2>Who we are</h2>

    <p>
      WeatherValet is built and operated by Michael Reynolds in Indianapolis,
      Indiana. The launch meteorologist team is led by working broadcast
      meteorologists with on-air forecasting experience across the Midwest.
      We&rsquo;ll introduce them by name on this page as the team grows.
    </p>

    <p>
      Our goal isn&rsquo;t to be the biggest weather company. It&rsquo;s to be the most
      trusted &mdash; the one people call when the decision really matters.
    </p>

    <h2>What&rsquo;s next</h2>

    <p>
      We launched in May 2026 with a small group of first customers in the
      Midwest. We&rsquo;re expanding our Crew network, adding meteorologists, and
      building out coverage. If you have ideas for what we should build next,
      tell us: <a href="mailto:hello@weathervalet.ai">hello@weathervalet.ai</a>.
    </p>

    <p style="margin-top:36px;padding-top:24px;border-top:1px solid rgba(15,17,22,0.10);font-size:14px;">
      Ready to try us? <a href="/">Get a forecast</a> or
      <a href="?legal=terms" style="color:rgba(15,17,22,0.50);">read the fine print</a>.
    </p>
  </div>
</div>

<style>
  /* FAQ collapsible items */
  .faq-item {
    border-top: 1px solid rgba(15,17,22,0.10);
    padding: 18px 0;
  }
  .faq-item:last-of-type {
    border-bottom: 1px solid rgba(15,17,22,0.10);
  }
  .faq-item summary {
    cursor: pointer;
    font-size: 16px;
    font-weight: 500;
    color: #0E1116;
    list-style: none;
    position: relative;
    padding-right: 32px;
    line-height: 1.5;
  }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-item summary::after {
    content: '+';
    position: absolute;
    right: 0;
    top: -2px;
    font-size: 22px;
    color: rgba(15,17,22,0.50);
    font-weight: 400;
    line-height: 1;
    transition: transform 0.15s ease;
  }
  .faq-item[open] summary::after {
    content: '\2212';
    transform: rotate(0deg);
  }
  .faq-item summary:hover {
    color: #2E4FB8;
  }
  .faq-answer {
    padding: 16px 0 4px;
    font-size: 14.5px;
    line-height: 1.65;
    color: rgba(15,17,22,0.75);
  }
  .faq-answer p {
    margin: 0 0 12px;
  }
  .faq-answer p:last-child { margin-bottom: 0; }
  .faq-answer ul {
    margin: 0 0 12px;
    padding-left: 22px;
  }
  .faq-answer ul li {
    margin-bottom: 6px;
  }
  .faq-answer strong {
    color: #0E1116;
    font-weight: 600;
  }
</style>


<!-- ── Met-initiated Pro Thread: subscriber picker modal ──
     Opens when Met clicks "+ New" in Messages tab. Lists Pro subscribers
     in a searchable picker. Picking one opens (or reuses) a thread. -->
<div id="met-new-thread-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="met-new-thread-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="met-new-thread-title" style="max-width: 520px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="met-new-thread-title">Start a new thread</h2>
      <button type="button" class="met-post-modal-close" id="met-new-thread-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body">
      <p style="font-size:12.5px;color:rgba(15,17,22,0.65);margin:0 0 14px;line-height:1.55;">
        Pick a Pro subscriber to message. If you&rsquo;ve messaged them before, you&rsquo;ll continue the existing thread.
      </p>
      <input type="text" id="met-new-thread-search" placeholder="Search by name, email, or location&hellip;"
             style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;margin-bottom:14px;">
      <div id="met-new-thread-list" style="max-height:360px;overflow-y:auto;border:1px solid rgba(15,17,22,0.08);border-radius:8px;">
        <div style="padding:30px 16px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Loading&hellip;</div>
      </div>
    </div>
  </div>
</div>


<!-- ── Create Mission Template modal (F-X2) ──
     Opens from "+ Create new mission template" in Met Missions tab.
     Routine templates auto-approve; severe ones need second-Met or admin. -->
<div id="create-template-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="create-tmpl-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="create-tmpl-title" style="max-width: 540px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="create-tmpl-title">Create mission template</h2>
      <button type="button" class="met-post-modal-close" id="create-tmpl-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body">
      <p style="font-size:12.5px;color:rgba(15,17,22,0.65);margin:0 0 18px;line-height:1.55;">
        Build a custom mission to send to your Crew on demand. Routine missions
        go live immediately. Severe missions need approval from another Met
        or an admin before they can fire.
      </p>

      <div style="margin-bottom:12px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Template name *</label>
        <input type="text" id="create-tmpl-name" placeholder="e.g. Wind gust check" maxlength="60" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
      </div>

      <div style="margin-bottom:12px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Eyebrow text (optional)</label>
        <input type="text" id="create-tmpl-eyebrow" placeholder="e.g. Wind verification" maxlength="40" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
        <div style="font-size:11px;color:rgba(15,17,22,0.50);margin-top:4px;">Small text above the headline on Crew's mission card.</div>
      </div>

      <div style="margin-bottom:12px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Button headline *</label>
        <input type="text" id="create-tmpl-headline" placeholder="e.g. Are gusts above 30 mph?" maxlength="80" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
      </div>

      <div style="margin-bottom:12px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">SMS prompt *</label>
        <textarea id="create-tmpl-prompt" rows="3" maxlength="400" placeholder="The text Crew sees on their phone." style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;resize:vertical;"></textarea>
        <div style="font-size:11px;color:rgba(15,17,22,0.50);margin-top:4px;">Crew gets this as an SMS with a tap-response link. Keep it concise.</div>
      </div>

      <div style="margin-bottom:16px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Mode *</label>
        <div style="display:flex;gap:10px;">
          <label style="flex:1;display:flex;align-items:center;gap:8px;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;cursor:pointer;font-size:13.5px;">
            <input type="radio" name="create-tmpl-mode" value="routine" checked>
            <div>
              <div style="font-weight:600;color:#0E1116;">Routine</div>
              <div style="font-size:11px;color:rgba(15,17,22,0.55);">Auto-approved &middot; goes live immediately</div>
            </div>
          </label>
          <label style="flex:1;display:flex;align-items:center;gap:8px;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;cursor:pointer;font-size:13.5px;">
            <input type="radio" name="create-tmpl-mode" value="severe">
            <div>
              <div style="font-weight:600;color:#0E1116;">Severe</div>
              <div style="font-size:11px;color:rgba(15,17,22,0.55);">Needs second Met or admin approval</div>
            </div>
          </label>
        </div>
      </div>

      <p id="create-tmpl-error" hidden style="background:rgba(194,52,43,0.08);border:1px solid rgba(194,52,43,0.20);border-radius:8px;padding:10px 12px;font-size:12.5px;color:#9b2823;margin:0 0 14px;"></p>

      <div style="display:flex;justify-content:flex-end;gap:10px;">
        <button type="button" id="create-tmpl-cancel"
                style="padding:11px 18px;background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.65);border-radius:8px;font-size:13px;cursor:pointer;">Cancel</button>
        <button type="button" id="create-tmpl-save"
                style="padding:11px 22px;background:#2E4FB8;border:none;color:#fff;border-radius:8px;font-size:13px;font-weight:600;cursor:pointer;">Create template</button>
      </div>
    </div>
  </div>
</div>


<!-- ── Met scheduling: schedule-for-later popup (Phase 10) ──
     Opens from "Schedule" button on Daily Brief / Pro Brief composers
     and from the Crew post send modal. Lets the Met pick date, hour,
     minute, AM/PM, timezone. On save, submits to the schedule API. -->
<div id="schedule-popup" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="schedule-popup-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="schedule-popup-title" style="max-width: 460px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="schedule-popup-title">Schedule send</h2>
      <button type="button" class="met-post-modal-close" id="schedule-popup-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body">
      <p style="font-size:12.5px;color:rgba(15,17,22,0.65);margin:0 0 16px;line-height:1.55;">
        Choose when this should be sent. The message will fire automatically &mdash;
        you don&rsquo;t need to be online at that time.
      </p>

      <!-- Date -->
      <div style="margin-bottom:14px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Date</label>
        <input type="date" id="schedule-date" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
      </div>

      <!-- Time row: hour, minute, AM/PM -->
      <div style="margin-bottom:14px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Time</label>
        <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;">
          <select id="schedule-hour" style="padding:10px 8px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
            <!-- 1..12 populated by JS -->
          </select>
          <select id="schedule-minute" style="padding:10px 8px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
            <!-- 0,5,10,... populated by JS -->
          </select>
          <select id="schedule-ampm" style="padding:10px 8px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
            <option value="AM">AM</option>
            <option value="PM">PM</option>
          </select>
        </div>
      </div>

      <!-- Timezone -->
      <div style="margin-bottom:18px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Timezone</label>
        <select id="schedule-tz" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
          <option value="America/New_York">Eastern Time</option>
          <option value="America/Chicago">Central Time</option>
          <option value="America/Denver">Mountain Time</option>
          <option value="America/Los_Angeles">Pacific Time</option>
        </select>
      </div>

      <!-- Preview -->
      <div id="schedule-preview" style="background:rgba(46,79,184,0.05);border:1px solid rgba(46,79,184,0.15);border-radius:8px;padding:12px 14px;margin-bottom:18px;font-size:13px;color:rgba(15,17,22,0.75);line-height:1.5;">
        <span id="schedule-preview-text">Pick a date and time above.</span>
      </div>

      <!-- Pro brief staleness warning (F-10) -->
      <div id="schedule-stale-warning" hidden style="background:rgba(245,165,36,0.10);border:1px solid rgba(245,165,36,0.30);border-radius:8px;padding:12px 14px;margin-bottom:18px;font-size:12.5px;color:#7a5710;line-height:1.55;">
        <strong style="color:#5a3f00;">Heads up:</strong>
        Pro briefs are based on current weather data. Scheduling more than
        ~6 hours out means the forecast may be stale by the time it sends &mdash;
        review and update the body if conditions change before then.
      </div>

      <!-- Error -->
      <p id="schedule-error" hidden style="background:rgba(194,52,43,0.08);border:1px solid rgba(194,52,43,0.20);border-radius:8px;padding:10px 12px;font-size:12.5px;color:#9b2823;margin:0 0 14px;"></p>

      <div style="display:flex;justify-content:flex-end;gap:10px;">
        <button type="button" id="schedule-popup-cancel"
                style="padding:11px 18px;background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.65);border-radius:8px;font-size:13px;cursor:pointer;">Cancel</button>
        <button type="button" id="schedule-popup-save"
                style="padding:11px 22px;background:#2E4FB8;border:none;color:#fff;border-radius:8px;font-size:13px;font-weight:600;cursor:pointer;">Schedule Send</button>
      </div>
    </div>
  </div>
</div>


<!-- ── Starter Month landing page ──
     Sales-team route: weathervalet.ai/starter?rep=brian
     One-month Pro Single at $99, rolls over to $400/mo.
     The ?rep= parameter is captured by JS and passed to Stripe Checkout
     as metadata.wv_rep so the rep gets commission credit.       -->
<div id="screen-starter" class="screen">
  <div style="max-width: 680px; margin: 0 auto; padding: 60px 24px 100px;">
    <a href="/" class="legal-back" style="display: inline-block; margin-bottom: 28px; color: #2E4FB8; text-decoration: none; font-size: 13.5px;">&larr; weathervalet.ai</a>

    <div style="background: #fff; border: 1px solid rgba(15,17,22,0.08); border-radius: 16px; padding: 40px 36px; box-shadow: 0 4px 16px rgba(15,17,22,0.04);">
      <div style="font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.10em; color: #2E4FB8; margin-bottom: 14px;">
        WeatherValet Starter Month
      </div>
      <h1 style="font-size: 34px; line-height: 1.15; font-weight: 700; color: #0E1116; margin: 0 0 12px;">
        Your first month of WeatherValet &mdash; $99
      </h1>
      <p style="font-size: 16px; line-height: 1.55; color: rgba(15,17,22,0.70); margin: 0 0 28px;">
        Daily forecasts signed by a real meteorologist. Severe weather confirmed
        by a human. Full Pro Single service for 30 days, then $400/month.
        Cancel anytime.
      </p>

      <div style="background: rgba(46,79,184,0.05); border-left: 3px solid #2E4FB8; padding: 12px 16px; margin-bottom: 28px; font-size: 13.5px; color: rgba(15,17,22,0.75); font-style: italic;">
        Used by concrete and roofing crews who can&rsquo;t afford to be wrong about the weather.
      </div>

      <div style="margin: 0 0 28px;">
        <div style="font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: rgba(15,17,22,0.55); margin-bottom: 14px;">What you get this month</div>
        <div style="display: flex; align-items: flex-start; gap: 14px; padding: 14px 0; border-bottom: 1px solid rgba(15,17,22,0.06);">
          <div style="width: 28px; height: 28px; border-radius: 50%; background: rgba(46,79,184,0.10); color: #2E4FB8; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
            <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
          <div>
            <div style="font-size: 14.5px; font-weight: 600; color: #0E1116; margin-bottom: 2px;">Daily Met-reviewed brief</div>
            <div style="font-size: 13px; color: rgba(15,17,22,0.65); line-height: 1.5;">A meteorologist reads the AI draft, edits it, and signs it. Delivered to your phone and email every morning.</div>
          </div>
        </div>
        <div style="display: flex; align-items: flex-start; gap: 14px; padding: 14px 0; border-bottom: 1px solid rgba(15,17,22,0.06);">
          <div style="width: 28px; height: 28px; border-radius: 50%; background: rgba(46,79,184,0.10); color: #2E4FB8; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
            <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
          <div>
            <div style="font-size: 14.5px; font-weight: 600; color: #0E1116; margin-bottom: 2px;">Pro Threads &mdash; text your meteorologist</div>
            <div style="font-size: 13px; color: rgba(15,17,22,0.65); line-height: 1.5;">Direct text-message access to our Met team during business hours (6 AM &ndash; 9 PM ET). 30-minute response time.</div>
          </div>
        </div>
        <div style="display: flex; align-items: flex-start; gap: 14px; padding: 14px 0;">
          <div style="width: 28px; height: 28px; border-radius: 50%; background: rgba(46,79,184,0.10); color: #2E4FB8; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
            <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
          <div>
            <div style="font-size: 14.5px; font-weight: 600; color: #0E1116; margin-bottom: 2px;">Severe weather confirmation from a human</div>
            <div style="font-size: 13px; color: rgba(15,17,22,0.65); line-height: 1.5;">When NWS issues a Tornado, Severe Thunderstorm, or Flash Flood Warning that hits your location, you get a personalized SMS from our Met team with what to do.</div>
          </div>
        </div>
      </div>

      <button type="button" id="starter-cta-btn" style="width: 100%; padding: 16px 24px; background: #2E4FB8; color: #fff; border: none; border-radius: 10px; font-size: 17px; font-weight: 700; cursor: pointer; box-shadow: 0 4px 12px rgba(46,79,184,0.30); transition: transform 0.08s, box-shadow 0.08s;">
        Start my $99 month
      </button>
      <p style="font-size: 12px; color: rgba(15,17,22,0.55); text-align: center; margin: 14px 0 0; line-height: 1.5;">
        Renews at $400/month after day 30 unless you cancel.<br>
        Cancel anytime from your dashboard, no questions asked.
      </p>

      <p id="starter-rep-line" hidden style="font-size: 12px; color: rgba(15,17,22,0.50); text-align: center; margin: 20px 0 0; font-style: italic;">
        Referred by <span id="starter-rep-name"></span>
      </p>
    </div>
  </div>
</div>



<!-- ── Add Sales Rep modal — admin Operations tab ──
     Opens when admin clicks "+ Add Rep" on the Sales Commissions tile.
     Slug is required (URL identifier like /starter?rep=brian). -->
<div id="admin-add-rep-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="admin-add-rep-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="admin-add-rep-title" style="max-width: 460px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="admin-add-rep-title">Add a sales rep</h2>
      <button type="button" class="met-post-modal-close" id="admin-add-rep-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body">
      <p style="font-size:12.5px;color:rgba(15,17,22,0.65);margin:0 0 16px;line-height:1.55;">
        The slug is what goes in the URL: <code style="background:rgba(15,17,22,0.06);padding:2px 5px;border-radius:3px;">weathervalet.ai/starter?rep=<strong>brian</strong></code>. Use lowercase letters, numbers, underscores only.
      </p>

      <div style="margin-bottom:12px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Slug (URL identifier) *</label>
        <input type="text" id="admin-rep-slug" placeholder="brian" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
      </div>

      <div style="margin-bottom:12px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Full name *</label>
        <input type="text" id="admin-rep-name" placeholder="Brian Smith" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
      </div>

      <div style="margin-bottom:12px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Email</label>
        <input type="email" id="admin-rep-email" placeholder="brian@example.com" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
      </div>

      <div style="margin-bottom:16px;">
        <label style="display:block;font-size:11.5px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.06em;margin-bottom:6px;">Phone</label>
        <input type="tel" id="admin-rep-phone" placeholder="(555) 555-5555" style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;">
      </div>

      <p id="admin-rep-error" hidden style="background:rgba(194,52,43,0.08);border:1px solid rgba(194,52,43,0.20);border-radius:8px;padding:10px 12px;font-size:12.5px;color:#9b2823;margin:0 0 14px;"></p>

      <div style="display:flex;justify-content:flex-end;gap:10px;">
        <button type="button" id="admin-rep-cancel" style="padding:11px 18px;background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.65);border-radius:8px;font-size:13px;cursor:pointer;">Cancel</button>
        <button type="button" id="admin-rep-save" style="padding:11px 22px;background:#2E4FB8;border:none;color:#fff;border-radius:8px;font-size:13px;font-weight:600;cursor:pointer;">Add rep</button>
      </div>
    </div>
  </div>
</div>


<!-- ── Subscriber portal: Brief preferences edit modal (Phase 10 B1) ──
     Opens when the "Edit" button on the Brief delivery section is tapped.
     Form-based — no map. Submits a PATCH to /api/v1/me/brief-preferences.
     On success, the modal closes and the portal re-loads to show new values. -->
<div id="brief-prefs-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="brief-prefs-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="brief-prefs-title" style="max-width: 520px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="brief-prefs-title">Brief delivery preferences</h2>
      <button type="button" class="met-post-modal-close" id="brief-prefs-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body">

      <!-- Morning brief -->
      <div class="prefs-section">
        <label class="prefs-toggle-row">
          <input type="checkbox" id="prefs-morning-enabled" class="prefs-toggle-input">
          <span class="prefs-toggle-label">Morning brief</span>
        </label>
        <div class="prefs-time-row" id="prefs-morning-times">
          <div class="prefs-time-field">
            <label class="prefs-time-label">From</label>
            <input type="time" id="prefs-morning-start" class="prefs-time-input" value="05:30">
          </div>
          <div class="prefs-time-field">
            <label class="prefs-time-label">To</label>
            <input type="time" id="prefs-morning-end" class="prefs-time-input" value="07:00">
          </div>
        </div>
      </div>

      <!-- Evening brief -->
      <div class="prefs-section">
        <label class="prefs-toggle-row">
          <input type="checkbox" id="prefs-evening-enabled" class="prefs-toggle-input">
          <span class="prefs-toggle-label">Evening brief</span>
        </label>
        <div class="prefs-time-row" id="prefs-evening-times">
          <div class="prefs-time-field">
            <label class="prefs-time-label">From</label>
            <input type="time" id="prefs-evening-start" class="prefs-time-input" value="17:30">
          </div>
          <div class="prefs-time-field">
            <label class="prefs-time-label">To</label>
            <input type="time" id="prefs-evening-end" class="prefs-time-input" value="18:30">
          </div>
        </div>
      </div>

      <!-- Quiet hours -->
      <div class="prefs-section">
        <div class="prefs-section-label">Quiet hours <span class="prefs-section-detail">(severe weather bypasses)</span></div>
        <div class="prefs-time-row">
          <div class="prefs-time-field">
            <label class="prefs-time-label">From</label>
            <input type="time" id="prefs-quiet-start" class="prefs-time-input" value="21:00">
          </div>
          <div class="prefs-time-field">
            <label class="prefs-time-label">To</label>
            <input type="time" id="prefs-quiet-end" class="prefs-time-input" value="05:00">
          </div>
        </div>
      </div>

      <!-- Channels -->
      <div class="prefs-section">
        <div class="prefs-section-label">Notification channels</div>
        <div class="prefs-channels-row">
          <label class="prefs-channel-check">
            <input type="checkbox" id="prefs-channel-sms" value="sms">
            <span>SMS</span>
          </label>
          <label class="prefs-channel-check">
            <input type="checkbox" id="prefs-channel-email" value="email">
            <span>Email</span>
          </label>
          <label class="prefs-channel-check">
            <input type="checkbox" id="prefs-channel-push" value="push">
            <span>Push</span>
          </label>
        </div>
      </div>

      <p class="prefs-error" id="prefs-error" hidden></p>
    </div>
    <div class="met-post-modal-foot">
      <button type="button" class="met-post-modal-cancel" id="prefs-cancel">Cancel</button>
      <button type="button" class="met-post-modal-send" id="prefs-save">Save changes</button>
    </div>
  </div>
</div>


<!-- ── Subscriber portal: Threshold alert edit modal (Phase 10 B1) ──
     Used for BOTH adding a new alert and editing an existing one.
     Mode is set via data attribute on open; the modal title and the
     submit button label change accordingly. -->
<div id="threshold-alert-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="threshold-alert-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="threshold-alert-title" style="max-width: 480px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="threshold-alert-title">Add threshold alert</h2>
      <button type="button" class="met-post-modal-close" id="threshold-alert-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body">

      <!-- Metric + comparator + value all on one line for at-a-glance reading -->
      <div class="prefs-section">
        <div class="prefs-section-label">Notify me when</div>
        <div class="threshold-form-row">
          <select id="threshold-metric" class="threshold-select">
            <option value="wind">Wind</option>
            <option value="temp_high">Temperature (high)</option>
            <option value="temp_low">Temperature (low)</option>
            <option value="precip_chance">Precipitation chance</option>
            <option value="humidity">Humidity</option>
          </select>
          <select id="threshold-comparator" class="threshold-select">
            <option value="gt">exceeds</option>
            <option value="gte">reaches</option>
            <option value="lt">drops below</option>
            <option value="lte">falls to</option>
            <option value="eq">equals</option>
          </select>
          <input type="number" id="threshold-value" class="threshold-value-input" placeholder="25" step="0.1">
          <select id="threshold-units" class="threshold-select threshold-units-select">
            <option value="mph">mph</option>
            <option value="F">°F</option>
            <option value="C">°C</option>
            <option value="pct">%</option>
          </select>
        </div>
      </div>

      <!-- Channels -->
      <div class="prefs-section">
        <div class="prefs-section-label">Notify via</div>
        <div class="prefs-channels-row">
          <label class="prefs-channel-check">
            <input type="checkbox" id="threshold-channel-sms" value="sms" checked>
            <span>SMS</span>
          </label>
          <label class="prefs-channel-check">
            <input type="checkbox" id="threshold-channel-email" value="email" checked>
            <span>Email</span>
          </label>
          <label class="prefs-channel-check">
            <input type="checkbox" id="threshold-channel-push" value="push">
            <span>Push</span>
          </label>
        </div>
      </div>

      <!-- Enabled toggle (only relevant when editing) -->
      <div class="prefs-section" id="threshold-enabled-section">
        <label class="prefs-toggle-row">
          <input type="checkbox" id="threshold-enabled" class="prefs-toggle-input" checked>
          <span class="prefs-toggle-label">Active</span>
        </label>
      </div>

      <p class="prefs-error" id="threshold-error" hidden></p>
    </div>
    <div class="met-post-modal-foot">
      <button type="button" class="met-post-modal-cancel" id="threshold-cancel">Cancel</button>
      <button type="button" class="met-post-modal-delete" id="threshold-delete" hidden>Delete alert</button>
      <button type="button" class="met-post-modal-send" id="threshold-save">Add alert</button>
    </div>
  </div>
</div>


<!-- ══════════════════════════════════════════════════════════════
     Pro Brief editor modal (Phase 10 Item #3 Chunk 3B-2)
     Met clicks "Review & send" on a draft → modal opens with the
     AI body pre-loaded into editable fields. Met edits, picks a
     verdict, optionally adds internal notes, hits Send.
     ══════════════════════════════════════════════════════════════ -->
<div id="met-pro-brief-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="met-pro-brief-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="met-pro-brief-title" style="max-width: 720px;">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="met-pro-brief-title">Review &amp; send brief</h2>
      <button type="button" class="met-post-modal-close" id="met-pro-brief-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div class="met-post-modal-body" style="max-height: 70vh; overflow-y: auto;">

      <!-- Subscriber context strip -->
      <div style="background: rgba(15,17,22,0.04); padding: 12px 16px; border-radius: 8px; margin-bottom: 16px; display: flex; align-items: center; justify-content: space-between; gap: 12px;">
        <div>
          <div style="font-weight: 600; color: #0E1116; font-size: 14px;" id="met-pro-brief-subscriber-name">&mdash;</div>
          <div style="font-size: 11.5px; color: rgba(15,17,22,0.55); margin-top: 3px;" id="met-pro-brief-subscriber-meta">&mdash;</div>
        </div>
        <div style="display:flex;flex-direction:column;align-items:flex-end;gap:4px;">
          <span id="met-pro-brief-tier-badge"
                style="background:rgba(65,105,225,0.12);color:#2E4FB8;padding:3px 10px;border-radius:4px;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;">Pro Single</span>
          <span style="font-size:10px;color:rgba(15,17,22,0.45);font-family:'JetBrains Mono',monospace;" id="met-pro-brief-channels">&mdash;</span>
        </div>
      </div>

      <!-- AI draft hint — collapsible reminder of what AI suggested -->
      <details style="margin-bottom: 16px; background: rgba(15,17,22,0.03); padding: 10px 14px; border-radius: 6px;">
        <summary style="cursor: pointer; font-size: 11px; font-weight: 600; color: rgba(15,17,22,0.65); text-transform: uppercase; letter-spacing: 0.05em;">AI starting draft</summary>
        <div style="margin-top: 8px; font-size: 12.5px; color: rgba(15,17,22,0.70); line-height: 1.55;" id="met-pro-brief-ai-preview">&mdash;</div>
      </details>

      <!-- Verdict pills -->
      <div style="margin-bottom: 14px;">
        <label style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); margin-bottom: 8px;">Verdict</label>
        <div style="display: flex; gap: 8px;" id="met-pro-brief-verdict-row">
          <button type="button" class="met-pro-brief-verdict-btn" data-pro-verdict="clear"
                  style="flex:1;padding:10px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;background:#fff;cursor:pointer;font-size:13px;font-weight:500;">
            Clear
          </button>
          <button type="button" class="met-pro-brief-verdict-btn" data-pro-verdict="caution"
                  style="flex:1;padding:10px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;background:#fff;cursor:pointer;font-size:13px;font-weight:500;">
            Caution
          </button>
          <button type="button" class="met-pro-brief-verdict-btn" data-pro-verdict="risk"
                  style="flex:1;padding:10px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;background:#fff;cursor:pointer;font-size:13px;font-weight:500;">
            Risk
          </button>
        </div>
      </div>

      <!-- Snippet — for SMS preview (auto-filled from bottom_line if blank) -->
      <div style="margin-bottom: 14px;">
        <label style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); margin-bottom: 6px;">
          SMS preview <span style="color: rgba(15,17,22,0.40); font-weight: 400; text-transform: none; letter-spacing: 0;">&middot; first line in the text (auto-derived if blank)</span>
        </label>
        <input type="text" id="met-pro-brief-snippet" maxlength="280"
               style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;box-sizing:border-box;"
               placeholder="Leave blank to auto-use the start of the Bottom Line">
      </div>

      <!-- Bottom Line — 2-3 sentence prose (replaces old single body) -->
      <div style="margin-bottom: 14px;">
        <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 6px;">
          <label for="met-pro-brief-bottom-line" style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); margin: 0;">
            Bottom line <span style="color: rgba(15,17,22,0.40); font-weight: 400; text-transform: none; letter-spacing: 0;">&middot; 2-3 sentences, what they need to know</span>
          </label>
          <!-- Polish writing button -->
          <button type="button" id="met-pro-brief-polish-btn"
                  style="background: transparent; border: 1px solid rgba(46,79,184,0.30); color: #2E4FB8; padding: 5px 10px; border-radius: 6px; font-size: 11px; font-weight: 600; cursor: pointer; display: inline-flex; align-items: center; gap: 5px;"
                  title="Run a quick grammar + spelling check on the Bottom Line">
            <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 2l1.4 4.2L18 8l-4.6 1.8L12 14l-1.4-4.2L6 8l4.6-1.8L12 2z" stroke-linejoin="round"/>
              <path d="M19 14l.7 2.1L22 17l-2.3.9L19 20l-.7-2.1L16 17l2.3-.9L19 14z"/>
            </svg>
            <span>Polish writing</span>
          </button>
        </div>
        <textarea id="met-pro-brief-bottom-line" rows="3" maxlength="800"
                  style="width:100%;padding:12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;box-sizing:border-box;resize:vertical;line-height:1.55;"
                  placeholder="The bottom line for the subscriber. Specific, actionable."></textarea>

        <!-- Polish suggestions panel — hidden until "Polish writing" runs -->
        <div id="met-pro-brief-polish-panel" hidden style="margin-top: 12px; padding: 12px 14px; background: rgba(46,79,184,0.05); border: 1px solid rgba(46,79,184,0.20); border-radius: 8px;">
          <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 10px;">
            <div style="font-size: 11.5px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #2E4FB8;" id="met-pro-brief-polish-status">
              Suggestions
            </div>
            <button type="button" id="met-pro-brief-polish-close" style="background: transparent; border: 0; padding: 4px; cursor: pointer; color: rgba(15,17,22,0.55);" aria-label="Close suggestions">
              <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
          </div>
          <div id="met-pro-brief-polish-list" style="display: flex; flex-direction: column; gap: 10px;">
            <!-- Populated by JS -->
          </div>
        </div>
      </div>

      <!-- Weather Details — bullet-style metrics -->
      <div style="margin-bottom: 14px;">
        <label for="met-pro-brief-weather-details" style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); margin-bottom: 6px;">
          Weather details <span style="color: rgba(15,17,22,0.40); font-weight: 400; text-transform: none; letter-spacing: 0;">&middot; one metric per line (temp, wind, rain chance, etc.)</span>
        </label>
        <textarea id="met-pro-brief-weather-details" rows="5" maxlength="2000"
                  style="width:100%;padding:12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;box-sizing:border-box;resize:vertical;line-height:1.6;"
                  placeholder="Temp: 58° → 76°&#10;Wind: 5-12 mph SW, gusts to 18&#10;Rain chance: 65% (2-5 PM), 0% rest of day&#10;Soil moisture: 0.4&quot; past 3 days"></textarea>
      </div>

      <!-- What's Ahead — narrative about what could change -->
      <div style="margin-bottom: 14px;">
        <label for="met-pro-brief-whats-ahead" style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); margin-bottom: 6px;">
          What's ahead <span style="color: rgba(15,17,22,0.40); font-weight: 400; text-transform: none; letter-spacing: 0;">&middot; what could change, what to watch, multi-day outlook</span>
        </label>
        <textarea id="met-pro-brief-whats-ahead" rows="3" maxlength="1500"
                  style="width:100%;padding:12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:14px;font-family:inherit;box-sizing:border-box;resize:vertical;line-height:1.55;"
                  placeholder="What could shift today, multi-day outlook, things to monitor."></textarea>
      </div>

      <!-- Optional image URL (SPC outlook, radar screenshot, etc.) -->
      <div style="margin-bottom: 14px;">
        <label for="met-pro-brief-image-url" style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); margin-bottom: 6px;">
          Image URL <span style="color: rgba(15,17,22,0.40); font-weight: 400; text-transform: none; letter-spacing: 0;">&middot; optional &middot; paste a link to a graphic (SPC outlook, radar, etc.)</span>
        </label>
        <input type="url" id="met-pro-brief-image-url" maxlength="500"
               style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:13px;font-family:inherit;box-sizing:border-box;"
               placeholder="https://www.spc.noaa.gov/products/outlook/day1otlk.png">
        <div style="font-size: 10.5px; color: rgba(15,17,22,0.50); margin-top: 4px;">
          Pasted images appear in the email and web view. Image upload coming soon.
        </div>
      </div>

      <!-- Internal Met notes -->
      <div>
        <label style="display: block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); margin-bottom: 6px;">
          Internal notes <span style="color: rgba(15,17,22,0.40); font-weight: 400; text-transform: none; letter-spacing: 0;">&middot; NOT sent to subscriber</span>
        </label>
        <textarea id="met-pro-brief-notes" rows="2" maxlength="2000"
                  style="width:100%;padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:13px;font-family:inherit;box-sizing:border-box;resize:vertical;background:rgba(255,184,0,0.04);"
                  placeholder="For your future self or other Mets. Not visible to the subscriber."></textarea>
      </div>

    </div>
    <div class="met-post-modal-foot">
      <button type="button" class="met-post-modal-cancel" id="met-pro-brief-cancel">Cancel</button>
      <button type="button" class="met-post-modal-send" id="met-pro-brief-save">Save draft</button>
      <button type="button" class="met-post-modal-send" id="met-pro-brief-schedule" style="background:#fff;border:1px solid #2E4FB8;color:#2E4FB8;">Schedule</button>
      <button type="button" class="met-post-modal-send" id="met-pro-brief-send" style="background:#2E4FB8;">Send to subscriber</button>
    </div>
  </div>
</div>

<style>
  .met-pro-brief-verdict-btn:hover { background: rgba(15,17,22,0.03); }
  .met-pro-brief-verdict-btn.is-active[data-pro-verdict="clear"]   { background: rgba(18,161,80,0.12);  border-color: #0c7a3c; color: #0c7a3c; }
  .met-pro-brief-verdict-btn.is-active[data-pro-verdict="caution"] { background: rgba(255,184,0,0.12);  border-color: #b78900; color: #b78900; }
  .met-pro-brief-verdict-btn.is-active[data-pro-verdict="risk"]    { background: rgba(194,52,43,0.10);  border-color: #C2342B; color: #C2342B; }
</style>

<!-- ── Mission Deploy modal (Phase 8) ───────────────────────
     Opens from a "Deploy" button on a mission template card.
     Shows the template summary, lets the Met draw a polygon for
     audience targeting, customize the prompt text, and fire. -->
<div id="mission-deploy-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="mission-deploy-scrim"></div>
  <div class="met-post-modal-card mission-deploy-card" role="dialog" aria-modal="true" aria-labelledby="mission-deploy-title">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="mission-deploy-title">Deploy mission</h2>
      <button type="button" class="met-post-modal-close" id="mission-deploy-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 6 6 18"/><path d="m6 6 12 12"/>
        </svg>
      </button>
    </div>

    <div class="met-post-modal-body">
      <!-- Template summary card — shows what the Met is about to fire -->
      <div class="mission-deploy-summary" id="mission-deploy-summary">
        <div class="mission-deploy-summary-icon" id="mission-deploy-summary-icon">FC</div>
        <div>
          <p class="mission-deploy-summary-name" id="mission-deploy-summary-name">&mdash;</p>
          <p class="mission-deploy-summary-meta" id="mission-deploy-summary-meta">&mdash;</p>
        </div>
      </div>

      <!-- Severe mission warning — shown only for severe-mode templates -->
      <div class="mission-deploy-warning" id="mission-deploy-warning" hidden>
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
          <line x1="12" y1="9" x2="12" y2="13"/>
          <line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span>Severe-mode mission &middot; requires admin approval after submit.</span>
      </div>

      <p class="met-post-modal-eyebrow">Prompt to Crew</p>
      <p class="met-post-modal-help">
        Customize the message sent to Crew in your targeted area. Keep it
        short and specific — Crew typically respond in under a minute.
      </p>
      <textarea id="mission-deploy-prompt" class="met-message-textarea"
                placeholder="What are you seeing right now? Wind speed, visibility, any flags moving?"
                maxlength="600" rows="3"></textarea>
      <p class="met-message-count">
        <span id="mission-deploy-prompt-count">0</span> / 600 characters
      </p>

      <p class="met-post-modal-eyebrow" style="margin-top:18px">Target area</p>
      <p class="met-post-modal-help">
        Draw a polygon on the map to limit who receives this mission.
        Only Crew inside the shape will get the prompt. Leave empty to
        send to all Crew in your coverage area.
      </p>
      <div class="ws-polygon-map-wrap" style="height: 320px;">
        <div class="ws-polygon-map-loading" data-polygon-loading="mission">
          <div class="met-map-loading-dot"></div>
          <span>Loading the map&hellip;</span>
        </div>
        <div class="ws-polygon-map" id="mission-deploy-polygon-map"></div>
      </div>
      <div class="ws-polygon-map-status" id="mission-deploy-polygon-status">
        No target area drawn &middot; will send to all Crew in coverage.
      </div>
      <input type="hidden" id="mission-deploy-polygon-geojson" value="">

      <!-- Estimated audience — when polygon drawn, count Crew inside.
           For now this is a static estimate based on Crew_NEARBY fake data. -->
      <div class="mission-deploy-audience" id="mission-deploy-audience">
        <span class="mission-deploy-audience-label">Estimated audience</span>
        <span class="mission-deploy-audience-value" id="mission-deploy-audience-value">&mdash; Crew in coverage area</span>
      </div>
    </div>

    <div class="met-post-modal-actions">
      <button type="button" class="met-post-modal-cancel" id="mission-deploy-cancel">Cancel</button>
      <button type="button" class="met-post-modal-send" id="mission-deploy-fire" disabled>
        Fire mission
      </button>
    </div>
  </div>
</div>

<!-- ── Message Again composer modal (Phase 7) ─────────────
     Opens from the "Message again" button on a Met history row.
     Shows the customer's original plan for context, then lets the
     Met type a follow-up message. Send → POST /api/v1/met/messages. -->
<div id="met-message-modal" class="met-post-modal" hidden>
  <div class="met-post-modal-scrim" id="met-message-modal-scrim"></div>
  <div class="met-post-modal-card" role="dialog" aria-modal="true" aria-labelledby="met-message-modal-title">
    <div class="met-post-modal-head">
      <h2 class="met-post-modal-title" id="met-message-modal-title">Message customer again</h2>
      <button type="button" class="met-post-modal-close" id="met-message-modal-close" aria-label="Close">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 6 6 18"/><path d="m6 6 12 12"/>
        </svg>
      </button>
    </div>

    <div class="met-post-modal-body">
      <p class="met-post-modal-eyebrow">Original review</p>
      <div class="met-message-context" id="met-message-context">
        <div class="met-message-context-row">
          <span class="met-message-context-label">Customer</span>
          <span class="met-message-context-value" id="met-message-context-customer">&mdash;</span>
        </div>
        <div class="met-message-context-row">
          <span class="met-message-context-label">Plan</span>
          <span class="met-message-context-value" id="met-message-context-plan">&mdash;</span>
        </div>
        <div class="met-message-context-row">
          <span class="met-message-context-label">Delivered</span>
          <span class="met-message-context-value" id="met-message-context-when">&mdash;</span>
        </div>
      </div>

      <p class="met-post-modal-eyebrow" style="margin-top:18px">Your follow-up message</p>
      <p class="met-post-modal-help">
        Send a quick update to the customer. Common cases: storm has shifted,
        timing has changed, an earlier verdict needs to be revised.
        The customer receives this as an SMS to the phone they signed up with.
      </p>
      <textarea id="met-message-body" class="met-message-textarea"
                placeholder="Update for your plan: the storm cell has shifted east. Your window should hold dry through 4 PM. — [Your name]"
                maxlength="1000" rows="5"></textarea>
      <p class="met-message-count">
        <span id="met-message-count-num">0</span> / 1000 characters
      </p>
    </div>

    <div class="met-post-modal-actions">
      <button type="button" class="met-post-modal-cancel" id="met-message-modal-cancel">Cancel</button>
      <button type="button" class="met-post-modal-send" id="met-message-modal-send" disabled>
        Send message
      </button>
    </div>
  </div>
</div>

<!-- Tip toast — global, fixed-position; appears with a small slide+fade
     animation when a tip is given. Lives outside any screen so it can
     appear over either the detail screen or anywhere else. -->
<div id="tip-toast" class="tip-toast" role="status" aria-live="polite">
  <svg class="tip-toast-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"
       stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
    <path d="M20 6 9 17l-5-5"/>
  </svg>
  <span id="tip-toast-text">Tipped.</span>
</div>


<!-- ============ EMAIL CAPTURE MODAL ====================================
     Triggered by any element with [data-email-capture] attribute. The
     attribute value selects which variant to show: "subscriber" for the
     subscription-waitlist (default), "crew" for Crew membership waitlist.
     Modal copy adapts to the variant.

     For May 24 launch this replaces the "Subscribe" and "Join the Crew"
     CTAs since real signup flows aren't wired yet. Captures email +
     name into the backend; real subscription/Crew signup goes live in
     June/July. -->
<div id="email-capture-modal" class="email-capture-scrim" hidden>
  <div class="email-capture-modal" role="dialog" aria-modal="true" aria-labelledby="email-capture-title">
    <button type="button" class="email-capture-close" id="email-capture-close" aria-label="Close">
      <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
    <div class="email-capture-eyebrow" id="email-capture-eyebrow">
      <span class="email-capture-eyebrow-dot"></span>
      <span id="email-capture-eyebrow-text">Subscription waitlist</span>
    </div>
    <h2 class="email-capture-title" id="email-capture-title">Get notified when subscriptions open.</h2>
    <p class="email-capture-body" id="email-capture-body">
      WeatherValet subscriptions launch in June. Drop your email and we'll let you know the moment they're live &mdash; with a launch discount for early signups.
    </p>
    <form class="email-capture-form" id="email-capture-form">
      <div class="email-capture-fields">
        <input type="text" class="form-input" id="email-capture-name" name="name" placeholder="Your name" required>
        <input type="email" class="form-input" id="email-capture-email" name="email" placeholder="you@example.com" required>
      </div>
      <div class="email-capture-meta">
        <input type="text" class="form-input" id="email-capture-zip" name="zip" placeholder="ZIP code (so we know where to forecast)">
      </div>
      <button type="submit" class="email-capture-submit">
        <span id="email-capture-submit-text">Join the waitlist</span>
      </button>
      <p class="email-capture-fine">We won&rsquo;t spam you. One launch announcement, possibly a follow-up about beta access.</p>
    </form>
    <div class="email-capture-success" id="email-capture-success" hidden>
      <svg class="email-capture-success-icon" viewBox="0 0 24 24" width="48" height="48" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
        <polyline points="22 4 12 14.01 9 11.01"/>
      </svg>
      <h3 class="email-capture-success-title">You&rsquo;re on the list.</h3>
      <p class="email-capture-success-body" id="email-capture-success-body">We&rsquo;ll be in touch when subscriptions open. In the meantime, the free walk-up search is always here.</p>
      <button type="button" class="email-capture-success-close" id="email-capture-success-close">Back to WeatherValet</button>
    </div>
  </div>
</div>


<!-- ============ SIGN-IN MODAL — workspace selector =====================
     Opened by the floating "Sign in" pill in the top-right corner. In v1
     this is purely a navigation aid: clicking a workspace routes to the
     existing prototype view (same as the dev menu). In v2 (post-backend),
     this becomes the real auth entry point — each workspace selection
     leads to an email/magic-link auth step before reaching the actual
     dashboard. The position and visible structure stay the same; only
     what happens after workspace selection changes between v1 and v2.
     ===================================================================== -->
<div id="signin-modal" class="signin-scrim" hidden>
  <div class="signin-modal" role="dialog" aria-modal="true" aria-labelledby="signin-modal-title" data-step="email">
    <button type="button" class="signin-modal-close" id="signin-modal-close" aria-label="Close sign in">
      <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"/>
        <line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>

    <!-- ── Step 1: Email + password login (primary auth path) ── -->
    <!-- The default step. User enters their email and password;
         submission POSTs to /api/v1/auth/login. On success, the response
         sets a session cookie and we route to the user's workspace
         (or workspace picker if they have multiple roles). On failure,
         we show a generic error to prevent account enumeration.
         The "Forgot password?" link uses the magic-link infrastructure
         as a password-reset mechanism (built but not yet wired into
         this UI; admin handles resets manually for now). -->
    <div class="signin-step" data-signin-step="email">
      <h2 id="signin-modal-title" class="signin-modal-title">Sign in to WeatherValet</h2>
      <p class="signin-modal-body">Welcome back. Enter your email and password to continue.</p>
      <form id="signin-email-form" novalidate>
        <label for="signin-email-input" class="signin-field-label">Email address</label>
        <input
          type="email"
          id="signin-email-input"
          name="email"
          class="signin-email-input"
          placeholder="you@example.com"
          autocomplete="email"
          required
          spellcheck="false"
          autocapitalize="off"
        />
        <label for="signin-password-input" class="signin-field-label" style="margin-top: 14px;">Password</label>
        <input
          type="password"
          id="signin-password-input"
          name="password"
          class="signin-email-input"
          placeholder="Your password"
          autocomplete="current-password"
          required
        />
        <p class="signin-error-text" id="signin-email-error" hidden>Please check your email and password.</p>
        <button type="submit" class="signin-submit-btn" id="signin-email-submit">
          <span class="signin-submit-label">Sign in</span>
          <span class="signin-submit-spinner" hidden>Signing in&hellip;</span>
        </button>
      </form>
      <p class="signin-modal-foot" style="margin-top: 14px;">
        <button type="button" class="signin-link-btn" data-signin-action="forgot-password">Forgot password?</button>
      </p>
      <p class="signin-modal-foot">
        New to WeatherValet? <a href="#" data-show-screen="screen-pricing">See plans &amp; subscribe &rarr;</a>
      </p>
    </div>

    <!-- ── Step 2: Check your email ── -->
    <!-- Shown after a successful magic-link request. The "check your email"
         message is identical regardless of whether the email matched a real
         account — this prevents revealing account existence. If the email
         was made up, no link is sent and the user just waits. -->
    <div class="signin-step" data-signin-step="check-email" hidden>
      <div class="signin-check-email-icon">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="5" width="18" height="14" rx="2"/>
          <path d="m3 7 9 6 9-6"/>
        </svg>
      </div>
      <h2 class="signin-modal-title">Check your email</h2>
      <p class="signin-modal-body">
        We sent a password-reset link to <strong id="signin-check-email-address">your email</strong>.
        Click the link in that email to set a new password. The link expires
        in <strong>15 minutes</strong>.
      </p>
      <p class="signin-modal-body" style="font-size: 12px; opacity: 0.75; margin-top: 16px;">
        Don&rsquo;t see it? Check your spam folder, or
        <button type="button" class="signin-link-btn" data-signin-action="back-to-email">try a different email</button>.
      </p>
    </div>

    <!-- ── Step 3: Workspace picker (for users with multiple roles) ── -->
    <!-- Reused after successful magic-link verification when the user
         has 2+ roles. For single-role users we route directly to their
         workspace without showing this step. -->
    <div class="signin-step" data-signin-step="workspace-picker" hidden>
      <h2 class="signin-modal-title">Welcome back<span id="signin-welcome-name"></span></h2>
      <p class="signin-modal-body">You can access multiple workspaces. Where do you want to go?</p>
      <div class="signin-workspace-list" id="signin-workspace-list">
        <!-- Filled in dynamically by JS based on user's actual roles.
             Structure matches the old hard-coded buttons so existing CSS works. -->
      </div>
    </div>

    <!-- ── Step 4: Account pending (zero roles) ── -->
    <!-- Shown when a user successfully verifies but has no roles assigned.
         New signups land here until admin grants them a role. This is
         per Decision 4 of the implementation plan — admin-driven role
         assignment, no automatic role grants in v1. -->
    <div class="signin-step" data-signin-step="account-pending" hidden>
      <h2 class="signin-modal-title">You&rsquo;re in &mdash; almost</h2>
      <p class="signin-modal-body">
        We&rsquo;ve confirmed your email. Your account is being set up &mdash;
        we&rsquo;ll be in touch within 24 hours to finish onboarding you.
      </p>
      <p class="signin-modal-body" style="font-size: 12px; opacity: 0.75; margin-top: 16px;">
        Questions? Email <a href="mailto:michael@weathervalet.com" style="color: var(--rb, #4169E1); font-weight: 600;">michael@weathervalet.com</a>.
      </p>
    </div>

    <!-- ── Step 5: Verification error ── -->
    <!-- Shown when /auth/verify returns an error (expired link, used link,
         invalid token). User can retry from this state. -->
    <div class="signin-step" data-signin-step="verify-error" hidden>
      <h2 class="signin-modal-title" id="signin-verify-error-title">Link expired</h2>
      <p class="signin-modal-body" id="signin-verify-error-body">
        This sign-in link is no longer valid. Request a new one and try again.
      </p>
      <button type="button" class="signin-submit-btn" data-signin-action="back-to-email" style="margin-top: 16px;">
        Request a new link
      </button>
    </div>

    <!-- ── Step: Reset password (request side) ──
         Email-only form. User types their email, we POST to
         /api/v1/auth/request-magic-link with intent=password-reset.
         Backend sends a reset email with a magic link. We transition
         to the check-email step on success. -->
    <div class="signin-step" data-signin-step="reset-request" hidden>
      <h2 class="signin-modal-title">Reset your password</h2>
      <p class="signin-modal-body">
        Enter your email and we'll send you a link to set a new password.
        The link expires in 15 minutes.
      </p>
      <form id="signin-reset-form" novalidate>
        <label for="signin-reset-email-input" class="signin-field-label">Email address</label>
        <input
          type="email"
          id="signin-reset-email-input"
          name="email"
          class="signin-email-input"
          placeholder="you@example.com"
          autocomplete="email"
          required
          spellcheck="false"
          autocapitalize="off"
        />
        <p class="signin-error-text" id="signin-reset-error" hidden>Please enter a valid email.</p>
        <button type="submit" class="signin-submit-btn" id="signin-reset-submit">
          <span class="signin-submit-label">Send reset link</span>
          <span class="signin-submit-spinner" hidden>Sending&hellip;</span>
        </button>
      </form>
      <p class="signin-modal-foot" style="margin-top: 14px;">
        <button type="button" class="signin-link-btn" data-signin-action="back-to-email">Back to sign in</button>
      </p>
    </div>

    <!-- ── Step: Set new password (after clicking reset email link) ──
         Shown after the frontend detects ?auth=verify&token=...&intent=password-reset
         in the URL, verifies the token (which signs the user in), and then
         needs them to set a new password before continuing. -->
    <div class="signin-step" data-signin-step="set-password" hidden>
      <h2 class="signin-modal-title">Set a new password</h2>
      <p class="signin-modal-body">
        You're signed in. Choose a new password to finish.
      </p>
      <form id="signin-setpw-form" novalidate>
        <label for="signin-setpw-input" class="signin-field-label">New password</label>
        <input
          type="password"
          id="signin-setpw-input"
          name="password"
          class="signin-email-input"
          placeholder="At least 8 characters"
          autocomplete="new-password"
          required
        />
        <label for="signin-setpw-confirm" class="signin-field-label" style="margin-top: 14px;">Confirm new password</label>
        <input
          type="password"
          id="signin-setpw-confirm"
          name="password_confirm"
          class="signin-email-input"
          placeholder="Re-type the password"
          autocomplete="new-password"
          required
        />
        <p class="signin-error-text" id="signin-setpw-error" hidden></p>
        <button type="submit" class="signin-submit-btn" id="signin-setpw-submit">
          <span class="signin-submit-label">Set password and continue</span>
          <span class="signin-submit-spinner" hidden>Saving&hellip;</span>
        </button>
      </form>
    </div>

  </div>
</div>


<!-- ============ METEOROLOGIST VIEW (simulated) ============ -->
<!-- Mockup of what the meteorologist portal will look like in production.
     The real version lives in the Flask backend at /meteorologist/home;
     this is a UI-only simulation accessible via the dev menu so you can
     give feedback on the experience before the real version is deployed. -->
<div id="screen-met" class="screen met-screen workspace">
  <div class="sim-banner">
    <span class="sim-banner-strong">Prototype Simulation</span> &middot;
    The real meteorologist workspace will live in the deployed backend
  </div>

  <div class="met-ws-header">
    <div class="met-ws-header-top">
      <div class="met-ws-brand">
        <span class="met-ws-eyebrow">WeatherValet Workspace</span>
        <h1 class="met-ws-title" id="met-ws-title">Welcome back</h1>
        <p class="met-ws-coverage" id="met-ws-coverage" style="margin: 4px 0 0 0; font-size: 12px; color: rgba(255,255,255,0.65); font-weight: 500;">
          <span id="met-ws-coverage-zone">Indiana + Ohio</span>
          <span style="opacity: 0.5; margin: 0 6px;">&middot;</span>
          <span id="met-ws-coverage-hours">6:00 AM &ndash; 10:00 PM ET</span>
        </p>
      </div>
      <div class="met-ws-meta">
        <div class="met-ws-meta-item">
          <span class="met-ws-shift-dot" id="met-ws-shift-dot"></span>
          <span class="met-ws-meta-value" id="met-ws-shift-status">On shift</span>
        </div>
        <!-- Team chat (Discord) — always-visible button so Mets can
             jump to team coordination from anywhere in the workspace.
             Opens the WeatherValet Discord in a new tab. -->
        <a href="https://discord.gg/s7Sa9jZ7" target="_blank" rel="noopener"
           class="ws-exit-btn" style="text-decoration:none;color:inherit;"
           title="Open team chat on Discord">
          <svg viewBox="0 0 24 24" width="13" height="13" fill="currentColor" style="opacity:0.85;">
            <path d="M20.317 4.37a19.79 19.79 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028c.462-.63.874-1.295 1.226-1.994a.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03zM8.02 15.331c-1.182 0-2.157-1.085-2.157-2.419 0-1.333.956-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.956 2.42-2.157 2.42zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.946 2.42-2.157 2.42z"/>
          </svg>
          <span>Team chat</span>
        </a>
        <button type="button" id="rosie-open-btn" class="ws-exit-btn" aria-label="Ask Rosie"
                style="background:linear-gradient(135deg, rgba(245,112,142,0.18), rgba(194,52,80,0.12)); border-color:rgba(245,112,142,0.45); color:#f5708e;"
                title="Open Rosie — your team assistant">
          <span style="font-size:14px; line-height:1;">🌹</span>
          <span>Ask Rosie</span>
        </button>
        <button type="button" class="ws-exit-btn" data-ws-exit="met" aria-label="Exit workspace">
          <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
            <polyline points="16 17 21 12 16 7"/>
            <line x1="21" y1="12" x2="9" y2="12"/>
          </svg>
          <span>Exit to search</span>
        </button>
      </div>
    </div>
    <div class="met-ws-tabs" role="tablist">
      <button type="button" class="met-ws-tab is-active" data-met-tab="queue" role="tab" aria-selected="true">
        Review Queue
        <span class="met-ws-tab-count" id="met-queue-count">4</span>
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="pro-briefs" role="tab" aria-selected="false">
        Pro Briefs
        <span class="met-ws-tab-count" id="met-pro-briefs-count" hidden>0</span>
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="messages" role="tab" aria-selected="false">
        Messages
        <span class="met-ws-tab-count" id="met-messages-count" hidden>0</span>
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="subscribers" role="tab" aria-selected="false">
        My Subscribers
        <span class="met-ws-tab-count" id="met-subscribers-count" hidden>0</span>
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="brief" role="tab" aria-selected="false">
        Daily Brief
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="missions" role="tab" aria-selected="false">
        Missions
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="history" role="tab" aria-selected="false">
        History
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="schedule" role="tab" aria-selected="false">
        Schedule
      </button>
      <button type="button" class="met-ws-tab" data-met-tab="map" role="tab" aria-selected="false">
        Map
      </button>
    </div>
  </div>

  <div class="met-ws-body">

    <!-- ══════════════════════════════════════════════════════════════
         Met welcome card — first-login orientation.
         Hidden by default; setupMetWelcomeCard() shows it when
         users.met_onboarded_at IS NULL. Dismissible with one tap.
         ══════════════════════════════════════════════════════════════ -->
    <div id="met-welcome-card" class="ws-tile ws-span-12" hidden style="background:linear-gradient(135deg,#1e3a8a 0%,#2E4FB8 100%);border:1px solid rgba(46,79,184,0.40);margin-bottom:20px;position:relative;color:#fff;">
      <button type="button" id="met-welcome-dismiss" aria-label="Dismiss welcome card"
              style="position:absolute;top:14px;right:14px;background:transparent;border:none;color:rgba(255,255,255,0.55);cursor:pointer;padding:6px;border-radius:6px;line-height:0;">
        <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
      <div style="display:flex;align-items:flex-start;gap:16px;padding:8px 4px;">
        <div style="flex-shrink:0;width:42px;height:42px;background:rgba(46,79,184,0.30);border-radius:50%;display:flex;align-items:center;justify-content:center;">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#9BB5F0"
               stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
        </div>
        <div style="flex:1;">
          <h2 style="font-size:18px;font-weight:600;color:#fff;margin:0 0 6px;letter-spacing:-0.01em;">
            Welcome, <span id="met-welcome-name">meteorologist</span>
          </h2>
          <p style="font-size:13.5px;color:rgba(255,255,255,0.75);margin:0 0 16px;line-height:1.55;">
            Quick orientation. This is your workspace &mdash; everything you need lives in the tabs above.
          </p>
          <ul style="list-style:none;padding:0;margin:0 0 18px;font-size:13px;color:rgba(255,255,255,0.80);line-height:1.7;">
            <li style="display:flex;gap:10px;margin-bottom:6px;">
              <span style="color:#5DECA8;font-weight:700;">&#x2713;</span>
              <span><strong style="color:#fff;">Review Queue</strong> &mdash; $19 customer reviews land here. You&rsquo;ll get an SMS when one comes in. Claim, write the call, send.</span>
            </li>
            <li style="display:flex;gap:10px;margin-bottom:6px;">
              <span style="color:#5DECA8;font-weight:700;">&#x2713;</span>
              <span><strong style="color:#fff;">Pro Briefs</strong> &mdash; AI-drafted morning briefs for Pro subscribers wait for your sign-off. Edit and send.</span>
            </li>
            <li style="display:flex;gap:10px;margin-bottom:6px;">
              <span style="color:#5DECA8;font-weight:700;">&#x2713;</span>
              <span><strong style="color:#fff;">Messages</strong> &mdash; Pro subscribers can DM you directly. Reply within 30 minutes during business hours.</span>
            </li>
            <li style="display:flex;gap:10px;">
              <span style="color:#5DECA8;font-weight:700;">&#x2713;</span>
              <span><strong style="color:#fff;">History</strong> &mdash; your past reviews, plus tip earnings (customers can thank you).</span>
            </li>
          </ul>
          <p style="font-size:12px;color:rgba(255,255,255,0.55);margin:0;line-height:1.5;">
            Questions or something broken? Text Michael directly.
            We&rsquo;re launching with a small group, so feedback shapes what we build next.
          </p>

          <!-- Team chat (Discord) tile — opens the WeatherValet Discord
               server where Mets coordinate on severe weather, shift
               handoffs, and forecast discussion. Available to all Mets. -->
          <a href="https://discord.gg/s7Sa9jZ7" target="_blank" rel="noopener"
             style="display:flex;align-items:center;gap:12px;background:rgba(88,101,242,0.16);border:1px solid rgba(88,101,242,0.45);border-radius:10px;padding:12px 14px;margin-top:18px;text-decoration:none;color:#fff;transition:background 120ms;"
             onmouseover="this.style.background='rgba(88,101,242,0.26)'"
             onmouseout="this.style.background='rgba(88,101,242,0.16)'">
            <!-- Discord icon -->
            <svg viewBox="0 0 24 24" width="22" height="22" fill="#9BA9F5" style="flex-shrink:0;">
              <path d="M20.317 4.37a19.79 19.79 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028c.462-.63.874-1.295 1.226-1.994a.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03zM8.02 15.331c-1.182 0-2.157-1.085-2.157-2.419 0-1.333.956-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.956 2.42-2.157 2.42zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.418 2.157-2.418 1.21 0 2.176 1.094 2.157 2.418 0 1.334-.946 2.42-2.157 2.42z"/>
            </svg>
            <div style="flex:1;">
              <div style="font-size:13.5px;font-weight:600;color:#fff;line-height:1.3;">Team chat on Discord</div>
              <div style="font-size:11.5px;color:rgba(255,255,255,0.65);margin-top:2px;line-height:1.4;">Coordinate severe weather &middot; handoffs &middot; forecast discussion</div>
            </div>
            <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="rgba(255,255,255,0.55)" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;">
              <path d="M7 17L17 7M7 7h10v10"/>
            </svg>
          </a>
        </div>
      </div>
    </div>

    <!-- ── Tab: Review Queue ── -->
    <div class="met-panel is-active" data-met-panel="queue" style="display: contents;">

      <!-- Row 1: Personal vitals strip -->
      <div class="ws-section-eyebrow">
        <span>Today &amp; this week</span>
        <span class="ws-section-eyebrow-detail">your stats</span>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-2">
        <p class="ws-vital-label">Reviews today</p>
        <div class="ws-vital-value">3</div>
        <p class="ws-vital-context">avg 52s response</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-2">
        <p class="ws-vital-label">Earned today</p>
        <div class="ws-vital-value is-money">$37<span class="ws-vital-value-unit">.05</span></div>
        <p class="ws-vital-context">3 &times; $12.35</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-2">
        <p class="ws-vital-label">Reviews this week</p>
        <div class="ws-vital-value">19</div>
        <p class="ws-vital-context">96% helpful rate</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-2">
        <p class="ws-vital-label">Earned this week</p>
        <div class="ws-vital-value is-money">$234<span class="ws-vital-value-unit">.65</span></div>
        <p class="ws-vital-context">19 &times; $12.35</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-2">
        <p class="ws-vital-label">Avg response</p>
        <div class="ws-vital-value">52<span class="ws-vital-value-unit">s</span></div>
        <p class="ws-vital-context">team avg 47s</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-2">
        <p class="ws-vital-label">Streak</p>
        <div class="ws-vital-value">12<span class="ws-vital-value-unit">d</span></div>
        <p class="ws-vital-context">consecutive shifts</p>
      </div>

      <!-- Row 1.5: Month-to-date compensation breakdown — per Met Compensation Spec -->
      <div class="ws-section-eyebrow">
        <span>This month so far</span>
        <span class="ws-section-eyebrow-detail">your earnings &middot; updates in real time</span>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Subscription pool share</p>
        <div class="ws-vital-value is-money">$487<span class="ws-vital-value-unit">.50</span></div>
        <p class="ws-vital-context">15 morning briefs delivered</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Paid review earnings</p>
        <div class="ws-vital-value is-money">$987<span class="ws-vital-value-unit">.30</span></div>
        <p class="ws-vital-context">80 reviews &times; 65% of $19</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Storm Shelter bonuses</p>
        <div class="ws-vital-value is-money" id="met-shelter-earnings-value">$0<span class="ws-vital-value-unit">.00</span></div>
        <p class="ws-vital-context" id="met-shelter-earnings-context">0 activations &times; $25</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Total month-to-date</p>
        <div class="ws-vital-value is-money">$1,524<span class="ws-vital-value-unit">.80</span></div>
        <p class="ws-vital-context">Final on May 31</p>
      </div>

      <!-- Row 2: Team context -->
      <div class="ws-section-eyebrow">
        <span>The team this week</span>
        <span class="ws-section-eyebrow-detail">we, not me</span>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Team reviews</p>
        <div class="ws-vital-value">73</div>
        <p class="ws-vital-context">12 today &middot; on track for our weekly avg</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Team avg response</p>
        <div class="ws-vital-value">47<span class="ws-vital-value-unit">s</span></div>
        <p class="ws-vital-context">8s faster than last week</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Team helpful rate</p>
        <div class="ws-vital-value">94<span class="ws-vital-value-unit">%</span></div>
        <p class="ws-vital-context">47 of 50 customers said yes</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">On shift now</p>
        <div class="ws-vital-value">3<span class="ws-vital-value-unit">/6</span></div>
        <p class="ws-vital-context">Chris, Keefer, you</p>
      </div>

      <!-- Composer (hidden until claimed) -->
      <div id="met-composer" class="ws-composer-shell" hidden>
        <div class="ws-composer-strip">
          <div class="ws-composer-strip-left">
            <span class="ws-composer-strip-eyebrow">Reviewing</span>
            <span class="ws-composer-strip-customer" id="met-composer-customer">—</span>
          </div>
          <div class="ws-composer-timer">
            <span class="ws-composer-timer-dot"></span>
            <span id="ws-composer-timer-text">Claimed just now</span>
          </div>
        </div>
        <div class="ws-composer-grid">
          <div class="ws-composer-write">

            <!-- Brief Type selector — the load-bearing decision at the
                 top of every forecast. The type determines what delivery
                 rule applies (Morning Brief = customer's morning window,
                 Paid Review = send immediately, Severe Alert = bypass
                 everything, etc.). The Met always picks explicitly —
                 no auto-magic. See MET-HANDBOOK.md "Forecast types and
                 delivery timing" for the full rule set. -->
            <div class="ws-composer-section ws-brief-type-section">
              <p class="ws-composer-section-label">Brief type</p>
              <div class="ws-brief-type-grid">
                <button type="button" class="ws-brief-type-btn is-active" data-brief-type="morning">
                  <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"/><circle cx="12" cy="12" r="4"/>
                  </svg>
                  <span>Morning Brief</span>
                </button>
                <button type="button" class="ws-brief-type-btn" data-brief-type="evening">
                  <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
                  </svg>
                  <span>Evening Brief</span>
                </button>
                <button type="button" class="ws-brief-type-btn" data-brief-type="paid-review">
                  <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
                  </svg>
                  <span>Paid Review</span>
                </button>
                <button type="button" class="ws-brief-type-btn" data-brief-type="thread-reply">
                  <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
                  </svg>
                  <span>Thread Reply</span>
                </button>
                <button type="button" class="ws-brief-type-btn ws-brief-type-severe" data-brief-type="severe">
                  <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                    <path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>
                  </svg>
                  <span>Severe Alert</span>
                </button>
              </div>
            </div>

            <div class="ws-composer-section">
              <p class="ws-composer-section-label">Verdict</p>
              <div class="ws-verdict-grid">
                <button type="button" class="ws-verdict-btn verdict-clear" data-verdict="clear">Clear</button>
                <button type="button" class="ws-verdict-btn verdict-caution is-active" data-verdict="caution">Caution</button>
                <button type="button" class="ws-verdict-btn verdict-risk" data-verdict="risk">Risk</button>
              </div>
            </div>
            <div class="ws-composer-section">
              <p class="ws-composer-section-label">Confidence</p>
              <div class="ws-confidence-row">
                <input type="range" class="ws-confidence-input" id="met-confidence-input" min="50" max="100" value="85">
                <span class="ws-confidence-value" id="met-confidence-value">Confident &middot; 85%</span>
              </div>
            </div>
            <div class="ws-composer-section">
              <p class="ws-composer-section-label">Why &mdash; your reasoning</p>
              <textarea class="ws-textarea" id="met-reason" rows="3" placeholder="Wind shifting NW with a frontal passage at 14:00..."></textarea>
            </div>
            <div class="ws-composer-section">
              <p class="ws-composer-section-label">What to watch</p>
              <textarea class="ws-textarea" id="met-watch" rows="3" placeholder="Storm complex moving E-SE at 25 kt..."></textarea>
            </div>
            <div class="ws-composer-section">
              <p class="ws-composer-section-label">Bottom line for the customer</p>
              <textarea class="ws-textarea" id="met-bottom-line" rows="2" placeholder="Plan as scheduled..."></textarea>
            </div>
            <div class="ws-composer-section">
              <p class="ws-composer-section-label">Alternate window <span style="font-weight:400; color:rgba(15,17,22,0.45);">— optional</span></p>
              <input type="text" class="ws-input" id="met-alternate" placeholder="e.g. Wednesday 9 AM – 1 PM looks materially better">
            </div>

            <!-- Delivery block — quiet by default, expandable on demand.
                 The Met composes a forecast, glances at "will deliver at X,"
                 hits send, walks away. The override option is present but
                 unobtrusive — clicking it expands an inline dropdown with
                 explicit alternatives. Default rules computed from the
                 selected forecast type (Morning Brief, Evening Brief, etc.)
                 plus the customer's record. See MET-HANDBOOK.md and
                 BUILD-QUEUE.md item #8 for the full rule set. -->
            <div class="ws-composer-section ws-delivery-block">
              <p class="ws-composer-section-label">Delivery</p>
              <div class="ws-delivery-card">
                <div class="ws-delivery-summary">
                  <div class="ws-delivery-icon">
                    <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                      <circle cx="12" cy="12" r="10"/>
                      <polyline points="12 6 12 12 16 14"/>
                    </svg>
                  </div>
                  <div class="ws-delivery-summary-text">
                    <div class="ws-delivery-when" id="met-delivery-when">
                      Will deliver in <strong>Marcus&rsquo;s morning brief window</strong>
                      <span class="ws-delivery-relative">5:30&ndash;7:00 AM Friday</span>
                    </div>
                    <div class="ws-delivery-why" id="met-delivery-why">
                      Customer chose this window during onboarding &middot; system holds until then
                    </div>
                  </div>
                </div>
                <button type="button" class="ws-delivery-override-toggle" id="met-delivery-override-toggle" aria-expanded="false">
                  <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="6 9 12 15 18 9"/>
                  </svg>
                  <span>Override delivery time</span>
                </button>
                <div class="ws-delivery-override-panel" id="met-delivery-override-panel" hidden>
                  <label class="ws-delivery-override-option">
                    <input type="radio" name="met-delivery-override" value="default" checked>
                    <span class="ws-delivery-override-label">Use brief-type default</span>
                    <span class="ws-delivery-override-detail">Recommended &middot; matches customer&rsquo;s onboarding preferences</span>
                  </label>
                  <label class="ws-delivery-override-option">
                    <input type="radio" name="met-delivery-override" value="asap-quiet">
                    <span class="ws-delivery-override-label">Send as soon as possible</span>
                    <span class="ws-delivery-override-detail">Still respects quiet hours &middot; useful when running late</span>
                  </label>
                  <label class="ws-delivery-override-option ws-delivery-override-danger">
                    <input type="radio" name="met-delivery-override" value="now-bypass">
                    <span class="ws-delivery-override-label">Send immediately, bypass quiet hours</span>
                    <span class="ws-delivery-override-detail">Use only for severe weather or paid reviews flagged urgent</span>
                  </label>
                </div>
              </div>
            </div>

            <div class="ws-composer-actions">
              <button type="button" class="ws-composer-cancel" id="met-cancel-btn">Release back to queue</button>
              <button type="button" class="ws-composer-submit" id="met-submit-btn">Submit review</button>
            </div>
          </div>
          <aside class="ws-composer-context">
            <div class="ws-context-block">
              <div class="ws-context-block-label">Customer plan</div>
              <p class="ws-context-customer-plan" id="ws-context-customer-plan">—</p>
              <p class="ws-context-customer-meta" id="ws-context-customer-meta">—</p>
            </div>
            <div class="ws-context-block">
              <div class="ws-context-block-label">AI suggestion</div>
              <div class="ws-context-ai">
                <div class="ws-context-ai-head">
                  <span class="ws-context-ai-verdict verdict-caution" id="ws-context-ai-verdict">Caution</span>
                  <span class="ws-context-ai-confidence" id="ws-context-ai-confidence">71% confidence</span>
                </div>
                <p class="ws-context-ai-paragraph" id="ws-context-ai-paragraph">—</p>
              </div>
            </div>
            <div class="ws-context-block">
              <div class="ws-context-block-label">Weather data</div>
              <div class="ws-context-weather-grid">
                <div class="ws-context-weather-cell">
                  <div class="ws-context-weather-cell-label">Temperature</div>
                  <div class="ws-context-weather-cell-value" id="ws-context-temp">—</div>
                </div>
                <div class="ws-context-weather-cell">
                  <div class="ws-context-weather-cell-label">Wind</div>
                  <div class="ws-context-weather-cell-value" id="ws-context-wind">—</div>
                </div>
                <div class="ws-context-weather-cell">
                  <div class="ws-context-weather-cell-label">Precipitation</div>
                  <div class="ws-context-weather-cell-value" id="ws-context-precip">—</div>
                </div>
                <div class="ws-context-weather-cell">
                  <div class="ws-context-weather-cell-label">Sky</div>
                  <div class="ws-context-weather-cell-value" id="ws-context-sky">—</div>
                </div>
              </div>
            </div>
            <div class="ws-context-block">
              <div class="ws-context-block-label">Why the rules flagged this</div>
              <ul class="ws-context-reasons" id="ws-context-reasons"></ul>
            </div>
          </aside>
        </div>
      </div>

      <!-- Row 3: Queue + Recognition side-by-side -->
      <div class="ws-section-eyebrow">
        <span>Active work</span>
        <span class="ws-section-eyebrow-detail">queue and team recognition</span>
      </div>
      <div class="ws-tile ws-span-8" id="met-queue-label">
        <div class="ws-tile-label">
          <span>Pending reviews</span>
          <span class="ws-tile-label-detail">&middot; first-claim wins, 30-min lock</span>
        </div>
        <div class="ws-queue-list" id="met-queue-list">
          <!-- Populated by JS — review rows rendered here -->
        </div>
      </div>
      <div class="ws-tile ws-span-4">
        <div class="ws-tile-label">
          <span>Recognition</span>
          <span class="ws-tile-label-detail">&middot; teammates worth a nod</span>
        </div>
        <div class="ws-recognize-col">
          <div class="ws-recognize-suggestion">
            <span class="ws-recognize-suggestion-text">
              <strong>M. Reynolds</strong> just delivered their 4th review of the day &mdash; all under 60 seconds.
            </span>
            <button type="button" class="ws-recognize-btn" data-recognize-target="sarah">
              <span>👏</span><span>Send</span>
            </button>
          </div>
          <div class="ws-recognize-suggestion">
            <span class="ws-recognize-suggestion-text">
              <strong>Chris Sramek</strong> stayed late last night to push out the Westfield gust-front advisory.
            </span>
            <button type="button" class="ws-recognize-btn" data-recognize-target="marcus">
              <span>🙏</span><span>Send</span>
            </button>
          </div>
        </div>
      </div>

      <!-- Row 4: Forecasting toolkit placeholder -->
      <div class="ws-section-eyebrow">
        <span>Forecasting toolkit</span>
        <span class="ws-section-eyebrow-detail">your one-stop shop &middot; coming together</span>
      </div>
      <div class="ws-tile ws-span-12">
        <div class="ws-tools-placeholder">
          <svg class="ws-tools-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
          <h3 class="ws-tools-headline">Your forecasting tools, building toward one screen.</h3>
          <p class="ws-tools-detail">
            Model data, NWS feeds, Valet Crew reports, radar overlays, severe-weather graphics &mdash; all integrated into the workspace
            so you never have to leave to forecast. Building these in piece by piece.
          </p>
          <div class="ws-tools-list">
            <span class="ws-tools-chip">HRRR &middot; soon</span>
            <span class="ws-tools-chip">NAM3 &middot; soon</span>
            <span class="ws-tools-chip">NEXRAD radar &middot; soon</span>
            <span class="ws-tools-chip">NWS alerts &middot; soon</span>
            <span class="ws-tools-chip">Crew reports &middot; soon</span>
            <span class="ws-tools-chip">Severe graphics &middot; soon</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ══════════════════════════════════════════════════════════════
         Tab: Pro Briefs — Met-touched daily briefs for Pro subscribers.
         Drafts created by the scheduler; Met reviews, edits, sends.
         (Item #3 Chunk 3B-2)
         ══════════════════════════════════════════════════════════════ -->
    <div class="met-panel" data-met-panel="pro-briefs" style="display: none;">
      <div class="ws-tile ws-span-12">
        <div class="ws-tile-label">
          <span>Pending Pro briefs</span>
          <span class="ws-tile-label-detail">
            &middot; <span id="met-pro-briefs-pending-count">0</span> waiting on you
            &middot;
            <button type="button" id="met-pro-briefs-refresh"
                    style="background:transparent;border:1px solid rgba(255,255,255,0.10);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;margin-left:8px;">Refresh</button>
          </span>
        </div>
        <div id="met-pro-briefs-list" style="margin-top:14px;">
          <div style="padding:24px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>
    </div>

    <!-- ══════════════════════════════════════════════════════════════
         Tab: Messages — Pro Threads (subscriber DMs) [PT-C]
         Two-pane layout: thread list on the left (sticky), conversation
         view on the right. Click a thread → load + auto-mark-read.
         ══════════════════════════════════════════════════════════════ -->
    <div class="met-panel" data-met-panel="messages" style="display: none;">
      <div class="ws-tile ws-span-12" style="padding:0;overflow:hidden;">
        <div style="display:grid;grid-template-columns:300px 1fr;min-height:600px;background:#1a1d24;border-radius:14px;">

          <!-- Thread list (left pane) -->
          <div style="border-right:1px solid rgba(255,255,255,0.08);display:flex;flex-direction:column;">
            <div style="padding:14px 16px;border-bottom:1px solid rgba(255,255,255,0.08);display:flex;align-items:center;justify-content:space-between;gap:8px;">
              <span style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:rgba(255,255,255,0.55);">All threads</span>
              <div style="display:flex;gap:6px;">
                <button type="button" id="met-messages-new-thread"
                        style="background:#2E4FB8;border:none;color:#fff;padding:4px 10px;border-radius:5px;font-size:11px;font-weight:600;cursor:pointer;">+ New</button>
                <button type="button" id="met-messages-refresh"
                        style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:3px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
              </div>
            </div>
            <div id="met-messages-thread-list" style="flex:1;overflow-y:auto;">
              <div style="padding:30px 16px;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
            </div>
          </div>

          <!-- Conversation pane (right) -->
          <div style="display:flex;flex-direction:column;">
            <!-- Header: subscriber info (populated when thread selected) -->
            <div id="met-messages-conv-header" hidden
                 style="padding:14px 18px;border-bottom:1px solid rgba(255,255,255,0.08);">
              <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;">
                <div>
                  <div style="font-size:14px;font-weight:500;color:#fff;" id="met-messages-conv-name">&mdash;</div>
                  <div style="font-size:11px;color:rgba(255,255,255,0.55);margin-top:2px;" id="met-messages-conv-meta">&mdash;</div>
                </div>
                <span id="met-messages-conv-tier"
                      style="background:rgba(65,105,225,0.15);color:#9BB5F0;padding:3px 9px;border-radius:4px;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;">Pro</span>
              </div>
            </div>

            <!-- Empty selector state -->
            <div id="met-messages-conv-empty"
                 style="flex:1;display:flex;align-items:center;justify-content:center;padding:40px 20px;color:rgba(255,255,255,0.45);font-size:13.5px;text-align:center;line-height:1.55;">
              Pick a thread from the left to view the conversation.
            </div>

            <!-- Messages list (populated when thread selected) -->
            <div id="met-messages-conv-body" hidden
                 style="flex:1;overflow-y:auto;padding:16px 18px;display:flex;flex-direction:column;gap:10px;">
              <!-- messages by JS -->
            </div>

            <!-- Composer (populated when thread selected) -->
            <div id="met-messages-conv-composer" hidden
                 style="padding:12px 16px;border-top:1px solid rgba(255,255,255,0.08);display:flex;gap:10px;align-items:flex-start;">
              <textarea id="met-messages-input" rows="2" maxlength="4000"
                        placeholder="Type your reply..."
                        style="flex:1;padding:10px 12px;background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.10);color:#fff;border-radius:8px;font-size:13.5px;font-family:inherit;resize:vertical;line-height:1.5;"></textarea>
              <button type="button" id="met-messages-send"
                      style="background:#2E4FB8;color:#fff;border:none;padding:10px 18px;border-radius:8px;font-size:13px;font-weight:600;cursor:pointer;align-self:flex-start;">
                Send
              </button>
            </div>
          </div>

        </div>
      </div>
    </div>

    <!-- ── Tab: My Subscribers — full subscriber roster + ops context ── -->
    <div class="met-panel" data-met-panel="subscribers" style="display: none;">

      <div class="ws-tile ws-span-12" style="margin-bottom: 16px;">
        <div class="ws-tile-label">
          <span>My Subscribers</span>
          <span class="ws-tile-label-detail">
            &middot; who is paying for what
            &middot;
            <button type="button" id="met-subs-refresh"
                    style="background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.65);padding:3px 10px;border-radius:5px;font-size:11px;cursor:pointer;margin-left:8px;">Refresh</button>
          </span>
        </div>
        <p style="font-size:12.5px;color:rgba(15,17,22,0.65);margin:10px 0 14px;line-height:1.55;">
          Everyone paying for WeatherValet. Use this to know who needs what
          information &mdash; the athletic director, the roofer, the grandmother
          all want different things. Search by name, business, location, or
          subscription tier.
        </p>

        <!-- Filter row -->
        <div style="display:grid;grid-template-columns:1fr auto auto;gap:10px;margin-bottom:14px;">
          <input type="text" id="met-subs-search"
                 placeholder="Search by name, email, location, county..."
                 style="padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:13.5px;font-family:inherit;">
          <select id="met-subs-tier-filter" style="padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:13px;font-family:inherit;background:#fff;cursor:pointer;">
            <option value="all">All tiers</option>
            <option value="pro_enterprise">Pro Enterprise</option>
            <option value="pro_multi">Pro Multi</option>
            <option value="pro_single">Pro Single</option>
            <option value="hobbyist">Hobbyist</option>
            <option value="free">Free</option>
          </select>
          <select id="met-subs-channel-filter" style="padding:10px 12px;border:1px solid rgba(15,17,22,0.15);border-radius:8px;font-size:13px;font-family:inherit;background:#fff;cursor:pointer;">
            <option value="all">All channels</option>
            <option value="sms">SMS</option>
            <option value="email">Email</option>
            <option value="both">Both</option>
          </select>
        </div>

        <div id="met-subs-list">
          <div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

    </div>

    <!-- ── Tab: Daily Brief — multi-state county selection ── -->
    <div class="met-panel" data-met-panel="brief" style="display: none;">

      <!-- Row 1: Message type — Routine vs Severe Alert -->
      <div class="ws-tile ws-span-12">
        <div class="ws-tile-label">
          <span>Message type</span>
          <span class="ws-tile-label-detail">&middot; what kind of message is this?</span>
        </div>
        <div class="ws-brief-mode-row">
          <button type="button" class="ws-brief-mode-btn is-active" data-brief-mode="routine">
            <span class="ws-brief-mode-icon">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/>
                <line x1="9" y1="13" x2="15" y2="13"/>
                <line x1="9" y1="17" x2="15" y2="17"/>
              </svg>
            </span>
            <div class="ws-brief-mode-info">
              <p class="ws-brief-mode-title">Routine daily brief</p>
              <p class="ws-brief-mode-desc">Once-a-day territory note, appears at top of search results &middot; in-app notification</p>
            </div>
          </button>
          <button type="button" class="ws-brief-mode-btn mode-severe" data-brief-mode="severe">
            <span class="ws-brief-mode-icon">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
                <line x1="12" y1="9" x2="12" y2="13"/>
                <line x1="12" y1="17" x2="12.01" y2="17"/>
              </svg>
            </span>
            <div class="ws-brief-mode-info">
              <p class="ws-brief-mode-title">Severe alert</p>
              <p class="ws-brief-mode-desc">Urgent push to selected subscribers &middot; SMS &middot; in-app banner</p>
            </div>
          </button>
        </div>
      </div>

      <!-- Row 2: Composer — coverage area with county multi-select, audience preview, fields -->
      <div class="ws-composer-shell ws-span-12" id="brief-composer-shell">
        <div class="ws-composer-strip">
          <div class="ws-composer-strip-left">
            <span class="ws-composer-strip-eyebrow" id="brief-mode-eyebrow">Routine brief &middot; Saturday, May 9</span>
            <span class="ws-composer-strip-customer" id="brief-mode-headline">Daily Brief</span>
          </div>
          <div class="ws-composer-timer">
            <span class="ws-composer-timer-dot"></span>
            <span id="brief-status-text">Draft &middot; not yet published</span>
          </div>
        </div>
        <p class="ws-composer-hint">
          Every field below is optional &mdash; fill what's useful for today.
          Send a full brief, just a Crew post, both, or neither.
        </p>
        <div class="ws-composer-grid">
          <!-- Left: writing surface -->
          <div class="ws-composer-write">
            <div class="ws-composer-section">
              <p class="ws-composer-section-label">Coverage area &mdash; state &amp; counties</p>
              <div class="ws-county-shell">
                <div class="ws-county-controls">
                  <select class="ws-county-state" id="met-county-state" aria-label="Select state">
                    <!-- Populated by JS — all 50 states + DC -->
                  </select>
                  <input type="text" class="ws-county-search" id="met-county-search" placeholder="Filter counties in this state...">
                  <button type="button" class="ws-county-select-all-btn" id="met-county-select-all">Select state</button>
                  <button type="button" class="ws-county-clear-btn" id="met-county-clear">Clear all</button>
                </div>
                <div class="ws-county-list" id="met-county-list"><!-- populated by JS --></div>
                <div class="ws-county-chips" id="met-county-chips">
                  <span class="ws-county-chips-empty">No counties selected yet &mdash; pick a state, then check counties above.</span>
                </div>
              </div>
              <div class="ws-polygon-map-section">
                <p class="ws-polygon-map-label">
                  Targeting area
                  <span class="ws-polygon-map-label-detail">
                    &middot; draw a shape to limit who receives this brief
                  </span>
                </p>
                <div class="ws-polygon-map-help">
                  Use the toolbar at the top-left of the map to draw a
                  rectangle, circle, or freehand polygon. The brief will
                  go to subscribers whose location falls inside the shape.
                  Leave it empty to send to everyone in your selected counties.
                </div>
                <div class="ws-polygon-map-wrap">
                  <div class="ws-polygon-map-loading" data-polygon-loading="brief">
                    <div class="met-map-loading-dot"></div>
                    <span>Loading the map&hellip;</span>
                  </div>
                  <div class="ws-polygon-map" id="brief-polygon-map"></div>
                </div>
                <div class="ws-polygon-map-status" id="brief-polygon-status">
                  No shape drawn yet.
                </div>
                <!-- Hidden field for the parent flow to pick up the
                     drawn shape when saving the brief. Format: GeoJSON
                     Feature JSON, or empty if no shape drawn. -->
                <input type="hidden" id="brief-polygon-geojson" value="">
              </div>
            </div>
            <div class="ws-composer-section">
              <p class="ws-composer-section-label" id="brief-verdict-label">Headline verdict for the day</p>
              <div class="ws-verdict-grid" id="brief-verdict-grid">
                <button type="button" class="ws-verdict-btn verdict-clear" data-daily-verdict="clear">Generally Clear</button>
                <button type="button" class="ws-verdict-btn verdict-caution is-active" data-daily-verdict="caution">Mixed</button>
                <button type="button" class="ws-verdict-btn verdict-risk" data-daily-verdict="risk">Active Weather</button>
              </div>
            </div>
            <div class="ws-composer-section">
              <p class="ws-composer-section-label" id="brief-time-label">Time windows of concern</p>
              <input type="text" class="ws-input" id="brief-time-windows" value="14:00–18:00 thunderstorms possible · gust front earlier than models suggest">
            </div>
            <div class="ws-composer-section">
              <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 6px;">
                <p class="ws-composer-section-label" id="brief-summary-label" style="margin: 0;">Summary &mdash; visible to subscribers</p>
                <!-- F-X3 (May 17): Polish writing on Daily Brief summary -->
                <button type="button" id="daily-brief-polish-btn"
                        style="background: transparent; border: 1px solid rgba(46,79,184,0.30); color: #2E4FB8; padding: 5px 10px; border-radius: 6px; font-size: 11px; font-weight: 600; cursor: pointer; display: inline-flex; align-items: center; gap: 5px;"
                        title="Run a quick grammar + spelling check">
                  <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 2l1.4 4.2L18 8l-4.6 1.8L12 14l-1.4-4.2L6 8l4.6-1.8L12 2z" stroke-linejoin="round"/>
                    <path d="M19 14l.7 2.1L22 17l-2.3.9L19 20l-.7-2.1L16 17l2.3-.9L19 14z"/>
                  </svg>
                  <span>Polish writing</span>
                </button>
              </div>
              <textarea class="ws-textarea" id="brief-summary" rows="5">Cold front passes through Central Indiana mid-afternoon. Morning is clear, mild, and stable — favorable for outdoor work or events. After 14:00, watch for thunderstorm development along the front; HRRR is bringing it through Boone County around 15:30. Storm motion E-SE at 28-34 kt with strong gust potential ahead of the line. By 18:30 most activity has cleared the territory; evening is calm and humid.</textarea>
              <div id="daily-brief-polish-panel" hidden style="margin-top: 12px; padding: 12px 14px; background: rgba(46,79,184,0.05); border: 1px solid rgba(46,79,184,0.20); border-radius: 8px;">
                <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 10px;">
                  <div style="font-size: 11.5px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: #2E4FB8;" id="daily-brief-polish-status">
                    Suggestions
                  </div>
                  <button type="button" id="daily-brief-polish-close" style="background: transparent; border: 0; padding: 4px; cursor: pointer; color: rgba(15,17,22,0.55);" aria-label="Close suggestions">
                    <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                  </button>
                </div>
                <div id="daily-brief-polish-list" style="display: flex; flex-direction: column; gap: 10px;"></div>
              </div>
            </div>

            <!-- ── Met-to-Crew post composer (Chunk 6c-1) ──────────
                 A separate channel from the subscriber brief above.
                 Mets type a short line they want broadcast to the Crew
                 layer (Live Feed, ticker, map, on-air rail). The "Send
                 to Crew feed" button opens a small modal with a pin-
                 drop map so the Met can pick where on the territory
                 the post applies — that lat/long becomes the pulse
                 location for the Met's post on the map surfaces. -->
            <div class="ws-composer-section met-post-composer">
              <p class="ws-composer-section-label">
                Quick post to Crew feed
                <span class="ws-composer-section-label-detail">&middot; independent of the brief &middot; send on its own anytime</span>
              </p>
              <textarea class="ws-textarea ws-textarea-short" id="met-post-text" rows="2"
                placeholder="e.g., Watch for the gust front earlier than 14:00 — Crew west of Lebanon, you'll feel it first."></textarea>
              <div class="met-post-composer-actions">
                <button type="button" class="met-post-send-btn" id="met-post-send-btn" disabled>
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                       stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="22" y1="2" x2="11" y2="13"/>
                    <polygon points="22 2 15 22 11 13 2 9 22 2"/>
                  </svg>
                  Send to Crew feed
                </button>
                <span class="met-post-composer-hint" id="met-post-composer-hint">
                  Type a message to enable
                </span>
              </div>

              <!-- ── Scheduled posts queue (Chunk 6c-2) ──────────
                   Inline list of Met posts scheduled for future
                   publishing. Each row shows the message preview,
                   scheduled time, and edit + cancel buttons.
                   Hidden when there are no scheduled posts. -->
              <div class="met-post-queue" id="met-post-queue" hidden>
                <p class="met-post-queue-label">
                  Scheduled posts
                  <span class="met-post-queue-count" id="met-post-queue-count">0</span>
                </p>
                <ul class="met-post-queue-list" id="met-post-queue-list">
                  <!-- Populated by renderMetPostQueue() -->
                </ul>
              </div>
            </div>

            <div class="ws-composer-section">
              <p class="ws-composer-section-label">Notes for fellow meteorologists &mdash; private</p>
              <textarea class="ws-textarea" id="brief-private-notes" rows="3">HRRR has been overestimating storm coverage all week — prefer NAM3 hourly for timing. Crew reports from Lebanon and Zionsville already showing strong S winds at the surface; gust front likely earlier than 14:00.</textarea>
            </div>
            <div class="ws-composer-actions">
              <button type="button" class="ws-composer-cancel">Save as draft</button>
              <button type="button" class="ws-composer-submit" id="brief-submit-btn">Publish daily brief</button>
              <button type="button" id="brief-schedule-btn"
                      style="padding:11px 18px;background:#fff;border:1px solid #2E4FB8;color:#2E4FB8;border-radius:8px;font-size:14px;font-weight:600;cursor:pointer;display:inline-flex;align-items:center;gap:6px;">
                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="10"/>
                  <polyline points="12 6 12 12 16 14"/>
                </svg>
                Schedule
              </button>
            </div>
          </div>
          <!-- Right: audience preview + last brief reference -->
          <aside class="ws-composer-context">
            <div class="ws-context-block">
              <div class="ws-context-block-label">Audience preview</div>
              <div class="ws-audience-preview">
                <div class="ws-audience-icon">
                  <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
                    <circle cx="9" cy="7" r="4"/>
                    <path d="M22 21v-2a4 4 0 0 0-3-3.87"/>
                    <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
                  </svg>
                </div>
                <div class="ws-audience-text">
                  <p class="ws-audience-headline" id="brief-audience-headline">
                    Select counties to see your audience.
                  </p>
                  <p class="ws-audience-detail" id="brief-audience-detail">
                    Subscriber counts are estimated from county population and current
                    WeatherValet penetration.
                  </p>
                </div>
              </div>
            </div>
            <div class="ws-context-block">
              <div class="ws-context-block-label">Last brief published</div>
              <p class="ws-context-customer-plan" style="font-style: normal; font-family: 'Inter', system-ui, sans-serif; font-size: 13px;">
                Mixed conditions, frontal passage 14:00–18:00.
              </p>
              <p class="ws-context-customer-meta">
                Friday, May 8 &middot; Central Indiana &middot; 5 counties &middot; 92% read rate
              </p>
            </div>
            <div class="ws-context-block">
              <div class="ws-context-block-label">Active in territory</div>
              <ul class="ws-context-reasons">
                <li>Crew reports: 18 in last 30 min (5 verified)</li>
                <li>Active subscribers with location: 340</li>
                <li>NWS alerts in selected counties: 0 active</li>
              </ul>
            </div>
          </aside>
        </div>
      </div>
    </div>


    <!-- ── Tab: Missions — author and deploy ground-truth missions to Crew ── -->
    <div class="met-panel" data-met-panel="missions" style="display: none;">

      <!-- Row 1: Overview tiles -->
      <div class="ws-section-eyebrow">
        <span>Mission activity</span>
        <span class="ws-section-eyebrow-detail">your authored templates &middot; recent deployments</span>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Templates you authored</p>
        <div class="ws-vital-value">4</div>
        <p class="ws-vital-context">3 live, 1 in review</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Deployments this week</p>
        <div class="ws-vital-value">23</div>
        <p class="ws-vital-context">18 auto-fired, 5 manual</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Completion rate</p>
        <div class="ws-vital-value">68<span class="ws-vital-value-unit">%</span></div>
        <p class="ws-vital-context">Crew completed 16 of 23</p>
      </div>
      <div class="ws-tile ws-vital-tile ws-span-3">
        <p class="ws-vital-label">Citations sent</p>
        <div class="ws-vital-value">14</div>
        <p class="ws-vital-context">Crew readings used in your briefs</p>
      </div>

      <!-- Row 2: Quick deploy + create new -->
      <div class="ws-section-eyebrow">
        <span>Deploy a mission</span>
        <span class="ws-section-eyebrow-detail">fire to Crew in your coverage area</span>
      </div>
      <div class="ws-tile ws-span-8">
        <div class="ws-tile-label">
          <span>Quick deploy from catalog</span>
          <span class="ws-tile-label-detail">&middot; choose template, set audience, fire</span>
        </div>
        <!-- Mission template cards — rendered by renderMissionTemplates() from
             MISSION_TEMPLATES. Each card's Deploy button has data-deploy-mission
             that the click handler reads to populate the composer modal. -->
        <div id="mission-template-list" style="display: flex; flex-direction: column; gap: 10px; margin-top: 12px;">
          <!-- Populated by JS -->
        </div>
        <button type="button" id="mission-catalog-browse-btn" style="margin-top: 12px; width: 100%; font-size: 12px; padding: 10px; background: transparent; border: 1px dashed rgba(15,17,22,0.25); color: rgba(15,17,22,0.65); border-radius: 6px; cursor: pointer;">Browse full catalog (15 templates) &rarr;</button>
      </div>

      <!-- Right column: author new template -->
      <div class="ws-tile ws-span-4">
        <div class="ws-tile-label">
          <span>Author a new template</span>
          <span class="ws-tile-label-detail">&middot; submit for admin review</span>
        </div>
        <p style="margin: 12px 0; font-size: 12px; color: rgba(15,17,22,0.70); line-height: 1.5;">
          See a gap our missions don&rsquo;t cover? Draft a new template
          and submit it for review. Once approved, it&rsquo;s live for
          all Mets to deploy.
        </p>
        <button type="button" id="met-new-template-btn" style="width: 100%; padding: 10px 16px; background: linear-gradient(135deg, #4169E1, #2E4FCC); color: white; border: none; border-radius: 6px; cursor: pointer; font-weight: 600; font-size: 13px;">+ Create new mission template</button>
        <p style="margin: 12px 0 0 0; font-size: 11px; color: rgba(15,17,22,0.55); line-height: 1.5;">
          <strong style="color: rgba(15,17,22,0.75);">Draft &rarr; Review &rarr; Live.</strong>
          For now, text Michael with template ideas — full draft/review flow coming after launch.
        </p>
      </div>

      <!-- Row 3: My recent deployments -->
      <div class="ws-section-eyebrow">
        <span>Your recent deployments</span>
        <span class="ws-section-eyebrow-detail">missions you fired or authored</span>
      </div>
      <div class="ws-tile ws-span-12">
        <div class="ws-tile-label">
          <span>Last 5 deployments</span>
        </div>
        <div style="margin-top: 12px;">
          <div style="display: grid; grid-template-columns: 1fr 1.5fr 1fr 1fr auto; gap: 16px; padding: 10px 0; border-bottom: 1px solid rgba(15,17,22,0.06); font-size: 11px; color: rgba(15,17,22,0.55); text-transform: uppercase; letter-spacing: 0.06em; font-weight: 600;">
            <div>Mission</div>
            <div>Audience</div>
            <div>Fired</div>
            <div>Completed</div>
            <div>Cited</div>
          </div>
          <!-- Phase 10 Item #4: real deployments rendered here by JS.
               renderMetDeployments() is called on Mission tab entry + after
               each new fire. Falls back to placeholder text on empty/error. -->
          <div id="met-deployments-list">
            <div style="padding: 18px 0; font-size: 12px; color: rgba(15,17,22,0.55); text-align: center;">
              Loading deployments&hellip;
            </div>
          </div>
        </div>
      </div>

    </div>


    <!-- ── Tab: History — wide table layout ── -->
    <div class="met-panel" data-met-panel="history" style="display: none;">

      <!-- Phase 10 Met scheduling: scheduled messages section.
           Shows pending scheduled briefs/posts with edit/cancel. -->
      <div class="ws-tile ws-span-12" id="met-scheduled-tile" style="margin-bottom: 20px;">
        <div class="ws-tile-label">
          <span>Scheduled</span>
          <span class="ws-tile-label-detail">
            &middot; messages waiting to send
            &middot;
            <button type="button" id="met-scheduled-refresh"
                    style="background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.65);padding:3px 10px;border-radius:5px;font-size:11px;cursor:pointer;margin-left:8px;">Refresh</button>
          </span>
        </div>
        <div id="met-scheduled-list" style="margin-top: 14px;">
          <div style="padding: 20px 16px; text-align: center; color: rgba(15,17,22,0.50); font-size: 13px;">Loading&hellip;</div>
        </div>
      </div>

      <!-- Phase 10 Met tips: earnings summary tile -->
      <!-- Phase 10 Met monthly earnings (TIP-B + payroll work) -->
      <div class="ws-tile ws-span-12" style="margin-bottom: 20px;">
        <div class="ws-tile-label">
          <span>Earnings</span>
          <span class="ws-tile-label-detail">
            &middot; <span id="met-earn-month-label">this month</span>
            &middot;
            <button type="button" id="met-earn-refresh"
                    style="background:transparent;border:1px solid rgba(15,17,22,0.15);color:rgba(15,17,22,0.65);padding:3px 10px;border-radius:5px;font-size:11px;cursor:pointer;margin-left:8px;">Refresh</button>
          </span>
        </div>

        <!-- This Month + Last Month side-by-side -->
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px;">
          <!-- This month -->
          <div style="background:rgba(46,79,184,0.08);border:1px solid rgba(46,79,184,0.20);border-radius:10px;padding:18px 20px;">
            <div style="font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:rgba(15,17,22,0.55);margin-bottom:6px;">This month</div>
            <div style="font-size:28px;font-weight:600;color:#0E1116;line-height:1.1;" id="met-earn-this-total">$0.00</div>
            <div style="font-size:11px;color:rgba(15,17,22,0.65);margin-top:8px;line-height:1.5;">
              <div>$19 reviews: <span style="color:#0E1116;font-weight:600;" id="met-earn-this-reviews">$0.00</span></div>
              <div>Brief share: <span style="color:#0E1116;font-weight:600;" id="met-earn-this-briefs">$0.00</span></div>
              <div>Tips: <span style="color:#0E1116;font-weight:600;" id="met-earn-this-tips">$0.00</span></div>
            </div>
          </div>
          <!-- Last month — payday confirmation -->
          <div style="background:rgba(15,17,22,0.03);border:1px solid rgba(15,17,22,0.08);border-radius:10px;padding:18px 20px;">
            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">
              <div style="font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:rgba(15,17,22,0.55);">Last month</div>
              <span style="font-size:9.5px;color:#0c7a3c;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;background:rgba(18,161,80,0.10);padding:2px 7px;border-radius:4px;">Payday</span>
            </div>
            <div style="font-size:28px;font-weight:600;color:#0E1116;line-height:1.1;" id="met-earn-last-total">$0.00</div>
            <div style="font-size:11px;color:rgba(15,17,22,0.65);margin-top:8px;line-height:1.5;" id="met-earn-last-breakdown">
              <div>$19 reviews: <span style="color:#0E1116;font-weight:600;" id="met-earn-last-reviews">$0.00</span></div>
              <div>Brief share: <span style="color:#0E1116;font-weight:600;" id="met-earn-last-briefs">$0.00</span></div>
              <div>Tips: <span style="color:#0E1116;font-weight:600;" id="met-earn-last-tips">$0.00</span></div>
            </div>
            <div style="font-size:10.5px;color:rgba(15,17,22,0.50);margin-top:8px;font-style:italic;" id="met-earn-last-label">
              <!-- e.g. "April 2026 — confirm against pay stub" -->
            </div>
          </div>
        </div>

        <!-- Recent tips list (preserved from previous tile) -->
        <div id="met-tips-recent" style="margin-top:18px;">
          <!-- Recent tips populated by JS -->
        </div>
      </div>

      <div class="ws-tile ws-span-12">
        <div class="ws-tile-label">
          <span>Your last 10 reviews</span>
          <span class="ws-tile-label-detail">&middot; sortable filtering coming</span>
        </div>
        <div class="ws-history-table">
          <div class="ws-history-header">
            <div>When</div>
            <div>Customer</div>
            <div>Plan</div>
            <div>Verdict</div>
            <div>Helpful?</div>
            <div style="text-align:right;">Earned</div>
            <div style="text-align:center;">Action</div>
          </div>
          <div id="met-history-list">
            <!-- Populated by JS -->
          </div>
        </div>
      </div>
    </div>

    <!-- ── Tab: Map ──────────────────────────────────────────────
         The Met's geographic workspace. Phase 1 (current): a clean
         zoomable/draggable US map with state and county boundaries,
         centered on Indiana by default.

         Phase 2 (next session): NWS active warnings overlay.
         Phase 3 (session after): polygon-to-subscriber messaging.

         Leaflet + OpenStreetMap are loaded lazily — only when this
         tab is first clicked. Keeps the page fast for the 99% of
         visits that never open the map. -->
    <!-- ── Tab: Schedule (Phase 11, May 17) ──
         Met's coverage schedule. Three parts:
           1. Today's brief tasks — actionable list with "I'm on it"
           2. My weekly recurring shifts — what subscribers and pool
              they're claiming, by day of week
           3. Next 14 days resolved view — what they actually owe
                each upcoming day with override controls
         Loaded on first tab click via setupMetSchedule(). -->
    <div class="met-panel" data-met-panel="schedule" style="display: none;">

      <!-- Profile tile: Met's contact info. Phone is required for SMS
           reminders, severe-weather heads-ups, and proactive Rosie texts. -->
      <div class="ws-tile ws-span-12">
        <div class="ws-tile-label">My contact info</div>
        <div style="padding: 14px; display: grid; grid-template-columns: 1fr 1fr; gap: 16px; max-width: 720px;">
          <div>
            <label for="met-profile-name" style="display: block; font-size: 11px; font-weight: 600; color: rgba(15,17,22,0.55); margin-bottom: 4px;">Full name</label>
            <input type="text" id="met-profile-name" placeholder="Your full name"
                   style="width: 100%; padding: 8px 10px; border: 1px solid rgba(15,17,22,0.15); border-radius: 6px; font-size: 13px; font-family: inherit;">
          </div>
          <div>
            <label for="met-profile-phone" style="display: block; font-size: 11px; font-weight: 600; color: rgba(15,17,22,0.55); margin-bottom: 4px;">Mobile phone <span style="color: rgba(194,52,43,0.85);">*</span></label>
            <input type="tel" id="met-profile-phone" placeholder="(555) 123-4567"
                   style="width: 100%; padding: 8px 10px; border: 1px solid rgba(15,17,22,0.15); border-radius: 6px; font-size: 13px; font-family: inherit;">
          </div>
          <div style="grid-column: 1 / -1; display: flex; justify-content: space-between; align-items: center; gap: 12px;">
            <div id="met-profile-status" style="font-size: 11.5px; color: rgba(15,17,22,0.55);">Used for SMS reminders, severe weather heads-ups, and Rosie's texts.</div>
            <button type="button" id="met-profile-save-btn"
                    style="background: #2563eb; color: #fff; border: 0; border-radius: 6px; padding: 8px 16px; font-size: 12.5px; font-weight: 600; cursor: pointer;">Save</button>
          </div>
        </div>
      </div>

      <!-- Storm Shelter tile: open/manage emergency activations -->
      <div class="ws-tile ws-span-12" style="margin-top: 18px;">
        <div class="ws-tile-label" style="display: flex; align-items: center; justify-content: space-between;">
          <span>Storm Shelter activations <span style="font-size: 11px; font-weight: 400; color: rgba(15,17,22,0.55); margin-left: 8px;">&middot; $25 per activation when closed</span></span>
          <button type="button" id="met-shelter-open-btn"
                  style="background: #C2342B; border: 0; color: #fff; padding: 7px 14px; font-size: 12px; font-weight: 600; border-radius: 6px; cursor: pointer;">
            ⚡ Open new activation
          </button>
        </div>
        <div id="met-shelter-list" style="margin-top: 12px;">
          <div style="font-size: 12.5px; color: rgba(15,17,22,0.55); padding: 12px;">Loading…</div>
        </div>
      </div>

      <div class="ws-tile ws-span-12" style="margin-top: 18px;">
        <div class="ws-tile-label">
          <span>Today's brief tasks</span>
          <span class="ws-tile-label-detail">&middot; what you owe right now &middot; refreshes every minute</span>
        </div>
        <div id="met-tasks-list" style="margin-top: 12px;">
          <div style="font-size: 13px; color: rgba(15,17,22,0.55); padding: 16px;">Loading tasks…</div>
        </div>
      </div>

      <div class="ws-tile ws-span-12" style="margin-top: 18px;">
        <div class="ws-tile-label">
          <span>My weekly schedule</span>
          <span class="ws-tile-label-detail">&middot; recurring shifts &middot; covers every week unless you drop a day</span>
        </div>
        <p style="font-size: 13px; color: rgba(15,17,22,0.65); margin: 6px 0 14px; line-height: 1.5;">
          Sign up to cover your subscribers' daily briefs, cover another Met's subscribers, or
          claim Review Pool ($19 reviews). You can drop specific days below.
        </p>

        <div id="met-schedule-recurring-grid" style="overflow-x: auto;">
          <!-- Filled by JS: 7-column grid (Sun-Sat) showing claimed scopes per day -->
          <div style="font-size: 13px; color: rgba(15,17,22,0.55); padding: 16px;">Loading your schedule…</div>
        </div>

        <div style="margin-top: 14px; padding-top: 14px; border-top: 1px solid rgba(15,17,22,0.06);">
          <button type="button" id="met-schedule-add-shift-btn"
                  style="background: #2E4FB8; border: 0; color: #fff; padding: 9px 16px; font-size: 13px; font-weight: 600; border-radius: 8px; cursor: pointer;">
            + Add recurring shift
          </button>
        </div>
      </div>

      <div class="ws-tile ws-span-12" style="margin-top: 18px;">
        <div class="ws-tile-label">
          <span>Next 14 days</span>
          <span class="ws-tile-label-detail">&middot; what you actually owe &middot; drop or claim individual days</span>
        </div>

        <div id="met-schedule-upcoming-list" style="margin-top: 12px;">
          <div style="font-size: 13px; color: rgba(15,17,22,0.55); padding: 16px;">Loading…</div>
        </div>
      </div>
    </div>

    <!-- Add Recurring Shift modal -->
    <div id="met-add-shift-modal" class="met-post-modal" hidden>
      <div class="met-post-modal-scrim" id="met-add-shift-scrim"></div>
      <div class="met-post-modal-card" role="dialog" aria-modal="true" style="max-width: 480px;">
        <div class="met-post-modal-head">
          <h2 class="met-post-modal-title">Add recurring shift</h2>
          <button type="button" class="met-post-modal-close" id="met-add-shift-close" aria-label="Close">
            <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
          </button>
        </div>
        <div class="met-post-modal-body">
          <p style="font-size: 12.5px; color: rgba(15,17,22,0.65); margin: 0 0 16px; line-height: 1.55;">
            Pick a day and what you're covering. This repeats every week.
          </p>

          <div style="margin-bottom: 14px;">
            <label style="display: block; font-size: 11.5px; font-weight: 700; color: rgba(15,17,22,0.55); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 6px;">Day of week *</label>
            <select id="met-add-shift-day" style="width: 100%; padding: 9px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; font-size: 14px; font-family: inherit;">
              <option value="0">Sunday</option>
              <option value="1">Monday</option>
              <option value="2">Tuesday</option>
              <option value="3">Wednesday</option>
              <option value="4">Thursday</option>
              <option value="5">Friday</option>
              <option value="6">Saturday</option>
            </select>
          </div>

          <div style="margin-bottom: 14px;">
            <label style="display: block; font-size: 11.5px; font-weight: 700; color: rgba(15,17,22,0.55); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 6px;">What are you covering? *</label>
            <div style="display: flex; flex-direction: column; gap: 8px;">
              <label style="display: flex; align-items: center; gap: 8px; padding: 10px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; cursor: pointer; font-size: 13.5px;">
                <input type="radio" name="met-add-shift-scope" value="review_pool" checked>
                <div>
                  <div style="font-weight: 600; color: #0E1116;">Review Pool</div>
                  <div style="font-size: 11.5px; color: rgba(15,17,22,0.55);">National $19 review requests</div>
                </div>
              </label>
              <label style="display: flex; align-items: center; gap: 8px; padding: 10px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; cursor: pointer; font-size: 13.5px;">
                <input type="radio" name="met-add-shift-scope" value="subscriber_set">
                <div>
                  <div style="font-weight: 600; color: #0E1116;">All of another Met's subscribers</div>
                  <div style="font-size: 11.5px; color: rgba(15,17,22,0.55);">Cover someone's whole region for a day</div>
                </div>
              </label>
              <label style="display: flex; align-items: center; gap: 8px; padding: 10px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; cursor: pointer; font-size: 13.5px;">
                <input type="radio" name="met-add-shift-scope" value="subscriber">
                <div>
                  <div style="font-weight: 600; color: #0E1116;">A specific subscriber</div>
                  <div style="font-size: 11.5px; color: rgba(15,17,22,0.55);">Narrow coverage for one customer</div>
                </div>
              </label>
            </div>
          </div>

          <div id="met-add-shift-set-owner-wrap" hidden style="margin-bottom: 14px;">
            <label style="display: block; font-size: 11.5px; font-weight: 700; color: rgba(15,17,22,0.55); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 6px;">Whose subscribers?</label>
            <select id="met-add-shift-set-owner" style="width: 100%; padding: 9px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; font-size: 14px; font-family: inherit;">
              <option value="">Select a Met…</option>
            </select>
          </div>

          <div id="met-add-shift-subscriber-wrap" hidden style="margin-bottom: 14px;">
            <label style="display: block; font-size: 11.5px; font-weight: 700; color: rgba(15,17,22,0.55); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 6px;">Which subscriber?</label>
            <select id="met-add-shift-subscriber" style="width: 100%; padding: 9px 12px; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; font-size: 14px; font-family: inherit;">
              <option value="">Select a subscriber…</option>
            </select>
          </div>

          <p id="met-add-shift-error" hidden style="background: rgba(194,52,43,0.08); border: 1px solid rgba(194,52,43,0.20); border-radius: 8px; padding: 10px 12px; font-size: 12.5px; color: #9b2823; margin: 0 0 14px;"></p>

          <div style="display: flex; justify-content: flex-end; gap: 10px;">
            <button type="button" id="met-add-shift-cancel"
                    style="padding: 11px 18px; background: transparent; border: 1px solid rgba(15,17,22,0.15); color: rgba(15,17,22,0.65); border-radius: 8px; font-size: 13px; cursor: pointer;">Cancel</button>
            <button type="button" id="met-add-shift-save"
                    style="padding: 11px 22px; background: #2E4FB8; border: none; color: #fff; border-radius: 8px; font-size: 13px; font-weight: 600; cursor: pointer;">Add shift</button>
          </div>
        </div>
      </div>
    </div>

    <!-- ── Tab: Map (existing) ──
         Leaflet + OpenStreetMap are loaded lazily — only when this
         tab is first clicked. Keeps the page fast for the 99% of
         visits that never open the map. -->
    <div class="met-panel" data-met-panel="map" style="display: none;">
      <div class="ws-tile ws-span-12">
        <div class="ws-tile-label">
          <span>Coverage map</span>
          <span class="ws-tile-label-detail">&middot; scroll to zoom &middot; click and drag to pan</span>
        </div>

        <!-- Loading state — shown while Leaflet + boundary data load.
             Replaced by the live map once met-map.js fires the ready event. -->
        <div class="met-map-loading" id="met-map-loading">
          <div class="met-map-loading-dot"></div>
          <span>Loading the map…</span>
        </div>

        <!-- The Leaflet container. JS attaches the map instance to this div. -->
        <div class="met-map-container" id="met-map-container"></div>

        <!-- Legend / controls footer
             Row 1: boundary line legend (static info)
             Row 2: NWS alert toggles + live counts + refresh button -->
        <div class="met-map-footer">
          <span class="met-map-footer-item">
            <span class="met-map-footer-dot" style="background: #4169E1;"></span>
            State boundaries
          </span>
          <span class="met-map-footer-item">
            <span class="met-map-footer-dot" style="background: rgba(65, 105, 225, 0.45);"></span>
            County boundaries (zoom in to see)
          </span>
        </div>

        <div class="met-map-alerts-bar">
          <div class="met-map-alerts-bar-left">
            <span class="met-map-alerts-label">NWS alerts</span>

            <div style="position: relative; display: inline-flex; align-items: center;">
              <label class="met-map-toggle" data-alert-category="warning">
                <input type="checkbox" checked>
                <span class="met-map-toggle-dot" style="background: #C2342B;"></span>
                <span class="met-map-toggle-label">Warnings</span>
                <span class="met-map-toggle-count" id="met-alert-count-warning">—</span>
              </label>
              <button type="button" class="met-map-filter-caret" data-filter-toggle="warning"
                      title="Filter which warnings to show"
                      aria-label="Filter warnings"
                      style="background: transparent; border: 0; padding: 4px 6px; cursor: pointer; color: rgba(15,17,22,0.50); display: inline-flex; align-items: center;">
                <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
              </button>
            </div>

            <div style="position: relative; display: inline-flex; align-items: center;">
              <label class="met-map-toggle" data-alert-category="watch">
                <input type="checkbox" checked>
                <span class="met-map-toggle-dot" style="background: #F5A524;"></span>
                <span class="met-map-toggle-label">Watches</span>
                <span class="met-map-toggle-count" id="met-alert-count-watch">—</span>
              </label>
              <button type="button" class="met-map-filter-caret" data-filter-toggle="watch"
                      title="Filter which watches to show"
                      aria-label="Filter watches"
                      style="background: transparent; border: 0; padding: 4px 6px; cursor: pointer; color: rgba(15,17,22,0.50); display: inline-flex; align-items: center;">
                <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
              </button>
            </div>

            <div style="position: relative; display: inline-flex; align-items: center;">
              <label class="met-map-toggle" data-alert-category="advisory">
                <input type="checkbox" checked>
                <span class="met-map-toggle-dot" style="background: #4169E1;"></span>
                <span class="met-map-toggle-label">Advisories</span>
                <span class="met-map-toggle-count" id="met-alert-count-advisory">—</span>
              </label>
              <button type="button" class="met-map-filter-caret" data-filter-toggle="advisory"
                      title="Filter which advisories to show"
                      aria-label="Filter advisories"
                      style="background: transparent; border: 0; padding: 4px 6px; cursor: pointer; color: rgba(15,17,22,0.50); display: inline-flex; align-items: center;">
                <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
              </button>
            </div>

            <!-- Radar toggle -->
            <label class="met-map-toggle" data-radar-toggle style="margin-left: 6px;">
              <input type="checkbox" id="met-map-radar-checkbox">
              <span class="met-map-toggle-dot" style="background: linear-gradient(90deg, #00ff00, #ffff00, #ff0000); border-radius: 2px;"></span>
              <span class="met-map-toggle-label">Radar</span>
            </label>
          </div>

          <div class="met-map-alerts-bar-right">
            <span class="met-map-alerts-updated" id="met-alert-updated">Loading…</span>
            <button type="button" class="met-map-refresh-btn" id="met-map-refresh-btn" title="Fetch the latest alerts now">
              <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="23 4 23 10 17 10"/>
                <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/>
              </svg>
              <span>Refresh</span>
            </button>
          </div>
        </div>

        <!-- Per-event-type filter dropdown (single dropdown, repositioned
             when each caret is clicked). Hidden by default. -->
        <div id="met-event-filter-dropdown" hidden
             style="position: absolute; z-index: 1000; background: #fff; border: 1px solid rgba(15,17,22,0.15); border-radius: 8px; box-shadow: 0 8px 24px rgba(15,17,22,0.12); min-width: 260px; max-width: 320px; max-height: 360px; overflow-y: auto; padding: 8px 0;">
          <div id="met-event-filter-header" style="padding: 8px 14px 6px; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; color: rgba(15,17,22,0.55); border-bottom: 1px solid rgba(15,17,22,0.06); margin-bottom: 4px;">
            Filter warnings
          </div>
          <div id="met-event-filter-list">
            <!-- Populated by JS -->
          </div>
        </div>

        <!-- Zone-based alerts (no polygon geometry; mostly watches).
             NWS issues some alerts by county-zone code rather than as a
             drawn polygon. We can't render them on the map without a
             5MB zone shapefile, but we can list them so Mets know they
             exist. Collapsible to avoid taking up space when empty. -->
        <details id="met-zone-alerts-details" style="background: rgba(15,17,22,0.03); border-top: 1px solid rgba(15,17,22,0.06); padding: 0; margin: 0;" hidden>
          <summary style="padding: 10px 16px; cursor: pointer; font-size: 12px; color: rgba(15,17,22,0.75); user-select: none;">
            <strong id="met-zone-alerts-count">0</strong> alerts without map polygons
            <span style="color: rgba(15,17,22,0.50); font-weight: 400;">— mostly county-level watches. Tap to view list.</span>
          </summary>
          <div id="met-zone-alerts-list" style="padding: 4px 16px 14px; max-height: 280px; overflow-y: auto;">
            <!-- Filled by JS -->
          </div>
        </details>
      </div>
    </div>

  </div>
</div>



<!-- ============ VALET CREW WORKSPACE (operational view) ============ -->
<!-- This is the logged-in Crew member's operational zone. Different from
     screen-crew-profile (which is the customer-facing "Join the Crew"
     marketing surface). This screen shows when a Crew member is signed
     in and ready to receive missions.

     Mission-first design: the centerpiece is the active mission card.
     Around it sit the supporting tiles (status, check-in, contributions,
     nearby Crew). The whole zone shifts visual mode when a severe-weather
     mission activates — same screen, different posture.

     States the JS toggles between:
       1. routine — Mesonet calibration mission active (default)
       2. severe  — Ground Truth Recon active (urgent weather)
       3. complete — Confirmation after mission submitted
       4. idle    — No mission available right now -->
<!-- ============ VALET CREW WORKSPACE (operational view) ============ -->
<!-- Mission Impossible briefing pattern: missions are tucked behind a
     button by default. Tap the button to deploy (expand inline). The
     check-in is the first thing a Crew member does each session;
     missions come second, available work comes third.

     Structure:
       Row 1: Top stats strip (operator status pills + receipts count)
       Row 2: Daily check-in (4 condition tiles + streak)
       Row 3: Available missions (collapsed buttons; tap to deploy)
       Row 4: Active mission body (hidden until a button is deployed)
       Row 5: Operator status + Crew nearby (side-by-side as before)
       Row 6: "What your work did" receipts (with citation badges
              and Second-this affordances)

     Severe mode auto-expands the active mission body and keeps it
     pinned. Routine missions stay collapsed unless deployed. -->
<div id="screen-crew-ws" class="screen crew-ws-screen" data-crew-state="routine">
  <div class="sim-banner" style="background: linear-gradient(180deg, rgba(245, 165, 36, 0.20), rgba(245, 165, 36, 0.08)); border-bottom-color: rgba(245, 165, 36, 0.30);">
    <span class="sim-banner-strong" style="color: #F5A524;">Prototype Simulation</span> &middot;
    The real Crew workspace will be wired to live mission triggers, photo upload, and NWS alerts
  </div>

  <!-- ── Live strip ──────────────────────────────────────
       A thin band that signals "this page is live, this system
       is breathing." Pulsing red LIVE dot on the left, today's
       date and a short tagline center, "X watching" counter
       on the right (real subscribers viewing the Live Feed in
       this region). In severe mode the whole strip turns red. -->
  <div class="crew-ws-episode">
    <span class="crew-ws-episode-rec">
      <span class="crew-ws-episode-rec-dot"></span>
      LIVE
    </span>
    <span class="crew-ws-episode-label" id="crew-ws-episode-label">
      <strong id="crew-ws-episode-date">Wednesday, May 13</strong>
      <span class="crew-ws-episode-sep">&middot;</span>
      <span id="crew-ws-episode-tagline">the calm before something</span>
    </span>
    <span class="crew-ws-episode-watchers" id="crew-ws-episode-watchers">
      <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
        <circle cx="12" cy="12" r="3"/>
      </svg>
      <span id="crew-ws-watchers-count">340</span>
      <span class="crew-ws-episode-watchers-label">watching</span>
    </span>
  </div>

  <!-- ── Header strip ─────────────────────────────────── -->
  <div class="crew-ws-header">
    <div class="crew-ws-header-top">
      <div class="crew-ws-brand">
        <span class="crew-ws-eyebrow">Valet Crew</span>
        <h1 class="crew-ws-title" id="crew-ws-title">Operator: K.C. Thomas</h1>
      </div>
      <div class="crew-ws-meta">
        <div class="crew-ws-meta-item">
          <span class="crew-ws-meta-label">Station:</span>
          <span class="crew-ws-meta-value">Lebanon, IN</span>
        </div>
        <div class="crew-ws-meta-item">
          <span class="crew-ws-meta-label">Local:</span>
          <span class="crew-ws-meta-value" id="crew-ws-conditions">62&deg;F, light wind</span>
        </div>
        <div class="crew-ws-meta-item">
          <span class="crew-ws-operator-dot"></span>
          <span class="crew-ws-meta-value" id="crew-ws-operator-status">On station</span>
        </div>
        <button type="button" class="ws-exit-btn" data-ws-exit="crew" aria-label="Exit workspace">
          <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
            <polyline points="16 17 21 12 16 7"/>
            <line x1="21" y1="12" x2="9" y2="12"/>
          </svg>
          <span>Exit to search</span>
        </button>
      </div>
    </div>
  </div>

  <!-- ── Body grid ──────────────────────────────────── -->
  <div class="crew-ws-body">

    <!-- ════════════════════════════════════════════════
         ROW 1: Daily check-in (the first thing they do)
         ════════════════════════════════════════════════ -->
    <div class="ws-section-eyebrow" style="color: rgba(232, 235, 240, 0.55);">
      <span>First check-in</span>
      <span class="ws-section-eyebrow-detail" style="color: rgba(232, 235, 240, 0.40);">what are you seeing out there right now?</span>
    </div>
    <div class="crew-checkin-row">
      <div class="crew-tile">
        <div class="crew-checkin">
          <div class="crew-checkin-options" id="crew-checkin-options">
            <button type="button" class="crew-checkin-btn" data-checkin="clear">
              <span class="crew-checkin-icon">☀</span>
              <span class="crew-checkin-label">Clear</span>
            </button>
            <button type="button" class="crew-checkin-btn" data-checkin="cloudy">
              <span class="crew-checkin-icon">☁</span>
              <span class="crew-checkin-label">Cloudy</span>
            </button>
            <button type="button" class="crew-checkin-btn" data-checkin="rain">
              <span class="crew-checkin-icon">☂</span>
              <span class="crew-checkin-label">Raining</span>
            </button>
            <button type="button" class="crew-checkin-btn" data-checkin="other">
              <span class="crew-checkin-icon">⊕</span>
              <span class="crew-checkin-label">Other</span>
            </button>
          </div>
          <!-- Other-conditions expansion: opens when the Crew member taps
               "Other" above. Ten spotter conditions Mets actually use.
               Each adds high-value signal that the four primary buttons
               can't capture (drizzle vs heavy rain matters; fog vs haze
               is actionable for visibility decisions; hail and thunder
               drive severe-weather flags). -->
          <div class="crew-checkin-other" id="crew-checkin-other">
            <div class="crew-checkin-other-eyebrow">
              <span>Specific conditions</span>
              <span>tap what you're seeing</span>
            </div>
            <div class="crew-checkin-other-grid">
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="drizzle">
                <span class="crew-checkin-other-icon">⋮</span>
                <span class="crew-checkin-other-label">Drizzle</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="heavy-rain">
                <span class="crew-checkin-other-icon">☔</span>
                <span class="crew-checkin-other-label">Heavy rain</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="snow">
                <span class="crew-checkin-other-icon">❄</span>
                <span class="crew-checkin-other-label">Snow</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="sleet">
                <span class="crew-checkin-other-icon">❅</span>
                <span class="crew-checkin-other-label">Sleet / freezing</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="fog">
                <span class="crew-checkin-other-icon">▓</span>
                <span class="crew-checkin-other-label">Fog</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="haze">
                <span class="crew-checkin-other-icon">▒</span>
                <span class="crew-checkin-other-label">Smoke / haze</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="hail">
                <span class="crew-checkin-other-icon">◉</span>
                <span class="crew-checkin-other-label">Hail</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="thunder">
                <span class="crew-checkin-other-icon">⚡</span>
                <span class="crew-checkin-other-label">Thunder/lightning</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="high-wind">
                <span class="crew-checkin-other-icon">⤳</span>
                <span class="crew-checkin-other-label">High wind</span>
              </button>
              <button type="button" class="crew-checkin-other-btn" data-checkin-other="dust">
                <span class="crew-checkin-other-icon">░</span>
                <span class="crew-checkin-other-label">Dust</span>
              </button>
            </div>
          </div>
          <div class="crew-conditions" style="margin-top: 12px;">
            <div class="crew-condition-cell">
              <p class="crew-condition-label">Temp</p>
              <p class="crew-condition-value">62&deg;F</p>
            </div>
            <div class="crew-condition-cell">
              <p class="crew-condition-label">Wind</p>
              <p class="crew-condition-value">8 SW</p>
            </div>
            <div class="crew-condition-cell">
              <p class="crew-condition-label">Sky</p>
              <p class="crew-condition-value">Mostly Clear</p>
            </div>
            <div class="crew-condition-cell">
              <p class="crew-condition-label">Vis</p>
              <p class="crew-condition-value">10+ mi</p>
            </div>
          </div>
        </div>
      </div>
      <div class="crew-checkin-streak-tile">
        <div class="crew-checkin-streak-flame">
          <svg viewBox="0 0 24 24" width="22" height="22" fill="currentColor">
            <path d="M13.5.67s.74 2.65.74 4.8c0 2.06-1.35 3.73-3.41 3.73-2.07 0-3.63-1.67-3.63-3.73l.03-.36C5.21 7.51 4 10.62 4 14c0 4.42 3.58 8 8 8s8-3.58 8-8C20 8.61 17.41 3.8 13.5.67zM11.71 19c-1.78 0-3.22-1.4-3.22-3.14 0-1.62 1.05-2.76 2.81-3.12 1.77-.36 3.6-1.21 4.62-2.58.39 1.29.59 2.65.59 4.04 0 2.65-2.15 4.8-4.8 4.8z"/>
          </svg>
        </div>
        <div class="crew-checkin-streak-info">
          <div>
            <span class="crew-checkin-streak-num">47</span><span class="crew-checkin-streak-num-unit">days</span>
          </div>
          <p class="crew-checkin-streak-label">consecutive check-ins</p>
        </div>
      </div>
    </div>

    <!-- ════════════════════════════════════════════════
         ROW 2: Available missions (collapsed buttons)
         JS renders the mission buttons here.
         ════════════════════════════════════════════════ -->
    <div class="ws-section-eyebrow" style="color: rgba(232, 235, 240, 0.55);">
      <span>The Meteorologist is calling you in</span>
      <span class="ws-section-eyebrow-detail" id="crew-mission-count-text" style="color: rgba(232, 235, 240, 0.40);">2 missions live &middot; tap to take one</span>
    </div>
    <div class="crew-mission-list" id="crew-mission-list">
      <!-- Populated by JS — mission buttons rendered here -->
    </div>

    <!-- ════════════════════════════════════════════════
         ROW 3: Active mission body (hidden until deployed)
         When a Crew member taps a mission button, the full
         briefing expands here. Severe missions auto-expand.
         ════════════════════════════════════════════════ -->
    <div class="crew-mission is-active" id="crew-mission-card" hidden>
      <!-- Mission body rendered by JS via renderCrewMission() -->
    </div>

    <!-- ════════════════════════════════════════════════
         ROW 4: Operator status + Crew nearby
         ════════════════════════════════════════════════ -->
    <div class="ws-section-eyebrow" style="color: rgba(232, 235, 240, 0.55);">
      <span>Crew on the ground near you</span>
      <span class="ws-section-eyebrow-detail" style="color: rgba(232, 235, 240, 0.40);">we, not me</span>
    </div>
    <div class="crew-tile crew-span-6">
      <div class="crew-tile-label">
        <span>Operator status</span>
      </div>
      <div class="crew-status-grid">
        <div class="crew-status-rank">
          <div class="crew-status-rank-badge">
            <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
            </svg>
          </div>
          <div class="crew-status-rank-info">
            <h3 class="crew-status-rank-name">Spotter, 2nd Class</h3>
            <p class="crew-status-rank-detail">Earned by 47 verified reports</p>
          </div>
        </div>
        <div>
          <p class="crew-status-stat-label">Reports verified</p>
          <p class="crew-status-stat-value">47</p>
          <p class="crew-status-stat-context">12 used by Mets</p>
        </div>
        <div>
          <p class="crew-status-stat-label">Days active</p>
          <p class="crew-status-stat-value">112<span class="crew-status-stat-value-unit">d</span></p>
          <p class="crew-status-stat-context">since June 2024</p>
        </div>
        <div class="crew-status-specialty">
          <svg class="crew-status-specialty-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 17l6-6 4 4 8-8"/>
            <polyline points="14 7 21 7 21 14"/>
          </svg>
          <span>Specialty: <strong>Wind &amp; surface conditions</strong></span>
        </div>
      </div>
    </div>
    <div class="crew-tile crew-span-6">
      <div class="crew-tile-label">
        <span>Crew nearby</span>
        <span class="crew-tile-label-detail">&middot; in your county</span>
      </div>
      <div class="crew-nearby-list">
        <div class="crew-nearby-row">
          <div class="crew-nearby-avatar">MR</div>
          <div class="crew-nearby-info">
            <p class="crew-nearby-name">Maria Reyes</p>
            <p class="crew-nearby-detail">Boone County &middot; 0.6 mi away</p>
          </div>
          <span class="crew-nearby-status is-on-mission">On Mission</span>
        </div>
        <div class="crew-nearby-row">
          <div class="crew-nearby-avatar">DT</div>
          <div class="crew-nearby-info">
            <p class="crew-nearby-name">Daniel T.</p>
            <p class="crew-nearby-detail">Zionsville &middot; 4.2 mi away</p>
          </div>
          <span class="crew-nearby-status is-active">Active</span>
        </div>
        <div class="crew-nearby-row">
          <div class="crew-nearby-avatar">SK</div>
          <div class="crew-nearby-info">
            <p class="crew-nearby-name">Sarah K.</p>
            <p class="crew-nearby-detail">Whitestown &middot; 5.8 mi away</p>
          </div>
          <span class="crew-nearby-status is-active">Active</span>
        </div>
        <div class="crew-nearby-row">
          <div class="crew-nearby-avatar">JM</div>
          <div class="crew-nearby-info">
            <p class="crew-nearby-name">Jordan M.</p>
            <p class="crew-nearby-detail">Lebanon &middot; 1.1 mi away</p>
          </div>
          <span class="crew-nearby-status is-idle">Idle 18m</span>
        </div>
      </div>
    </div>

    <!-- ════════════════════════════════════════════════
         ROW 5: Contribution receipts with citations
         ════════════════════════════════════════════════ -->
    <div class="ws-section-eyebrow" style="color: rgba(232, 235, 240, 0.55);">
      <span>Where your work made the call</span>
      <span class="ws-section-eyebrow-detail" style="color: rgba(232, 235, 240, 0.40);">recent impact &middot; real attribution</span>
    </div>
    <div class="crew-tile crew-span-12" id="crew-receipts-tile">
      <div class="crew-receipts" id="crew-receipts-list">
        <!-- Populated by JS — receipts rendered here -->
      </div>
    </div>

    <!-- Mission complete confirmation (hidden by default) -->
    <div class="crew-tile crew-span-12" id="crew-mission-complete-card" hidden>
      <div class="crew-mission-complete">
        <div class="crew-mission-complete-icon">
          <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
        </div>
        <h2 class="crew-mission-complete-headline" id="crew-complete-headline">You're on the record.</h2>
        <p class="crew-mission-complete-detail" id="crew-complete-detail">
          The Meteorologist working your county has your reading on screen. If it shapes their next call,
          you'll see your name on the brief and a notification on your phone.
        </p>
        <span class="crew-mission-complete-receipt" id="crew-complete-receipt">
          Mission #4582 &middot; Auto-assigned &middot; Submitted at <span id="crew-complete-time">—</span>
        </span>

        <!-- ── Post-submit confessional ────────────────────────
             Optional. Crew member's reading is already on the
             Meteorologist's screen by the time they see this — the
             note is bonus texture. Asking AFTER the action means
             memory is freshest and there's no pressure to write
             before answering. Dismissable; auto-attaches to the
             report they just submitted. -->
        <div class="crew-confessional" id="crew-confessional" hidden>
          <label for="crew-confessional-input" class="crew-confessional-label">
            Add a note while it&rsquo;s fresh
            <span class="crew-confessional-sublabel">
              The Meteorologists will see this with your reading.
            </span>
          </label>
          <textarea
            id="crew-confessional-input"
            class="crew-confessional-input"
            rows="3"
            placeholder="What would you like to tell the WeatherValet Meteorologists? Sounds, feel, what stood out &mdash; anything the numbers won&rsquo;t capture."
            maxlength="600"
          ></textarea>
          <div class="crew-confessional-footer">
            <label class="crew-confessional-quote">
              <input type="checkbox" id="crew-confessional-quote">
              <span>WeatherValet can quote this on social media</span>
            </label>
            <span class="crew-confessional-count">
              <span id="crew-confessional-count-num">0</span> / 600
            </span>
          </div>
          <div class="crew-confessional-actions">
            <button type="button" class="crew-confessional-skip" id="crew-confessional-skip">No note this time</button>
            <button type="button" class="crew-confessional-save" id="crew-confessional-save">Save note</button>
          </div>
        </div>
        <div class="crew-confessional-saved" id="crew-confessional-saved" hidden>
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
          <span>Note saved to your field notebook.</span>
        </div>
      </div>
    </div>

  </div>

  <!-- Met-citation toast — slides in from top-right when a Met uses
       a Crew member's report. Auto-dismisses after 7 seconds; can
       also be dismissed via the X button.
       Production: same payload also fires a Twilio SMS. -->
  <div class="crew-citation-toast" id="crew-citation-toast" role="status" aria-live="polite">
    <button type="button" class="crew-citation-toast-close" id="crew-citation-toast-close" aria-label="Dismiss">×</button>
    <div class="crew-citation-toast-eyebrow">
      <span class="crew-citation-toast-eyebrow-dot"></span>
      <span>Cited just now</span>
    </div>
    <p class="crew-citation-toast-headline" id="crew-citation-toast-headline">
      <strong>M. Reynolds</strong> just used your fog report.
    </p>
    <p class="crew-citation-toast-detail" id="crew-citation-toast-detail">
      Incorporated into the 6 AM brief for Boone County. Your reading helped 8 morning commuters
      make a safer decision.
    </p>
    <p class="crew-citation-toast-meta" id="crew-citation-toast-meta">
      Mission #4471 &middot; tap to view in your contribution log
    </p>
  </div>
</div>



<!-- ============ ADMIN VIEW (simulated) ============ -->
<!-- Mockup of the admin dashboard. Real version lives in Flask. -->
<div id="screen-admin" class="screen admin-screen cmd-center">
  <div class="sim-banner">
    <span class="sim-banner-strong">Prototype Simulation</span> &middot;
    The real command center will live in the deployed backend
  </div>

  <!-- ── Header strip — operator name, timestamp, live indicator ── -->
  <div class="cmd-header">
    <div class="cmd-header-brand">
      <span class="cmd-header-eyebrow">WeatherValet Command</span>
      <h1 class="cmd-header-title" id="cmd-header-title">Operator: Michael</h1>
    </div>
    <div class="cmd-header-meta">
      <div class="cmd-header-meta-item">
        <span class="cmd-header-meta-label">Watch:</span>
        <span class="cmd-header-meta-value" id="cmd-header-watch">Saturday afternoon</span>
      </div>
      <div class="cmd-header-meta-item">
        <span class="cmd-header-meta-label">Local:</span>
        <span class="cmd-header-meta-value" id="cmd-header-time">14:47 ET</span>
      </div>
      <div class="cmd-header-meta-item">
        <span class="cmd-live-dot"></span>
        <span class="cmd-header-meta-value">Live</span>
      </div>
      <button type="button" class="cmd-exit-btn" data-show-screen="screen-welcome"
              aria-label="Exit command center">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>
          <polyline points="16 17 21 12 16 7"/>
          <line x1="21" y1="12" x2="9" y2="12"/>
        </svg>
        Exit
      </button>
    </div>
  </div>

  <!-- ── Tab strip — Dashboard / Team ──────────────────────────
       The Team tab houses Met account management (add / edit /
       delete / reset password). Dashboard is the existing
       command-center grid. -->
  <div class="cmd-tabs">
    <button type="button" class="cmd-tab is-active" data-cmd-tab="dashboard">Dashboard</button>
    <button type="button" class="cmd-tab" data-cmd-tab="team">Team</button>
    <button type="button" class="cmd-tab" data-cmd-tab="operations">Operations</button>
  </div>

  <!-- ── Dashboard panel — the existing command-center grid ── -->
  <div class="cmd-tab-panel is-active" data-cmd-panel="dashboard">

  <!-- ── The grid canvas ─────────────────────────────────────── -->
  <div class="cmd-body">

    <!-- Row 1: vitals — five small metric tiles across the top -->
    <div class="cmd-tile span-2">
      <div class="cmd-tile-label">Revenue today</div>
      <div class="cmd-metric-value">$248<span class="cmd-metric-value-unit">.00</span></div>
      <div class="cmd-metric-delta delta-up">&uarr; $42 vs yesterday</div>
    </div>
    <div class="cmd-tile span-2">
      <div class="cmd-tile-label">Reviews delivered</div>
      <div class="cmd-metric-value">12</div>
      <div class="cmd-metric-delta delta-up">&uarr; 3 vs yesterday</div>
    </div>
    <div class="cmd-tile span-2">
      <div class="cmd-tile-label">Active mets</div>
      <div class="cmd-metric-value">2<span class="cmd-metric-value-unit">/3</span></div>
      <div class="cmd-metric-context">Albertson on call</div>
    </div>
    <div class="cmd-tile span-2">
      <div class="cmd-tile-label">Met response</div>
      <div class="cmd-metric-value">47<span class="cmd-metric-value-unit">s</span></div>
      <div class="cmd-metric-delta delta-up">&darr; 8s vs yesterday</div>
    </div>
    <div class="cmd-tile span-2">
      <div class="cmd-tile-label">Crew growth</div>
      <div class="cmd-metric-value">+8</div>
      <div class="cmd-metric-delta delta-up">&uarr; 18% w/w</div>
    </div>
    <div class="cmd-tile span-2">
      <div class="cmd-tile-label">Avg confidence</div>
      <div class="cmd-metric-value">91<span class="cmd-metric-value-unit">%</span></div>
      <div class="cmd-metric-context">post-Met verdict</div>
    </div>

    <!-- Row 2: review queue (left, wide) + Met activity (right) -->
    <div class="cmd-tile span-7">
      <div class="cmd-tile-label">
        <span>Review queue</span>
        <span class="cmd-tile-label-detail">&middot; 3 pending &middot; 1 claimed</span>
      </div>
      <div class="cmd-queue-list">
        <div class="cmd-queue-row">
          <span class="cmd-queue-tier tier-pro">Pro</span>
          <span class="cmd-queue-plan">Concrete pour 9 AM &mdash; Bell Concrete</span>
          <span class="cmd-queue-met">unclaimed</span>
          <span class="cmd-queue-status status-pending">Pending</span>
        </div>
        <div class="cmd-queue-row">
          <span class="cmd-queue-tier tier-single">Single</span>
          <span class="cmd-queue-plan">Outdoor wedding 4 PM &mdash; R. Greene</span>
          <span class="cmd-queue-met">unclaimed</span>
          <span class="cmd-queue-status status-pending">Pending</span>
        </div>
        <div class="cmd-queue-row">
          <span class="cmd-queue-tier tier-day">Day pass</span>
          <span class="cmd-queue-plan">Roofing job tomorrow &mdash; Westfield</span>
          <span class="cmd-queue-met">unclaimed</span>
          <span class="cmd-queue-status status-pending">Pending</span>
        </div>
        <div class="cmd-queue-row">
          <span class="cmd-queue-tier tier-single">Single</span>
          <span class="cmd-queue-plan">Bike ride 5 PM &mdash; J. Holloway</span>
          <span class="cmd-queue-met">M. Reynolds</span>
          <span class="cmd-queue-status status-claimed">Claimed</span>
        </div>
        <div class="cmd-queue-row">
          <span class="cmd-queue-tier tier-single">Single</span>
          <span class="cmd-queue-plan">Concrete pour 7 AM &mdash; Patel</span>
          <span class="cmd-queue-met">M. Reynolds</span>
          <span class="cmd-queue-status status-delivered">Delivered</span>
        </div>
      </div>
    </div>
    <div class="cmd-tile span-5">
      <div class="cmd-tile-label">
        <span>Met activity</span>
        <span class="cmd-tile-label-detail">&middot; 6-person team</span>
      </div>
      <div class="cmd-met-list">
        <!-- Primary Mets — each owns a state -->
        <div class="cmd-met-row">
          <span class="cmd-met-fatigue fatigue-yellow" title="4 reviews in last hour"></span>
          <div class="cmd-met-avatar">TA</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">M. Reynolds <span style="font-size: 10px; opacity: 0.6; font-weight: 400;">&middot; Indiana + Ohio</span></p>
            <p class="cmd-met-detail">Reviewing #3128 &middot; 4 today</p>
          </div>
          <span class="cmd-met-status-pill is-reviewing">Reviewing</span>
        </div>
        <div class="cmd-met-row">
          <span class="cmd-met-fatigue fatigue-green" title="3 reviews, well-paced"></span>
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #FFC366, #F5A524); color: #4a2c0a;">CS</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">Chris Sramek <span style="font-size: 10px; opacity: 0.6; font-weight: 400;">&middot; Kansas</span></p>
            <p class="cmd-met-detail">Idle &middot; last active 12 min ago</p>
          </div>
          <span class="cmd-met-status-pill is-online">Online</span>
        </div>
        <div class="cmd-met-row">
          <span class="cmd-met-fatigue fatigue-green" title="2 reviews today"></span>
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #B5A0FF, #7E5BEF); color: #1c1240;">K</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">Keefer <span style="font-size: 10px; opacity: 0.6; font-weight: 400;">&middot; New Jersey</span></p>
            <p class="cmd-met-detail">Idle &middot; 2 today</p>
          </div>
          <span class="cmd-met-status-pill is-online">Online</span>
        </div>

        <!-- Fill-in Mets — cover when primaries are offline -->
        <div class="cmd-met-row">
          <span class="cmd-met-fatigue" style="background: rgba(255,255,255,0.15);" title="Off shift"></span>
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #5DECA8, #16C77E); color: #073d23;">AF</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">AJ Franz <span style="font-size: 10px; opacity: 0.6; font-weight: 400;">&middot; Fill-in</span></p>
            <p class="cmd-met-detail">Off shift &middot; back at 6:00 PM ET</p>
          </div>
          <span class="cmd-met-status-pill is-offline">Offline</span>
        </div>
        <div class="cmd-met-row">
          <span class="cmd-met-fatigue" style="background: rgba(255,255,255,0.15);" title="Off shift"></span>
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #FF9B7A, #E55B3A); color: #4a1a0a;">J</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">Joe <span style="font-size: 10px; opacity: 0.6; font-weight: 400;">&middot; Fill-in</span></p>
            <p class="cmd-met-detail">Off shift</p>
          </div>
          <span class="cmd-met-status-pill is-offline">Offline</span>
        </div>
        <div class="cmd-met-row">
          <span class="cmd-met-fatigue" style="background: rgba(255,255,255,0.15);" title="Off shift"></span>
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #90C8FF, #4A9EE8); color: #0a2440;">N</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">Nick <span style="font-size: 10px; opacity: 0.6; font-weight: 400;">&middot; Fill-in</span></p>
            <p class="cmd-met-detail">Off shift</p>
          </div>
          <span class="cmd-met-status-pill is-offline">Offline</span>
        </div>
      </div>
    </div>

    <!-- Row 3: Shadow Mode delta + Challenges + Hot leads -->
    <div class="cmd-tile span-4">
      <div class="cmd-tile-label">Shadow Mode delta</div>
      <p class="cmd-shadow-headline">
        Mets caught <em>8 false-positives</em> this week.
      </p>
      <div class="cmd-shadow-bars">
        <div class="cmd-shadow-bar is-saved" style="flex: 8;"></div>
        <div class="cmd-shadow-bar is-upgrade" style="flex: 3;"></div>
        <div class="cmd-shadow-bar" style="flex: 50;"></div>
      </div>
      <p class="cmd-shadow-detail">
        8 Caution &rarr; Clear downgrades and 3 upgrades. AI agreed with Met
        on 82% of reviews. Disagreements concentrated in event windows
        with marginal wind.
      </p>
    </div>
    <div class="cmd-tile span-4">
      <div class="cmd-tile-label">Customer challenges</div>
      <div class="cmd-challenges-empty">
        <div class="cmd-challenges-check">
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
        </div>
        <div class="cmd-challenges-empty-text">
          <strong>No flagged tickets in the last 24 hours.</strong><br/>
          <span style="color: rgba(255,255,255,0.50); font-size: 11.5px;">
            Trust signal &middot; track record looks good.
          </span>
        </div>
      </div>
    </div>
    <div class="cmd-tile span-4">
      <div class="cmd-tile-label">
        <span>Hot leads</span>
        <span class="cmd-tile-label-detail">&middot; guest searches</span>
      </div>
      <div class="cmd-leads-list">
        <div class="cmd-leads-row">
          <span class="cmd-leads-pulse"></span>
          <span class="cmd-leads-query">Outdoor wedding July 12 in Bloomington, IN</span>
          <span class="cmd-leads-meta">12m</span>
        </div>
        <div class="cmd-leads-row">
          <span class="cmd-leads-pulse"></span>
          <span class="cmd-leads-query">Concrete schedule next week, Carmel</span>
          <span class="cmd-leads-meta">38m</span>
        </div>
        <div class="cmd-leads-row">
          <span class="cmd-leads-pulse"></span>
          <span class="cmd-leads-query">Roofing job tomorrow afternoon, Fishers</span>
          <span class="cmd-leads-meta">1h</span>
        </div>
      </div>
    </div>

    <!-- Row 4: Wins (the Nod feed) + Coverage gap -->
    <div class="cmd-tile span-6">
      <div class="cmd-tile-label">
        <span>Wins</span>
        <span class="cmd-tile-label-detail">&middot; the moments that matter</span>
      </div>
      <div class="cmd-wins-list">
        <div class="cmd-wins-row">
          <p class="cmd-wins-quote">&ldquo;Saved me a re-pour. The 9 AM call was right on the money.&rdquo;</p>
          <span class="cmd-wins-meta">Concrete contractor &middot; Lebanon, IN &middot; 2h ago</span>
        </div>
        <div class="cmd-wins-row">
          <p class="cmd-wins-quote">&ldquo;Caught the gust front 20 minutes before the local TV station did. Worth every penny.&rdquo;</p>
          <span class="cmd-wins-meta">Athletic director &middot; Westfield, IN &middot; yesterday</span>
        </div>
        <div class="cmd-wins-row">
          <p class="cmd-wins-quote">&ldquo;While everyone else was glued to their phones, I was watching the game.&rdquo;</p>
          <span class="cmd-wins-meta">Subscriber &middot; Indiana University game &middot; 3d ago</span>
        </div>
      </div>
    </div>
    <div class="cmd-tile span-6">
      <div class="cmd-tile-label">
        <span>Shift coverage</span>
        <span class="cmd-tile-label-detail">&middot; next 24 hours</span>
      </div>
      <div class="cmd-coverage-axis">
        <span>now</span>
        <span>+6h</span>
        <span>+12h</span>
        <span>+18h</span>
        <span>+24h</span>
      </div>
      <div class="cmd-coverage-bars" id="cmd-coverage-bars"><!-- populated by JS --></div>
      <div class="cmd-coverage-warn">
        <svg class="cmd-coverage-warn-icon" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 9v4"/><path d="M12 17h.01"/><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
        </svg>
        <span>3 AM Sunday: only 1 Met scheduled. Storm system forecast for the Midwest overnight &mdash; consider pulling someone in.</span>
      </div>
    </div>

    <!-- Row 5: Search heatmap + Missed opportunity -->
    <div class="cmd-tile span-7">
      <div class="cmd-tile-label">
        <span>Search activity</span>
        <span class="cmd-tile-label-detail">&middot; last 24 hours by region</span>
      </div>
      <div class="cmd-heatmap" id="cmd-heatmap"><!-- populated by JS --></div>
      <div class="cmd-heatmap-legend">
        <span>Less activity</span>
        <span>&rarr;</span>
        <span>More activity</span>
      </div>
      <p style="margin: 12px 0 0; font-size: 11.5px; color: rgba(255,255,255,0.50); line-height: 1.5;">
        Heaviest activity is concentrated in central Indiana (Marion, Hamilton, Boone counties).
        Bloomington and West Lafayette also active &mdash; likely game-day weather questions.
      </p>
    </div>
    <div class="cmd-tile span-5">
      <div class="cmd-tile-label">
        <span>Missed opportunity</span>
        <span class="cmd-tile-label-detail">&middot; outside coverage</span>
      </div>
      <div class="cmd-missed-list">
        <div class="cmd-missed-row">
          <span class="cmd-missed-location">Louisville, KY</span>
          <span class="cmd-missed-count">14</span>
        </div>
        <div class="cmd-missed-row">
          <span class="cmd-missed-location">Cincinnati, OH</span>
          <span class="cmd-missed-count">9</span>
        </div>
        <div class="cmd-missed-row">
          <span class="cmd-missed-location">Chicago, IL (suburbs)</span>
          <span class="cmd-missed-count">7</span>
        </div>
        <div class="cmd-missed-row">
          <span class="cmd-missed-location">Nashville, TN</span>
          <span class="cmd-missed-count">5</span>
        </div>
      </div>
    </div>

    <!-- Row 6: Top Crew contributors + AI vs Met agreement (smaller).
         Originally labeled "Crew leaderboard" but the framing was
         changed to remove ranking-as-competition language. Even in
         admin-only views, the language we use reinforces the
         product's culture. -->
    <div class="cmd-tile span-7">
      <div class="cmd-tile-label">
        <span>Top Crew contributors</span>
        <span class="cmd-tile-label-detail">&middot; this week</span>
      </div>
      <div class="cmd-met-list">
        <div class="cmd-met-row">
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #FFD53E, #FFB30A); color: #4a2c0a;">1</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">@grant_t</p>
            <p class="cmd-met-detail">Indianapolis &middot; 100% verified rate</p>
          </div>
          <span class="cmd-met-status-pill" style="color: rgba(255,255,255,0.85);">28 reports</span>
        </div>
        <div class="cmd-met-row">
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #d6dde5, #b6c0cb); color: #2c3a4a;">2</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">@dani_fishers</p>
            <p class="cmd-met-detail">Fishers &middot; 96% verified rate</p>
          </div>
          <span class="cmd-met-status-pill" style="color: rgba(255,255,255,0.85);">22 reports</span>
        </div>
        <div class="cmd-met-row">
          <div class="cmd-met-avatar" style="background: linear-gradient(135deg, #d8a47f, #b87a4f); color: #4a2810;">3</div>
          <div class="cmd-met-info">
            <p class="cmd-met-name">@zionsville_kev</p>
            <p class="cmd-met-detail">Zionsville &middot; 91% verified rate</p>
          </div>
          <span class="cmd-met-status-pill" style="color: rgba(255,255,255,0.85);">19 reports</span>
        </div>
      </div>
    </div>
    <div class="cmd-tile span-5">
      <div class="cmd-tile-label">User feedback</div>
      <div class="cmd-metric-value" style="color: #5DECA8;">94<span class="cmd-metric-value-unit">%</span></div>
      <div class="cmd-metric-context">helpful this week (47 / 50)</div>
      <p style="margin: 14px 0 0; font-size: 12px; color: rgba(255,255,255,0.65); line-height: 1.5;">
        Three thumbs-down responses cited timing precision &mdash;
        worth a conversation with the team.
      </p>
    </div>

    <!-- Row 7: Recent activity + Reports placeholder -->
    <div class="cmd-tile span-7">
      <div class="cmd-tile-label">Recent activity</div>
      <div class="cmd-activity-list" id="cmd-activity-list"><!-- populated by JS --></div>
    </div>
    <div class="cmd-tile span-5">
      <div class="cmd-tile-label">Reports</div>
      <div class="cmd-reports-empty">
        <svg class="cmd-reports-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
          <path d="M3 3v18h18"/>
          <path d="M7 14l4-4 4 4 5-5"/>
        </svg>
        <h3 class="cmd-reports-headline">Detailed reports coming soon.</h3>
        <p class="cmd-reports-detail">
          LTV by industry, churn warnings, geographic spread, conversion funnels, and
          per-Met cost ratios will appear here once there's longitudinal data to make
          them meaningful.
        </p>
      </div>
    </div>

  </div>
  </div><!-- /cmd-tab-panel dashboard -->

  <!-- ── Team panel — Met account management ──────────────────
       Hidden until the Team tab is clicked. Lists every account
       with role, status, last login. Add/Edit/Delete actions
       open the team-modal. -->
  <div class="cmd-tab-panel" data-cmd-panel="team">
    <div class="team-panel">
      <div class="team-panel-header">
        <div>
          <h2 class="team-panel-title">Team</h2>
          <p class="team-panel-sub">Add, edit, or deactivate meteorologist and crew accounts.</p>
        </div>
        <button type="button" class="team-add-btn" id="team-add-btn">
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <line x1="12" y1="5" x2="12" y2="19"/>
            <line x1="5" y1="12" x2="19" y2="12"/>
          </svg>
          <span>Add member</span>
        </button>
      </div>

      <div class="team-list" id="team-list">
        <div class="team-list-header">
          <div>Name</div>
          <div>Email</div>
          <div>Role</div>
          <div>Last login</div>
          <div>Status</div>
          <div></div>
        </div>
        <!-- Rows are rendered by JS — see renderTeamList() -->
      </div>
    </div>
  </div><!-- /cmd-tab-panel team -->


  <!-- ══════════════════════════════════════════════════════════
       Operations tab — real backend-wired admin tools.
       Four sections stacked vertically: Crew applications,
       Subscribers, Payments, Audit log. Each fetches from a
       backend endpoint and renders. No mock data.
       ══════════════════════════════════════════════════════════ -->
  <div class="cmd-tab-panel" data-cmd-panel="operations">
    <div class="cmd-body" style="display:block;padding-bottom:80px;">

      <!-- ── Crew applications ───────────────────────────────── -->
      <div class="cmd-tile" style="grid-column: span 12; margin-bottom: 20px;">
        <div class="cmd-tile-label" style="display:flex;align-items:center;justify-content:space-between;">
          <span>Pending Crew applications</span>
          <span style="display:flex;gap:8px;align-items:center;">
            <span id="admin-crew-count" style="background:rgba(255,184,0,0.15);color:#FFCC4D;padding:3px 9px;border-radius:4px;font-size:11px;font-weight:600;">&mdash;</span>
            <button type="button" id="admin-crew-refresh" style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
          </span>
        </div>
        <div id="admin-crew-list" style="margin-top:14px;">
          <div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

      <!-- ── Subscribers ───────────────────────────────────────── -->
      <div class="cmd-tile" style="grid-column: span 12; margin-bottom: 20px;">
        <div class="cmd-tile-label" style="display:flex;align-items:center;justify-content:space-between;">
          <span>Users</span>
          <span style="display:flex;gap:8px;align-items:center;">
            <input type="search" id="admin-users-search" placeholder="Search email or name"
                   style="background:rgba(0,0,0,0.20);border:1px solid rgba(255,255,255,0.10);color:#fff;padding:5px 10px;border-radius:5px;font-size:12px;width:200px;">
            <select id="admin-users-role-filter"
                    style="background:rgba(0,0,0,0.20);border:1px solid rgba(255,255,255,0.10);color:#fff;padding:5px 10px;border-radius:5px;font-size:12px;">
              <option value="">All roles</option>
              <option value="subscriber">Subscribers</option>
              <option value="crew">Crew</option>
              <option value="met">Mets</option>
              <option value="admin">Admins</option>
            </select>
            <button type="button" id="admin-users-refresh" style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
          </span>
        </div>
        <div id="admin-users-list" style="margin-top:14px;">
          <div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

      <!-- ── Payments ──────────────────────────────────────────── -->
      <div class="cmd-tile" style="grid-column: span 12; margin-bottom: 20px;">
        <div class="cmd-tile-label" style="display:flex;align-items:center;justify-content:space-between;">
          <span>Recent $19 reviews</span>
          <button type="button" id="admin-payments-refresh" style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
        </div>
        <div id="admin-payments-list" style="margin-top:14px;">
          <div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

      <!-- ── Met payroll (monthly) ─────────────────────────────── -->
      <div class="cmd-tile" style="grid-column: span 12; margin-bottom: 20px;">
        <div class="cmd-tile-label" style="display:flex;align-items:center;justify-content:space-between;">
          <span>Met payroll</span>
          <span style="display:flex;gap:8px;align-items:center;">
            <select id="admin-payroll-month" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.12);color:#fff;padding:4px 10px;border-radius:5px;font-size:11px;font-family:inherit;cursor:pointer;">
              <!-- Populated by JS -->
            </select>
            <button type="button" id="admin-payroll-refresh" style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
          </span>
        </div>
        <p style="font-size:11.5px;color:rgba(255,255,255,0.55);margin:10px 0 14px;line-height:1.55;">
          Per-Met earnings for the selected month, in Eastern Time. Use these numbers to enter into payroll.
        </p>
        <div id="admin-payroll-content" style="margin-top:14px;">
          <div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

      <!-- ── Coverage dashboard (Phase 11, May 17) ─────────────── -->
      <div class="cmd-tile" style="grid-column: span 12; margin-bottom: 20px;">
        <div class="cmd-tile-label" style="display:flex;align-items:center;justify-content:space-between;">
          <span>Coverage — next 7 days</span>
          <span style="display:flex;gap:8px;align-items:center;">
            <span id="admin-coverage-live-count" style="font-size:11px;color:rgba(255,255,255,0.65);"></span>
            <button type="button" id="admin-coverage-refresh" style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
          </span>
        </div>
        <p style="font-size:11.5px;color:rgba(255,255,255,0.55);margin:10px 0 14px;line-height:1.55;">
          Who's covering each Pro subscriber's daily brief and the Review Pool over the next 7 days.
          Red cells are GAPS &mdash; no Met is assigned. Yellow cells mean the primary Met is the default owner but hasn't claimed a recurring shift for that day. Click a subscriber name to set primary &amp; backup Met.
        </p>
        <div id="admin-coverage-content" style="margin-top:14px;overflow-x:auto;">
          <div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

      <!-- ── Sales Commissions (monthly) ───────────────────────── -->
      <div class="cmd-tile" style="grid-column: span 12; margin-bottom: 20px;">
        <div class="cmd-tile-label" style="display:flex;align-items:center;justify-content:space-between;">
          <span>Sales commissions</span>
          <span style="display:flex;gap:8px;align-items:center;">
            <select id="admin-comm-month" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.12);color:#fff;padding:4px 10px;border-radius:5px;font-size:11px;font-family:inherit;cursor:pointer;">
              <!-- Populated by JS -->
            </select>
            <button type="button" id="admin-comm-add-rep" style="background:#2E4FB8;border:none;color:#fff;padding:4px 12px;border-radius:5px;font-size:11px;font-weight:600;cursor:pointer;">+ Add Rep</button>
            <button type="button" id="admin-comm-refresh" style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
          </span>
        </div>
        <p style="font-size:11.5px;color:rgba(255,255,255,0.55);margin:10px 0 14px;line-height:1.55;">
          Per-rep commission for the selected month. 20% of monthly subscription cash, for 6 months from each customer&rsquo;s signup. Starter Month customers earn commission on month 0 ($99) plus months 1&ndash;6.
        </p>
        <div id="admin-comm-content" style="margin-top:14px;">
          <div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

      <!-- ── Audit log ─────────────────────────────────────────── -->
      <div class="cmd-tile" style="grid-column: span 12;">
        <div class="cmd-tile-label" style="display:flex;align-items:center;justify-content:space-between;">
          <span>Audit log</span>
          <button type="button" id="admin-audit-refresh" style="background:transparent;border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Refresh</button>
        </div>
        <div id="admin-audit-list" style="margin-top:14px;">
          <div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>
        </div>
      </div>

    </div>
  </div><!-- /cmd-tab-panel operations -->


  <!-- ══════════════════════════════════════════════════════════
       Admin Operations modals — refund + reject reason prompts.
       Light-themed (matching the admin command center treatment).
       ══════════════════════════════════════════════════════════ -->

  <!-- Refund modal — confirms amount + collects reason -->
  <div class="team-modal-scrim" id="admin-refund-scrim" hidden>
    <div class="team-modal" role="dialog" aria-modal="true" aria-labelledby="admin-refund-title">
      <h2 class="team-modal-title" id="admin-refund-title">Issue refund</h2>
      <p class="team-modal-sub" id="admin-refund-sub">This will refund the customer in full via Stripe and notify them by SMS.</p>
      <div class="team-modal-error" id="admin-refund-error" hidden></div>
      <div class="team-modal-field">
        <label class="team-modal-label">Customer</label>
        <input type="text" class="team-modal-input" id="admin-refund-customer" readonly>
      </div>
      <div class="team-modal-field">
        <label class="team-modal-label">Amount</label>
        <input type="text" class="team-modal-input" id="admin-refund-amount" readonly>
      </div>
      <div class="team-modal-field">
        <label class="team-modal-label" for="admin-refund-reason">Reason (recorded in audit log)</label>
        <textarea class="team-modal-input" id="admin-refund-reason" rows="3"
                  placeholder="e.g. Met missed 30-min SLA"
                  style="font-family:inherit;resize:vertical;"></textarea>
      </div>
      <div class="team-modal-actions">
        <button type="button" class="team-modal-cancel" id="admin-refund-cancel">Cancel</button>
        <button type="button" class="team-modal-submit" id="admin-refund-submit">Refund</button>
      </div>
    </div>
  </div>

  <!-- Reject Crew application modal -->
  <div class="team-modal-scrim" id="admin-reject-scrim" hidden>
    <div class="team-modal" role="dialog" aria-modal="true" aria-labelledby="admin-reject-title">
      <h2 class="team-modal-title" id="admin-reject-title">Reject application</h2>
      <p class="team-modal-sub" id="admin-reject-sub">The applicant will not be notified, but the reason is recorded in the audit log.</p>
      <div class="team-modal-error" id="admin-reject-error" hidden></div>
      <div class="team-modal-field">
        <label class="team-modal-label">Applicant</label>
        <input type="text" class="team-modal-input" id="admin-reject-applicant" readonly>
      </div>
      <div class="team-modal-field">
        <label class="team-modal-label" for="admin-reject-reason">Reason (optional)</label>
        <textarea class="team-modal-input" id="admin-reject-reason" rows="3"
                  placeholder="e.g. Outside our coverage area"
                  style="font-family:inherit;resize:vertical;"></textarea>
      </div>
      <div class="team-modal-actions">
        <button type="button" class="team-modal-cancel" id="admin-reject-cancel">Cancel</button>
        <button type="button" class="team-modal-submit" id="admin-reject-submit">Reject application</button>
      </div>
    </div>
  </div>

  <!-- ── Add / Edit member modal ────────────────────────────── -->
  <div class="team-modal-scrim" id="team-modal-scrim" hidden>
    <div class="team-modal" role="dialog" aria-modal="true" aria-labelledby="team-modal-title">
      <h2 class="team-modal-title" id="team-modal-title">Add member</h2>
      <p class="team-modal-sub" id="team-modal-sub">They'll get a temporary password and be required to change it on first login.</p>

      <div class="team-modal-error" id="team-modal-error" hidden></div>

      <div class="team-modal-field">
        <label class="team-modal-label" for="team-modal-name">Name</label>
        <input type="text" class="team-modal-input" id="team-modal-name" autocomplete="off" placeholder="Amanda Liu">
      </div>

      <div class="team-modal-field">
        <label class="team-modal-label" for="team-modal-email">Email</label>
        <input type="email" class="team-modal-input" id="team-modal-email" autocomplete="off" placeholder="amanda@weathervalet.ai">
      </div>

      <div class="team-modal-field">
        <label class="team-modal-label" for="team-modal-role">Role</label>
        <select class="team-modal-select" id="team-modal-role">
          <option value="meteorologist">Meteorologist</option>
          <option value="crew">Valet Crew</option>
          <option value="admin">Admin</option>
        </select>
      </div>

      <!-- Success card shown after add or password reset.
           Hidden during the form-input phase. -->
      <div class="team-temp-pw" id="team-temp-pw" hidden>
        <div class="team-temp-pw-label">Welcome email sent</div>
        <p class="team-temp-pw-hint" style="margin: 6px 0 12px;">
          A welcome email with sign-in instructions was sent to the new member.
          They'll be prompted to set their own password on first sign-in.
        </p>
        <div id="team-temp-pw-reveal-wrap" style="margin-top: 10px;">
          <button type="button" id="team-temp-pw-reveal-btn"
                  style="background: transparent; border: 1px solid rgba(255,255,255,0.18); color: rgba(255,255,255,0.65); padding: 6px 12px; border-radius: 6px; font-size: 11.5px; cursor: pointer;">
            Show temp password (backup)
          </button>
        </div>
        <div id="team-temp-pw-revealed" hidden style="margin-top: 10px;">
          <div class="team-temp-pw-label" style="font-size: 10px; opacity: 0.7;">Temporary password (backup only)</div>
          <div class="team-temp-pw-value" id="team-temp-pw-value"></div>
          <p style="font-size: 11px; color: rgba(255,255,255,0.55); margin: 8px 0 0; line-height: 1.45;">
            Only share this if the welcome email didn't arrive. It's the only time it'll be shown.
          </p>
        </div>
      </div>

      <!-- Reset password button — only visible in Edit mode -->
      <div id="team-modal-reset-row" hidden style="margin: 0 0 12px; padding: 12px 0; border-top: 1px solid rgba(255,255,255,0.08);">
        <button type="button" id="team-modal-reset-btn"
                style="background: transparent; border: 1px solid rgba(255,255,255,0.18); color: rgba(255,255,255,0.85); padding: 8px 14px; border-radius: 6px; font-size: 12.5px; cursor: pointer; display: inline-flex; align-items: center; gap: 8px;">
          <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
          Reset password &amp; email a new temp
        </button>
        <p style="font-size: 11px; color: rgba(255,255,255,0.50); margin: 6px 0 0; line-height: 1.45;">
          Use this if they didn&rsquo;t get the welcome email. They&rsquo;ll get a new temp password by email.
        </p>
      </div>

      <div class="team-modal-actions">
        <button type="button" class="team-modal-cancel" id="team-modal-cancel">Cancel</button>
        <button type="button" class="team-modal-submit" id="team-modal-submit">Save</button>
      </div>
    </div>
  </div>

  <!-- ── Confirm-delete modal ───────────────────────────────── -->
  <div class="team-modal-scrim" id="team-confirm-scrim" hidden>
    <div class="team-modal" role="dialog" aria-modal="true" aria-labelledby="team-confirm-title">
      <h2 class="team-modal-title" id="team-confirm-title">Deactivate member?</h2>
      <p class="team-modal-sub" id="team-confirm-sub">
        They'll no longer be able to sign in. Their past reviews and reports stay in the record.
        You can reactivate them later.
      </p>
      <div class="team-modal-error" id="team-confirm-error" hidden></div>
      <div class="team-modal-actions">
        <button type="button" class="team-modal-cancel" id="team-confirm-cancel">Cancel</button>
        <button type="button" class="team-modal-submit" id="team-confirm-submit" style="background: #C2342B;">Deactivate</button>
      </div>
    </div>
  </div>

  <!-- ── Force Alert button — fixed bottom-right ─────────────── -->
  <button type="button" class="cmd-force-alert" id="cmd-force-alert">
    <svg class="cmd-force-alert-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
      <path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/>
      <path d="M12 9v4"/><path d="M12 17h.01"/>
    </svg>
    <span>Force Alert</span>
  </button>
</div>


<!-- ══════════════════════════════════════════════════════════════
     Global site footer — appears below every screen.
     Sits AFTER all .screen siblings so when a screen is active,
     the footer flows naturally after that screen's content.
     Hidden via .site-footer.is-hidden class on certain immersive
     screens (handoff transition, storm-shelter overlay, customer
     review thank-you).
     ══════════════════════════════════════════════════════════════ -->
<div class="site-footer">
  <div class="site-footer-inner">
    <div class="site-footer-brand">
      <span class="site-footer-dot"></span>
      <span class="site-footer-name">WeatherValet</span>
    </div>
    <div class="site-footer-links">
      <a href="?page=about">About</a>
      <a href="?page=faq">FAQ</a>
      <a href="?legal=privacy">Privacy</a>
      <a href="?legal=terms">Terms</a>
      <a href="mailto:hello@weathervalet.ai">Contact</a>
    </div>
    <div class="site-footer-meta">
      &copy; 2026 WeatherValet &middot; Indianapolis, IN
    </div>
  </div>
</div>


<!-- ============ DEV MENU — only visible when ?dev=1 in URL ============ -->
<!-- Floating chip in the bottom-left. Tap it, three role buttons appear.
     Switching roles triggers a screen swap to the appropriate view.
     The HTML element is in the DOM at all times but kept display:none by
     default; the JS in init() un-hides it when ?dev=1 is detected. -->
<div id="dev-menu" class="dev-menu">
  <button type="button" class="dev-menu-trigger" id="dev-menu-trigger" aria-haspopup="true" aria-expanded="false">
    <span class="dev-menu-trigger-dot"></span>
    <span>Dev &middot; <span id="dev-menu-current-label">Customer</span></span>
  </button>
  <div class="dev-menu-options" role="menu">
    <div class="dev-menu-label">Switch view</div>
    <button type="button" class="dev-menu-option is-active" data-dev-view="customer" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
        <circle cx="12" cy="7" r="4"/>
      </svg>
      <span>Customer</span>
    </button>
    <button type="button" class="dev-menu-option" data-dev-view="met" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
        <polyline points="7.5 4.21 12 6.81 16.5 4.21"/>
        <polyline points="7.5 19.79 7.5 14.6 3 12"/>
        <polyline points="21 12 16.5 14.6 16.5 19.79"/>
      </svg>
      <span>Meteorologist</span>
    </button>
    <button type="button" class="dev-menu-option" data-dev-view="crew-ws" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 2L2 7l10 5 10-5-10-5z"/>
        <path d="M2 17l10 5 10-5"/>
        <path d="M2 12l10 5 10-5"/>
      </svg>
      <span>Valet Crew</span>
    </button>
    <button type="button" class="dev-menu-option" data-dev-view="admin" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <rect x="3" y="3" width="7" height="9" rx="1"/>
        <rect x="14" y="3" width="7" height="5" rx="1"/>
        <rect x="14" y="12" width="7" height="9" rx="1"/>
        <rect x="3" y="16" width="7" height="5" rx="1"/>
      </svg>
      <span>Admin</span>
    </button>

    <!-- Test scenarios — stuff a tester wants to flip on/off without
         memorizing a console command. -->
    <div class="dev-menu-divider"></div>
    <div class="dev-menu-label">Storm Shelter test</div>
    <button type="button" class="dev-menu-option" data-shelter-tier="watch" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="#F5A524"
           stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="9"/>
        <path d="M12 8v4M12 16.5v.01"/>
      </svg>
      <span>Watch (amber)</span>
    </button>
    <button type="button" class="dev-menu-option" data-shelter-tier="warning" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="#C2342B"
           stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 2 4 7v6c0 5 3.5 9 8 10 4.5-1 8-5 8-10V7l-8-5z"/>
        <path d="M12 9v4M12 16.5v.01"/>
      </svg>
      <span>Warning (red)</span>
    </button>
    <button type="button" class="dev-menu-option" data-shelter-tier="all-clear" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="#16C77E"
           stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="20 6 9 17 4 12"/>
      </svg>
      <span>All Clear (green)</span>
    </button>
    <button type="button" class="dev-menu-option" data-shelter-tier="dismiss" role="menuitem">
      <svg class="dev-menu-option-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
      <span>Dismiss shelter</span>
    </button>
  </div>
</div>


<!-- ============ VALET CREW · QUICK REPORT (BOTTOM SHEET) ============ -->
<!-- The report sheet is a global UI, not a screen — it overlays whichever
     Crew screen the user is on. Hidden by default; slides up on demand. -->
<div class="report-sheet-scrim" id="report-scrim"></div>
<div class="report-sheet" id="report-sheet" role="dialog" aria-modal="true" aria-label="Submit a Quick Report">
  <div class="report-sheet-handle"></div>

  <!-- Default state: pick a category -->
  <div id="report-pick-state">
    <div class="report-sheet-eyebrow">Quick Report · 2-tap submit</div>
    <h2 class="report-sheet-title">What are you seeing?</h2>
    <p class="report-sheet-sub">Tap a category. We'll log your location automatically.</p>

    <!-- Active weather section — events worth alerting the Crew about.
         These are the categories where a real-time report changes how
         someone uses the forecast (rain starting, wind picking up, hail). -->
    <div class="report-section-label">Active weather</div>
    <div class="report-grid">
      <button type="button" class="report-option" data-kind="storm" data-report="storm">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M19 16.9A5 5 0 0 0 18 7h-1.26a8 8 0 1 0-11.62 9"/>
            <polyline points="13 11 9 17 15 17 11 23"/>
          </svg>
        </span>
        <span class="report-option-label">Storm</span>
        <span class="report-option-sub">Lightning, thunder, severe</span>
      </button>
      <button type="button" class="report-option" data-kind="hail" data-report="hail">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="6" cy="20" r="1.5"/><circle cx="12" cy="20" r="1.5"/>
            <circle cx="18" cy="20" r="1.5"/><circle cx="9" cy="14" r="1.5"/>
            <circle cx="15" cy="14" r="1.5"/>
            <path d="M19 16.9A5 5 0 0 0 18 7h-1.26a8 8 0 1 0-11.62 9"/>
          </svg>
        </span>
        <span class="report-option-label">Hail</span>
        <span class="report-option-sub">Any size, any duration</span>
      </button>
      <button type="button" class="report-option" data-kind="wind" data-report="wind">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M9.59 4.59A2 2 0 1 1 11 8H2m10.59 11.41A2 2 0 1 0 14 16H2m15.73-8.27A2.5 2.5 0 1 1 19.5 12H2"/>
          </svg>
        </span>
        <span class="report-option-label">Wind</span>
        <span class="report-option-sub">Strong gusts or steady</span>
      </button>
      <button type="button" class="report-option" data-kind="rain" data-report="rain">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="8" y1="19" x2="8" y2="21"/><line x1="8" y1="13" x2="8" y2="15"/>
            <line x1="16" y1="19" x2="16" y2="21"/><line x1="16" y1="13" x2="16" y2="15"/>
            <line x1="12" y1="21" x2="12" y2="23"/><line x1="12" y1="15" x2="12" y2="17"/>
            <path d="M20 16.58A5 5 0 0 0 18 7h-1.26A8 8 0 1 0 4 15.25"/>
          </svg>
        </span>
        <span class="report-option-label">Rain</span>
        <span class="report-option-sub">Drizzle, shower, downpour</span>
      </button>
      <button type="button" class="report-option" data-kind="snow" data-report="snow">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 17.58A5 5 0 0 0 18 8h-1.26A8 8 0 1 0 4 16.25"/>
            <line x1="8" y1="16" x2="8.01" y2="16"/><line x1="8" y1="20" x2="8.01" y2="20"/>
            <line x1="12" y1="18" x2="12.01" y2="18"/><line x1="12" y1="22" x2="12.01" y2="22"/>
            <line x1="16" y1="16" x2="16.01" y2="16"/><line x1="16" y1="20" x2="16.01" y2="20"/>
          </svg>
        </span>
        <span class="report-option-label">Snow</span>
        <span class="report-option-sub">Flurries, accumulating, blizzard</span>
      </button>
      <button type="button" class="report-option" data-kind="sleet" data-report="sleet">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 16.58A5 5 0 0 0 18 7h-1.26A8 8 0 1 0 4 15.25"/>
            <circle cx="9" cy="18" r="1"/><circle cx="15" cy="18" r="1"/>
            <line x1="9" y1="22" x2="9" y2="22"/><line x1="15" y1="22" x2="15" y2="22"/>
            <line x1="12" y1="16" x2="12" y2="20"/>
          </svg>
        </span>
        <span class="report-option-label">Sleet</span>
        <span class="report-option-sub">Ice pellets, mixed precip</span>
      </button>
      <button type="button" class="report-option" data-kind="freezing" data-report="freezing">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 16.58A5 5 0 0 0 18 7h-1.26A8 8 0 1 0 4 15.25"/>
            <line x1="12" y1="14" x2="12" y2="22"/>
            <line x1="9" y1="17" x2="15" y2="17"/>
            <line x1="9" y1="20" x2="15" y2="20"/>
          </svg>
        </span>
        <span class="report-option-label">Freezing rain</span>
        <span class="report-option-sub">Glaze ice, slick surfaces</span>
      </button>
      <button type="button" class="report-option" data-kind="fog" data-report="fog">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="3" y1="8" x2="21" y2="8"/>
            <line x1="3" y1="13" x2="21" y2="13"/>
            <line x1="3" y1="18" x2="14" y2="18"/>
          </svg>
        </span>
        <span class="report-option-label">Fog</span>
        <span class="report-option-sub">Reduced visibility</span>
      </button>
    </div>

    <!-- Current conditions — softer status updates that don't rise to the
         level of an alert but still tell the Crew "this is what it's like
         here right now." Useful for tracking transitions (rain stopping,
         skies clearing) and for hot/humid days where the temperature alone
         doesn't capture the experience. -->
    <div class="report-section-label">Current conditions</div>
    <div class="report-grid">
      <button type="button" class="report-option" data-kind="sunny" data-report="sunny">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="4"/>
            <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"/>
          </svg>
        </span>
        <span class="report-option-label">Sunny</span>
        <span class="report-option-sub">Clear skies, full sun</span>
      </button>
      <button type="button" class="report-option" data-kind="cloudy" data-report="cloudy">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"/>
          </svg>
        </span>
        <span class="report-option-label">Cloudy</span>
        <span class="report-option-sub">Overcast or partly cloudy</span>
      </button>
      <button type="button" class="report-option" data-kind="humid" data-report="humid">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z"/>
          </svg>
        </span>
        <span class="report-option-label">Humid</span>
        <span class="report-option-sub">Sticky, oppressive air</span>
      </button>
      <button type="button" class="report-option" data-kind="clearing" data-report="clearing">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="4"/>
            <path d="M3 7h6M2 12h4"/>
            <path d="M12 2v2M17.66 6.34l1.41-1.41M20 12h2"/>
          </svg>
        </span>
        <span class="report-option-label">Clearing</span>
        <span class="report-option-sub">Weather is letting up</span>
      </button>
    </div>

    <!-- Photo — its own row, full width. The deepest interaction
         (camera + classify), so it gets visual emphasis. -->
    <div class="report-section-label">Submit a photo</div>
    <div class="report-grid">
      <button type="button" class="report-option report-option-photo" data-kind="photo" data-report="photo">
        <span class="report-option-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/>
            <circle cx="12" cy="13" r="4"/>
          </svg>
        </span>
        <span class="report-option-label">Photo</span>
        <span class="report-option-sub">Sky, clouds, what you see</span>
      </button>
    </div>

    <!-- ── Optional confessional note ──────────────────────────
         A collapsible "+ Add a note" expand below the category grid.
         Crew member types first (if they want), then taps a category
         which submits everything together. Preserves the 2-tap promise
         for users who skip; captures human texture for users who don't.

         The "Quote me" checkbox is the per-confessional publish toggle.
         Default off — Meteorologists always see the note; external
         promotional use (social, marketing) requires explicit opt-in. -->
    <details class="report-note-details" id="report-note-details">
      <summary class="report-note-summary">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/>
        </svg>
        <span>Add a note for the Meteorologists</span>
      </summary>
      <div class="report-note-body">
        <label for="report-note-input" class="report-note-label">
          What would you like to tell the WeatherValet Meteorologists?
        </label>
        <textarea
          id="report-note-input"
          class="report-note-input"
          rows="3"
          placeholder="Optional. A sentence about what stood out — sounds, feel, anything the numbers won't capture."
          maxlength="600"
        ></textarea>
        <div class="report-note-footer">
          <label class="report-note-quote">
            <input type="checkbox" id="report-note-quote">
            <span>WeatherValet can quote this on social media</span>
          </label>
          <span class="report-note-count">
            <span id="report-note-count-num">0</span> / 600
          </span>
        </div>
      </div>
    </details>
  </div>

  <!-- Photo-capture state: shown when the user taps "Photo" in the grid.
       Two-step flow:
         (1) File picker — tap to choose an image (camera or library)
         (2) Once an image is loaded, preview it + ask which weather type
             it shows (so the report carries proper type metadata).
       The image is compressed (max 800px, JPEG q=0.7) before being stored
       so it doesn't blow out the localStorage quota. Real photo upload
       in production would POST to S3 with a presigned URL — this layer
       inlines the data URL for the prototype. -->
  <div id="report-photo-state" hidden>
    <div class="report-sheet-eyebrow">Quick Report &middot; Photo</div>
    <h2 class="report-sheet-title">Add a photo</h2>
    <p class="report-sheet-sub">Show what the sky looks like. We&rsquo;ll classify it together.</p>

    <!-- Step A: file picker (no image yet) -->
    <div id="photo-picker-step">
      <label class="photo-picker" for="photo-input">
        <span class="photo-picker-icon" aria-hidden="true">
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
            <path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/>
            <circle cx="12" cy="13" r="4"/>
          </svg>
        </span>
        <span class="photo-picker-label">Tap to take or choose a photo</span>
        <span class="photo-picker-sub">JPEG or PNG &middot; phone camera works</span>
      </label>
      <input type="file" id="photo-input" accept="image/*" capture="environment" hidden>
    </div>

    <!-- Step B: preview + classify (shown after a file is picked) -->
    <div id="photo-classify-step" hidden>
      <div class="photo-preview-wrap">
        <img id="photo-preview" alt="Preview of the photo you'll submit">
        <button type="button" class="photo-retake" id="photo-retake" aria-label="Choose a different photo">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M18 6 6 18"/><path d="m6 6 12 12"/>
          </svg>
        </button>
      </div>
      <p class="photo-classify-prompt">What does this photo show?</p>
      <div class="photo-type-grid">
        <button type="button" class="photo-type-chip" data-photo-type="storm">
          <span class="photo-type-dot" style="background:#C2342B"></span>Storm
        </button>
        <button type="button" class="photo-type-chip" data-photo-type="hail">
          <span class="photo-type-dot" style="background:#5B6BD6"></span>Hail
        </button>
        <button type="button" class="photo-type-chip" data-photo-type="wind">
          <span class="photo-type-dot" style="background:#E8A400"></span>Wind
        </button>
        <button type="button" class="photo-type-chip" data-photo-type="rain">
          <span class="photo-type-dot" style="background:#2566D6"></span>Rain
        </button>
        <button type="button" class="photo-type-chip" data-photo-type="clear">
          <span class="photo-type-dot" style="background:#12A150"></span>Clear
        </button>
      </div>
      <button type="button" class="photo-submit" id="photo-submit" disabled>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20 6 9 17 4 12"/>
        </svg>
        <span>Submit photo report</span>
      </button>
    </div>
  </div>

  <!-- Thanks state (shown after a report is "submitted") — three pieces of
       choreography: (1) checkmark pops in with halo, (2) "Thank you" + body
       text rise into place, (3) +10 tokens chip appears with a floating "+10"
       burst above it and three confetti dots fanning outward. The whole
       sequence lasts ~1.4s; the sheet auto-closes 1.6s later, after the
       user has time to register the reward but before it overstays. -->
  <div id="report-thanks-state" hidden>
    <div class="report-thanks">
      <!-- Confetti — three dots that fan upward from the checkmark area -->
      <span class="thanks-confetti c1"></span>
      <span class="thanks-confetti c2"></span>
      <span class="thanks-confetti c3"></span>

      <!-- Animated +N burst, floats up over the checkmark area -->
      <span class="tokens-burst" id="tokens-burst" aria-hidden="true">+10</span>

      <div class="report-thanks-icon">
        <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">
          <polyline class="report-thanks-check" points="5 12 10 17 19 8"/>
        </svg>
      </div>
      <h2 class="report-thanks-title">Thank you.</h2>
      <p class="report-thanks-text">
        Your report is on the Crew feed.
        <span id="report-thanks-streak">Clarity streak: 9</span>.
      </p>
      <div class="report-thanks-tokens">
        <svg class="report-thanks-tokens-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
             stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="9"/>
          <path d="M8 12h8M12 8v8"/>
        </svg>
        <span class="report-thanks-tokens-amount">+10</span>
        <span>tokens</span>
      </div>
    </div>
  </div>
</div>


<!-- ============ AUTH GATE MODAL — Crew participation sign-in ============ -->
<!-- Shown when a signed-out user attempts to verify, tip, or submit a
     report. Calm, mockup-style — single "Continue" button flips state.
     Headline + body change based on intent (set by openAuthGate). -->
<div id="auth-gate-scrim" class="auth-gate-scrim"></div>
<div id="auth-gate-card" class="auth-gate-card" role="dialog" aria-modal="true" aria-labelledby="auth-gate-title">
  <button type="button" class="auth-gate-close" data-auth-action="close" aria-label="Close">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
         stroke-linecap="round" stroke-linejoin="round">
      <path d="M18 6 6 18"/><path d="m6 6 12 12"/>
    </svg>
  </button>
  <div class="auth-gate-eyebrow">
    <span class="auth-gate-eyebrow-dot"></span>
    <span>Valet Crew</span>
  </div>
  <h2 id="auth-gate-title" class="auth-gate-title">Sign in to participate</h2>
  <p id="auth-gate-body" class="auth-gate-body">
    Verifying, tipping, and reporting are all part of being in the Crew.
  </p>

  <!-- Tab switcher between Sign in and Create account -->
  <div class="auth-tabs" role="tablist">
    <button type="button" class="auth-tab is-active" id="auth-tab-signin"
            data-auth-tab="signin" role="tab" aria-selected="true">Sign in</button>
    <button type="button" class="auth-tab" id="auth-tab-signup"
            data-auth-tab="signup" role="tab" aria-selected="false">Create account</button>
  </div>

  <!-- SIGN IN PANEL -->
  <form id="auth-signin-form" class="auth-form auth-panel is-active" novalidate>
    <label class="auth-field">
      <span class="auth-field-label">Email</span>
      <input type="email" name="email" required autocomplete="email"
             placeholder="you@example.com" class="auth-input">
    </label>
    <label class="auth-field">
      <span class="auth-field-label">Password</span>
      <input type="password" name="password" required autocomplete="current-password"
             placeholder="At least 4 characters" class="auth-input">
    </label>
    <div class="auth-error" id="auth-signin-error" hidden></div>
    <button type="submit" class="auth-submit">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"/>
        <polyline points="10 17 15 12 10 7"/>
        <line x1="15" y1="12" x2="3" y2="12"/>
      </svg>
      <span>Sign in</span>
    </button>
    <p class="auth-quick">
      <button type="button" class="auth-quick-link" data-auth-tab="signup">
        New to WeatherValet? Create an account &rarr;
      </button>
    </p>
  </form>

  <!-- SIGN UP PANEL -->
  <form id="auth-signup-form" class="auth-form auth-panel" novalidate>
    <label class="auth-field">
      <span class="auth-field-label">Your name</span>
      <input type="text" name="name" required autocomplete="name"
             placeholder="M. Reynolds" class="auth-input">
    </label>
    <label class="auth-field">
      <span class="auth-field-label">Email</span>
      <input type="email" name="email" required autocomplete="email"
             placeholder="you@example.com" class="auth-input">
    </label>
    <div class="auth-field-row">
      <label class="auth-field auth-field-half">
        <span class="auth-field-label">Crew handle</span>
        <span class="auth-handle-wrap">
          <span class="auth-handle-prefix">@</span>
          <input type="text" name="handle" required autocomplete="off"
                 placeholder="timmy" class="auth-input auth-input-handle"
                 maxlength="18">
        </span>
      </label>
      <label class="auth-field auth-field-half">
        <span class="auth-field-label">Password</span>
        <input type="password" name="password" required autocomplete="new-password"
               placeholder="At least 4 chars" class="auth-input">
      </label>
    </div>
    <label class="auth-field">
      <span class="auth-field-label">Home base &middot; required for severe alerts</span>
      <span class="auth-location-wrap">
        <input type="text" name="city" required autocomplete="address-level2"
               placeholder="Lebanon, IN" class="auth-input" id="auth-signup-city">
        <button type="button" class="auth-location-geo-btn" id="auth-signup-geo-btn">
          <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
            <circle cx="12" cy="10" r="3"/>
          </svg>
          <span id="auth-signup-geo-label">Use my location</span>
        </button>
      </span>
      <p class="auth-location-help">
        We send severe weather alerts only to your home base.
        <span class="auth-location-help-em">You can change this anytime in settings.</span>
      </p>
    </label>
    <div class="auth-error" id="auth-signup-error" hidden></div>
    <button type="submit" class="auth-submit">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>
        <circle cx="9" cy="7" r="4"/>
        <path d="M22 11h-6"/><path d="M19 8v6"/>
      </svg>
      <span>Create account &amp; join the Crew</span>
    </button>
    <p class="auth-quick">
      <button type="button" class="auth-quick-link" data-auth-tab="signin">
        Already have an account? Sign in &rarr;
      </button>
    </p>
  </form>

  <p class="auth-gate-foot">
    Prototype build &middot; data is stored only on this device.
  </p>
</div>


<!-- ============ ONBOARDING — first-time-user three-card explainer ============ -->
<!-- Fires after a brand-new account signs up. Three short steps, skippable.
     Once dismissed, the user's onboardingShown flag persists so it never
     shows again for them on this device. -->
<div id="onboarding-scrim" class="onboarding-scrim"></div>
<div id="onboarding-card" class="onboarding-card" role="dialog" aria-modal="true" aria-labelledby="onboarding-title">
  <button type="button" class="onboarding-skip" data-onboard-action="skip">Skip</button>
  <div class="onboarding-illustration" id="onboarding-illustration"></div>
  <p class="onboarding-eyebrow" id="onboarding-eyebrow">Welcome &middot; Step 1 of 3</p>
  <h2 id="onboarding-title" class="onboarding-title"></h2>
  <p id="onboarding-body" class="onboarding-body"></p>
  <div class="onboarding-progress">
    <span class="onboarding-progress-dot is-active"></span>
    <span class="onboarding-progress-dot"></span>
    <span class="onboarding-progress-dot"></span>
  </div>
  <div class="onboarding-actions">
    <button type="button" class="onboarding-back" id="onboarding-back" data-onboard-action="back" hidden>
      Back
    </button>
    <button type="button" class="onboarding-cta" id="onboarding-cta" data-onboard-action="next">
      <span id="onboarding-cta-label">Next</span>
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
        <path d="M9 18l6-6-6-6"/>
      </svg>
    </button>
  </div>
</div>


<!-- ============ HUMAN VERIFICATION MODAL ============ -->
<div id="upsell-modal" class="modal-scrim">
  <div class="modal-card">
    <button class="modal-close" id="modal-close-x" type="button" aria-label="Close">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
    </button>

    <!-- STEP 1: Pricing & SMS opt-in (default visible) -->
    <div id="modal-step-pricing" class="modal-step active">
      <div class="modal-eyebrow">
        <span class="modal-eyebrow-dot"></span>
        <span class="modal-eyebrow-label">Premium · Meteorologist Review</span>
      </div>
      <h2 class="modal-title">WeatherValet Meteorologist Review</h2>
      <p class="modal-body">A WeatherValet Meteorologist will personally review your plan, the models, radar, and any Valet Crew reports — then provide a detailed written assessment with their professional forecast specifically for you.</p>

      <ul class="benefit-list">
        <li class="benefit-item">
          <span class="benefit-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
          </span>
          <div class="benefit-text">
            <span class="benefit-title">Direct analysis from a senior expert</span>
            <span class="benefit-sub">Years of experience reading HRRR, ECMWF, and NEXRAD for your region.</span>
          </div>
        </li>
        <li class="benefit-item">
          <span class="benefit-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
          </span>
          <div class="benefit-text">
            <span class="benefit-title">Detailed written assessment</span>
            <span class="benefit-sub">Not just a yes/no — the meteorologist's full reasoning, in plain English.</span>
          </div>
        </li>
        <li class="benefit-item">
          <span class="benefit-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
          </span>
          <div class="benefit-text">
            <span class="benefit-title">Faster updates if conditions change</span>
            <span class="benefit-sub">A live update arrives the moment your window shifts.</span>
          </div>
        </li>
        <li class="benefit-item">
          <span class="benefit-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
          </span>
          <div class="benefit-text">
            <span class="benefit-title">Peace of mind on high-stakes days</span>
            <span class="benefit-sub">For decisions where the cost of being wrong is bigger than $19.</span>
          </div>
        </li>
      </ul>

      <div class="pricing-divider">
        <span class="pricing-divider-label">Meteorologist review</span>
      </div>

      <div class="pricing-options">
        <button class="pricing-option is-selected" data-tier="single" type="button">
          <div class="pricing-option-head">
            <span class="pricing-option-label">Single Review</span>
            <span class="pricing-option-price">$19</span>
          </div>
          <span class="pricing-option-sub">One meteorologist review for this ticket. Want ongoing coverage? <a href="/starter" style="color:#2E4FB8;font-weight:600;">Try Pro Single for $99 your first month &rarr;</a></span>
        </button>
      </div>

      <div class="sms-block">
        <label class="sms-label">
          <span class="sms-label-text">Phone number for your update</span>
          <input type="tel" id="sms-phone" placeholder="(555) 555-5555" autocomplete="tel">
          <span class="sms-hint">We'll text you the moment your meteorologist delivers the review.</span>
        </label>
      </div>

      <div class="modal-actions">
        <button id="modal-cancel" class="modal-cancel" type="button">Maybe later</button>
        <button id="modal-confirm" class="modal-confirm" type="button">
          <span>Request My Review</span>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M7 7h10v10"/><path d="M7 17 17 7"/></svg>
        </button>
      </div>

      <p class="modal-trust">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="11" x="3" y="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        <span>Cancel anytime · No charge if no meteorologist available · Stripe secured</span>
      </p>
    </div>

    <!-- STEP 2: Standby confirmation -->
    <div id="modal-step-standby" class="modal-step">
      <div class="standby-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/>
          <polyline points="12 6 12 12 16 14"/>
        </svg>
      </div>
      <h2 class="modal-title">Your meteorologist is on the case.</h2>
      <p class="modal-body">A live meteorologist is now reviewing your plan. We'll text you at <span id="standby-phone-display" class="phone-display">your number</span> as soon as it's ready — usually within 30 minutes.</p>

      <div class="standby-meta">
        <div class="standby-meta-row">
          <span class="standby-meta-label">Assigned to</span>
          <span class="standby-meta-value">Amanda L. · Lead Meteorologist</span>
        </div>
        <div class="standby-meta-row">
          <span class="standby-meta-label">Reviewing</span>
          <span class="standby-meta-value">Models · Radar · Local Crew</span>
        </div>
        <div class="standby-meta-row">
          <span class="standby-meta-label">Estimated</span>
          <span class="standby-meta-value">Within 30 minutes</span>
        </div>
        <div class="standby-meta-row">
          <span class="standby-meta-label">You'll receive</span>
          <span class="standby-meta-value">SMS · then in-app</span>
        </div>
      </div>

      <p class="standby-reassurance">
        You can close this app — we'll bring you back the moment your review is ready.
      </p>

      <button id="modal-done" class="modal-confirm modal-confirm-full" type="button">
        Got it — I'll keep an eye on my phone
      </button>
    </div>
  </div>
</div>

<!-- ============ BACKEND CONFIGURATION ============
     Tells the prototype where to reach the WeatherValet backend (the
     Flask service deployed on Render). Used for: the AI explainer
     paragraph endpoint, the Stripe upsell flow, and any other
     server-side features.

     If you ever change the deployed URL or want testers to point at a
     different backend, edit this single value. To run the prototype
     fully offline (no backend at all), set this to an empty string —
     the AI explainer will gracefully fall back to a static paragraph
     and the Stripe upsell will run in simulated demo mode.
-->
<script>
  window.WV_API_BASE = 'https://wv-valet-backend.onrender.com';
</script>

<!-- ============ THE ENTIRE APP LOGIC IN ~150 LINES OF VANILLA JS ============ -->
<script>
(function() {
  'use strict';

  // ── Query parser ─────────────────────────────────────────────────────────

  // US state codes ↔ names. Used to disambiguate cities like "Lebanon" that
  // exist in multiple states. When the user types a state abbreviation or
  // full name, we use it to filter geocoder results to the right place.
  var US_STATES = {
    'AL': 'Alabama', 'AK': 'Alaska', 'AZ': 'Arizona', 'AR': 'Arkansas',
    'CA': 'California', 'CO': 'Colorado', 'CT': 'Connecticut', 'DE': 'Delaware',
    'FL': 'Florida', 'GA': 'Georgia', 'HI': 'Hawaii', 'ID': 'Idaho',
    'IL': 'Illinois', 'IN': 'Indiana', 'IA': 'Iowa', 'KS': 'Kansas',
    'KY': 'Kentucky', 'LA': 'Louisiana', 'ME': 'Maine', 'MD': 'Maryland',
    'MA': 'Massachusetts', 'MI': 'Michigan', 'MN': 'Minnesota', 'MS': 'Mississippi',
    'MO': 'Missouri', 'MT': 'Montana', 'NE': 'Nebraska', 'NV': 'Nevada',
    'NH': 'New Hampshire', 'NJ': 'New Jersey', 'NM': 'New Mexico', 'NY': 'New York',
    'NC': 'North Carolina', 'ND': 'North Dakota', 'OH': 'Ohio', 'OK': 'Oklahoma',
    'OR': 'Oregon', 'PA': 'Pennsylvania', 'RI': 'Rhode Island', 'SC': 'South Carolina',
    'SD': 'South Dakota', 'TN': 'Tennessee', 'TX': 'Texas', 'UT': 'Utah',
    'VT': 'Vermont', 'VA': 'Virginia', 'WA': 'Washington', 'WV': 'West Virginia',
    'WI': 'Wisconsin', 'WY': 'Wyoming', 'DC': 'District of Columbia'
  };
  // Reverse lookup: full name → code
  var US_STATE_NAMES_TO_CODES = (function() {
    var out = {};
    for (var code in US_STATES) {
      out[US_STATES[code].toLowerCase()] = code;
    }
    return out;
  })();

  /**
   * Normalize any state input ("IN", "Indiana", "indiana") to its 2-letter code.
   * Returns the uppercased code, or null if not a recognized US state.
   */
  function normalizeStateCode(raw) {
    if (!raw) return null;
    var trimmed = raw.trim();
    var upper = trimmed.toUpperCase();
    if (US_STATES[upper]) return upper;
    var lower = trimmed.toLowerCase();
    if (US_STATE_NAMES_TO_CODES[lower]) return US_STATE_NAMES_TO_CODES[lower];
    return null;
  }

  var KNOWN_CITIES = {
    'lebanon':       { city: 'Lebanon',      state: 'IN', zip: '46052' },
    'indianapolis':  { city: 'Indianapolis', state: 'IN', zip: '46204' },
    'whitestown':    { city: 'Whitestown',   state: 'IN', zip: '46075' },
    'zionsville':    { city: 'Zionsville',   state: 'IN', zip: '46077' },
    'dallas':        { city: 'Dallas',       state: 'TX', zip: '75201' },
    'austin':        { city: 'Austin',       state: 'TX', zip: '78701' },
    'houston':       { city: 'Houston',      state: 'TX', zip: '77002' },
    'phoenix':       { city: 'Phoenix',      state: 'AZ', zip: '85003' },
    'charlotte':     { city: 'Charlotte',    state: 'NC', zip: '28202' },
    'atlanta':       { city: 'Atlanta',      state: 'GA', zip: '30303' },
    'denver':        { city: 'Denver',       state: 'CO', zip: '80202' },
    'des moines':    { city: 'Des Moines',   state: 'IA', zip: '50309' },
    'chicago':       { city: 'Chicago',      state: 'IL', zip: '60601' },
    'nashville':     { city: 'Nashville',    state: 'TN', zip: '37203' },
    'tampa':         { city: 'Tampa',        state: 'FL', zip: '33602' },
    'miami':         { city: 'Miami',        state: 'FL', zip: '33130' }
  };

  var INDUSTRY_PATTERNS = [
    { match: /\b(concrete|pour|slab|foundation|footer|footing)\b/i,        noun: 'pour',         industry: 'concrete' },
    { match: /\b(roof|roofing|tear-?off|re-?roof|shingle)\b/i,             noun: 'roofing job',  industry: 'roofing' },
    { match: /\b(harvest|combine|soybean|corn|spray|sprayer)\b/i,          noun: 'harvest run',  industry: 'agriculture' },
    { match: /\b(wedding|ceremony|reception)\b/i,                          noun: 'ceremony',     industry: 'event' },
    { match: /\b(festival|concert|music|performance)\b/i,                  noun: 'festival',     industry: 'event' },
    // Sporting events and outdoor recreation — same sensitivities as events
    // (rain delays, lightning halts play, wind affects everything from punts to
    // serves to flyballs). Calling them "event" so they share rules and copy.
    { match: /\b(baseball|softball|football|soccer|tennis|golf|game|match|tournament|race|marathon|tailgate|cookout|barbecue|bbq|picnic|parade)\b/i,
                                                                            noun: 'game',         industry: 'event' },
    { match: /\b(crane|lift|rigging|hoist|excavator|equipment)\b/i,        noun: 'lift',         industry: 'crane' },
    { match: /\b(paint|coating|seal|sealant|asphalt|paving)\b/i,           noun: 'application',  industry: 'painting' }
  ];

  function parseQuery(q) {
    q = (q || '').trim();
    if (!q) return null;

    var noun = 'plan';
    var industry = 'default';
    for (var i = 0; i < INDUSTRY_PATTERNS.length; i++) {
      if (INDUSTRY_PATTERNS[i].match.test(q)) {
        noun = INDUSTRY_PATTERNS[i].noun;
        industry = INDUSTRY_PATTERNS[i].industry;
        break;
      }
    }

    // ────────────────────────────────────────────────────────────────────────
    // Time window parsing
    // ────────────────────────────────────────────────────────────────────────
    // The window concept replaces a single scheduled hour with a (startHour,
    // endHour) tuple. Most queries still produce a narrow window centered on
    // a specific time (a 10 AM pour gets hours 9-12, ±1.5h buffer), but the
    // parser also recognizes:
    //   • Time ranges: "6 PM to 10 PM", "from 2 to 5 PM", "between 4 and 7 PM"
    //   • Named periods: "all day", "this morning", "this evening", "tonight"
    // Ranges use the explicit user-given bounds with a small ±30min buffer.
    // Named periods use sensible defaults for that time of day.
    var scheduledShort = '9 AM';
    var hourNum = 9;
    var windowStartHour = 8;   // default "morning" window
    var windowEndHour = 11;
    var hasExplicitWindow = false;
    // ── Input-quality flags ───────────────────────────────────────────────
    // Track whether the user's query contained a time signal we could parse,
    // and whether it contained a location. Time defaults to 9 AM (with a
    // visible "Heads up" banner so the user knows we assumed). Location
    // does NOT default — when missing, the resolver throws NO_LOCATION
    // and the renderer asks the user to add one, since picking any single
    // city as a fallback would be wrong for a nationwide product.
    var timeFound = false;
    var locationFound = false;

    function parseHour(hourStr, periodStr) {
      var h = parseInt(hourStr, 10);
      var p = (periodStr || '').replace(/\./g, '').toUpperCase();
      if (p === 'PM' && h !== 12) h += 12;
      if (p === 'AM' && h === 12) h = 0;
      return h;
    }
    function formatTimeShort(h24, min) {
      var p = h24 >= 12 ? 'PM' : 'AM';
      var h = h24 % 12 || 12;
      return (min && min !== '00') ? (h + ':' + min + ' ' + p) : (h + ' ' + p);
    }

    // ── Range patterns (highest priority — explicit user intent) ──
    // "6 PM to 10 PM" / "from 2 to 5 PM" / "between 4 PM and 7 PM"
    // Capture two times with optional periods on either side.
    var rangeMatch = q.match(
      /(\d{1,2})(?::(\d{2}))?\s*(am|pm|a\.m\.|p\.m\.)?\s*(?:to|-|–|until|till|through|and)\s*(\d{1,2})(?::(\d{2}))?\s*(am|pm|a\.m\.|p\.m\.)/i
    );
    if (rangeMatch) {
      // Period on the second time always wins; if the first time has no
      // period, infer from the second (e.g. "2 to 5 PM" → both PM).
      var endPeriod = rangeMatch[6];
      var startPeriod = rangeMatch[3] || endPeriod;
      var startH = parseHour(rangeMatch[1], startPeriod);
      var endH = parseHour(rangeMatch[4], endPeriod);
      // If the range spans across noon (e.g. "10 to 2 PM" should be 10 AM to 2 PM)
      // and the start>end inference produced an inverted range, fall back.
      if (endH <= startH) {
        // Try with the start as AM if not explicit
        if (!rangeMatch[3]) {
          startH = parseHour(rangeMatch[1], 'AM');
        }
      }
      windowStartHour = Math.max(0, Math.min(23, startH));
      windowEndHour = Math.max(0, Math.min(23, endH));
      hourNum = windowStartHour;
      scheduledShort = formatTimeShort(startH, rangeMatch[2]) + '–' + formatTimeShort(endH, rangeMatch[5]);
      hasExplicitWindow = true;
      timeFound = true;
    }

    // ── Named long periods ──
    if (!hasExplicitWindow) {
      // "Right now" / "now" — the current 2-hour window. Detected before
      // other named periods because phrases like "right now this evening"
      // should still resolve to "right now" (the user is asking about
      // immediate conditions, not later). The window starts at the
      // current local hour and extends 2 hours forward.
      if (/\b(?:right\s+now|now)\b/i.test(q)) {
        var nowDate = new Date();
        var currentHour = nowDate.getHours();
        windowStartHour = currentHour;
        // Cap the window at 23 so we don't accidentally request hour 24+
        // — the same-day forecast array doesn't have those slots.
        windowEndHour = Math.min(23, currentHour + 2);
        hourNum = currentHour;
        // formatTimeShort handles the AM/PM formatting from a 24-hour input.
        scheduledShort = 'right now (' + formatTimeShort(currentHour, null) + ')';
        hasExplicitWindow = true;
        timeFound = true;
      } else if (/\ball\s+day\b/i.test(q)) {
        // Conservative working-hours band: 7 AM to 8 PM
        windowStartHour = 7;
        windowEndHour = 20;
        hourNum = 12;
        scheduledShort = 'all day';
        hasExplicitWindow = true;
        timeFound = true;
      } else if (/\btonight\b/i.test(q)) {
        windowStartHour = 19;
        windowEndHour = 23;
        hourNum = 20;
        scheduledShort = 'tonight';
        hasExplicitWindow = true;
        timeFound = true;
      } else if (/\bthis\s+morning\b/i.test(q)) {
        windowStartHour = 7;
        windowEndHour = 11;
        hourNum = 9;
        scheduledShort = 'this morning';
        hasExplicitWindow = true;
        timeFound = true;
      } else if (/\bthis\s+afternoon\b/i.test(q)) {
        windowStartHour = 12;
        windowEndHour = 17;
        hourNum = 14;
        scheduledShort = 'this afternoon';
        hasExplicitWindow = true;
        timeFound = true;
      } else if (/\bthis\s+evening\b/i.test(q)) {
        windowStartHour = 17;
        windowEndHour = 21;
        hourNum = 18;
        scheduledShort = 'this evening';
        hasExplicitWindow = true;
        timeFound = true;
      } else if (/\b(?:12\s*)?noon\b/i.test(q)) {
        // "noon" / "12 noon" / "at noon" — the bug: this previously fell through
        // and the lock-screen showed the default '9 AM'. Now: 12 PM with a
        // ±1-hour window centered on noon.
        windowStartHour = 11;
        windowEndHour = 14;
        hourNum = 12;
        scheduledShort = '12 PM';
        hasExplicitWindow = true;
        timeFound = true;
      } else if (/\bmidnight\b/i.test(q)) {
        // Edge case but worth handling cleanly. 11 PM to 1 AM band.
        windowStartHour = 23;
        windowEndHour = 23;  // Open-Meteo gives us next-day hours separately
        hourNum = 0;
        scheduledShort = '12 AM';
        hasExplicitWindow = true;
        timeFound = true;
      }
    }

    // ── Relative time phrases (B-X5 fix, May 17) ─────────────────────
    // "in an hour" / "in 2 hours" / "in 30 minutes" / "today" / "later"
    // / "soon" / "shortly". Previously these fell through and the lock
    // screen displayed the initial-default '9 AM', which was wrong and
    // confusing — the user clearly wanted near-term weather but got an
    // arbitrary morning slot.
    if (!hasExplicitWindow) {
      var nowDate = new Date();
      var nowHour = nowDate.getHours();

      // "in N hour(s)" / "in an hour" / "in a couple hours"
      var hoursAheadMatch = q.match(/\bin\s+(an?|one|two|three|four|five|six|a\s+couple|a\s+few|\d{1,2})\s+(?:hour|hr)s?\b/i);
      if (hoursAheadMatch) {
        var word = hoursAheadMatch[1].toLowerCase().replace(/\s+/g, ' ');
        var hoursAhead = 1;
        if (/^\d+$/.test(word)) {
          hoursAhead = Math.min(12, parseInt(word, 10));
        } else if (word === 'an' || word === 'a' || word === 'one') {
          hoursAhead = 1;
        } else if (word === 'two') { hoursAhead = 2; }
        else if (word === 'three') { hoursAhead = 3; }
        else if (word === 'four') { hoursAhead = 4; }
        else if (word === 'five') { hoursAhead = 5; }
        else if (word === 'six') { hoursAhead = 6; }
        else if (word === 'a couple') { hoursAhead = 2; }
        else if (word === 'a few') { hoursAhead = 3; }
        var targetHour = Math.min(23, nowHour + hoursAhead);
        windowStartHour = targetHour;
        windowEndHour = Math.min(23, targetHour + 1);
        hourNum = targetHour;
        scheduledShort = 'in ' + (hoursAhead === 1 ? 'an hour' : (hoursAhead + ' hours'))
                       + ' (~' + formatTimeShort(targetHour, null) + ')';
        hasExplicitWindow = true;
        timeFound = true;
      }

      // "in N minute(s)" / "in 30 min" — same-hour window since the
      // hourly forecast resolution is hourly anyway, but we acknowledge
      // the request with current-hour data.
      if (!hasExplicitWindow) {
        var minsAheadMatch = q.match(/\bin\s+(\d{1,3})\s+(?:minute|min)s?\b/i);
        if (minsAheadMatch) {
          var mins = parseInt(minsAheadMatch[1], 10);
          var addHours = Math.floor(mins / 60);
          var targetH = Math.min(23, nowHour + addHours);
          windowStartHour = nowHour;
          windowEndHour = Math.min(23, targetH + 1);
          hourNum = targetH;
          scheduledShort = 'in ' + mins + ' min (~' + formatTimeShort(targetH, null) + ')';
          hasExplicitWindow = true;
          timeFound = true;
        }
      }

      // "soon" / "shortly" / "in a bit" / "in a minute"
      if (!hasExplicitWindow && /\b(?:soon|shortly|in\s+a\s+bit|in\s+a\s+minute|in\s+a\s+sec)\b/i.test(q)) {
        windowStartHour = nowHour;
        windowEndHour = Math.min(23, nowHour + 2);
        hourNum = nowHour;
        scheduledShort = 'soon (~' + formatTimeShort(nowHour, null) + ')';
        hasExplicitWindow = true;
        timeFound = true;
      }

      // "today" / "later today" / "rest of the day" — covers the
      // remaining waking hours, not the whole 24h. If it's already
      // evening, narrow to evening; afternoon → afternoon onwards;
      // morning → broad daytime window.
      if (!hasExplicitWindow && /\b(?:later\s+today|today|rest\s+of\s+(?:the\s+)?day|remainder\s+of\s+(?:the\s+)?day)\b/i.test(q)) {
        if (nowHour < 12) {
          // Morning — full waking day ahead
          windowStartHour = Math.max(nowHour, 7);
          windowEndHour = 20;
          hourNum = 12;
          scheduledShort = 'today';
        } else if (nowHour < 17) {
          // Afternoon
          windowStartHour = nowHour;
          windowEndHour = 20;
          hourNum = Math.min(15, nowHour + 1);
          scheduledShort = 'rest of today';
        } else {
          // Evening — narrow to remaining evening hours
          windowStartHour = nowHour;
          windowEndHour = Math.min(23, nowHour + 4);
          hourNum = nowHour;
          scheduledShort = 'this evening';
        }
        hasExplicitWindow = true;
        timeFound = true;
      }
    }

    // ── Single-time pattern (the original parser path) ──
    if (!hasExplicitWindow) {
      var timeMatch = q.match(/(\d{1,2})(?::(\d{2}))?\s*(am|pm|a\.m\.|p\.m\.)/i);
      if (timeMatch) {
        var hour = timeMatch[1];
        var min = timeMatch[2] || '00';
        var period = timeMatch[3].replace(/\./g, '').toUpperCase();
        scheduledShort = (min === '00') ? (hour + ' ' + period) : (hour + ':' + min + ' ' + period);
        hourNum = parseHour(hour, period);
        windowStartHour = Math.max(0, hourNum - 1);
        windowEndHour = Math.min(23, hourNum + 2);
        timeFound = true;
      } else if (/\bmorning\b/i.test(q))   { scheduledShort = '8 AM'; hourNum = 8;  windowStartHour = 7; windowEndHour = 10; timeFound = true; }
      else if (/\bafternoon\b/i.test(q))   { scheduledShort = '1 PM'; hourNum = 13; windowStartHour = 12; windowEndHour = 16; timeFound = true; }
      else if (/\bevening\b/i.test(q))     { scheduledShort = '5 PM'; hourNum = 17; windowStartHour = 16; windowEndHour = 19; timeFound = true; }
    }

    var lowerQ = q.toLowerCase();
    // No hardcoded default city. WeatherValet is nationwide — picking any
    // single town as a fallback would be wrong for every user not in that
    // town. When parsing fails to find a location, `city` stays null and
    // the renderer gates on `locationFound === false` to ask the user
    // for one rather than silently guessing.
    var city = null, area = null;
    var stateCode = null;  // Explicit state from query, e.g. "IN" or "Indiana"
    var zip = null;

    // ── Multi-location: "driving from X to Y" / "from X to Y" / "X to Y trip" ──
    // Routes are sets of locations the user cares about — usually a journey
    // where the worst weather along the path matters. We capture an array of
    // {city, stateCode} stops here; the resolver will geocode each and produce
    // a worst-case-across-stops verdict.
    //
    // For now: 2-stop routes only. Multi-leg ("from A to B to C") is parsed
    // but stops are concatenated linearly; no time-of-arrival modeling yet.
    var locations = []; // populated only for routes; empty for single-city queries

    // ── Location parsing helpers ─────────────────────────────────────────
    // The user's query is freeform — capitalization, punctuation, and word
    // order are all up for grabs. Three bits of post-processing make the
    // raw regex captures usable:
    //
    //   LEADING_STOPS — words that can never start a city. If the regex
    //     captures "in <word>" where <word> is one of these, we reject
    //     the match. Example: "rain in the morning" should not produce
    //     city="the".
    //
    //   TRAILING_STOPS — words that can never appear inside a city name
    //     (English connectors, time tokens, day names). When a multi-
    //     word city capture contains one of these, we truncate at it.
    //     Example: "in columbus ohio at 4 PM" → captured "columbus ohio at"
    //     gets cleaned to "columbus ohio".
    //
    //   US_STATE_CODES — when the user typed the state without a comma
    //     ("Columbus OH", "Fort Wayne IN"), the regex puts the state
    //     code in the city capture. We detect 2-letter trailing tokens
    //     that match US codes and pull them out as the state.
    //
    // Both regexes below use the `i` flag so lowercase queries
    // ("saturday wedding in columbus, ohio") parse correctly. The
    // post-processing handles the false positives that case-insensitivity
    // would otherwise create.
    var LEADING_STOPS = {
      'the':1,'a':1,'an':1,'my':1,'your':1,'our':1,'this':1,'that':1,'these':1,'those':1,
      'some':1,'any':1,'no':1,'few':1,'couple':1,'several':1,'many':1,'most':1,
      'morning':1,'afternoon':1,'evening':1,'night':1,'tonight':1,'tomorrow':1,'today':1,
      'fact':1,'case':1,'general':1,'particular':1,'reality':1,'practice':1,'theory':1,
      'order':1,'front':1,'back':1,'middle':1,'center':1,'favor':1,'time':1,'sight':1
    };
    var TRAILING_STOPS = {
      'at':1,'by':1,'for':1,'to':1,'from':1,'on':1,'near':1,'around':1,'with':1,
      'tomorrow':1,'today':1,'tonight':1,
      'monday':1,'tuesday':1,'wednesday':1,'thursday':1,'friday':1,'saturday':1,'sunday':1,
      'mon':1,'tue':1,'tues':1,'wed':1,'thu':1,'thur':1,'thurs':1,'fri':1,'sat':1,'sun':1,
      'morning':1,'afternoon':1,'evening':1,'night':1,
      'am':1,'pm':1
    };
    var US_STATE_CODES_LOWER = {
      'al':1,'ak':1,'az':1,'ar':1,'ca':1,'co':1,'ct':1,'de':1,'fl':1,'ga':1,'hi':1,'id':1,
      'il':1,'in':1,'ia':1,'ks':1,'ky':1,'la':1,'me':1,'md':1,'ma':1,'mi':1,'mn':1,'ms':1,
      'mo':1,'mt':1,'ne':1,'nv':1,'nh':1,'nj':1,'nm':1,'ny':1,'nc':1,'nd':1,'oh':1,'ok':1,
      'or':1,'pa':1,'ri':1,'sc':1,'sd':1,'tn':1,'tx':1,'ut':1,'vt':1,'va':1,'wa':1,'wv':1,
      'wi':1,'wy':1,'dc':1
    };

    // Capitalize a city name nicely: "columbus" → "Columbus",
    // "winston-salem" → "Winston-Salem", "st. louis" → "St. Louis".
    // Used for display so the resolved ticket reads naturally even
    // when the user typed in all lowercase.
    function titleCaseCity(s) {
      if (!s) return s;
      return s.replace(/\b([a-z])([a-z]*)/g, function(_, first, rest) {
        return first.toUpperCase() + rest;
      });
    }

    // Clean a raw city capture: reject if first word is a leading-stop,
    // truncate at any trailing-stop, and split off a trailing US state code
    // when present (e.g. "columbus oh" → city="Columbus" + state="OH").
    // Returns { city, stateCode } or null if the match should be rejected.
    function cleanCityCapture(rawCity, rawState) {
      if (!rawCity) return null;
      var words = rawCity.trim().split(/\s+/);
      var firstLower = words[0].toLowerCase();
      if (LEADING_STOPS[firstLower]) return null;
      var clean = [words[0]];
      var inferredState = null;
      for (var i = 1; i < words.length; i++) {
        var bareWord = words[i].toLowerCase().replace(/\.$/, '');
        if (TRAILING_STOPS[bareWord]) break;
        // If we have no explicit state and this word is a US state code,
        // promote it to the state and stop accumulating city words.
        if (!rawState && US_STATE_CODES_LOWER[bareWord]) {
          inferredState = words[i];
          break;
        }
        clean.push(words[i]);
      }
      return {
        city: titleCaseCity(clean.join(' ')),
        stateCode: rawState || inferredState
      };
    }

    // Route regex — case-insensitive. Cities may contain internal hyphens
    // (Winston-Salem) or trailing-period abbreviations (St. Louis, Mt. Pleasant).
    var routeMatch = q.match(
      /(?:driving\s+|drive\s+|trip\s+|route\s+|going\s+)?from\s+([A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?(?:\s+[A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?){0,2})(?:\s*,\s*([A-Za-z]{2,}))?\s+to\s+([A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?(?:\s+[A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?){0,2})(?:\s*,\s*([A-Za-z]{2,}))?(?:\s+(?:to|via|through)\s+([A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?(?:\s+[A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?){0,2})(?:\s*,\s*([A-Za-z]{2,}))?)?/i
    );
    if (routeMatch) {
      var startClean = cleanCityCapture(routeMatch[1], routeMatch[2]);
      var endClean   = cleanCityCapture(routeMatch[3], routeMatch[4]);
      // If either end fails the cleaner (rare — would mean the route
      // started with a stopword like "from the"), fall through to the
      // inline regex below.
      if (startClean && endClean) {
        var stops = [
          { city: startClean.city, stateCode: normalizeStateCode((startClean.stateCode || '').trim()) },
          { city: endClean.city,   stateCode: normalizeStateCode((endClean.stateCode || '').trim()) }
        ];
        if (routeMatch[5]) {
          // 3-stop route: A → B → C
          var viaClean = cleanCityCapture(routeMatch[5], routeMatch[6]);
          if (viaClean) {
            stops.splice(1, 0, { city: viaClean.city, stateCode: normalizeStateCode((viaClean.stateCode || '').trim()) });
          }
        }
        locations = stops;
        city = stops[0].city;
        stateCode = stops[0].stateCode;
        locationFound = true;
      }
    }

    // Inline "in <City>, <State>" pattern — case-insensitive. Cleaning is
    // applied to handle lowercase input, missing punctuation, and false-
    // positive phrases like "in the morning".
    if (locations.length === 0) {
      var explicitMatch = q.match(
        /\bin\s+([A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?(?:\s+[A-Za-z][a-zA-Z]+(?:[-\u2013][A-Za-z]+)?\.?){0,2})(?:\s*,\s*([A-Za-z][A-Za-z\s]*?))?(?=\s|[?!.]|$)/i
      );
      if (explicitMatch) {
        var cleaned = cleanCityCapture(explicitMatch[1], explicitMatch[2]);
        if (cleaned) {
          city = cleaned.city;
          if (cleaned.stateCode) {
            stateCode = normalizeStateCode(cleaned.stateCode.trim());
          }
          locationFound = true;
        }
      }
    }

    // Fall back to KNOWN_CITIES dictionary for short queries that don't
    // include "in <City>" structure (e.g. "Roofing tomorrow, Charlotte").
    // Only use the dictionary if we don't already have a state from explicit
    // parsing — explicit always wins.
    if (!stateCode) {
      for (var key in KNOWN_CITIES) {
        if (lowerQ.indexOf(key) !== -1) {
          // If the query says "in Lebanon, TX" we already captured Lebanon+TX
          // above; we should not overwrite with the IN default.
          // The check above with !stateCode ensures we only fall through
          // to the dictionary when no state was given.
          city = KNOWN_CITIES[key].city;
          stateCode = KNOWN_CITIES[key].state;
          zip = KNOWN_CITIES[key].zip;
          locationFound = true;
          break;
        }
      }
    }

    // Build the human-readable area string. The geocoder will replace this
    // with authoritative data once it resolves; this is the placeholder.
    if (stateCode && zip) {
      area = city + ', ' + stateCode + ' · ' + zip;
    } else if (stateCode) {
      area = city + ', ' + stateCode;
    } else {
      area = city;
    }

    var pad = function(n) { return (n < 10 ? '0' : '') + n; };
    var windowStart = pad(windowStartHour) + ':30';
    var windowEnd = pad(windowEndHour) + ':00';

    // ── Date parsing ───────────────────────────────────────────────────
    // Three layers, highest-specificity first:
    //   1. Explicit "tomorrow" → +1 day
    //   2. Day name (Mon, Tuesday, Fri, etc.) → days until next occurrence
    //      Today (e.g. Thursday) + "Friday" → +1. Today + "Thursday" → 0.
    //      Today + "Wednesday" → +6 (next week's Wednesday).
    //   3. Default: today (offset 0)
    // We don't yet handle "this Friday" / "next Friday" disambiguation —
    // both English speakers and dictionaries disagree on what "next Friday"
    // means, so we treat any Friday mention as "the soonest Friday."
    // Backlog: explicit dates ("May 12"), relative weeks ("next week"),
    // and the this/next disambiguation if testers ask.
    var dateOffsetDays = 0;
    var dayMatch = q.match(/\b(monday|tuesday|wednesday|thursday|friday|saturday|sunday|mon|tues?|wed|thurs?|fri|sat|sun)\b/i);
    // Reject the match if the day-name token is part of a city name
    // ("Sun City, FL", "Wed Springs"). The simplest reliable check:
    // is the token preceded by "in " in the original query? If so, it's
    // a location, not a date.
    if (dayMatch) {
      var matchIndex = dayMatch.index;
      var leadIn = q.substring(Math.max(0, matchIndex - 3), matchIndex).toLowerCase();
      if (leadIn === 'in ') {
        dayMatch = null;
      }
    }
    if (/\btomorrow\b/i.test(q)) {
      dateOffsetDays = 1;
    } else if (dayMatch) {
      var DAY_MAP = {
        sun: 0, sunday: 0,
        mon: 1, monday: 1,
        tue: 2, tues: 2, tuesday: 2,
        wed: 3, wednesday: 3,
        thu: 4, thur: 4, thurs: 4, thursday: 4,
        fri: 5, friday: 5,
        sat: 6, saturday: 6
      };
      var targetDow = DAY_MAP[dayMatch[1].toLowerCase()];
      var todayDow = new Date().getDay();
      // Days until next occurrence. If user types today's day name, treat
      // as today (offset 0). Otherwise count forward to the next match.
      var diff = targetDow - todayDow;
      if (diff < 0) diff += 7;
      dateOffsetDays = diff;
    }

    // When the plan is for a future day, fold the day into the visible
    // time string so the ticket clearly reads "5 PM Friday" rather than
    // a bare "5 PM" that could be mistaken for today. "tomorrow" stays
    // as "tomorrow" because that reads more naturally than "5 PM
    // Friday" when someone literally typed "tomorrow."
    if (dateOffsetDays === 1 && /\btomorrow\b/i.test(q)) {
      scheduledShort = scheduledShort + ' tomorrow';
    } else if (dateOffsetDays > 0) {
      var DAY_NAMES = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
      var planDay = (new Date().getDay() + dateOffsetDays) % 7;
      scheduledShort = scheduledShort + ' ' + DAY_NAMES[planDay];
    }

    var seed = 0;
    for (var j = 0; j < q.length; j++) seed += q.charCodeAt(j);
    var id = String(20000 + (seed % 9999));

    return {
      id: id,
      query: q,
      industryLabel: noun,
      industry: industry,
      city: city,
      stateCode: stateCode,
      locations: locations,    // [] for single-city; [{city,stateCode}, ...] for routes
      area: area,
      scheduledShort: scheduledShort,
      windowStart: windowStart,
      windowEnd: windowEnd,
      windowStartHour: windowStartHour,
      windowEndHour: windowEndHour,
      plannedHour: hourNum,    // user's declared start hour (vs. the buffered window start)
      dateOffsetDays: dateOffsetDays,
      // Input-quality flags. The ticket renderer surfaces a "we assumed..."
      // banner when either is false, so the user knows what we made up.
      timeFound: timeFound,
      locationFound: locationFound,
      seed: seed
    };
  }

  // ── Status semantics ─────────────────────────────────────────────────────
  function computeStatus(seed) {
    var roll = seed % 10;
    if (roll < 6) return 'clear';
    if (roll < 9) return 'caution';
    return 'risk';
  }

  var STATUS = {
    clear: {
      label: 'Status: Clear',
      summary: 'Your window looks good.',
      verb: 'Clear',
      verbClass: 'notif-text-em-clear',
      pillClass: 'status-clear',
      tail: '.',
      stats: [
        { key: 'Precipitation', value: '0%',           verifier: 'HRRR Model' },
        { key: 'Wind',          value: '4–6 mph',      verifier: 'FAA METAR' },
        { key: 'Sky',           value: 'Mostly Sunny', verifier: 'Valet Crew' }
      ],
      confidence: 94
    },
    caution: {
      label: 'Status: Caution',
      summary: 'Your window needs attention.',
      verb: 'Caution',
      verbClass: 'notif-text-em-caution',
      pillClass: 'status-caution',
      tail: ' — watch wind shift.',
      stats: [
        { key: 'Precipitation', value: '20%',            verifier: 'HRRR + Crew' },
        { key: 'Wind',          value: '12–18 mph',      verifier: 'ASOS · Local' },
        { key: 'Sky',           value: 'Building Cells', verifier: 'Valet Crew' }
      ],
      confidence: 82
    },
    risk: {
      label: 'Status: Risk',
      summary: 'Your window is at risk.',
      verb: 'Risk',
      verbClass: 'notif-text-em-risk',
      pillClass: 'status-risk',
      tail: ' — re-plan recommended.',
      stats: [
        { key: 'Precipitation', value: '78%',           verifier: 'HRRR + NEXRAD' },
        { key: 'Wind',          value: '22–34 mph',     verifier: 'ASOS · Gust' },
        { key: 'Sky',           value: 'Active Storm',  verifier: 'Valet Crew' }
      ],
      confidence: 68
    }
  };

  // ── State ────────────────────────────────────────────────────────────────
  var currentQuery = '';

  // ── DOM helpers ──────────────────────────────────────────────────────────
  function $(id) { return document.getElementById(id); }
  function escapeHtml(s) {
    return String(s)
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;')
      .replace(/'/g, '&#39;');
  }

  /**
   * Confidence-band system. Every forecast confidence percentage maps to
   * one of four bands, each tied to a specific customer action. The number
   * itself is secondary; the band label carries the meaning.
   *
   *   95+  → "High confidence"  : models agree, data clean, no edge cases
   *   80-94 → "Confident"       : forecast holds up under normal variability
   *   65-79 → "Watching"        : some uncertainty in models or timing
   *   <65  → "Low confidence"   : significant model spread or rapid changes
   *
   * action is the recommended posture for the customer. We surface this
   * alongside the percentage on the ticket so the customer doesn't have
   * to translate a number into an action themselves.
   *
   * For Met-reviewed forecasts, the Met owns the confidence number — they
   * can override the AI's calculation with their professional judgment.
   */
  function confidenceBand(pct) {
    var n = Number(pct) || 0;
    if (n >= 95) return { label: 'High confidence', action: 'Bet on it', tier: 'high' };
    if (n >= 80) return { label: 'Confident',       action: 'Plan as scheduled', tier: 'mid' };
    if (n >= 65) return { label: 'Watching',        action: 'Plan with a backup', tier: 'low' };
    return                  { label: 'Low confidence', action: 'Worth a Met review', tier: 'flag' };
  }

  /**
   * Compress a verbose reason string into a short notification-ready phrase.
   *
   * Reasons in the ticket are 5-15 words long because they have to be
   * actionable on their own. Notifications need to fit on a lock screen
   * with the rest of the message — typically 3-6 words for the tail.
   *
   * The strategy is intent-detection: figure out what the reason is
   * about (rain, wind, storms, temp, alerts, lookahead), then return
   * a hand-tuned short phrase. Falls back to truncating the original
   * if no pattern matches.
   */
  function shortenReasonForNotif(reason) {
    if (!reason) return 'check the details';
    var lower = reason.toLowerCase();
    // NWS alerts get high priority
    if (lower.indexOf('nws') === 0) {
      // "NWS Severe Thunderstorm Warning active" → "NWS warning active"
      var ev = reason.replace(/^NWS\s+/i, '');
      // Trim to roughly 4-5 words
      var bits = ev.split(/\s+/).slice(0, 4);
      return 'NWS ' + bits.join(' ').toLowerCase();
    }
    // Lightning / thunderstorm / storms
    if (/\b(lightning|thunderstorm|storms?\s+nearby|storms?\s+possible|scattered\s+storms?)\b/i.test(reason)) {
      if (lower.indexOf('lightning') !== -1) return 'lightning risk nearby';
      if (lower.indexOf('within') !== -1) return 'storms close to your window';
      return 'storms developing nearby';
    }
    // Wind / gusts
    if (/\b(wind|gusts?|breezy)\b/i.test(reason)) {
      if (/\bgusts?\b/i.test(reason)) return 'watch the gusts';
      return 'watch the wind';
    }
    // Rain / precip
    if (/\b(rain|precipitation|showers?|drizzle)\b/i.test(reason)) {
      if (/rising|trend/i.test(reason)) return 'rain chances rising';
      if (/cure|after\s+pour|after.*application/i.test(reason)) return 'rain after the work';
      if (/scattered\s+showers?/i.test(reason)) return 'scattered showers nearby';
      if (/possible\s+rain|rain\s+possible|showers?\s+possible/i.test(reason)) return 'showers possible';
      return 'plan for rain';
    }
    // Temperature
    if (/\b(°F|temperature|cold|warm|hot|cool)\b/i.test(reason)) {
      if (/below|cold|cool/i.test(reason)) return 'temps too cool';
      if (/above|hot|warm|flash/i.test(reason)) return 'temps too warm';
      return 'watch the temps';
    }
    // Humidity
    if (/humid/i.test(reason)) return 'high humidity';
    // Coverage / nearby / route
    if (/route|along your route/i.test(reason)) return 'rough patch on your route';
    // Generic fallback — truncate to ~4 words
    var words = reason.split(/\s+/).slice(0, 5);
    return words.join(' ').toLowerCase().replace(/[—,.:;]/g, '').trim();
  }

  /**
   * Build the human-verification upsell block, adapting tone and prominence
   * to the verdict status.
   *
   * Clear  → understated. The customer trusts the AI; we offer the option
   *          without pushing it.
   * Caution → prominent. There's uncertainty in the call — a meteorologist
   *           review is genuinely valuable. Add the "definitive answer" line.
   * Risk   → most prominent. The customer is potentially canceling a $40K
   *          decision based on a model — get a human in front of it before
   *          they lose money on a false positive (or proceed on a false
   *          negative). Same definitive-answer line, slightly sharper edge.
   */
  function buildUpsellHtml(statusKey) {
    var prominent = (statusKey === 'caution' || statusKey === 'risk');
    var headline, body, definitiveLine;
    // The body copy is the same across all three statuses — it's the new
    // canonical description of what a meteorologist review actually delivers
    // (a written assessment with their professional forecast). What changes
    // by status is the headline (urgency) and whether we add the definitive
    // line (which makes the option feel meaningfully different on uncertain
    // verdicts vs. clear ones).
    body = 'A WeatherValet Meteorologist will personally review your plan, the models, radar, and any Valet Crew reports — then provide a detailed written assessment with their professional forecast specifically for you.';
    if (statusKey === 'risk') {
      headline = 'Want a WeatherValet Meteorologist to confirm before you decide?';
      definitiveLine = 'For a written assessment from a live meteorologist, tap below.';
    } else if (statusKey === 'caution') {
      headline = 'Get a WeatherValet Meteorologist Review on this one.';
      definitiveLine = 'For a written assessment from a live meteorologist, tap below.';
    } else {
      // Clear day — keep the understated framing
      headline = 'Want a real WeatherValet Meteorologist Review?';
      definitiveLine = null;
    }

    var html = ''
      + '<div class="upsell-block' + (prominent ? ' upsell-prominent' : '') + '">'
      +   '<div class="upsell-head">'
      +     '<div class="upsell-eyebrow">'
      +       '<span class="upsell-eyebrow-dot"></span>'
      +       '<span class="upsell-eyebrow-label">Premium · Meteorologist Review</span>'
      +     '</div>'
      +   '</div>'
      +   '<h3 class="upsell-title">' + escapeHtml(headline) + '</h3>'
      +   '<p class="upsell-body">' + escapeHtml(body) + '</p>'
      + (definitiveLine
          ? '<p class="upsell-definitive"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 4 6v6c0 5 3.5 9.5 8 10 4.5-.5 8-5 8-10V6l-8-4z"/><path d="m9 12 2 2 4-4"/></svg><span>' + escapeHtml(definitiveLine) + '</span></p>'
          : '')
      +   '<button id="upsell-cta" class="upsell-cta" type="button">'
      +     '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 4 6v6c0 5 3.5 9.5 8 10 4.5-.5 8-5 8-10V6l-8-4z"/><path d="m9 12 2 2 4-4"/></svg>'
      +     '<span>Request Meteorologist Review</span>'
      +   '</button>'
      +   '<div class="upsell-pricing">'
      +     '<span class="upsell-price">From $19</span>'
      +     '<span class="upsell-price-divider">·</span>'
      +     '<span class="upsell-price-alt">Most reviews delivered in under 30 minutes</span>'
      +   '</div>'
      + '</div>';
    return html;
  }
  function showScreen(id) {
    var screens = document.querySelectorAll('.screen');
    for (var i = 0; i < screens.length; i++) screens[i].classList.remove('active');
    var target = $(id);
    if (target) target.classList.add('active');
    // Workspace screens (Met / Crew / Admin) need edge-to-edge layout — the
    // .page wrapper's default padding leaves a white margin around them
    // which reads as a broken empty rectangle. Toggle a body class so CSS
    // can zero the page padding when a workspace is active.
    var isWorkspace = (id === 'screen-met' || id === 'screen-crew-ws' || id === 'screen-admin');
    document.body.classList.toggle('is-workspace-active', isWorkspace);

    // Footer visibility — hide on immersive/transient screens.
    // Show everywhere else so legal links + brand are reachable.
    var hideFooter = (
      id === 'screen-handoff' ||           // transient transition
      id === 'screen-mission-response' ||  // SMS landing for Crew, no chrome
      id === 'screen-nws-page' ||          // SMS landing for Met, no chrome
      id === 'screen-customer-review'      // SMS landing for $19 customer, branded chrome
    );
    document.body.classList.toggle('is-footer-hidden', hideFooter);
    // Phase 9 Push A — initialize the new Leaflet Live Feed map on entry.
    // Idempotent — re-entry just calls invalidateSize() to refresh.
    if (id === 'screen-crew-feed' && window.WV_FeedMapAPI) {
      try { window.WV_FeedMapAPI.init(); } catch (e) { console.warn('feed map init failed', e); }
    }
    // Phase 10 — subscriber portal real data load on entry. Idempotent.
    if (id === 'screen-subscriber-portal' && typeof loadSubscriberPortal === 'function') {
      try { loadSubscriberPortal(); } catch (e) { console.warn('portal load failed', e); }
    }
    // Item #2 — refresh the Met queue every time the Met enters their
    // workspace. Pulls real paid verification_requests from the backend
    // so the queue reflects what customers have purchased since last view.
    if (id === 'screen-met' && typeof loadMetQueueFromBackend === 'function') {
      try { loadMetQueueFromBackend(); } catch (e) { console.warn('met queue load failed', e); }
    }
    // Item #4 — refresh mission deployments so the "Last 5" tile reflects
    // what the Met has fired since last view.
    if (id === 'screen-met' && typeof renderMetDeployments === 'function') {
      try { renderMetDeployments(); } catch (e) { console.warn('met deployments load failed', e); }
    }
    // Item #3 Chunk 3B-2 — load Pro briefs on workspace entry so the
    // tab count badge reflects pending work immediately.
    if (id === 'screen-met' && typeof loadMetProBriefs === 'function') {
      try { loadMetProBriefs(); } catch (e) { console.warn('pro briefs load failed', e); }
    }
    // Pro Threads PT-C — load Met thread list on workspace entry so the
    // Messages tab badge is accurate.
    if (id === 'screen-met' && typeof loadMetThreadsList === 'function') {
      try { loadMetThreadsList(); } catch (e) { console.warn('met threads load failed', e); }
    }
    // Met tips (TIP-B) — load earnings summary on workspace entry
    if (id === 'screen-met' && typeof loadMetTipEarnings === 'function') {
      try { loadMetTipEarnings(); } catch (e) { console.warn('met tips load failed', e); }
    }
    // Met welcome card — first-login orientation
    if (id === 'screen-met' && typeof loadMetWelcomeCard === 'function') {
      try { loadMetWelcomeCard(); } catch (e) { console.warn('met welcome load failed', e); }
    }
    // Phase 11 (May 17) — start the heartbeat so admin coverage
    // dashboard knows this Met is around right now. The interval
    // continues for the page lifetime; we don't need to stop it on
    // screen leave (Mets sometimes go to other screens but still want
    // to be considered "available" if they're going to come back).
    if (id === 'screen-met' && typeof startMetHeartbeat === 'function') {
      try { startMetHeartbeat(); } catch (e) {}
    }
    // Phase 10 scheduling — refresh scheduled-messages list on workspace entry
    if (id === 'screen-met' && typeof loadMetScheduledMessages === 'function') {
      try { loadMetScheduledMessages(); } catch (e) { console.warn('met scheduled load failed', e); }
    }
    // F-1: Load subscriber roster on entry so the tab count badge populates
    if (id === 'screen-met' && typeof loadMySubscribers === 'function') {
      try { loadMySubscribers(); } catch (e) { console.warn('met subs load failed', e); }
    }
    // Item #6 — refresh Crew home base when entering Crew Profile.
    if (id === 'screen-crew-profile' && typeof loadCrewHomeBase === 'function') {
      try { loadCrewHomeBase(); } catch (e) { console.warn('crew homebase load failed', e); }
    }
    window.scrollTo(0, 0);
  }
  // Expose on window so handlers attached from elsewhere can call it
  // without the IIFE-scope gymnastics that other parts of this file do.
  // Cheap one-line fix that unblocks the exit-to-search button + all
  // the workspace nav links that already check for window.showScreen.
  window.showScreen = showScreen;

  // ════════════════════════════════════════════════════════════════════════
  // VALET CREW · DATA & STATE
  // ════════════════════════════════════════════════════════════════════════
  //
  // The Crew layer needs three things to be interactive:
  //
  //   1. A single source of truth for reports — so a pulse on the map and
  //      a story card in the feed can both reference the same record by ID.
  //      Tapping either opens the same Detail screen for that report.
  //
  //   2. A token ledger — balance, earn, spend, canAfford. Needed for
  //      tipping (which subtracts) and for the existing +10/report flow
  //      (which adds). Updates flow through one place so every chip stays
  //      in sync (Profile chip + any future surface that shows balance).
  //
  //   3. Per-user verification state — a Set of report IDs the user has
  //      already verified. Prevents the same user from tapping "I see it
  //      too" twice on the same report. In production this is server-side
  //      with a (user_id, report_id) unique constraint and a 5-mile/30-min
  //      proximity check. For v1 mockup it's just an in-memory Set.

  // ════════════════════════════════════════════════════════════════════════
  // STORAGE LAYER — a thin abstraction over localStorage
  // ════════════════════════════════════════════════════════════════════════
  // Every read/write goes through `storage.get(key)` and `storage.set(key, val)`
  // so when we swap to a real backend, we change one module instead of
  // hunting through the codebase. Values are JSON-encoded automatically.
  // Wrapped in try/catch — if localStorage is unavailable (private browsing,
  // disk full, browser policy), we fall back to an in-memory map. The app
  // doesn't crash, it just loses persistence for the session.
  var storage = (function() {
    var memFallback = {};
    var available = (function() {
      try {
        var k = '__wv_test__';
        window.localStorage.setItem(k, '1');
        window.localStorage.removeItem(k);
        return true;
      } catch (e) { return false; }
    })();

    function get(key) {
      try {
        var raw = available
          ? window.localStorage.getItem(key)
          : (memFallback[key] || null);
        if (raw === null || raw === undefined) return null;
        return JSON.parse(raw);
      } catch (e) { return null; }
    }
    function set(key, value) {
      try {
        var s = JSON.stringify(value);
        if (available) window.localStorage.setItem(key, s);
        else memFallback[key] = s;
        return true;
      } catch (e) { return false; }
    }
    function remove(key) {
      try {
        if (available) window.localStorage.removeItem(key);
        else delete memFallback[key];
      } catch (e) {}
    }
    return { get: get, set: set, remove: remove, available: available };
  })();

  // Storage key prefix — versioned so we can migrate later without colliding
  // with other apps in localStorage or older versions of our own data.
  var WV_STORAGE_PREFIX = 'wv_v1_';
  var K_USERS = WV_STORAGE_PREFIX + 'users';      // accounts list
  var K_SESSION = WV_STORAGE_PREFIX + 'session';  // currently signed-in user id
  var K_REPORTS = WV_STORAGE_PREFIX + 'reports';  // user-submitted reports
  var K_USER_DATA_PREFIX = WV_STORAGE_PREFIX + 'user_'; // per-user state

  // ════════════════════════════════════════════════════════════════════════
  // IDENTITY LAYER — multi-account auth on a single device
  // ════════════════════════════════════════════════════════════════════════
  // Mock auth that's "real enough" for testing: real signup form captures
  // real fields, accounts persist in localStorage, multiple accounts can
  // coexist on the same device, sign-in actually checks the password.
  //
  // Threat model (deliberately limited): this is NOT secure. Passwords are
  // stored as base64-of-utf8-bytes, not hashed. There's no encryption.
  // Anyone with devtools can read everything. That's appropriate for a
  // prototype — we're emulating UX, not security. When the real backend
  // ships, this whole module is replaced with HTTP calls + JWTs and the
  // rest of the app barely changes because everything routes through the
  // same `auth.currentUser()` / `auth.signIn()` / etc. interface.

  var identity = (function() {
    function loadUsers() {
      return storage.get(K_USERS) || [];
    }
    function saveUsers(users) {
      storage.set(K_USERS, users);
    }
    function makeUserId() {
      // Stable random ID. Not cryptographically secure but unique enough
      // for a single-device prototype.
      return 'u-' + Math.random().toString(36).slice(2, 10) + Date.now().toString(36).slice(-4);
    }
    // "Encode" — not encrypt. See threat model note above.
    function encode(s) {
      try { return btoa(unescape(encodeURIComponent(s))); }
      catch (e) { return s; }
    }

    return {
      // Returns array of all user accounts (id, name, email, handle, joined)
      list: function() {
        return loadUsers().map(function(u) {
          return { id: u.id, name: u.name, email: u.email, handle: u.handle, joined: u.joined };
        });
      },
      // Look up a user by email (case-insensitive). Returns null if not found.
      findByEmail: function(email) {
        var key = (email || '').trim().toLowerCase();
        var users = loadUsers();
        for (var i = 0; i < users.length; i++) {
          if (users[i].email.toLowerCase() === key) return users[i];
        }
        return null;
      },
      // Look up by id
      findById: function(id) {
        var users = loadUsers();
        for (var i = 0; i < users.length; i++) {
          if (users[i].id === id) return users[i];
        }
        return null;
      },
      // Create a new account. Returns { ok: true, user } or { ok: false, error }.
      // Validation: name + email + password required, email format basic,
      // password 4+ chars (relaxed for demo), email must not already exist,
      // handle must not already exist (case-insensitive). City is now
      // required — severe alerts depend on knowing where the subscriber is.
      // Optional lat/lng are persisted when the user clicked "Use my
      // location" so the alert geofence can be precise; otherwise the
      // city string alone gets reverse-geocoded server-side later.
      create: function(input) {
        var name = (input.name || '').trim();
        var email = (input.email || '').trim();
        var handle = (input.handle || '').trim().replace(/^@/, '').toLowerCase();
        var password = input.password || '';
        var city = (input.city || '').trim();
        var lat = (typeof input.lat === 'number') ? input.lat : null;
        var lng = (typeof input.lng === 'number') ? input.lng : null;
        var locationSource = input.locationSource || 'typed';

        if (!name) return { ok: false, error: 'Please enter your name.' };
        if (!email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) return { ok: false, error: 'Please enter a valid email.' };
        if (!password || password.length < 4) return { ok: false, error: 'Password should be at least 4 characters.' };
        if (!handle || !/^[a-z0-9_]{2,18}$/.test(handle)) return { ok: false, error: 'Handle should be 2–18 letters, numbers, or underscores.' };
        if (!city) return { ok: false, error: 'Home base is required. Type your city or tap "Use my location".' };

        var users = loadUsers();
        for (var i = 0; i < users.length; i++) {
          if (users[i].email.toLowerCase() === email.toLowerCase()) {
            return { ok: false, error: 'An account with this email already exists. Try signing in.' };
          }
          if (users[i].handle.toLowerCase() === handle.toLowerCase()) {
            return { ok: false, error: 'That handle is taken. Try another.' };
          }
        }

        var newUser = {
          id: makeUserId(),
          name: name,
          email: email,
          handle: handle,
          city: city,
          lat: lat,
          lng: lng,
          locationSource: locationSource,  // 'typed' or 'geolocation'
          password: encode(password),
          joined: new Date().toISOString()
        };
        users.push(newUser);
        saveUsers(users);
        return { ok: true, user: newUser };
      },
      // Verify credentials. Returns user object or null.
      verify: function(email, password) {
        var u = this.findByEmail(email);
        if (!u) return null;
        return u.password === encode(password) ? u : null;
      },
      // Update an existing user's mutable fields (location, name, etc.).
      // Password and immutable identifiers are not editable here.
      // Returns { ok: true, user } or { ok: false, error }.
      update: function(userId, patch) {
        var users = loadUsers();
        for (var i = 0; i < users.length; i++) {
          if (users[i].id === userId) {
            // Whitelist of editable fields
            var editable = ['name', 'city', 'lat', 'lng', 'locationSource'];
            for (var k = 0; k < editable.length; k++) {
              var key = editable[k];
              if (Object.prototype.hasOwnProperty.call(patch, key)) {
                users[i][key] = patch[key];
              }
            }
            saveUsers(users);
            return { ok: true, user: users[i] };
          }
        }
        return { ok: false, error: 'User not found' };
      }
    };
  })();

  // Per-user persistent state. Each user has a record stored at
  // `wv_v1_user_<id>` containing tokens, verifications, tips, badges, etc.
  // Default state is built when a user signs up; loaded into memory on
  // sign-in; saved on every mutation through tokens.earn/.spend/etc.
  function defaultUserState() {
    return {
      tokens: 50,                  // signup bonus — enough to send a Nod or two
      verifiedByMe: [],            // array of report IDs (Set serializes to array)
      tipsByMe: {},                // { reportId: ['nod','coffee'] }
      myReports: [],               // IDs of reports this user submitted
      streak: 0,                   // clarity streak
      protectedHours: 0,           // accumulated protected hours
      reportsCount: 0,             // total reports submitted
      verificationsCount: 0,       // total verifications given
      onboardingShown: false,      // has the user seen the onboarding flow?
      rank: 'Junior Attendant',    // starting rank — promotes with reports
      badges: []                   // array of earned badge IDs
    };
  }
  function loadUserState(userId) {
    var raw = storage.get(K_USER_DATA_PREFIX + userId);
    if (!raw) return defaultUserState();
    // Migration: ensure all default keys exist (forward-compatible)
    var def = defaultUserState();
    for (var k in def) { if (!(k in raw)) raw[k] = def[k]; }
    return raw;
  }
  function saveUserState(userId, state) {
    storage.set(K_USER_DATA_PREFIX + userId, state);
  }

  // CREW_REPORTS is the single source of truth for all Crew reports
  // shown on the Live Feed map and story list. Each report has:
  //   - zoomTier: 'county' | 'region' | 'state' — the WIDEST zoom level
  //     at which this report was hyperlocal enough to surface there.
  //     county-tier reports show at all three zoom levels (most local);
  //     region-tier reports show at region + state zoom;
  //     state-tier reports show only at state zoom.
  //   - mapPos: percentage coordinates within the map viewport. Each zoom
  //     level uses its own viewport mapping, so the same lat/lng renders
  //     at different positions depending on zoom (handled at render time).
  //   - county: name string used for the bottom strip aggregation.
  //   - timeMinutes: how recent (minutes since now). Story list sorts by
  //     this ascending and keeps the 10 most recent.
  var CREW_REPORTS = [
    // ─── LEBANON, IN (city-tier — only visible at city zoom AND wider) ───
    // These are seed reports near the founding territory's main town.
    // At city zoom, only these surface. At county+region+state they
    // continue to appear (city ⊂ county ⊂ region ⊂ state).
    {
      id: 'r-reynolds-wind',
      type: 'wind',
      author: '@mreynolds',
      authorFull: 'Michael Reynolds',
      city: 'Lebanon',
      county: 'Boone County',
      timeAgo: '18 min ago',
      timeMinutes: 18,
      text: 'Steady WSW wind picking up — guess 18-22 mph. Tarps lifting on site. Have the crew tying things down before the front hits.',
      verified: 3,
      tips: { nod: 1, coffee: 0, handshake: 0 },
      streak: 9,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'city',
      mapPosCity: { top: '46%', left: '38%' },
      mapPosCounty: { top: '52%', left: '48%' },
      mapPosRegion: { top: '32%', left: '24%' },
      mapPosState: { top: '38%', left: '34%' }
    },
    {
      id: 'r-anon-rain-1',
      type: 'rain',
      author: '@reilly_b',
      authorFull: 'Reilly Bertram',
      city: 'Lebanon',
      county: 'Boone County',
      timeAgo: '14 min ago',
      timeMinutes: 14,
      text: 'Steady rain just started, looks like it will stick around for a bit. Wind picking up too.',
      verified: 2,
      tips: { nod: 0, coffee: 0, handshake: 0 },
      streak: 2,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'city',
      mapPosCity: { top: '62%', left: '58%' },
      mapPosCounty: { top: '58%', left: '52%' },
      mapPosRegion: { top: '40%', left: '20%' },
      mapPosState: { top: '40%', left: '36%' }
    },
    {
      id: 'r-zionsville-storm',
      type: 'storm',
      author: '@zionsville_kev',
      authorFull: 'Kevin Pruitt',
      city: 'Zionsville',
      county: 'Boone County',
      timeAgo: '6 min ago',
      timeMinutes: 6,
      text: 'Cell building rapidly to the SW. Frequent cloud-to-cloud lightning. Looks like it\u2019ll cross 421 in 15 min.',
      verified: 5,
      tips: { nod: 2, coffee: 1, handshake: 0 },
      streak: 5,
      hasPhoto: true,
      photoStyle: 'storm',
      zoomTier: 'county',
      mapPosCounty: { top: '68%', left: '70%' },
      mapPosRegion: { top: '46%', left: '38%' },
      mapPosState: { top: '42%', left: '40%' }
    },
    {
      id: 'r-thorntown-clear',
      type: 'clear',
      author: '@thorntown_meg',
      authorFull: 'Meg Atwell',
      city: 'Thorntown',
      county: 'Boone County',
      timeAgo: '21 min ago',
      timeMinutes: 21,
      text: 'Sun back out west of town. Clouds breaking up rapidly. Looks like the line passed.',
      verified: 1,
      tips: { nod: 0, coffee: 0, handshake: 0 },
      streak: 3,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'county',
      mapPosCounty: { top: '34%', left: '24%' },
      mapPosRegion: { top: '24%', left: '14%' },
      mapPosState: { top: '36%', left: '32%' }
    },

    // ─── CENTRAL INDIANA REGION (region-tier — region + state zoom) ───
    {
      id: 'r-grant-storm',
      type: 'storm',
      author: '@grant_t',
      authorFull: 'Grant Thompson',
      city: 'Indianapolis',
      county: 'Marion County',
      timeAgo: '3 min ago',
      timeMinutes: 3,
      text: 'Lightning visible from downtown, moving NE. Outdoor crews packing up. Sirens audible — looks like the cell is intensifying as it crosses White River.',
      verified: 4,
      tips: { nod: 2, coffee: 0, handshake: 0 },
      streak: 4,
      hasPhoto: true,
      photoStyle: 'storm',
      zoomTier: 'region',
      mapPosRegion: { top: '70%', left: '52%' },
      mapPosState: { top: '50%', left: '40%' }
    },
    {
      id: 'r-jay-hail',
      type: 'hail',
      author: '@noblesville_jay',
      authorFull: 'Jay Hendricks',
      city: 'Noblesville',
      county: 'Hamilton County',
      timeAgo: '12 min ago',
      timeMinutes: 12,
      text: 'Pea-sized hail on the patio for about 4 minutes. Bouncing off the table. About 0.25 inches by my eye. Stopped now but sky still dark to the southwest.',
      verified: 7,
      tips: { nod: 5, coffee: 2, handshake: 1 },
      streak: 7,
      hasPhoto: true,
      photoStyle: 'hail',
      zoomTier: 'region',
      mapPosRegion: { top: '32%', left: '70%' },
      mapPosState: { top: '44%', left: '44%' }
    },
    {
      id: 'r-gibson-clear',
      type: 'clear',
      author: '@gibson',
      authorFull: 'Sam Gibson',
      city: 'Crawfordsville',
      county: 'Montgomery County',
      timeAgo: '22 min ago',
      timeMinutes: 22,
      text: 'Front passed cleanly — clearing here, sun back out. Safe to resume work outdoors. Light westerly breeze, temperature recovering.',
      verified: 0,
      tips: { nod: 0, coffee: 0, handshake: 0 },
      streak: 6,
      hasPhoto: true,
      photoStyle: 'clear',
      zoomTier: 'region',
      mapPosRegion: { top: '52%', left: '8%' },
      mapPosState: { top: '46%', left: '28%' }
    },
    {
      id: 'r-anon-rain-2',
      type: 'rain',
      author: '@dpaulsen',
      authorFull: 'Diana Paulsen',
      city: 'Greenwood',
      county: 'Johnson County',
      timeAgo: '8 min ago',
      timeMinutes: 8,
      text: 'Heavy rain band moving through. Visibility down to maybe a quarter mile. Glad we wrapped up the pour at noon.',
      verified: 5,
      tips: { nod: 2, coffee: 1, handshake: 0 },
      streak: 4,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'region',
      mapPosRegion: { top: '82%', left: '60%' },
      mapPosState: { top: '54%', left: '42%' }
    },
    {
      id: 'r-fishers-rain',
      type: 'rain',
      author: '@dani_fishers',
      authorFull: 'Dani Stovall',
      city: 'Fishers',
      county: 'Hamilton County',
      timeAgo: '4 min ago',
      timeMinutes: 4,
      text: 'Light rain just started here in Fishers. Wind dying down though.',
      verified: 2,
      tips: { nod: 1, coffee: 0, handshake: 0 },
      streak: 3,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'region',
      mapPosRegion: { top: '46%', left: '76%' },
      mapPosState: { top: '46%', left: '46%' }
    },
    {
      id: 'r-carmel-wind',
      type: 'wind',
      author: '@carmel_dave',
      authorFull: 'Dave Schoening',
      city: 'Carmel',
      county: 'Hamilton County',
      timeAgo: '11 min ago',
      timeMinutes: 11,
      text: 'Strong gusts here, maybe 25 mph at peak. Trash cans down on the sidewalk.',
      verified: 3,
      tips: { nod: 1, coffee: 0, handshake: 0 },
      streak: 4,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'region',
      mapPosRegion: { top: '40%', left: '60%' },
      mapPosState: { top: '47%', left: '44%' }
    },
    {
      id: 'r-plainfield-clear',
      type: 'clear',
      author: '@plainfield_amy',
      authorFull: 'Amy Walters',
      city: 'Plainfield',
      county: 'Hendricks County',
      timeAgo: '17 min ago',
      timeMinutes: 17,
      text: 'Cleared up here. Sun out, slight breeze. Lawn crew back to it.',
      verified: 1,
      tips: { nod: 0, coffee: 0, handshake: 0 },
      streak: 2,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'region',
      mapPosRegion: { top: '74%', left: '32%' },
      mapPosState: { top: '52%', left: '40%' }
    },
    {
      id: 'r-westfield-storm',
      type: 'storm',
      author: '@westfield_pj',
      authorFull: 'PJ Lambert',
      city: 'Westfield',
      county: 'Hamilton County',
      timeAgo: '9 min ago',
      timeMinutes: 9,
      text: 'Frequent lightning to the east, getting closer. Crew called off the job site.',
      verified: 4,
      tips: { nod: 2, coffee: 0, handshake: 0 },
      streak: 5,
      hasPhoto: true,
      photoStyle: 'storm',
      zoomTier: 'region',
      mapPosRegion: { top: '24%', left: '54%' },
      mapPosState: { top: '42%', left: '44%' }
    },

    // ─── ELSEWHERE IN INDIANA (state-tier — only at state zoom) ───
    {
      id: 'r-fortwayne-storm',
      type: 'storm',
      author: '@fortwayne_eli',
      authorFull: 'Eli Reichert',
      city: 'Fort Wayne',
      county: 'Allen County',
      timeAgo: '24 min ago',
      timeMinutes: 24,
      text: 'Heavy storms moving through downtown. Lots of cloud-to-ground.',
      verified: 6,
      tips: { nod: 3, coffee: 1, handshake: 0 },
      streak: 5,
      hasPhoto: true,
      photoStyle: 'storm',
      zoomTier: 'state',
      mapPosState: { top: '20%', left: '70%' }
    },
    {
      id: 'r-southbend-rain',
      type: 'rain',
      author: '@southbend_kira',
      authorFull: 'Kira Plaszczyk',
      city: 'South Bend',
      county: 'St. Joseph County',
      timeAgo: '15 min ago',
      timeMinutes: 15,
      text: 'Steady rain all afternoon. Field is saturated, won\u2019t be working tomorrow.',
      verified: 4,
      tips: { nod: 2, coffee: 0, handshake: 0 },
      streak: 3,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'state',
      mapPosState: { top: '12%', left: '50%' }
    },
    {
      id: 'r-evansville-clear',
      type: 'clear',
      author: '@evansville_ros',
      authorFull: 'Roslyn Hartke',
      city: 'Evansville',
      county: 'Vanderburgh County',
      timeAgo: '29 min ago',
      timeMinutes: 29,
      text: 'Clear skies, calm winds. Beautiful afternoon down here.',
      verified: 2,
      tips: { nod: 1, coffee: 0, handshake: 0 },
      streak: 4,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'state',
      mapPosState: { top: '88%', left: '22%' }
    },
    {
      id: 'r-bloomington-wind',
      type: 'wind',
      author: '@bloomington_t',
      authorFull: 'Tao Chen',
      city: 'Bloomington',
      county: 'Monroe County',
      timeAgo: '20 min ago',
      timeMinutes: 20,
      text: 'Gusty here, maybe 20-25. Front coming through.',
      verified: 3,
      tips: { nod: 0, coffee: 0, handshake: 0 },
      streak: 2,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'state',
      mapPosState: { top: '64%', left: '40%' }
    },
    {
      id: 'r-terrehaute-rain',
      type: 'rain',
      author: '@th_construction',
      authorFull: 'Brian Vance',
      city: 'Terre Haute',
      county: 'Vigo County',
      timeAgo: '26 min ago',
      timeMinutes: 26,
      text: 'Light rain on and off all afternoon. Concrete pour delayed til Thursday.',
      verified: 2,
      tips: { nod: 1, coffee: 0, handshake: 0 },
      streak: 3,
      hasPhoto: false,
      photoStyle: null,
      zoomTier: 'state',
      mapPosState: { top: '56%', left: '20%' }
    },
    {
      id: 'r-muncie-hail',
      type: 'hail',
      author: '@muncie_carrie',
      authorFull: 'Carrie Boyd',
      city: 'Muncie',
      county: 'Delaware County',
      timeAgo: '7 min ago',
      timeMinutes: 7,
      text: 'Small hail just blew through. Maybe 30 seconds, dime-sized at most.',
      verified: 5,
      tips: { nod: 2, coffee: 1, handshake: 0 },
      streak: 4,
      hasPhoto: true,
      photoStyle: 'hail',
      zoomTier: 'state',
      mapPosState: { top: '38%', left: '58%' }
    }
  ];

  // Backfill mapPos (defaults to mapPosRegion) so old code that reads
  // report.mapPos still works during transition. The new zoom-aware code
  // uses mapPosCounty/Region/State directly via getReportPos().
  // Also backfill state — all seed reports are Indiana for the founding
  // territory; user-submitted reports inherit state from the user's
  // signup city. The Crew-evidence section on the ticket uses this to
  // match reports to a forecast's location.
  for (var i = 0; i < CREW_REPORTS.length; i++) {
    var r = CREW_REPORTS[i];
    r.mapPos = r.mapPosRegion || r.mapPosState || r.mapPosCounty || { top: '50%', left: '50%' };
    if (!r.state) r.state = 'IN';
  }

  // ── CREW_REPORTS persistence ─────────────────────────────────────────────
  // Reports themselves persist across refreshes. We store ONLY the dynamic
  // fields (verified count, tips object) for each seed report — the static
  // metadata (text, author, position) stays in the in-memory array. User-
  // submitted reports are stored as full records in a separate array,
  // appended to CREW_REPORTS at boot.
  //
  // Schema of stored value:
  //   { mutations: { reportId: { verified: N, tips: {nod, coffee, handshake} } },
  //     userReports: [ {full report record}, ... ] }
  function loadReportsState() {
    // Backfill submittedAt timestamps on seeded reports. The prototype
    // reports were authored with a static `timeMinutes` field (e.g.
    // "18 min ago"). For aging to work, every report needs a real
    // timestamp. We compute one at boot: submittedAt = now - timeMinutes.
    // From here on, the aging system treats all reports uniformly.
    var nowMs = Date.now();
    for (var ri = 0; ri < CREW_REPORTS.length; ri++) {
      var rep = CREW_REPORTS[ri];
      if (!rep.submittedAt && typeof rep.timeMinutes === 'number') {
        rep.submittedAt = new Date(nowMs - rep.timeMinutes * 60000).toISOString();
      }
    }

    var stored = storage.get(K_REPORTS);
    if (!stored) return;
    if (stored.mutations) {
      for (var rid in stored.mutations) {
        var existing = getReportById(rid);
        if (existing) {
          if (typeof stored.mutations[rid].verified === 'number') {
            existing.verified = stored.mutations[rid].verified;
          }
          if (stored.mutations[rid].tips) {
            existing.tips = stored.mutations[rid].tips;
          }
        }
      }
    }
    if (stored.userReports && stored.userReports.length) {
      // Push each user-submitted report into CREW_REPORTS if not already present
      for (var u = 0; u < stored.userReports.length; u++) {
        var ur = stored.userReports[u];
        if (!getReportById(ur.id)) {
          ur.mapPos = ur.mapPosRegion || ur.mapPosState || ur.mapPosCounty || { top: '50%', left: '50%' };
          CREW_REPORTS.push(ur);
        }
      }
    }
  }

  // ── Aging tick ─────────────────────────────────────────────────────
  // Every minute, re-render the feed so reports visibly age over time
  // without requiring a page reload. Only fires when the feed screen
  // is visible — saves work when nobody's looking. Skips re-render
  // when the user is mid-interaction (e.g. detail screen open).
  var agingTickHandle = null;
  function setupAgingTick() {
    if (agingTickHandle) return;
    agingTickHandle = setInterval(function() {
      var feedScreen = $('screen-crew-feed');
      if (!feedScreen) return;
      // Only re-render when the feed screen is the visible one.
      if (!feedScreen.classList.contains('active')) return;
      try { renderFeed(); } catch (e) {}
    }, 60000);
  }

  // ── Broadcast ticker (Chunk 4a) ────────────────────────────────────
  // A "slot machine" of live events at the top of the Live Feed.
  // Cycles every ~3.5 seconds. Pulls real events from CREW_REPORTS,
  // CREW_RECEIPTS, and known verification counts. Only ticks while
  // the Live Feed is visible — saves work elsewhere.
  //
  // Event types it mixes (with rough frequency weight):
  //   - report       (a Crew member reported something)
  //   - verify       (a report just hit N verifications)
  //   - cited        (a Met cited a Crew member's report)
  //   - milestone    (a Crew member just hit a streak or rank)
  //
  // Events are NOT shuffled — they're served in age order (newest first),
  // wrapping when we reach the end. This creates a sense of "this is
  // what's happening right now" rather than "here are random facts."
  var tickerTickHandle = null;
  var tickerQueue = [];
  var tickerIndex = 0;
  var TICKER_INTERVAL_MS = 3500;
  var TICKER_FADE_MS = 280;

  function buildTickerQueue() {
    var events = [];

    // 1. Recent reports — last 30 minutes, newest first
    if (typeof CREW_REPORTS !== 'undefined' && CREW_REPORTS.length) {
      var sortedReports = CREW_REPORTS.slice().sort(function(a, b) {
        var aT = a.submittedAt ? new Date(a.submittedAt).getTime() : (Date.now() - (a.timeMinutes || 999) * 60000);
        var bT = b.submittedAt ? new Date(b.submittedAt).getTime() : (Date.now() - (b.timeMinutes || 999) * 60000);
        return bT - aT;
      });
      for (var i = 0; i < Math.min(8, sortedReports.length); i++) {
        var r = sortedReports[i];
        // Skip stale reports — they're history, not news
        var age = (typeof getReportAge === 'function') ? getReportAge(r) : { stage: 'fresh' };
        if (age.stage === 'stale') continue;
        events.push({
          kind: 'report',
          tag: 'REPORT',
          tagClass: 'ticker-tag-report',
          text: formatTickerReport(r)
        });
      }

      // 2. High-verified reports as "verify" events
      var verifiedReports = sortedReports.filter(function(r) {
        return (r.verified || 0) >= 2;
      }).slice(0, 4);
      for (var v = 0; v < verifiedReports.length; v++) {
        var vr = verifiedReports[v];
        events.push({
          kind: 'verify',
          tag: 'VERIFIED',
          tagClass: 'ticker-tag-verify',
          text: 'Report from <strong>' + escapeHtml(vr.author) + '</strong> '
              + 'now confirmed by <strong>' + vr.verified + ' Crew</strong> '
              + 'in ' + escapeHtml(vr.city) + '.'
        });
      }
    }

    // 3. Citations from CREW_RECEIPTS (where a Met used a report)
    if (typeof CREW_RECEIPTS !== 'undefined' && CREW_RECEIPTS.length) {
      for (var c = 0; c < CREW_RECEIPTS.length; c++) {
        var rec = CREW_RECEIPTS[c];
        if (rec.citation && rec.citation.metName) {
          events.push({
            kind: 'cited',
            tag: 'CITED',
            tagClass: 'ticker-tag-cited',
            text: '<strong>' + escapeHtml(rec.citation.metName) + '</strong> '
                + 'cited a Crew report in today\u2019s brief.'
          });
        }
      }
    }

    // 3b. Met posts (Chunk 6c-1) — fresh meteorologist commentary in the
    // ticker. Recent posts only (last 60 min) so old posts don't keep
    // looping after they've been seen. Chunk 6c-2: only 'live' posts
    // (scheduled/cancelled posts are filtered out).
    if (typeof MET_POSTS !== 'undefined' && MET_POSTS.length) {
      var freshCutoffMs = Date.now() - 60 * 60 * 1000;
      for (var mp = 0; mp < MET_POSTS.length; mp++) {
        var mPost = MET_POSTS[mp];
        if (mPost.status !== 'live') continue;
        var mTime = mPost.submittedAt ? new Date(mPost.submittedAt).getTime() : 0;
        if (mTime < freshCutoffMs) continue;
        events.push({
          kind: 'met-post',
          tag: 'MET',
          tagClass: 'ticker-tag-cited',
          text: '<strong>' + escapeHtml(mPost.author) + '</strong>: '
              + escapeHtml((mPost.text || '').slice(0, 160))
        });
      }
    }

    // 4. Quiet-day fallback so the ticker always has something to say.
    if (events.length === 0) {
      events = [
        { kind: 'idle', tag: 'WATCHING', tagClass: 'ticker-tag-verify',
          text: 'Quiet across the territory. The Crew is on station.' },
        { kind: 'idle', tag: 'WATCHING', tagClass: 'ticker-tag-verify',
          text: 'No active weather. Meteorologists watching the models for what\u2019s next.' }
      ];
    }

    // De-duplicate by text (same headline appearing twice in a row reads
    // like a glitch). Shuffle slightly so the ticker doesn't read in the
    // exact same order on every page load.
    var seen = {};
    var deduped = [];
    for (var e = 0; e < events.length; e++) {
      if (seen[events[e].text]) continue;
      seen[events[e].text] = true;
      deduped.push(events[e]);
    }

    return deduped;
  }

  // Format a single report as a ticker line. Different report types get
  // slightly different framing — "spotted hail" reads better than
  // "reported hail." Concise, sentence-cased, no double punctuation.
  function formatTickerReport(r) {
    var author = '<strong>' + escapeHtml(r.author) + '</strong>';
    var city = escapeHtml(r.city || 'the field');
    var type = r.type;
    var verb;
    switch (type) {
      case 'storm':    verb = 'reporting a storm cell over'; break;
      case 'hail':     verb = 'spotted hail in'; break;
      case 'wind':     verb = 'tracking strong wind in'; break;
      case 'rain':     verb = 'reporting rain in'; break;
      case 'snow':     verb = 'reporting snow in'; break;
      case 'sleet':    verb = 'reporting sleet in'; break;
      case 'freezing': verb = 'reporting freezing rain in'; break;
      case 'fog':      verb = 'reporting fog in'; break;
      case 'sunny':    verb = 'reporting clear skies in'; break;
      case 'cloudy':   verb = 'reporting overcast in'; break;
      case 'humid':    verb = 'reporting humid conditions in'; break;
      case 'clearing': verb = 'reporting clearing conditions in'; break;
      case 'clear':    verb = 'reporting clear conditions in'; break;
      default:         verb = 'reporting from'; break;
    }
    return author + ' ' + verb + ' <strong>' + city + '</strong>.';
  }

  function renderTickerItem(item) {
    var textEl = $('feed-ticker-text');
    if (!textEl || !item) return;
    // Build the HTML — a tag pill + the event text. Keep it small enough
    // to fit on one line; the CSS clamps with text-overflow: ellipsis.
    var html = '<span class="ticker-tag-tag ' + item.tagClass + '">' + item.tag + '</span>'
      + item.text;
    textEl.innerHTML = html;
  }

  function tickerStep() {
    if (!tickerQueue.length) return;
    var textEl = $('feed-ticker-text');
    if (!textEl) return;
    // Fade out current; after fade, swap content; fade in.
    textEl.classList.add('is-leaving');
    setTimeout(function() {
      tickerIndex = (tickerIndex + 1) % tickerQueue.length;
      renderTickerItem(tickerQueue[tickerIndex]);
      textEl.classList.remove('is-leaving');
      textEl.classList.add('is-entering');
      // Force reflow so the next class change re-triggers the transition
      // eslint-disable-next-line no-unused-expressions
      void textEl.offsetWidth;
      textEl.classList.remove('is-entering');
    }, TICKER_FADE_MS);
  }

  function setupBroadcastTicker() {
    if (tickerTickHandle) return;
    var textEl = $('feed-ticker-text');
    if (!textEl) return;

    // Build the queue and seed with the first event so the page never
    // shows "Loading the feed…" past the first paint.
    tickerQueue = buildTickerQueue();
    if (tickerQueue.length) {
      tickerIndex = 0;
      renderTickerItem(tickerQueue[0]);
    }

    // Rebuild the queue every 2 minutes so newly-submitted reports
    // flow into the ticker without a page reload.
    setInterval(function() {
      tickerQueue = buildTickerQueue();
    }, 120000);

    // Cycle the visible item every TICKER_INTERVAL_MS. Only step
    // when the Live Feed screen is visible (saves CPU; also avoids
    // the jarring "where did the time go" feeling when the user
    // comes back to a paused tab and sees a different event).
    tickerTickHandle = setInterval(function() {
      var feedScreen = $('screen-crew-feed');
      if (!feedScreen || !feedScreen.classList.contains('active')) return;
      if (document.hidden) return;
      tickerStep();
    }, TICKER_INTERVAL_MS);
  }

  // ── "Now on air" rail (Chunk 4b) ───────────────────────────────────
  // Build a cast list: Crew members who've reported in the recency
  // window, grouped by author. Each author appears once with their
  // most recent report. Sorted by most-recent-activity first.
  //
  // Active dot: shown when their most recent report is within
  // ONAIR_ACTIVE_WINDOW_MIN minutes. Past that, they're still on
  // the rail (recent enough to be "on air for this episode") but
  // without the active dot.
  var ONAIR_RECENCY_WINDOW_MIN = 30;   // who appears on the rail at all
  var ONAIR_ACTIVE_WINDOW_MIN  = 8;    // who gets the live "active" dot
  var ONAIR_MAX_CARDS          = 6;    // visual cap; rail scrolls if more

  // Pull each Crew member's verification COUNT — the total number of
  // their reports that have been verified. For seeded reports we use
  // the `verified` field on each report (sum across their reports).
  function crewMemberVerifyCount(authorHandle) {
    if (typeof CREW_REPORTS === 'undefined') return 0;
    var total = 0;
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      if (CREW_REPORTS[i].author === authorHandle) {
        total += (CREW_REPORTS[i].verified || 0);
      }
    }
    return total;
  }

  function buildOnAirCast() {
    if (typeof CREW_REPORTS === 'undefined' || !CREW_REPORTS.length) {
      // Even if there are no Crew reports, Met posts may exist — fall
      // through to the loops below rather than returning empty.
    }

    // Map of authorHandle -> most-recent-report
    var byAuthor = {};
    for (var i = 0; i < (CREW_REPORTS || []).length; i++) {
      var r = CREW_REPORTS[i];
      if (!r.author) continue;

      // Age — only include if within the recency window
      var ageMin;
      if (r.submittedAt) {
        ageMin = (Date.now() - new Date(r.submittedAt).getTime()) / 60000;
      } else if (typeof r.timeMinutes === 'number') {
        ageMin = r.timeMinutes;
      } else {
        ageMin = 999;
      }
      if (ageMin > ONAIR_RECENCY_WINDOW_MIN) continue;

      // Keep the newest report for this author
      var existing = byAuthor[r.author];
      if (!existing || ageMin < existing._ageMin) {
        byAuthor[r.author] = Object.assign({}, r, { _ageMin: ageMin });
      }
    }

    // Met posts (Chunk 6c-1) — also appear on the on-air rail as their
    // own cast cards, marked is-met-post. One card per Met author with
    // their most recent post. Chunk 6c-2: scheduled/cancelled excluded.
    if (typeof MET_POSTS !== 'undefined' && MET_POSTS.length) {
      var byMet = {};
      for (var mp = 0; mp < MET_POSTS.length; mp++) {
        var mPost = MET_POSTS[mp];
        if (mPost.status !== 'live') continue;
        var mAge;
        if (mPost.submittedAt) {
          mAge = (Date.now() - new Date(mPost.submittedAt).getTime()) / 60000;
        } else { mAge = 999; }
        if (mAge > ONAIR_RECENCY_WINDOW_MIN) continue;
        var key = '_met_' + mPost.author;
        if (!byMet[key] || mAge < byMet[key]._ageMin) {
          byMet[key] = Object.assign({}, mPost, {
            _ageMin: mAge,
            type: 'met-post',
            city: 'Coverage'
          });
        }
      }
      Object.keys(byMet).forEach(function(k) {
        byAuthor[k] = byMet[k];
      });
    }

    // Flatten + sort newest-first; cap at ONAIR_MAX_CARDS
    var cast = Object.keys(byAuthor).map(function(k) { return byAuthor[k]; });
    cast.sort(function(a, b) { return a._ageMin - b._ageMin; });
    return cast.slice(0, ONAIR_MAX_CARDS);
  }

  // Short, sentence-cased summary of a single report for an on-air card.
  // Different from the ticker formatter — the card has 2 lines of space
  // for a fuller "what they're seeing" line, not just a who-where-what
  // headline.
  function formatOnAirSummary(report) {
    if (!report || !report.text) return '\u2014';
    // Use the report's own text, trimmed to fit the 2-line clamp.
    var t = report.text.trim();
    if (t.length > 110) t = t.slice(0, 108) + '\u2026';
    return t;
  }

  function authorInitial(handleOrName) {
    if (!handleOrName) return '?';
    // @handle → H; "Reilly B." → R; etc.
    var clean = handleOrName.replace(/^@/, '').replace(/[^a-zA-Z]/g, '');
    return clean ? clean.charAt(0).toUpperCase() : '?';
  }

  function renderOnAirRail() {
    var rail = $('onair-rail');
    var scroll = $('onair-rail-scroll');
    var countEl = $('onair-rail-count');
    if (!rail || !scroll) return;

    var cast = buildOnAirCast();
    if (!cast.length) {
      rail.hidden = true;
      return;
    }
    rail.hidden = false;

    if (countEl) {
      countEl.textContent = cast.length + ' Crew \u00b7 last ' + ONAIR_RECENCY_WINDOW_MIN + ' min';
    }

    var html = '';
    for (var i = 0; i < cast.length; i++) {
      var r = cast[i];
      var isActive = r._ageMin <= ONAIR_ACTIVE_WINDOW_MIN;
      var verifies = r.isMetPost ? (r.verified || 0) : crewMemberVerifyCount(r.author);
      var timeLabel = (typeof formatAgeLabel === 'function')
        ? formatAgeLabel(r._ageMin)
        : (Math.floor(r._ageMin) + ' min ago');
      var initial = r.isMetPost
        ? (r.authorInitials || authorInitial(r.author))
        : authorInitial(r.authorFull || r.author);
      var metClass = r.isMetPost ? ' is-met-post' : '';
      var displayName = r.isMetPost
        ? r.author
        : r.author;
      var citySubtitle = r.isMetPost ? 'Met \u00b7 Coverage' : (r.city || '\u2014');

      html += '<button type="button" '
        +   'class="onair-card' + (isActive ? ' is-active' : '') + metClass + '" '
        +   'data-type="' + escapeHtml(r.type || '') + '" '
        +   'data-onair-report="' + escapeHtml(r.id) + '" '
        +   'aria-label="' + escapeHtml((r.authorFull || r.author) + (r.isMetPost ? ' (Meteorologist)' : ' in ' + (r.city || ''))) + '">'
        +   '<span class="onair-card-type-stripe"' + (r.isMetPost ? ' style="background:#4169E1"' : '') + '></span>'
        +   '<div class="onair-card-head">'
        +     '<span class="onair-card-avatar"' + (r.isMetPost ? ' style="background:linear-gradient(135deg,#4169E1,#2E4FB8)"' : '') + '>' + escapeHtml(initial) + '</span>'
        +     '<div class="onair-card-id">'
        +       '<div class="onair-card-handle">' + escapeHtml(displayName) + '</div>'
        +       '<div class="onair-card-city">' + escapeHtml(citySubtitle) + '</div>'
        +     '</div>'
        +   '</div>'
        +   '<p class="onair-card-summary">' + escapeHtml(formatOnAirSummary(r)) + '</p>'
        +   '<div class="onair-card-meta">'
        +     '<span class="onair-card-time">' + escapeHtml(timeLabel) + '</span>'
        +     (verifies > 0
              ? '<span class="onair-card-verifies">'
                + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
                + '<polyline points="20 6 9 17 4 12"/></svg>'
                + verifies + ' verified'
                + '</span>'
              : '')
        +   '</div>'
        + '</button>';
    }
    scroll.innerHTML = html;
  }

  // Wire tap → open the most recent report for that Crew member.
  // Done via a single delegated listener on the document so this works
  // even if renderOnAirRail() re-renders the cards out from under us.
  function setupOnAirRail() {
    if (window._onairRailWired) return;
    window._onairRailWired = true;
    document.addEventListener('click', function(ev) {
      var card = ev.target.closest('[data-onair-report]');
      if (!card) return;
      ev.preventDefault();
      ev.stopPropagation();
      var reportId = card.getAttribute('data-onair-report');
      // openReportDetail is the existing handler used by map pulses and
      // side-list cards — reuse it for consistency.
      if (typeof openReportDetail === 'function') {
        openReportDetail(reportId);
      }
    });
    // Initial render
    renderOnAirRail();
    // Re-render on aging tick (every minute) so the rail stays current.
    // We piggyback on the existing aging tick rather than adding our own.
    setInterval(function() {
      var feedScreen = $('screen-crew-feed');
      if (!feedScreen || !feedScreen.classList.contains('active')) return;
      renderOnAirRail();
    }, 60000);
  }

  // Wire tap-to-open on the Profile "Your recent reports" list (Chunk 5d).
  // Mirrors the on-air rail pattern: one delegated listener at the document
  // level so the handler survives any number of re-renders.
  function setupProfileRecent() {
    if (window._profileRecentWired) return;
    window._profileRecentWired = true;
    document.addEventListener('click', function(ev) {
      var item = ev.target.closest('[data-profile-report]');
      if (!item) return;
      ev.preventDefault();
      ev.stopPropagation();
      var reportId = item.getAttribute('data-profile-report');
      if (typeof openReportDetail === 'function') {
        openReportDetail(reportId);
      }
    });
  }

  // ── Tonight's story tagline (Chunk 4c) ─────────────────────────────
  // Synthesizes a single line stating what's happening across the
  // territory right now. Pulls from CREW_REPORTS in a 30-min window,
  // categorizes by severity (severe / active / current), then picks
  // a phrasing template that fits the dominant signal.
  //
  // Severity heuristics:
  //   3+ severe reports        → "Severe weather tracking through X"
  //   3+ active reports        → "Active weather across X"
  //   2+ current cool/calm     → "Calm conditions across X"
  //   anything else            → "Watching X" (the default attentive state)
  //
  // The whole strip also gets a data-severity attribute so the CSS can
  // tint the leading bullet (red pulse on severe, amber on active).
  var TONIGHT_STORY_WINDOW_MIN = 30;

  function getZoomAreaName() {
    // Maps the current zoom level to a human-readable area name. Falls
    // back to "this area" so the tagline never reads broken.
    if (typeof currentZoom === 'undefined') return 'this area';
    if (currentZoom === 'city')   return 'Lebanon, IN';
    if (currentZoom === 'county') return 'Boone County';
    if (currentZoom === 'state')  return 'Indiana';
    return 'Central Indiana';  // region default
  }

  function buildTonightStory() {
    // Bucket recent reports by severity. Skip stale and out-of-window.
    var counts = { severe: 0, active: 0, current: 0 };
    if (typeof CREW_REPORTS !== 'undefined') {
      for (var i = 0; i < CREW_REPORTS.length; i++) {
        var r = CREW_REPORTS[i];
        // Age check
        var ageMin;
        if (r.submittedAt) {
          ageMin = (Date.now() - new Date(r.submittedAt).getTime()) / 60000;
        } else if (typeof r.timeMinutes === 'number') {
          ageMin = r.timeMinutes;
        } else {
          ageMin = 999;
        }
        if (ageMin > TONIGHT_STORY_WINDOW_MIN) continue;
        // Reuse the existing severity bucketing logic from aging.
        var severity = (typeof getReportSeverity === 'function')
          ? getReportSeverity(r.type)
          : 'active';
        counts[severity] = (counts[severity] || 0) + 1;
      }
    }

    var area = getZoomAreaName();
    var line, severity;

    if (counts.severe >= 3) {
      severity = 'severe';
      line = 'Severe weather tracking through ' + area;
    } else if (counts.severe >= 1) {
      // Even one severe report is worth flagging, but with softer phrasing.
      severity = 'severe';
      line = 'Severe weather reports coming in from ' + area;
    } else if (counts.active >= 3) {
      severity = 'active';
      line = 'Active weather across ' + area;
    } else if (counts.active >= 1) {
      severity = 'active';
      line = 'Conditions shifting across ' + area;
    } else if (counts.current >= 2) {
      severity = 'calm';
      line = 'Calm conditions across ' + area;
    } else {
      severity = 'calm';
      line = 'Watching ' + area;
    }

    return { line: line, severity: severity };
  }

  function renderTonightStory() {
    var strip = $('tonight-story');
    var lineEl = $('tonight-story-line');
    if (!strip || !lineEl) return;
    var story = buildTonightStory();
    lineEl.textContent = story.line;
    strip.setAttribute('data-severity', story.severity);
  }

  // ── Up Next footer (Chunk 4d) ──────────────────────────────────────
  // Fetches NWS active alerts, filters to the current map zoom, renders
  // a "looking ahead" footer below the side list. Auto-refreshes every
  // 5 minutes. Falls back to a "next forecast update" line on quiet days.
  //
  // We fetch independently of the Met map (which has its own cache via
  // window._metAlerts). Crew users may never visit the Met portal, so
  // the Live Feed shouldn't depend on that surface having been loaded.
  // Fetch is small (~50 KB), 5-min refresh, separate concerns.
  var UPNEXT_NWS_URL = 'https://api.weather.gov/alerts/active?status=actual';
  var UPNEXT_REFRESH_MS = 5 * 60 * 1000;
  var upnextState = {
    alerts: null,    // last fetched array of features (or null on error)
    fetchedAt: null, // Date of last successful fetch
    error: null,     // last error message
    handle: null     // interval handle
  };

  // Map of current zoom level → list of county names whose alerts
  // should appear. Used as substring matches against alert.properties.areaDesc
  // (NWS returns area as "Boone, IN; Hamilton, IN"). Region defaults to
  // a representative set of Central Indiana counties.
  function getZoomAreaFilter() {
    var zoom = (typeof currentZoom !== 'undefined') ? currentZoom : 'region';
    if (zoom === 'city' || zoom === 'county') {
      return { state: 'IN', counties: ['Boone'] };
    }
    if (zoom === 'region') {
      return {
        state: 'IN',
        counties: ['Boone', 'Hamilton', 'Hancock', 'Hendricks', 'Johnson',
                   'Madison', 'Marion', 'Morgan', 'Shelby', 'Tipton']
      };
    }
    // state zoom or anything else
    return { state: 'IN', counties: null };  // null = match any county in the state
  }

  // Check whether a single NWS feature touches the current zoom area.
  function alertMatchesZoom(feature, areaFilter) {
    var props = feature && feature.properties;
    if (!props) return false;
    var areaDesc = (props.areaDesc || '').toString();
    // Quick state check first — alerts include state in areaDesc like
    // "Boone, IN; Hamilton, IN". If we don't see ", IN" anywhere, it's
    // not an Indiana alert.
    if (areaFilter.state) {
      var statePat = new RegExp(',\\s*' + areaFilter.state + '\\b', 'i');
      if (!statePat.test(areaDesc)) return false;
    }
    // If no county list, accept any in-state alert.
    if (!areaFilter.counties) return true;
    // Otherwise match any county name. Case-insensitive substring; rough
    // but reliable for the freeform areaDesc format.
    var lower = areaDesc.toLowerCase();
    for (var i = 0; i < areaFilter.counties.length; i++) {
      if (lower.indexOf(areaFilter.counties[i].toLowerCase()) !== -1) {
        return true;
      }
    }
    return false;
  }

  // Bin an NWS event name into our three buckets — same scheme as the
  // Met map uses, for visual consistency between the two surfaces.
  function upnextCategorizeAlert(props) {
    var event = (props.event || '').toLowerCase();
    if (event.indexOf('warning') !== -1) return 'warning';
    if (event.indexOf('watch') !== -1)   return 'watch';
    return 'advisory';
  }

  // Compute the next routine NWS forecast issuance time in the user's
  // local timezone. NWS issues forecasts roughly every 6 hours at
  // 3 AM, 9 AM, 3 PM, 9 PM local. We return the next one.
  function nextForecastIssuanceLabel() {
    var now = new Date();
    var hours = [3, 9, 15, 21];
    for (var i = 0; i < hours.length; i++) {
      if (now.getHours() < hours[i]) {
        var next = new Date(now);
        next.setHours(hours[i], 0, 0, 0);
        return formatTimeLabel(next);
      }
    }
    // Past 9 PM — next issuance is tomorrow at 3 AM
    var tomorrow = new Date(now);
    tomorrow.setDate(tomorrow.getDate() + 1);
    tomorrow.setHours(3, 0, 0, 0);
    return formatTimeLabel(tomorrow) + ' tomorrow';
  }

  // Format a Date object as a readable "3:00 PM" string in 12-hour
  // local time. Avoids the localization quirks of toLocaleTimeString.
  function formatTimeLabel(d) {
    var h = d.getHours();
    var m = d.getMinutes();
    var meridiem = h >= 12 ? 'PM' : 'AM';
    var hh = h % 12;
    if (hh === 0) hh = 12;
    var mm = m < 10 ? '0' + m : m;
    return hh + ':' + mm + ' ' + meridiem;
  }

  // Format an ISO timestamp as "until 8:45 PM" for an upcoming expiry.
  // Returns empty string if the date can't be parsed.
  function formatUpnextExpiry(isoStr) {
    if (!isoStr) return '';
    var d = new Date(isoStr);
    if (isNaN(d.getTime())) return '';
    return 'until ' + formatTimeLabel(d);
  }

  // Pull the most useful "headline" from an NWS alert. NWS gives us a
  // `headline` field (e.g. "Severe Thunderstorm Warning issued May 13
  // at 3:42PM EDT until May 13 at 4:15PM EDT by NWS Indianapolis IN")
  // which is too verbose for our footer. We prefer the cleaner `event`
  // field as the title and skip the issued-at boilerplate.
  function upnextHeadline(props) {
    return props.event || props.headline || 'Active alert';
  }

  // Strip the area description down to just the county names, comma-
  // separated. "Boone, IN; Hamilton, IN" → "Boone, Hamilton".
  function upnextArea(props) {
    var raw = (props.areaDesc || '').toString();
    if (!raw) return '';
    var parts = raw.split(';').map(function(p) {
      return p.replace(/,\s*[A-Z]{2}\s*$/, '').trim();
    }).filter(Boolean);
    // Limit to first 3 counties + "and N more" if longer; keeps the row scannable.
    if (parts.length <= 3) return parts.join(', ');
    return parts.slice(0, 3).join(', ') + ' + ' + (parts.length - 3) + ' more';
  }

  // Render the footer body based on current upnextState. Called after
  // each fetch and on zoom changes.
  function renderUpNextFooter() {
    var body = $('upnext-footer-body');
    var updatedEl = $('upnext-footer-updated');
    if (!body) return;

    if (upnextState.error && !upnextState.alerts) {
      body.innerHTML = '<div class="upnext-error">'
        + 'Couldn\u2019t reach the National Weather Service right now. '
        + 'Retrying in a few minutes.'
        + '</div>';
      return;
    }

    // Update the "X ago" label in the header
    if (updatedEl && upnextState.fetchedAt) {
      var delta = Math.floor((Date.now() - upnextState.fetchedAt.getTime()) / 60000);
      if (delta < 1)        updatedEl.textContent = 'just now';
      else if (delta === 1) updatedEl.textContent = '1 min ago';
      else                  updatedEl.textContent = delta + ' min ago';
    }

    if (!upnextState.alerts) {
      // Still loading on first paint
      body.innerHTML = '<div class="upnext-loading">Checking the horizon\u2026</div>';
      return;
    }

    // Filter to the current zoom
    var filter = getZoomAreaFilter();
    var inScope = upnextState.alerts.filter(function(f) {
      return alertMatchesZoom(f, filter);
    });

    if (inScope.length === 0) {
      // Quiet-day fallback: next forecast issuance time.
      var nextTime = nextForecastIssuanceLabel();
      body.innerHTML = '<div class="upnext-quiet">'
        +   '<div class="upnext-quiet-icon">'
        +     '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">'
        +       '<circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>'
        +     '</svg>'
        +   '</div>'
        +   '<div class="upnext-quiet-text">'
        +     '<p class="upnext-quiet-headline">No active watches or warnings in scope.</p>'
        +     '<p class="upnext-quiet-sub">Next NWS forecast update: ' + escapeHtml(nextTime) + '</p>'
        +   '</div>'
        + '</div>';
      // Still re-render the overlay — alerts may have come and gone since
      // the last render, so old zones need to clear.
      try {
        if (typeof renderNwsOverlay === 'function') renderNwsOverlay();
        if (typeof window._crewMapRefreshOverlay === 'function') window._crewMapRefreshOverlay();
        if (typeof window._syncCrewMapPillUI === 'function') window._syncCrewMapPillUI();
      } catch (e) {}
      return;
    }

    // Render each in-scope alert. Sort by severity: warnings first, then
    // watches, then advisories. Within a tier, sort by expiry (soonest
    // first — most imminent gets top spot).
    var ordered = inScope.slice().sort(function(a, b) {
      var ca = upnextCategorizeAlert(a.properties);
      var cb = upnextCategorizeAlert(b.properties);
      var priorityMap = { warning: 0, watch: 1, advisory: 2 };
      if (priorityMap[ca] !== priorityMap[cb]) {
        return priorityMap[ca] - priorityMap[cb];
      }
      var ea = new Date(a.properties.expires || a.properties.ends || 0).getTime();
      var eb = new Date(b.properties.expires || b.properties.ends || 0).getTime();
      return ea - eb;
    });

    var html = '';
    for (var i = 0; i < ordered.length; i++) {
      var props = ordered[i].properties || {};
      var category = upnextCategorizeAlert(props);
      var headline = upnextHeadline(props);
      var area = upnextArea(props);
      var expiry = formatUpnextExpiry(props.expires || props.ends);
      html += '<div class="upnext-item" data-category="' + escapeHtml(category) + '">'
        +   '<div class="upnext-item-stripe"></div>'
        +   '<div class="upnext-item-body">'
        +     '<div class="upnext-item-head">'
        +       '<p class="upnext-item-headline">' + escapeHtml(headline) + '</p>'
        +       '<span class="upnext-item-category">' + escapeHtml(category) + '</span>'
        +     '</div>'
        +     '<div class="upnext-item-meta">'
        +       (area  ? '<span class="upnext-item-area">' + escapeHtml(area) + '</span>' : '')
        +       (expiry ? '<span class="upnext-item-time">' + escapeHtml(expiry) + '</span>' : '')
        +     '</div>'
        +   '</div>'
        + '</div>';
    }
    body.innerHTML = html;

    // Also re-render the map overlay (Chunk 6a) — same data feeds both
    // the footer list and the map zones.
    try {
      if (typeof renderNwsOverlay === 'function') renderNwsOverlay();
      if (typeof window._crewMapRefreshOverlay === 'function') window._crewMapRefreshOverlay();
      if (typeof window._syncCrewMapPillUI === 'function') window._syncCrewMapPillUI();
    } catch (e) {}
  }

  function fetchUpNextAlerts() {
    // Use cached Met-map result if it's fresh enough — saves a duplicate
    // request when the user has the Met portal open in another tab/session.
    if (window._metAlerts && window._metAlertsFetchedAt &&
        (Date.now() - window._metAlertsFetchedAt) < 2 * 60 * 1000) {
      upnextState.alerts = window._metAlerts;
      upnextState.fetchedAt = new Date(window._metAlertsFetchedAt);
      upnextState.error = null;
      renderUpNextFooter();
      return;
    }
    fetch(UPNEXT_NWS_URL, { headers: { 'Accept': 'application/geo+json' } })
      .then(function(r) {
        if (!r.ok) throw new Error('NWS returned ' + r.status);
        return r.json();
      })
      .then(function(geojson) {
        upnextState.alerts = (geojson && geojson.features) || [];
        upnextState.fetchedAt = new Date();
        upnextState.error = null;
        renderUpNextFooter();
      })
      .catch(function(err) {
        upnextState.error = String(err);
        // Keep any previously-rendered alerts visible on a transient
        // failure — only show the error state if we have nothing.
        renderUpNextFooter();
      });
  }

  function setupUpNextFooter() {
    if (window._upnextWired) return;
    window._upnextWired = true;
    fetchUpNextAlerts();
    // Auto-refresh every 5 minutes; pause when the feed isn't visible.
    upnextState.handle = setInterval(function() {
      var feedScreen = $('screen-crew-feed');
      if (!feedScreen || !feedScreen.classList.contains('active')) return;
      if (document.hidden) return;
      fetchUpNextAlerts();
    }, UPNEXT_REFRESH_MS);

    // Also listen for the Met map's update event — if a Met has the
    // portal open, their fresher data updates our footer too.
    window.addEventListener('metMapAlertsUpdated', function(ev) {
      if (ev && ev.detail && ev.detail.fetchedAt) {
        window._metAlertsFetchedAt = ev.detail.fetchedAt.getTime
          ? ev.detail.fetchedAt.getTime()
          : Date.now();
      }
      if (window._metAlerts) {
        upnextState.alerts = window._metAlerts;
        upnextState.fetchedAt = new Date();
        renderUpNextFooter();
      }
    });
  }

  // ── NWS overlay on the Live Feed map (Chunk 6a) ────────────────────
  // Renders warning/watch/advisory zones onto the existing stylized SVG
  // map. Reuses the alerts data fetched by setupUpNextFooter (no
  // duplicate fetch). Filter state controls which categories appear;
  // a sub-menu controls which advisory sub-types pass through.
  //
  // The Live Feed map is a stylized SVG, not a real geographic tile
  // map. We project NWS lat/long polygon centroids onto SVG percent
  // coordinates using a hand-tuned bounding box. The result isn't
  // geographically accurate to the meter, but the relative position
  // of zones reads correctly (Boone County in NW Indiana shows up
  // toward the upper-left, Marion County center, etc).

  // Bounding box of the stylized SVG. These lat/longs frame Indiana
  // plus slivers of neighboring states — chosen by eyeballing the
  // existing SVG against a real map of the region. Tweak if zones
  // land in the wrong relative position.
  var NWS_MAP_BBOX = {
    minLat: 37.4, maxLat: 42.0,
    minLng: -88.5, maxLng: -84.0
  };
  // Expose a clean API to crew-map.js so it can read shared state
  // without us having to keep pasting expose-points throughout. Each
  // getter is lazy so values reflect current state (not what they
  // were at the moment crew-map.js loaded).
  window.WV_CrewMapAPI = {
    bbox: NWS_MAP_BBOX,
    getReports:        function() {
      // Returns Crew reports + Met posts so the Leaflet crew map renders
      // both kinds. Met posts have lat/lng directly (set when the Met
      // dropped the pin); Crew reports have mapPos* percent positions
      // that the crew-map.js renderer reverse-projects via the bbox.
      var base = (typeof CREW_REPORTS !== 'undefined') ? CREW_REPORTS.slice() : [];
      if (typeof MET_POSTS !== 'undefined' && MET_POSTS.length) {
        for (var i = 0; i < MET_POSTS.length; i++) {
          var p = MET_POSTS[i];
          // Chunk 6c-2: only 'live' Met posts appear on the Crew map.
          // Scheduled posts wait for their time; cancelled posts never appear.
          if (p.status !== 'live') continue;
          base.push({
            id: p.id,
            isMetPost: true,
            type: 'met-post',
            text: p.text,
            author: p.author,
            authorInitials: p.authorInitials,
            city: 'Coverage',
            submittedAt: p.submittedAt,
            verified: p.verified || 0,
            // Lat/lng directly — crew-map.js skips the reverse-projection
            // when these fields are present.
            lat: p.lat,
            lng: p.lng
          });
        }
      }
      return base;
    },
    getAlerts:         function() { return (upnextState && upnextState.alerts) ? upnextState.alerts : []; },
    getFilters:        function() { return nwsFilters; },
    getCurrentUser:    function() { try { return auth.currentUser && auth.currentUser(); } catch (e) { return null; } },
    reportBelongsTo:   function(r, u) { return (typeof reportBelongsTo === 'function') ? reportBelongsTo(r, u) : false; },
    categorizeAlert:   function(eventName) { return categorizeAlertEvent(eventName); },
    alertPassesFilter: function(category, type) { return alertPassesFilter(category, type); },
    alertGeometryBounds: function(g) { return alertGeometryBounds(g); },
    formatTimeLabel:   function(d) { return (typeof formatTimeLabel === 'function') ? formatTimeLabel(d) : d.toString(); },
    getReportAge:      function(r) { return (typeof getReportAge === 'function') ? getReportAge(r) : { stage: 'fresh' }; },
    openReportDetail:  function(id) { if (typeof openReportDetail === 'function') openReportDetail(id); },
    onAlertsRefresh:   function(callback) {
      // Crew map can register here to know when fresh NWS data arrives.
      // We piggyback on the upnext refresh — same data, same cadence.
      var prev = window._crewMapRefreshOverlay;
      window._crewMapRefreshOverlay = function() {
        try { if (prev) prev(); } catch (e) {}
        try { callback(); } catch (e) { console.warn('crew map alerts callback failed', e); }
      };
    }
  };

  // Filter state — persisted across sessions.
  var NWS_FILTER_KEY = 'wv_nws_filters_v1';
  var nwsFilters = {
    warning: true,
    watch: true,
    advisory: false,
    advisoryTypes: {
      winter: true,
      wind: false,
      heat: false,
      flood: false,
      other: false
    }
  };

  function loadNwsFilters() {
    try {
      var raw = localStorage.getItem(NWS_FILTER_KEY);
      if (!raw) return;
      var parsed = JSON.parse(raw);
      if (parsed && typeof parsed === 'object') {
        nwsFilters.warning = parsed.warning !== false;
        nwsFilters.watch   = parsed.watch   !== false;
        nwsFilters.advisory = parsed.advisory === true;
        if (parsed.advisoryTypes) {
          ['winter', 'wind', 'heat', 'flood', 'other'].forEach(function(k) {
            if (typeof parsed.advisoryTypes[k] === 'boolean') {
              nwsFilters.advisoryTypes[k] = parsed.advisoryTypes[k];
            }
          });
        }
      }
    } catch (e) { /* default state stays */ }
  }
  function saveNwsFilters() {
    try {
      localStorage.setItem(NWS_FILTER_KEY, JSON.stringify(nwsFilters));
    } catch (e) {}
  }

  // Map an NWS event name to a top-level category + advisory sub-type.
  function categorizeAlertEvent(eventName) {
    var e = (eventName || '').toLowerCase();
    var category;
    if (e.indexOf('warning') !== -1) category = 'warning';
    else if (e.indexOf('watch') !== -1) category = 'watch';
    else category = 'advisory';

    var advisoryType = 'other';
    if (/winter|ice|sleet|snow|blizzard|freezing rain|lake effect/.test(e)) {
      advisoryType = 'winter';
    } else if (/wind|dust|fog/.test(e)) {
      advisoryType = 'wind';
    } else if (/heat|cold|freeze|frost/.test(e)) {
      advisoryType = 'heat';
    } else if (/flood|hydro/.test(e)) {
      advisoryType = 'flood';
    }
    return { category: category, advisoryType: advisoryType };
  }

  function alertGeometryBounds(geometry) {
    if (!geometry || !geometry.coordinates) return null;
    var coords = [];
    if (geometry.type === 'Polygon') {
      coords = geometry.coordinates[0] || [];
    } else if (geometry.type === 'MultiPolygon') {
      (geometry.coordinates || []).forEach(function(poly) {
        (poly[0] || []).forEach(function(pt) { coords.push(pt); });
      });
    } else {
      return null;
    }
    if (!coords.length) return null;
    var minLng = Infinity, maxLng = -Infinity;
    var minLat = Infinity, maxLat = -Infinity;
    for (var i = 0; i < coords.length; i++) {
      var lng = coords[i][0], lat = coords[i][1];
      if (lng < minLng) minLng = lng;
      if (lng > maxLng) maxLng = lng;
      if (lat < minLat) minLat = lat;
      if (lat > maxLat) maxLat = lat;
    }
    return {
      minLat: minLat, maxLat: maxLat,
      minLng: minLng, maxLng: maxLng
    };
  }

  // Project a lat/long bbox onto our SVG's percent-coordinate space.
  function projectBoundsToSvg(bounds) {
    if (!bounds) return null;
    var bbox = NWS_MAP_BBOX;
    var topPct = ((bbox.maxLat - bounds.maxLat) / (bbox.maxLat - bbox.minLat)) * 100;
    var botPct = ((bbox.maxLat - bounds.minLat) / (bbox.maxLat - bbox.minLat)) * 100;
    var leftPct  = ((bounds.minLng - bbox.minLng) / (bbox.maxLng - bbox.minLng)) * 100;
    var rightPct = ((bounds.maxLng - bbox.minLng) / (bbox.maxLng - bbox.minLng)) * 100;

    topPct  = Math.max(0, Math.min(100, topPct));
    botPct  = Math.max(0, Math.min(100, botPct));
    leftPct = Math.max(0, Math.min(100, leftPct));
    rightPct = Math.max(0, Math.min(100, rightPct));

    var width  = rightPct - leftPct;
    var height = botPct - topPct;
    if (width <= 0 || height <= 0) return null;

    if (width  < 3) width  = 3;
    if (height < 3) height = 3;

    return {
      top:    topPct.toFixed(2) + '%',
      left:   leftPct.toFixed(2) + '%',
      width:  width.toFixed(2) + '%',
      height: height.toFixed(2) + '%'
    };
  }

  function alertPassesFilter(category, advisoryType) {
    if (category === 'warning') return nwsFilters.warning;
    if (category === 'watch') return nwsFilters.watch;
    if (category === 'advisory') {
      if (!nwsFilters.advisory) return false;
      return nwsFilters.advisoryTypes[advisoryType] === true;
    }
    return false;
  }

  function renderNwsOverlay() {
    var overlay = $('feed-nws-overlay');
    if (!overlay) return;

    var counts = { warning: 0, watch: 0, advisory: 0 };
    var alerts = (upnextState && upnextState.alerts) ? upnextState.alerts : [];

    // First pass — count visible alerts per category for the pill counters.
    for (var i = 0; i < alerts.length; i++) {
      var props = alerts[i].properties || {};
      var cat = categorizeAlertEvent(props.event || '').category;
      counts[cat] = (counts[cat] || 0) + 1;
    }
    var countWarning  = $('nws-filter-count-warning');
    var countWatch    = $('nws-filter-count-watch');
    var countAdvisory = $('nws-filter-count-advisory');
    if (countWarning)  countWarning.textContent  = counts.warning;
    if (countWatch)    countWatch.textContent    = counts.watch;
    if (countAdvisory) countAdvisory.textContent = counts.advisory;
    // Mirror counts to the Crew map filter pills (Chunk 6b) — same data,
    // two surfaces. The IDs are crewmap-count-* (defined on screen-crew-map).
    var cmW = $('crewmap-count-warning');
    var cmA = $('crewmap-count-watch');
    var cmD = $('crewmap-count-advisory');
    if (cmW) cmW.textContent = counts.warning;
    if (cmA) cmA.textContent = counts.watch;
    if (cmD) cmD.textContent = counts.advisory;

    // Second pass — build the visible zone list.
    var html = '';
    for (var j = 0; j < alerts.length; j++) {
      var feature = alerts[j];
      var p = feature.properties || {};
      var meta = categorizeAlertEvent(p.event || '');
      if (!alertPassesFilter(meta.category, meta.advisoryType)) continue;

      var bounds = alertGeometryBounds(feature.geometry);
      if (!bounds) continue;
      var pos = projectBoundsToSvg(bounds);
      if (!pos) continue;

      var headline = p.event || 'Active alert';
      var expires = p.expires || p.ends || '';
      html += '<div class="feed-nws-zone" data-category="' + escapeHtml(meta.category) + '" '
        + 'data-headline="' + escapeHtml(headline) + '" '
        + 'data-expires="' + escapeHtml(expires) + '" '
        + 'style="top:' + pos.top + ';left:' + pos.left
        + ';width:' + pos.width + ';height:' + pos.height + ';" '
        + 'aria-label="' + escapeHtml(meta.category + ': ' + headline) + '">'
        + '</div>';
    }
    overlay.innerHTML = html;
  }

  function setupNwsOverlayClicks() {
    if (window._nwsOverlayClicksWired) return;
    window._nwsOverlayClicksWired = true;
    document.addEventListener('click', function(ev) {
      // Close any open popup if clicking outside
      var existingPopup = document.querySelector('.feed-nws-popup');
      var clickedZone = ev.target.closest('.feed-nws-zone');
      var clickedPopup = ev.target.closest('.feed-nws-popup');
      if (existingPopup && !clickedPopup && !clickedZone) {
        existingPopup.remove();
      }
      if (!clickedZone) return;
      ev.preventDefault();
      ev.stopPropagation();
      if (existingPopup) existingPopup.remove();

      var category = clickedZone.getAttribute('data-category') || 'advisory';
      var headline = clickedZone.getAttribute('data-headline') || 'Active alert';
      var expiresRaw = clickedZone.getAttribute('data-expires') || '';
      var expiry = '';
      if (expiresRaw) {
        try {
          var d = new Date(expiresRaw);
          if (!isNaN(d.getTime()) && typeof formatTimeLabel === 'function') {
            expiry = 'Until ' + formatTimeLabel(d);
          }
        } catch (e) {}
      }

      var popup = document.createElement('div');
      popup.className = 'feed-nws-popup';
      popup.innerHTML = ''
        + '<button type="button" class="feed-nws-popup-close" aria-label="Close">'
        +   '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        +     '<path d="M18 6 6 18"/><path d="m6 6 12 12"/>'
        +   '</svg>'
        + '</button>'
        + '<div class="feed-nws-popup-eyebrow" data-category="' + category + '">'
        +   '<span>' + escapeHtml(category) + '</span>'
        + '</div>'
        + '<p class="feed-nws-popup-headline">' + escapeHtml(headline) + '</p>'
        + (expiry ? '<p class="feed-nws-popup-meta">' + escapeHtml(expiry) + '</p>' : '');

      var zoneRect = clickedZone.getBoundingClientRect();
      var mapEl = $('feed-map');
      var mapRect = mapEl ? mapEl.getBoundingClientRect() : { top: 0, left: 0 };
      var topPx = (zoneRect.top - mapRect.top) + zoneRect.height + 6;
      var leftPx = (zoneRect.left - mapRect.left) + (zoneRect.width / 2) - 110;
      if (leftPx < 6) leftPx = 6;
      if (mapEl && (leftPx + 220) > mapEl.offsetWidth) leftPx = Math.max(6, mapEl.offsetWidth - 226);
      popup.style.top  = topPx + 'px';
      popup.style.left = leftPx + 'px';
      if (mapEl) mapEl.appendChild(popup);

      var closeBtn = popup.querySelector('.feed-nws-popup-close');
      if (closeBtn) closeBtn.addEventListener('click', function(e) {
        e.stopPropagation();
        popup.remove();
      });
    });
  }

  function setupNwsFilterControls() {
    if (window._nwsFiltersWired) return;
    window._nwsFiltersWired = true;
    loadNwsFilters();

    function syncPillUI() {
      var pills = document.querySelectorAll('.nws-filter-pill[data-filter]');
      pills.forEach(function(pill) {
        var filter = pill.getAttribute('data-filter');
        pill.setAttribute('aria-pressed', nwsFilters[filter] ? 'true' : 'false');
      });
      var inputs = document.querySelectorAll('[data-advisory-type]');
      inputs.forEach(function(input) {
        var t = input.getAttribute('data-advisory-type');
        input.checked = !!nwsFilters.advisoryTypes[t];
      });
    }
    syncPillUI();

    document.querySelectorAll('.nws-filter-pill[data-filter]').forEach(function(pill) {
      pill.addEventListener('click', function() {
        var filter = pill.getAttribute('data-filter');
        nwsFilters[filter] = !nwsFilters[filter];
        saveNwsFilters();
        syncPillUI();
        renderNwsOverlay();
        // Phase 9 Push B: also sync the Leaflet feed-map layers
        try {
          if (window.WV_FeedMapAPI && window.WV_FeedMapAPI.applyFilters) {
            window.WV_FeedMapAPI.applyFilters();
          }
        } catch (e) {}
      });
    });

    var submenuBtn = $('nws-filter-submenu-btn');
    var submenu = $('nws-filter-submenu');
    if (submenuBtn && submenu) {
      submenuBtn.addEventListener('click', function(ev) {
        ev.stopPropagation();
        var isOpen = !submenu.hidden;
        submenu.hidden = isOpen;
        submenuBtn.setAttribute('aria-expanded', isOpen ? 'false' : 'true');
      });
      document.addEventListener('click', function(ev) {
        if (submenu.hidden) return;
        if (ev.target.closest('#nws-filter-submenu') || ev.target.closest('#nws-filter-submenu-btn')) return;
        submenu.hidden = true;
        submenuBtn.setAttribute('aria-expanded', 'false');
      });
    }

    // Phase 4 (May 17) — Crew Live Feed map radar toggle pill.
    // Wires the pill to WV_FeedMapAPI.setRadar (the Live Feed map's
    // public API). Preference persists in localStorage (key:
    // wv_feed_map_radar_on, owned by the Feed map module).
    var radarPill = $('crew-map-radar-pill');
    if (radarPill) {
      try {
        var savedRadar = localStorage.getItem('wv_feed_map_radar_on') === '1';
        if (savedRadar) {
          radarPill.setAttribute('aria-pressed', 'true');
          radarPill.classList.add('is-active');
        }
      } catch (e) {}
      radarPill.addEventListener('click', function() {
        var pressed = radarPill.getAttribute('aria-pressed') === 'true';
        var nextOn = !pressed;
        radarPill.setAttribute('aria-pressed', nextOn ? 'true' : 'false');
        radarPill.classList.toggle('is-active', nextOn);
        if (window.WV_FeedMapAPI && typeof window.WV_FeedMapAPI.setRadar === 'function') {
          window.WV_FeedMapAPI.setRadar(nextOn);
        }
      });
    }

    // Phase 4 (May 17) — Crew Live Feed per-event-type filter dropdowns.
    // Each filter pill has a caret next to it. Click → dropdown lists
    // every event type active in that category with a checkbox. Uncheck
    // to hide that event type from the map. State persists via the
    // Feed map module's localStorage key.
    var FEED_FILTER_CATEGORY_LABELS = {
      warning: 'Filter warnings',
      watch: 'Filter watches',
      advisory: 'Filter advisories'
    };
    var feedFilterDropdown = $('feed-event-filter-dropdown');
    var feedFilterHeader = $('feed-event-filter-header');
    var feedFilterList = $('feed-event-filter-list');
    var feedOpenCategory = null;

    function _renderFeedEventFilter(category) {
      if (!feedFilterList) return;
      var api = window.WV_FeedMapAPI;
      if (!api || typeof api.getEventFilter !== 'function') {
        feedFilterList.innerHTML = '<div style="padding: 14px; font-size: 12.5px; color: rgba(15,17,22,0.55); text-align: center;">Map not ready yet.</div>';
        return;
      }
      var f = api.getEventFilter();
      var eventsObj = (f.eventTypesByCategory && f.eventTypesByCategory[category]) || {};
      var keys = Object.keys(eventsObj).sort(function(a, b) {
        return eventsObj[b].count - eventsObj[a].count;
      });
      if (feedFilterHeader) {
        feedFilterHeader.textContent = FEED_FILTER_CATEGORY_LABELS[category] || 'Filter';
      }
      if (keys.length === 0) {
        feedFilterList.innerHTML = '<div style="padding: 14px; font-size: 12.5px; color: rgba(15,17,22,0.55); text-align: center;">No ' + category + 's are active right now.</div>';
        return;
      }
      var html = '';
      for (var i = 0; i < keys.length; i++) {
        var key = keys[i];
        var info = eventsObj[key];
        var isVisible = !info.hidden;
        var displayName = info.event || key;
        html += '<label data-feed-evt-key="' + escapeHtml(key) + '" style="display: flex; align-items: center; gap: 10px; padding: 8px 14px; cursor: pointer; font-size: 13px; color: #0E1116;">'
          + '<input type="checkbox" ' + (isVisible ? 'checked' : '') + ' style="margin: 0;">'
          + '<span style="flex: 1;">' + escapeHtml(displayName) + '</span>'
          + '<span style="font-size: 11px; font-weight: 600; color: rgba(15,17,22,0.50); background: rgba(15,17,22,0.05); padding: 2px 7px; border-radius: 4px;">' + info.count + '</span>'
          + '</label>';
      }
      feedFilterList.innerHTML = html;
      // Hover + change handlers
      var labels = feedFilterList.querySelectorAll('label');
      for (var j = 0; j < labels.length; j++) {
        (function(label) {
          label.addEventListener('mouseenter', function() { label.style.background = 'rgba(15,17,22,0.04)'; });
          label.addEventListener('mouseleave', function() { label.style.background = ''; });
          var input = label.querySelector('input');
          if (input) {
            input.addEventListener('change', function() {
              var key = label.getAttribute('data-feed-evt-key');
              if (window.WV_FeedMapAPI && typeof window.WV_FeedMapAPI.setEventVisibility === 'function') {
                window.WV_FeedMapAPI.setEventVisibility(key, input.checked);
              }
            });
          }
        })(labels[j]);
      }
    }

    function _positionFeedDropdown(caretBtn) {
      if (!feedFilterDropdown || !caretBtn) return;
      var rect = caretBtn.getBoundingClientRect();
      feedFilterDropdown.style.top = (rect.bottom + window.scrollY + 4) + 'px';
      var leftPos = rect.left + window.scrollX;
      var dropdownWidth = 280;
      var maxLeft = window.innerWidth - dropdownWidth - 12;
      if (leftPos > maxLeft) leftPos = maxLeft;
      if (leftPos < 8) leftPos = 8;
      feedFilterDropdown.style.left = leftPos + 'px';
    }

    function _openFeedDropdown(category, caretBtn) {
      if (!feedFilterDropdown) return;
      feedOpenCategory = category;
      _renderFeedEventFilter(category);
      _positionFeedDropdown(caretBtn);
      feedFilterDropdown.hidden = false;
    }

    function _closeFeedDropdown() {
      if (feedFilterDropdown) feedFilterDropdown.hidden = true;
      feedOpenCategory = null;
    }

    var feedCarets = document.querySelectorAll('[data-event-filter-cat]');
    for (var k = 0; k < feedCarets.length; k++) {
      (function(caret) {
        caret.addEventListener('click', function(ev) {
          ev.stopPropagation();
          var cat = caret.getAttribute('data-event-filter-cat');
          if (feedOpenCategory === cat && !feedFilterDropdown.hidden) {
            _closeFeedDropdown();
          } else {
            _openFeedDropdown(cat, caret);
          }
        });
      })(feedCarets[k]);
    }

    document.addEventListener('click', function(ev) {
      if (!feedFilterDropdown || feedFilterDropdown.hidden) return;
      if (feedFilterDropdown.contains(ev.target)) return;
      if (ev.target.closest && ev.target.closest('[data-event-filter-cat]')) return;
      _closeFeedDropdown();
    });

    // When the Feed map dispatches alert updates, refresh the dropdown
    // if it's currently open so counts stay live.
    window.addEventListener('feedMapAlertsUpdated', function() {
      if (feedOpenCategory && feedFilterDropdown && !feedFilterDropdown.hidden) {
        _renderFeedEventFilter(feedOpenCategory);
      }
    });

    document.querySelectorAll('[data-advisory-type]').forEach(function(input) {
      input.addEventListener('change', function() {
        var t = input.getAttribute('data-advisory-type');
        nwsFilters.advisoryTypes[t] = input.checked;
        saveNwsFilters();
        renderNwsOverlay();
      });
    });
  }

  // ── Crew map filter pills (Chunk 6b) ───────────────────────────────
  // The full-map screen has its own filter pill row, but it shares the
  // same nwsFilters state object as the Live Feed. A user who turns
  // Warnings off on the Live Feed sees them off on the Full Map too —
  // one preference, two surfaces. The advisory sub-menu is NOT
  // duplicated here (single source of truth on the Live Feed); a
  // hint string on the row points users there if they want to tune.
  function setupCrewMapFilterPills() {
    if (window._crewMapFiltersWired) return;
    window._crewMapFiltersWired = true;
    loadNwsFilters();  // idempotent

    function syncCrewMapPillUI() {
      var pills = document.querySelectorAll('[data-crewmap-filter]');
      pills.forEach(function(pill) {
        var filter = pill.getAttribute('data-crewmap-filter');
        pill.setAttribute('aria-pressed', nwsFilters[filter] ? 'true' : 'false');
      });
      // Update count badges too
      var alerts = (typeof upnextState !== 'undefined' && upnextState.alerts) ? upnextState.alerts : [];
      var counts = { warning: 0, watch: 0, advisory: 0 };
      for (var i = 0; i < alerts.length; i++) {
        var props = alerts[i].properties || {};
        var cat = categorizeAlertEvent(props.event || '').category;
        counts[cat] = (counts[cat] || 0) + 1;
      }
      var cw = $('crewmap-count-warning');
      var cv = $('crewmap-count-watch');
      var ca = $('crewmap-count-advisory');
      if (cw) cw.textContent = counts.warning;
      if (cv) cv.textContent = counts.watch;
      if (ca) ca.textContent = counts.advisory;
    }
    // Expose to crew-map.js so it can re-sync after every NWS render
    window._syncCrewMapPillUI = syncCrewMapPillUI;
    syncCrewMapPillUI();

    document.querySelectorAll('[data-crewmap-filter]').forEach(function(pill) {
      pill.addEventListener('click', function() {
        var filter = pill.getAttribute('data-crewmap-filter');
        nwsFilters[filter] = !nwsFilters[filter];
        saveNwsFilters();
        syncCrewMapPillUI();
        // Also sync the Live Feed pills if they exist (one preference,
        // two surfaces — both UIs need to reflect the change).
        if (typeof renderNwsOverlay === 'function') {
          var lfPills = document.querySelectorAll('.nws-filter-pill[data-filter]');
          lfPills.forEach(function(p) {
            var f = p.getAttribute('data-filter');
            p.setAttribute('aria-pressed', nwsFilters[f] ? 'true' : 'false');
          });
          renderNwsOverlay();
        }
        // Tell the Crew map to re-render its NWS overlay
        if (typeof window._crewMapRefreshOverlay === 'function') {
          window._crewMapRefreshOverlay();
        }
      });
    });
  }

  // ════════════════════════════════════════════════════════════════════
  // MET POSTS (Chunk 6c-1)
  // ════════════════════════════════════════════════════════════════════
  //
  // Met posts are messages from a meteorologist that appear in the Crew
  // feed alongside Crew reports. They share most of the Crew report's
  // rendering paths (side list, map pulse, ticker, on-air rail), but
  // are visually marked as Met-authored so Crew can distinguish them.
  //
  // Data model parallels CREW_REPORTS but with extra fields:
  //   id           — auto-incrementing string identifier
  //   text         — the message body
  //   author       — Met display name (e.g., "M. Reynolds")
  //   authorInitials — for the avatar circle
  //   isMetPost    — boolean flag, used by all renderers to switch styling
  //   lat, lng     — the centroid the Met dropped on the pin map
  //   submittedAt  — ISO timestamp of publish
  //   verified     — count of Crew who've tapped "I see it too"
  //
  // We persist to localStorage so posts survive a refresh. Real backend
  // will replace this with POST /api/v1/met/posts on the server.
  var MET_POSTS = [];
  var MET_POSTS_KEY = 'wv_met_posts_v1';

  function loadMetPosts() {
    // First, fill from localStorage so the UI renders something instantly.
    // Backend GET will overwrite with authoritative data once it arrives.
    try {
      var raw = localStorage.getItem(MET_POSTS_KEY);
      if (raw) {
        var parsed = JSON.parse(raw);
        if (Array.isArray(parsed)) MET_POSTS = parsed;
      }
    } catch (e) {}

    // Phase 6b: pull from backend. Replaces in-memory + localStorage on
    // success. On failure (offline, not signed in), keep the cache so
    // the Met still sees their own work.
    fetch('https://wv-valet-backend.onrender.com/api/v1/met-posts', {
      credentials: 'include'
    })
    .then(function(r) {
      if (!r.ok) throw new Error('http-' + r.status);
      return r.json();
    })
    .then(function(j) {
      if (!j || !j.ok || !Array.isArray(j.posts)) return;
      MET_POSTS = j.posts;
      try { localStorage.setItem(MET_POSTS_KEY, JSON.stringify(MET_POSTS)); } catch (e) {}
      // Re-render all the surfaces that show Met posts so the freshly-
      // loaded data appears immediately.
      try { if (typeof refreshAllMetPostSurfaces === 'function') refreshAllMetPostSurfaces(); } catch (e) {}
      try { if (typeof renderMetPostQueue === 'function') renderMetPostQueue(); } catch (e) {}
    })
    .catch(function(err) {
      // Most common reason: not signed in yet. Normal on first load.
      console.log('[met-posts] load failed (using cache):', err.message);
    });
  }
  function saveMetPosts() {
    try {
      localStorage.setItem(MET_POSTS_KEY, JSON.stringify(MET_POSTS));
    } catch (e) {}
  }

  // Get the currently signed-in Met (the post's author). Falls back to
  // generic "Meteorologist" if auth state isn't ready.
  function currentMetIdentity() {
    try {
      var u = auth.currentUser && auth.currentUser();
      if (u && u.name) {
        var parts = u.name.split(/\s+/);
        var initials = parts.map(function(p) { return p.charAt(0); }).join('').toUpperCase().slice(0, 2);
        return { author: u.name, authorInitials: initials || 'MT' };
      }
    } catch (e) {}
    return { author: 'Meteorologist', authorInitials: 'MT' };
  }

  // Publish a Met post into the live data store + all four render
  // surfaces. Called from the modal's "Send to Crew feed" button.
  function publishMetPost(text, lat, lng) {
    return savePostAsLive(text, lat, lng);
  }

  // Migrate any loaded posts to have a status field. Old 6c-1 posts
  // were always immediate-publish, so they're 'live' by default.
  function migrateMetPostsForScheduling() {
    if (!MET_POSTS || !MET_POSTS.length) return;
    var changed = false;
    for (var i = 0; i < MET_POSTS.length; i++) {
      if (!MET_POSTS[i].status) {
        MET_POSTS[i].status = 'live';
        changed = true;
      }
    }
    if (changed) saveMetPosts();
  }

  // Create a new "live" post and trigger all-surface refresh. Used by
  // both the "Send now" path and the promotion timer (when a scheduled
  // post's time arrives).
  function savePostAsLive(text, lat, lng) {
    var identity = currentMetIdentity();
    var post = {
      id: 'met-post-' + Date.now() + '-' + Math.floor(Math.random() * 1000),
      text: text,
      author: identity.author,
      authorInitials: identity.authorInitials,
      isMetPost: true,
      lat: lat,
      lng: lng,
      submittedAt: new Date().toISOString(),
      verified: 0,
      status: 'live'
    };
    MET_POSTS.unshift(post);
    saveMetPosts();
    refreshAllMetPostSurfaces(post);
    // Phase 6b — also persist to backend (fire and forget)
    fetch('https://wv-valet-backend.onrender.com/api/v1/met-posts', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({
        id: post.id, text: text, lat: lat, lng: lng, status: 'live'
      })
    }).catch(function(err) { console.warn('[met-posts] POST failed:', err); });
    return post;
  }

  // Create a scheduled post — same shape but status='scheduled' and
  // scheduledFor holds the ISO timestamp when it should go live.
  function savePostAsScheduled(text, lat, lng, scheduledForISO) {
    var identity = currentMetIdentity();
    var post = {
      id: 'met-post-' + Date.now() + '-' + Math.floor(Math.random() * 1000),
      text: text,
      author: identity.author,
      authorInitials: identity.authorInitials,
      isMetPost: true,
      lat: lat,
      lng: lng,
      verified: 0,
      status: 'scheduled',
      scheduledFor: scheduledForISO
    };
    MET_POSTS.unshift(post);
    saveMetPosts();
    renderMetPostQueue();
    // Phase 6b — also persist to backend (fire and forget)
    fetch('https://wv-valet-backend.onrender.com/api/v1/met-posts', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({
        id: post.id, text: text, lat: lat, lng: lng,
        status: 'scheduled', scheduledFor: scheduledForISO
      })
    }).catch(function(err) { console.warn('[met-posts] POST failed:', err); });
    return post;
  }

  // Update an existing scheduled post (used by the edit flow).
  function updateScheduledPost(id, text, lat, lng, scheduledForISO) {
    for (var i = 0; i < MET_POSTS.length; i++) {
      if (MET_POSTS[i].id === id && MET_POSTS[i].status === 'scheduled') {
        MET_POSTS[i].text = text;
        MET_POSTS[i].lat = lat;
        MET_POSTS[i].lng = lng;
        MET_POSTS[i].scheduledFor = scheduledForISO;
        saveMetPosts();
        renderMetPostQueue();
        // Phase 6b — also update on backend (fire and forget)
        fetch('https://wv-valet-backend.onrender.com/api/v1/met-posts/' + encodeURIComponent(id), {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({
            text: text, lat: lat, lng: lng, scheduledFor: scheduledForISO
          })
        }).catch(function(err) { console.warn('[met-posts] PATCH failed:', err); });
        return MET_POSTS[i];
      }
    }
    return null;
  }

  // Cancel a scheduled post — flips status to 'cancelled' so it
  // disappears from the queue. We keep the record (rather than splicing
  // it out) for future audit/history. Cancelled posts never appear in
  // Crew surfaces because every filter checks status==='live'.
  function cancelScheduledPost(id) {
    for (var i = 0; i < MET_POSTS.length; i++) {
      if (MET_POSTS[i].id === id && MET_POSTS[i].status === 'scheduled') {
        MET_POSTS[i].status = 'cancelled';
        MET_POSTS[i].cancelledAt = new Date().toISOString();
        saveMetPosts();
        renderMetPostQueue();
        // Phase 6b — also delete on backend (fire and forget)
        fetch('https://wv-valet-backend.onrender.com/api/v1/met-posts/' + encodeURIComponent(id), {
          method: 'DELETE',
          credentials: 'include'
        }).catch(function(err) { console.warn('[met-posts] DELETE failed:', err); });
        return true;
      }
    }
    return false;
  }

  // Centralized refresh — called from any path that surfaces a new
  // live post. Re-renders Crew feed surfaces and fires the map event.
  function refreshAllMetPostSurfaces(post) {
    try { if (typeof renderFeed === 'function') renderFeed(); } catch (e) {}
    try { if (typeof renderOnAirRail === 'function') renderOnAirRail(); } catch (e) {}
    try { if (typeof tickerQueue !== 'undefined') { tickerQueue = buildTickerQueue(); } } catch (e) {}
    try {
      window.dispatchEvent(new CustomEvent('wvMetPostPublished', { detail: post }));
    } catch (e) {}
  }

  // ── Promotion timer (Chunk 6c-2) ──────────────────────────────────
  // Wakes every minute, finds any scheduled post whose scheduledFor is
  // in the past, flips it to status='live', triggers all-surface
  // refresh. Idempotent — safe to run repeatedly.
  function checkScheduledPosts() {
    var now = Date.now();
    var anyPromoted = false;
    var promotedIds = [];
    for (var i = 0; i < MET_POSTS.length; i++) {
      var p = MET_POSTS[i];
      if (p.status !== 'scheduled' || !p.scheduledFor) continue;
      var scheduledMs;
      try { scheduledMs = new Date(p.scheduledFor).getTime(); }
      catch (e) { continue; }
      if (isNaN(scheduledMs)) continue;
      if (scheduledMs <= now) {
        p.status = 'live';
        p.submittedAt = new Date(scheduledMs).toISOString();
        anyPromoted = true;
        promotedIds.push({ id: p.id, submittedAt: p.submittedAt });
      }
    }
    if (anyPromoted) {
      saveMetPosts();
      refreshAllMetPostSurfaces();
      renderMetPostQueue();
      // Phase 6b — fire PATCH for each promoted post so other devices
      // see status=live next time they pull. Multiple PATCHes are fine
      // (one per promoted post). If any fail, the local state is still
      // correct; next page load reconciles via the backend GET.
      promotedIds.forEach(function(p) {
        fetch('https://wv-valet-backend.onrender.com/api/v1/met-posts/' + encodeURIComponent(p.id), {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ status: 'live', submittedAt: p.submittedAt })
        }).catch(function(err) { console.warn('[met-posts] promotion PATCH failed:', err); });
      });
    }
  }
  function startMetPostPromotionTimer() {
    if (window._metPostPromotionTimerStarted) return;
    window._metPostPromotionTimerStarted = true;
    // Catch any posts that became overdue while the tab was closed.
    checkScheduledPosts();
    setInterval(checkScheduledPosts, 60 * 1000);
  }

  // ── Queue rendering (Chunk 6c-2) ──────────────────────────────────
  // Renders the inline list of scheduled posts below the composer.
  function renderMetPostQueue() {
    var panel = $('met-post-queue');
    var list  = $('met-post-queue-list');
    var countEl = $('met-post-queue-count');
    if (!panel || !list) return;

    var scheduled = MET_POSTS.filter(function(p) { return p.status === 'scheduled'; });
    // Sort soonest-first
    scheduled.sort(function(a, b) {
      return new Date(a.scheduledFor).getTime() - new Date(b.scheduledFor).getTime();
    });

    if (countEl) countEl.textContent = String(scheduled.length);

    if (scheduled.length === 0) {
      panel.hidden = true;
      list.innerHTML = '';
      return;
    }
    panel.hidden = false;

    var html = '';
    var now = Date.now();
    for (var i = 0; i < scheduled.length; i++) {
      var p = scheduled[i];
      var when;
      try { when = new Date(p.scheduledFor); } catch (e) { continue; }
      var msUntil = when.getTime() - now;
      var isOverdue = msUntil <= 0;  // promotion timer didn't catch yet
      var whenLabel = formatScheduledForLabel(when);
      var relativeLabel = isOverdue
        ? 'publishing now\u2026'
        : formatRelativeFutureLabel(msUntil);

      html += '<li class="met-post-queue-item" data-queue-id="' + escapeHtml(p.id) + '">'
        +   '<span class="met-post-queue-icon">'
        +     '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        +       '<circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>'
        +     '</svg>'
        +   '</span>'
        +   '<div class="met-post-queue-body">'
        +     '<p class="met-post-queue-text">' + escapeHtml(p.text) + '</p>'
        +     '<span class="met-post-queue-meta' + (isOverdue ? ' met-post-queue-meta-overdue' : '') + '">'
        +       escapeHtml(whenLabel) + ' \u00b7 ' + escapeHtml(relativeLabel)
        +     '</span>'
        +   '</div>'
        +   '<div class="met-post-queue-actions">'
        +     '<button type="button" class="met-post-queue-action-btn is-edit" '
        +             'data-queue-edit="' + escapeHtml(p.id) + '" title="Edit this scheduled post" aria-label="Edit">'
        +       '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        +         '<path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>'
        +         '<path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>'
        +       '</svg>'
        +     '</button>'
        +     '<button type="button" class="met-post-queue-action-btn is-cancel" '
        +             'data-queue-cancel="' + escapeHtml(p.id) + '" title="Cancel this scheduled post" aria-label="Cancel">'
        +       '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        +         '<path d="M18 6 6 18"/><path d="m6 6 12 12"/>'
        +       '</svg>'
        +     '</button>'
        +   '</div>'
        + '</li>';
    }
    list.innerHTML = html;
  }

  // ── Date/time formatting helpers ──────────────────────────────────
  function formatScheduledForLabel(d) {
    // "Today, 3:45 PM" / "Tomorrow, 8:00 AM" / "Wed May 14, 10:30 AM"
    if (!(d instanceof Date) || isNaN(d.getTime())) return '\u2014';
    var now = new Date();
    var sameDay = d.toDateString() === now.toDateString();
    var tomorrow = new Date(now);
    tomorrow.setDate(tomorrow.getDate() + 1);
    var isTomorrow = d.toDateString() === tomorrow.toDateString();
    var time = d.toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit' });
    if (sameDay) return 'Today, ' + time;
    if (isTomorrow) return 'Tomorrow, ' + time;
    var dateStr = d.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });
    return dateStr + ', ' + time;
  }
  function formatRelativeFutureLabel(msUntil) {
    if (msUntil <= 0) return 'any moment';
    var min = Math.round(msUntil / 60000);
    if (min < 1)    return '<1 min from now';
    if (min < 60)   return 'in ' + min + ' min';
    var hours = Math.round(min / 60);
    if (hours < 24) return 'in ' + hours + ' hr';
    var days = Math.round(hours / 24);
    return 'in ' + days + ' day' + (days === 1 ? '' : 's');
  }

  // Local timezone string ("EDT", "EST", or fallback like "GMT-4").
  function localTimezoneLabel() {
    try {
      // Format a date and grab the timezone abbreviation from the result.
      var parts = new Intl.DateTimeFormat('en-US', { timeZoneName: 'short' }).formatToParts(new Date());
      var tz = parts.find(function(p) { return p.type === 'timeZoneName'; });
      if (tz) return tz.value;
    } catch (e) {}
    var offset = new Date().getTimezoneOffset();
    var sign = offset > 0 ? '-' : '+';
    var hours = Math.abs(Math.floor(offset / 60));
    return 'UTC' + sign + hours;
  }


  // ════════════════════════════════════════════════════════════════════
  // Polygon-drawing map (Chunk D)
  // ════════════════════════════════════════════════════════════════════
  //
  // Reusable Leaflet map with drawing tools. Used in two places:
  //   1. Daily Brief tab — Met draws targeting area for the brief
  //   2. Mission tab — Met draws targeting area for an alert mission
  //
  // Uses leaflet-draw plugin from CDN (loaded lazily on first use).
  // The drawn shape is captured as GeoJSON and stored in a hidden
  // input field that the parent flow reads at save time.
  //
  // Public API:
  //   window.initPolygonMap(opts)  — mount a map instance
  //     opts.containerId      — element id where the map renders
  //     opts.loadingSelector  — selector for the loading overlay (cleared on ready)
  //     opts.hiddenInputId    — id of input field that receives the GeoJSON
  //     opts.statusElId       — id of status text element (updated on draw)
  //     opts.initialCenter    — [lat, lng], optional, defaults to Indianapolis
  //     opts.initialZoom      — number, optional, defaults to 7
  //     opts.initialShape     — GeoJSON Feature, optional, pre-drops a saved shape
  //
  // Returns the Leaflet map instance for the caller, or null if init failed.

  var _polygonMapInstances = {};  // containerId -> map instance, for re-init guard

  function initPolygonMap(opts) {
    var containerId   = opts.containerId;
    var loadingEl     = opts.loadingSelector ? document.querySelector(opts.loadingSelector) : null;
    var hiddenInput   = opts.hiddenInputId ? document.getElementById(opts.hiddenInputId) : null;
    var statusEl      = opts.statusElId ? document.getElementById(opts.statusElId) : null;
    var initialCenter = opts.initialCenter || [39.8, -86.2];
    var initialZoom   = opts.initialZoom || 7;

    // Re-init guard. Each containerId gets one map instance for its life.
    if (_polygonMapInstances[containerId]) {
      // Make sure size is fresh — the container may have been hidden
      // and just got revealed (tab switch).
      setTimeout(function() {
        try { _polygonMapInstances[containerId].invalidateSize(); } catch (e) {}
      }, 60);
      return _polygonMapInstances[containerId];
    }

    // Load Leaflet CSS + JS, then Leaflet-Draw CSS + JS, in order.
    // Each loader is idempotent so multiple polygon maps share resources.
    function ensureLeafletCss() {
      if (document.querySelector('link[data-leaflet]')) return;
      var link = document.createElement('link');
      link.rel = 'stylesheet';
      link.href = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
      link.setAttribute('data-leaflet', 'css');
      document.head.appendChild(link);
    }
    function ensureLeafletDrawCss() {
      if (document.querySelector('link[data-leaflet-draw]')) return;
      var link = document.createElement('link');
      link.rel = 'stylesheet';
      link.href = 'https://unpkg.com/leaflet-draw@1.0.4/dist/leaflet.draw.css';
      link.setAttribute('data-leaflet-draw', 'css');
      document.head.appendChild(link);
    }
    function loadScript(src, attr, onload, onerror) {
      if (document.querySelector('script[' + attr + ']')) { onload(); return; }
      var s = document.createElement('script');
      s.src = src;
      s.async = true;
      s.setAttribute(attr, '1');
      s.onload = onload;
      s.onerror = onerror;
      document.head.appendChild(s);
    }

    function showLoadError(msg) {
      if (loadingEl) {
        loadingEl.innerHTML = '<span style="color:#C2342B;text-align:center;padding:20px;">'
          + (msg || 'Couldn\u2019t load the map. Check your connection and try again.')
          + '</span>';
      }
    }

    function buildMap() {
      var container = document.getElementById(containerId);
      if (!container || !window.L || !window.L.Control || !window.L.Control.Draw) {
        showLoadError('Map drawing tools failed to load.');
        return null;
      }

      var map = L.map(container, {
        center: initialCenter,
        zoom: initialZoom,
        scrollWheelZoom: true,
        zoomControl: true
      });
      L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors &copy; <a href="https://carto.com/">CARTO</a>',
        subdomains: 'abcd',
        maxZoom: 19
      }).addTo(map);

      // Layer for drawn shapes. One shape at a time — drawing a new
      // shape clears the previous one (handled in created/edited events).
      var drawnItems = L.featureGroup().addTo(map);

      // Draw control. Limit to rectangle, circle, polygon, and freehand-
      // ish polygon (Leaflet-draw doesn't have a true freehand mode out
      // of the box, but the polygon tool with simplify gets close).
      var drawControl = new L.Control.Draw({
        position: 'topleft',
        draw: {
          marker: false,
          polyline: false,
          circlemarker: false,
          rectangle: { shapeOptions: { color: '#4169E1', weight: 2 } },
          circle: { shapeOptions: { color: '#4169E1', weight: 2 } },
          polygon: {
            shapeOptions: { color: '#4169E1', weight: 2 },
            allowIntersection: false,
            showArea: false
          }
        },
        edit: {
          featureGroup: drawnItems,
          remove: true
        }
      });
      map.addControl(drawControl);

      function shapeToGeoJSON(layer) {
        // Leaflet's toGeoJSON works for polygons + rectangles but loses
        // circles (which become Points). We tag the type ourselves and
        // include the radius for circles so the backend can rebuild.
        var feature = layer.toGeoJSON();
        feature.properties = feature.properties || {};
        if (layer instanceof L.Circle) {
          feature.properties.shape_type = 'circle';
          feature.properties.radius_meters = layer.getRadius();
          var c = layer.getLatLng();
          feature.properties.center = [c.lng, c.lat];
        } else if (layer instanceof L.Rectangle) {
          feature.properties.shape_type = 'rectangle';
        } else {
          feature.properties.shape_type = 'polygon';
        }
        return feature;
      }

      function persistDrawn() {
        // Stringify whatever shape exists right now. Empty if cleared.
        var layers = drawnItems.getLayers();
        if (!layers.length) {
          if (hiddenInput) hiddenInput.value = '';
          if (statusEl) {
            statusEl.textContent = 'No shape drawn yet.';
            statusEl.classList.remove('is-active');
          }
          return;
        }
        var feature = shapeToGeoJSON(layers[0]);
        if (hiddenInput) hiddenInput.value = JSON.stringify(feature);
        if (statusEl) {
          var label = feature.properties.shape_type;
          var detail;
          if (label === 'circle') {
            var km = (feature.properties.radius_meters / 1000).toFixed(1);
            detail = 'Circle, ' + km + ' km radius';
          } else if (label === 'rectangle') {
            detail = 'Rectangle drawn';
          } else {
            detail = 'Polygon drawn';
          }
          statusEl.textContent = '\u2713 ' + detail + ' \u00b7 use the toolbar to edit or delete';
          statusEl.classList.add('is-active');
        }
      }

      // When a new shape is drawn, clear any prior shape (single-shape
      // model) and add the new one.
      map.on(L.Draw.Event.CREATED, function(e) {
        drawnItems.clearLayers();
        drawnItems.addLayer(e.layer);
        persistDrawn();
      });
      map.on(L.Draw.Event.EDITED, persistDrawn);
      map.on(L.Draw.Event.DELETED, persistDrawn);

      // Pre-load a saved shape, if one was passed in.
      if (opts.initialShape) {
        try {
          var feat = typeof opts.initialShape === 'string'
            ? JSON.parse(opts.initialShape)
            : opts.initialShape;
          var stype = (feat.properties && feat.properties.shape_type) || '';
          if (stype === 'circle' && feat.properties.center) {
            var c = feat.properties.center;
            drawnItems.addLayer(L.circle([c[1], c[0]], {
              radius: feat.properties.radius_meters,
              color: '#4169E1', weight: 2
            }));
          } else {
            // Polygon or rectangle — let GeoJSON layer handle it
            L.geoJSON(feat, {
              style: { color: '#4169E1', weight: 2 }
            }).eachLayer(function(layer) { drawnItems.addLayer(layer); });
          }
          persistDrawn();
        } catch (e) {
          console.warn('Could not pre-load saved shape:', e);
        }
      }

      _polygonMapInstances[containerId] = map;
      if (loadingEl) loadingEl.classList.add('is-hidden');
      setTimeout(function() { map.invalidateSize(); }, 80);
      return map;
    }

    // Load Leaflet → Leaflet.draw → buildMap. The latter assumes both
    // are present on window.L.
    ensureLeafletCss();
    ensureLeafletDrawCss();
    function loadLeafletThenDraw() {
      if (window.L && window.L.Control && window.L.Control.Draw) {
        buildMap();
        return;
      }
      if (window.L) {
        // Leaflet present, need Leaflet-Draw
        loadScript('https://unpkg.com/leaflet-draw@1.0.4/dist/leaflet.draw.js',
          'data-leaflet-draw-js', buildMap,
          function() { showLoadError('Couldn\u2019t load leaflet-draw.'); });
        return;
      }
      loadScript('https://unpkg.com/leaflet@1.9.4/dist/leaflet.js', 'data-leaflet-js',
        function() {
          loadScript('https://unpkg.com/leaflet-draw@1.0.4/dist/leaflet.draw.js',
            'data-leaflet-draw-js', buildMap,
            function() { showLoadError('Couldn\u2019t load leaflet-draw.'); });
        },
        function() { showLoadError('Couldn\u2019t load Leaflet.'); });
    }
    loadLeafletThenDraw();

    return null;  // map is built async; caller can re-call to retrieve later
  }

  // Mount the polygon map for the Daily Brief tab whenever the Met
  // workspace shows the brief panel. We lazily init the first time
  // the brief panel becomes visible — Leaflet needs the container to
  // have nonzero dimensions.
  function setupBriefPolygonMap() {
    if (window._briefPolygonMapWired) return;
    window._briefPolygonMapWired = true;

    function tryInit() {
      var container = document.getElementById('brief-polygon-map');
      if (!container) return;
      var briefPanel = container.closest('[data-met-panel="brief"]');
      // Only init when the panel is visible (display !== none)
      if (briefPanel && briefPanel.style.display === 'none') return;
      initPolygonMap({
        containerId:    'brief-polygon-map',
        loadingSelector:'[data-polygon-loading="brief"]',
        hiddenInputId:  'brief-polygon-geojson',
        statusElId:     'brief-polygon-status',
        initialCenter:  [39.8, -86.2],
        initialZoom:    7
      });
    }

    // Try immediately, and also on tab clicks (the brief tab may not
    // be the active one on first load).
    tryInit();
    var tabBtns = document.querySelectorAll('[data-met-tab]');
    tabBtns.forEach(function(btn) {
      btn.addEventListener('click', function() {
        // Defer to let the panel become visible before measuring
        setTimeout(tryInit, 60);
      });
    });
  }


  // ── Met-post composer wiring (Chunk 6c-1) ───────────────────────
  // Lives inside the brief panel on the Met workspace. Met types a
  // message, clicks "Send to Crew feed," modal opens with pin-drop
  // map. On send, the post publishes to MET_POSTS and the modal closes.
  function setupMetPostComposer() {
    if (window._metPostComposerWired) return;
    window._metPostComposerWired = true;

    var textarea = $('met-post-text');
    var sendBtn  = $('met-post-send-btn');
    var hint     = $('met-post-composer-hint');
    if (!textarea || !sendBtn) return;

    function updateState() {
      var text = textarea.value.trim();
      var hasText = text.length > 0;
      sendBtn.disabled = !hasText;
      if (hint && !hint.classList.contains('is-success')) {
        hint.textContent = hasText
          ? 'Click to open the pin-drop map'
          : 'Type a message to enable';
      }
    }
    textarea.addEventListener('input', function() {
      // Clear any lingering success hint as soon as the Met types again
      if (hint) hint.classList.remove('is-success');
      updateState();
    });
    sendBtn.addEventListener('click', function() {
      var text = textarea.value.trim();
      if (!text) return;
      openMetPostModal(text);
    });
    updateState();
  }

  // ── Met-post pin-drop modal wiring ─────────────────────────────
  var metPostModalState = {
    map: null,
    marker: null,
    pinLatLng: null,
    pendingText: '',
    loaded: false,
    // Chunk 6c-2 additions
    scheduleMode: 'now',         // 'now' | 'later'
    editingId: null              // post id being edited, or null for new post
  };

  // Open modal for a new post.
  function openMetPostModal(text) {
    openMetPostModalInternal({ text: text, editingPost: null });
  }
  // Open modal pre-filled to edit an existing scheduled post.
  function openMetPostModalForEdit(post) {
    openMetPostModalInternal({ text: post.text, editingPost: post });
  }
  function openMetPostModalInternal(opts) {
    var modal = $('met-post-modal');
    var preview = $('met-post-modal-preview');
    if (!modal || !preview) return;

    var text = opts.text || '';
    var editingPost = opts.editingPost || null;

    metPostModalState.pendingText = text;
    metPostModalState.editingId = editingPost ? editingPost.id : null;
    metPostModalState.pinLatLng = editingPost ? { lat: editingPost.lat, lng: editingPost.lng } : null;
    metPostModalState.scheduleMode = editingPost ? 'later' : 'now';  // edits always re-schedule

    preview.textContent = text;
    modal.hidden = false;

    // Reset pin status. If editing, we'll drop the saved pin once the map's built.
    var status = $('met-post-modal-pin-status');
    if (status) {
      status.classList.remove('is-pinned');
      status.textContent = editingPost
        ? 'Pin at saved location \u00b7 click to move it.'
        : 'No pin dropped yet \u00b7 click on the map to set one.';
      if (editingPost) status.classList.add('is-pinned');
    }

    syncScheduleToggleUI();
    populateScheduleFields(editingPost);
    refreshSendButtonState();

    // Init the mini Leaflet map (idempotent). On edit, drop the saved pin
    // once the map is ready.
    initMetPostModalMap(editingPost);

    // Update modal title to reflect edit mode
    var title = $('met-post-modal-title');
    if (title) title.textContent = editingPost ? 'Edit scheduled post' : 'Send to Crew feed';
  }
  function closeMetPostModal() {
    var modal = $('met-post-modal');
    if (modal) modal.hidden = true;
    // Don't clear editingId here — the cancel-confirm modal may need it.
  }

  // Reflect metPostModalState.scheduleMode in the UI: which toggle button
  // is "active" and whether the date/time fields are visible.
  function syncScheduleToggleUI() {
    var buttons = document.querySelectorAll('[data-schedule-mode]');
    buttons.forEach(function(b) {
      var mode = b.getAttribute('data-schedule-mode');
      var isActive = mode === metPostModalState.scheduleMode;
      b.classList.toggle('is-active', isActive);
      b.setAttribute('aria-pressed', isActive ? 'true' : 'false');
    });
    var fields = $('met-post-schedule-fields');
    if (fields) fields.hidden = (metPostModalState.scheduleMode !== 'later');
    var sendBtn = $('met-post-modal-send');
    if (sendBtn) {
      sendBtn.textContent = metPostModalState.scheduleMode === 'later'
        ? (metPostModalState.editingId ? 'Update scheduled' : 'Schedule')
        : 'Send to Crew feed';
    }
  }
  function populateScheduleFields(editingPost) {
    var dateInput = $('met-post-schedule-date');
    var timeInput = $('met-post-schedule-time');
    var tzEl = $('met-post-schedule-tz');
    var errEl = $('met-post-schedule-error');
    if (tzEl) tzEl.textContent = localTimezoneLabel();
    if (errEl) errEl.hidden = true;

    if (editingPost && editingPost.scheduledFor) {
      // Pre-fill with the saved scheduled time. Both inputs expect
      // local-time format ("YYYY-MM-DD" and "HH:MM").
      var d = new Date(editingPost.scheduledFor);
      if (dateInput) dateInput.value = formatDateInputValue(d);
      if (timeInput) timeInput.value = formatTimeInputValue(d);
    } else {
      // Default to 1 hour from now, rounded to the next 5 minutes.
      var future = new Date(Date.now() + 60 * 60 * 1000);
      var roundedMin = Math.ceil(future.getMinutes() / 5) * 5;
      if (roundedMin === 60) {
        future.setHours(future.getHours() + 1);
        future.setMinutes(0);
      } else {
        future.setMinutes(roundedMin);
      }
      future.setSeconds(0);
      if (dateInput) dateInput.value = formatDateInputValue(future);
      if (timeInput) timeInput.value = formatTimeInputValue(future);
    }
    // Set a min date of today so the picker UI prevents past dates.
    if (dateInput) {
      var today = new Date();
      dateInput.min = formatDateInputValue(today);
      // Sensible max: 30 days out
      var thirty = new Date(Date.now() + 30 * 24 * 60 * 60 * 1000);
      dateInput.max = formatDateInputValue(thirty);
    }
  }
  function formatDateInputValue(d) {
    // YYYY-MM-DD in local time
    var y = d.getFullYear();
    var m = String(d.getMonth() + 1).padStart(2, '0');
    var day = String(d.getDate()).padStart(2, '0');
    return y + '-' + m + '-' + day;
  }
  function formatTimeInputValue(d) {
    // HH:MM in local time, 24-hour (what <input type="time"> expects)
    var h = String(d.getHours()).padStart(2, '0');
    var min = String(d.getMinutes()).padStart(2, '0');
    return h + ':' + min;
  }

  // Parse the current date+time field values into an ISO timestamp.
  // Returns null with an error string if the values don't make sense.
  function readScheduleFields() {
    var dateInput = $('met-post-schedule-date');
    var timeInput = $('met-post-schedule-time');
    var dateVal = dateInput ? dateInput.value : '';
    var timeVal = timeInput ? timeInput.value : '';
    if (!dateVal || !timeVal) {
      return { error: 'Please pick both a date and a time.' };
    }
    // Construct a Date in the local timezone from the parts.
    var parts = dateVal.split('-');
    var timeParts = timeVal.split(':');
    if (parts.length !== 3 || timeParts.length < 2) {
      return { error: 'Date or time is malformed.' };
    }
    var d = new Date(
      parseInt(parts[0], 10),
      parseInt(parts[1], 10) - 1,
      parseInt(parts[2], 10),
      parseInt(timeParts[0], 10),
      parseInt(timeParts[1], 10),
      0, 0
    );
    if (isNaN(d.getTime())) {
      return { error: 'Date or time is invalid.' };
    }
    if (d.getTime() <= Date.now()) {
      return { error: 'That time is in the past. Pick a future time.' };
    }
    return { iso: d.toISOString(), date: d };
  }

  // Enable/disable the modal send button based on current state.
  function refreshSendButtonState() {
    var sendBtn = $('met-post-modal-send');
    if (!sendBtn) return;
    // Always need a pin and text to enable
    if (!metPostModalState.pinLatLng || !metPostModalState.pendingText) {
      sendBtn.disabled = true;
      return;
    }
    if (metPostModalState.scheduleMode === 'later') {
      // Need a valid future date/time too
      var sched = readScheduleFields();
      sendBtn.disabled = !!sched.error;
      var errEl = $('met-post-schedule-error');
      if (errEl) {
        if (sched.error) {
          errEl.textContent = sched.error;
          errEl.hidden = false;
        } else {
          errEl.hidden = true;
        }
      }
    } else {
      sendBtn.disabled = false;
    }
  }

  function initMetPostModalMap(editingPost) {
    function maybeDropSavedPin() {
      if (!editingPost || editingPost.lat == null || editingPost.lng == null) return;
      if (!metPostModalState.map || !window.L) return;
      if (metPostModalState.marker) {
        metPostModalState.map.removeLayer(metPostModalState.marker);
      }
      metPostModalState.marker = L.marker([editingPost.lat, editingPost.lng], {
        icon: L.divIcon({
          className: '',
          html: '<div class="crew-leaflet-pulse is-met-post" data-type="met">'
              + '<div class="crew-leaflet-pulse-dot"></div></div>',
          iconSize: [28, 28],
          iconAnchor: [14, 14]
        })
      }).addTo(metPostModalState.map);
      metPostModalState.map.setView([editingPost.lat, editingPost.lng], 8);
    }

    if (metPostModalState.loaded) {
      // Map already created — clear any prior marker, then maybe drop the
      // saved one (edit mode).
      if (metPostModalState.marker) {
        metPostModalState.map.removeLayer(metPostModalState.marker);
        metPostModalState.marker = null;
      }
      setTimeout(function() {
        if (metPostModalState.map && metPostModalState.map.invalidateSize) {
          metPostModalState.map.invalidateSize();
        }
        maybeDropSavedPin();
      }, 60);
      return;
    }
    function buildMap() {
      var container = $('met-post-modal-map');
      var loadingEl = $('met-post-modal-map-loading');
      if (!container || !window.L) return;
      var m = L.map(container, {
        center: [39.8, -86.2],
        zoom: 7,
        scrollWheelZoom: true,
        zoomControl: true
      });
      L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors &copy; <a href="https://carto.com/">CARTO</a>',
        subdomains: 'abcd',
        maxZoom: 19
      }).addTo(m);
      m.on('click', function(ev) {
        var ll = ev.latlng;
        if (metPostModalState.marker) {
          m.removeLayer(metPostModalState.marker);
        }
        metPostModalState.marker = L.marker([ll.lat, ll.lng], {
          icon: L.divIcon({
            className: '',
            html: '<div class="crew-leaflet-pulse is-met-post" data-type="met">'
                + '<div class="crew-leaflet-pulse-dot"></div>'
                + '</div>',
            iconSize: [28, 28],
            iconAnchor: [14, 14]
          })
        }).addTo(m);
        metPostModalState.pinLatLng = { lat: ll.lat, lng: ll.lng };

        var status = $('met-post-modal-pin-status');
        if (status) {
          status.textContent = 'Pin set at ' + ll.lat.toFixed(3) + ', ' + ll.lng.toFixed(3)
            + ' \u00b7 click again to move it.';
          status.classList.add('is-pinned');
        }
        refreshSendButtonState();
      });
      metPostModalState.map = m;
      metPostModalState.loaded = true;
      if (loadingEl) loadingEl.classList.add('is-hidden');
      setTimeout(function() {
        m.invalidateSize();
        maybeDropSavedPin();
      }, 80);
    }

    if (window.L) {
      buildMap();
    } else {
      if (!document.querySelector('link[data-leaflet]')) {
        var link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
        link.setAttribute('data-leaflet', 'css');
        document.head.appendChild(link);
      }
      var s = document.createElement('script');
      s.src = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';
      s.async = true;
      s.onload = buildMap;
      s.onerror = function() {
        var loadingEl = $('met-post-modal-map-loading');
        if (loadingEl) loadingEl.innerHTML = '<span style="color:#C2342B;">Couldn\u2019t load the map.</span>';
      };
      document.head.appendChild(s);
    }
  }

  function setupMetPostModal() {
    if (window._metPostModalWired) return;
    window._metPostModalWired = true;
    var closeBtn = $('met-post-modal-close');
    var cancelBtn = $('met-post-modal-cancel');
    var scrim = $('met-post-modal-scrim');
    var sendBtn = $('met-post-modal-send');

    if (closeBtn)  closeBtn.addEventListener('click', closeMetPostModal);
    if (cancelBtn) cancelBtn.addEventListener('click', closeMetPostModal);
    if (scrim)     scrim.addEventListener('click', closeMetPostModal);
    document.addEventListener('keydown', function(ev) {
      var modal = $('met-post-modal');
      if (!modal || modal.hidden) return;
      if (ev.key === 'Escape') closeMetPostModal();
    });

    // Wire the schedule toggle buttons
    var toggleBtns = document.querySelectorAll('[data-schedule-mode]');
    toggleBtns.forEach(function(b) {
      b.addEventListener('click', function() {
        metPostModalState.scheduleMode = b.getAttribute('data-schedule-mode');
        syncScheduleToggleUI();
        // If switching to "later", make sure fields are populated
        if (metPostModalState.scheduleMode === 'later') {
          populateScheduleFields(null);
        }
        refreshSendButtonState();
      });
    });

    // Wire date/time inputs — revalidate on every change
    var dateInput = $('met-post-schedule-date');
    var timeInput = $('met-post-schedule-time');
    if (dateInput) dateInput.addEventListener('input', refreshSendButtonState);
    if (timeInput) timeInput.addEventListener('input', refreshSendButtonState);

    if (sendBtn) {
      sendBtn.addEventListener('click', function() {
        if (!metPostModalState.pinLatLng) return;
        if (!metPostModalState.pendingText) return;
        var text = metPostModalState.pendingText;
        var lat = metPostModalState.pinLatLng.lat;
        var lng = metPostModalState.pinLatLng.lng;

        if (metPostModalState.scheduleMode === 'later') {
          var sched = readScheduleFields();
          if (sched.error) {
            // Error already surfaced by refreshSendButtonState; just bail.
            return;
          }
          if (metPostModalState.editingId) {
            // Update existing scheduled post in place
            updateScheduledPost(metPostModalState.editingId, text, lat, lng, sched.iso);
            if (typeof showTipToast === 'function') {
              showTipToast('Scheduled post updated');
            }
          } else {
            savePostAsScheduled(text, lat, lng, sched.iso);
            if (typeof showTipToast === 'function') {
              showTipToast('Scheduled for ' + formatScheduledForLabel(sched.date));
            }
            // Phase 10 — also persist to the real backend so the scheduler
            // tick fires it. savePostAsScheduled() is frontend-only; this
            // makes the schedule actually durable across page reloads
            // and across server restarts.
            try {
              var fireMs = new Date(sched.iso).getTime();
              if (fireMs && fireMs > Date.now() + 30000) {
                var BACKEND = 'https://wv-valet-backend.onrender.com';
                fetch(BACKEND + '/api/v1/me/scheduled-messages', {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' },
                  credentials: 'include',
                  body: JSON.stringify({
                    type: 'crew_post',
                    scheduled_for_ms: fireMs,
                    scheduled_tz: Intl.DateTimeFormat().resolvedOptions().timeZone || 'America/New_York',
                    content_payload: {
                      body: text,
                      pin_lat: lat,
                      pin_lng: lng
                    }
                  })
                }).catch(function(){ /* silent — frontend queue still works */ });
              }
            } catch (e) { /* silent */ }
          }
        } else {
          // Send-now path. If editing an existing scheduled post and the
          // Met switched to "now," we save as live and cancel the
          // scheduled record (it's been superseded).
          if (metPostModalState.editingId) {
            cancelScheduledPost(metPostModalState.editingId);
          }
          savePostAsLive(text, lat, lng);
          if (typeof showTipToast === 'function') {
            showTipToast('Posted to Crew feed');
          }
        }

        // Reset composer if this was a brand-new post (not an edit)
        if (!metPostModalState.editingId) {
          var textarea = $('met-post-text');
          var hint = $('met-post-composer-hint');
          if (textarea) textarea.value = '';
          if (hint) {
            hint.textContent = metPostModalState.scheduleMode === 'later'
              ? '\u2713 Scheduled'
              : '\u2713 Sent to Crew feed';
            hint.classList.add('is-success');
          }
          var sBtn = $('met-post-send-btn');
          if (sBtn) sBtn.disabled = true;
        }

        // Clear editing state and close
        metPostModalState.editingId = null;
        closeMetPostModal();
      });
    }

    // Wire queue action buttons via delegation (queue re-renders, so
    // a static listener at document level survives re-renders).
    document.addEventListener('click', function(ev) {
      var editBtn = ev.target.closest('[data-queue-edit]');
      var cancelBtn = ev.target.closest('[data-queue-cancel]');
      if (editBtn) {
        ev.preventDefault();
        var editId = editBtn.getAttribute('data-queue-edit');
        var target = null;
        for (var i = 0; i < MET_POSTS.length; i++) {
          if (MET_POSTS[i].id === editId) { target = MET_POSTS[i]; break; }
        }
        if (target) openMetPostModalForEdit(target);
        return;
      }
      if (cancelBtn) {
        ev.preventDefault();
        var cancelId = cancelBtn.getAttribute('data-queue-cancel');
        openCancelConfirm(cancelId);
        return;
      }
    });

    // Wire the cancel-confirm dialog
    var ccKeep = $('met-queue-cancel-keep');
    var ccConfirm = $('met-queue-cancel-confirm');
    var ccScrim = $('met-queue-cancel-scrim');
    if (ccKeep) ccKeep.addEventListener('click', closeCancelConfirm);
    if (ccScrim) ccScrim.addEventListener('click', closeCancelConfirm);
    if (ccConfirm) ccConfirm.addEventListener('click', function() {
      var id = ccConfirm.getAttribute('data-confirm-id');
      if (id) {
        cancelScheduledPost(id);
        if (typeof showTipToast === 'function') {
          showTipToast('Scheduled post cancelled');
        }
      }
      closeCancelConfirm();
    });
  }

  function openCancelConfirm(id) {
    var modal = $('met-queue-cancel-modal');
    if (!modal) return;
    var target = null;
    for (var i = 0; i < MET_POSTS.length; i++) {
      if (MET_POSTS[i].id === id) { target = MET_POSTS[i]; break; }
    }
    if (!target) return;
    var preview = $('met-queue-cancel-preview');
    var meta = $('met-queue-cancel-meta');
    var confirm = $('met-queue-cancel-confirm');
    if (preview) preview.textContent = target.text;
    if (meta && target.scheduledFor) {
      try {
        var d = new Date(target.scheduledFor);
        meta.textContent = 'Was scheduled for ' + formatScheduledForLabel(d);
      } catch (e) { meta.textContent = ''; }
    }
    if (confirm) confirm.setAttribute('data-confirm-id', id);
    modal.hidden = false;
  }
  function closeCancelConfirm() {
    var modal = $('met-queue-cancel-modal');
    if (modal) modal.hidden = true;
  }

  function persistReports() {
    var mutations = {};
    var userReports = [];
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      var r = CREW_REPORTS[i];
      if (r.userSubmitted) {
        userReports.push(r);
      } else {
        // Only persist fields that may have mutated (verified count + tips)
        mutations[r.id] = { verified: r.verified || 0, tips: r.tips || {} };
      }
    }
    storage.set(K_REPORTS, { mutations: mutations, userReports: userReports });
  }

  function getReportById(id) {
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      if (CREW_REPORTS[i].id === id) return CREW_REPORTS[i];
    }
    return null;
  }

  // ── Crew evidence matcher ──────────────────────────────────────────────
  // Find Crew reports that are relevant to a given ticket. Returns an
  // object { reports, windowMinutes } where reports is an array of recent
  // matching reports, sorted newest-first.
  //
  // Match criteria:
  //   1. Same state as the ticket (the cleanest signal we have without
  //      lat/lng on every seed report)
  //   2. Within the last 30 minutes (timeMinutes < 30)
  //   3. Ticket is for TODAY (dateOffsetDays === 0). Future-dated tickets
  //      get no Crew evidence — Crew can't corroborate a Friday forecast
  //      from observations made on Wednesday.
  //   4. Excludes the user's own reports (you don't corroborate yourself)
  //
  // Returns up to 5 reports. The UI typically shows count summary plus a
  // few recent ones.
  function findNearbyCrewReports(ticket) {
    var WINDOW_MINUTES = 30;
    if (!ticket) return { reports: [], windowMinutes: WINDOW_MINUTES };
    if (ticket.dateOffsetDays && ticket.dateOffsetDays > 0) {
      // Future-dated ticket — Crew evidence isn't applicable.
      return { reports: [], windowMinutes: WINDOW_MINUTES, futureDate: true };
    }
    var ticketState = ticket.stateCode || ticket.resolvedState;
    if (!ticketState) return { reports: [], windowMinutes: WINDOW_MINUTES };
    // Normalize: state codes may be 2 letters; resolved state may be full
    // name (e.g. "Indiana"). Compare against US_STATES if available.
    var ticketStateCode = ticketState.length === 2
      ? ticketState.toUpperCase()
      : (typeof US_STATE_NAMES_TO_CODES === 'object'
          ? US_STATE_NAMES_TO_CODES[ticketState.toLowerCase()] || ticketState.toUpperCase()
          : ticketState.toUpperCase());

    var matches = [];
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      var r = CREW_REPORTS[i];
      if (isMyReport(r)) continue;
      if (typeof r.timeMinutes !== 'number' || r.timeMinutes >= WINDOW_MINUTES) continue;
      var rState = (r.state || '').toUpperCase();
      if (rState !== ticketStateCode) continue;
      matches.push(r);
    }
    // Sort newest first (smaller timeMinutes = more recent)
    matches.sort(function(a, b) { return a.timeMinutes - b.timeMinutes; });
    return {
      reports: matches.slice(0, 5),
      totalCount: matches.length,
      windowMinutes: WINDOW_MINUTES
    };
  }

  // Whether a report belongs to the currently signed-in user. Only true
  // for user-submitted reports (which carry an authorId set at submit
  // time). Seed reports never match — they don't have an authorId.
  // When signed-out, returns false (no concept of "mine" exists).
  function isMyReport(report) {
    if (!report) return false;
    var u = auth.currentUser();
    if (!u) return false;
    return report.authorId === u.id;
  }

  // Filter + sort reports for the current zoom level. Returns an object
  // with .pulses (all visible reports for the map) and .stories (the 10
  // most recent for the story list).
  var ZOOM_TIER_RANK = { city: 0, county: 1, region: 2, state: 3 };

  function getReportsForZoom(zoom) {
    // Phase 9 Push B: if the new Leaflet map is up and reporting a
    // viewport, the story list should reflect what's IN VIEW rather
    // than the legacy city/county/region/state buckets.
    var inViewport = null;
    try {
      if (window.WV_FeedMapAPI && window.WV_FeedMapAPI.ready &&
          window.WV_FeedMapAPI.ready() &&
          window.WV_FeedMapAPI.getReportsInViewport) {
        inViewport = window.WV_FeedMapAPI.getReportsInViewport();
      }
    } catch (e) { inViewport = null; }

    if (inViewport && inViewport.length >= 0) {
      // Viewport mode — return the in-view reports as both pulses and
      // (sorted) stories. The old `pulses` field is now mostly a no-op
      // for the new map (it manages its own markers), but renderFeed's
      // legacy SVG path expects the field; passing a populated array
      // keeps the stats strip consistent.
      var sortedViewport = inViewport.slice().sort(function(a, b) {
        var atA = a.submittedAt ? new Date(a.submittedAt).getTime() : 0;
        var atB = b.submittedAt ? new Date(b.submittedAt).getTime() : 0;
        return atB - atA;
      });
      return {
        pulses: inViewport,
        stories: sortedViewport.slice(0, 10)
      };
    }

    // Legacy SVG path (still used during the brief moment before the
    // map is ready, and as a fallback if WV_FeedMapAPI isn't available).
    // Use bracket access + fallback that handles zero correctly. Plain
    // `|| 2` would coerce city's tier (rank 0) to the region default
    // because 0 is falsy.
    var threshold = (zoom in ZOOM_TIER_RANK) ? ZOOM_TIER_RANK[zoom] : 2;
    var visible = CREW_REPORTS.filter(function(r) {
      return ZOOM_TIER_RANK[r.zoomTier] <= threshold;
    });

    // Met posts (Chunk 6c-1) appear at all zoom levels — they're
    // territory-wide commentary, not zoom-tier-scoped observations.
    // Each Met post is converted to a report-shaped object so the
    // existing render paths (story list, map pulses, ticker, on-air
    // rail) can consume them with minimal branching.
    if (typeof MET_POSTS !== 'undefined' && MET_POSTS.length) {
      for (var m = 0; m < MET_POSTS.length; m++) {
        var p = MET_POSTS[m];
        // Chunk 6c-2: only 'live' Met posts appear in the feed.
        if (p.status !== 'live') continue;
        // Convert lat/long to percent positions on the stylized SVG
        // using the same bbox the rest of the page uses.
        var pos = null;
        if (typeof NWS_MAP_BBOX !== 'undefined' && p.lat != null && p.lng != null) {
          var bbox = NWS_MAP_BBOX;
          var topPct  = ((bbox.maxLat - p.lat) / (bbox.maxLat - bbox.minLat)) * 100;
          var leftPct = ((p.lng - bbox.minLng) / (bbox.maxLng - bbox.minLng)) * 100;
          pos = { top: topPct.toFixed(2) + '%', left: leftPct.toFixed(2) + '%' };
        }
        var ageMin = 0;
        try {
          ageMin = Math.floor((Date.now() - new Date(p.submittedAt).getTime()) / 60000);
        } catch (e) { ageMin = 0; }
        visible.push({
          id: p.id,
          isMetPost: true,
          type: 'met-post',
          text: p.text,
          author: p.author,
          authorInitials: p.authorInitials,
          city: 'Coverage',
          county: '',
          submittedAt: p.submittedAt,
          timeMinutes: ageMin,
          verified: p.verified || 0,
          // Met posts use one position at every zoom level
          mapPosCity: pos, mapPosCounty: pos, mapPosRegion: pos, mapPosState: pos,
          zoomTier: 'city'  // visible at every tier
        });
      }
    }

    var sorted = visible.slice().sort(function(a, b) {
      return a.timeMinutes - b.timeMinutes;  // ascending = newest first
    });
    return {
      pulses: visible,
      stories: sorted.slice(0, 10)  // 10 most recent in the visible area
    };
  }

  function getReportPos(report, zoom) {
    if (zoom === 'city' && report.mapPosCity) return report.mapPosCity;
    if (zoom === 'county' && report.mapPosCounty) return report.mapPosCounty;
    if (zoom === 'state' && report.mapPosState) return report.mapPosState;
    return report.mapPosRegion || report.mapPos || { top: '50%', left: '50%' };
  }

  // ── Token ledger ─────────────────────────────────────────────────────────
  // Single source of truth for the user's balance. Every visible chip
  // (Profile member card, detail screen, anywhere else) reads via tokens.get()
  // and updates via tokens.earn(n) / tokens.spend(n). On every change, all
  // registered chip elements are updated so they stay in sync.
  // Token ledger — backed by the signed-in user's persistent state.
  // When signed out, returns 0 and refuses earn/spend (no anonymous tokens).
  // When signed in, every change persists immediately so the next page load
  // shows the same balance.
  var tokens = (function() {
    var subscribers = [];
    function getBalance() {
      var s = auth.currentState();
      return s ? (s.tokens || 0) : 0;
    }
    function notify() {
      var b = getBalance();
      for (var i = 0; i < subscribers.length; i++) {
        try { subscribers[i](b); } catch (e) {}
      }
    }
    return {
      get: getBalance,
      canAfford: function(n) { return getBalance() >= n; },
      earn: function(n) {
        var s = auth.currentState();
        if (!s) return 0;
        s.tokens = (s.tokens || 0) + n;
        auth.persist();
        notify();
        return s.tokens;
      },
      spend: function(n) {
        var s = auth.currentState();
        if (!s) return false;
        if ((s.tokens || 0) < n) return false;
        s.tokens -= n;
        auth.persist();
        notify();
        return true;
      },
      subscribe: function(fn) {
        subscribers.push(fn);
        try { fn(getBalance()); } catch (e) {}
      },
      // Called when auth state changes — re-notify all subscribers so
      // chips update from "0 (signed out)" to "N (signed in)".
      refresh: notify
    };
  })();

  // ── Per-user verification state ──────────────────────────────────────────
  // The Set of report IDs the current user has verified. Used to:
  //   - Disable the "I see it too" button on already-verified reports
  //   - Show the verified-state visual on those reports across screens
  // In production this is a server check; here it's session-only memory.
  var verifiedByMe = new Set();

  // Map of reportId → timestamp(ms) when the current user verified it.
  // Used to power the 5-minute un-verify window for mistaps. Survives
  // sessions via the auth state below.
  var verifiedByMeAt = {};

  // Window during which a user can un-verify (e.g. accidental tap).
  // After this, the verification is part of the historical record.
  var UNVERIFY_WINDOW_MS = 5 * 60 * 1000;

  function canUnverify(reportId) {
    if (!verifiedByMe.has(reportId)) return false;
    var t = verifiedByMeAt[reportId];
    if (!t) return false;
    return (Date.now() - t) <= UNVERIFY_WINDOW_MS;
  }

  // ── Backend sync for verifies (Phase 5b) ──────────────────────────
  // The legacy auth module stores verifies in localStorage. Phase 5b
  // adds backend persistence on top — same in-memory data structures,
  // but POST/DELETE fire alongside the local writes so verifies survive
  // device changes and reinstalls.
  //
  // Strategy: on sign-in or page-load with valid Render session, pull
  // the user's verifies from the backend and merge into the in-memory
  // Set + timestamps map. On verify/unverify, fire POST/DELETE in the
  // background. If the call fails, the local state already reflects
  // the change; next page load reconciles.

  function loadVerifiesFromBackend() {
    fetch('https://wv-valet-backend.onrender.com/api/v1/verifies', {
      credentials: 'include'
    })
    .then(function(r) {
      if (!r.ok) throw new Error('http-' + r.status);
      return r.json();
    })
    .then(function(j) {
      if (!j || !j.ok || !Array.isArray(j.verifies)) return;
      // Merge: keep existing in-memory entries AND add anything from
      // the backend the local set doesn't know about. The backend is
      // authoritative — if it has the verify, it's a verify, regardless
      // of what localStorage said.
      for (var i = 0; i < j.verifies.length; i++) {
        var v = j.verifies[i];
        verifiedByMe.add(v.report_id);
        // Backend stores ms-since-epoch; map straight in.
        verifiedByMeAt[v.report_id] = v.created_at;
      }
      // Re-render any visible feed so verified state shows on cards.
      try { if (typeof renderFeed === 'function') renderFeed(); } catch (e) {}
      // Persist the merged state back to localStorage cache.
      try { if (window.auth && window.auth.persist) window.auth.persist(); } catch (e) {}
    })
    .catch(function(err) {
      // Most common: user isn't signed in yet, normal on first load.
      console.log('[verifies] load failed (will use cache):', err.message);
    });
  }

  function backendVerifyReport(reportId) {
    // Fire-and-forget POST. Local state already updated by caller.
    fetch('https://wv-valet-backend.onrender.com/api/v1/verifies', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ report_id: reportId })
    })
    .then(function(r) {
      if (!r.ok) console.warn('[verifies] POST failed:', r.status);
    })
    .catch(function(err) {
      console.warn('[verifies] POST network error:', err);
    });
  }

  function backendUnverifyReport(reportId) {
    fetch('https://wv-valet-backend.onrender.com/api/v1/verifies/' +
          encodeURIComponent(reportId), {
      method: 'DELETE',
      credentials: 'include'
    })
    .then(function(r) {
      if (!r.ok) console.warn('[verifies] DELETE failed:', r.status);
    })
    .catch(function(err) {
      console.warn('[verifies] DELETE network error:', err);
    });
  }

  // ── Report aging ───────────────────────────────────────────────────
  // Reports age based on their submission time and their category.
  // Severe weather stays relevant longer; current-conditions reports
  // (sunny/cloudy/humid) go stale fast. Aging produces three stages:
  //   fresh  — full color, full size, top of mind
  //   fading — clearly aging (50%+ of the window)
  //   stale  — past the window; needs fresh eyes
  //
  // Aging windows are in MINUTES. Tunable as a single object.
  var AGING_WINDOWS = {
    severe: 60,    // storm, hail
    active: 30,    // rain, snow, wind, sleet, freezing, fog
    current: 15    // sunny, cloudy, humid, clearing, clear
  };
  var SEVERE_TYPES  = ['storm', 'hail'];
  var CURRENT_TYPES = ['sunny', 'cloudy', 'humid', 'clearing', 'clear'];

  function getReportSeverity(type) {
    if (SEVERE_TYPES.indexOf(type) !== -1) return 'severe';
    if (CURRENT_TYPES.indexOf(type) !== -1) return 'current';
    return 'active';
  }

  // Compute age and stage for a single report. Falls back to the static
  // `timeMinutes` field when there's no `submittedAt` timestamp (the
  // seeded prototype reports were written with timeMinutes only; we
  // backfill submittedAt at boot, but this guards anything we missed).
  function getReportAge(report) {
    var ageMinutes;
    if (report.submittedAt) {
      ageMinutes = (Date.now() - new Date(report.submittedAt).getTime()) / 60000;
    } else if (typeof report.timeMinutes === 'number') {
      ageMinutes = report.timeMinutes;
    } else {
      ageMinutes = 0;
    }
    ageMinutes = Math.max(0, ageMinutes);
    var severity = getReportSeverity(report.type);
    var windowMin = AGING_WINDOWS[severity] || 30;
    var stage;
    if (ageMinutes < windowMin * 0.5) stage = 'fresh';
    else if (ageMinutes < windowMin)  stage = 'fading';
    else                               stage = 'stale';
    return {
      ageMinutes: ageMinutes,
      severity: severity,
      windowMin: windowMin,
      stage: stage,
      isStale: stage === 'stale'
    };
  }

  // Human-friendly label for an age in minutes. Mirrors the existing
  // timeAgo strings but is computed live so reports age on screen
  // without a page reload.
  function formatAgeLabel(ageMinutes) {
    var m = Math.floor(ageMinutes);
    if (m < 1) return 'just now';
    if (m === 1) return '1 min ago';
    if (m < 60) return m + ' min ago';
    var h = Math.floor(m / 60);
    if (h === 1) return '1 hr ago';
    if (h < 24) return h + ' hr ago';
    var d = Math.floor(h / 24);
    if (d === 1) return 'yesterday';
    return d + ' days ago';
  }

  // Track which tip levels the user has given each report. Multiple tip
  // levels per report are allowed (a Nod and later a Handshake), but each
  // level can only be given once per report — prevents accidental double-tap
  // and keeps token economics from being trivially gameable in the demo.
  var tipsByMe = {};  // { reportId: Set('nod'|'coffee'|'handshake') }

  function hasTipped(reportId, level) {
    return tipsByMe[reportId] && tipsByMe[reportId].has(level);
  }
  function markTipped(reportId, level) {
    if (!tipsByMe[reportId]) tipsByMe[reportId] = new Set();
    tipsByMe[reportId].add(level);
  }

  // ── Auth state ───────────────────────────────────────────────────────────
  // Multi-account auth on a single device, backed by `identity` + storage.
  // The `auth` module is the public surface — verify/tip/report flows call
  // through here. Real backend swap-in is a one-module change.
  //
  // Session is restored on page load: if K_SESSION holds a valid user ID,
  // we sign that user in automatically. Sign-out clears the session key
  // and resets the in-memory state (but per-user persistent state stays
  // intact — sign back in later and your tokens are still there).
  var auth = (function() {
    var currentUser = null;
    var currentState = null;
    var subscribers = [];

    function notify() {
      for (var i = 0; i < subscribers.length; i++) {
        try { subscribers[i](currentUser); } catch (e) {}
      }
    }
    function loadSession() {
      var sessionUserId = storage.get(K_SESSION);
      if (!sessionUserId) return false;
      var user = identity.findById(sessionUserId);
      if (!user) {
        storage.remove(K_SESSION);  // stale session
        return false;
      }
      currentUser = user;
      currentState = loadUserState(user.id);
      // Restore per-user mutable state into the legacy in-memory holders
      // so the rest of the app (which already reads these) keeps working.
      verifiedByMe = new Set(currentState.verifiedByMe || []);
      verifiedByMeAt = currentState.verifiedByMeAt || {};
      tipsByMe = {};
      for (var rid in currentState.tipsByMe) {
        tipsByMe[rid] = new Set(currentState.tipsByMe[rid]);
      }
      return true;
    }

    return {
      isSignedIn: function() { return currentUser !== null; },
      currentUser: function() { return currentUser; },
      currentState: function() { return currentState; },
      // Sign in by email + password. Returns { ok, error? }.
      signInWithCredentials: function(email, password) {
        var user = identity.verify(email, password);
        if (!user) return { ok: false, error: 'Email or password didn\u2019t match. Try again or create an account.' };
        currentUser = user;
        currentState = loadUserState(user.id);
        verifiedByMe = new Set(currentState.verifiedByMe || []);
        verifiedByMeAt = currentState.verifiedByMeAt || {};
        tipsByMe = {};
        for (var rid in currentState.tipsByMe) {
          tipsByMe[rid] = new Set(currentState.tipsByMe[rid]);
        }
        storage.set(K_SESSION, user.id);
        notify();
        return { ok: true };
      },
      // Create + sign in. Returns { ok, error? }.
      signUp: function(input) {
        var result = identity.create(input);
        if (!result.ok) return result;
        currentUser = result.user;
        currentState = defaultUserState();
        saveUserState(result.user.id, currentState);
        verifiedByMe = new Set();
        verifiedByMeAt = {};
        tipsByMe = {};
        storage.set(K_SESSION, result.user.id);
        notify();
        return { ok: true };
      },
      signOut: function() {
        if (!currentUser) return;
        // Persist any pending state changes first
        if (currentState) saveUserState(currentUser.id, currentState);
        currentUser = null;
        currentState = null;
        verifiedByMe = new Set();
        verifiedByMeAt = {};
        tipsByMe = {};
        storage.remove(K_SESSION);
        notify();
      },
      // Persist current state (call after any in-memory mutation that
      // should survive a refresh — token earn/spend, verify, tip, report).
      persist: function() {
        if (!currentUser || !currentState) return;
        currentState.verifiedByMe = Array.from(verifiedByMe);
        currentState.verifiedByMeAt = verifiedByMeAt;
        currentState.tipsByMe = {};
        for (var rid in tipsByMe) {
          currentState.tipsByMe[rid] = Array.from(tipsByMe[rid]);
        }
        saveUserState(currentUser.id, currentState);
      },
      subscribe: function(fn) {
        subscribers.push(fn);
        try { fn(currentUser); } catch (e) {}
      },
      // Restore session at boot. Called once during init.
      restore: function() {
        if (loadSession()) notify();
      },
      // Update the signed-in user's home base. Persists to the user
      // record so severe alert geofencing reflects the change. In
      // production this will surface in a Settings panel; for now
      // it's exposed as a console hook for prototype testing:
      //   _updateMyLocation('Indianapolis, IN')
      //   _updateMyLocation('Lebanon, IN', { lat: 40.0489, lng: -86.4691 })
      updateLocation: function(city, coords) {
        if (!currentUser) return { ok: false, error: 'Not signed in' };
        var patch = {
          city: (city || '').trim()
        };
        if (coords && typeof coords.lat === 'number') {
          patch.lat = coords.lat;
          patch.lng = coords.lng;
          patch.locationSource = 'geolocation';
        } else {
          patch.lat = null;
          patch.lng = null;
          patch.locationSource = 'typed';
        }
        var result = identity.update(currentUser.id, patch);
        if (result.ok) {
          currentUser = result.user;
          notify();
        }
        return result;
      }
    };
  })();

  // Console hook for editing location during prototype testing.
  // Real settings UI is deferred to a future build.
  window._updateMyLocation = function(city, coords) {
    var result = auth.updateLocation(city, coords);
    if (result.ok) {
      console.log('Location updated:', result.user.city,
        result.user.lat ? '(' + result.user.lat + ', ' + result.user.lng + ')' : '(no coords)');
      if (typeof showTipToast === 'function') {
        showTipToast('Home base updated to ' + result.user.city);
      }
    } else {
      console.warn('Location update failed:', result.error);
    }
    return result;
  };

  // requireAuth(intent, callback): if signed in, run the callback. If not,
  // open the auth gate modal with a contextualized headline based on intent.
  // Stash the callback so the modal's "Continue" button can run it after
  // sign-in. Intent strings: 'verify', 'tip', 'report'.
  var pendingAuthAction = null;
  function requireAuth(intent, callback) {
    if (auth.isSignedIn()) {
      callback();
      return;
    }
    pendingAuthAction = callback;
    openAuthGate(intent);
  }

  function openAuthGate(intent) {
    var copy = {
      verify: {
        title: 'Sign in to verify reports',
        body: 'Verifying confirms a Crew member\u2019s observation and earns you tokens.'
      },
      tip: {
        title: 'Sign in to tip the Crew',
        body: 'Tipping sends tokens from your balance to thank a contributor.'
      },
      report: {
        title: 'Sign in to submit reports',
        body: 'Reporting weather you\u2019re seeing helps the rest of the Crew.'
      },
      profile: {
        title: 'Join the Valet Crew',
        body: 'Your member card, badges, and protected hours unlock once you\u2019re part of the Crew.'
      }
    };
    var c = copy[intent] || copy.report;
    var titleEl = $('auth-gate-title');
    var bodyEl = $('auth-gate-body');
    if (titleEl) titleEl.textContent = c.title;
    if (bodyEl) bodyEl.textContent = c.body;
    // Default tab: signup if intent is "profile" (they're new); signin otherwise
    var defaultTab = (intent === 'profile') ? 'signup' : 'signin';
    var tabs = document.querySelectorAll('.auth-tab');
    var panels = document.querySelectorAll('.auth-panel');
    for (var i = 0; i < tabs.length; i++) {
      var match = tabs[i].getAttribute('data-auth-tab') === defaultTab;
      tabs[i].classList.toggle('is-active', match);
      tabs[i].setAttribute('aria-selected', match ? 'true' : 'false');
    }
    for (var j = 0; j < panels.length; j++) {
      panels[j].classList.toggle('is-active',
        panels[j].id === 'auth-' + defaultTab + '-form');
    }
    var scrim = $('auth-gate-scrim');
    var card = $('auth-gate-card');
    if (scrim) scrim.classList.add('active');
    if (card) card.classList.add('active');
    // Focus the first input for keyboard users
    setTimeout(function() {
      var visible = document.querySelector('.auth-panel.is-active .auth-input');
      if (visible) visible.focus();
    }, 360);
  }

  function closeAuthGate() {
    var scrim = $('auth-gate-scrim');
    var card = $('auth-gate-card');
    if (scrim) scrim.classList.remove('active');
    if (card) card.classList.remove('active');
    pendingAuthAction = null;
  }

  // ── Reusable mockup photo SVGs ───────────────────────────────────────────
  // The Crew layer uses stylized SVG illustrations as photo stand-ins until
  // real photo upload is wired. Each style maps to a report type. Returned
  // as raw SVG markup so callers can drop into innerHTML — the SVG is sized
  // to fill its container (preserveAspectRatio="xMidYMid slice"). Same
  // illustrations the story cards already use, hoisted into a function so
  // the detail screen can reuse them at any size.
  function crewPhotoSvg(style) {
    if (style === 'storm') {
      return '<svg viewBox="0 0 80 80" preserveAspectRatio="xMidYMid slice" xmlns="http://www.w3.org/2000/svg">'
        + '<defs><linearGradient id="d-storm-sky" x1="0" y1="0" x2="0" y2="1">'
        + '<stop offset="0%" stop-color="#3a4856"/><stop offset="100%" stop-color="#1f2730"/></linearGradient></defs>'
        + '<rect width="80" height="80" fill="url(#d-storm-sky)"/>'
        + '<ellipse cx="20" cy="22" rx="22" ry="9" fill="#5a6878" opacity="0.85"/>'
        + '<ellipse cx="55" cy="30" rx="20" ry="8" fill="#4a5868" opacity="0.85"/>'
        + '<ellipse cx="36" cy="38" rx="26" ry="8" fill="#404e5e" opacity="0.85"/>'
        + '<path d="M48 32 L42 50 L48 50 L40 70 L52 46 L46 46 L52 32 Z" fill="#fce570" stroke="#fff7c2" stroke-width="0.6" opacity="0.95"/>'
        + '<line x1="15" y1="55" x2="13" y2="65" stroke="#7a8898" stroke-width="0.6" opacity="0.6"/>'
        + '<line x1="25" y1="60" x2="23" y2="70" stroke="#7a8898" stroke-width="0.6" opacity="0.6"/>'
        + '<line x1="65" y1="55" x2="63" y2="65" stroke="#7a8898" stroke-width="0.6" opacity="0.6"/>'
        + '<line x1="72" y1="60" x2="70" y2="70" stroke="#7a8898" stroke-width="0.6" opacity="0.6"/>'
        + '</svg>';
    }
    if (style === 'hail') {
      return '<svg viewBox="0 0 80 80" preserveAspectRatio="xMidYMid slice" xmlns="http://www.w3.org/2000/svg">'
        + '<defs><linearGradient id="d-hail-bg" x1="0" y1="0" x2="0" y2="1">'
        + '<stop offset="0%" stop-color="#a8b5c4"/><stop offset="55%" stop-color="#8e9eaf"/>'
        + '<stop offset="56%" stop-color="#a87a4f"/><stop offset="100%" stop-color="#8a623e"/></linearGradient></defs>'
        + '<rect width="80" height="80" fill="url(#d-hail-bg)"/>'
        + '<line x1="0" y1="58" x2="80" y2="58" stroke="#6f4e30" stroke-width="0.4" opacity="0.6"/>'
        + '<line x1="0" y1="68" x2="80" y2="68" stroke="#6f4e30" stroke-width="0.4" opacity="0.6"/>'
        + '<circle cx="14" cy="64" r="2.4" fill="#fff" opacity="0.95"/><circle cx="14" cy="64" r="1" fill="#dfe9f3" opacity="0.6"/>'
        + '<circle cx="28" cy="70" r="2" fill="#fff" opacity="0.92"/>'
        + '<circle cx="42" cy="62" r="2.6" fill="#fff" opacity="0.95"/><circle cx="42" cy="62" r="1.1" fill="#dfe9f3" opacity="0.6"/>'
        + '<circle cx="56" cy="70" r="2.2" fill="#fff" opacity="0.92"/><circle cx="68" cy="63" r="2" fill="#fff" opacity="0.94"/>'
        + '<circle cx="22" cy="74" r="1.8" fill="#fff" opacity="0.88"/><circle cx="50" cy="74" r="2" fill="#fff" opacity="0.92"/>'
        + '<circle cx="62" cy="76" r="1.6" fill="#fff" opacity="0.86"/>'
        + '<line x1="20" y1="20" x2="19" y2="28" stroke="#fff" stroke-width="1.4" opacity="0.7" stroke-linecap="round"/>'
        + '<line x1="36" y1="14" x2="35" y2="22" stroke="#fff" stroke-width="1.4" opacity="0.7" stroke-linecap="round"/>'
        + '<line x1="52" y1="22" x2="51" y2="30" stroke="#fff" stroke-width="1.4" opacity="0.7" stroke-linecap="round"/>'
        + '<line x1="64" y1="16" x2="63" y2="24" stroke="#fff" stroke-width="1.4" opacity="0.7" stroke-linecap="round"/>'
        + '</svg>';
    }
    if (style === 'clear') {
      return '<svg viewBox="0 0 80 80" preserveAspectRatio="xMidYMid slice" xmlns="http://www.w3.org/2000/svg">'
        + '<defs><linearGradient id="d-clear-sky" x1="0" y1="0" x2="0" y2="1">'
        + '<stop offset="0%" stop-color="#7eb6e5"/><stop offset="60%" stop-color="#a8d2ed"/>'
        + '<stop offset="61%" stop-color="#7da64f"/><stop offset="100%" stop-color="#5e8a3a"/></linearGradient>'
        + '<radialGradient id="d-sun-glow" cx="65%" cy="22%">'
        + '<stop offset="0%" stop-color="#fff7c2" stop-opacity="0.95"/>'
        + '<stop offset="100%" stop-color="#fff7c2" stop-opacity="0"/></radialGradient></defs>'
        + '<rect width="80" height="80" fill="url(#d-clear-sky)"/>'
        + '<rect width="80" height="80" fill="url(#d-sun-glow)"/>'
        + '<circle cx="58" cy="20" r="7" fill="#fff7c2"/><circle cx="58" cy="20" r="4.5" fill="#fde47a"/>'
        + '<ellipse cx="20" cy="24" rx="11" ry="3.5" fill="#fff" opacity="0.88"/>'
        + '<ellipse cx="34" cy="36" rx="9" ry="2.8" fill="#fff" opacity="0.78"/>'
        + '<ellipse cx="14" cy="50" rx="6" ry="5" fill="#456a30"/><ellipse cx="22" cy="49" rx="4" ry="4" fill="#456a30"/>'
        + '<ellipse cx="68" cy="51" rx="7" ry="5" fill="#456a30"/>'
        + '</svg>';
    }
    return '';
  }

  // ── Crew Detail screen rendering ─────────────────────────────────────────
  // Builds the detail-body innerHTML from a report record. Three sections:
  //   1. Photo (or no-photo placeholder) with type tag pill
  //   2. Attribution row (avatar + name + city · timeAgo)
  //   3. Report text (Fraunces serif for the focused single-report view)
  //   4. Verify button + count + helpful copy
  //   5. Tipping menu (3 cards: Nod / Coffee / Handshake)
  //
  // State drives the button states:
  //   - isMyReport(report) → verify is disabled (you can't verify your own report)
  //   - verifiedByMe.has(id) → verify shows "Verified ✓" green state
  //   - hasTipped(id, level) → that tip card shows the green "given" state
  //   - !tokens.canAfford(cost) → tip card disabled
  function renderReportDetail(reportId) {
    var report = getReportById(reportId);
    if (!report) {
      $('detail-body').innerHTML = '<p style="text-align:center;padding:60px 20px;color:rgba(15,17,22,0.55)">Report not found.</p>';
      return;
    }

    var typeLabels = {
      storm: 'Storm', hail: 'Hail', wind: 'Wind',
      rain: 'Rain', clear: 'Clear'
    };
    var typeLabel = typeLabels[report.type] || report.type;

    // Photo block — three cases, in priority order:
    //   1. User-submitted photo with a stored data URL → render <img>
    //   2. Seed report with hasPhoto + photoStyle → SVG mockup
    //   3. No photo → text-only placeholder
    var photoHtml;
    if (report.photoDataUrl) {
      photoHtml = ''
        + '<div class="detail-photo">'
        +   '<img src="' + escapeHtml(report.photoDataUrl) + '" alt="Crew photo" '
        +        'style="width:100%;height:100%;object-fit:cover;display:block">'
        +   '<div class="detail-type-tag type-' + report.type + '">'
        +     '<span class="detail-type-dot"></span>'
        +     escapeHtml(typeLabel)
        +   '</div>'
        + '</div>';
    } else if (report.hasPhoto && report.photoStyle) {
      photoHtml = ''
        + '<div class="detail-photo">'
        +   crewPhotoSvg(report.photoStyle)
        +   '<div class="detail-type-tag type-' + report.type + '">'
        +     '<span class="detail-type-dot"></span>'
        +     escapeHtml(typeLabel)
        +   '</div>'
        + '</div>';
    } else {
      photoHtml = ''
        + '<div class="detail-photo no-photo">'
        +   '<div class="detail-type-tag type-' + report.type + '">'
        +     '<span class="detail-type-dot"></span>'
        +     escapeHtml(typeLabel)
        +   '</div>'
        +   '<span class="detail-no-photo-text">Text report &middot; no photo</span>'
        + '</div>';
    }

    // Avatar — first letter of authorFull as initial
    var initial = (report.authorFull || report.author || 'U').replace(/^@/, '').charAt(0).toUpperCase();

    // Attribution row
    var attrHtml = ''
      + '<div class="detail-attr">'
      +   '<div class="detail-avatar">' + escapeHtml(initial) + '</div>'
      +   '<div class="detail-attr-text">'
      +     '<p class="detail-attr-name">' + escapeHtml(report.authorFull) + ' <span style="color:rgba(15,17,22,0.55);font-weight:400">' + escapeHtml(report.author) + '</span></p>'
      +     '<p class="detail-attr-meta">' + escapeHtml(report.city) + ' &middot; ' + escapeHtml(report.timeAgo) + '</p>'
      +   '</div>'
      + '</div>';

    var textHtml = '<p class="detail-text">' + escapeHtml(report.text) + '</p>';

    // Verify button — three states: mine / already-verified / fresh
    var verifyState, verifyLabel, verifyHint;
    if (isMyReport(report)) {
      verifyState = 'is-mine';
      verifyLabel = 'This is your report';
      verifyHint = 'You can\'t verify your own report — but it\'s on the feed for the rest of the Crew.';
    } else if (verifiedByMe.has(report.id)) {
      verifyState = 'is-verified';
      verifyLabel = 'Verified &check;';
      verifyHint = 'Thanks for confirming. The report is now stronger because of you.';
    } else {
      verifyState = '';
      verifyLabel = 'I see it too';
      verifyHint = 'Tap if you\'re nearby and seeing the same conditions. You\'ll earn +2 tokens for verifying.';
    }

    var verifyHtml = ''
      + '<div class="detail-verify-section">'
      +   '<div class="detail-verify-row">'
      +     '<button type="button" class="detail-verify-btn ' + verifyState + '" '
      +            'data-verify-id="' + escapeHtml(report.id) + '" '
      +            (isMyReport(report) || verifiedByMe.has(report.id) ? 'disabled' : '')
      +            '>'
      +       (verifyState === 'is-verified'
        ? '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>'
        : '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>')
      +       '<span>' + verifyLabel + '</span>'
      +     '</button>'
      +     '<div class="detail-verify-count">'
      +       '<div class="detail-verify-count-num" id="detail-verify-count-num">' + report.verified + '</div>'
      +       '<div class="detail-verify-count-label">Verified</div>'
      +     '</div>'
      +   '</div>'
      +   '<p class="detail-verify-hint">' + escapeHtml(verifyHint) + '</p>'
      + '</div>';

    // Tipping menu — three cards. Disabled if: this is the user's own report,
    // they can't afford the cost, or they've already given that tip level.
    function tipCardHtml(level, name, displayLabel, emoji) {
      // The internal "cost" (which drives availability and one-per-report
      // rules) is mapped from level → cost here, so the function signature
      // can present a display label like "free" instead of a token count.
      // The tokens system is preserved underneath; the customer-visible
      // label is just clearer copy.
      var costByLevel = { nod: 1, coffee: 5, handshake: 10 };
      var cost = costByLevel[level] || 1;
      var alreadyTipped = hasTipped(report.id, level);
      var canAfford = tokens.canAfford(cost);
      var ownReport = isMyReport(report);
      var disabled = ownReport || (!canAfford && !alreadyTipped);
      var cls = 'tip-btn' + (alreadyTipped ? ' is-given' : '');
      return ''
        + '<button type="button" class="' + cls + '" '
        +        'data-tip-id="' + escapeHtml(report.id) + '" '
        +        'data-tip-level="' + level + '" '
        +        'data-tip-cost="' + cost + '" '
        +        (disabled || alreadyTipped ? 'disabled' : '')
        +        '>'
        +   '<span class="tip-icon">' + emoji + '</span>'
        +   '<span class="tip-name">' + escapeHtml(name) + '</span>'
        +   '<span class="tip-cost">' + escapeHtml(displayLabel) + '</span>'
        + '</button>';
    }

    var tipHelpHtml;
    if (isMyReport(report)) {
      tipHelpHtml = 'You can\'t thank your own report &mdash; but the rest of the Crew can.';
    } else {
      tipHelpHtml = 'Send a free recognition tap to thank the contributor &mdash; or send a real tip that goes to them directly.';
    }

    var tipHtml = ''
      + '<div class="detail-tip-section">'
      +   '<div class="detail-tip-label">'
      +     '<span>Thank the contributor</span>'
      +     '<button type="button" class="detail-tip-help" id="detail-tip-help-btn" aria-label="What are these?">'
      +       '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
      +         '<circle cx="12" cy="12" r="10"/>'
      +         '<path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/>'
      +         '<line x1="12" y1="17" x2="12.01" y2="17"/>'
      +       '</svg>'
      +     '</button>'
      +   '</div>'
      +   '<div class="detail-tip-help-popover" id="detail-tip-help-popover">'
      +     '<p><strong>Recognition gestures (free):</strong> The Nod, Buy a Coffee, and The Handshake are free ways to thank a Crew contributor. They\u2019re social signals \u2014 the contributor sees who appreciated their work, building their standing in the Crew network. No money changes hands.</p>'
      +     '<p><strong>Real tips (coming soon):</strong> When real-money tipping launches, you\u2019ll be able to send actual dollars directly to the contributor. Real tips help compensate Crew members for valuable ground-truth reports that saved your day.</p>'
      +   '</div>'
      +   '<p class="detail-tip-sub">' + tipHelpHtml + '</p>'
      +   '<div class="detail-tip-grid">'
      +     tipCardHtml('nod', 'The Nod', 'free', '👋')
      +     tipCardHtml('coffee', 'Buy a Coffee', 'free', '☕')
      +     tipCardHtml('handshake', 'The Handshake', 'free', '🤝')
      +   '</div>'
      +   '<div class="detail-tip-real-wrap">'
      +     '<button type="button" class="detail-tip-real-btn" disabled aria-disabled="true" title="Real tipping arrives when payments are wired up">'
      +       '<svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">'
      +         '<line x1="12" y1="1" x2="12" y2="23"/>'
      +         '<path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>'
      +       '</svg>'
      +       '<span>Send a real tip</span>'
      +       '<span class="detail-tip-real-soon">Coming soon</span>'
      +     '</button>'
      +     '<p class="detail-tip-real-sub">Real money tips go directly to the Crew contributor, minus payment processing.</p>'
      +   '</div>'
      + '</div>';

    $('detail-body').innerHTML = photoHtml + attrHtml + textHtml + verifyHtml + tipHtml;

    // Wire the help popover toggle. Lives here (post-render) so it
    // re-attaches every time the detail view rebuilds.
    var helpBtn = document.getElementById('detail-tip-help-btn');
    var helpPop = document.getElementById('detail-tip-help-popover');
    if (helpBtn && helpPop) {
      helpBtn.addEventListener('click', function(ev) {
        ev.preventDefault();
        helpPop.classList.toggle('is-open');
      });
    }
  }

  // Navigation: open the Detail screen for a specific report
  function openReportDetail(reportId) {
    renderReportDetail(reportId);
    showScreen('screen-crew-detail');
  }

  // Show the tip-toast for ~2.4s then auto-hide. Reusable for both verify
  // confirmations and tip confirmations.
  function showTipToast(text) {
    var toast = $('tip-toast');
    var textEl = $('tip-toast-text');
    if (!toast || !textEl) return;
    textEl.textContent = text;
    toast.classList.add('active');
    clearTimeout(showTipToast._timer);
    showTipToast._timer = setTimeout(function() {
      toast.classList.remove('active');
    }, 2400);
  }

  // ── Apply signed-in user to the Profile member card ───────────────────
  // The member-card markup is static HTML; we update its dynamic fields
  // (name, handle, city, rank, tokens, stat values) to reflect the
  // currently signed-in user's data. Called from auth.subscribe whenever
  // the user changes.
  function applySignedInProfile(user) {
    if (!user) return;
    var state = auth.currentState() || {};

    // Name + handle + city
    var nameEl = document.querySelector('.member-name');
    if (nameEl) nameEl.textContent = user.name;
    var handleEl = document.querySelector('.member-handle');
    if (handleEl) handleEl.textContent = '@' + user.handle + ' \u00b7 ' + (user.city || 'Boone County, IN');

    // Rank label — promoted automatically based on protected hours.
    var rankLabel = computeRank(state.protectedHours || 0);
    var rankTier = document.querySelector('.member-rank-tier');
    if (rankTier) rankTier.textContent = rankLabel.name;
    var rankIcon = document.querySelector('.member-rank-icon');
    if (rankIcon) rankIcon.textContent = rankLabel.numeral;
    var rankProgress = document.querySelector('.member-rank-progress');
    if (rankProgress) {
      rankProgress.textContent = rankLabel.toNext > 0
        ? rankLabel.toNext + ' protected hours to ' + rankLabel.next
        : 'Top of the ranks \u2014 keep contributing!';
    }

    // Stat values — Protected Hours + Clarity Streak
    var statValues = document.querySelectorAll('.crew-stat-value');
    if (statValues[0]) {
      // Protected Hours: replace number leaving "hrs" unit span
      statValues[0].innerHTML = (state.protectedHours || 0)
        + '<span class="crew-stat-unit">hrs</span>';
    }
    var streakValue = document.querySelector('.crew-stat.streak .crew-stat-value');
    if (streakValue) {
      // Streak: number + flame + "in a row"
      streakValue.innerHTML = '<span>' + (state.streak || 0) + '</span>'
        + '<span class="crew-stat-flame" aria-hidden="true">\ud83d\udd25</span>'
        + '<span class="crew-stat-unit">in a row</span>';
    }
    // Stat sub-lines
    var protSub = document.querySelector('.crew-stat:not(.streak) .crew-stat-sub');
    if (protSub) {
      protSub.innerHTML = 'Across <strong>' + (state.reportsCount || 0) + ' report'
        + ((state.reportsCount === 1) ? '' : 's') + '</strong> this season.';
    }
    var protMilestone = document.querySelector('.crew-stat:not(.streak) .crew-stat-milestone');
    if (protMilestone) {
      protMilestone.textContent = rankLabel.toNext > 0
        ? rankLabel.toNext + ' hrs to ' + rankLabel.next
        : 'Top rank reached';
    }
    var streakMilestone = document.querySelector('.crew-stat.streak .crew-stat-milestone');
    if (streakMilestone) {
      var streakNeeded = Math.max(0, 10 - (state.streak || 0));
      streakMilestone.textContent = streakNeeded > 0
        ? streakNeeded + ' more for Clarity 10 badge'
        : 'Clarity 10 unlocked!';
    }

    // Progress bars
    var protProgress = document.querySelector('.crew-stat:not(.streak) .crew-stat-progress-fill');
    if (protProgress) {
      var progressPct = rankLabel.next
        ? Math.min(100, ((state.protectedHours || 0) / rankLabel.nextThreshold) * 100)
        : 100;
      protProgress.style.width = progressPct + '%';
    }
    var streakProgress = document.querySelector('.crew-stat.streak .crew-stat-progress-fill');
    if (streakProgress) {
      streakProgress.style.width = Math.min(100, ((state.streak || 0) / 10) * 100) + '%';
    }

    // Eyebrow: "Member since {year}" — derived from joined timestamp
    var eyebrowText = $('member-eyebrow-text');
    if (eyebrowText) {
      var joinYear = '';
      try {
        if (user.joined) joinYear = ' \u00b7 Member since ' + new Date(user.joined).getFullYear();
      } catch (e) {}
      eyebrowText.textContent = 'Valet Crew' + joinYear;
    }

    // ── Bio strip (Chunk 5a) ─────────────────────────────
    // Season: hardcoded — we're literally Season 1 of WeatherValet.
    // Member since: short month-and-year from user.joined.
    // Days active: now minus joined date, floored.
    // Specialty: dominant report category from this user's own
    //   reports, mapped to a Survivor-cast-bio-style phrase.
    var seasonEl = $('member-bio-season');
    if (seasonEl) seasonEl.textContent = '1';

    var sinceEl = $('member-bio-since');
    if (sinceEl) {
      try {
        if (user.joined) {
          var jd = new Date(user.joined);
          sinceEl.textContent = jd.toLocaleString('en-US', { month: 'short', year: 'numeric' });
        } else {
          sinceEl.textContent = 'May 2026';
        }
      } catch (e) { sinceEl.textContent = 'May 2026'; }
    }

    var daysEl = $('member-bio-days');
    if (daysEl) {
      var days = 0;
      try {
        if (user.joined) {
          days = Math.max(0, Math.floor((Date.now() - new Date(user.joined).getTime()) / 86400000));
        }
      } catch (e) { days = 0; }
      daysEl.textContent = (days === 0 ? 'Day one' : days + (days === 1 ? ' day' : ' days'));
    }

    var specEl = $('member-bio-specialty');
    if (specEl) specEl.textContent = computeCrewSpecialty(user, state);

    // ── Receipt row (Chunk 5b) ───────────────────────────
    // Three tiles below the journey stats. Reports = total
    // submitted. Verifies = sum of `verified` across reports
    // authored by this user (verifications RECEIVED, not given).
    // Cited = count of CREW_RECEIPTS with a citation field set.
    var receiptReports = $('receipt-reports');
    var receiptVerifies = $('receipt-verifies');
    var receiptCited = $('receipt-cited');

    var reportsTotal = state.reportsCount || 0;
    var verifiesReceived = 0;
    if (typeof CREW_REPORTS !== 'undefined') {
      for (var ri = 0; ri < CREW_REPORTS.length; ri++) {
        var rep = CREW_REPORTS[ri];
        // Match by authorId (set on user-submitted reports) OR by handle
        // ("@reilly_b" pattern used in seeded reports). This mirrors the
        // same author-match logic used in computeCrewSpecialty above.
        var isMine = false;
        if (rep.authorId && user && rep.authorId === user.id) isMine = true;
        else if (user && rep.author === '@' + (user.handle || '')) isMine = true;
        if (isMine) verifiesReceived += (rep.verified || 0);
      }
    }
    var citedCount = 0;
    if (typeof CREW_RECEIPTS !== 'undefined') {
      for (var ci = 0; ci < CREW_RECEIPTS.length; ci++) {
        if (CREW_RECEIPTS[ci].citation) citedCount += 1;
      }
    }

    if (receiptReports)  receiptReports.textContent  = formatReceiptNumber(reportsTotal);
    if (receiptVerifies) receiptVerifies.textContent = formatReceiptNumber(verifiesReceived);
    if (receiptCited)    receiptCited.textContent    = formatReceiptNumber(citedCount);

    // Render Season Highlights (Chunk 5c) — derived moments from real
    // data, with quality gates so weak numbers don't get a card.
    renderSeasonHighlights(user);

    // Render Your recent reports (Chunk 5d) — last 10 of this user's
    // own reports. Mirror of the Live Feed side list but filtered.
    renderProfileRecent(user);

    // Render the field notebook (moved from Workspace in Chunk 5d).
    // renderCrewNotebook now targets the Profile container.
    if (typeof renderCrewNotebook === 'function') renderCrewNotebook();

    // Render the badges grid based on current criteria + earned set
    renderBadges();
  }

  // Format a receipt number — short and compact. Past 999, switch to
  // "1.2k" / "12k" / "123k" to keep tile widths consistent and the
  // Fraunces 26px from wrapping or shrinking awkwardly.
  function formatReceiptNumber(n) {
    n = Math.max(0, Math.floor(n));
    if (n < 1000) return String(n);
    if (n < 10000) return (n / 1000).toFixed(1).replace(/\.0$/, '') + 'k';
    if (n < 1000000) return Math.floor(n / 1000) + 'k';
    return (n / 1000000).toFixed(1).replace(/\.0$/, '') + 'M';
  }

  // Shared author-match helper. A report belongs to `user` if its
  // authorId matches the user's id (set on user-submitted reports)
  // or its `author` field matches "@<handle>" (used in seeded reports
  // for the test account). Used by specialty derivation, receipt row,
  // and Season Highlights.
  function reportBelongsTo(report, user) {
    if (!report || !user) return false;
    if (report.authorId && report.authorId === user.id) return true;
    if (report.author && report.author === '@' + (user.handle || '')) return true;
    return false;
  }

  // ── Season Highlights helpers (Chunk 5c) ─────────────────────────────
  // Each finder returns either a "highlight" object (kind, headline, meta,
  // optional quote) or null when the underlying data doesn't pass the
  // quality gate. The renderer takes whatever passes and shows it; if
  // nothing passes, the fallback card is shown instead.
  //
  // Quality gates are deliberate: a "highlight" with weak numbers
  // (1-2 verifications) would feel like a participation trophy. The
  // gates ensure every card represents an actual moment of recognition.

  var HIGHLIGHT_MIN_VERIFIES        = 3;   // gate for most-verified card
  var HIGHLIGHT_PHOTO_MIN_VERIFIES  = 2;   // gate for photo card

  // Sentence-cased category label for a report type.
  function highlightTypeLabel(type) {
    var map = {
      storm: 'storm', hail: 'hail', wind: 'wind', rain: 'rain',
      snow: 'snow', sleet: 'sleet', freezing: 'freezing-rain',
      fog: 'fog', sunny: 'sunny', cloudy: 'cloudy', humid: 'humid',
      clearing: 'clearing', clear: 'clear'
    };
    return map[type] || type || 'weather';
  }

  // Format a report's submission timestamp into something human:
  // "May 11" or "today" or "yesterday".
  function highlightDateLabel(report) {
    var d;
    if (report.submittedAt) {
      d = new Date(report.submittedAt);
    } else if (typeof report.timeMinutes === 'number') {
      d = new Date(Date.now() - report.timeMinutes * 60000);
    } else {
      return '';
    }
    if (isNaN(d.getTime())) return '';
    var now = new Date();
    var sameDay = d.toDateString() === now.toDateString();
    if (sameDay) return 'today';
    var yesterday = new Date(now);
    yesterday.setDate(yesterday.getDate() - 1);
    if (d.toDateString() === yesterday.toDateString()) return 'yesterday';
    return d.toLocaleString('en-US', { month: 'short', day: 'numeric' });
  }

  // Find the user's most-verified report. Gated: must have >= 3 verifies
  // to qualify (below that, "most verified" isn't a real moment).
  function findMostVerifiedHighlight(user) {
    if (typeof CREW_REPORTS === 'undefined') return null;
    var best = null;
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      var r = CREW_REPORTS[i];
      if (!reportBelongsTo(r, user)) continue;
      var v = r.verified || 0;
      if (v < HIGHLIGHT_MIN_VERIFIES) continue;
      if (!best || v > (best.verified || 0)) best = r;
    }
    if (!best) return null;
    var typeLabel = highlightTypeLabel(best.type);
    var headline = 'The ' + typeLabel + ' call that ' + best.verified + ' Crew confirmed.';
    var meta = [highlightDateLabel(best), best.city || ''].filter(Boolean).join(' \u00b7 ');
    return {
      kind: 'verified',
      eyebrow: 'Most verified report',
      headline: headline,
      meta: meta,
      quote: best.text || ''
    };
  }

  // Find the user's first Met citation. Sorted by receipt ID descending
  // in the seed data (newest first), so we reverse to find the earliest.
  // Quality gate: must have a citation object with a Met name.
  function findFirstCitationHighlight(user) {
    if (typeof CREW_RECEIPTS === 'undefined' || !CREW_RECEIPTS.length) return null;
    // Receipts in the seed are ordered newest-first by id (4471, 4423,
    // 4318, 4287). The earliest is the one at the end with a citation.
    var ordered = CREW_RECEIPTS.slice().reverse();
    var first = null;
    for (var i = 0; i < ordered.length; i++) {
      var rec = ordered[i];
      if (rec.citation && rec.citation.metName) {
        first = rec;
        break;
      }
    }
    if (!first) return null;
    var headline = 'The day ' + first.citation.metName + ' used your reading.';
    // The receipt's `meta` line has the date and mission context already
    // formatted; strip the mission part for the highlight's meta line.
    var metaLine = (first.meta || '').split('\u00b7')[0].trim();
    if (!metaLine) metaLine = 'Mission #' + first.id;
    return {
      kind: 'cited',
      eyebrow: 'First Met citation',
      headline: headline,
      meta: metaLine,
      // The receipt's headline includes HTML <strong> tags — strip them
      // for the plain-text quote so we don't double-render or break clamp.
      quote: (first.headline || '').replace(/<[^>]+>/g, '')
    };
  }

  // Find the user's best photo report. Gated: must have a photo AND >= 2
  // verifications. A photo nobody confirmed isn't a highlight, it's just
  // a picture.
  function findPhotoHighlight(user) {
    if (typeof CREW_REPORTS === 'undefined') return null;
    var best = null;
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      var r = CREW_REPORTS[i];
      if (!reportBelongsTo(r, user)) continue;
      var hasPhoto = !!(r.photoDataUrl || (r.hasPhoto && r.photoStyle));
      if (!hasPhoto) continue;
      var v = r.verified || 0;
      if (v < HIGHLIGHT_PHOTO_MIN_VERIFIES) continue;
      if (!best || v > (best.verified || 0)) best = r;
    }
    if (!best) return null;
    var headline = 'The photo that put ' + (best.city || 'your corner') + ' on the map.';
    var meta = [highlightDateLabel(best), best.verified + ' Crew confirmed'].filter(Boolean).join(' \u00b7 ');
    return {
      kind: 'photo',
      eyebrow: 'Photo highlight',
      headline: headline,
      meta: meta,
      quote: best.text || ''
    };
  }

  // Build and render the Season Highlights grid for the signed-in user.
  function renderSeasonHighlights(user) {
    var section = $('season-highlights');
    var grid = $('season-highlights-grid');
    if (!section || !grid) return;

    // Always show the section — it's part of the Profile's identity.
    // The grid content varies based on what the user has earned.
    section.hidden = false;

    var highlights = [
      findMostVerifiedHighlight(user),
      findFirstCitationHighlight(user),
      findPhotoHighlight(user)
    ].filter(Boolean);

    // No qualifying highlights — show the fallback card.
    if (highlights.length === 0) {
      grid.innerHTML = ''
        + '<div class="highlight-empty">'
        +   '<div class="highlight-empty-icon">'
        +     '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'
        +       '<polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>'
        +     '</svg>'
        +   '</div>'
        +   '<div class="highlight-empty-text">'
        +     '<p class="highlight-empty-headline">Your first highlight is one report away.</p>'
        +     '<p class="highlight-empty-sub">Submit a report on the Live Feed, get verified by other Crew, and your first moment lands here.</p>'
        +   '</div>'
        + '</div>';
      return;
    }

    // Render each qualifying card. Order: most-verified first (it's the
    // most universal), then citation, then photo (photo is the smallest
    // population so it lands last as a "bonus" reveal).
    var html = '';
    for (var i = 0; i < highlights.length; i++) {
      var h = highlights[i];
      html += '<article class="highlight-card" data-kind="' + escapeHtml(h.kind) + '">'
        +   '<span class="highlight-card-stripe"></span>'
        +   '<div class="highlight-card-eyebrow">'
        +     '<span class="highlight-card-eyebrow-dot"></span>'
        +     '<span>' + escapeHtml(h.eyebrow) + '</span>'
        +   '</div>'
        +   '<h3 class="highlight-card-headline">' + escapeHtml(h.headline) + '</h3>'
        +   (h.meta
            ? '<div class="highlight-card-meta"><span>' + escapeHtml(h.meta) + '</span></div>'
            : '')
        +   (h.quote
            ? '<p class="highlight-card-quote">' + escapeHtml(h.quote) + '</p>'
            : '')
        + '</article>';
    }
    grid.innerHTML = html;
  }

  // Derive a Crew member's "specialty" — the dominant category among
  // the reports they've submitted, mapped to Survivor-cast-bio phrasing.
  // Falls back to a calm default for new members with no reports yet.
  function computeCrewSpecialty(user, state) {
    if (typeof CREW_REPORTS === 'undefined') return 'Field reporting';
    var mine = [];
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      var r = CREW_REPORTS[i];
      if (r.authorId && user && r.authorId === user.id) {
        mine.push(r);
      } else if (user && r.author === '@' + (user.handle || '')) {
        mine.push(r);
      }
    }
    if (mine.length < 3) return 'Field reporting';

    // Count by category bucket
    var buckets = { severe: 0, active: 0, current: 0 };
    for (var j = 0; j < mine.length; j++) {
      var sev = (typeof getReportSeverity === 'function')
        ? getReportSeverity(mine[j].type)
        : 'active';
      buckets[sev] = (buckets[sev] || 0) + 1;
    }
    var top = 'active';
    if (buckets.severe >= buckets.active && buckets.severe >= buckets.current) top = 'severe';
    else if (buckets.current > buckets.active) top = 'current';

    if (top === 'severe')  return 'Severe weather';
    if (top === 'current') return 'Daily conditions';
    return 'Active weather';
  }

  // ── Badge catalog ────────────────────────────────────────────────────
  // Each badge has an ID, display name, sub-line text, icon class, SVG
  // path data, and a `criteria(state)` predicate. evaluateBadges() runs
  // the predicates against current user state to find newly-earned ones,
  // adds their IDs to state.badges, and persists. Then renderBadges()
  // draws the grid showing earned badges first, locked badges after.
  var BADGE_CATALOG = [
    {
      id: 'first-report',
      name: 'First Report',
      subEarned: 'first one in!',
      subLocked: 'submit a report',
      iconClass: 'badge-icon-volume',
      svg: '<polyline points="20 6 9 17 4 12"/>',
      criteria: function(s) { return (s.reportsCount || 0) >= 1; }
    },
    {
      id: 'first-verify',
      name: 'First Verify',
      subEarned: 'thanks for confirming',
      subLocked: 'verify a report',
      iconClass: 'badge-icon-eye',
      svg: '<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/>',
      criteria: function(s) { return (s.verificationsCount || 0) >= 1; }
    },
    {
      id: 'storm-spotter',
      name: 'Storm Spotter',
      subEarned: '\xd73 storms',
      subLocked: 'spot 3 storms',
      iconClass: 'badge-icon-storm',
      svg: '<path d="M19 16.9A5 5 0 0 0 18 7h-1.26a8 8 0 1 0-11.62 9"/><polyline points="13 11 9 17 15 17 11 23"/>',
      criteria: function(s) { return false; }  // requires storm-typed reports — TODO
    },
    {
      id: 'clarity-5',
      name: 'Clarity 5+',
      subEarned: 'on a streak',
      subLocked: 'streak of 5',
      iconClass: 'badge-icon-streak',
      svg: '<path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/>',
      criteria: function(s) { return (s.streak || 0) >= 5; }
    },
    {
      id: 'reports-25',
      name: '25 Reports',
      subEarned: 'committed Crew',
      subLocked: 'submit 25 reports',
      iconClass: 'badge-icon-volume',
      svg: '<polyline points="20 6 9 17 4 12"/>',
      criteria: function(s) { return (s.reportsCount || 0) >= 25; }
    },
    {
      id: 'first-tip',
      name: 'Generous',
      subEarned: 'tipped a Crew member',
      subLocked: 'send your first tip',
      iconClass: 'badge-icon-save',
      svg: '<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>',
      criteria: function(s) {
        // Any tip given to any report?
        var t = s.tipsByMe || {};
        for (var k in t) { if (t[k] && t[k].length > 0) return true; }
        return false;
      }
    },
    {
      id: 'reports-100',
      name: '100 Reports',
      subEarned: 'centurion',
      subLocked: 'submit 100 reports',
      iconClass: 'badge-icon-volume',
      svg: '<polyline points="20 6 9 17 4 12"/>',
      criteria: function(s) { return (s.reportsCount || 0) >= 100; }
    },
    {
      id: 'clarity-10',
      name: 'Clarity 10',
      subEarned: 'reliable eye',
      subLocked: 'streak of 10',
      iconClass: 'badge-icon-streak',
      svg: '<path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/>',
      criteria: function(s) { return (s.streak || 0) >= 10; }
    }
  ];

  // Evaluate criteria against current state. Returns array of newly-earned
  // badge IDs (those whose criteria pass but aren't already in state.badges).
  // Mutates state.badges to include them and persists. Designed to be cheap
  // — call after every state change that could potentially unlock a badge.
  function evaluateBadges() {
    var state = auth.currentState();
    if (!state) return [];
    state.badges = state.badges || [];
    var newlyEarned = [];
    for (var i = 0; i < BADGE_CATALOG.length; i++) {
      var b = BADGE_CATALOG[i];
      // Check criteria but pass tipsByMe in a serializable form for the
      // generous-tip badge predicate to inspect.
      var snapshot = Object.assign({}, state, {
        tipsByMe: (function() {
          var out = {};
          for (var k in tipsByMe) out[k] = Array.from(tipsByMe[k]);
          return out;
        })()
      });
      if (b.criteria(snapshot) && state.badges.indexOf(b.id) === -1) {
        state.badges.push(b.id);
        newlyEarned.push(b.id);
      }
    }
    if (newlyEarned.length) auth.persist();
    return newlyEarned;
  }

  // Session-only Set of badge IDs the current user has earned during this
  // browser session. Used to show the "NEW" ribbon on freshly-earned
  // badges. Cleared on sign-out / page reload — meaning a new session
  // starts with no NEW ribbons even if the badges were earned recently
  // by the calendar. Honest UX: the ribbon means "you just earned this,"
  // not "you earned this in the last week."
  var sessionRecentBadges = new Set();

  function renderBadges() {
    var grid = $('badge-grid');
    var section = $('crew-badges-section');
    if (!grid) return;
    var state = auth.currentState();
    if (!state) {
      grid.innerHTML = '';
      // No signed-in user — hide the whole section (label + grid) so
      // we don't show "Badges earned" floating above empty space.
      if (section) section.style.display = 'none';
      return;
    }
    if (section) section.style.display = '';
    state.badges = state.badges || [];

    // First pass: evaluate criteria so newly-met-conditions are reflected
    var newlyEarned = evaluateBadges();
    for (var n = 0; n < newlyEarned.length; n++) {
      sessionRecentBadges.add(newlyEarned[n]);
    }

    var html = '';
    // Earned first
    for (var i = 0; i < BADGE_CATALOG.length; i++) {
      var b = BADGE_CATALOG[i];
      if (state.badges.indexOf(b.id) === -1) continue;
      var isRecent = sessionRecentBadges.has(b.id);
      html += '<div class="badge' + (isRecent ? ' recent' : '') + '">'
        + '<div class="badge-icon ' + b.iconClass + '">'
        +   '<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        +     ' stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">'
        +     b.svg
        +   '</svg>'
        + '</div>'
        + '<div class="badge-name">' + escapeHtml(b.name) + '</div>'
        + '<div class="badge-meta">' + escapeHtml(b.subEarned) + '</div>'
        + '</div>';
    }
    // Then locked (in catalog order)
    for (var j = 0; j < BADGE_CATALOG.length; j++) {
      var bl = BADGE_CATALOG[j];
      if (state.badges.indexOf(bl.id) !== -1) continue;
      html += '<div class="badge locked">'
        + '<div class="badge-icon badge-icon-locked">'
        +   '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        +     ' stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">'
        +     '<rect x="3" y="11" width="18" height="11" rx="2"/>'
        +     '<path d="M7 11V7a5 5 0 0 1 10 0v4"/>'
        +   '</svg>'
        + '</div>'
        + '<div class="badge-name">' + escapeHtml(bl.name) + '</div>'
        + '<div class="badge-meta">' + escapeHtml(bl.subLocked) + '</div>'
        + '</div>';
    }
    grid.innerHTML = html;
  }

  // Rank progression — five tiers from your gamification vision document.
  // Promotion is based on protected hours accrued.
  function computeRank(hours) {
    var tiers = [
      { name: 'Junior Attendant', numeral: 'I',  threshold: 0   },
      { name: 'Valet',            numeral: 'II', threshold: 50  },
      { name: 'Senior Valet',     numeral: 'III', threshold: 150 },
      { name: 'Lead Valet',       numeral: 'IV', threshold: 350 },
      { name: 'Valet Legend',     numeral: 'V',  threshold: 750 }
    ];
    var current = tiers[0];
    var next = tiers[1] || null;
    for (var i = 0; i < tiers.length; i++) {
      if (hours >= tiers[i].threshold) {
        current = tiers[i];
        next = tiers[i + 1] || null;
      }
    }
    return {
      name: current.name,
      numeral: current.numeral,
      next: next ? next.name : null,
      nextThreshold: next ? next.threshold : current.threshold,
      toNext: next ? Math.max(0, next.threshold - hours) : 0
    };
  }

  // After any state mutation, run this to re-evaluate badges, re-render
  // the Profile member card, and surface a small toast if a new badge
  // was just earned. Cheap to call (skips work if signed-out).
  function afterStateChange() {
    if (!auth.isSignedIn()) return;
    var newlyEarned = evaluateBadges();
    var user = auth.currentUser();
    applySignedInProfile(user);
    if (newlyEarned.length > 0) {
      // Find the badge name for the most recently earned for the toast
      var latestId = newlyEarned[newlyEarned.length - 1];
      var latest = null;
      for (var i = 0; i < BADGE_CATALOG.length; i++) {
        if (BADGE_CATALOG[i].id === latestId) { latest = BADGE_CATALOG[i]; break; }
      }
      if (latest) {
        // Defer slightly so it doesn't stack with the earn-toast that already fired
        setTimeout(function() {
          showTipToast('\ud83c\udfc6 Badge earned: ' + latest.name);
        }, 800);
      }
    }
  }


  // Three-card explainer shown after a brand-new account signs up.
  // After completion (or skip), the user's onboardingShown flag persists.
  var ONBOARDING_STEPS = [
    {
      eyebrow: 'Welcome',
      title: 'You\u2019re part of the Crew now',
      body: 'The Valet Crew is a community of people on the ground sharing real-time weather observations. Every report you submit makes the forecast smarter for someone planning their day.',
      cta: 'Show me how it works',
      icon: 'M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2|M9 11A4 4 0 1 0 9 3a4 4 0 0 0 0 8|M22 21v-2a4 4 0 0 0-3-3.87|M16 3.13a4 4 0 0 1 0 7.75'
    },
    {
      eyebrow: 'How it works',
      title: 'Tap. Verify. Tip.',
      body: 'Quick Reports take two taps \u2014 hail, wind, rain, or photo. Other Crew members can verify what you saw if they\u2019re nearby. You can also tip reports you find useful with The Nod, a Coffee, or The Handshake.',
      cta: 'What do I get?',
      icon: 'M22 11.08V12a10 10 0 1 1-5.93-9.14|M22 4 12 14.01l-3-3'
    },
    {
      eyebrow: 'Rewards',
      title: 'Tokens, badges, ranks',
      body: 'Earn tokens for reporting (+10) and verifying (+2). Spend them tipping good reports. Build clarity streaks, unlock badges, and climb from Junior Attendant all the way to Valet Legend.',
      cta: 'Let\u2019s go',
      icon: 'M12 2 4 6v6c0 5 3.5 9.5 8 10 4.5-.5 8-5 8-10V6l-8-4z|m9 12 2 2 4-4'
    }
  ];
  var onboardingIndex = 0;
  var onboardingDoneCallback = null;

  function startOnboarding(callback) {
    var s = auth.currentState();
    // If already shown (e.g. user is signing back in), just run the callback
    if (s && s.onboardingShown) {
      if (callback) callback();
      return;
    }
    onboardingIndex = 0;
    onboardingDoneCallback = callback || null;
    showOnboardingStep(0);
    var scrim = $('onboarding-scrim');
    var card = $('onboarding-card');
    if (scrim) scrim.classList.add('active');
    if (card) card.classList.add('active');
  }

  function showOnboardingStep(idx) {
    var step = ONBOARDING_STEPS[idx];
    if (!step) return;
    var eyebrow = $('onboarding-eyebrow');
    var title = $('onboarding-title');
    var body = $('onboarding-body');
    var ctaLabel = $('onboarding-cta-label');
    var backBtn = $('onboarding-back');
    var illust = $('onboarding-illustration');
    if (eyebrow) eyebrow.textContent = step.eyebrow + ' \u00b7 Step ' + (idx + 1) + ' of ' + ONBOARDING_STEPS.length;
    if (title) title.textContent = step.title;
    if (body) body.textContent = step.body;
    if (ctaLabel) ctaLabel.textContent = step.cta;
    if (backBtn) backBtn.hidden = (idx === 0);
    if (illust) {
      // Build SVG from the icon path string (paths separated by |)
      var paths = step.icon.split('|').map(function(d) {
        return '<path d="' + d + '"/>';
      }).join('');
      illust.innerHTML = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">'
        + paths + '</svg>';
    }
    // Update progress dots
    var dots = document.querySelectorAll('.onboarding-progress-dot');
    for (var i = 0; i < dots.length; i++) {
      dots[i].classList.toggle('is-active', i === idx);
    }
  }

  function endOnboarding() {
    var s = auth.currentState();
    if (s) {
      s.onboardingShown = true;
      auth.persist();
    }
    var scrim = $('onboarding-scrim');
    var card = $('onboarding-card');
    if (scrim) scrim.classList.remove('active');
    if (card) card.classList.remove('active');
    var cb = onboardingDoneCallback;
    onboardingDoneCallback = null;
    if (cb) setTimeout(cb, 320);
  }

  function handleOnboardAction(action) {
    if (action === 'next') {
      if (onboardingIndex < ONBOARDING_STEPS.length - 1) {
        onboardingIndex++;
        showOnboardingStep(onboardingIndex);
      } else {
        endOnboarding();
      }
    } else if (action === 'back') {
      if (onboardingIndex > 0) {
        onboardingIndex--;
        showOnboardingStep(onboardingIndex);
      }
    } else if (action === 'skip') {
      endOnboarding();
    }
  }

  // ── Live Feed map zoom state + render ──────────────────────────────────
  // Three discrete zoom tiers: 'county' (Boone County only), 'region'
  // (Central Indiana — Boone + adjacent counties), 'state' (all of IN).
  // Default is 'region' since that's the most demo-friendly first view —
  // shows enough variety to feel populated without being overwhelming.
  var currentZoom = 'region';

  // Map backgrounds — different SVG per zoom tier. Each is a stylized
  // mockup, not a real geographic projection. The labels and shapes give
  // the user a sense of "I'm zoomed in/out" without pretending to be
  // Google Maps.
  function getMapBackground(zoom) {
    if (zoom === 'city') {
      // City view — Lebanon, IN. Tightest scale, shows downtown grid:
      // Lebanon Square (center), Indianapolis Ave (the main N-S drag),
      // Sugar Creek (curving NE-SW), Lebanon Memorial Park.
      // The grid pattern is denser at this zoom so it reads as "streets."
      return ''
        + '<svg viewBox="0 0 800 480" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">'
        + '<defs>'
        +   '<linearGradient id="bgCity" x1="0" y1="0" x2="0" y2="1">'
        +     '<stop offset="0%" stop-color="#eef3fa"/><stop offset="100%" stop-color="#dde6f1"/>'
        +   '</linearGradient>'
        +   '<pattern id="gridCity" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">'
        +     '<path d="M40 0L0 0 0 40" fill="none" stroke="rgba(15,17,22,0.06)" stroke-width="1"/>'
        +   '</pattern>'
        + '</defs>'
        + '<rect width="800" height="480" fill="url(#bgCity)"/>'
        + '<rect width="800" height="480" fill="url(#gridCity)"/>'
        // Park (green-tinted block in the SW)
        + '<rect x="120" y="320" width="160" height="100" fill="rgba(35,140,60,0.10)" stroke="rgba(35,140,60,0.25)" stroke-width="1" rx="4"/>'
        + '<text x="135" y="378" fill="rgba(35,140,60,0.55)" font-family="JetBrains Mono, monospace" font-size="9" font-weight="700" letter-spacing="0.5">MEMORIAL PARK</text>'
        // Major streets — Indianapolis Ave (N-S spine), Main St (E-W)
        + '<path d="M 380 0 L 380 480" stroke="rgba(15,17,22,0.18)" stroke-width="3" fill="none"/>'
        + '<path d="M 0 240 L 800 240" stroke="rgba(15,17,22,0.18)" stroke-width="3" fill="none"/>'
        // Secondary streets
        + '<path d="M 0 120 L 800 120" stroke="rgba(15,17,22,0.10)" stroke-width="1.5" fill="none"/>'
        + '<path d="M 0 360 L 800 360" stroke="rgba(15,17,22,0.10)" stroke-width="1.5" fill="none"/>'
        + '<path d="M 200 0 L 200 480" stroke="rgba(15,17,22,0.10)" stroke-width="1.5" fill="none"/>'
        + '<path d="M 580 0 L 580 480" stroke="rgba(15,17,22,0.10)" stroke-width="1.5" fill="none"/>'
        // Sugar Creek — curves through NE quadrant
        + '<path d="M 800 80 Q 650 140 580 200 Q 520 260 460 360 Q 400 440 340 480" stroke="rgba(65,105,225,0.30)" stroke-width="3" fill="none" stroke-linecap="round"/>'
        // Lebanon Square — small block at the intersection of the main streets
        + '<rect x="362" y="222" width="36" height="36" fill="rgba(65,105,225,0.18)" stroke="rgba(65,105,225,0.50)" stroke-width="1.5"/>'
        + '<text x="412" y="245" fill="rgba(15,17,22,0.65)" font-family="JetBrains Mono, monospace" font-size="11" font-weight="700" letter-spacing="0.6">LEBANON SQUARE</text>'
        // Street name labels
        + '<g fill="rgba(15,17,22,0.45)" font-family="JetBrains Mono, monospace" font-size="9" font-weight="700" letter-spacing="0.5">'
        +   '<text x="392" y="20" transform="rotate(0 392 20)">INDIANAPOLIS AVE</text>'
        +   '<text x="20" y="232">MAIN ST</text>'
        + '</g>'
        + '</svg>';
    }
    if (zoom === 'county') {
      // County view — Boone County, IN. Tight scale, shows Lebanon
      // (county seat), Zionsville, Thorntown, and the I-65 + I-865 lines.
      return ''
        + '<svg viewBox="0 0 800 480" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">'
        + '<defs>'
        +   '<linearGradient id="bgCounty" x1="0" y1="0" x2="0" y2="1">'
        +     '<stop offset="0%" stop-color="#e6edf6"/><stop offset="100%" stop-color="#d2dde9"/>'
        +   '</linearGradient>'
        +   '<pattern id="gridCounty" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">'
        +     '<path d="M60 0L0 0 0 60" fill="none" stroke="rgba(15,17,22,0.04)" stroke-width="1"/>'
        +   '</pattern>'
        + '</defs>'
        + '<rect width="800" height="480" fill="url(#bgCounty)"/>'
        + '<rect width="800" height="480" fill="url(#gridCounty)"/>'
        // County boundary — irregular polygon suggesting Boone County outline
        + '<path d="M 80 40 L 720 60 L 740 410 L 100 440 Z" fill="rgba(65,105,225,0.04)" stroke="rgba(65,105,225,0.30)" stroke-width="1.5" stroke-dasharray="6,5"/>'
        // Major roads — I-65 vertical, I-74 horizontal-ish
        + '<path d="M 510 40 L 540 440" stroke="rgba(15,17,22,0.16)" stroke-width="2.5" fill="none"/>'
        + '<path d="M 80 360 L 720 320" stroke="rgba(15,17,22,0.12)" stroke-width="2" fill="none" stroke-dasharray="6,4"/>'
        // Sugar Creek
        + '<path d="M 200 80 Q 300 200 400 280 Q 500 380 600 440" stroke="rgba(65,105,225,0.20)" stroke-width="2.5" fill="none" stroke-linecap="round"/>'
        // City labels and dots
        + '<g fill="rgba(15,17,22,0.50)" font-family="JetBrains Mono, monospace" font-size="11" font-weight="700" letter-spacing="0.6">'
        +   '<text x="265" y="295">LEBANON</text>'
        +   '<text x="555" y="350">ZIONSVILLE</text>'
        +   '<text x="120" y="195">THORNTOWN</text>'
        +   '<text x="540" y="120">WHITESTOWN</text>'
        + '</g>'
        + '<circle cx="245" cy="285" r="5" fill="rgba(15,17,22,0.55)"/>'
        + '<circle cx="555" cy="340" r="5" fill="rgba(15,17,22,0.50)"/>'
        + '<circle cx="125" cy="180" r="4" fill="rgba(15,17,22,0.45)"/>'
        + '<circle cx="540" cy="105" r="4" fill="rgba(15,17,22,0.45)"/>'
        + '</svg>';
    }
    if (zoom === 'state') {
      // State view — all of Indiana. Wide scale, shows Indy in the
      // middle, Fort Wayne NE, South Bend N, Evansville S, Bloomington S-Central.
      return ''
        + '<svg viewBox="0 0 800 480" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">'
        + '<defs>'
        +   '<linearGradient id="bgState" x1="0" y1="0" x2="0" y2="1">'
        +     '<stop offset="0%" stop-color="#dde6f1"/><stop offset="100%" stop-color="#c6d2e2"/>'
        +   '</linearGradient>'
        +   '<pattern id="gridState" x="0" y="0" width="30" height="30" patternUnits="userSpaceOnUse">'
        +     '<path d="M30 0L0 0 0 30" fill="none" stroke="rgba(15,17,22,0.04)" stroke-width="1"/>'
        +   '</pattern>'
        + '</defs>'
        + '<rect width="800" height="480" fill="url(#bgState)"/>'
        + '<rect width="800" height="480" fill="url(#gridState)"/>'
        // Indiana state outline — stylized
        + '<path d="M 200 30 L 580 35 L 600 80 L 610 250 L 580 360 L 540 410 L 480 460 L 420 460 L 350 440 L 280 420 L 230 380 L 200 320 L 180 220 L 200 30 Z" fill="rgba(65,105,225,0.04)" stroke="rgba(65,105,225,0.40)" stroke-width="2"/>'
        // Major water (Wabash + Ohio rivers stylized)
        + '<path d="M 130 60 Q 180 200 230 360 Q 280 440 410 470" stroke="rgba(65,105,225,0.18)" stroke-width="2" fill="none" stroke-linecap="round"/>'
        + '<path d="M 580 460 Q 470 450 350 460 Q 230 470 130 440" stroke="rgba(65,105,225,0.22)" stroke-width="3" fill="none" stroke-linecap="round"/>'
        // City labels and dots — major Indiana cities
        + '<g fill="rgba(15,17,22,0.55)" font-family="JetBrains Mono, monospace" font-size="11" font-weight="700" letter-spacing="0.6">'
        +   '<text x="290" y="240">INDIANAPOLIS</text>'
        +   '<text x="530" y="120">FORT WAYNE</text>'
        +   '<text x="370" y="80">SOUTH BEND</text>'
        +   '<text x="120" y="430">EVANSVILLE</text>'
        +   '<text x="320" y="320">BLOOMINGTON</text>'
        +   '<text x="120" y="290">TERRE HAUTE</text>'
        +   '<text x="450" y="200">MUNCIE</text>'
        + '</g>'
        + '<circle cx="350" cy="250" r="6" fill="rgba(15,17,22,0.65)"/>'
        + '<circle cx="555" cy="135" r="5" fill="rgba(15,17,22,0.55)"/>'
        + '<circle cx="395" cy="65" r="4" fill="rgba(15,17,22,0.50)"/>'
        + '<circle cx="160" cy="420" r="4" fill="rgba(15,17,22,0.50)"/>'
        + '<circle cx="350" cy="310" r="4" fill="rgba(15,17,22,0.50)"/>'
        + '<circle cx="160" cy="280" r="4" fill="rgba(15,17,22,0.45)"/>'
        + '<circle cx="480" cy="200" r="4" fill="rgba(15,17,22,0.50)"/>'
        + '</svg>';
    }
    // Default: region (Central Indiana — same as the original pre-zoom map)
    return ''
      + '<svg viewBox="0 0 800 480" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">'
      + '<defs>'
      +   '<linearGradient id="bgRegion" x1="0" y1="0" x2="0" y2="1">'
      +     '<stop offset="0%" stop-color="#dde6f1"/><stop offset="100%" stop-color="#cfd9e6"/>'
      +   '</linearGradient>'
      +   '<pattern id="gridRegion" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">'
      +     '<path d="M40 0L0 0 0 40" fill="none" stroke="rgba(15,17,22,0.04)" stroke-width="1"/>'
      +   '</pattern>'
      + '</defs>'
      + '<rect width="800" height="480" fill="url(#bgRegion)"/>'
      + '<rect width="800" height="480" fill="url(#gridRegion)"/>'
      + '<path d="M0 180 Q200 140 400 200 T800 180" stroke="rgba(15,17,22,0.08)" stroke-width="1" fill="none"/>'
      + '<path d="M0 240 Q220 200 420 250 T800 240" stroke="rgba(15,17,22,0.06)" stroke-width="1" fill="none"/>'
      + '<path d="M0 300 Q180 260 380 310 T800 300" stroke="rgba(15,17,22,0.06)" stroke-width="1" fill="none"/>'
      + '<path d="M120 80 Q180 200 220 280 Q260 360 340 440" stroke="rgba(65,105,225,0.18)" stroke-width="2.5" fill="none" stroke-linecap="round"/>'
      + '<path d="M0 360 L800 320" stroke="rgba(15,17,22,0.10)" stroke-width="2" fill="none" stroke-dasharray="6,4"/>'
      + '<path d="M340 0 L420 480" stroke="rgba(15,17,22,0.08)" stroke-width="2" fill="none" stroke-dasharray="6,4"/>'
      + '<g fill="rgba(15,17,22,0.45)" font-family="JetBrains Mono, monospace" font-size="10" font-weight="700" letter-spacing="0.6">'
      +   '<text x="180" y="220">LEBANON</text>'
      +   '<text x="420" y="380">INDIANAPOLIS</text>'
      +   '<text x="560" y="180">NOBLESVILLE</text>'
      +   '<text x="120" y="100">CRAWFORDSVILLE</text>'
      + '</g>'
      + '<circle cx="170" cy="230" r="3" fill="rgba(15,17,22,0.35)"/>'
      + '<circle cx="420" cy="390" r="4" fill="rgba(15,17,22,0.45)"/>'
      + '<circle cx="560" cy="190" r="3" fill="rgba(15,17,22,0.35)"/>'
      + '<circle cx="120" cy="110" r="3" fill="rgba(15,17,22,0.35)"/>'
      + '</svg>';
  }

  // SVG icons inside the pulse dot keyed by report type. These match the
  // legend swatches and the tip-button icons, so users see consistent
  // semantics across surfaces.
  function pulseIconSvg(type) {
    if (type === 'storm') {
      return '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
        + '<polyline points="13 11 9 17 15 17 11 23"/></svg>';
    }
    if (type === 'hail') {
      return '<svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor">'
        + '<circle cx="8" cy="9" r="2"/><circle cx="16" cy="9" r="2"/><circle cx="12" cy="15" r="2"/></svg>';
    }
    if (type === 'wind') {
      return '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
        + '<path d="M9 6a2 2 0 1 1 2 2H4m11 11a2 2 0 1 0 2-2H4m12-7a2.5 2.5 0 1 1 2 4H4"/></svg>';
    }
    if (type === 'rain') {
      return '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
        + '<line x1="8" y1="14" x2="8" y2="20"/><line x1="12" y1="14" x2="12" y2="20"/>'
        + '<line x1="16" y1="14" x2="16" y2="20"/></svg>';
    }
    if (type === 'clear') {
      return '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
        + '<circle cx="12" cy="12" r="4"/></svg>';
    }
    return '';
  }

  function storyIconSvg(type) {
    if (type === 'storm') {
      return '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        + '<path d="M19 16.9A5 5 0 0 0 18 7h-1.26a8 8 0 1 0-11.62 9"/>'
        + '<polyline points="13 11 9 17 15 17 11 23"/></svg>';
    }
    if (type === 'hail') {
      return '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        + '<circle cx="6" cy="20" r="1.5"/><circle cx="12" cy="20" r="1.5"/>'
        + '<circle cx="18" cy="20" r="1.5"/><circle cx="9" cy="14" r="1.5"/>'
        + '<circle cx="15" cy="14" r="1.5"/>'
        + '<path d="M19 16.9A5 5 0 0 0 18 7h-1.26a8 8 0 1 0-11.62 9"/></svg>';
    }
    if (type === 'wind') {
      return '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        + '<path d="M9.59 4.59A2 2 0 1 1 11 8H2m10.59 11.41A2 2 0 1 0 14 16H2m15.73-8.27A2.5 2.5 0 1 1 19.5 12H2"/></svg>';
    }
    if (type === 'rain') {
      return '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        + '<line x1="8" y1="19" x2="8" y2="21"/><line x1="8" y1="13" x2="8" y2="15"/>'
        + '<line x1="16" y1="19" x2="16" y2="21"/><line x1="16" y1="13" x2="16" y2="15"/>'
        + '<line x1="12" y1="21" x2="12" y2="23"/><line x1="12" y1="15" x2="12" y2="17"/>'
        + '<path d="M20 16.58A5 5 0 0 0 18 7h-1.26A8 8 0 1 0 4 15.25"/></svg>';
    }
    if (type === 'clear') {
      return '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
        + ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        + '<circle cx="12" cy="12" r="4"/>'
        + '<path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"/></svg>';
    }
    return '';
  }

  // ════════════════════════════════════════════════════════════════════
  // Live Feed map (Phase 9 — Push A)
  // ════════════════════════════════════════════════════════════════════
  //
  // Real Leaflet map replacing the stylized SVG version. Lazy-loaded on
  // first init. Handles:
  //   - Map creation with CartoDB Positron tiles
  //   - Geolocation with timeout fallback
  //   - NWS overlay layers (warning / watch / advisory / statement)
  //   - "Recenter on me" button visibility based on pan distance
  //
  // PUSH A scope: map + NWS overlay only. Pulse rendering and viewport-
  // driven story list filtering land in Push B.
  //
  // Public API exposed on window.WV_FeedMapAPI for the rest of the code:
  //   .map()              - Leaflet map instance
  //   .markerLayer()      - LayerGroup for report pulses
  //   .ready()            - boolean
  //   .recenter()         - return to home center
  //   .setHomeLatLng(...) - update the "home" point if geolocation arrives late
  //   .getViewportBounds() - current Leaflet bounds (used by Push B)

  (function() {
    var FEED_MAP_DEFAULT_CENTER = [39.7684, -86.1581];  // Indianapolis
    var FEED_MAP_DEFAULT_ZOOM = 9;
    var TILE_URL = 'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png';
    var TILE_ATTRIBUTION = '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors &copy; <a href="https://carto.com/">CARTO</a>';

    var state = {
      map: null,
      markerLayer: null,
      nwsLayers: {},      // category -> L.LayerGroup
      homeLatLng: FEED_MAP_DEFAULT_CENTER,
      ready: false,
      // Phase 4 (May 17): NEXRAD radar overlay state
      radarLayer: null,
      radarRefreshTimer: null,
      // Per-event-type filter — hidden event names (lowercased+trimmed)
      hiddenEventTypes: {}
    };

    // Phase 4 NEXRAD radar constants — same provider as the Met map.
    var FEED_RADAR_TILE_URL = 'https://mesonet.agron.iastate.edu/cache/tile.py/1.0.0/nexrad-n0q-900913/{z}/{x}/{y}.png';
    var FEED_RADAR_ATTRIBUTION = 'Radar &copy; <a href="https://mesonet.agron.iastate.edu/">Iowa State Mesonet</a> &middot; NWS NEXRAD';
    var FEED_RADAR_REFRESH_MS = 5 * 60 * 1000;
    var FEED_RADAR_LS_KEY = 'wv_feed_map_radar_on';
    var FEED_EVENT_FILTER_LS_KEY = 'wv_feed_map_event_filter';

    // Restore saved event filter on module load
    try {
      var __ef = localStorage.getItem(FEED_EVENT_FILTER_LS_KEY);
      if (__ef) {
        var __parsed = JSON.parse(__ef);
        if (__parsed && typeof __parsed === 'object') state.hiddenEventTypes = __parsed;
      }
    } catch (e) {}

    function _feedPersistEventFilter() {
      try {
        localStorage.setItem(FEED_EVENT_FILTER_LS_KEY, JSON.stringify(state.hiddenEventTypes));
      } catch (e) {}
    }

    function _createFeedRadarLayer() {
      // Cache-bust URL forces fresh tiles on each refresh cycle
      return L.tileLayer(FEED_RADAR_TILE_URL + '?ts=' + Date.now(), {
        attribution: FEED_RADAR_ATTRIBUTION,
        opacity: 0.55,
        maxZoom: 12
      });
    }

    function showFeedRadar() {
      if (!state.map || state.radarLayer) return;
      state.radarLayer = _createFeedRadarLayer().addTo(state.map);
      if (state.radarRefreshTimer) clearInterval(state.radarRefreshTimer);
      state.radarRefreshTimer = setInterval(function() {
        if (!state.radarLayer || !state.map) return;
        state.map.removeLayer(state.radarLayer);
        state.radarLayer = _createFeedRadarLayer().addTo(state.map);
      }, FEED_RADAR_REFRESH_MS);
    }

    function hideFeedRadar() {
      if (state.radarLayer && state.map) {
        state.map.removeLayer(state.radarLayer);
        state.radarLayer = null;
      }
      if (state.radarRefreshTimer) {
        clearInterval(state.radarRefreshTimer);
        state.radarRefreshTimer = null;
      }
    }

    function ensureLeaflet(callback) {
      if (window.L) { callback(); return; }
      // Leaflet CSS
      if (!document.querySelector('link[data-leaflet]')) {
        var link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
        link.setAttribute('data-leaflet', 'css');
        document.head.appendChild(link);
      }
      // Leaflet JS
      if (document.querySelector('script[data-leaflet-js]')) {
        var poll = setInterval(function() {
          if (window.L) { clearInterval(poll); callback(); }
        }, 40);
        return;
      }
      var s = document.createElement('script');
      s.src = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';
      s.async = true;
      s.setAttribute('data-leaflet-js', '1');
      s.onload = callback;
      s.onerror = function() {
        var loadingEl = document.getElementById('feed-map-loading');
        if (loadingEl) loadingEl.innerHTML = '<span style="color:#C2342B;">Map failed to load.</span>';
      };
      document.head.appendChild(s);
    }

    function tryGeolocation(onPosition, onFailure) {
      if (!navigator.geolocation) { onFailure(); return; }
      var done = false;
      var timer = setTimeout(function() {
        if (done) return;
        done = true;
        onFailure('timeout');
      }, 4000);
      navigator.geolocation.getCurrentPosition(
        function(pos) {
          if (done) return;
          done = true;
          clearTimeout(timer);
          onPosition([pos.coords.latitude, pos.coords.longitude]);
        },
        function(err) {
          if (done) return;
          done = true;
          clearTimeout(timer);
          onFailure(err && err.code === 1 ? 'denied' : 'error');
        },
        { enableHighAccuracy: false, timeout: 4000, maximumAge: 600000 }
      );
    }

    function hideLoading() {
      var loadingEl = document.getElementById('feed-map-loading');
      if (loadingEl) loadingEl.classList.add('is-ready');
    }

    function buildMap(center, zoom) {
      var container = document.getElementById('feed-map-leaflet');
      if (!container) return;

      var map = L.map(container, {
        center: center,
        zoom: zoom,
        scrollWheelZoom: true,
        zoomControl: true,
        attributionControl: true
      });

      L.tileLayer(TILE_URL, {
        attribution: TILE_ATTRIBUTION,
        subdomains: 'abcd',
        maxZoom: 19
      }).addTo(map);

      // Layer for report markers — populated by Push B's renderer
      state.markerLayer = L.layerGroup().addTo(map);

      // NWS overlay layers — one per category, filterable via the
      // existing filter pills above the map. Categories match the
      // categorizeAlertEvent output (warning / watch / advisory).
      state.nwsLayers = {
        warning:  L.layerGroup().addTo(map),
        watch:    L.layerGroup().addTo(map),
        advisory: L.layerGroup().addTo(map)
      };

      state.map = map;
      state.ready = true;
      hideLoading();

      // Phase 4: restore radar preference if user had it on previously
      try {
        if (localStorage.getItem(FEED_RADAR_LS_KEY) === '1') {
          showFeedRadar();
        }
      } catch (e) {}

      // "Recenter on me" button visibility — show when user has panned
      // far enough from home that recentering is meaningful.
      var locateBtn = document.getElementById('feed-map-locate');
      if (locateBtn) {
        locateBtn.addEventListener('click', function() {
          map.setView(state.homeLatLng, FEED_MAP_DEFAULT_ZOOM);
        });
        map.on('moveend', function() {
          var c = map.getCenter();
          var dx = c.lat - state.homeLatLng[0];
          var dy = c.lng - state.homeLatLng[1];
          var distSq = dx * dx + dy * dy;
          // ~0.3 degrees ≈ 30 km; show button beyond that.
          locateBtn.hidden = (distSq < 0.09);
        });
      }

      // Resize fix — Leaflet calculates dimensions on init. If the screen
      // wasn't visible (e.g. user landed via a deep link), the container
      // had height 0 and the map would render at the wrong size.
      setTimeout(function() { map.invalidateSize(); }, 120);

      // Notify any listeners that the map is ready (Push B's pulse and
      // viewport renderers will hook into this event).
      try {
        window.dispatchEvent(new CustomEvent('wvFeedMapReady', { detail: { map: map } }));
      } catch (e) {}
    }

    function init() {
      var container = document.getElementById('feed-map-leaflet');
      if (!container) return;
      if (state.ready) {
        // Already initialized — just make sure dimensions are fresh.
        if (state.map) state.map.invalidateSize();
        return;
      }
      ensureLeaflet(function() {
        // Try geolocation, fall back quickly to Indianapolis if it
        // takes too long or fails.
        tryGeolocation(
          function(latLng) {
            state.homeLatLng = latLng;
            buildMap(latLng, FEED_MAP_DEFAULT_ZOOM);
          },
          function() {
            buildMap(FEED_MAP_DEFAULT_CENTER, FEED_MAP_DEFAULT_ZOOM);
          }
        );
      });
    }

    // ─── Pulse rendering (Push B) ────────────────────────────────────
    // Mirrors crew-map.js's reverse-projection. Reports with native
    // lat/lng (Met posts, post-launch real-data reports) skip the
    // projection. Seeded prototype reports use mapPosState percent
    // positions converted via the shared bbox.
    //
    // Pulse divIcons preserve the multi-ring HTML structure from the
    // SVG version so the existing CSS animations still apply.
    var FEED_MAP_BBOX = { minLat: 37.4, maxLat: 42.0, minLng: -88.5, maxLng: -84.0 };

    function reportToLatLng(report) {
      if (report.lat != null && report.lng != null) {
        return [report.lat, report.lng];
      }
      var pos = report.mapPosState
             || report.mapPosRegion
             || report.mapPosCounty
             || report.mapPosCity
             || report.mapPos;
      if (!pos || !pos.top || !pos.left) return null;
      var topPct  = parseFloat(pos.top);
      var leftPct = parseFloat(pos.left);
      if (isNaN(topPct) || isNaN(leftPct)) return null;
      var lat = FEED_MAP_BBOX.maxLat - (topPct  / 100) * (FEED_MAP_BBOX.maxLat - FEED_MAP_BBOX.minLat);
      var lng = FEED_MAP_BBOX.minLng + (leftPct / 100) * (FEED_MAP_BBOX.maxLng - FEED_MAP_BBOX.minLng);
      return [lat, lng];
    }

    function buildPulseIcon(report, isMine, ageStage) {
      var typeAttr = report.type || 'rain';
      var classes = 'feed-pulse';
      if (isMine) classes += ' is-mine';
      if (report.isMetPost) classes += ' is-met-post';
      var inner;
      if (report.isMetPost) {
        inner = '<div class="feed-pulse-dot"></div>';
      } else {
        inner = '<div class="feed-pulse-halo"></div>'
              + '<div class="feed-pulse-ring"></div>'
              + '<div class="feed-pulse-ring ring-2"></div>'
              + '<div class="feed-pulse-ring ring-3"></div>'
              + '<div class="feed-pulse-dot">'
              + (typeof pulseIconSvg === 'function' ? pulseIconSvg(typeAttr) : '')
              + '</div>';
      }
      return L.divIcon({
        className: '',
        html: '<div class="' + classes + '" data-type="' + typeAttr
              + '" data-stage="' + (ageStage || 'fresh') + '"'
              + ' style="position:relative;width:100%;height:100%;">' + inner + '</div>',
        iconSize: [32, 32],
        iconAnchor: [16, 16]
      });
    }

    // Diff-based render: keep existing markers, add new ones, remove
    // stale ones. Avoids tearing down all markers every refresh.
    state.markersById = {};

    function renderPulses() {
      if (!state.map || !state.markerLayer) return;

      // Pull the live report set from whatever's available. CREW_REPORTS
      // is the main array; MET_POSTS gets stirred in like the Crew API does.
      var reports = (typeof CREW_REPORTS !== 'undefined') ? CREW_REPORTS.slice() : [];
      if (typeof MET_POSTS !== 'undefined' && MET_POSTS.length) {
        for (var i = 0; i < MET_POSTS.length; i++) {
          var p = MET_POSTS[i];
          if (p.status !== 'live') continue;
          reports.push({
            id: p.id,
            isMetPost: true,
            type: 'met-post',
            text: p.text,
            author: p.author,
            authorInitials: p.authorInitials,
            city: 'Coverage',
            submittedAt: p.submittedAt,
            verified: p.verified || 0,
            lat: p.lat,
            lng: p.lng
          });
        }
      }

      // Pull user context for "is mine" styling
      var currentUser = null;
      try { currentUser = window.auth && window.auth.currentUser && window.auth.currentUser(); } catch (e) {}

      var visibleIds = {};
      for (var r = 0; r < reports.length; r++) {
        var rpt = reports[r];
        var latlng = reportToLatLng(rpt);
        if (!latlng) continue;

        // Skip stale reports — same lifecycle policy as crew-map.js.
        var stage = 'fresh';
        try {
          var ageInfo = (typeof getReportAge === 'function') ? getReportAge(rpt) : null;
          if (ageInfo) {
            stage = ageInfo.stage;
            if (stage === 'stale') continue;
          }
        } catch (e) {}

        visibleIds[rpt.id] = true;
        var isMine = false;
        if (currentUser) {
          if (rpt.authorId && rpt.authorId === currentUser.id) isMine = true;
          else if (rpt.author === '@' + (currentUser.handle || '')) isMine = true;
        }

        var existing = state.markersById[rpt.id];
        if (existing) {
          // Update icon (in case stage/mine state changed); leave position
          // alone — seeded coords don't move.
          existing.setIcon(buildPulseIcon(rpt, isMine, stage));
        } else {
          var marker = L.marker(latlng, {
            icon: buildPulseIcon(rpt, isMine, stage),
            riseOnHover: true,
            zIndexOffset: isMine ? 1000 : 0
          });
          (function(reportId) {
            marker.on('click', function() {
              if (typeof openReportDetail === 'function') openReportDetail(reportId);
            });
          })(rpt.id);
          state.markerLayer.addLayer(marker);
          state.markersById[rpt.id] = marker;
        }
      }

      // Remove markers no longer in the report set
      for (var id in state.markersById) {
        if (!visibleIds[id]) {
          try { state.markerLayer.removeLayer(state.markersById[id]); } catch (e) {}
          delete state.markersById[id];
        }
      }
    }

    // ─── NWS overlay rendering (Push B) ──────────────────────────────
    // Same data + filter state as the Live Feed used to project onto
    // the SVG. Now renders as real Leaflet polygons across 4 layer
    // groups (warning/watch/advisory/statement). Filter pills toggle
    // visibility by add/remove of the layer group.
    // Event-specific NWS polygon colors. Follows the standard NWS color
    // conventions used by their own products (tornado red, severe t-storm
    // amber, flash flood green, etc) so emergency responders + weather-
    // literate viewers see what they expect.
    //
    // Fallback by category if event isn't in this table.
    var ALERT_STYLES = {
      warning:  { color: '#C2342B', fillColor: '#C2342B', fillOpacity: 0.18, weight: 2, opacity: 0.85 },
      watch:    { color: '#F5A524', fillColor: '#F5A524', fillOpacity: 0.14, weight: 1.5, opacity: 0.80 },
      advisory: { color: '#4169E1', fillColor: '#4169E1', fillOpacity: 0.10, weight: 1.5, opacity: 0.75 }
    };

    // Event-specific overrides — keys are normalized NWS event names.
    var ALERT_STYLES_BY_EVENT = {
      // Tornado — red family (most severe)
      'tornado warning':                  { color: '#C2342B', fillColor: '#C2342B', fillOpacity: 0.25, weight: 2.5, opacity: 0.95 },
      'tornado watch':                    { color: '#FFCC00', fillColor: '#FFCC00', fillOpacity: 0.18, weight: 2, opacity: 0.85 },
      // Severe Thunderstorm — amber/orange
      'severe thunderstorm warning':      { color: '#F5A524', fillColor: '#F5A524', fillOpacity: 0.20, weight: 2, opacity: 0.90 },
      'severe thunderstorm watch':        { color: '#FFCC00', fillColor: '#FFCC00', fillOpacity: 0.14, weight: 2, opacity: 0.80 },
      // Flash Flood — green (NWS standard for flood)
      'flash flood warning':              { color: '#2E8B57', fillColor: '#2E8B57', fillOpacity: 0.20, weight: 2, opacity: 0.90 },
      'flash flood watch':                { color: '#7BC97B', fillColor: '#7BC97B', fillOpacity: 0.14, weight: 2, opacity: 0.80 },
      // Flood (areal/river) — lighter green
      'flood warning':                    { color: '#1F8F4F', fillColor: '#1F8F4F', fillOpacity: 0.16, weight: 2, opacity: 0.85 },
      'flood watch':                      { color: '#7BC97B', fillColor: '#7BC97B', fillOpacity: 0.12, weight: 1.5, opacity: 0.75 },
      // Winter weather — light blue/purple
      'winter storm warning':             { color: '#8B5DB7', fillColor: '#8B5DB7', fillOpacity: 0.18, weight: 2, opacity: 0.85 },
      'winter storm watch':               { color: '#B69CD8', fillColor: '#B69CD8', fillOpacity: 0.14, weight: 2, opacity: 0.80 },
      'blizzard warning':                 { color: '#6E4DB2', fillColor: '#6E4DB2', fillOpacity: 0.22, weight: 2.5, opacity: 0.90 },
      // Heat — orange
      'excessive heat warning':           { color: '#E66A1F', fillColor: '#E66A1F', fillOpacity: 0.18, weight: 2, opacity: 0.85 },
      // Wind — tan/brown
      'high wind warning':                { color: '#A66E3A', fillColor: '#A66E3A', fillOpacity: 0.18, weight: 2, opacity: 0.85 },
      'wind advisory':                    { color: '#C99E70', fillColor: '#C99E70', fillOpacity: 0.12, weight: 1.5, opacity: 0.75 },
      // Fire/smoke — burnt orange
      'red flag warning':                 { color: '#D44A1F', fillColor: '#D44A1F', fillOpacity: 0.20, weight: 2, opacity: 0.90 },
    };

    function renderAlerts() {
      if (!state.map) return;
      var alerts = (typeof upnextState !== 'undefined' && upnextState && upnextState.alerts) ? upnextState.alerts : [];

      // Clear all existing layers
      Object.keys(state.nwsLayers).forEach(function(cat) {
        state.nwsLayers[cat].clearLayers();
      });

      // Phase 4 (May 17): track event types per category for the UI's
      // per-event-type filter dropdown. Counts every alert (even if
      // hidden by the filter) so the dropdown shows totals.
      var eventTypesByCategory = {
        warning: {}, watch: {}, advisory: {}
      };

      for (var i = 0; i < alerts.length; i++) {
        var feature = alerts[i];
        var props = feature.properties || {};
        var category = (typeof categorizeAlertEvent === 'function')
          ? categorizeAlertEvent(props.event || '')
          : { category: 'advisory', advisoryType: null };
        if (!feature.geometry || !feature.geometry.coordinates) continue;
        var cat = category.category || 'advisory';

        var eventKey = (props.event || 'Weather alert').toLowerCase().trim();
        var displayEvent = props.event || 'Weather alert';

        // Tally the event type for the dropdown UI (even hidden ones)
        if (eventTypesByCategory[cat]) {
          if (!eventTypesByCategory[cat][eventKey]) {
            eventTypesByCategory[cat][eventKey] = {
              event: displayEvent,
              count: 0,
              hidden: !!state.hiddenEventTypes[eventKey]
            };
          }
          eventTypesByCategory[cat][eventKey].count++;
        }

        // Skip drawing if the user has hidden this specific event type
        if (state.hiddenEventTypes[eventKey]) continue;

        // Try event-specific style first (exact match, then prefix match),
        // fall back to category style.
        var style = ALERT_STYLES_BY_EVENT[eventKey];
        if (!style) {
          for (var styleKey in ALERT_STYLES_BY_EVENT) {
            if (eventKey.indexOf(styleKey) === 0) {
              style = ALERT_STYLES_BY_EVENT[styleKey];
              break;
            }
          }
        }
        if (!style) style = ALERT_STYLES[cat] || ALERT_STYLES.advisory;
        var layer;
        try { layer = L.geoJSON(feature, { style: style }); } catch (e) { continue; }

        var headline = props.event || 'Active alert';
        var area = props.areaDesc || '';
        var expires = props.expires || props.ends;
        var expiryText = '';
        if (expires) {
          try {
            var d = new Date(expires);
            if (!isNaN(d.getTime()) && typeof formatTimeLabel === 'function') {
              expiryText = 'Until ' + formatTimeLabel(d);
            }
          } catch (e) {}
        }
        var popupHtml = ''
          + '<div style="font-family:Inter,system-ui,sans-serif;min-width:200px;">'
          +   '<div style="font-family:\'JetBrains Mono\',monospace;font-size:9.5px;'
          +              'font-weight:700;letter-spacing:1.4px;text-transform:uppercase;'
          +              'color:' + style.color + ';margin-bottom:6px;">' + cat + '</div>'
          +   '<div style="font-size:13.5px;font-weight:600;color:#0E1116;line-height:1.35;margin-bottom:4px;">'
          +     escapeHtml(headline) + '</div>'
          +   (area
              ? '<div style="font-family:\'JetBrains Mono\',monospace;font-size:10.5px;'
              + 'color:rgba(15,17,22,0.55);margin-bottom:4px;">'
              +   escapeHtml(area.length > 80 ? area.slice(0, 80) + '\u2026' : area) + '</div>'
              : '')
          +   (expiryText
              ? '<div style="font-family:\'JetBrains Mono\',monospace;font-size:10.5px;'
              + 'color:rgba(15,17,22,0.55);">' + escapeHtml(expiryText) + '</div>'
              : '')
          + '</div>';
        layer.bindPopup(popupHtml, { maxWidth: 320 });
        state.nwsLayers[cat].addLayer(layer);
      }

      // Save event-type state on the module so the UI can read it
      state._eventTypesByCategory = eventTypesByCategory;

      // Dispatch event so any listening UI (filter dropdown) can re-render
      try {
        window.dispatchEvent(new CustomEvent('feedMapAlertsUpdated', {
          detail: {
            eventTypesByCategory: eventTypesByCategory,
            hiddenEventTypes: state.hiddenEventTypes
          }
        }));
      } catch (e) {}
    }

    // ─── Filter pill wiring (Push B) ─────────────────────────────────
    // Toggle a category's layer group on/off based on the current
    // nwsFilters state. Called when the existing filter pill click
    // handler updates state.
    function applyFilters() {
      if (!state.map) return;
      var filters = (typeof nwsFilters !== 'undefined') ? nwsFilters : {};
      Object.keys(state.nwsLayers).forEach(function(cat) {
        var layer = state.nwsLayers[cat];
        var on = false;
        if (cat === 'warning')  on = !!filters.warning;
        else if (cat === 'watch')    on = !!filters.watch;
        else if (cat === 'advisory') on = !!filters.advisory;
        if (on) {
          if (!state.map.hasLayer(layer)) layer.addTo(state.map);
        } else {
          if (state.map.hasLayer(layer)) state.map.removeLayer(layer);
        }
      });
    }

    // ─── Viewport-driven story list (Push B) ──────────────────────────
    // Replaces zoom-mode filtering. The story list re-renders to show
    // only reports inside the current Leaflet viewport. Debounced 300ms
    // so panning doesn't churn the DOM.
    var viewportTimer = null;
    function scheduleViewportRender() {
      if (viewportTimer) clearTimeout(viewportTimer);
      viewportTimer = setTimeout(function() {
        renderViewportStoryList();
      }, 300);
    }

    function getReportsInViewport() {
      if (!state.map) return [];
      var bounds = state.map.getBounds();
      var reports = (typeof CREW_REPORTS !== 'undefined') ? CREW_REPORTS.slice() : [];
      var out = [];
      for (var i = 0; i < reports.length; i++) {
        var r = reports[i];
        var latlng = reportToLatLng(r);
        if (!latlng) continue;
        if (bounds.contains(L.latLng(latlng))) {
          out.push(r);
        }
      }
      // Sort newest first, cap at 10
      out.sort(function(a, b) {
        var atA = a.submittedAt ? new Date(a.submittedAt).getTime() : 0;
        var atB = b.submittedAt ? new Date(b.submittedAt).getTime() : 0;
        return atB - atA;
      });
      return out.slice(0, 10);
    }

    function renderViewportStoryList() {
      // Update stats strip
      var stories = getReportsInViewport();
      var countEl = document.getElementById('feed-stats-count');
      var areaEl  = document.getElementById('feed-stats-area');
      if (countEl) {
        countEl.textContent = stories.length + ' report' + (stories.length === 1 ? '' : 's') + ' \u00b7 in view';
      }
      if (areaEl) {
        areaEl.textContent = 'pan or zoom to see more';
      }
      // Update story list — reuses renderFeed's story HTML by calling
      // the existing renderFeed function in a way that respects the new
      // viewport-driven data. Simpler: just call renderFeed which now
      // pulls stories via getReportsInViewport when available.
      try { if (typeof renderFeed === 'function') renderFeed(); } catch (e) {}
    }

    // Once the map is ready, install the viewport handlers + first render
    window.addEventListener('wvFeedMapReady', function() {
      if (!state.map) return;
      renderPulses();
      renderAlerts();
      applyFilters();
      renderViewportStoryList();
      state.map.on('moveend', scheduleViewportRender);
      // If alerts refresh later (the upnext loader fires every 5 min),
      // re-render. We piggyback on the existing crew-map refresh hook.
      var prevRefresh = window._crewMapRefreshOverlay;
      window._crewMapRefreshOverlay = function() {
        try { if (prevRefresh) prevRefresh(); } catch (e) {}
        renderAlerts();
        applyFilters();
      };
    });

    // Public API
    window.WV_FeedMapAPI = {
      init: init,
      map: function() { return state.map; },
      markerLayer: function() { return state.markerLayer; },
      nwsLayers: function() { return state.nwsLayers; },
      ready: function() { return state.ready; },
      recenter: function() {
        if (state.map) state.map.setView(state.homeLatLng, FEED_MAP_DEFAULT_ZOOM);
      },
      setHomeLatLng: function(ll) {
        state.homeLatLng = ll;
        if (state.map) state.map.setView(ll, FEED_MAP_DEFAULT_ZOOM);
      },
      getViewportBounds: function() {
        if (!state.map) return null;
        return state.map.getBounds();
      },
      // Public render hooks for the rest of the code:
      renderPulses: renderPulses,
      renderAlerts: renderAlerts,
      applyFilters: applyFilters,
      getReportsInViewport: getReportsInViewport,
      // Phase 4 (May 17) — radar overlay + per-event-type filter
      setRadar: function(enabled) {
        if (enabled) showFeedRadar();
        else hideFeedRadar();
        try {
          if (enabled) localStorage.setItem(FEED_RADAR_LS_KEY, '1');
          else localStorage.removeItem(FEED_RADAR_LS_KEY);
        } catch (e) {}
      },
      setEventVisibility: function(eventKey, visible) {
        var key = String(eventKey || '').toLowerCase().trim();
        if (!key) return;
        if (visible) delete state.hiddenEventTypes[key];
        else state.hiddenEventTypes[key] = true;
        _feedPersistEventFilter();
        if (typeof renderAlerts === 'function') renderAlerts();
        if (typeof applyFilters === 'function') applyFilters();
      },
      getEventFilter: function() {
        return {
          hidden: Object.assign({}, state.hiddenEventTypes),
          eventTypesByCategory: state._eventTypesByCategory || {}
        };
      },
      isEventHidden: function(eventKey) {
        return !!state.hiddenEventTypes[String(eventKey || '').toLowerCase().trim()];
      }
    };
  })();


  // Render the entire Live Feed for the current zoom: map background,
  // pulses, story list, stats strip, zoom label.
  // Tracks the zoom level last rendered into the map background so we
  // know whether to crossfade (zoom changed) or just replace (nothing
  // changed — initial render or unrelated re-render).
  var lastRenderedZoom = null;

  function renderFeed() {
    var data = getReportsForZoom(currentZoom);

    // 1. Map background — crossfade if the zoom actually changed.
    var bgWrap = $('feed-map-bg-wrap');
    if (bgWrap) {
      if (lastRenderedZoom === currentZoom) {
        // Same zoom — just ensure the bg is present without animation
        if (!bgWrap.firstChild) {
          bgWrap.innerHTML = getMapBackground(currentZoom);
        }
      } else {
        // Zoom changed: keep old bg, append new bg starting at opacity 0,
        // animate old to 0 and new to 1 simultaneously.
        var existing = bgWrap.firstElementChild;
        var temp = document.createElement('div');
        temp.innerHTML = getMapBackground(currentZoom);
        var newBg = temp.firstElementChild;
        if (newBg) {
          newBg.style.opacity = '0';
          bgWrap.appendChild(newBg);
          // Force reflow so the transition runs from 0
          // eslint-disable-next-line no-unused-expressions
          void newBg.offsetWidth;
          newBg.style.opacity = '1';
          if (existing) {
            existing.classList.add('fading-out');
            // Remove old after transition. IIFE captures `existing` by
            // value so subsequent renderFeed invocations don't clobber
            // the variable before this timeout fires.
            (function(node) {
              setTimeout(function() {
                if (node && node.parentNode === bgWrap) {
                  bgWrap.removeChild(node);
                }
              }, 480);
            })(existing);
          }
        } else {
          // Fallback: just replace
          bgWrap.innerHTML = getMapBackground(currentZoom);
        }
        lastRenderedZoom = currentZoom;
      }
      // First render seed
      if (lastRenderedZoom === null) lastRenderedZoom = currentZoom;
    }

    // 2. Pulses — diff-based render. Pulses that are visible in both the
    // old and new zoom keep their DOM element so the CSS top/left
    // transition animates them between positions. Only create new
    // elements for newly-visible reports; remove elements for ones that
    // dropped out of the current zoom.
    var pulsesContainer = $('feed-pulses-container');
    if (pulsesContainer) {
      // Build a Set of report IDs that should be visible at this zoom
      var visibleIds = {};
      for (var i = 0; i < data.pulses.length; i++) {
        visibleIds[data.pulses[i].id] = true;
      }
      // Index existing pulses by report ID
      var existingPulses = pulsesContainer.querySelectorAll('.feed-pulse');
      var existingById = {};
      for (var ex = 0; ex < existingPulses.length; ex++) {
        existingById[existingPulses[ex].getAttribute('data-report-id')] = existingPulses[ex];
      }
      // Remove pulses that should no longer be visible
      for (var rid in existingById) {
        if (!visibleIds[rid]) {
          var el = existingById[rid];
          // Fade out then remove (reuses fading-out pattern from bg)
          el.style.opacity = '0';
          setTimeout(function(node) {
            return function() { if (node.parentNode) node.parentNode.removeChild(node); };
          }(el), 280);
        }
      }
      // Update or create pulses for the new visible set
      for (var p = 0; p < data.pulses.length; p++) {
        var r = data.pulses[p];
        var pos = getReportPos(r, currentZoom);
        var existing = existingById[r.id];
        var pulseAge = getReportAge(r);
        // "Mine" check — a Crew member's own pulses get a distinguishing
        // ring via CSS. Match by authorId on user-submitted reports OR
        // by handle on seeded reports (same logic as the Profile's
        // "your recent reports" filter uses).
        var pulseIsMine = false;
        try {
          var currentUserForPulse = auth.currentUser && auth.currentUser();
          if (currentUserForPulse) {
            if (r.authorId && r.authorId === currentUserForPulse.id) pulseIsMine = true;
            else if (r.author === '@' + (currentUserForPulse.handle || '')) pulseIsMine = true;
          }
        } catch (e) { /* auth not ready yet — ignore */ }
        if (existing) {
          // Just update position — CSS transition handles the smooth move
          existing.style.top = pos.top;
          existing.style.left = pos.left;
          existing.style.opacity = '1';
          existing.setAttribute('data-type', r.type);
          existing.setAttribute('data-stage', pulseAge.stage);
          existing.classList.toggle('is-mine', pulseIsMine);
          existing.classList.toggle('is-met-post', !!r.isMetPost);
        } else {
          var div = document.createElement('div');
          var classNames = 'feed-pulse';
          if (pulseIsMine) classNames += ' is-mine';
          if (r.isMetPost) classNames += ' is-met-post';
          div.className = classNames;
          div.setAttribute('data-type', r.type);
          div.setAttribute('data-stage', pulseAge.stage);
          div.setAttribute('data-report-id', r.id);
          div.style.top = pos.top;
          div.style.left = pos.left;
          div.style.opacity = '0';  // fade in
          // Met posts get a simpler inner structure (no rings/halo — the
          // CSS hides them anyway, but skipping them saves DOM weight).
          if (r.isMetPost) {
            div.innerHTML = '<div class="feed-pulse-dot"></div>';
          } else {
            div.innerHTML = ''
              + '<div class="feed-pulse-halo"></div>'
              + '<div class="feed-pulse-ring"></div>'
              + '<div class="feed-pulse-ring ring-2"></div>'
              + '<div class="feed-pulse-ring ring-3"></div>'
              + '<div class="feed-pulse-dot">' + pulseIconSvg(r.type) + '</div>';
          }
          pulsesContainer.appendChild(div);
          // Force reflow then animate to opacity 1
          // eslint-disable-next-line no-unused-expressions
          void div.offsetWidth;
          div.style.transition = (div.style.transition || '') + ', opacity 320ms ease';
          div.style.opacity = '1';
        }
      }
    }

    // 3. Stats strip
    var countEl = $('feed-stats-count');
    var areaEl = $('feed-stats-area');
    if (countEl) countEl.textContent = data.pulses.length + ' report' + (data.pulses.length === 1 ? '' : 's') + ' \u00b7 last 30 min';
    if (areaEl) {
      areaEl.textContent = currentZoom === 'city' ? 'Lebanon, IN'
        : currentZoom === 'county' ? 'Boone County, IN'
        : currentZoom === 'state' ? 'Indiana statewide'
        : 'Central Indiana';
    }
    var zoomLabel = $('zoom-label');
    if (zoomLabel) {
      zoomLabel.textContent = currentZoom === 'city' ? 'City \u00b7 Lebanon, IN'
        : currentZoom === 'county' ? 'County \u00b7 Boone County, IN'
        : currentZoom === 'state' ? 'State \u00b7 Indiana'
        : 'Region \u00b7 Central Indiana';
    }

    // 4. Update zoom button availability — disable in/out at extremes
    var btnIn = document.querySelector('.zoom-btn[data-zoom-action="in"]');
    var btnOut = document.querySelector('.zoom-btn[data-zoom-action="out"]');
    if (btnIn) btnIn.disabled = (currentZoom === 'city');
    if (btnOut) btnOut.disabled = (currentZoom === 'state');

    // 5. Story list — 10 most recent in current zoom area
    var storyList = $('feed-story-list');
    if (storyList) {
      if (data.stories.length === 0) {
        storyList.innerHTML = '<li class="story-empty">No recent reports in this area. Try zooming out to see more.</li>';
      } else {
        var storiesHtml = '';
        for (var j = 0; j < data.stories.length; j++) {
          var s = data.stories[j];
          var hasPhoto = !!(s.photoDataUrl || (s.hasPhoto && s.photoStyle));
          // Aging — compute fresh / fading / stale stage for this report.
          var age = getReportAge(s);
          var ageClass = ' is-' + age.stage;  // is-fresh, is-fading, is-stale
          var iVerified = verifiedByMe.has(s.id);
          var iOwn = isMyReport(s);

          // Verified-count chip (existing) — shows total verifications
          var verifiedChip = '';
          if (s.verified > 0) {
            verifiedChip = '<span class="story-verified">'
              + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
              + '<polyline points="20 6 9 17 4 12"/></svg>'
              + 'Verified \u00d7' + s.verified + '</span>';
          }

          // Inline verify action — a button when you can verify, a
          // confirmation chip ("✓ You verified") when you have.
          // Hidden entirely on your own reports.
          var verifyAction = '';
          if (iOwn) {
            // Your own report — no button, just an "Your report" tag for clarity.
            verifyAction = '<span class="story-action-mine">Your report</span>';
          } else if (iVerified) {
            // You've verified — show the badge. If within 5 min of your verify,
            // a small "Undo" link appears (only on hover/focus for cleanliness).
            var canUndo = canUnverify(s.id);
            verifyAction = '<span class="story-action-verified">'
              + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
              + '<polyline points="20 6 9 17 4 12"/></svg>'
              + 'You verified'
              + (canUndo
                  ? ' <button type="button" class="story-action-undo" data-unverify-id="' + escapeHtml(s.id) + '" title="Tap mistake? Undo within 5 minutes.">Undo</button>'
                  : '')
              + '</span>';
          } else {
            // Open to verify — the main inline action button.
            verifyAction = '<button type="button" class="story-action-verify" '
              + 'data-verify-id="' + escapeHtml(s.id) + '" '
              + 'aria-label="I see this too. Verify this report.">'
              + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'
              + '<polyline points="20 6 9 17 4 12"/></svg>'
              + '<span>Verify</span>'
              + '</button>';
          }

          // Stale-report "Things are different here" action — appears only on
          // stale reports. One-tap submits a counter-observation at the
          // current user's location. The handler builds the right type
          // (clear/clearing) based on the original report's category.
          var staleAction = '';
          if (age.stage === 'stale' && !iOwn) {
            staleAction = '<button type="button" class="story-action-refresh" '
              + 'data-refresh-id="' + escapeHtml(s.id) + '" '
              + 'data-refresh-type="' + escapeHtml(s.type) + '" '
              + 'title="Conditions have changed? Submit a fresh observation from your location.">'
              + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">'
              + '<polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>'
              + '<span>Things are different here</span>'
              + '</button>';
          }

          // Stale label in the meta row, shown alongside the existing meta.
          var staleLabel = '';
          if (age.stage === 'stale') {
            staleLabel = '<span class="story-stale-label">Needs fresh eyes</span>';
          } else if (age.stage === 'fading') {
            staleLabel = '<span class="story-fading-label">Aging</span>';
          }

          var metaParts = [s.type.charAt(0).toUpperCase() + s.type.slice(1)];
          if (s.streak >= 5) metaParts.push('clarity ' + s.streak + ' streak');
          // Met posts override meta to read "Meteorologist · forecast" instead
          // of the type-based label. Photos aren't supported on Met posts in
          // 6c-1 (commentary, not observation).
          if (s.isMetPost) {
            metaParts = ['Meteorologist forecast'];
          }
          // Photo: real data URL takes priority over the SVG mockup
          var photoHtml;
          if (s.photoDataUrl) {
            photoHtml = '<div class="story-photo">'
              + '<img src="' + escapeHtml(s.photoDataUrl) + '" alt="" '
              +      'style="width:100%;height:100%;object-fit:cover;display:block">'
              + '</div>';
          } else if (s.hasPhoto && s.photoStyle) {
            photoHtml = '<div class="story-photo">' + crewPhotoSvg(s.photoStyle) + '</div>';
          } else {
            photoHtml = '';
          }

          // Met-post styling — adds "is-met-post" to the li for the left-stripe
          // CSS, and prepends a "MET" eyebrow chip in the author line.
          var metPostClass = s.isMetPost ? ' is-met-post' : '';
          var metEyebrow = s.isMetPost
            ? '<span class="met-post-eyebrow-chip">Met</span>'
            : '';
          // Met posts use the brand royal-blue icon instead of a weather icon.
          var iconHtml;
          if (s.isMetPost) {
            iconHtml = '<div class="story-icon" style="background:rgba(65,105,225,0.10);color:#4169E1;">'
              + '<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">'
              + '<circle cx="12" cy="12" r="10"/>'
              + '<line x1="12" y1="8" x2="12" y2="12"/>'
              + '<line x1="12" y1="16" x2="12.01" y2="16"/>'
              + '</svg></div>';
          } else {
            iconHtml = '<div class="story-icon story-' + s.type + '">' + storyIconSvg(s.type) + '</div>';
          }
          // Author display: Met posts show "Meteorologist Name · Coverage"
          // (without the @handle prefix that Crew posts use).
          var authorDisplay = s.isMetPost
            ? escapeHtml(s.author) + ' \u00b7 ' + escapeHtml(s.city || 'Coverage')
            : escapeHtml(s.author) + ' \u00b7 ' + escapeHtml(s.city);

          storiesHtml += '<li class="story' + (hasPhoto ? ' has-photo' : '') + ageClass + metPostClass + '" data-report-id="' + s.id + '">'
            + iconHtml
            + '<div class="story-body">'
            +   '<div class="story-head">'
            +     '<span class="story-author">' + metEyebrow + authorDisplay + '</span>'
            +     '<span class="story-time">' + escapeHtml(formatAgeLabel(age.ageMinutes)) + '</span>'
            +   '</div>'
            +   '<p class="story-text">' + escapeHtml(s.text.length > 110 ? s.text.slice(0, 108) + '\u2026' : s.text) + '</p>'
            +   '<div class="story-meta">' + escapeHtml(metaParts.join(' \u00b7 ')) + ' ' + verifiedChip + ' ' + staleLabel + '</div>'
            +   '<div class="story-actions">' + verifyAction + staleAction + '</div>'
            + '</div>'
            + photoHtml
            + '</li>';
        }
        storyList.innerHTML = storiesHtml;
      }
    }

    // 6. Now-on-air rail — re-render so the cast stays in sync with
    // any newly submitted reports. Wrapped in try/catch because the
    // rail is decorative; a render failure here must not break the
    // feed.
    try {
      if (typeof renderOnAirRail === 'function') renderOnAirRail();
    } catch (e) {}

    // 7. Tonight's story tagline — situational headline above the
    // page title. Re-renders on every feed update so the severity
    // tint and phrasing reflect the current report mix.
    try {
      if (typeof renderTonightStory === 'function') renderTonightStory();
    } catch (e) {}

    // 8. Up Next footer — geographic filter depends on currentZoom,
    // so a zoom change must re-render the footer to match. We don't
    // re-fetch here (that's on the 5-min interval); just re-render
    // with whatever cached alerts we have.
    try {
      if (typeof renderUpNextFooter === 'function') renderUpNextFooter();
    } catch (e) {}

    // 9. Phase 9 Push B — keep the new Leaflet map's pulses in sync
    // with whatever state changed (new report, verify, Met post, etc.).
    // Safe to call before the map is ready (no-op when not ready).
    try {
      if (window.WV_FeedMapAPI && window.WV_FeedMapAPI.ready && window.WV_FeedMapAPI.ready()) {
        window.WV_FeedMapAPI.renderPulses();
      }
    } catch (e) {}
  }

  // Step the zoom one tier in or out. Edge cases (already at extreme)
  // do nothing — handled by disabling the button in renderFeed.
  function stepZoom(direction) {
    var tiers = ['city', 'county', 'region', 'state'];
    var idx = tiers.indexOf(currentZoom);
    if (direction === 'in' && idx > 0) {
      currentZoom = tiers[idx - 1];
    } else if (direction === 'out' && idx < tiers.length - 1) {
      currentZoom = tiers[idx + 1];
    } else {
      return;  // at extreme already
    }
    renderFeed();
  }


  // ── Render the ticket screen ─────────────────────────────────────────────

  /**
   * Build the tiered conditions block for the ticket card. Real values
   * come from the reading where available (temp, humidity, wind, precip,
   * sky); the rest are synthesized deterministically from the ticket
   * seed so a given plan always shows consistent values across reloads.
   *
   * Returns HTML for three tiers:
   *   - Top (4 big cells):   Temp · Wind · Precipitation · Sky
   *   - Middle (4 medium):   Visibility · UV · Humidity · Sunrise/Sunset
   *   - Bottom (4 small):    Pressure · Air Quality · Pollen · Feels Like
   *
   * "Feels Like" intelligently switches between Wind Chill (cold) and
   * Heat Index (hot) based on the temperature.
   *
   * Real implementation note: in production these all come from
   * NWS/Open-Meteo + an air quality API + a pollen API + sun ephemeris.
   * The shape is locked so wiring is a drop-in replacement.
   */
  function buildConditionsTiers(ticket, reading) {
    // ── Pull real values where we have them ──
    var tempF = (reading.avgTempF != null) ? Math.round(reading.avgTempF) : null;
    var windStr = '\u2014';
    var precipStr = '\u2014';
    var skyStr = '\u2014';
    var humidityPct = (reading.avgHumidityPct != null) ? Math.round(reading.avgHumidityPct) : null;
    if (reading.stats) {
      for (var i = 0; i < reading.stats.length; i++) {
        var s = reading.stats[i];
        if (s.key === 'Wind') windStr = s.value;
        else if (s.key === 'Precipitation') precipStr = s.value;
        else if (s.key === 'Sky') skyStr = s.value;
      }
    }

    // ── Deterministic synthesis for the rest ──
    // Seed off the ticket id so reloads give the same number.
    var seed = 0;
    var sid = String(ticket.id || ticket.seed || 'wv');
    for (var k = 0; k < sid.length; k++) seed = ((seed << 5) - seed) + sid.charCodeAt(k);
    seed = Math.abs(seed);
    var rand = function(min, max) {
      seed = (seed * 9301 + 49297) % 233280;
      var x = seed / 233280;
      return min + x * (max - min);
    };

    // Temp fallback: if we don't have a real one, synthesize a moderate
    // value depending on month so demos look reasonable year-round.
    if (tempF == null) {
      var month = new Date().getMonth(); // 0-11
      var seasonal = [38, 42, 52, 62, 72, 80, 84, 82, 74, 62, 50, 40][month];
      tempF = Math.round(seasonal + rand(-6, 8));
    }

    // Humidity fallback
    if (humidityPct == null) humidityPct = Math.round(rand(35, 75));

    // ── Visibility — strong correlation with sky/precip ──
    var visMi;
    if (skyStr.indexOf('Active') !== -1 || skyStr.indexOf('Storm') !== -1) {
      visMi = (rand(2, 6)).toFixed(1);
    } else if (skyStr.indexOf('Building') !== -1 || skyStr.indexOf('Cells') !== -1) {
      visMi = (rand(6, 9)).toFixed(1);
    } else if (skyStr.indexOf('Fog') !== -1 || skyStr.indexOf('Haze') !== -1) {
      visMi = (rand(0.5, 3)).toFixed(1);
    } else {
      visMi = '10+';
    }

    // ── UV index — drops to 0 for storms and heavy clouds ──
    var statusKey = reading.statusKey;
    var uvBase = (tempF >= 75) ? 7 : (tempF >= 60) ? 5 : 3;
    if (statusKey === 'risk') uvBase = Math.max(1, uvBase - 4);
    else if (statusKey === 'caution') uvBase = Math.max(2, uvBase - 1);
    var uv = Math.round(uvBase);
    var uvLabel = (uv <= 2) ? 'Low' : (uv <= 5) ? 'Moderate' : (uv <= 7) ? 'High' : (uv <= 10) ? 'Very High' : 'Extreme';

    // ── Sunrise/Sunset — computed from a simple cosine model based
    //    on day-of-year. Real impl uses a proper ephemeris library.
    //    This is good enough to be plausible (within ~15 min). ──
    var dayOfYear = (function() {
      var now = new Date();
      var start = new Date(now.getFullYear(), 0, 0);
      return Math.floor((now - start) / 86400000);
    })();
    var solarAngle = Math.cos((dayOfYear - 172) / 365 * 2 * Math.PI); // 1=summer, -1=winter
    var sunriseMin = Math.round(360 + solarAngle * 90);  // 4:30am summer, 7:30am winter
    var sunsetMin  = Math.round(1170 - solarAngle * 90); // 9:30pm summer, 4:30pm winter
    var fmtTime = function(min) {
      var h = Math.floor(min / 60);
      var m = min % 60;
      var ampm = h >= 12 ? 'PM' : 'AM';
      var h12 = h % 12 || 12;
      return h12 + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    };
    var sunriseStr = fmtTime(sunriseMin);
    var sunsetStr = fmtTime(sunsetMin);

    // ── Pressure — millibar, with realistic ranges for verdict ──
    var pressure;
    if (statusKey === 'risk')      pressure = Math.round(rand(992, 1004));
    else if (statusKey === 'caution') pressure = Math.round(rand(1006, 1014));
    else                              pressure = Math.round(rand(1015, 1024));
    var pressureTrend = (statusKey === 'risk') ? 'falling' : (statusKey === 'caution' ? 'falling' : 'steady');

    // ── Air quality — AQI, with category ──
    var aqi = Math.round(rand(28, 65));
    if (statusKey === 'risk') aqi += 10;
    var aqiLabel;
    if      (aqi <= 50)  aqiLabel = 'Good';
    else if (aqi <= 100) aqiLabel = 'Moderate';
    else if (aqi <= 150) aqiLabel = 'Unhealthy for Sensitive Groups';
    else                 aqiLabel = 'Unhealthy';

    // ── Pollen — month-driven baseline ──
    var pollenMonths = [1, 2, 5, 8, 8, 7, 6, 5, 7, 5, 2, 1];
    var pollenBase = pollenMonths[(new Date()).getMonth()];
    var pollenLabel = (pollenBase <= 2) ? 'Low' : (pollenBase <= 5) ? 'Moderate' : (pollenBase <= 7) ? 'High' : 'Very High';

    // ── Feels Like — Wind Chill below 50, Heat Index above 80 ──
    var feelsLikeKey, feelsLikeVal, feelsLikeSub;
    var windMph = reading.rawWindMph || 8;
    if (tempF <= 50 && windMph >= 5) {
      var wc = 35.74 + 0.6215 * tempF - 35.75 * Math.pow(windMph, 0.16) + 0.4275 * tempF * Math.pow(windMph, 0.16);
      feelsLikeKey = 'Wind Chill';
      feelsLikeVal = Math.round(wc) + '\u00b0F';
      feelsLikeSub = 'feels colder';
    } else if (tempF >= 80 && humidityPct >= 40) {
      // Simplified heat index — Steadman's formula approximation
      var hi = -42.379 + 2.04901523 * tempF + 10.14333127 * humidityPct
        - 0.22475541 * tempF * humidityPct - 0.00683783 * tempF * tempF
        - 0.05481717 * humidityPct * humidityPct + 0.00122874 * tempF * tempF * humidityPct
        + 0.00085282 * tempF * humidityPct * humidityPct;
      feelsLikeKey = 'Heat Index';
      feelsLikeVal = Math.round(hi) + '\u00b0F';
      feelsLikeSub = 'feels hotter';
    } else {
      feelsLikeKey = 'Feels Like';
      feelsLikeVal = tempF + '\u00b0F';
      feelsLikeSub = 'as the air';
    }

    // Sky verifier (top tier shows attribution)
    var skyVerifier = '';
    if (reading.stats) {
      for (var j = 0; j < reading.stats.length; j++) {
        if (reading.stats[j].key === 'Sky') skyVerifier = reading.stats[j].verifier;
      }
    }

    // ── Build HTML ──
    // Reordered per Michael's spec:
    //   Row 1 (top, large):    Sky · Temperature · Feels Like · Wind
    //   Row 2 (middle, medium): UV Index · Humidity · Visibility · Pressure
    //   Row 3 (bottom, small):  Sunrise/Sunset · Precipitation · Air Quality · Pollen
    // Rationale on the changes Michael didn't directly call out:
    //   - UV Index opens Row 2 (his Row 2 listed Wind twice; UV is the
    //     uncontroversial slot to fill)
    //   - Precipitation drops to Row 3 since Wind took its Row 1 slot
    //     (everything is still present; just smaller)
    //   - Pressure stays in Row 2 only (his Row 3 had Pressure twice)
    var html =
      '<div class="ticket-conditions">'
      + '<p class="ticket-conditions-eyebrow">The full reading</p>'

      // Row 1: Sky · Temperature · Feels Like · Wind
      + '<div class="ticket-conditions-tier ticket-conditions-top">'
      +   '<div class="ticket-cond ticket-cond-top">'
      +     '<p class="ticket-cond-label">Sky</p>'
      +     '<div class="ticket-cond-value" style="font-size: 17px;">' + escapeHtml(skyStr) + '</div>'
      +     '<p class="ticket-cond-sub">' + escapeHtml(skyVerifier || 'observed') + '</p>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-top">'
      +     '<p class="ticket-cond-label">Temperature</p>'
      +     '<div class="ticket-cond-value">' + tempF + '\u00b0F</div>'
      +     '<p class="ticket-cond-sub">at your window</p>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-top">'
      +     '<p class="ticket-cond-label">' + feelsLikeKey + '</p>'
      +     '<div class="ticket-cond-value">' + feelsLikeVal + '</div>'
      +     '<p class="ticket-cond-sub">' + escapeHtml(feelsLikeSub) + '</p>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-top">'
      +     '<p class="ticket-cond-label">Wind</p>'
      +     '<div class="ticket-cond-value">' + escapeHtml(windStr) + '</div>'
      +     '<p class="ticket-cond-sub">sustained / gusts</p>'
      +   '</div>'
      + '</div>'

      // Row 2: UV Index · Humidity · Visibility · Pressure
      + '<div class="ticket-conditions-tier ticket-conditions-middle">'
      +   '<div class="ticket-cond ticket-cond-mid">'
      +     '<p class="ticket-cond-label">UV Index</p>'
      +     '<div class="ticket-cond-value">' + uv + ' \u00b7 ' + uvLabel + '</div>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-mid">'
      +     '<p class="ticket-cond-label">Humidity</p>'
      +     '<div class="ticket-cond-value">' + humidityPct + '%</div>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-mid">'
      +     '<p class="ticket-cond-label">Visibility</p>'
      +     '<div class="ticket-cond-value">' + visMi + ' mi</div>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-mid">'
      +     '<p class="ticket-cond-label">Pressure</p>'
      +     '<div class="ticket-cond-value">' + pressure + ' mb \u00b7 ' + pressureTrend + '</div>'
      +   '</div>'
      + '</div>'

      // Row 3: Sunrise/Sunset · Precipitation · Air Quality · Pollen
      + '<div class="ticket-conditions-tier ticket-conditions-bottom">'
      +   '<div class="ticket-cond ticket-cond-sm">'
      +     '<p class="ticket-cond-label">Sun</p>'
      +     '<div class="ticket-cond-value" style="font-size: 12px;">'
      +       sunriseStr + ' \u2192 ' + sunsetStr
      +     '</div>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-sm">'
      +     '<p class="ticket-cond-label">Precipitation</p>'
      +     '<div class="ticket-cond-value">' + escapeHtml(precipStr) + '</div>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-sm">'
      +     '<p class="ticket-cond-label">Air Quality</p>'
      +     '<div class="ticket-cond-value">' + aqi + ' \u00b7 ' + aqiLabel + '</div>'
      +   '</div>'
      +   '<div class="ticket-cond ticket-cond-sm">'
      +     '<p class="ticket-cond-label">Pollen</p>'
      +     '<div class="ticket-cond-value">' + pollenLabel + '</div>'
      +   '</div>'
      + '</div>'

      // ── Subscriber-continuity block ─────────────────────────────
      // Answers the question "the free search gives a lot, why pay?"
      // by surfacing what subscribers GET CONTINUOUSLY that this
      // one-shot snapshot doesn't: a daily brief, threshold alerts,
      // severe coverage. Frames the free search as a snapshot vs.
      // subscription as ongoing care.
      //
      // Calm, not pushy — the reviewer praised the ticket's clean
      // design and we don't want to break that. Sits as a quiet
      // "want this every day?" prompt, not a sales banner.
      + '<div class="ticket-continuity">'
      +   '<div class="ticket-continuity-head">'
      +     '<span class="ticket-continuity-eyebrow">'
      +       '<span class="ticket-continuity-eyebrow-dot"></span>'
      +       'This is a snapshot'
      +     '</span>'
      +   '</div>'
      +   '<h3 class="ticket-continuity-title">Want this every day, without asking?</h3>'
      +   '<p class="ticket-continuity-body">Subscribers don\u2019t hunt for forecasts. We watch the weather for them.</p>'
      +   '<ul class="ticket-continuity-features">'
      +     '<li>'
      +       '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>'
      +       '<span><strong>Morning brief</strong> for your saved location, delivered when you want to read it.</span>'
      +     '</li>'
      +     '<li>'
      +       '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>'
      +       '<span><strong>Threshold alerts</strong> &mdash; wind at 25 mph, temp below freezing, your call.</span>'
      +     '</li>'
      +     '<li>'
      +       '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 4 6v6c0 5 3.5 9.5 8 10 4.5-.5 8-5 8-10V6l-8-4z"/></svg>'
      +       '<span><strong>Severe weather</strong> handled before you have to ask.</span>'
      +     '</li>'
      +   '</ul>'
      +   '<div class="ticket-continuity-cta-row">'
      +     '<button type="button" class="ticket-continuity-cta" data-show-screen="screen-pricing">'
      +       '<span>See subscription plans</span>'
      +       '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>'
      +     '</button>'
      +     '<span class="ticket-continuity-price">From $30/month &middot; cancel anytime</span>'
      +   '</div>'
      + '</div>'

      + '</div>';

    return html;
  }

  function renderTicket(ticket, reading) {
    // Defensive: if no reading was passed (shouldn't happen with the new flow,
    // but guard anyway for robustness), fall back to synthetic.
    if (!reading) {
      reading = buildSyntheticReading(ticket);
    }
    var statusKey = reading.statusKey;

    var now = new Date();
    var lockHour = now.getHours() % 12 || 12;
    var lockMin = now.getMinutes();
    var lockTime = lockHour + ':' + (lockMin < 10 ? '0' : '') + lockMin;
    var dateStr = now.toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' });

    var statsHtml = '';
    for (var i = 0; i < reading.stats.length; i++) {
      var s = reading.stats[i];
      var bigClass = (s.value.length <= 4) ? 'stat-value-big' : 'stat-value-small';
      statsHtml += ''
        + '<div class="stat-cell">'
        +   '<div class="stat-label">' + escapeHtml(s.key) + '</div>'
        +   '<div class="stat-value ' + bigClass + '">' + escapeHtml(s.value) + '</div>'
        +   '<div class="stat-verifier">'
        +     '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>'
        +     '<span>' + escapeHtml(s.verifier) + '</span>'
        +   '</div>'
        + '</div>';
    }

    // Build the notification "tail" — the part after "is Clear/Caution/Risk"
    // Consistency rule: the notification should reflect THE SAME primary signal
    // the ticket headline and reasons block are showing. If wind is the lead
    // reason, the notif says "watch the wind." If scattered storms drove the
    // call, the notif says "scattered storms nearby." We derive a concise
    // 4-7 word phrase from the first reason, falling back to the generic
    // STATUS table tail when there are no reasons (clear days).
    var notifTail = reading.tail; // generic fallback ("." for clear, " — re-plan recommended." for risk)
    if (reading.reasons && reading.reasons.length > 0) {
      // Take the first reason and shorten to a notification-appropriate phrase
      notifTail = ' — ' + shortenReasonForNotif(reading.reasons[0]) + '.';
    }
    // Notification format change (v2.5): we dropped "Verified" because this is
    // a forward-looking forecast, not a past-tense observation. "Verified" is
    // reserved for the post-meteorologist-review state in Live Valet flow.
    var notifHtml = ''
      + 'Your ' + escapeHtml(ticket.scheduledShort) + ' '
      + escapeHtml(ticket.industryLabel) + ' in '
      + escapeHtml(ticket.city) + ' is '
      + '<span class="' + reading.verbClass + '">' + escapeHtml(reading.verb) + '</span>'
      + escapeHtml(notifTail);

    // Source badge — small "Live data" / "Demo data" indicator near the AI row
    var sourceBadge = '';
    if (reading.source === 'live') {
      sourceBadge = ''
        + '<span class="source-badge source-badge-live" title="Powered by Open-Meteo · live forecast">'
        +   '<span class="source-badge-dot"></span>'
        +   '<span>Live</span>'
        + '</span>';
    } else {
      sourceBadge = ''
        + '<span class="source-badge source-badge-fallback" title="Live data unavailable — using cached estimate">'
        +   '<span>Cached</span>'
        + '</span>';
    }

    // ── Alternates hint: "Did you mean a different Lebanon?" ──
    // Only renders when the user did NOT specify a state and the geocoder
    // returned multiple US matches. We show up to 3 alternates as small
    // chips the user can tap to re-resolve the ticket against that location.
    var alternatesHtml = '';
    if (ticket.alternates && ticket.alternates.length > 0) {
      var altItems = ticket.alternates.map(function(alt, idx) {
        var label = alt.name + (alt.state ? (', ' + alt.state) : '');
        var subdetail = alt.zip ? alt.zip : '';
        return ''
          + '<button type="button" class="alternate-chip" data-alt-idx="' + idx + '"'
          +   ' data-alt-name="' + escapeHtml(alt.name) + '"'
          +   ' data-alt-state="' + escapeHtml(alt.state) + '"'
          +   ' data-alt-zip="' + escapeHtml(alt.zip) + '"'
          +   ' data-alt-lat="' + alt.latitude + '"'
          +   ' data-alt-lng="' + alt.longitude + '">'
          +   '<span class="alternate-chip-label">' + escapeHtml(label) + '</span>'
          +   (subdetail ? '<span class="alternate-chip-detail">' + escapeHtml(subdetail) + '</span>' : '')
          + '</button>';
      }).join('');
      alternatesHtml = ''
        + '<div class="alternates-hint">'
        +   '<span class="alternates-hint-label">Not the right ' + escapeHtml(ticket.alternates[0].name) + '?</span>'
        +   '<div class="alternates-list">' + altItems + '</div>'
        + '</div>';
    }

    // ── Route stops block (only renders for multi-location tickets) ──
    // Shows the journey as a row of stop chips, each colored by that stop's
    // status. Each chip carries the projected arrival time computed from
    // the heuristic drive-time estimate (see estimateDriveHours).
    var routeHtml = '';
    if (ticket.resolvedStops && ticket.resolvedStops.length > 1) {
      var stopChips = ticket.resolvedStops.map(function(s, idx) {
        var label = s.name + (s.state ? (', ' + s.state) : '');
        var statusClass = 'route-stop-' + s.status;
        var connector = (idx < ticket.resolvedStops.length - 1)
          ? '<svg class="route-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>'
          : '';
        var arrivalLine = s.arrivalLabel
          ? '<span class="route-stop-arrival">' + (idx === 0 ? 'depart' : 'arrive') + ' ~' + escapeHtml(s.arrivalLabel) + '</span>'
          : '';
        return '<span class="route-stop ' + statusClass + '">'
             + '<span class="route-stop-dot"></span>'
             + '<span class="route-stop-info">'
             +   '<span class="route-stop-name">' + escapeHtml(label) + '</span>'
             +   arrivalLine
             + '</span>'
             + '</span>' + connector;
      }).join('');
      // Compose the route-block label with total drive time when available
      var routeLabel = 'Your route';
      if (ticket.totalDriveHours && ticket.totalDriveHours >= 0.5) {
        var hrs = ticket.totalDriveHours;
        var dt = hrs >= 1
          ? Math.round(hrs * 10) / 10 + (hrs >= 1.95 ? ' hours' : ' hour') + ' drive'
          : Math.round(hrs * 60) + ' min drive';
        routeLabel = 'Your route · ~' + dt;
      }
      routeHtml = ''
        + '<div class="route-block">'
        +   '<div class="route-block-label">' + escapeHtml(routeLabel) + '</div>'
        +   '<div class="route-stops">' + stopChips + '</div>'
        + '</div>';
    }

    // ── Assumption banner (only renders when we had to assume a time) ──
    // When the user types "Concrete pour in Lebanon, IN" with no time, we
    // don't refuse to render — that would block contractors typing fast.
    // Instead we use a sensible default time (9 AM) AND tell them what we
    // assumed. This is the difference between silently-wrong and obviously-
    // defaulting. (Missing-location is handled separately by an error
    // screen, since defaulting to any single city would be wrong for most
    // users of a nationwide product.)
    var assumptionBannerHtml = '';
    if (ticket.timeFound === false) {
      // Build a contextualized example using the ticket's actual location
      // so the suggestion isn't a misleading promo for a different city.
      var exampleLocation = ticket.city
        ? ('in ' + ticket.city + (ticket.stateCode ? ', ' + ticket.stateCode : ''))
        : 'in Lebanon, IN';
      var assumptionText = 'Heads up — you didn\'t specify a time, so we used '
        + '<strong>' + escapeHtml(ticket.scheduledShort) + '</strong>. '
        + 'For best results, try something like <em>"'
        + escapeHtml(ticket.industryLabel || 'work') + ' at 10 AM '
        + escapeHtml(exampleLocation) + '."</em>';
      assumptionBannerHtml = ''
        + '<div class="assumption-banner">'
        +   '<svg class="assumption-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">'
        +     '<circle cx="12" cy="12" r="10"/><path d="M12 8v4"/><path d="M12 16h.01"/>'
        +   '</svg>'
        +   '<div class="assumption-text">' + assumptionText + '</div>'
        + '</div>';
    }

    // ── NWS alert ribbon (renders if a primary alert is present) ──
    // The ribbon is integrated into the verdict zone — sits visually
    // attached to the verdict block, not as a separate card. Click the
    // ribbon head to expand for full alert text. Pro-tier-styled.
    var alertHtml = '';
    if (reading.primaryAlert) {
      var a = reading.primaryAlert;
      var sevLower = (a.severity || 'unknown').toLowerCase();
      var alertId = 'ticket-alert-ribbon-' + Math.random().toString(36).slice(2, 8);
      alertHtml = ''
        + '<div class="ticket-alert-ribbon alert-' + escapeHtml(sevLower) + '" id="' + alertId + '">'
        +   '<button type="button" class="ticket-alert-ribbon-head" data-alert-toggle="' + alertId + '" aria-expanded="false">'
        +     '<svg class="ticket-alert-ribbon-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        +       '<path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"/>'
        +       '<line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>'
        +     '</svg>'
        +     '<span class="ticket-alert-ribbon-label">' + escapeHtml(a.severity || 'Active') + '</span>'
        +     '<span class="ticket-alert-ribbon-event">' + escapeHtml(a.event)
        +       '<span class="ticket-alert-ribbon-pro-pill">Pro context</span>'
        +     '</span>'
        +     '<svg class="ticket-alert-ribbon-chev" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
        +       '<polyline points="6 9 12 15 18 9"/>'
        +     '</svg>'
        +   '</button>'
        +   '<div class="ticket-alert-ribbon-body">'
        +     (a.headline ? '<p class="ticket-alert-ribbon-headline">' + escapeHtml(a.headline) + '</p>' : '')
        +     '<div class="ticket-alert-ribbon-source">via NWS &middot; api.weather.gov</div>'
        +   '</div>'
        + '</div>';
    }

    // ── "Why this call" reasoning block ──
    // Only renders if we have at least one reason. For Clear status with no
    // reasons, we deliberately omit it — silence reads as confidence.
    var reasonsHtml = '';
    if (reading.reasons && reading.reasons.length > 0) {
      var topReasons = reading.reasons.slice(0, 3);
      var reasonItems = topReasons.map(function(r) {
        return '<li class="reason-item"><span class="reason-bullet"></span><span>' + escapeHtml(r) + '</span></li>';
      }).join('');
      reasonsHtml = ''
        + '<div class="reasons-block">'
        +   '<div class="reasons-label">Why this call</div>'
        +   '<ul class="reasons-list">' + reasonItems + '</ul>'
        +   (reading.lookaheadConcern ? '<p class="reasons-lookahead">' + escapeHtml(reading.lookaheadConcern) + '</p>' : '')
        + '</div>';
    } else if (reading.lookaheadConcern) {
      // Even on a Clear-in-window status, surface a lookahead concern if it exists
      reasonsHtml = ''
        + '<div class="reasons-block">'
        +   '<div class="reasons-label">Heads up</div>'
        +   '<p class="reasons-lookahead">' + escapeHtml(reading.lookaheadConcern) + '</p>'
        + '</div>';
    }

    // ── Crew evidence — what nearby Crew members are seeing right now ──
    // The Crew layer is supplementary, not authoritative. We show what the
    // Crew is reporting near the user's location so they can weigh it
    // alongside the AI verdict, but the Crew doesn't currently change the
    // verdict itself. (When a reputation system exists in production,
    // verified reports from trusted contributors WILL be allowed to nudge
    // the status — but for the prototype, restraint.)
    //
    // Three states:
    //   1. Future-dated ticket → render nothing (Crew can't corroborate
    //      Friday's forecast from observations made today)
    //   2. Today's ticket WITH matching reports → show the section with
    //      a count, a few example reports, and a "tap to see all" affordance
    //   3. Today's ticket with NO matches → show the section with empty-state
    //      copy that's honest: "No Crew reports nearby in the last 30 min"
    //      so the user knows we looked
    var crewEvidence = findNearbyCrewReports(ticket);
    var crewHtml = '';
    if (!crewEvidence.futureDate) {
      var crewLabel = '<div class="crew-evidence-label">'
        + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'
        + '<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/>'
        + '<circle cx="9" cy="7" r="4"/>'
        + '<path d="M22 21v-2a4 4 0 0 0-3-3.87"/>'
        + '<path d="M16 3.13a4 4 0 0 1 0 7.75"/>'
        + '</svg>'
        + '<span>Valet Crew nearby</span>'
        + '</div>';

      if (crewEvidence.reports.length === 0) {
        // Empty state — honest about what we looked for
        crewHtml = ''
          + '<div class="crew-evidence-block crew-evidence-empty">'
          +   crewLabel
          +   '<p class="crew-evidence-empty-text">'
          +     'No Crew reports near you in the last ' + crewEvidence.windowMinutes + ' minutes. '
          +     'The forecast above is based on AI models and weather data alone.'
          +   '</p>'
          + '</div>';
      } else {
        // Active state — count + a few sample reports + tap-through chip
        var verifiedCount = 0;
        for (var ce = 0; ce < crewEvidence.reports.length; ce++) {
          if (crewEvidence.reports[ce].verified > 0) verifiedCount++;
        }
        var summaryLine = crewEvidence.totalCount + ' Crew '
          + (crewEvidence.totalCount === 1 ? 'report' : 'reports')
          + ' in the area in the last ' + crewEvidence.windowMinutes + ' min'
          + (verifiedCount > 0
              ? ' &middot; ' + verifiedCount + ' verified'
              : '');
        // List up to 3 of the most recent reports as compact chips
        var reportChips = crewEvidence.reports.slice(0, 3).map(function(r) {
          var verifiedTick = r.verified > 0
            ? '<span class="crew-evidence-chip-verified" aria-label="Verified">'
              + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">'
              + '<polyline points="20 6 9 17 4 12"/></svg></span>'
            : '';
          var typeLabel = (r.type || '').charAt(0).toUpperCase() + (r.type || '').slice(1);
          return ''
            + '<div class="crew-evidence-chip" data-report-id="' + escapeHtml(r.id) + '">'
            +   '<span class="crew-evidence-chip-type type-' + escapeHtml(r.type) + '">'
            +     escapeHtml(typeLabel)
            +   '</span>'
            +   '<span class="crew-evidence-chip-author">' + escapeHtml(r.author) + '</span>'
            +   '<span class="crew-evidence-chip-where">' + escapeHtml(r.city) + '</span>'
            +   '<span class="crew-evidence-chip-when">' + escapeHtml(r.timeAgo) + '</span>'
            +   verifiedTick
            + '</div>';
        }).join('');
        crewHtml = ''
          + '<div class="crew-evidence-block">'
          +   crewLabel
          +   '<p class="crew-evidence-summary">' + summaryLine + '</p>'
          +   '<div class="crew-evidence-list">' + reportChips + '</div>'
          +   '<button type="button" class="crew-evidence-cta" data-show-screen="screen-crew-feed">'
          +     '<span>See the Crew feed</span>'
          +     '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
          +     ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
          +     '<path d="M9 18l6-6-6-6"/></svg>'
          +   '</button>'
          + '</div>';
      }
    }

    // ── Sources roster (footer) — show every contributing data source ──
    var sourcesHtml = '';
    if (reading.sources && reading.sources.length > 0) {
      var sourceItems = reading.sources.map(function(src) {
        return ''
          + '<div class="sources-item">'
          +   '<span class="sources-item-name">' + escapeHtml(src.name) + '</span>'
          +   '<span class="sources-item-detail">' + escapeHtml(src.detail) + '</span>'
          + '</div>';
      }).join('');
      sourcesHtml = ''
        + '<div class="sources-block">'
        +   '<div class="sources-label">'
        +     '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"/><path d="M3.6 9h16.8"/><path d="M3.6 15h16.8"/><path d="M11.5 3a17 17 0 0 0 0 18"/><path d="M12.5 3a17 17 0 0 1 0 18"/></svg>'
        +     '<span>Sources</span>'
        +   '</div>'
        +   '<div class="sources-list">' + sourceItems + '</div>'
        + '</div>';
    }

    // Build the issue-date string for the masthead. Premium tickets
    // declare when they were issued — like a real reservation card.
    var issuedShort = now.toLocaleDateString('en-US', { month: 'short', day: 'numeric' })
                       + ' \u00b7 ' + lockTime + ' ' + (now.getHours() >= 12 ? 'PM' : 'AM');

    // Strip the city's state/zip suffix for the masthead reference area
    // since we already display them inline below the reference number.
    // Keeps the upper-right tidy.
    var refLocation = ticket.area || ticket.city || '';

    // Map verdict to a stamp value with appropriate ticket-character.
    // "CLEAR" / "CAUTION" / "RISK" — short, declarative, all-caps.
    var stampValue = (statusKey === 'clear') ? 'CLEAR'
                   : (statusKey === 'caution') ? 'CAUTION'
                   : 'RISK';

    $('ticket-content').innerHTML = ''
      + '<div class="ticket-card" data-status="' + escapeHtml(statusKey) + '">'
      +   '<span class="ticket-watermark" aria-hidden="true">WV</span>'

      // ── Masthead: WeatherValet wordmark + tagline + issue date,
      //    plus the ticket reference number on the right ──
      +   '<div class="ticket-masthead">'
      +     '<div class="ticket-masthead-brand">'
      +       '<span class="ticket-masthead-wordmark">'
      +         'WeatherValet<span class="ticket-masthead-wordmark-em">.ai</span>'
      +       '</span>'
      +       '<span class="ticket-masthead-tagline">Forecast Ticket</span>'
      +     '</div>'
      +     '<div class="ticket-reference">'
      +       '<span class="ticket-reference-label">Reference</span>'
      +       '<span class="ticket-reference-value">#' + escapeHtml(ticket.id) + '</span>'
      +       '<span class="ticket-reference-location">' + escapeHtml(refLocation) + '</span>'
      +     '</div>'
      +   '</div>'

      // ── Old ticket-head retained but hidden via CSS so any existing
      //    JS that references status-pill / ticket-meta still finds them ──
      +   '<div class="ticket-head">'
      +     '<div class="status-pill ' + reading.pillClass + '">'
      +       '<span class="status-pill-dot"></span>'
      +       escapeHtml(reading.label)
      +     '</div>'
      +     '<div class="ticket-meta">'
      +       '<span>Plan #' + escapeHtml(ticket.id) + '</span>'
      +       '<span class="ticket-meta-divider"></span>'
      +       '<span>' + escapeHtml(ticket.area) + '</span>'
      +     '</div>'
      +   '</div>'

      // ── New: status stamp ───────────────────────────
      +   '<div class="ticket-stamp">'
      +     '<span class="ticket-stamp-label">Status</span>'
      +     '<span class="ticket-stamp-value">' + escapeHtml(stampValue) + '</span>'
      +   '</div>'
      +   alternatesHtml
      +   routeHtml
      +   assumptionBannerHtml
      +   '<div class="ticket-plan">'
      +     '<span class="ticket-plan-label">Your plan</span>'
      +     '<span class="ticket-plan-text">' + escapeHtml(ticket.query) + '</span>'
      +   '</div>'
      +   '<h2 class="ticket-verdict">' + escapeHtml(reading.summary) + '</h2>'
      +   '<div class="ticket-confidence-band" data-band-tier="' + confidenceBand(reading.confidence).tier + '">'
      +     '<span class="ticket-confidence-band-label">' + confidenceBand(reading.confidence).label + '</span>'
      +     '<span class="ticket-confidence-band-sep">&middot;</span>'
      +     '<span class="ticket-confidence-band-action">' + confidenceBand(reading.confidence).action + '</span>'
      +     '<span class="ticket-confidence-band-pct">' + reading.confidence + '%</span>'
      +   '</div>'
      +   alertHtml

      // ── AI PARAGRAPH (the hero) ─────────────────────────────────────
      // Renders in a loading state initially. After the ticket is in the
      // DOM, fetchAiExplanation runs in the background and swaps the
      // skeleton for the real paragraph when Gemini responds. If the
      // backend is unreachable or rate-limited, the fallback paragraph
      // (returned by the backend itself) appears instead — never broken.
      +   '<div class="ai-paragraph-block" id="ai-paragraph-block">'
      +     '<div class="ai-paragraph-eyebrow">'
      +       '<span class="ai-paragraph-eyebrow-dot"></span>'
      +       '<span>For your plan</span>'
      +     '</div>'
      +     '<div class="ai-paragraph-content" id="ai-paragraph-content">'
      +       '<div class="ai-paragraph-loading">'
      +         '<div class="ai-paragraph-skeleton"></div>'
      +         '<div class="ai-paragraph-skeleton"></div>'
      +         '<div class="ai-paragraph-skeleton"></div>'
      +       '</div>'
      +     '</div>'
      +   '</div>'

      +   '<div class="ticket-rule"></div>'
      +   buildConditionsTiers(ticket, reading)

      // ── "How we got there" — the AI reasoning, collapsed by default ──
      // Most users won't open this. The ones who want to verify the AI's
      // reasoning can. Demoting this from always-visible to expandable
      // is part of the "paragraph is the hero" reframing — the AI's
      // bullet-list reasoning is supporting evidence, not the headline.
      +   '<div class="reasons-collapse" id="reasons-collapse">'
      +     '<button type="button" class="reasons-collapse-toggle" id="reasons-collapse-toggle" aria-expanded="false">'
      +       '<svg class="reasons-collapse-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor"'
      +       ' stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
      +       '<path d="M9 18l6-6-6-6"/></svg>'
      +       '<span>How we got there</span>'
      +     '</button>'
      +     '<div class="reasons-collapse-panel">'
      +       '<div class="reasons-collapse-inner">'
      +         '<div class="reasons-collapse-ai-row">'
      +           '<span class="ai-badge">'
      +             '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">'
      +               '<rect x="4" y="4" width="16" height="16" rx="2"/><rect x="9" y="9" width="6" height="6"/>'
      +               '<path d="M15 2v2"/><path d="M15 20v2"/><path d="M2 15h2"/><path d="M2 9h2"/>'
      +               '<path d="M20 15h2"/><path d="M20 9h2"/><path d="M9 2v2"/><path d="M9 20v2"/>'
      +             '</svg>'
      +           '</span>'
      +           '<span>WeatherValet Forecast: ' + escapeHtml(reading.verb) + '</span>'
      +           '<span class="ai-dot"></span>'
      +           '<span>' + confidenceBand(reading.confidence).label
      +             ' \u00b7 ' + reading.confidence + '%</span>'
      +           sourceBadge
      +         '</div>'
      +         reasonsHtml
      +       '</div>'
      +     '</div>'
      +   '</div>'

      +   crewHtml

      // ── HUMAN VERIFICATION UPSELL — adapts to verdict confidence ──
      // When status is Caution or Risk, the customer is staring at uncertainty
      // and that's the moment a live meteorologist matters most. Prominent
      // styling + explicit "definitive answer" copy. When Clear, the upsell
      // stays understated — no need to undermine our own confident verdict.
      +   buildUpsellHtml(reading.statusKey)

      +   sourcesHtml

      + '</div>'

      // ── Thank-you row ──────────────────────────────────────────────────
      // Sits just below the ticket card, above the lock-screen mock.
      // Two states:
      //   1. AI-only forecast (current default for all tickets):
      //      "Was this forecast helpful? Tell WeatherValet."
      //   2. Met-reviewed forecast (future, after $19 review is wired):
      //      "Forecast worked by Timmy Albertson. Send a quick thanks."
      // Right now every rendered ticket is pre-Met-review (AI-only),
      // so we always render the AI version. The Met version will turn
      // on automatically when reading.reviewedBy is populated.
      + (reading.reviewedBy
         ? '<div class="thank-met-row">'
         +   '<div class="thank-met-eyebrow">'
         +     'Forecast worked by <strong>' + escapeHtml(reading.reviewedBy) + '</strong>. '
         +     'Was this useful? Send a quick thanks &mdash; they\u2019ll see it in their workspace.'
         +   '</div>'
         +   '<button type="button" class="thank-met-btn" data-thank-met="' + escapeHtml(reading.reviewedBy) + '">'
         +     '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
         +       '<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>'
         +     '</svg>'
         +     '<span>Thank ' + escapeHtml(reading.reviewedBy) + '</span>'
         +   '</button>'
         + '</div>'
         : '<div class="thank-met-row">'
         +   '<div class="thank-met-eyebrow">'
         +     'AI-generated forecast by <strong>WeatherValet.ai</strong>. '
         +     'Was this helpful? A quick thanks helps us tune the model.'
         +   '</div>'
         +   '<button type="button" class="thank-met-btn" data-thank-met="weathervalet">'
         +     '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
         +       '<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>'
         +     '</svg>'
         +     '<span>Thank WeatherValet</span>'
         +   '</button>'
         + '</div>'
        )

      // Share-this-forecast row — captures the share-to-text moment.
      // On Indy 500 race day, a tailgater gets a useful forecast for
      // their plan; the natural next move is to text it to whoever
      // they're meeting up with. Clipboard copy is universal and
      // reliable across platforms. Data attributes hold the values
      // the share handler reads when constructing the text.
      + '<div class="share-forecast-row">'
      +   '<div class="share-forecast-text">'
      +     '<div class="share-forecast-title">Sharing with someone who needs this?</div>'
      +     '<div class="share-forecast-sub">Copy a friendly summary to send in a text or Slack.</div>'
      +   '</div>'
      +   '<button type="button" class="share-forecast-btn"'
      +     ' data-share-plan="' + escapeHtml(ticket.plan || 'WeatherValet forecast') + '"'
      +     ' data-share-verdict="' + escapeHtml(reading.statusLabel || '') + '"'
      +     ' data-share-conf="' + escapeHtml(String(reading.aiConfidence || '')) + '"'
      +     ' data-share-snippet="' + escapeHtml(reading.aiParagraph ? reading.aiParagraph.substring(0, 180) : '') + '">'
      +     '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
      +       '<circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/>'
      +       '<line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/>'
      +       '<line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/>'
      +     '</svg>'
      +     '<span>Copy to share</span>'
      +   '</button>'
      + '</div>'

      + '<div class="ticket-mock">'
      +   '<div class="ticket-mock-head">'
      +     '<div class="ticket-mock-eyebrow">Lock-screen preview</div>'
      +     '<h3 class="ticket-mock-title">How it lands on your phone.</h3>'
      +   '</div>'
      +   '<div class="phone-frame">'
      +     '<div class="phone-screen">'
      +       '<div class="phone-notch"></div>'
      +       '<div class="phone-status-bar">'
      +         '<span class="phone-time-bar">' + escapeHtml(lockTime) + '</span>'
      +         '<div class="phone-status-icons">'
      +           '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 20h.01"/><path d="M7 20v-4"/><path d="M12 20v-8"/><path d="M17 20V8"/><path d="M22 4v16"/></svg>'
      +           '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 13a10 10 0 0 1 14 0"/><path d="M8.5 16.5a5 5 0 0 1 7 0"/><path d="M2 8.82a15 15 0 0 1 20 0"/><line x1="12" y1="20" x2="12.01" y2="20"/></svg>'
      +           '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="16" height="10" x="2" y="7" rx="2" ry="2"/><line x1="22" y1="11" x2="22" y2="13"/></svg>'
      +         '</div>'
      +       '</div>'
      +       '<div class="phone-glow"></div>'
      +       '<div class="phone-body">'
      +         '<div class="phone-clock">' + escapeHtml(lockTime) + '</div>'
      +         '<div class="phone-date">' + escapeHtml(dateStr) + '</div>'
      +         '<div class="phone-lock-icon">'
      +           '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="11" x="3" y="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>'
      +         '</div>'
      +         '<div class="notif-card">'
      +           '<div class="notif-icon">'
      +             '<svg viewBox="0 0 70 100"><path fill="#fff" d="M65.48,24.52h-12.51l2.78-16.68L34.9,32.86h12.51l-2.78,16.68L65.48,24.52Z" transform="scale(2)"/></svg>'
      +           '</div>'
      +           '<div class="notif-body">'
      +             '<div class="notif-head">'
      +               '<span class="notif-app">Valet Update</span>'
      +               '<span class="notif-now">now</span>'
      +             '</div>'
      +             '<div class="notif-text">' + notifHtml + '</div>'
      +             '<div class="notif-sig">WeatherValet Forecast · ' + reading.confidence + '% confidence</div>'
      +           '</div>'
      +         '</div>'
      +       '</div>'
      +     '</div>'
      +   '</div>'
      + '</div>';

    // Wire the upsell CTA inside the freshly-rendered ticket
    var upsellCta = $('upsell-cta');
    if (upsellCta) {
      upsellCta.addEventListener('click', openUpsellModal);
    }

    // Wire alternate-location chips (when user didn't specify a state and
    // multiple matches exist). Tapping one re-resolves the ticket against
    // the alternate's coordinates without needing to re-type the query.
    var altChips = document.querySelectorAll('.alternate-chip');
    for (var ai = 0; ai < altChips.length; ai++) {
      (function(chip) {
        chip.addEventListener('click', function() {
          // Show the handoff screen during the re-resolve so it feels
          // intentional, not janky
          showScreen('screen-handoff');
          var startedAt = Date.now();
          var altTicket = Object.assign({}, ticket, {
            city: chip.getAttribute('data-alt-name'),
            stateCode: null,  // we have explicit coords; no need for state filtering
            lat: parseFloat(chip.getAttribute('data-alt-lat')),
            lng: parseFloat(chip.getAttribute('data-alt-lng')),
            alternates: []     // hide the hint after the user picks
          });
          var altState = chip.getAttribute('data-alt-state');
          var altZip = chip.getAttribute('data-alt-zip');
          altTicket.area = altZip
            ? (altTicket.city + ', ' + altState + ' · ' + altZip)
            : (altState ? (altTicket.city + ', ' + altState) : altTicket.city);
          // Skip geocoding (we already have coords); fetch forecast + alerts directly
          Promise.all([
            fetchForecast(altTicket.lat, altTicket.lng),
            fetchNWSAlerts(altTicket.lat, altTicket.lng).catch(function() { return null; })
          ]).then(function(results) {
            var forecast = results[0];
            var alerts = results[1];
            var reading;
            if (!forecast) {
              reading = buildSyntheticReading(altTicket);
            } else {
              reading = computeReadingFromForecast(altTicket, forecast, alerts) || buildSyntheticReading(altTicket);
            }
            var elapsed = Date.now() - startedAt;
            var wait = Math.max(0, 1200 - elapsed); // shorter dwell on switch
            setTimeout(function() {
              renderTicket(altTicket, reading);
              showScreen('screen-ticket');
            }, wait);
          });
        });
      })(altChips[ai]);
    }

    // ── AI paragraph: fetch and swap into the loading skeleton ────────
    // Runs after innerHTML is set so the skeleton is visible immediately.
    // The verdict and stats render in <50ms; the AI paragraph appears
    // 1-3 seconds later. If the backend is unreachable or returns nothing,
    // we render a graceful client-side fallback so the user never stares
    // at a stuck loading state.
    var paragraphContent = $('ai-paragraph-content');
    if (paragraphContent) {
      fetchAiExplanation(ticket, reading).then(function(result) {
        // The user may have navigated away or rendered a new ticket while
        // we were waiting for Gemini. Don't write to a stale element.
        var currentContent = $('ai-paragraph-content');
        if (!currentContent) return;
        var text = (result && result.paragraph) ? result.paragraph : clientFallbackParagraph(reading);
        currentContent.innerHTML = '<p class="ai-paragraph-text">' + escapeHtml(text) + '</p>';
        // If the cold-start retry path swapped the eyebrow to "Warming
        // up..." earlier, restore it now that the paragraph has loaded.
        var eyebrow = document.querySelector('.ai-paragraph-eyebrow span:last-child');
        if (eyebrow && eyebrow.textContent !== 'For your plan') {
          eyebrow.textContent = 'For your plan';
        }
        // Apply activity-aware verdict override if the AI suggested one.
        // The backend has already validated this is a safe downgrade
        // (caution → clear only); we just apply it to the UI.
        if (result && result.verdict_override) {
          applyVerdictOverride(result.verdict_override);
        }
      });
    }

    // ── "How we got there" collapse toggle ──────────────────────────
    var collapseToggle = $('reasons-collapse-toggle');
    var collapseWrap = $('reasons-collapse');
    if (collapseToggle && collapseWrap) {
      collapseToggle.addEventListener('click', function() {
        var nowOpen = !collapseWrap.classList.contains('is-open');
        collapseWrap.classList.toggle('is-open', nowOpen);
        collapseToggle.setAttribute('aria-expanded', nowOpen ? 'true' : 'false');
      });
    }

    // ── NWS alert ribbon expand/collapse ────────────────────────────
    // Ribbon collapsed by default — the colored bar at top of the
    // verdict zone is enough to alert the customer. Tap to expand for
    // the full alert event text and NWS source attribution.
    var alertHead = document.querySelector('[data-alert-toggle]');
    if (alertHead) {
      alertHead.addEventListener('click', function() {
        var targetId = alertHead.getAttribute('data-alert-toggle');
        var ribbon = document.getElementById(targetId);
        if (!ribbon) return;
        var nowOpen = !ribbon.classList.contains('is-open');
        ribbon.classList.toggle('is-open', nowOpen);
        alertHead.setAttribute('aria-expanded', nowOpen ? 'true' : 'false');
      });
    }

    // ── Thank-your-Met button ───────────────────────────────────────
    // Sends a thank-you to either the meteorologist who worked the
    // forecast (Met-reviewed path) or to WeatherValet (AI path).
    // In production:
    //   - Met path writes to a Recognition table that surfaces in the
    //     Met's workspace AND fires a Twilio SMS to the Met's phone.
    //   - AI path writes to a feedback table that the model team uses
    //     for tuning.
    // Prototype: visual confirmation + customer-side toast that adapts
    // to which path was rendered.
    var thankBtn = document.querySelector('[data-thank-met]');
    if (thankBtn && !thankBtn.dataset.wired) {
      thankBtn.dataset.wired = '1';
      thankBtn.addEventListener('click', function() {
        thankBtn.classList.add('is-sent');
        var label = thankBtn.querySelector('span');
        if (label) label.textContent = 'Thanks sent';
        if (typeof showTipToast === 'function') {
          var who = thankBtn.getAttribute('data-thank-met');
          var msg = (who === 'weathervalet')
            ? 'Thanks \u2014 noted for the model team'
            : 'Sent to ' + who + ' \u2014 they\u2019ll see it in their workspace';
          showTipToast(msg);
        }
      });
    }

    // ── Share-forecast handler ───────────────────────────────────────
    // Builds a friendly summary from data attributes on the button and
    // copies it to the clipboard. Falls back gracefully if the
    // Clipboard API isn't available (older browsers, file:// in some
    // contexts) by selecting the text in a hidden textarea and
    // calling document.execCommand('copy').
    var shareBtn = document.querySelector('.share-forecast-btn');
    if (shareBtn && !shareBtn.dataset.wired) {
      shareBtn.dataset.wired = '1';
      shareBtn.addEventListener('click', function() {
        var plan = shareBtn.getAttribute('data-share-plan') || 'WeatherValet forecast';
        var verdict = shareBtn.getAttribute('data-share-verdict') || '';
        var conf = shareBtn.getAttribute('data-share-conf') || '';
        var snippet = shareBtn.getAttribute('data-share-snippet') || '';

        // Build the shareable text. Order matters: plan first
        // (recipient knows what's being shared), verdict next (the
        // bottom line), then a one-line snippet, then the link.
        var lines = ['WeatherValet \u2014 ' + plan, ''];
        if (verdict) {
          lines.push(verdict + (conf ? ' \u00b7 ' + conf + '% confidence' : ''));
        }
        if (snippet) {
          // Strip any trailing punctuation that would make the link
          // run on awkwardly, then ensure it ends in a period.
          var clean = snippet.trim();
          if (!/[.!?]$/.test(clean)) clean += '.';
          lines.push(clean);
        }
        lines.push('');
        lines.push('Get your own forecast: weathervalet.ai');

        var text = lines.join('\n');

        function showCopied() {
          shareBtn.classList.add('is-copied');
          var label = shareBtn.querySelector('span');
          if (label) label.textContent = 'Copied to clipboard';
          if (typeof showTipToast === 'function') {
            showTipToast('Copied \u2014 paste it into a text');
          }
          // Revert label after a few seconds so the button is
          // re-usable if the user wants to copy again.
          setTimeout(function() {
            shareBtn.classList.remove('is-copied');
            if (label) label.textContent = 'Copy to share';
          }, 4000);
        }

        // Modern Clipboard API
        if (navigator.clipboard && navigator.clipboard.writeText) {
          navigator.clipboard.writeText(text).then(showCopied).catch(function() {
            fallbackCopy(text, showCopied);
          });
        } else {
          fallbackCopy(text, showCopied);
        }
      });
    }

    // Clipboard fallback for environments without navigator.clipboard.
    function fallbackCopy(text, onDone) {
      try {
        var ta = document.createElement('textarea');
        ta.value = text;
        ta.setAttribute('readonly', '');
        ta.style.position = 'fixed';
        ta.style.top = '-9999px';
        document.body.appendChild(ta);
        ta.select();
        document.execCommand('copy');
        document.body.removeChild(ta);
        if (onDone) onDone();
      } catch (err) {
        if (typeof showTipToast === 'function') {
          showTipToast('Couldn\u2019t copy \u2014 try long-pressing the text');
        }
      }
    }

    // ── Threshold alerts placeholder ─────────────────────────────────
    // Placeholder that signals the future feature without building it.
    // Real implementation: Pro/Business tier subscription where
    // customers configure threshold rules per location and the system
    // fires SMS/in-app alerts when conditions cross. This is a
    // dedicated weekend build with its own pricing tier.
    var thresholdBtn = document.querySelector('[data-threshold-cta]');
    if (thresholdBtn && !thresholdBtn.dataset.wired) {
      thresholdBtn.dataset.wired = '1';
      thresholdBtn.addEventListener('click', function() {
        if (typeof showTipToast === 'function') {
          showTipToast('Threshold alerts \u2014 a Pro/Business feature, arriving soon');
        }
      });
    }
  }

  /**
   * Apply an activity-aware verdict downgrade to the rendered ticket.
   *
   * The rule-based verdict above the ticket sometimes flags a situation
   * as Caution that's actually fine for the specific activity (e.g.
   * walking into church on a windy day). When the AI explainer returns
   * a `verdict_override` field, the backend has already validated it
   * as a safe downgrade — this function applies it visually.
   *
   * Currently: transitions the status pill, the verdict headline, and
   * the verb in the "How we got there" panel from Caution to Clear.
   * Stats are left untouched (the underlying numbers haven't changed).
   *
   * Future-proofed: if the backend ever returns "caution" as an
   * override (e.g. risk → caution downgrade, which the backend
   * currently rejects), this function would handle it the same way.
   */
  function applyVerdictOverride(newStatusKey) {
    if (!STATUS[newStatusKey]) return;
    var s = STATUS[newStatusKey];
    // Update the pill: replace the existing status-* class with the new
    // one, and update its text. The pill has structure:
    //   <div class="status-pill status-X"><span class="status-pill-dot"/>Status: X</div>
    var pill = document.querySelector('.status-pill');
    if (pill) {
      pill.classList.remove('status-clear', 'status-caution', 'status-risk');
      pill.classList.add(s.pillClass);
      // Rebuild inner content — preserve the dot, update the label
      pill.innerHTML = '<span class="status-pill-dot"></span>' + escapeHtml(s.label);
    }
    // Update the verdict headline ("Your window looks good." etc.)
    var verdictHeadline = document.querySelector('.ticket-verdict');
    if (verdictHeadline) {
      verdictHeadline.textContent = s.summary;
    }
    // Update the ticket-card data-status attribute (some CSS keys off this)
    var ticketCard = document.querySelector('.ticket-card');
    if (ticketCard) {
      ticketCard.setAttribute('data-status', newStatusKey);
    }
    // Update the verb shown in the "How we got there" collapsible
    var howRow = document.querySelector('.reasons-collapse-ai-row');
    if (howRow) {
      // The structure has the verb in the second span (after the badge).
      // We do a targeted text-content swap rather than a full rebuild
      // so the SVG icon and other spans stay intact.
      var spans = howRow.querySelectorAll('span');
      for (var i = 0; i < spans.length; i++) {
        var t = spans[i].textContent || '';
        if (t.indexOf('WeatherValet Forecast:') === 0) {
          spans[i].textContent = 'WeatherValet Forecast: ' + s.verb;
          break;
        }
      }
    }
    // Update the lock-screen notification mock — the verb span and tail.
    // Without this, a user looking at the rendered ticket sees Status:
    // Clear at the top but a lock-screen preview that still says Caution,
    // which feels like a bug even though it's an artifact of the override.
    // Scope the selector to the rendered ticket content so we don't
    // accidentally rewrite the static demo notif on the welcome screen.
    var ticketContentEl = document.getElementById('ticket-content');
    var notifTextEl = ticketContentEl ? ticketContentEl.querySelector('.notif-text') : null;
    if (notifTextEl) {
      var verbSpan = notifTextEl.querySelector('span');
      if (verbSpan) {
        verbSpan.className = s.verbClass;
        verbSpan.textContent = s.verb;
      }
      // Also rewrite the tail (the "— watch the wind" part). The notif's
      // last text node is the tail. For Clear we want a period; for
      // Caution/Risk the existing tail might still be relevant. Since
      // we currently only honor caution → clear overrides, replace with
      // the Clear tail (just a period).
      var lastText = notifTextEl.lastChild;
      while (lastText && lastText.nodeType !== 3) lastText = lastText.previousSibling;
      if (lastText) lastText.textContent = s.tail;
    }
  }

  /**
   * Generate a friendly fallback paragraph when the AI backend isn't
   * reachable. Deliberately plain — we'd rather sound honest and useful
   * than fake-AI. The verdict pill above still gives the at-a-glance
   * answer; this paragraph just fills the visual slot the AI would.
   */
  function clientFallbackParagraph(reading) {
    var verdict = (reading && reading.statusKey) || '';
    if (verdict === 'clear') {
      return 'Conditions are looking workable for your plan. The numbers below have the specifics — temperature, wind, and what the sky should look like during your window.';
    }
    if (verdict === 'caution') {
      return 'There are a few things worth keeping an eye on for your plan. Take a look at the numbers below — wind, precipitation, and timing all matter for what you\u2019re doing.';
    }
    if (verdict === 'risk') {
      return 'Conditions are stacked against this one. Take a careful look at the details below before you commit, and consider whether shifting the timing might help.';
    }
    return 'Here\u2019s the snapshot for your plan. The specifics are below.';
  }

  // ── Modal control ────────────────────────────────────────────────────────
  function openUpsellModal() {
    // Reset to the pricing step every time
    showModalStep('pricing');
    var modal = $('upsell-modal');
    if (modal) modal.classList.add('active');
  }

  function closeUpsellModal() {
    var modal = $('upsell-modal');
    if (modal) modal.classList.remove('active');
  }

  function showModalStep(stepName) {
    var steps = document.querySelectorAll('.modal-step');
    for (var i = 0; i < steps.length; i++) steps[i].classList.remove('active');
    var target = $('modal-step-' + stepName);
    if (target) target.classList.add('active');
  }

  function handleConfirmVerification() {
    // Capture the phone number for the standby display
    var phoneInput = $('sms-phone');
    var phoneVal = phoneInput ? phoneInput.value.trim() : '';
    var display = $('standby-phone-display');
    if (display) {
      display.textContent = phoneVal || 'your phone';
    }
    showModalStep('standby');
  }

  // ════════════════════════════════════════════════════════════════════
  // REAL DATA — Open-Meteo integration
  // ════════════════════════════════════════════════════════════════════
  // Free, no-auth, browser-callable. Two endpoints:
  //   1. Geocoding: city name → coordinates
  //   2. Forecast:  coordinates → hourly forecast values
  // Docs: https://open-meteo.com/en/docs

  var GEOCODE_URL = 'https://geocoding-api.open-meteo.com/v1/search';
  var FORECAST_URL = 'https://api.open-meteo.com/v1/forecast';

  /**
   * Resolve a city name to coordinates via Open-Meteo's geocoding API.
   * Returns null if the city can't be found (caller surfaces this to the user).
   */
  /**
   * Resolve a city name to coordinates via Open-Meteo's geocoding API.
   *
   * When `stateHint` is provided (e.g. "IN"), we filter results to that state
   * before picking — this prevents Lebanon, IN being silently shadowed by
   * Lebanon, TN (which has more population and ranks higher by default).
   *
   * Returns null if the city can't be found, or if a stateHint was given
   * and no result matched that state (caller surfaces this to the user).
   */
  function geocodeCity(name, stateHint) {
    if (!name || name === 'your area') {
      return Promise.resolve(null);
    }
    var url = GEOCODE_URL + '?name=' + encodeURIComponent(name) + '&count=10&language=en&format=json';
    return fetch(url)
      .then(function(res) {
        if (!res.ok) throw new Error('Geocode HTTP ' + res.status);
        return res.json();
      })
      .then(function(data) {
        var results = (data && data.results) || [];
        if (results.length === 0) return null;

        // Prefer US results since this is a US-targeted product
        var us = results.filter(function(r) { return r.country_code === 'US'; });
        var pool = us.length > 0 ? us : results;

        // ── Disambiguate by state when the user specified one ──
        // The query "Lebanon, IN" must resolve to Indiana, never Tennessee.
        // Open-Meteo returns the full state name in `admin1` ("Indiana"),
        // so we match against that. We also accept exact state-code match
        // in case the API ever returns codes.
        if (stateHint) {
          var stateName = US_STATES[stateHint];
          var stateMatched = pool.filter(function(r) {
            if (!r.admin1) return false;
            var rState = r.admin1.toLowerCase();
            return rState === (stateName || '').toLowerCase()
                || rState === stateHint.toLowerCase();
          });
          if (stateMatched.length > 0) {
            pool = stateMatched;
          } else {
            // User explicitly asked for "Lebanon, XX" but no Lebanon exists in
            // state XX. Treat as a hard miss — better to surface an error than
            // silently fall back to the wrong state.
            return null;
          }
        }

        var pick = pool[0];
        return {
          name: pick.name,
          latitude: pick.latitude,
          longitude: pick.longitude,
          state: pick.admin1 || '',
          zip: (pick.postcodes && pick.postcodes[0]) || '',
          country: pick.country_code || '',
          // For UI: list of alternate matches if the user might want to switch
          alternates: pool.slice(1, 4).map(function(r) {
            return {
              name: r.name,
              state: r.admin1 || '',
              zip: (r.postcodes && r.postcodes[0]) || '',
              latitude: r.latitude,
              longitude: r.longitude
            };
          })
        };
      })
      .catch(function(err) {
        console.warn('[geocode] failed:', err);
        return null;
      });
  }

  /**
   * Fetch hourly forecast at given coordinates. Returns raw response object
   * with .hourly{time, temperature_2m, precipitation_probability, wind_speed_10m,
   * wind_gusts_10m, weather_code, ...} or null on failure.
   */
  function fetchForecast(lat, lng) {
    var params = [
      'latitude=' + lat.toFixed(4),
      'longitude=' + lng.toFixed(4),
      'hourly=temperature_2m,apparent_temperature,relative_humidity_2m,dew_point_2m,precipitation_probability,precipitation,wind_speed_10m,wind_gusts_10m,weather_code,cloud_cover',
      'temperature_unit=fahrenheit',
      'wind_speed_unit=mph',
      'precipitation_unit=inch',
      'forecast_days=3',
      'timezone=auto'
    ].join('&');
    var url = FORECAST_URL + '?' + params;
    return fetch(url)
      .then(function(res) {
        if (!res.ok) throw new Error('Forecast HTTP ' + res.status);
        return res.json();
      })
      .catch(function(err) {
        console.warn('[forecast] failed:', err);
        return null;
      });
  }

  /**
   * Fetch the AI-generated friendly paragraph for a ticket.
   *
   * This is the "secret sauce" — calls our Render backend, which calls
   * Gemini, which returns 2-4 sentences that translate the weather data
   * into plain-English experience for the user's specific activity.
   *
   * Returns a Promise<string> that resolves to the paragraph. On any
   * failure (backend down, rate limited, network), resolves to null and
   * the caller renders a graceful fallback. NEVER throws — we don't want
   * a missing AI paragraph to break the whole ticket.
   */
  function fetchAiExplanation(ticket, reading) {
    var apiBase = (typeof window !== 'undefined' && window.WV_API_BASE) || '';
    if (!apiBase) {
      // No backend configured — running in fully-offline mode.
      // Skip the call; renderTicket falls back to its built-in copy.
      return Promise.resolve(null);
    }

    // Format the time window and date in a human-friendly way for the AI.
    // The AI uses this to phrase its paragraph naturally (e.g. "Saturday
    // afternoon" rather than "May 10 14:00").
    var whenStr = '';
    try {
      var now = new Date();
      var planDate = new Date(now);
      if (ticket.dateOffsetDays) planDate.setDate(planDate.getDate() + ticket.dateOffsetDays);
      var dayName = planDate.toLocaleDateString('en-US', { weekday: 'long' });
      var monthDay = planDate.toLocaleDateString('en-US', { month: 'long', day: 'numeric' });
      var timeStr = ticket.scheduledShort || (ticket.windowStartHour + ':00');
      whenStr = dayName + ', ' + monthDay + ' at ' + timeStr;
    } catch (e) {
      whenStr = ticket.scheduledShort || '';
    }

    // Build the weather payload from the reading. We pass everything we
    // know that's useful for the AI; the backend pulls only the fields
    // it needs into the Gemini prompt.
    var weatherPayload = {};
    if (typeof reading.avgTempF === 'number')        weatherPayload.temperature_f = reading.avgTempF;
    if (typeof reading.avgHumidityPct === 'number')  weatherPayload.humidity_pct = reading.avgHumidityPct;
    if (typeof reading.rawWindMph === 'number')      weatherPayload.wind_mph = reading.rawWindMph;
    if (typeof reading.rawGustMph === 'number')      weatherPayload.wind_gust_mph = reading.rawGustMph;
    if (typeof reading.rawPrecipPct === 'number')    weatherPayload.precip_probability_pct = reading.rawPrecipPct;
    // Cloud cover — derive a rough percentage from the sky stat label
    var skyStat = (reading.stats || []).find(function(s) { return s.key === 'Sky'; });
    if (skyStat && skyStat.value) {
      var skyText = String(skyStat.value).toLowerCase();
      if (skyText.indexOf('clear') !== -1)        weatherPayload.cloud_cover_pct = 10;
      else if (skyText.indexOf('mostly sunny') !== -1) weatherPayload.cloud_cover_pct = 25;
      else if (skyText.indexOf('partly') !== -1)  weatherPayload.cloud_cover_pct = 50;
      else if (skyText.indexOf('mostly cloudy') !== -1) weatherPayload.cloud_cover_pct = 75;
      else if (skyText.indexOf('overcast') !== -1) weatherPayload.cloud_cover_pct = 95;
      else if (skyText.indexOf('cloud') !== -1)   weatherPayload.cloud_cover_pct = 60;
    }

    var body = {
      plan: ticket.query || '',
      location: ticket.area || ticket.city || '',
      when: whenStr,
      // Send the lowercase status key (clear/caution/risk), not the
      // display label. The backend parses this to decide whether a
      // suggested AI verdict counts as a downgrade.
      verdict: reading.statusKey || '',
      weather: weatherPayload
    };

    // Helper: a single attempt with a timeout. AbortController lets us
    // cancel a fetch that's hanging — important for cold-start scenarios
    // where the request would otherwise sit forever waiting for a backend
    // that's still booting up.
    function attemptOnce(timeoutMs) {
      var controller = (typeof AbortController !== 'undefined') ? new AbortController() : null;
      var timeoutId = controller ? setTimeout(function() { controller.abort(); }, timeoutMs) : null;
      var opts = {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body)
      };
      if (controller) opts.signal = controller.signal;
      return fetch(apiBase + '/api/v1/forecast/explain', opts)
        .then(function(res) {
          if (timeoutId) clearTimeout(timeoutId);
          if (!res.ok) throw new Error('Explain HTTP ' + res.status);
          return res.json();
        })
        .then(function(data) {
          // Return the full object so callers can read paragraph AND
          // verdict_override. Returning null signals the caller to fall
          // back to the client-side static paragraph.
          if (!data || !data.paragraph) return null;
          return {
            paragraph: data.paragraph,
            verdict_override: data.verdict_override || null
          };
        });
    }

    // First attempt: 8-second timeout. Render's free tier wakes from sleep
    // in ~30 seconds, but a warm backend responds in 2-3 seconds — so 8s
    // is a clean cutoff between "hot path" and "needs a wakeup."
    return attemptOnce(8000).catch(function(firstErr) {
      console.warn('[explain] first attempt failed, likely cold start:', firstErr);
      // Signal to the UI that we're warming up the backend — the loading
      // skeleton swaps its eyebrow text so the user knows there's a
      // legitimate reason this is taking longer than usual.
      var eyebrow = document.querySelector('.ai-paragraph-eyebrow span:last-child');
      if (eyebrow) eyebrow.textContent = 'Warming up\u2026';
      // Give Render a moment to finish booting, then try again with a
      // generous 25-second timeout that comfortably covers a full
      // cold-start sequence.
      return new Promise(function(resolve) { setTimeout(resolve, 1500); })
        .then(function() { return attemptOnce(25000); })
        .catch(function(secondErr) {
          console.warn('[explain] retry also failed:', secondErr);
          return null;
        });
    });
  }

  /**
   * Map a WMO weather code to a plain-English sky condition string.
   * Reference: https://open-meteo.com/en/docs (WMO Weather interpretation)
   */
  /**
   * Fetch active NWS alerts for a coordinate. Returns an array of alert objects
   * (or empty array if no active alerts, or null on hard failure). Uses the
   * GeoJSON endpoint which works from browser origins without auth.
   *
   * Endpoint: https://api.weather.gov/alerts/active?point=LAT,LNG
   * Docs:     https://www.weather.gov/documentation/services-web-api
   *
   * NWS advises clients to send a User-Agent identifying the app. The browser
   * sends its own UA on fetch — we can't override it from JS — but NWS accepts
   * browser UAs on this endpoint. (For server-side calls later, set
   * `User-Agent: WeatherValet/1.0 (contact@weathervalet.ai)`)
   */
  function fetchNWSAlerts(lat, lng) {
    var url = 'https://api.weather.gov/alerts/active?point=' + lat.toFixed(4) + ',' + lng.toFixed(4);
    return fetch(url, { headers: { 'Accept': 'application/geo+json' } })
      .then(function(res) {
        if (!res.ok) {
          // 404 from NWS for points outside US is expected and not an error
          if (res.status === 404) return [];
          throw new Error('NWS alerts HTTP ' + res.status);
        }
        return res.json();
      })
      .then(function(data) {
        var features = (data && data.features) || [];
        var now = Date.now();
        var alerts = [];
        for (var i = 0; i < features.length; i++) {
          var p = (features[i] && features[i].properties) || {};
          var expires = p.expires ? Date.parse(p.expires) : now + 86400000;
          if (expires > now) {
            alerts.push({
              event: p.event || 'Weather Alert',
              severity: p.severity || 'Unknown',     // Minor | Moderate | Severe | Extreme | Unknown
              urgency: p.urgency || 'Unknown',        // Immediate | Expected | Future | Past | Unknown
              certainty: p.certainty || 'Unknown',
              headline: p.headline || '',
              description: (p.description || '').substring(0, 280)
            });
          }
        }
        return alerts;
      })
      .catch(function(err) {
        console.warn('[nws] alerts fetch failed:', err.message);
        // Soft fail — never break the ticket because NWS had a hiccup
        return null;
      });
  }


  /**
   * Map a WMO weather code to a plain-English sky condition string.
   * Reference: https://open-meteo.com/en/docs (WMO Weather interpretation)
   */
  function skyConditionFromCode(code) {
    if (code === 0) return 'Clear Sky';
    if (code <= 3) return 'Mostly Sunny';
    if (code <= 48) return 'Foggy';
    if (code <= 57) return 'Drizzling';
    if (code <= 67) return 'Rainy';
    if (code <= 77) return 'Snowy';
    if (code <= 82) return 'Rain Showers';
    if (code <= 86) return 'Snow Showers';
    if (code <= 99) return 'Thunderstorms';
    return 'Variable';
  }

  /**
   * Industry-aware status engine.
   *
   * Each contractor has different sensitivities — a 30% rain chance is
   * disqualifying for a concrete pour but acceptable for a wedding under
   * a tent. This function takes the full sampled environment (in-window
   * worst case + lookahead window for cure-sensitive trades) and returns
   * a status keyed off the user's plan industry.
   *
   * Returns: { status, reasons, lookaheadConcern }
   *   status: 'clear' | 'caution' | 'risk'
   *   reasons: array of human-readable strings explaining the call
   *   lookaheadConcern: optional string for "0% now, but rain at 4 PM" cases
   */
  function computeStatusForPlan(industry, sampled, lookahead) {
    // sampled = { precipPct, windMph, gustMph, weatherCode, tempF, humidityPct }
    // lookahead = { maxPrecipPct, willRainWithin, hoursUntilRain,
    //               convectiveActivity, nearbyStormHour, maxPrecipNearby,
    //               trendRising, trendDelta }
    var reasons = [];
    var status = 'clear';
    var lookaheadConcern = null;
    var summaryOverride = null; // optional context-specific verdict copy

    function escalate(target, reason) {
      var rank = { clear: 0, caution: 1, risk: 2 };
      if (rank[target] > rank[status]) status = target;
      if (reason) reasons.push(reason);
    }

    // Universal hard floors — a thunderstorm is always Risk, a Severe NWS alert
    // is always Risk, regardless of industry. These take precedence over rules.
    // (Severe NWS alerts are applied later by the resolver before this is called.)
    if (sampled.weatherCode >= 95) {
      escalate('risk', 'Thunderstorm activity in your window');
    }

    // Industry-specific dispatch
    switch (industry) {
      case 'concrete': {
        // Concrete is sensitive to: precip in window, precip in cure window
        // (24h after), temperature extremes, and high humidity slows curing.
        if (sampled.precipPct >= 30) escalate('risk', 'Rain likely during your pour (' + sampled.precipPct + '% chance)');
        else if (sampled.precipPct >= 10) escalate('caution', 'Light rain possible — keep covers ready (' + sampled.precipPct + '% chance)');

        if (sampled.tempF != null) {
          if (sampled.tempF < 40)      escalate('risk',    'Too cold to pour (' + sampled.tempF + '°F is below safe range)');
          else if (sampled.tempF < 50) escalate('caution', 'Cool temps will slow your cure (' + sampled.tempF + '°F)');
          else if (sampled.tempF > 90) escalate('risk',    'Heat will flash-set the mix (' + sampled.tempF + '°F)');
          else if (sampled.tempF > 85) escalate('caution', 'Warm temps — mind the set time (' + sampled.tempF + '°F)');
        }

        if (sampled.humidityPct != null && sampled.humidityPct > 85) {
          escalate('caution', 'High humidity will extend your cure time');
        }

        // Cure-window lookahead: rain within 12h after pour can ruin a finish
        if (lookahead && lookahead.willRainWithin && lookahead.hoursUntilRain <= 12) {
          if (lookahead.maxPrecipPct >= 50) {
            escalate('risk', 'Rain expected ' + lookahead.hoursUntilRain + 'h after pour — protect the finish');
            lookaheadConcern = 'Rain expected within ' + lookahead.hoursUntilRain + (lookahead.hoursUntilRain === 1 ? ' hour' : ' hours') + ' after your pour. Plan covers and finishing accordingly.';
          } else if (lookahead.maxPrecipPct >= 25) {
            escalate('caution', 'Possible rain ' + lookahead.hoursUntilRain + 'h after pour');
            lookaheadConcern = 'Some chance of rain within ' + lookahead.hoursUntilRain + (lookahead.hoursUntilRain === 1 ? ' hour' : ' hours') + ' after your pour. Keep covers staged.';
          }
        }
        break;
      }

      case 'roofing': {
        // Roofing: wind is the primary concern (workers + materials),
        // precipitation is the secondary concern.
        if (sampled.windMph >= 22) escalate('risk',    'Unsafe wind on the roof (' + sampled.windMph + ' mph)');
        else if (sampled.windMph >= 15) escalate('caution', 'Breezy — secure your materials (' + sampled.windMph + ' mph)');

        if (sampled.gustMph >= 28) escalate('risk',    'Gusts strong enough to call work-stop (' + sampled.gustMph + ' mph)');
        else if (sampled.gustMph >= 22) escalate('caution', 'Gusty — extra caution on edges (' + sampled.gustMph + ' mph)');

        if (sampled.precipPct >= 35) escalate('risk',    'Rain likely during your job (' + sampled.precipPct + '% chance)');
        else if (sampled.precipPct >= 15) escalate('caution', 'Plan for some rain (' + sampled.precipPct + '% chance) — schedule covers');
        break;
      }

      case 'event': {
        // Outdoor events: precip + lightning + wind for tents/structures.
        // Plus convective potential — scattered storms read low at point
        // forecasts but are real risk for venues with no rain plan.
        // Lightning risk (weatherCode >=95) already escalated to Risk above.

        // ── In-window precip ──
        // Event-industry thresholds are tighter than other industries because
        // outdoor events have low tolerance for any rain — tents, electronics,
        // guests in non-rain attire, etc.
        if (sampled.precipPct >= 30) escalate('risk',    'Rain likely during your event (' + sampled.precipPct + '% chance) — guests need indoor option');
        else if (sampled.precipPct >= 12) escalate('caution', 'Scattered showers possible — have a backup plan (' + sampled.precipPct + '% chance)');

        // ── Wind ──
        if (sampled.windMph >= 20) escalate('risk',    'Wind is a tent-stability concern (' + sampled.windMph + ' mph)');
        else if (sampled.windMph >= 12) escalate('caution', 'Breezy — secure decor and signage (' + sampled.windMph + ' mph)');

        // ── Drizzle/rain weather codes ──
        if (sampled.weatherCode >= 51 && sampled.weatherCode <= 67) {
          escalate('caution', 'Light rain expected during your window');
        }

        // ── Convective potential (the "scattered storms" signal) ──
        if (lookahead && lookahead.convectiveActivity) {
          var hr = lookahead.nearbyStormHour;
          if (hr !== null && hr <= 3) {
            escalate('risk', 'Thunderstorms close to your window — lightning risk' + (hr === 0 ? '' : (' (within ' + hr + 'h)')));
            summaryOverride = 'Scattered storms developing nearby.';
          } else {
            escalate('caution', 'Scattered storms in the area — keep an eye on radar');
            if (!summaryOverride) summaryOverride = 'Scattered storms possible.';
          }
        }

        // ── Storm coverage trend ──
        if (lookahead && lookahead.trendRising && !lookahead.convectiveActivity) {
          escalate('caution', 'Rain chances rising into the evening (+' + lookahead.trendDelta + '% over the next 6h)');
          if (!summaryOverride) summaryOverride = 'Rain chances rising into the evening.';
        }

        // ── Wider-band precip check (catches model spread) ──
        if (lookahead && lookahead.maxPrecipNearby >= 40 && sampled.precipPct < 25) {
          escalate('caution', 'Storms possible just outside your window (peaking at ' + lookahead.maxPrecipNearby + '% nearby)');
          if (!summaryOverride) summaryOverride = 'Your window has some risk.';
        }
        break;
      }

      case 'painting': {
        // Painting/sealing: temperature and humidity drive cure quality;
        // rain within 8h after application can ruin the surface.
        if (sampled.precipPct >= 25) escalate('risk', 'Rain likely during your work (' + sampled.precipPct + '% chance)');
        else if (sampled.precipPct >= 10) escalate('caution', 'Light rain possible (' + sampled.precipPct + '% chance)');

        if (sampled.tempF != null) {
          if (sampled.tempF < 40)       escalate('risk',    'Too cold for paint to cure (' + sampled.tempF + '°F)');
          else if (sampled.tempF < 50)  escalate('caution', 'Cool temps will slow cure time (' + sampled.tempF + '°F)');
          else if (sampled.tempF > 90)  escalate('risk',    'Too hot — paint will set too fast (' + sampled.tempF + '°F)');
          else if (sampled.tempF > 85)  escalate('caution', 'Warm — work in the shade (' + sampled.tempF + '°F)');
        }

        if (sampled.humidityPct != null && sampled.humidityPct >= 80) {
          escalate('caution', 'High humidity will slow your cure');
        }

        if (lookahead && lookahead.willRainWithin && lookahead.hoursUntilRain <= 8) {
          if (lookahead.maxPrecipPct >= 40) {
            escalate('risk', 'Rain expected ' + lookahead.hoursUntilRain + 'h after — surface won\'t cure');
            lookaheadConcern = 'Rain expected within ' + lookahead.hoursUntilRain + ' hours. Paint needs at least 8 hours of dry weather to cure.';
          }
        }
        break;
      }

      case 'crane': {
        // Crane/lift: wind is sharply enforced — OSHA stop-work at 20 mph,
        // most operators stop at 25 mph gusts.
        if (sampled.windMph >= 22)      escalate('risk',    'Past safe lift threshold (' + sampled.windMph + ' mph)');
        else if (sampled.windMph >= 15) escalate('caution', 'Breezy — monitor conditions (' + sampled.windMph + ' mph)');

        if (sampled.gustMph >= 28)      escalate('risk',    'Gusts trigger work-stop (' + sampled.gustMph + ' mph)');
        else if (sampled.gustMph >= 22) escalate('caution', 'Gusts borderline (' + sampled.gustMph + ' mph)');

        if (sampled.precipPct >= 40) escalate('risk', 'Rain will hurt visibility (' + sampled.precipPct + '% chance)');
        break;
      }

      case 'agriculture': {
        // Harvest/spray: rain delays harvest; spraying needs calm + dry.
        if (sampled.precipPct >= 40) escalate('risk',    'Rain likely — field operations affected (' + sampled.precipPct + '% chance)');
        else if (sampled.precipPct >= 20) escalate('caution', 'Some rain possible (' + sampled.precipPct + '% chance)');

        if (sampled.windMph >= 12) escalate('caution', 'Wind risks spray drift (' + sampled.windMph + ' mph)');
        if (sampled.windMph >= 18) escalate('risk',    'Too windy to spray (' + sampled.windMph + ' mph)');
        break;
      }

      default: {
        // Fallback: the v2.0 generic thresholds.
        if (sampled.precipPct >= 60) escalate('risk',    sampled.precipPct + '% precipitation in your window');
        else if (sampled.precipPct >= 25) escalate('caution', sampled.precipPct + '% chance of rain');

        if (sampled.windMph >= 25) escalate('risk',    'Wind ' + sampled.windMph + ' mph');
        else if (sampled.windMph >= 15) escalate('caution', 'Breezy (' + sampled.windMph + ' mph)');

        if (sampled.gustMph >= 35) escalate('risk', 'Gusts to ' + sampled.gustMph + ' mph');
        if (sampled.weatherCode >= 51 && sampled.weatherCode <= 67) escalate('caution', 'Drizzle or light rain in window');
        break;
      }
    }

    return { status: status, reasons: reasons, lookaheadConcern: lookaheadConcern, summaryOverride: summaryOverride };
  }

  /**
   * Legacy thin wrapper kept for backward compatibility with any test code
   * that called computeStatusFromValues directly. Internally dispatches to the
   * default industry rules.
   */
  function computeStatusFromValues(precipPct, windMph, gustMph, weatherCode) {
    var result = computeStatusForPlan('default', {
      precipPct: precipPct, windMph: windMph, gustMph: gustMph,
      weatherCode: weatherCode, tempF: null, humidityPct: null
    }, null);
    return result.status;
  }

  /**
   * Sample the user's plan window across the forecast data + the lookahead
   * window after it, run the industry-aware status engine, blend NWS alerts,
   * and synthesize a `reading` object that renderTicket can consume.
   *
   * `ticket.dateOffsetDays` is 0 for today, 1 for "tomorrow"
   * `ticket.windowStartHour` and `ticket.windowEndHour` define the sample range
   * `ticket.industry` selects the industry rules
   * `alerts` is an array of NWS alert objects (or null/empty)
   */
  function computeReadingFromForecast(ticket, forecast, alerts) {
    if (!forecast || !forecast.hourly || !forecast.hourly.time) {
      return null;
    }
    var hourly = forecast.hourly;

    // Find the array indices that fall inside the user's plan window
    var planDate = new Date();
    if (ticket.dateOffsetDays) planDate.setDate(planDate.getDate() + ticket.dateOffsetDays);
    var planY = planDate.getFullYear();
    var planM = String(planDate.getMonth() + 1).padStart(2, '0');
    var planD = String(planDate.getDate()).padStart(2, '0');
    var planDateStr = planY + '-' + planM + '-' + planD;

    var inWindowIdx = [];
    var windowEndAbs = -1; // The absolute index of the last in-window hour
    for (var i = 0; i < hourly.time.length; i++) {
      var t = hourly.time[i]; // e.g. "2026-04-29T10:00"
      var dayPart = t.substring(0, 10);
      var hr = parseInt(t.substr(11, 2), 10);
      if (dayPart === planDateStr && hr >= ticket.windowStartHour && hr <= ticket.windowEndHour) {
        inWindowIdx.push(i);
        windowEndAbs = i;
      }
    }
    if (inWindowIdx.length === 0) {
      // Plan window isn't within the forecast horizon — fall through to synthetic
      return null;
    }

    // ── In-window worst-case sampling ──
    var maxPrecip = 0, maxWind = 0, minWind = Infinity, maxGust = 0, maxCode = 0;
    var sumTemp = 0, tempCount = 0, sumHumidity = 0, humidityCount = 0;
    for (var j = 0; j < inWindowIdx.length; j++) {
      var idx = inWindowIdx[j];
      var p = hourly.precipitation_probability[idx] || 0;
      var w = hourly.wind_speed_10m[idx] || 0;
      var g = hourly.wind_gusts_10m[idx] || 0;
      var c = hourly.weather_code[idx] || 0;
      var tp = hourly.temperature_2m[idx];
      var hm = hourly.relative_humidity_2m ? hourly.relative_humidity_2m[idx] : null;
      if (p > maxPrecip) maxPrecip = p;
      if (w > maxWind) maxWind = w;
      if (w < minWind) minWind = w;
      if (g > maxGust) maxGust = g;
      if (c > maxCode) maxCode = c;
      if (typeof tp === 'number') { sumTemp += tp; tempCount++; }
      if (typeof hm === 'number') { sumHumidity += hm; humidityCount++; }
    }
    if (minWind === Infinity) minWind = 0;
    var avgTemp = tempCount > 0 ? Math.round(sumTemp / tempCount) : null;
    var avgHumidity = humidityCount > 0 ? Math.round(sumHumidity / humidityCount) : null;

    // ── Lookahead window — next 24 hours after the plan window ──
    // For concrete and painting, rain after the pour/application can still
    // ruin the work. We track the worst precip in the next 24h and how soon
    // any rain (≥25%) appears.
    var lookaheadMaxPrecip = 0;
    var hoursUntilRain = null;
    var willRainWithin = false;
    if (windowEndAbs !== -1) {
      for (var k = windowEndAbs + 1; k < hourly.time.length && k <= windowEndAbs + 24; k++) {
        var lp = hourly.precipitation_probability[k] || 0;
        if (lp > lookaheadMaxPrecip) lookaheadMaxPrecip = lp;
        if (lp >= 25 && hoursUntilRain === null) {
          hoursUntilRain = k - windowEndAbs;
          willRainWithin = true;
        }
      }
    }

    // ── Convective potential (the "scattered storms" signal) ──
    // Open-Meteo's hourly precip_probability is a point-forecast value.
    // Scattered convective storms read low at any single grid point even
    // when there's real risk to the venue. We look at:
    //   1. Weather codes in the 6h surrounding the window (95+ thunderstorm,
    //      80-82 rain showers — these signal active convection nearby)
    //   2. Storm coverage trend (rising precip prob across the next 6h)
    //   3. Maximum precip prob anywhere in a wider 6h band around the window
    var convectiveActivity = false;     // true if thunderstorm/shower codes appear nearby
    var nearbyStormHour = null;         // how many hours away is the nearest storm code?
    var maxPrecipNearby = 0;            // worst precip prob in the 6h surrounding the window
    var trendRising = false;            // storm coverage increasing through the evening?
    var trendDelta = 0;                 // % point increase from window start to +6h
    if (windowEndAbs !== -1 && inWindowIdx.length > 0) {
      var windowStartAbs = inWindowIdx[0];
      // Sample 3 hours before the window start through 6 hours after window end
      var nearStart = Math.max(0, windowStartAbs - 3);
      var nearEnd = Math.min(hourly.time.length - 1, windowEndAbs + 6);
      for (var n = nearStart; n <= nearEnd; n++) {
        var nc = hourly.weather_code[n] || 0;
        var np = hourly.precipitation_probability[n] || 0;
        // Thunderstorm codes (95-99) or rain shower codes (80-82) signal
        // active convection — even a single hour of these in the surrounding
        // band means radar is showing returns somewhere near the venue.
        if (nc >= 95 || (nc >= 80 && nc <= 82)) {
          convectiveActivity = true;
          var hoursAway = Math.abs(n - windowStartAbs);
          if (nearbyStormHour === null || hoursAway < nearbyStormHour) {
            nearbyStormHour = hoursAway;
          }
        }
        if (np > maxPrecipNearby) maxPrecipNearby = np;
      }
      // Trend: compare in-window max to 6h-after max. A rising trend means
      // coverage is expanding through the evening — high relevance for
      // outdoor events that run into the night.
      var inWindowMaxPrecip = maxPrecip;
      var afterWindowMaxPrecip = 0;
      var afterStart = windowEndAbs + 1;
      var afterEnd = Math.min(hourly.time.length - 1, windowEndAbs + 6);
      for (var a = afterStart; a <= afterEnd; a++) {
        var ap = hourly.precipitation_probability[a] || 0;
        if (ap > afterWindowMaxPrecip) afterWindowMaxPrecip = ap;
      }
      trendDelta = afterWindowMaxPrecip - inWindowMaxPrecip;
      // A 15-point or more rise across 6h after the window is a meaningful trend
      if (trendDelta >= 15) trendRising = true;
    }

    // ── NWS alert merge ──
    // A Severe or Extreme alert that's Immediate or Expected escalates to Risk
    // regardless of forecast values. Moderate alerts add Caution.
    var nwsEscalation = null;
    var primaryAlert = null;
    if (alerts && alerts.length > 0) {
      // Sort alerts: Severe/Extreme > Moderate > Minor; within tier, Immediate first
      var sevRank = { 'Extreme': 4, 'Severe': 3, 'Moderate': 2, 'Minor': 1, 'Unknown': 0 };
      var urgRank = { 'Immediate': 3, 'Expected': 2, 'Future': 1, 'Past': 0, 'Unknown': 0 };
      alerts.sort(function(a, b) {
        var sa = sevRank[a.severity] || 0, sb = sevRank[b.severity] || 0;
        if (sa !== sb) return sb - sa;
        var ua = urgRank[a.urgency] || 0, ub = urgRank[b.urgency] || 0;
        return ub - ua;
      });
      primaryAlert = alerts[0];
      if (primaryAlert.severity === 'Extreme') nwsEscalation = 'risk';
      else if (primaryAlert.severity === 'Severe') nwsEscalation = 'risk';
      else if (primaryAlert.severity === 'Moderate') nwsEscalation = 'caution';
    }

    // ── Run the industry-aware status engine ──
    var planResult = computeStatusForPlan(ticket.industry || 'default', {
      precipPct: maxPrecip,
      windMph: maxWind,
      gustMph: maxGust,
      weatherCode: maxCode,
      tempF: avgTemp,
      humidityPct: avgHumidity
    }, {
      maxPrecipPct: lookaheadMaxPrecip,
      willRainWithin: willRainWithin,
      hoursUntilRain: hoursUntilRain,
      // Convective potential signals — primarily for outdoor events
      convectiveActivity: convectiveActivity,
      nearbyStormHour: nearbyStormHour,
      maxPrecipNearby: maxPrecipNearby,
      trendRising: trendRising,
      trendDelta: trendDelta
    });

    // Apply the NWS escalation if it's stricter than the rules engine result
    var rank = { clear: 0, caution: 1, risk: 2 };
    var statusKey = planResult.status;
    var reasons = planResult.reasons.slice();
    if (nwsEscalation && rank[nwsEscalation] > rank[statusKey]) {
      statusKey = nwsEscalation;
      reasons.unshift('NWS ' + primaryAlert.event + ' active');
    } else if (primaryAlert) {
      // Even if we didn't escalate, surface the alert so the user knows it's there
      reasons.unshift('NWS ' + primaryAlert.event + ' in effect');
    }

    var sky = skyConditionFromCode(maxCode);
    var winLow = Math.round(minWind), winHigh = Math.round(maxWind);
    var windLabel = (winHigh - winLow >= 2) ? (winLow + '–' + winHigh + ' mph') : (winHigh + ' mph');

    // Confidence — refined by status and source breadth.
    // More sources = higher confidence; risk/caution naturally implies
    // less certainty than a clear forecast.
    var sourceCount = 1; // Open-Meteo always
    if (alerts !== null) sourceCount++; // NWS responded (even if no alerts)
    var confidence = 94;
    if (statusKey === 'caution') confidence = 82;
    if (statusKey === 'risk') confidence = 68;
    if (sourceCount >= 2 && statusKey === 'clear') confidence = 96; // bumped slightly when both sources clear

    // ── Build the verifier strings — show actual contributing sources ──
    var precipVerifier = 'Open-Meteo · HRRR';
    var windVerifier = 'Open-Meteo · ECMWF';
    var skyVerifier = 'Open-Meteo';
    if (primaryAlert) {
      // Surface NWS attribution on whichever stat the alert most relates to
      var ev = (primaryAlert.event || '').toLowerCase();
      if (ev.indexOf('wind') !== -1 || ev.indexOf('gale') !== -1) {
        windVerifier = 'NWS · ' + primaryAlert.event;
      } else if (ev.indexOf('rain') !== -1 || ev.indexOf('flood') !== -1 || ev.indexOf('storm') !== -1) {
        precipVerifier = 'NWS · ' + primaryAlert.event;
      } else {
        skyVerifier = 'NWS · ' + primaryAlert.event;
      }
    }

    // ── Build the source roster shown in the ticket footer ──
    var sources = [{ name: 'Open-Meteo', kind: 'forecast', detail: 'HRRR + ECMWF blend' }];
    if (alerts !== null) {
      sources.push({
        name: 'NWS',
        kind: 'alerts',
        detail: alerts.length === 0 ? 'No active alerts' : (alerts.length + ' active ' + (alerts.length === 1 ? 'alert' : 'alerts'))
      });
    }

    return {
      statusKey: statusKey,
      summary: planResult.summaryOverride || STATUS[statusKey].summary,
      verb: STATUS[statusKey].verb,
      verbClass: STATUS[statusKey].verbClass,
      pillClass: STATUS[statusKey].pillClass,
      tail: STATUS[statusKey].tail,
      label: STATUS[statusKey].label,
      confidence: confidence,
      avgTempF: avgTemp,
      avgHumidityPct: avgHumidity,
      // Raw values used by the AI explainer endpoint. The stats array
      // above carries display-formatted strings; the explainer needs
      // numbers it can reason with.
      rawWindMph: Math.round(maxWind),
      rawGustMph: Math.round(maxGust),
      rawPrecipPct: Math.round(maxPrecip),
      stats: [
        { key: 'Precipitation', value: Math.round(maxPrecip) + '%', verifier: precipVerifier },
        { key: 'Wind',          value: windLabel,                   verifier: windVerifier },
        { key: 'Sky',           value: sky,                         verifier: skyVerifier }
      ],
      reasons: reasons,
      lookaheadConcern: planResult.lookaheadConcern,
      alerts: alerts || [],
      primaryAlert: primaryAlert,
      sources: sources,
      source: 'live'
    };
  }

  /**
   * Build a synthetic reading using the deterministic seed (offline fallback).
   * Returns the same shape as computeReadingFromForecast so renderTicket
   * doesn't need to know which path produced it.
   */
  function buildSyntheticReading(ticket) {
    var statusKey = computeStatus(ticket.seed);
    var s = STATUS[statusKey];
    return {
      statusKey: statusKey,
      summary: s.summary,
      verb: s.verb,
      verbClass: s.verbClass,
      pillClass: s.pillClass,
      tail: s.tail,
      label: s.label,
      confidence: s.confidence,
      avgTempF: null,
      avgHumidityPct: null,
      rawWindMph: null,
      rawGustMph: null,
      rawPrecipPct: null,
      stats: s.stats,
      reasons: [],          // No reasons in fallback — renderer hides the block
      lookaheadConcern: null,
      alerts: [],
      primaryAlert: null,
      sources: [],          // No sources roster shown for fallback tickets
      source: 'fallback'
    };
  }

  /**
   * The orchestrator. Takes a parsed ticket, returns a Promise of {ticket, reading}.
   * Falls back to synthetic data on any unrecoverable failure (network down, etc.)
   * but surfaces a specific error if the city can't be geocoded.
   */
  function resolveTicket(ticket) {
    // ── Route handling: multiple locations require multi-geocode + multi-fetch ──
    // We geocode all stops in parallel, fetch all forecasts in parallel,
    // compute a reading for each stop, then pick the worst-case verdict
    // across the route. The ticket displays the worst-case reading with
    // metadata listing all stops that contributed.
    if (ticket.locations && ticket.locations.length > 1) {
      return resolveRoute(ticket);
    }

    // ── No city in the query at all ──────────────────────────────────────
    // Don't silently default to any town. WeatherValet is nationwide and
    // a fixed default would be wrong for almost every user. Surface a
    // clean, specific error that the missing-input UX renders as a
    // friendly nudge with a corrected example.
    if (!ticket.city) {
      var err = new Error('Add a location to your plan, like "in Lebanon, IN" or "in St. Louis, MO".');
      err.code = 'NO_LOCATION';
      return Promise.reject(err);
    }

    return geocodeCity(ticket.city, ticket.stateCode)
      .then(function(geo) {
        if (!geo) {
          // Two failure modes to distinguish:
          //   (a) City genuinely doesn't exist anywhere (typo)
          //   (b) City exists but not in the state the user specified
          var msg;
          if (ticket.stateCode) {
            msg = "We couldn't find \"" + ticket.city + "\" in " + (US_STATES[ticket.stateCode] || ticket.stateCode) + ". Double-check the spelling, or try without the state.";
          } else {
            msg = "We couldn't find \"" + ticket.city + "\". Try adding a state, like \"in Lebanon, IN\".";
          }
          var err = new Error(msg);
          err.code = 'GEOCODE_NOT_FOUND';
          throw err;
        }
        ticket.city = geo.name;
        ticket.area = geo.zip
          ? (geo.name + ', ' + geo.state + ' · ' + geo.zip)
          : (geo.state ? (geo.name + ', ' + geo.state) : geo.name);
        ticket.lat = geo.latitude;
        ticket.lng = geo.longitude;
        ticket.resolvedState = geo.state;
        ticket.alternates = (!ticket.stateCode && geo.alternates && geo.alternates.length > 0)
          ? geo.alternates
          : [];

        return Promise.all([
          fetchForecast(geo.latitude, geo.longitude),
          fetchNWSAlerts(geo.latitude, geo.longitude).catch(function() { return null; })
        ]);
      })
      .then(function(results) {
        var forecast = results[0];
        var alerts = results[1];

        if (!forecast) {
          return { ticket: ticket, reading: buildSyntheticReading(ticket) };
        }
        var reading = computeReadingFromForecast(ticket, forecast, alerts);
        if (!reading) {
          return { ticket: ticket, reading: buildSyntheticReading(ticket) };
        }
        return { ticket: ticket, reading: reading };
      });
  }

  /**
   * Resolve a route — multiple locations the user cares about.
   *
   * For now this is "worst case across stops" — we geocode each stop, fetch
   * each forecast, compute a reading per stop, and present the stop with the
   * highest-severity status. The ticket carries `resolvedStops[]` so the UI
   * can show what the route considered.
   *
   * What this is NOT yet: time-of-arrival modeling. A 3-hour drive from
   * Chicago to Indianapolis would mean Chicago weather at hour T, Indy
   * weather at hour T+3 — but for v2.4 we use the same plan window for all
   * stops. Good enough for "is this trip a bad idea?" — not yet good enough
   * for "which segment will I hit storms in?"
   */
  /**
   * Estimate driving time between two lat/lng points using great-circle
   * distance + an average highway-mix speed.
   *
   * This is a HEURISTIC — without a routing API (Mapbox Directions / Google
   * Maps), we can't account for actual road network, traffic, terrain. But
   * it's the right order-of-magnitude for the user-facing question:
   * "is the weather different by the time I get there?"
   *
   * Empirically: as-the-crow-flies miles × 1.2 (route inefficiency) ÷ 55 mph
   * lands within about 20% of Google Maps for trips between 50 and 500 miles.
   * Below 30 miles we cap at a 30-min minimum (city traffic dominates).
   *
   * Returns hours (float).
   */
  function estimateDriveHours(lat1, lng1, lat2, lng2) {
    var R = 3958.8; // earth radius in miles
    var toRad = function(d) { return d * Math.PI / 180; };
    var dLat = toRad(lat2 - lat1);
    var dLng = toRad(lng2 - lng1);
    var a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
            Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) *
            Math.sin(dLng / 2) * Math.sin(dLng / 2);
    var c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    var miles = R * c;
    var roadMiles = miles * 1.2; // route inefficiency factor
    var avgMph = 55;
    var hours = roadMiles / avgMph;
    // Floor at 30 minutes — under that, city traffic dominates and our
    // simple model isn't useful anyway
    return Math.max(0.5, hours);
  }

  /**
   * Format an hour offset (e.g. 2.5 hours) as a friendly arrival time string.
   * Takes the start hour (24h int) and offset hours, returns "1 PM" / "5:30 PM".
   */
  function offsetHourLabel(startHour24, offsetHours) {
    var totalHours = startHour24 + offsetHours;
    var dayHours = ((totalHours % 24) + 24) % 24;
    var hour24 = Math.floor(dayHours);
    var minutes = Math.round((dayHours - hour24) * 60);
    var period = hour24 >= 12 ? 'PM' : 'AM';
    var hour12 = hour24 % 12 || 12;
    if (minutes === 0) return hour12 + ' ' + period;
    if (minutes < 10) return hour12 + ':0' + minutes + ' ' + period;
    return hour12 + ':' + minutes + ' ' + period;
  }

  function resolveRoute(ticket) {
    var stops = ticket.locations;
    // Geocode every stop in parallel
    return Promise.all(stops.map(function(s) {
      return geocodeCity(s.city, s.stateCode).then(function(g) {
        return { input: s, geo: g };
      });
    })).then(function(geocoded) {
      var failed = geocoded.filter(function(g) { return !g.geo; });
      if (failed.length > 0) {
        var bad = failed[0].input;
        var msg = "We couldn't find \"" + bad.city + "\"" + (bad.stateCode ? (' in ' + (US_STATES[bad.stateCode] || bad.stateCode)) : '') + " for your route.";
        var err = new Error(msg);
        err.code = 'GEOCODE_NOT_FOUND';
        throw err;
      }

      // ── Compute cumulative drive-time offset for each stop ──
      // Stop 0 (start) gets offset = 0. Each subsequent stop's offset is
      // the previous stop's offset PLUS the drive from prev to current.
      // This becomes the per-stop time delta from the user's specified
      // start hour, used to sample the right forecast hours per stop.
      var offsets = [0];
      var cumulativeHours = 0;
      for (var i = 1; i < geocoded.length; i++) {
        var prev = geocoded[i - 1].geo;
        var curr = geocoded[i].geo;
        cumulativeHours += estimateDriveHours(prev.latitude, prev.longitude,
                                              curr.latitude, curr.longitude);
        offsets.push(cumulativeHours);
      }

      // Fetch forecast + alerts for every stop in parallel
      return Promise.all(geocoded.map(function(g, idx) {
        return Promise.all([
          fetchForecast(g.geo.latitude, g.geo.longitude),
          fetchNWSAlerts(g.geo.latitude, g.geo.longitude).catch(function() { return null; })
        ]).then(function(rs) {
          return {
            geo: g.geo,
            forecast: rs[0],
            alerts: rs[1],
            offsetHours: offsets[idx],
          };
        });
      }));
    }).then(function(stopData) {
      // Compute a reading for each stop, with a TIME-OFFSET window per stop
      var statusRank = { clear: 0, caution: 1, risk: 2 };
      var readings = stopData.map(function(s) {
        // Shift the forecast window by the cumulative drive offset.
        // Round to nearest hour (forecast resolution is 1h anyway).
        var hourShift = Math.round(s.offsetHours);
        var shiftedStart = Math.max(0, Math.min(23, ticket.windowStartHour + hourShift));
        var shiftedEnd   = Math.max(0, Math.min(23, ticket.windowEndHour + hourShift));
        // If the shift crosses midnight, the forecast won't match; for v1
        // we cap at 23:00 same day and accept the loss of precision.
        // (A future version with multi-day forecasts can do this properly.)

        var stopTicket = Object.assign({}, ticket, {
          city: s.geo.name,
          lat: s.geo.latitude,
          lng: s.geo.longitude,
          windowStartHour: shiftedStart,
          windowEndHour: shiftedEnd,
        });
        if (!s.forecast) {
          return { geo: s.geo, reading: buildSyntheticReading(stopTicket),
                   offsetHours: s.offsetHours };
        }
        var r = computeReadingFromForecast(stopTicket, s.forecast, s.alerts);
        return { geo: s.geo, reading: r || buildSyntheticReading(stopTicket),
                 offsetHours: s.offsetHours };
      });

      // Sort by status severity for headline; preserve original index for display
      var sortedByStatus = readings.slice().sort(function(a, b) {
        return (statusRank[b.reading.statusKey] || 0) - (statusRank[a.reading.statusKey] || 0);
      });
      var worst = sortedByStatus[0];

      // Surface route metadata on the ticket — INCLUDING per-stop arrival times
      ticket.resolvedStops = readings.map(function(r) {
        return {
          name: r.geo.name,
          state: r.geo.state,
          status: r.reading.statusKey,
          offsetHours: r.offsetHours,
          arrivalLabel: offsetHourLabel(ticket.plannedHour || ticket.windowStartHour, r.offsetHours),
        };
      });
      ticket.city = worst.geo.name;
      ticket.area = worst.geo.zip
        ? (worst.geo.name + ', ' + worst.geo.state + ' · ' + worst.geo.zip)
        : (worst.geo.state ? (worst.geo.name + ', ' + worst.geo.state) : worst.geo.name);
      ticket.lat = worst.geo.latitude;
      ticket.lng = worst.geo.longitude;
      ticket.alternates = [];
      // Total drive time for the trip (last stop's offset)
      ticket.totalDriveHours = readings[readings.length - 1].offsetHours;

      var routeReading = worst.reading;
      if (worst.reading.statusKey !== 'clear') {
        var arrivalContext = worst.offsetHours > 0
          ? ' at ' + worst.geo.name + ' (~' + offsetHourLabel(ticket.plannedHour || ticket.windowStartHour, worst.offsetHours) + ')'
          : ' at ' + worst.geo.name;
        routeReading.reasons = ['Worst conditions along your route' + arrivalContext]
          .concat(routeReading.reasons || []);
      }
      if (routeReading.statusKey === 'clear') {
        routeReading.summary = 'Route looks good across all stops.';
      } else if (routeReading.statusKey === 'caution') {
        routeReading.summary = 'Watch ' + worst.geo.name + ' along your route.';
      } else {
        routeReading.summary = worst.geo.name + ' is the rough patch on your route.';
      }
      return { ticket: ticket, reading: routeReading };
    });
  }

  // ════════════════════════════════════════════════════════════════════
  // Submission flow — wired to real data with synthetic fallback
  // ════════════════════════════════════════════════════════════════════
  function handleSubmit(submittedQuery) {
    var query = (submittedQuery || '').trim();
    // Empty submit: nothing to do. The form's native required validation
    // already prevents reaching here from the search bar; this guard
    // protects programmatic callers and chip data attributes.
    if (!query) return;

    currentQuery = query;
    var ticket = parseQuery(query);
    if (!ticket) return;

    showScreen('screen-handoff');

    // Kick off the API resolution immediately (don't wait for the handoff animation).
    // We also enforce a minimum dwell on the handoff screen so the brand moment lands —
    // resolving the API faster than 2.4s shouldn't make the experience feel rushed.
    var startedAt = Date.now();
    var MIN_HANDOFF_MS = 2400;

    resolveTicket(ticket)
      .then(function(result) {
        var elapsed = Date.now() - startedAt;
        var wait = Math.max(0, MIN_HANDOFF_MS - elapsed);
        setTimeout(function() {
          renderTicket(result.ticket, result.reading);
          showScreen('screen-ticket');
        }, wait);
      })
      .catch(function(err) {
        // Hard failure (geocode miss). Show the error in place of the ticket
        // after the handoff dwell. Don't silently fake data.
        var elapsed = Date.now() - startedAt;
        var wait = Math.max(0, MIN_HANDOFF_MS - elapsed);
        setTimeout(function() {
          renderTicketError(err);
          showScreen('screen-ticket');
        }, wait);
      });
  }

  /**
   * Render a graceful error state when the geocode misses entirely.
   * The user gets a clear message about what to fix and a button back to search.
   * Uses the same ticket-card aesthetic so the screen doesn't feel like a 500 page.
   */
  function renderTicketError(err) {
    var msg = err && err.message ? err.message : "Something went wrong while looking up your plan.";
    // Intent-aware headline: "add a location" cases get a different framing
    // than "we looked but couldn't find it" cases. Same visual treatment;
    // the words just feel right for each situation.
    var headline = (err && err.code === 'NO_LOCATION')
      ? 'Where is this happening?'
      : 'We couldn\'t find that one.';
    $('ticket-content').innerHTML = ''
      + '<div class="ticket-card ticket-card-error">'
      +   '<div class="ticket-error-head">'
      +     '<div class="ticket-error-icon">'
      +       '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'
      +         '<circle cx="12" cy="12" r="10"/><path d="M12 8v4"/><path d="M12 16h.01"/>'
      +       '</svg>'
      +     '</div>'
      +     '<h2 class="ticket-error-title">' + escapeHtml(headline) + '</h2>'
      +   '</div>'
      +   '<p class="ticket-error-body">' + escapeHtml(msg) + '</p>'
      +   '<button id="ticket-error-back" class="ticket-error-cta" type="button">'
      +     '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="m12 19-7-7 7-7"/><path d="M19 12H5"/></svg>'
      +     '<span>Edit your plan</span>'
      +   '</button>'
      + '</div>';
    var backBtn = $('ticket-error-back');
    if (backBtn) backBtn.addEventListener('click', handleBack);
  }

  function handleBack() {
    var input = $('search-input');
    if (input) input.value = currentQuery;
    showScreen('screen-welcome');
    setTimeout(function() { if (input) input.focus(); }, 100);
  }

  // ════════════════════════════════════════════════════════════════════════
  // DEV MENU + SIMULATED MET / ADMIN VIEWS
  // ════════════════════════════════════════════════════════════════════════
  // The dev menu is gated on `?dev=1` in the URL. When present, a small
  // floating chip appears in the bottom-left corner with three role
  // toggles: Customer / Meteorologist / Admin. Customer is the default
  // (and is whatever the user was already on); Meteorologist and Admin
  // route to the simulated portal screens.
  //
  // Important: the simulated views are UI mockups, not connections to the
  // real Flask backend. Every "Submit review" button etc. is a toast-and-
  // dismiss interaction. That's appropriate for a prototype — testers
  // get to react to the experience without us building the real auth and
  // data flow until we know the experience is right.

  // Mock review queue for the Met view. Five items spanning all three
  // pricing tiers with realistic plans, cities, timing, and AI-suggested
  // verdicts. The "minutes-ago-paid" values drive urgency styling.
  var MET_QUEUE_MOCK = [
    {
      id: 'rev-1',
      tier: 'pro',
      tierLabel: 'Pro',
      customerName: 'Marcus Bell, Bell Concrete',
      plan: 'Concrete pour at 9 AM Saturday for foundation slab',
      area: 'Lebanon, IN \u00b7 46052',
      paidMinutesAgo: 22,
      aiVerdict: 'caution',
      aiConfidence: 71,
      aiReason: 'Wind gusts up to 24 mph after 11 AM. Surface temp 58\u00b0F at pour time \u2014 marginal for rapid set.',
      exampleReason: 'Pour timing of 9 AM is good \u2014 winds are still light through 10:30. Surface temp climbs to 62\u00b0F by 9:30, which is in your acceptable range. The AI is right that gusts pick up after 11 AM, but you\u2019ll be screeded and finishing by then.',
      exampleWatch: 'Cell of cumulus development west of you around 12:30. If your finishing crew is still on the slab at that point, watch for sudden gust front. NWS not flagging it but HRRR has it on radar.',
      exampleBottomLine: 'Pour as planned. Get the slab screeded by 11. Have plastic sheeting on standby for the 12:30 storm window in case it slides over you faster than expected.',
      exampleAlternate: ''
    },
    {
      id: 'rev-2',
      tier: 'single',
      tierLabel: 'Single',
      customerName: 'Rachel Greene, private wedding',
      plan: 'Outdoor wedding ceremony at 4 PM Saturday',
      area: 'Charlotte, NC',
      paidMinutesAgo: 14,
      aiVerdict: 'clear',
      aiConfidence: 88,
      aiReason: 'Mostly sunny, light SE winds 6-9 mph, 5% chance of precipitation in window.',
      exampleReason: 'Confirming Clear. The SE winds at 6-9 mph are pleasant, not problematic for veil or dress. Temperature at 4 PM forecast 76\u00b0F with low humidity. No precipitation expected through 8 PM.',
      exampleWatch: 'Sun angle drops behind tall trees around 6 PM \u2014 worth knowing for outdoor photo timing if photographer hasn\u2019t scouted.',
      exampleBottomLine: 'Beautiful day for it. Plan as scheduled. The only thing I\u2019d adjust is letting your photographer know about the 6 PM tree shadow if outdoor shots run that late.',
      exampleAlternate: ''
    },
    {
      id: 'rev-3',
      tier: 'day',
      tierLabel: 'Day Pass',
      customerName: 'Westfield Roofing crew',
      plan: 'Roofing job tomorrow 8 AM start, full day',
      area: 'Chicago, IL',
      paidMinutesAgo: 8,
      aiVerdict: 'risk',
      aiConfidence: 64,
      aiReason: 'Frontal passage estimated 11:00\u201313:00 with possible thunderstorm development. High shear.',
      exampleReason: 'Confirming Risk. The front is real and the shear environment supports thunderstorm development. Models are unanimous that storms cross your area between 11:30 and 13:30. You will not be able to safely work through this window.',
      exampleWatch: 'Lightning is the safety issue, not just the wind. Once cloud-to-ground strikes are within 10 miles, get crews off the roof immediately. Storms may also produce damaging winds 50-60 mph.',
      exampleBottomLine: 'Don\u2019t start a full day. Either push to a different day, or plan to be tarped down and off the roof by 11 AM. Lightning will not give you warning.',
      exampleAlternate: 'Sunday looks materially better \u2014 Clear with light NW winds, no precip until late evening.'
    },
    {
      id: 'rev-4',
      tier: 'single',
      tierLabel: 'Single',
      customerName: 'James Holloway',
      plan: 'Bike ride at 5 PM Friday \u2014 100 mile route',
      area: 'St. Louis, MO',
      paidMinutesAgo: 4,
      aiVerdict: 'caution',
      aiConfidence: 76,
      aiReason: 'Wind shifts to NW after 16:00, gusts 18-22 mph. Direction unfavorable for west-bound legs.',
      exampleReason: 'AI is right about the wind shift, but for a 100-mile route the magnitude matters as much as direction. NW gusts of 22 mph against your westbound legs (roughly miles 40-65) will cost real time and energy.',
      exampleWatch: 'Wind speeds peak around 18:00. If you start at 5 PM, you\u2019ll hit the worst of it on the long west-northwest stretch through Chesterfield. Heat index is fine \u2014 80\u00b0F dropping to 72\u00b0F by sunset.',
      exampleBottomLine: 'Doable but harder than typical. If you can swap to an early start (6 AM Saturday), you\u2019ll have the wind at your back on the same route and save 20-30 minutes.',
      exampleAlternate: 'Saturday 6 AM start \u2014 light SE winds 5-8 mph favorable for the west-bound first half.'
    }
  ];

  // ─── Real Met queue (Item #2) ──────────────────────────────────────
  // Holds real verification_requests fetched from the backend. Rendered
  // BEFORE the mock rows in renderMetQueue, so real customers always
  // get attention first. Mock rows stay for demo continuity until the
  // app has organic traffic.
  //
  // Shape mirrors what /api/v1/met/queue returns. We project these into
  // the same shape openMetComposer expects so the existing UI just works.
  var MET_REAL_QUEUE = [];

  function loadMetQueueFromBackend() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    return fetch(BACKEND + '/api/v1/met/queue', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          // 401 / 403 / 500 — silently fall back to mock-only display.
          // The Met may not be signed in yet; queue will reload on next entry.
          return;
        }
        var requests = res.body.requests || [];
        // Project backend rows into the shape the existing composer expects.
        // We keep BOTH the canonical fields AND populate the "example*" fields
        // from the ai_brief_markdown so the composer textareas have content.
        MET_REAL_QUEUE = requests.map(function(r) {
          return {
            id: 'req-' + r.id,             // string id, prefixed to avoid collision with mock 'rev-N'
            backendId: r.id,                // numeric id for backend ops
            claimToken: r.claim_token,
            isReal: true,                   // marker so submit/cancel knows to call backend
            tier: r.tier,
            tierLabel: r.tier_label,
            customerName: (r.customer_email || r.customer_phone || 'Anonymous'),
            plan: r.plan_text || '',
            area: r.plan_location || '',
            paidMinutesAgo: r.paid_minutes_ago,
            aiVerdict: r.ai_status_key || 'caution',
            aiConfidence: 70,               // not tracked yet; default
            aiReason: r.ai_brief_markdown || '',
            // The composer textareas: dump the AI brief into reason; leave
            // the rest blank so the Met fills them.
            exampleReason: r.ai_brief_markdown || '',
            exampleWatch: '',
            exampleBottomLine: '',
            exampleAlternate: '',
            status: r.status                // 'paid' or 'claimed'
          };
        });
        // Re-render to show fresh data
        renderMetQueue();
      })
      .catch(function(err) {
        console.warn('[met-queue] load failed:', err);
      });
  }

  // ════════════════════════════════════════════════════════════════════
  // Pro Briefs — Met workspace (Phase 10 Item #3 Chunk 3B-2)
  // ════════════════════════════════════════════════════════════════════
  // Loads pending + claimed + recently-sent drafts. Each pending row
  // has "Review & send" which opens the editor modal.

  var MET_PRO_BRIEFS = [];
  var MET_PRO_BRIEF_EDITING = null;  // currently-editing draft

  function loadMetProBriefs() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var list = $('met-pro-briefs-list');
    if (!list) return;
    fetch(BACKEND + '/api/v1/met/pro-briefs', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          // 401/403 — show empty state, no noise
          MET_PRO_BRIEFS = [];
          window.MET_PRO_BRIEFS = MET_PRO_BRIEFS;
          renderMetProBriefs();
          return;
        }
        MET_PRO_BRIEFS = res.body.drafts || [];
        window.MET_PRO_BRIEFS = MET_PRO_BRIEFS;  // expose for deep-linking
        renderMetProBriefs();
      })
      .catch(function(err) {
        console.warn('[pro-briefs] load failed:', err);
      });
  }

  function _proBriefTierLabel(tier) {
    if (tier === 'pro_multi') return 'Pro Multi';
    if (tier === 'pro_enterprise') return 'Pro Enterprise';
    return 'Pro Single';
  }

  function _proBriefVerdictPill(v) {
    var label = (v || 'caution').charAt(0).toUpperCase() + (v || 'caution').slice(1);
    var bg = v === 'clear' ? 'rgba(65,200,130,0.15)'
            : v === 'risk' ? 'rgba(194,52,43,0.15)'
            : 'rgba(255,184,0,0.15)';
    var color = v === 'clear' ? '#5DECA8' : v === 'risk' ? '#E89890' : '#FFCC4D';
    return '<span style="background:' + bg + ';color:' + color
      + ';padding:2px 8px;border-radius:3px;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;">'
      + escapeHtml(label) + '</span>';
  }

  function _proBriefMinutesRemaining(windowEndMs) {
    if (!windowEndMs) return null;
    var now = Date.now();
    var mins = Math.round((windowEndMs - now) / 60000);
    return mins;
  }

  function _proBriefDeadlineLabel(d) {
    if (d.status === 'sent') {
      var sentMins = Math.round((Date.now() - (d.sent_at || 0)) / 60000);
      if (sentMins < 1) return 'sent just now';
      if (sentMins < 60) return 'sent ' + sentMins + ' min ago';
      return 'sent ' + Math.round(sentMins / 60) + ' hr ago';
    }
    var mins = _proBriefMinutesRemaining(d.window_end_at);
    if (mins === null) return '';
    if (mins < 0) return '<span style="color:#E89890;">past window</span>';
    if (mins < 15) return '<span style="color:#FFCC4D;">' + mins + ' min left</span>';
    if (mins < 60) return mins + ' min left';
    return Math.round(mins / 60) + ' hr left';
  }

  // ───────────────────────────────────────────────────────────────
  // Pro Brief prototype (May 17, 2026) — shown in the Pro Briefs tab
  // when no real drafts exist yet. This is the proposed structured
  // format for launch. Three TEST subscribers, three views each
  // (SMS, email, web view). All data is fake. Replaced automatically
  // by real draft cards once Pro-tier subscribers come online.
  // ───────────────────────────────────────────────────────────────
  function _renderProBriefPrototype() {
    var css = ''
      + '<style>'
      + '  .pbp-banner{background:rgba(245,158,11,0.15);border:1px solid rgba(245,158,11,0.45);color:#fde68a;padding:12px 16px;border-radius:8px;font-size:13px;margin-bottom:20px;font-weight:500;line-height:1.5;}'
      + '  .pbp-sub{background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);border-radius:10px;padding:20px;margin-bottom:18px;}'
      + '  .pbp-name{font-size:16px;font-weight:700;color:#fff;margin-bottom:3px;}'
      + '  .pbp-meta{font-size:12px;color:rgba(255,255,255,0.55);margin-bottom:18px;line-height:1.4;}'
      + '  .pbp-views{display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px;}'
      + '  @media (max-width:900px){.pbp-views{grid-template-columns:1fr;}}'
      + '  .pbp-col h4{margin:0 0 8px;font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;color:rgba(255,255,255,0.5);}'
      + '  .pbp-phone{background:#1a1a1a;border-radius:16px;padding:10px 8px;max-width:240px;}'
      + '  .pbp-screen{background:#fff;color:#0f1116;border-radius:8px;padding:10px;min-height:160px;}'
      + '  .pbp-from{font-size:10px;color:rgba(15,17,22,0.55);margin-bottom:5px;}'
      + '  .pbp-msg{background:#e9f3ff;color:#0f1116;border-radius:10px;padding:9px 11px;font-size:12px;line-height:1.4;white-space:pre-wrap;word-break:break-word;}'
      + '  .pbp-email,.pbp-web{background:#fff;color:#0f1116;border-radius:6px;overflow:hidden;}'
      + '  .pbp-email-hdr{background:#f8f9fa;padding:8px 12px;border-bottom:1px solid rgba(15,17,22,0.08);font-size:10px;line-height:1.5;}'
      + '  .pbp-email-hdr .lbl{color:rgba(15,17,22,0.5);width:50px;display:inline-block;}'
      + '  .pbp-body{padding:14px 12px;font-size:12.5px;line-height:1.5;}'
      + '  .pbp-web-hdr{background:#0f172a;color:#fff;padding:10px 12px;font-size:10.5px;font-weight:600;}'
      + '  .pbp-chip{display:inline-block;padding:3px 10px;border-radius:999px;font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;}'
      + '  .pbp-chip-clear{background:rgba(18,161,80,0.18);color:#0e6e3a;}'
      + '  .pbp-chip-caution{background:rgba(245,158,11,0.22);color:#92400e;}'
      + '  .pbp-sectlbl{font-size:9.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:rgba(15,17,22,0.5);margin:11px 0 3px;}'
      + '  .pbp-sectlbl:first-child{margin-top:0;}'
      + '  .pbp-list{margin:0;padding-left:0;list-style:none;}'
      + '  .pbp-list li{padding:2px 0;font-size:12px;}'
      + '  .pbp-list li::before{content:"\\2022";color:rgba(15,17,22,0.4);margin-right:7px;}'
      + '  .pbp-sig{margin-top:12px;padding-top:10px;border-top:1px dashed rgba(15,17,22,0.1);font-size:11px;color:rgba(15,17,22,0.6);font-style:italic;}'
      + '  .pbp-reply{margin-top:10px;padding:7px 10px;background:rgba(37,99,235,0.07);border-left:3px solid #2563eb;font-size:10.5px;color:rgba(15,17,22,0.7);border-radius:0 3px 3px 0;}'
      + '  .pbp-feedback{margin-top:24px;padding:16px 20px;background:rgba(37,99,235,0.08);border:1px dashed #2563eb;border-radius:10px;font-size:13px;color:rgba(255,255,255,0.85);line-height:1.5;}'
      + '  .pbp-feedback strong{color:#60a5fa;}'
      + '</style>';

    var banner = '<div class="pbp-banner">'
      + '<strong>📐 PROTOTYPE — Pro Brief proposed format for May 24 launch</strong><br>'
      + 'Three TEST subscribers below, each shown three ways (SMS, email, web view). No real briefs are sent. Real Pro Briefs will replace this view once Pro-tier subscribers come online. <strong>Your feedback shapes the real build — reply in #brief collab on Discord.</strong>'
      + '</div>';

    // Subscriber 1: Eric Stoddard Farms (farmer)
    var sub1 = '<div class="pbp-sub">'
      + '<div class="pbp-name">Eric Stoddard Farms <span style="color:#fbbf24;font-weight:600;font-size:12px;">(TEST)</span></div>'
      + '<div class="pbp-meta">Pro Single &middot; Kansas &middot; Farmer &middot; Cares about: soil saturation, wind for spraying, frost &middot; Delivery: 6:00 AM CT &middot; Primary Met: Chris</div>'
      + '<div class="pbp-views">'
      + '<div class="pbp-col"><h4>📱 SMS view</h4>'
      +   '<div class="pbp-phone"><div class="pbp-screen">'
      +     '<div class="pbp-from">From: WeatherValet</div>'
      +     '<div class="pbp-msg">⚠️ CAUTION · Tue May 19\n\nMorning workable, PM thunderstorms 2-5 PM (65%). Spray window before noon if winds stay below 12 mph.\n\nFull brief: weathervalet.ai/brief/k3p9\n— Chris</div>'
      +   '</div></div>'
      + '</div>'
      + '<div class="pbp-col"><h4>📧 Email view</h4>'
      +   '<div class="pbp-email">'
      +     '<div class="pbp-email-hdr">'
      +       '<div><span class="lbl">From:</span> Chris &lt;briefs@weathervalet.ai&gt;</div>'
      +       '<div><span class="lbl">To:</span> Eric Stoddard Farms</div>'
      +       '<div><span class="lbl">Subject:</span> Your brief — Tue May 19</div>'
      +     '</div>'
      +     '<div class="pbp-body">'
      +       '<div style="font-size:10px;color:rgba(15,17,22,0.5);margin-bottom:6px;">Eric Stoddard Farms · Tuesday, May 19, 2026</div>'
      +       '<div class="pbp-sectlbl">Today\'s call</div><span class="pbp-chip pbp-chip-caution">⚠️ Caution</span>'
      +       '<div class="pbp-sectlbl">Bottom line</div>'
      +       '<div>Morning starts clear and dry. Afternoon brings 60-70% chance of thunderstorms 2-5 PM, then clearing by dusk. One spray window before lunch if winds cooperate.</div>'
      +       '<div class="pbp-sectlbl">Key numbers</div>'
      +       '<ul class="pbp-list">'
      +         '<li>Soil moisture: 0.4" past 3 days (workable AM)</li>'
      +         '<li>Wind: 5-12 mph SW, gusts to 18</li>'
      +         '<li>Temp: 58° → 76°</li>'
      +         '<li>Rain chance: 65% (2-5 PM), 0% rest of day</li>'
      +       '</ul>'
      +       '<div class="pbp-sectlbl">What to watch</div>'
      +       '<div>Storms could trigger earlier if the cap breaks — watching a 1 PM window. I\'ll text if it shifts.</div>'
      +       '<div class="pbp-sig">— Chris, your meteorologist</div>'
      +       '<div class="pbp-reply">💬 Reply to this email or text the SMS to talk to Chris directly.</div>'
      +     '</div>'
      +   '</div>'
      + '</div>'
      + '<div class="pbp-col"><h4>🌐 Web view</h4>'
      +   '<div class="pbp-web">'
      +     '<div class="pbp-web-hdr">weathervalet.ai/brief/k3p9 · 🔒 logged in as Eric</div>'
      +     '<div class="pbp-body">'
      +       '<div style="font-size:10px;color:rgba(15,17,22,0.5);margin-bottom:6px;">Tuesday, May 19, 2026 · Eric Stoddard Farms</div>'
      +       '<div class="pbp-sectlbl">Today\'s call</div><span class="pbp-chip pbp-chip-caution">⚠️ Caution</span>'
      +       '<div class="pbp-sectlbl">Bottom line</div>'
      +       '<div>Morning starts clear and dry. PM thunderstorms 2-5 PM. One spray window before lunch.</div>'
      +       '<div class="pbp-sectlbl">Key numbers</div>'
      +       '<ul class="pbp-list">'
      +         '<li>Soil moisture: 0.4" past 3 days</li>'
      +         '<li>Wind: 5-12 mph SW</li>'
      +         '<li>Temp: 58° → 76°</li>'
      +         '<li>Rain: 65% (2-5 PM)</li>'
      +       '</ul>'
      +       '<div class="pbp-sectlbl">What to watch</div>'
      +       '<div>Cap break could trigger storms 1 PM. I\'ll text if it shifts.</div>'
      +       '<div style="margin-top:14px;padding-top:10px;border-top:1px solid rgba(15,17,22,0.08);font-size:11px;">'
      +         '<a href="#" style="color:#2563eb;text-decoration:none;">📨 Reply to Chris</a> &nbsp;·&nbsp; '
      +         '<a href="#" style="color:#2563eb;text-decoration:none;">📜 Past briefs</a>'
      +       '</div>'
      +       '<div class="pbp-sig">— Chris, your meteorologist</div>'
      +     '</div>'
      +   '</div>'
      + '</div>'
      + '</div></div>';

    // Subscriber 2: Dunker HS Athletics
    var sub2 = '<div class="pbp-sub">'
      + '<div class="pbp-name">Dunker High School Athletics <span style="color:#fbbf24;font-weight:600;font-size:12px;">(TEST)</span></div>'
      + '<div class="pbp-meta">Pro Single &middot; Kansas &middot; Athletic Director &middot; Cares about: lightning safety, rain timing for outdoor events, field conditions &middot; Delivery: 5:30 AM CT &middot; Primary Met: Chris</div>'
      + '<div class="pbp-views">'
      + '<div class="pbp-col"><h4>📱 SMS view</h4>'
      +   '<div class="pbp-phone"><div class="pbp-screen">'
      +     '<div class="pbp-from">From: WeatherValet</div>'
      +     '<div class="pbp-msg">⚠️ CAUTION · Tue May 19\n\nTrack meet 4-6 PM at risk. Storms 2-5 PM with lightning. Recommend moving events earlier or postponing.\n\nFull: weathervalet.ai/brief/m7q2\n— Chris</div>'
      +   '</div></div>'
      + '</div>'
      + '<div class="pbp-col"><h4>📧 Email view</h4>'
      +   '<div class="pbp-email">'
      +     '<div class="pbp-email-hdr">'
      +       '<div><span class="lbl">From:</span> Chris &lt;briefs@weathervalet.ai&gt;</div>'
      +       '<div><span class="lbl">To:</span> Dunker HS Athletics</div>'
      +       '<div><span class="lbl">Subject:</span> Your brief — Tue May 19</div>'
      +     '</div>'
      +     '<div class="pbp-body">'
      +       '<div style="font-size:10px;color:rgba(15,17,22,0.5);margin-bottom:6px;">Dunker HS Athletics · Tuesday, May 19, 2026</div>'
      +       '<div class="pbp-sectlbl">Today\'s call</div><span class="pbp-chip pbp-chip-caution">⚠️ Caution</span>'
      +       '<div class="pbp-sectlbl">Bottom line</div>'
      +       '<div>Today\'s track meet (4-6 PM) is in the bullseye for afternoon thunderstorms. Lightning risk is real — recommend moving events to early afternoon or postponing. Morning practice is fine.</div>'
      +       '<div class="pbp-sectlbl">Key numbers</div>'
      +       '<ul class="pbp-list">'
      +         '<li>Storm window: 2-5 PM (65% chance)</li>'
      +         '<li>Lightning risk: ELEVATED 3-6 PM</li>'
      +         '<li>Wind: 5-12 mph (no field-condition concerns)</li>'
      +         '<li>Temp: 58° → 76°, comfortable for athletes</li>'
      +         '<li>Field surface: dry overnight, no AM dew issues</li>'
      +       '</ul>'
      +       '<div class="pbp-sectlbl">What to watch</div>'
      +       '<div>Storms could fire earlier (1 PM trigger possible). I\'ll text immediately if first lightning strike is within 10 miles of the school.</div>'
      +       '<div class="pbp-sig">— Chris, your meteorologist</div>'
      +       '<div class="pbp-reply">💬 Reply to this email or text the SMS to talk to Chris directly.</div>'
      +     '</div>'
      +   '</div>'
      + '</div>'
      + '<div class="pbp-col"><h4>🌐 Web view</h4>'
      +   '<div class="pbp-web">'
      +     '<div class="pbp-web-hdr">weathervalet.ai/brief/m7q2 · 🔒 logged in as Dunker HS</div>'
      +     '<div class="pbp-body">'
      +       '<div style="font-size:10px;color:rgba(15,17,22,0.5);margin-bottom:6px;">Tuesday, May 19, 2026 · Dunker HS Athletics</div>'
      +       '<div class="pbp-sectlbl">Today\'s call</div><span class="pbp-chip pbp-chip-caution">⚠️ Caution</span>'
      +       '<div class="pbp-sectlbl">Bottom line</div>'
      +       '<div>Track meet 4-6 PM at risk. Lightning ELEVATED 3-6 PM. Move events earlier or postpone.</div>'
      +       '<div class="pbp-sectlbl">Key numbers</div>'
      +       '<ul class="pbp-list">'
      +         '<li>Storm window: 2-5 PM (65%)</li>'
      +         '<li>Lightning risk: ELEVATED 3-6 PM</li>'
      +         '<li>Wind: 5-12 mph</li>'
      +         '<li>Temp: 58° → 76°</li>'
      +         '<li>Field: dry overnight</li>'
      +       '</ul>'
      +       '<div class="pbp-sectlbl">What to watch</div>'
      +       '<div>1 PM trigger possible. I\'ll text on first lightning strike within 10 mi.</div>'
      +       '<div style="margin-top:14px;padding-top:10px;border-top:1px solid rgba(15,17,22,0.08);font-size:11px;">'
      +         '<a href="#" style="color:#2563eb;text-decoration:none;">📨 Reply to Chris</a> &nbsp;·&nbsp; '
      +         '<a href="#" style="color:#2563eb;text-decoration:none;">📜 Past briefs</a>'
      +       '</div>'
      +       '<div class="pbp-sig">— Chris, your meteorologist</div>'
      +     '</div>'
      +   '</div>'
      + '</div>'
      + '</div></div>';

    // Subscriber 3: Kevin Holle Farms Partnership
    var sub3 = '<div class="pbp-sub">'
      + '<div class="pbp-name">Kevin Holle Farms Partnership <span style="color:#fbbf24;font-weight:600;font-size:12px;">(TEST)</span></div>'
      + '<div class="pbp-meta">Pro Single &middot; Kansas &middot; Farming partnership &middot; Cares about: harvest timing, equipment ops, soil conditions, frost &middot; Delivery: 5:00 AM CT &middot; Primary Met: Chris</div>'
      + '<div class="pbp-views">'
      + '<div class="pbp-col"><h4>📱 SMS view</h4>'
      +   '<div class="pbp-phone"><div class="pbp-screen">'
      +     '<div class="pbp-from">From: WeatherValet</div>'
      +     '<div class="pbp-msg">✓ CLEAR · Tue May 19\n\nExcellent harvest day. Dry through midnight, no field-work limits. Use this window.\n\nFull: weathervalet.ai/brief/v4n7\n— Chris</div>'
      +   '</div></div>'
      + '</div>'
      + '<div class="pbp-col"><h4>📧 Email view</h4>'
      +   '<div class="pbp-email">'
      +     '<div class="pbp-email-hdr">'
      +       '<div><span class="lbl">From:</span> Chris &lt;briefs@weathervalet.ai&gt;</div>'
      +       '<div><span class="lbl">To:</span> Kevin Holle Farms</div>'
      +       '<div><span class="lbl">Subject:</span> Your brief — Tue May 19</div>'
      +     '</div>'
      +     '<div class="pbp-body">'
      +       '<div style="font-size:10px;color:rgba(15,17,22,0.5);margin-bottom:6px;">Kevin Holle Farms · Tuesday, May 19, 2026</div>'
      +       '<div class="pbp-sectlbl">Today\'s call</div><span class="pbp-chip pbp-chip-clear">✓ Clear</span>'
      +       '<div class="pbp-sectlbl">Bottom line</div>'
      +       '<div>Excellent day for harvest and field work. Dry all day, no precipitation through midnight. Light winds, mild temperatures. Take advantage — Wednesday looks similar but rain returns Thursday.</div>'
      +       '<div class="pbp-sectlbl">Key numbers</div>'
      +       '<ul class="pbp-list">'
      +         '<li>Rain chance: 0% all day, 0% overnight</li>'
      +         '<li>Wind: 5-10 mph S, no equipment limits</li>'
      +         '<li>Temp: 52° → 78°, no frost risk</li>'
      +         '<li>Humidity at sunrise: 72% (light dew, dries by 9 AM)</li>'
      +         '<li>Soil temp at 4": 64° (favorable)</li>'
      +       '</ul>'
      +       '<div class="pbp-sectlbl">What to watch</div>'
      +       '<div>Long-range models hint at a Thursday-Friday rain event, 0.5-1.0" possible. If you have wrap-up to do, today and Wednesday are your windows.</div>'
      +       '<div class="pbp-sig">— Chris, your meteorologist</div>'
      +       '<div class="pbp-reply">💬 Reply to this email or text the SMS to talk to Chris directly.</div>'
      +     '</div>'
      +   '</div>'
      + '</div>'
      + '<div class="pbp-col"><h4>🌐 Web view</h4>'
      +   '<div class="pbp-web">'
      +     '<div class="pbp-web-hdr">weathervalet.ai/brief/v4n7 · 🔒 logged in as Kevin Holle</div>'
      +     '<div class="pbp-body">'
      +       '<div style="font-size:10px;color:rgba(15,17,22,0.5);margin-bottom:6px;">Tuesday, May 19, 2026 · Kevin Holle Farms</div>'
      +       '<div class="pbp-sectlbl">Today\'s call</div><span class="pbp-chip pbp-chip-clear">✓ Clear</span>'
      +       '<div class="pbp-sectlbl">Bottom line</div>'
      +       '<div>Excellent harvest day. Dry through midnight. Light winds, mild temps.</div>'
      +       '<div class="pbp-sectlbl">Key numbers</div>'
      +       '<ul class="pbp-list">'
      +         '<li>Rain: 0% all day &amp; overnight</li>'
      +         '<li>Wind: 5-10 mph S</li>'
      +         '<li>Temp: 52° → 78°, no frost</li>'
      +         '<li>Humidity sunrise: 72%</li>'
      +         '<li>Soil temp at 4": 64°</li>'
      +       '</ul>'
      +       '<div class="pbp-sectlbl">What to watch</div>'
      +       '<div>Thursday-Friday rain (0.5-1.0"). Today/Wednesday are your windows.</div>'
      +       '<div style="margin-top:14px;padding-top:10px;border-top:1px solid rgba(15,17,22,0.08);font-size:11px;">'
      +         '<a href="#" style="color:#2563eb;text-decoration:none;">📨 Reply to Chris</a> &nbsp;·&nbsp; '
      +         '<a href="#" style="color:#2563eb;text-decoration:none;">📜 Past briefs</a>'
      +       '</div>'
      +       '<div class="pbp-sig">— Chris, your meteorologist</div>'
      +     '</div>'
      +   '</div>'
      + '</div>'
      + '</div></div>';

    // ───────────────────────────────────────────────────────────
    // Met's composer view — what Chris sees when editing a draft
    // ───────────────────────────────────────────────────────────
    var composer = ''
      + '<div style="margin-top:32px;padding-bottom:8px;border-bottom:1px solid rgba(255,255,255,0.1);">'
      +   '<div style="font-size:14px;font-weight:700;color:#fff;margin-bottom:4px;">📝 What the Met sees when editing</div>'
      +   '<div style="font-size:12px;color:rgba(255,255,255,0.55);margin-bottom:18px;">The composer view: Met edits the AI-drafted brief on the left, sees subscriber profile on the right, clicks Send.</div>'
      + '</div>'
      + '<div class="pbp-sub" style="padding:0;overflow:hidden;">'

      // Top bar of the composer
      +   '<div style="background:rgba(255,255,255,0.04);padding:14px 18px;border-bottom:1px solid rgba(255,255,255,0.08);display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;">'
      +     '<div>'
      +       '<div style="font-size:14px;font-weight:700;color:#fff;">Editing: Eric Stoddard Farms <span style="color:#fbbf24;font-size:11px;">(TEST)</span></div>'
      +       '<div style="font-size:11px;color:rgba(255,255,255,0.55);">Tuesday, May 19 · Delivery at 6:00 AM CT · 22 minutes until send</div>'
      +     '</div>'
      +     '<div style="display:flex;gap:8px;">'
      +       '<button type="button" style="background:rgba(255,255,255,0.08);border:1px solid rgba(255,255,255,0.15);color:#fff;padding:7px 14px;border-radius:6px;font-size:12px;font-weight:600;cursor:pointer;">Save draft</button>'
      +       '<button type="button" style="background:#2563eb;border:0;color:#fff;padding:7px 18px;border-radius:6px;font-size:12px;font-weight:600;cursor:pointer;">Send to subscriber →</button>'
      +     '</div>'
      +   '</div>'

      // Two-column body: editor (left) + subscriber profile (right)
      +   '<div style="display:grid;grid-template-columns:1.6fr 1fr;gap:0;">'

      // Left: editor
      +     '<div style="padding:18px 20px;border-right:1px solid rgba(255,255,255,0.08);">'
      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:6px;">Today\'s call</div>'
      +       '<div style="display:flex;gap:8px;margin-bottom:14px;">'
      +         '<button type="button" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.6);padding:6px 14px;border-radius:6px;font-size:11.5px;font-weight:600;cursor:pointer;">✓ Clear</button>'
      +         '<button type="button" style="background:rgba(245,158,11,0.22);border:1px solid #f59e0b;color:#fbbf24;padding:6px 14px;border-radius:6px;font-size:11.5px;font-weight:700;cursor:pointer;">⚠ Caution</button>'
      +         '<button type="button" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.6);padding:6px 14px;border-radius:6px;font-size:11.5px;font-weight:600;cursor:pointer;">⚠ Risk</button>'
      +       '</div>'

      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:6px;">Bottom line <span style="color:rgba(255,255,255,0.35);font-weight:400;letter-spacing:0;text-transform:none;">· 2-3 sentences</span></div>'
      +       '<div style="background:rgba(0,0,0,0.25);border:1px solid rgba(255,255,255,0.1);border-radius:6px;padding:10px 12px;font-size:13px;color:rgba(255,255,255,0.9);line-height:1.5;margin-bottom:14px;font-family:inherit;">Morning starts clear and dry. Afternoon brings 60-70% chance of thunderstorms 2-5 PM, then clearing by dusk. One spray window before lunch if winds cooperate.</div>'

      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:6px;">Key numbers <span style="color:rgba(255,255,255,0.35);font-weight:400;letter-spacing:0;text-transform:none;">· one per line</span></div>'
      +       '<div style="background:rgba(0,0,0,0.25);border:1px solid rgba(255,255,255,0.1);border-radius:6px;padding:10px 12px;font-size:13px;color:rgba(255,255,255,0.9);line-height:1.6;margin-bottom:14px;">Soil moisture: 0.4" past 3 days (workable AM)<br>Wind: 5-12 mph SW, gusts to 18<br>Temp: 58° → 76°<br>Rain chance: 65% (2-5 PM), 0% rest of day</div>'

      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:6px;">What to watch</div>'
      +       '<div style="background:rgba(0,0,0,0.25);border:1px solid rgba(255,255,255,0.1);border-radius:6px;padding:10px 12px;font-size:13px;color:rgba(255,255,255,0.9);line-height:1.5;margin-bottom:14px;">Storms could trigger earlier if the cap breaks — watching a 1 PM window. I\'ll text if it shifts.</div>'

      +       '<div style="display:flex;gap:8px;align-items:center;padding-top:8px;border-top:1px dashed rgba(255,255,255,0.1);">'
      +         '<button type="button" style="background:rgba(124,58,237,0.18);border:1px solid #7c3aed;color:#a78bfa;padding:6px 12px;border-radius:6px;font-size:11.5px;font-weight:600;cursor:pointer;">✨ Polish writing</button>'
      +         '<button type="button" style="background:rgba(255,255,255,0.05);border:1px solid rgba(255,255,255,0.12);color:rgba(255,255,255,0.7);padding:6px 12px;border-radius:6px;font-size:11.5px;font-weight:600;cursor:pointer;">👁 Preview</button>'
      +         '<div style="font-size:10.5px;color:rgba(255,255,255,0.4);margin-left:auto;">AI draft generated 5:42 AM · Last edited 1 min ago</div>'
      +       '</div>'
      +     '</div>'

      // Right: subscriber profile sidebar
      +     '<div style="padding:18px 16px;background:rgba(0,0,0,0.15);">'
      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:6px;">Subscriber profile</div>'

      +       '<div style="font-size:14px;font-weight:700;color:#fff;margin-bottom:2px;">Eric Stoddard Farms</div>'
      +       '<div style="font-size:11px;color:rgba(255,255,255,0.55);margin-bottom:14px;">Pro Single · Family farm · Kansas</div>'

      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:4px;">Cares about</div>'
      +       '<ul style="margin:0 0 14px 0;padding-left:18px;font-size:12px;color:rgba(255,255,255,0.85);line-height:1.6;">'
      +         '<li>Soil saturation</li>'
      +         '<li>Wind for spraying</li>'
      +         '<li>Frost risk</li>'
      +       '</ul>'

      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:4px;">Decision window</div>'
      +       '<div style="font-size:12px;color:rgba(255,255,255,0.85);margin-bottom:14px;">Before 7 AM</div>'

      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:4px;">Recent context</div>'
      +       '<ul style="margin:0 0 14px 0;padding-left:18px;font-size:11.5px;color:rgba(255,255,255,0.75);line-height:1.5;">'
      +         '<li>3-day rainfall: 1.2"</li>'
      +         '<li>Last brief Sat: "Conditions favorable"</li>'
      +         '<li>Last reply: 2 days ago re: spray window</li>'
      +       '</ul>'

      +       '<div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.5);margin-bottom:4px;">Channels</div>'
      +       '<div style="font-size:11.5px;color:rgba(255,255,255,0.85);">📱 SMS + 📧 Email</div>'
      +     '</div>'

      +   '</div>'
      + '</div>';

    // ───────────────────────────────────────────────────────────
    // Pending list — left-side queue showing today's subscribers
    // ordered by delivery time (earliest first)
    // ───────────────────────────────────────────────────────────
    var pendingList = ''
      + '<div style="margin-top:32px;padding-bottom:8px;border-bottom:1px solid rgba(255,255,255,0.1);">'
      +   '<div style="font-size:14px;font-weight:700;color:#fff;margin-bottom:4px;">📋 Met\'s daily queue view</div>'
      +   '<div style="font-size:12px;color:rgba(255,255,255,0.55);margin-bottom:18px;">When Chris opens Pro Briefs tab in the morning, he sees today\'s pending briefs ordered by delivery time, earliest first. Each card opens the editor above. Sent briefs gray out and move to the bottom.</div>'
      + '</div>'

      + '<div class="pbp-sub" style="padding:18px 20px;">'
      +   '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;flex-wrap:wrap;gap:8px;">'
      +     '<div style="font-size:13px;font-weight:700;color:#fff;">⏳ Pending · 7 of 12 today</div>'
      +     '<div style="font-size:11px;color:rgba(255,255,255,0.55);">All briefs due by 7:00 AM CT</div>'
      +   '</div>'

      // Pending cards — earliest delivery first
      +   '<div style="display:grid;grid-template-columns:repeat(auto-fill, minmax(240px, 1fr));gap:10px;margin-bottom:24px;">'

      // 5:00 AM - Kevin Holle Farms (was already sent in mock - show sent state)
      +     '<div style="background:rgba(18,161,80,0.08);border:1px solid rgba(18,161,80,0.3);border-radius:8px;padding:12px;opacity:0.7;">'
      +       '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">'
      +         '<div style="font-size:11px;color:#86efac;font-weight:700;">✓ SENT 5:00 AM</div>'
      +         '<div style="font-size:10px;color:rgba(255,255,255,0.4);">CLEAR</div>'
      +       '</div>'
      +       '<div style="font-size:13px;font-weight:600;color:#fff;margin-bottom:3px;">Kevin Holle Farms</div>'
      +       '<div style="font-size:10.5px;color:rgba(255,255,255,0.5);">Harvest day · clear conditions</div>'
      +     '</div>'

      // 5:30 AM - Dunker HS
      +     '<div style="background:rgba(245,158,11,0.10);border:1px solid #f59e0b;border-radius:8px;padding:12px;cursor:pointer;">'
      +       '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">'
      +         '<div style="font-size:11px;color:#fbbf24;font-weight:700;">⏳ DUE 5:30 AM</div>'
      +         '<div style="font-size:10px;color:#fbbf24;">CAUTION</div>'
      +       '</div>'
      +       '<div style="font-size:13px;font-weight:600;color:#fff;margin-bottom:3px;">Dunker HS Athletics</div>'
      +       '<div style="font-size:10.5px;color:rgba(255,255,255,0.6);">Track meet 4-6 PM at risk</div>'
      +       '<div style="margin-top:8px;padding-top:8px;border-top:1px dashed rgba(255,255,255,0.1);font-size:10px;color:#fbbf24;font-weight:600;">Open editor →</div>'
      +     '</div>'

      // 6:00 AM - Eric Stoddard
      +     '<div style="background:rgba(37,99,235,0.10);border:2px solid #2563eb;border-radius:8px;padding:12px;cursor:pointer;">'
      +       '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">'
      +         '<div style="font-size:11px;color:#60a5fa;font-weight:700;">⏳ DUE 6:00 AM · OPEN</div>'
      +         '<div style="font-size:10px;color:#fbbf24;">CAUTION</div>'
      +       '</div>'
      +       '<div style="font-size:13px;font-weight:600;color:#fff;margin-bottom:3px;">Eric Stoddard Farms</div>'
      +       '<div style="font-size:10.5px;color:rgba(255,255,255,0.6);">PM storms · spray window AM</div>'
      +       '<div style="margin-top:8px;padding-top:8px;border-top:1px dashed rgba(255,255,255,0.15);font-size:10px;color:#60a5fa;font-weight:600;">Currently editing ↑</div>'
      +     '</div>'

      // 6:15 AM
      +     '<div style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.12);border-radius:8px;padding:12px;cursor:pointer;">'
      +       '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">'
      +         '<div style="font-size:11px;color:rgba(255,255,255,0.6);font-weight:700;">⏳ DUE 6:15 AM</div>'
      +         '<div style="font-size:10px;color:rgba(255,255,255,0.5);">DRAFT</div>'
      +       '</div>'
      +       '<div style="font-size:13px;font-weight:600;color:#fff;margin-bottom:3px;">Riverside Construction</div>'
      +       '<div style="font-size:10.5px;color:rgba(255,255,255,0.5);">Pavement temps · wind limits</div>'
      +     '</div>'

      // 6:30 AM
      +     '<div style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.12);border-radius:8px;padding:12px;cursor:pointer;">'
      +       '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">'
      +         '<div style="font-size:11px;color:rgba(255,255,255,0.6);font-weight:700;">⏳ DUE 6:30 AM</div>'
      +         '<div style="font-size:10px;color:rgba(255,255,255,0.5);">DRAFT</div>'
      +       '</div>'
      +       '<div style="font-size:13px;font-weight:600;color:#fff;margin-bottom:3px;">Smith Lawn Care</div>'
      +       '<div style="font-size:10.5px;color:rgba(255,255,255,0.5);">Grass wetness · mowing window</div>'
      +     '</div>'

      // 6:45 AM
      +     '<div style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.12);border-radius:8px;padding:12px;cursor:pointer;">'
      +       '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">'
      +         '<div style="font-size:11px;color:rgba(255,255,255,0.6);font-weight:700;">⏳ DUE 6:45 AM</div>'
      +         '<div style="font-size:10px;color:rgba(255,255,255,0.5);">DRAFT</div>'
      +       '</div>'
      +       '<div style="font-size:13px;font-weight:600;color:#fff;margin-bottom:3px;">Greene Hay Co</div>'
      +       '<div style="font-size:10.5px;color:rgba(255,255,255,0.5);">Cutting window · drying time</div>'
      +     '</div>'

      // 7:00 AM
      +     '<div style="background:rgba(194,52,43,0.10);border:1px solid rgba(194,52,43,0.4);border-radius:8px;padding:12px;cursor:pointer;">'
      +       '<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;">'
      +         '<div style="font-size:11px;color:#fca5a5;font-weight:700;">⚠ DUE 7:00 AM · TIGHT</div>'
      +         '<div style="font-size:10px;color:rgba(255,255,255,0.5);">DRAFT</div>'
      +       '</div>'
      +       '<div style="font-size:13px;font-weight:600;color:#fff;margin-bottom:3px;">Lebanon Event Planning</div>'
      +       '<div style="font-size:10.5px;color:rgba(255,255,255,0.5);">Outdoor wedding · 2 PM ceremony</div>'
      +     '</div>'

      +   '</div>'

      // Sent today section
      +   '<div style="font-size:13px;font-weight:700;color:rgba(255,255,255,0.65);margin-bottom:10px;">✓ Sent today · 5 of 12</div>'
      +   '<div style="display:grid;grid-template-columns:repeat(auto-fill, minmax(240px, 1fr));gap:10px;opacity:0.55;">'

      +     '<div style="background:rgba(18,161,80,0.06);border:1px solid rgba(18,161,80,0.2);border-radius:8px;padding:10px 12px;">'
      +       '<div style="font-size:10.5px;color:#86efac;font-weight:700;margin-bottom:3px;">✓ 4:55 AM</div>'
      +       '<div style="font-size:12.5px;color:#fff;">Patel Orchards</div>'
      +     '</div>'
      +     '<div style="background:rgba(18,161,80,0.06);border:1px solid rgba(18,161,80,0.2);border-radius:8px;padding:10px 12px;">'
      +       '<div style="font-size:10.5px;color:#86efac;font-weight:700;margin-bottom:3px;">✓ 5:02 AM</div>'
      +       '<div style="font-size:12.5px;color:#fff;">Kevin Holle Farms</div>'
      +     '</div>'
      +     '<div style="background:rgba(18,161,80,0.06);border:1px solid rgba(18,161,80,0.2);border-radius:8px;padding:10px 12px;">'
      +       '<div style="font-size:10.5px;color:#86efac;font-weight:700;margin-bottom:3px;">✓ 5:08 AM</div>'
      +       '<div style="font-size:12.5px;color:#fff;">Wabash Athletics</div>'
      +     '</div>'
      +     '<div style="background:rgba(18,161,80,0.06);border:1px solid rgba(18,161,80,0.2);border-radius:8px;padding:10px 12px;">'
      +       '<div style="font-size:10.5px;color:#86efac;font-weight:700;margin-bottom:3px;">✓ 5:14 AM</div>'
      +       '<div style="font-size:12.5px;color:#fff;">Crawford Trucking</div>'
      +     '</div>'
      +     '<div style="background:rgba(18,161,80,0.06);border:1px solid rgba(18,161,80,0.2);border-radius:8px;padding:10px 12px;">'
      +       '<div style="font-size:10.5px;color:#86efac;font-weight:700;margin-bottom:3px;">✓ 5:22 AM</div>'
      +       '<div style="font-size:12.5px;color:#fff;">Hendricks Greenhouse</div>'
      +     '</div>'

      +   '</div>'

      + '</div>';

    var feedback = '<div class="pbp-feedback">'
      + '<strong>📣 For Met team review:</strong> What would you change? Section order? Different metrics per industry? More/less detail? Different verdict labels? Editor layout? Card design? Reply in <code>#brief collab</code> on Discord or text Michael directly. This is the format we\'d build for May 24 launch.'
      + '</div>';

    return css + banner + sub1 + sub2 + sub3 + pendingList + composer + feedback;
  }

  function renderMetProBriefs() {
    var list = $('met-pro-briefs-list');
    var countEl = $('met-pro-briefs-count');
    var pendingCountEl = $('met-pro-briefs-pending-count');
    if (!list) return;

    var pendingCount = 0;
    for (var i = 0; i < MET_PRO_BRIEFS.length; i++) {
      if (MET_PRO_BRIEFS[i].status === 'pending-review' || MET_PRO_BRIEFS[i].status === 'claimed') {
        pendingCount++;
      }
    }
    if (countEl) {
      countEl.textContent = pendingCount;
      countEl.hidden = pendingCount === 0;
    }
    if (pendingCountEl) pendingCountEl.textContent = pendingCount;

    if (MET_PRO_BRIEFS.length === 0) {
      // Show the structured Pro Brief prototype so Mets can review the
      // proposed format and give feedback in #brief collab on Discord.
      // Once real Pro-tier subscribers exist and have drafts, this is
      // replaced automatically by the draft cards above.
      list.innerHTML = _renderProBriefPrototype();
      return;
    }

    var html = '';
    for (var j = 0; j < MET_PRO_BRIEFS.length; j++) {
      var d = MET_PRO_BRIEFS[j];
      var statusBadge = '';
      if (d.status === 'pending-review') {
        statusBadge = '<span style="background:rgba(255,184,0,0.15);color:#FFCC4D;padding:2px 8px;border-radius:3px;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;">Pending</span>';
      } else if (d.status === 'claimed') {
        statusBadge = '<span style="background:rgba(65,105,225,0.15);color:#9BB5F0;padding:2px 8px;border-radius:3px;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;">In progress</span>';
      } else if (d.status === 'sent') {
        statusBadge = '<span style="background:rgba(65,200,130,0.15);color:#5DECA8;padding:2px 8px;border-radius:3px;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;">Sent</span>';
      }
      var actionBtn = '';
      if (d.status === 'pending-review' || d.status === 'claimed') {
        actionBtn = '<button type="button" data-pro-brief-edit="' + d.id + '" '
          + 'style="background:#2E4FB8;color:#fff;border:none;padding:8px 16px;border-radius:6px;font-size:12px;font-weight:500;cursor:pointer;">'
          + (d.status === 'claimed' ? 'Continue editing' : 'Review &amp; send')
          + '</button>';
      } else {
        actionBtn = '<span style="color:rgba(255,255,255,0.45);font-size:11px;font-family:JetBrains Mono,monospace;">by ' + escapeHtml(d.sent_by_name || '\u2014') + '</span>';
      }
      // Snippet preview — use met_snippet if Met has touched it, else AI snippet
      var previewSnippet = d.met_snippet || d.ai_snippet || '';

      html += '<div style="display:grid;grid-template-columns:1.4fr 1fr 1fr auto;gap:14px;padding:14px 0;border-bottom:1px solid rgba(255,255,255,0.06);align-items:center;">'
        +   '<div>'
        +     '<div style="display:flex;align-items:center;gap:8px;margin-bottom:4px;">'
        +       '<span style="font-weight:500;color:#fff;font-size:14px;">' + escapeHtml(d.subscriber_name || d.subscriber_email || 'Subscriber') + '</span>'
        +       statusBadge
        +     '</div>'
        +     '<div style="font-size:11.5px;color:rgba(255,255,255,0.55);">'
        +       escapeHtml(_proBriefTierLabel(d.user_tier)) + ' &middot; ' + escapeHtml(d.location_label || '\u2014')
        +     '</div>'
        +   '</div>'
        +   '<div>'
        +     '<div style="margin-bottom:4px;">' + _proBriefVerdictPill(d.met_verdict || d.ai_verdict) + '</div>'
        +     '<div style="font-size:11.5px;color:rgba(255,255,255,0.70);line-height:1.5;max-height:36px;overflow:hidden;">'
        +       escapeHtml(previewSnippet.slice(0, 120)) + (previewSnippet.length > 120 ? '\u2026' : '')
        +     '</div>'
        +   '</div>'
        +   '<div style="font-size:11.5px;color:rgba(255,255,255,0.60);">'
        +     _proBriefDeadlineLabel(d)
        +   '</div>'
        +   '<div>' + actionBtn + '</div>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  function openProBriefEditor(draftId) {
    var d = null;
    for (var i = 0; i < MET_PRO_BRIEFS.length; i++) {
      if (MET_PRO_BRIEFS[i].id === draftId) { d = MET_PRO_BRIEFS[i]; break; }
    }
    if (!d) return;
    MET_PRO_BRIEF_EDITING = d;

    var modal = $('met-pro-brief-modal');
    if (!modal) return;

    // Populate the header strip
    var nameEl = $('met-pro-brief-subscriber-name');
    var metaEl = $('met-pro-brief-subscriber-meta');
    var tierBadge = $('met-pro-brief-tier-badge');
    var channelsEl = $('met-pro-brief-channels');
    if (nameEl) nameEl.textContent = d.subscriber_name || d.subscriber_email || 'Subscriber';
    if (metaEl) metaEl.textContent = (d.subscriber_email || '') + (d.location_label ? ' \u00b7 ' + d.location_label : '');
    if (tierBadge) tierBadge.textContent = _proBriefTierLabel(d.user_tier);
    if (channelsEl) channelsEl.textContent = (d.channels || '\u2014').replace(/,/g, ', ');

    // AI starting draft (collapsible)
    var aiPreviewEl = $('met-pro-brief-ai-preview');
    if (aiPreviewEl) aiPreviewEl.textContent = d.ai_body || '(no AI draft)';

    // Verdict pills
    var verdictBtns = modal.querySelectorAll('[data-pro-verdict]');
    var currentVerdict = d.met_verdict || d.ai_verdict || 'caution';
    for (var v = 0; v < verdictBtns.length; v++) {
      verdictBtns[v].classList.toggle('is-active',
        verdictBtns[v].getAttribute('data-pro-verdict') === currentVerdict);
    }

    // Editable fields — prefer Met's existing edits over AI defaults
    var snippetEl = $('met-pro-brief-snippet');
    var bottomLineEl = $('met-pro-brief-bottom-line');
    var weatherDetailsEl = $('met-pro-brief-weather-details');
    var whatsAheadEl = $('met-pro-brief-whats-ahead');
    var imageUrlEl = $('met-pro-brief-image-url');
    var notesEl = $('met-pro-brief-notes');
    if (snippetEl) snippetEl.value = d.met_snippet || d.ai_snippet || '';
    // Structured sections: prefer the new columns if present, otherwise
    // seed Bottom Line from the legacy met_body/ai_body so old drafts
    // don't get lost. Mets can then split content into proper sections.
    if (bottomLineEl) bottomLineEl.value = d.bottom_line || d.met_body || d.ai_body || '';
    if (weatherDetailsEl) weatherDetailsEl.value = d.weather_details || '';
    if (whatsAheadEl) whatsAheadEl.value = d.whats_ahead || '';
    if (imageUrlEl) imageUrlEl.value = d.image_url || '';
    if (notesEl) notesEl.value = d.met_notes || '';

    // Show modal
    modal.hidden = false;

    // Claim it (idempotent on backend, fire-and-forget)
    if (d.status === 'pending-review') {
      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/met/pro-briefs/' + draftId + '/claim', {
        method: 'POST',
        credentials: 'include'
      }).catch(function() { /* non-fatal */ });
    }
  }

  function closeProBriefEditor() {
    var modal = $('met-pro-brief-modal');
    if (modal) modal.hidden = true;
    MET_PRO_BRIEF_EDITING = null;
  }

  function _proBriefGetEdits() {
    var modal = $('met-pro-brief-modal');
    var snippetEl = $('met-pro-brief-snippet');
    var bottomLineEl = $('met-pro-brief-bottom-line');
    var weatherDetailsEl = $('met-pro-brief-weather-details');
    var whatsAheadEl = $('met-pro-brief-whats-ahead');
    var imageUrlEl = $('met-pro-brief-image-url');
    var notesEl = $('met-pro-brief-notes');
    var activeVerdictBtn = modal ? modal.querySelector('[data-pro-verdict].is-active') : null;
    var bottomLine = bottomLineEl ? bottomLineEl.value.trim() : '';
    var snippet = snippetEl ? snippetEl.value.trim() : '';
    // Auto-derive SMS snippet from bottom line if Met left it blank
    if (!snippet && bottomLine) {
      snippet = bottomLine.split('\n')[0].slice(0, 140).trim();
    }
    return {
      met_verdict: activeVerdictBtn ? activeVerdictBtn.getAttribute('data-pro-verdict') : 'caution',
      met_snippet: snippet,
      // Keep met_body populated as a concatenation for backward compat
      // (admin tools, search, legacy views still read it)
      met_body: bottomLine,
      bottom_line: bottomLine,
      weather_details: weatherDetailsEl ? weatherDetailsEl.value.trim() : '',
      whats_ahead: whatsAheadEl ? whatsAheadEl.value.trim() : '',
      image_url: imageUrlEl ? imageUrlEl.value.trim() : '',
      met_notes: notesEl ? notesEl.value.trim() : ''
    };
  }

  function saveProBriefDraft(closeAfter) {
    if (!MET_PRO_BRIEF_EDITING) return;
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var saveBtn = $('met-pro-brief-save');
    var edits = _proBriefGetEdits();
    if (saveBtn) { saveBtn.disabled = true; saveBtn.textContent = 'Saving\u2026'; }
    fetch(BACKEND + '/api/v1/met/pro-briefs/' + MET_PRO_BRIEF_EDITING.id, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify(edits)
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.textContent = 'Save draft'; }
        if (res && res.ok) {
          if (typeof showTipToast === 'function') showTipToast('Draft saved');
          if (closeAfter) closeProBriefEditor();
          loadMetProBriefs();
        } else {
          window.alert('Couldn\u2019t save draft. ' + ((res && res.error) || ''));
        }
      })
      .catch(function() {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.textContent = 'Save draft'; }
        window.alert('Network error.');
      });
  }

  function sendProBrief() {
    if (!MET_PRO_BRIEF_EDITING) return;
    var edits = _proBriefGetEdits();
    if (!edits.bottom_line) {
      window.alert('The Bottom Line is empty. Add content there before sending.');
      return;
    }
    if (!window.confirm('Send this brief to ' + (MET_PRO_BRIEF_EDITING.subscriber_email || 'the subscriber') + ' via ' + (MET_PRO_BRIEF_EDITING.channels || 'their channels') + '?')) {
      return;
    }

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var sendBtn = $('met-pro-brief-send');
    if (sendBtn) { sendBtn.disabled = true; sendBtn.textContent = 'Sending\u2026'; }

    // Save edits first, then send
    fetch(BACKEND + '/api/v1/met/pro-briefs/' + MET_PRO_BRIEF_EDITING.id, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify(edits)
    })
      .then(function() {
        return fetch(BACKEND + '/api/v1/met/pro-briefs/' + MET_PRO_BRIEF_EDITING.id + '/send', {
          method: 'POST',
          credentials: 'include'
        });
      })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (sendBtn) { sendBtn.disabled = false; sendBtn.textContent = 'Send to subscriber'; }
        if (res && res.ok) {
          var ch = (res.channels_used || []).join(', ') || 'no channels';
          if (typeof showTipToast === 'function') {
            showTipToast('Brief sent via ' + ch + '.');
          }
          closeProBriefEditor();
          loadMetProBriefs();
        } else {
          window.alert('Couldn\u2019t send. ' + ((res && (res.message || res.error)) || ''));
        }
      })
      .catch(function() {
        if (sendBtn) { sendBtn.disabled = false; sendBtn.textContent = 'Send to subscriber'; }
        window.alert('Network error.');
      });
  }

  function setupProBriefControls() {
    if (window._proBriefControlsWired) return;
    window._proBriefControlsWired = true;

    // Refresh button
    var refreshBtn = $('met-pro-briefs-refresh');
    if (refreshBtn) refreshBtn.addEventListener('click', loadMetProBriefs);

    // Modal close
    var closeBtn = $('met-pro-brief-close');
    if (closeBtn) closeBtn.addEventListener('click', closeProBriefEditor);
    var cancelBtn = $('met-pro-brief-cancel');
    if (cancelBtn) cancelBtn.addEventListener('click', closeProBriefEditor);
    var scrim = $('met-pro-brief-scrim');
    if (scrim) scrim.addEventListener('click', closeProBriefEditor);

    // Save + Send
    var saveBtn = $('met-pro-brief-save');
    if (saveBtn) saveBtn.addEventListener('click', function() { saveProBriefDraft(false); });
    var sendBtn = $('met-pro-brief-send');
    if (sendBtn) sendBtn.addEventListener('click', sendProBrief);

    // Schedule (Phase 10 — Met scheduling)
    var schedBtn = $('met-pro-brief-schedule');
    if (schedBtn) schedBtn.addEventListener('click', function() {
      if (!MET_PRO_BRIEF_EDITING) return;
      var edits = _proBriefGetEdits();
      if (!edits.met_body) {
        window.alert('The brief body is empty. Add some content before scheduling.');
        return;
      }
      var BACKEND = 'https://wv-valet-backend.onrender.com';
      // First save the current edits so the draft has the latest body
      fetch(BACKEND + '/api/v1/met/pro-briefs/' + MET_PRO_BRIEF_EDITING.id, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify(edits)
      })
        .then(function() {
          // Then open the schedule popup with type=pro_brief
          openSchedulePopup({
            type: 'pro_brief',
            getContent: function() {
              return {
                draft_id: MET_PRO_BRIEF_EDITING.id,
                final_body: edits.met_body || edits.final_body,
                final_verdict: edits.met_verdict || edits.final_verdict
              };
            },
            getAudience: function() {
              return {
                subscriber_user_id: MET_PRO_BRIEF_EDITING.subscriber_user_id,
                subscriber_email: MET_PRO_BRIEF_EDITING.subscriber_email
              };
            },
            onScheduled: function(info) {
              window.alert('Scheduled for ' + (info.label || 'a future time') + '.\n\nThe Pro brief draft will be sent automatically at that time.');
              closeProBriefEditor();
              if (typeof loadMetScheduledMessages === 'function') {
                try { loadMetScheduledMessages(); } catch (e) {}
              }
              if (typeof loadProBriefDrafts === 'function') {
                try { loadProBriefDrafts(); } catch (e) {}
              }
            }
          });
        })
        .catch(function() {
          window.alert('Could not save the draft before scheduling. Try again.');
        });
    });

    // Verdict pills (event delegation within modal)
    var modal = $('met-pro-brief-modal');
    if (modal) {
      modal.addEventListener('click', function(ev) {
        var btn = ev.target.closest('[data-pro-verdict]');
        if (!btn) return;
        var allBtns = modal.querySelectorAll('[data-pro-verdict]');
        for (var i = 0; i < allBtns.length; i++) {
          allBtns[i].classList.toggle('is-active', allBtns[i] === btn);
        }
      });
    }

    // Event delegation for "Review & send" buttons in the list
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-pro-brief-edit]');
      if (!btn) return;
      var draftId = parseInt(btn.getAttribute('data-pro-brief-edit'), 10);
      openProBriefEditor(draftId);
    });

    // F-X3 (May 17): Polish writing button + suggestion panel.
    // Sends the body text to the backend, which calls Gemini for
    // grammar/spelling/clarity suggestions. Each suggestion is shown
    // as a card; the Met can Accept (applies replacement) or Skip.
    //
    // Reusable across multiple editors. Pass DOM IDs as opts:
    //   { btnId, textareaId, panelId, statusId, listId, closeId }
    wirePolishButton({
      btnId: 'met-pro-brief-polish-btn',
      textareaId: 'met-pro-brief-bottom-line',
      panelId: 'met-pro-brief-polish-panel',
      statusId: 'met-pro-brief-polish-status',
      listId: 'met-pro-brief-polish-list',
      closeId: 'met-pro-brief-polish-close'
    });
  }


  // Shared helper for the F-X3 Polish button. Both Pro Brief editor
  // and Daily Brief composer call this once, passing in their own
  // DOM IDs. Idempotent — won't double-wire if called twice.
  function wirePolishButton(opts) {
    if (!opts) return;
    var btn = $(opts.btnId);
    var panel = $(opts.panelId);
    var list = $(opts.listId);
    var statusEl = $(opts.statusId);
    var closeBtn = $(opts.closeId);
    if (!btn || btn._wvPolishWired) return;
    btn._wvPolishWired = true;

    function _renderSuggestions(suggestions) {
      if (!list) return;
      if (!suggestions || suggestions.length === 0) {
        list.innerHTML = '<div style="font-size: 12.5px; color: rgba(15,17,22,0.65);">No issues found. Looks good.</div>';
        if (statusEl) statusEl.textContent = 'All clear';
        return;
      }
      if (statusEl) statusEl.textContent = suggestions.length + ' suggestion' + (suggestions.length === 1 ? '' : 's');
      var html = '';
      for (var i = 0; i < suggestions.length; i++) {
        var s = suggestions[i];
        html += '<div data-polish-idx="' + i + '" style="background: #fff; border: 1px solid rgba(15,17,22,0.08); border-radius: 6px; padding: 10px 12px;">'
          +   '<div style="display: inline-block; background: rgba(46,79,184,0.10); color: #2E4FB8; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em; padding: 2px 7px; border-radius: 4px; margin-bottom: 8px;">' + escapeHtml(s.reason || 'polish') + '</div>'
          +   '<div style="font-size: 12.5px; color: rgba(15,17,22,0.55); margin-bottom: 4px;"><span style="color: rgba(194,52,43,0.85); text-decoration: line-through;">' + escapeHtml(s.original) + '</span></div>'
          +   '<div style="font-size: 13px; color: #0E1116; margin-bottom: 10px; line-height: 1.45;"><span style="color: #0c7a3c; font-weight: 500;">' + escapeHtml(s.suggestion) + '</span></div>'
          +   '<div style="display: flex; gap: 8px;">'
          +     '<button type="button" data-polish-accept="' + i + '" style="background: #2E4FB8; border: 0; color: #fff; font-size: 11.5px; font-weight: 600; padding: 5px 12px; border-radius: 5px; cursor: pointer;">Accept</button>'
          +     '<button type="button" data-polish-skip="' + i + '" style="background: transparent; border: 1px solid rgba(15,17,22,0.18); color: rgba(15,17,22,0.65); font-size: 11.5px; font-weight: 600; padding: 5px 12px; border-radius: 5px; cursor: pointer;">Skip</button>'
          +   '</div>'
          + '</div>';
      }
      list.innerHTML = html;

      list.querySelectorAll('[data-polish-accept]').forEach(function(acceptBtn) {
        acceptBtn.addEventListener('click', function() {
          var idx = parseInt(acceptBtn.getAttribute('data-polish-accept'), 10);
          var s = suggestions[idx];
          if (!s) return;
          var bodyEl = $(opts.textareaId);
          if (!bodyEl) return;
          var current = bodyEl.value;
          var pos = current.indexOf(s.original);
          if (pos === -1) {
            var rx = new RegExp(s.original.replace(/[.*+?^${}()|[\]\\]/g, '\\$&').replace(/\s+/g, '\\s+'), '');
            current = current.replace(rx, s.suggestion);
          } else {
            current = current.slice(0, pos) + s.suggestion + current.slice(pos + s.original.length);
          }
          bodyEl.value = current;
          var card = list.querySelector('[data-polish-idx="' + idx + '"]');
          if (card) card.parentNode.removeChild(card);
          if (!list.querySelector('[data-polish-idx]')) {
            list.innerHTML = '<div style="font-size: 12.5px; color: rgba(15,17,22,0.65);">All suggestions applied or skipped.</div>';
          }
        });
      });
      list.querySelectorAll('[data-polish-skip]').forEach(function(skipBtn) {
        skipBtn.addEventListener('click', function() {
          var idx = parseInt(skipBtn.getAttribute('data-polish-skip'), 10);
          var card = list.querySelector('[data-polish-idx="' + idx + '"]');
          if (card) card.parentNode.removeChild(card);
          if (!list.querySelector('[data-polish-idx]')) {
            list.innerHTML = '<div style="font-size: 12.5px; color: rgba(15,17,22,0.65);">All suggestions applied or skipped.</div>';
          }
        });
      });
    }

    btn.addEventListener('click', function() {
      var bodyEl = $(opts.textareaId);
      if (!bodyEl) return;
      var text = (bodyEl.value || '').trim();
      if (text.length < 20) {
        window.alert('Add more text first (at least a sentence or two).');
        return;
      }
      btn.disabled = true;
      var labelEl = btn.querySelector('span');
      var prevLabel = labelEl ? labelEl.textContent : '';
      if (labelEl) labelEl.textContent = 'Checking…';
      if (panel) panel.hidden = false;
      if (statusEl) statusEl.textContent = 'Checking…';
      if (list) list.innerHTML = '<div style="font-size: 12.5px; color: rgba(15,17,22,0.55);">Running a quick polish pass…</div>';

      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/met/polish-text', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify({ text: text })
      })
        .then(function(r) { return r.json(); })
        .then(function(res) {
          btn.disabled = false;
          if (labelEl) labelEl.textContent = prevLabel || 'Polish writing';
          if (!res || !res.ok) {
            if (statusEl) statusEl.textContent = 'Could not run polish';
            if (list) list.innerHTML = '<div style="font-size: 12.5px; color: rgba(194,52,43,0.85);">' + ((res && res.error) || 'Unknown error.') + '</div>';
            return;
          }
          _renderSuggestions(res.suggestions || []);
        })
        .catch(function(err) {
          btn.disabled = false;
          if (labelEl) labelEl.textContent = prevLabel || 'Polish writing';
          console.error('[polish] failed:', err);
          if (statusEl) statusEl.textContent = 'Network error';
          if (list) list.innerHTML = '<div style="font-size: 12.5px; color: rgba(194,52,43,0.85);">Network error. Try again.</div>';
        });
    });

    if (closeBtn && panel) {
      closeBtn.addEventListener('click', function() {
        panel.hidden = true;
      });
    }
  }


  // ════════════════════════════════════════════════════════════════════
  // Met Messages (Pro Threads) — PT-C
  // ════════════════════════════════════════════════════════════════════
  // Two-pane: thread list + conversation. Click a thread → load + render
  // its messages + auto-mark-read. Send button replies.

  var _MET_THREADS = [];
  var _MET_ACTIVE_THREAD_ID = null;
  var _MET_MSG_WIRED = false;

  function _metMsgFmtTime(ms) {
    if (!ms) return '';
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '';
    var now = new Date();
    var sameDay = d.getFullYear() === now.getFullYear()
               && d.getMonth() === now.getMonth()
               && d.getDate() === now.getDate();
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    var t = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    if (sameDay) return t;
    var diffDays = Math.floor((now - d) / 86400000);
    if (diffDays === 1) return 'Yesterday ' + t;
    if (diffDays < 7) return ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'][d.getDay()] + ' ' + t;
    return (d.getMonth() + 1) + '/' + d.getDate() + ' ' + t;
  }

  function _metMsgTierLabel(tier) {
    if (tier === 'pro_multi') return 'Pro Multi';
    if (tier === 'pro_enterprise') return 'Pro Enterprise';
    return 'Pro Single';
  }

  function loadMetThreadsList() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var listEl = $('met-messages-thread-list');
    if (!listEl) return;
    fetch(BACKEND + '/api/v1/met/threads', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          if (listEl) listEl.innerHTML =
            '<div style="padding:30px 16px;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">'
            + (res.status === 403 ? 'You must be a Met to view threads.' : 'Could not load.')
            + '</div>';
          return;
        }
        _MET_THREADS = res.body.threads || [];
        renderMetThreadList();
      })
      .catch(function() {
        if (listEl) listEl.innerHTML =
          '<div style="padding:30px 16px;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Network error.</div>';
      });
  }

  function renderMetThreadList() {
    var listEl = $('met-messages-thread-list');
    var countBadge = $('met-messages-count');
    if (!listEl) return;

    // Total unread count for the tab badge
    var totalUnread = 0;
    for (var i = 0; i < _MET_THREADS.length; i++) {
      totalUnread += (_MET_THREADS[i].unread_for_met || 0);
    }
    if (countBadge) {
      countBadge.textContent = totalUnread;
      countBadge.hidden = totalUnread === 0;
    }

    if (_MET_THREADS.length === 0) {
      listEl.innerHTML =
        '<div style="padding:40px 20px;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;line-height:1.55;">'
        + 'No Pro Threads yet. Threads appear when a Pro subscriber sends their first message.'
        + '</div>';
      return;
    }

    var html = '';
    for (var t = 0; t < _MET_THREADS.length; t++) {
      var th = _MET_THREADS[t];
      var isActive = th.id === _MET_ACTIVE_THREAD_ID;
      var unread = th.unread_for_met || 0;
      var preview = (th.last_message_preview || '').slice(0, 80);
      var fromLabel = th.last_message_from === 'met' ? 'You: ' : '';

      var bg = isActive ? 'rgba(46,79,184,0.20)'
             : unread > 0 ? 'rgba(255,184,0,0.04)'
             : 'transparent';
      var borderL = isActive ? '3px solid #2E4FB8'
                  : unread > 0 ? '3px solid #FFCC4D'
                  : '3px solid transparent';

      html += '<div data-met-thread="' + th.id + '" '
        + 'style="padding:12px 14px 12px 12px;border-left:' + borderL + ';border-bottom:1px solid rgba(255,255,255,0.05);background:' + bg + ';cursor:pointer;">'
        + '<div style="display:flex;align-items:baseline;justify-content:space-between;gap:8px;margin-bottom:3px;">'
        +   '<div style="font-size:13px;font-weight:' + (unread > 0 ? '600' : '500') + ';color:#fff;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1;">'
        +     escapeHtml(th.subscriber_name || th.subscriber_email || 'Subscriber')
        +   '</div>'
        + (unread > 0
            ? '<span style="background:#C2342B;color:#fff;padding:1px 7px;border-radius:8px;font-size:10px;font-weight:700;flex-shrink:0;">' + unread + '</span>'
            : '<span style="font-size:10px;color:rgba(255,255,255,0.45);flex-shrink:0;">' + escapeHtml(_metMsgFmtTime(th.last_message_at)) + '</span>')
        + '</div>'
        + '<div style="font-size:11.5px;color:rgba(255,255,255,0.55);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">'
        +   escapeHtml(fromLabel + (preview || '(no messages yet)'))
        + '</div>'
        + '</div>';
    }
    listEl.innerHTML = html;
  }

  function openMetThread(threadId) {
    _MET_ACTIVE_THREAD_ID = threadId;
    var BACKEND = 'https://wv-valet-backend.onrender.com';

    // Show conversation chrome, hide empty state
    var header = $('met-messages-conv-header');
    var empty = $('met-messages-conv-empty');
    var body = $('met-messages-conv-body');
    var composer = $('met-messages-conv-composer');
    if (header) header.hidden = false;
    if (empty) empty.style.display = 'none';
    if (body) {
      body.hidden = false;
      body.innerHTML = '<div style="text-align:center;color:rgba(255,255,255,0.45);font-size:13px;padding:20px 0;">Loading\u2026</div>';
    }
    if (composer) composer.hidden = false;

    // Re-render the list so the active thread highlights
    renderMetThreadList();

    fetch(BACKEND + '/api/v1/met/threads/' + threadId, { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          if (body) body.innerHTML = '<div style="text-align:center;color:rgba(255,255,255,0.45);font-size:13px;padding:20px 0;">Could not load thread.</div>';
          return;
        }
        var thread = res.body.thread || {};
        var messages = res.body.messages || [];

        // Header
        var nameEl = $('met-messages-conv-name');
        var metaEl = $('met-messages-conv-meta');
        var tierEl = $('met-messages-conv-tier');
        if (nameEl) nameEl.textContent = thread.subscriber_name || thread.subscriber_email || 'Subscriber';
        if (metaEl) {
          var metaBits = [];
          if (thread.subscriber_email) metaBits.push(thread.subscriber_email);
          if (thread.subscriber_phone) metaBits.push(thread.subscriber_phone);
          metaEl.textContent = metaBits.join(' \u00b7 ');
        }
        if (tierEl) tierEl.textContent = _metMsgTierLabel(thread.subscriber_tier);

        renderMetConversation(messages);

        // Auto-mark-read
        if ((thread.unread_for_met || 0) > 0) {
          fetch(BACKEND + '/api/v1/met/threads/' + threadId + '/mark-read', {
            method: 'POST',
            credentials: 'include'
          })
            .then(function() {
              // Update local state + re-render list to clear the unread badge
              for (var i = 0; i < _MET_THREADS.length; i++) {
                if (_MET_THREADS[i].id === threadId) {
                  _MET_THREADS[i].unread_for_met = 0;
                  break;
                }
              }
              renderMetThreadList();
            })
            .catch(function() {});
        }
      })
      .catch(function() {
        if (body) body.innerHTML = '<div style="text-align:center;color:rgba(255,255,255,0.45);font-size:13px;padding:20px 0;">Network error.</div>';
      });
  }

  function renderMetConversation(messages) {
    var body = $('met-messages-conv-body');
    if (!body) return;
    if (messages.length === 0) {
      body.innerHTML =
        '<div style="text-align:center;color:rgba(255,255,255,0.45);font-size:13px;padding:30px 0;line-height:1.55;">'
        + 'No messages yet. Send the first one below.'
        + '</div>';
      return;
    }

    var html = '';
    for (var i = 0; i < messages.length; i++) {
      var m = messages[i];
      var isMet = m.sender_role === 'met';

      // Date separator
      if (i > 0) {
        var prevDate = new Date(messages[i-1].created_at);
        var curDate = new Date(m.created_at);
        if (prevDate.getFullYear() !== curDate.getFullYear()
            || prevDate.getMonth() !== curDate.getMonth()
            || prevDate.getDate() !== curDate.getDate()) {
          html += '<div style="text-align:center;font-size:10px;color:rgba(255,255,255,0.40);margin:6px 0;font-weight:600;text-transform:uppercase;letter-spacing:0.05em;">'
            + escapeHtml(_metMsgDateLabel(curDate))
            + '</div>';
        }
      }

      var alignSide = isMet ? 'flex-end' : 'flex-start';
      var bubbleBg = isMet ? '#2E4FB8' : 'rgba(255,255,255,0.06)';
      var bubbleColor = '#fff';
      var senderLabel = isMet ? '' : (m.sender_name || 'Subscriber');

      html += '<div style="display:flex;flex-direction:column;align-items:' + alignSide + ';">'
        + (senderLabel
            ? '<div style="font-size:10px;font-weight:700;color:rgba(255,255,255,0.55);text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">' + escapeHtml(senderLabel) + '</div>'
            : '')
        + '<div style="background:' + bubbleBg + ';color:' + bubbleColor + ';padding:8px 13px;border-radius:13px;'
        +   'max-width:80%;font-size:13.5px;line-height:1.5;white-space:pre-wrap;word-wrap:break-word;">'
        +   escapeHtml(m.body)
        + '</div>'
        + '<div style="font-size:10px;color:rgba(255,255,255,0.40);margin-top:3px;">' + escapeHtml(_metMsgFmtTime(m.created_at)) + '</div>'
        + '</div>';
    }
    body.innerHTML = html;
    body.scrollTop = body.scrollHeight;
  }

  function _metMsgDateLabel(d) {
    var now = new Date();
    var sameDay = d.getFullYear() === now.getFullYear()
               && d.getMonth() === now.getMonth()
               && d.getDate() === now.getDate();
    if (sameDay) return 'Today';
    var diffDays = Math.floor((now - d) / 86400000);
    if (diffDays === 1) return 'Yesterday';
    if (diffDays < 7) return ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'][d.getDay()];
    var months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    return months[d.getMonth()] + ' ' + d.getDate();
  }

  function sendMetThreadReply() {
    if (!_MET_ACTIVE_THREAD_ID) return;
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var input = $('met-messages-input');
    var sendBtn = $('met-messages-send');
    if (!input || !sendBtn) return;
    var body = input.value.trim();
    if (!body) return;

    sendBtn.disabled = true;
    sendBtn.textContent = 'Sending\u2026';

    fetch(BACKEND + '/api/v1/met/threads/' + _MET_ACTIVE_THREAD_ID + '/messages', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ body: body })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        sendBtn.disabled = false;
        sendBtn.textContent = 'Send';
        if (res && res.ok) {
          input.value = '';
          // Reload the thread + the list (so last-message-preview updates)
          openMetThread(_MET_ACTIVE_THREAD_ID);
          loadMetThreadsList();
        } else {
          window.alert('Could not send. ' + ((res && (res.message || res.error)) || ''));
        }
      })
      .catch(function() {
        sendBtn.disabled = false;
        sendBtn.textContent = 'Send';
        window.alert('Network error.');
      });
  }

  function setupMetMessages() {
    if (_MET_MSG_WIRED) return;
    _MET_MSG_WIRED = true;

    var refreshBtn = $('met-messages-refresh');
    if (refreshBtn) refreshBtn.addEventListener('click', loadMetThreadsList);

    var sendBtn = $('met-messages-send');
    if (sendBtn) sendBtn.addEventListener('click', sendMetThreadReply);

    var input = $('met-messages-input');
    if (input) {
      input.addEventListener('keydown', function(ev) {
        if (ev.key === 'Enter' && !ev.shiftKey) {
          ev.preventDefault();
          sendMetThreadReply();
        }
      });
    }

    // Event delegation for thread row clicks
    var listEl = $('met-messages-thread-list');
    if (listEl) {
      listEl.addEventListener('click', function(ev) {
        var row = ev.target.closest('[data-met-thread]');
        if (!row) return;
        var threadId = parseInt(row.getAttribute('data-met-thread'), 10);
        if (threadId && threadId !== _MET_ACTIVE_THREAD_ID) {
          openMetThread(threadId);
        }
      });
    }

    // "+ New" button → open subscriber picker modal
    var newBtn = $('met-messages-new-thread');
    if (newBtn) newBtn.addEventListener('click', openMetNewThreadModal);

    // Picker modal: close + search wiring
    var pickerClose = $('met-new-thread-close');
    var pickerScrim = $('met-new-thread-scrim');
    if (pickerClose) pickerClose.addEventListener('click', closeMetNewThreadModal);
    if (pickerScrim) pickerScrim.addEventListener('click', closeMetNewThreadModal);

    var pickerSearch = $('met-new-thread-search');
    if (pickerSearch) {
      pickerSearch.addEventListener('input', function() {
        renderMetNewThreadList(pickerSearch.value || '');
      });
    }

    // Picker list: delegated click → open thread
    var pickerList = $('met-new-thread-list');
    if (pickerList) {
      pickerList.addEventListener('click', function(ev) {
        var row = ev.target.closest('[data-met-sub-pick]');
        if (!row) return;
        var subId = parseInt(row.getAttribute('data-met-sub-pick'), 10);
        if (subId) startMetNewThread(subId);
      });
    }
  }


  // ════════════════════════════════════════════════════════════════════
  // Met-initiated new thread — picker modal + start logic
  // ════════════════════════════════════════════════════════════════════
  var _MET_PRO_SUBSCRIBERS = [];

  function openMetNewThreadModal() {
    var modal = $('met-new-thread-modal');
    if (!modal) return;
    modal.hidden = false;
    // Reset search
    var search = $('met-new-thread-search');
    if (search) {
      search.value = '';
      search.focus();
    }
    loadMetProSubscribers();
  }

  function closeMetNewThreadModal() {
    var modal = $('met-new-thread-modal');
    if (modal) modal.hidden = true;
  }

  function loadMetProSubscribers() {
    var listEl = $('met-new-thread-list');
    if (listEl) listEl.innerHTML = '<div style="padding:30px 16px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Loading&hellip;</div>';
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/met/pro-subscribers', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          if (listEl) listEl.innerHTML = '<div style="padding:30px 16px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Could not load subscribers.</div>';
          return;
        }
        _MET_PRO_SUBSCRIBERS = res.body.subscribers || [];
        renderMetNewThreadList('');
      })
      .catch(function() {
        if (listEl) listEl.innerHTML = '<div style="padding:30px 16px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Network error.</div>';
      });
  }

  function renderMetNewThreadList(query) {
    var listEl = $('met-new-thread-list');
    if (!listEl) return;
    var q = (query || '').toLowerCase().trim();
    var filtered = _MET_PRO_SUBSCRIBERS;
    if (q) {
      filtered = filtered.filter(function(s) {
        return ((s.name || '').toLowerCase().indexOf(q) !== -1)
            || ((s.email || '').toLowerCase().indexOf(q) !== -1)
            || ((s.loc_label || '').toLowerCase().indexOf(q) !== -1)
            || ((s.loc_address || '').toLowerCase().indexOf(q) !== -1);
      });
    }
    if (filtered.length === 0) {
      listEl.innerHTML = '<div style="padding:30px 16px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">'
        + (_MET_PRO_SUBSCRIBERS.length === 0
            ? 'No Pro subscribers yet.'
            : 'No matches.')
        + '</div>';
      return;
    }
    var tierLabel = {
      'pro_single': 'Pro Single',
      'pro_multi': 'Pro Multi',
      'pro_enterprise': 'Pro Enterprise'
    };
    var html = '';
    for (var i = 0; i < filtered.length; i++) {
      var s = filtered[i];
      var displayName = s.name || s.email || '(no name)';
      var locBit = s.loc_label || s.loc_address || '';
      var tier = tierLabel[s.tier] || s.tier || 'Pro';
      html += '<div data-met-sub-pick="' + s.user_id + '" '
        + 'style="padding:12px 14px;border-bottom:1px solid rgba(15,17,22,0.06);cursor:pointer;transition:background 0.1s;" '
        + 'onmouseover="this.style.background=\'rgba(46,79,184,0.04)\'" '
        + 'onmouseout="this.style.background=\'\'">'
        + '<div style="display:flex;align-items:center;justify-content:space-between;gap:10px;">'
        +   '<div style="font-size:14px;font-weight:500;color:#0E1116;">' + escapeHtml(displayName) + '</div>'
        +   '<span style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;color:#2E4FB8;background:rgba(46,79,184,0.08);padding:2px 7px;border-radius:4px;">' + escapeHtml(tier) + '</span>'
        + '</div>'
        + '<div style="font-size:11.5px;color:rgba(15,17,22,0.55);margin-top:3px;">'
        +   escapeHtml(s.email || '')
        +   (locBit ? ' &middot; ' + escapeHtml(locBit) : '')
        + '</div>'
        + '</div>';
    }
    listEl.innerHTML = html;
  }

  function startMetNewThread(subscriberUserId) {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/met/threads/new', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ subscriber_user_id: subscriberUserId })
    })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          window.alert(((res.body && (res.body.message || res.body.error)) || 'Could not open thread.'));
          return;
        }
        closeMetNewThreadModal();
        // Refresh the thread list so the new thread appears, then open it
        loadMetThreadsList();
        setTimeout(function() {
          openMetThread(res.body.thread.id);
        }, 300);
      })
      .catch(function() {
        window.alert('Network error. Try again.');
      });
  }


  // ════════════════════════════════════════════════════════════════════
  // Met tip earnings (Phase 10 — Met tips, TIP-B Met side)
  // ════════════════════════════════════════════════════════════════════

  function loadMetTipEarnings() {
    // Fires three fetches in parallel:
    //   1. /api/v1/me/met-tips → recent tips list (preserved from prior)
    //   2. /api/v1/me/earnings/<this year>/<this month> → this-month totals
    //   3. /api/v1/me/earnings/<last year>/<last month> → last-month totals
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var now = new Date();
    var thisYear = now.getFullYear();
    var thisMonth = now.getMonth() + 1;
    var lastDate = new Date(thisYear, thisMonth - 2, 1);
    var lastYear = lastDate.getFullYear();
    var lastMonth = lastDate.getMonth() + 1;

    var months = ['January','February','March','April','May','June',
                  'July','August','September','October','November','December'];
    var monthLabelEl = $('met-earn-month-label');
    if (monthLabelEl) monthLabelEl.textContent = months[now.getMonth()];
    var lastLabelEl = $('met-earn-last-label');
    if (lastLabelEl) lastLabelEl.textContent = months[lastDate.getMonth()] + ' ' + lastYear + ' \u2014 confirm against pay stub';

    var fmt = function(c) { return '$' + ((c || 0) / 100).toFixed(2); };

    // ── This month earnings
    fetch(BACKEND + '/api/v1/me/earnings/' + thisYear + '/' + thisMonth, { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (!res || !res.ok) return;
        var e = res.earnings || {};
        var totalEl = $('met-earn-this-total');
        var rEl = $('met-earn-this-reviews');
        var bEl = $('met-earn-this-briefs');
        var tEl = $('met-earn-this-tips');
        if (totalEl) totalEl.textContent = fmt(e.total_cents);
        if (rEl) rEl.textContent = fmt(e.review_cents);
        if (bEl) bEl.textContent = fmt(e.brief_cents);
        if (tEl) tEl.textContent = fmt(e.tip_cents);

        // Storm Shelter earnings tile (Phase B, May 17)
        var shelterValEl = $('met-shelter-earnings-value');
        var shelterCtxEl = $('met-shelter-earnings-context');
        var shelterCents = e.shelter_cents || 0;
        var shelterCount = e.shelter_count || 0;
        if (shelterValEl) {
          var amt = (shelterCents / 100).toFixed(2);
          var parts = amt.split('.');
          shelterValEl.innerHTML = '$' + parts[0] + '<span class="ws-vital-value-unit">.' + parts[1] + '</span>';
        }
        if (shelterCtxEl) {
          shelterCtxEl.textContent = shelterCount + ' activation' + (shelterCount === 1 ? '' : 's') + ' \u00d7 $25';
        }
      })
      .catch(function() {});

    // ── Last month earnings (payday confirmation)
    fetch(BACKEND + '/api/v1/me/earnings/' + lastYear + '/' + lastMonth, { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (!res || !res.ok) return;
        var e = res.earnings || {};
        var totalEl = $('met-earn-last-total');
        var rEl = $('met-earn-last-reviews');
        var bEl = $('met-earn-last-briefs');
        var tEl = $('met-earn-last-tips');
        if (totalEl) totalEl.textContent = fmt(e.total_cents);
        if (rEl) rEl.textContent = fmt(e.review_cents);
        if (bEl) bEl.textContent = fmt(e.brief_cents);
        if (tEl) tEl.textContent = fmt(e.tip_cents);
      })
      .catch(function() {});

    // ── Recent tips list (preserved)
    fetch(BACKEND + '/api/v1/me/met-tips', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (!res || !res.ok) return;
        var recentEl = $('met-tips-recent');
        if (!recentEl) return;
        var tips = res.tips || [];
        if (tips.length === 0) {
          recentEl.innerHTML =
            '<div style="padding:16px 0 4px;text-align:center;color:rgba(15,17,22,0.50);font-size:12px;">'
            + 'No tips yet. They show up here when customers thank you.'
            + '</div>';
          return;
        }
        var html = '<div style="font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.08em;color:rgba(15,17,22,0.55);margin:14px 0 8px;">Recent tips</div>';
        for (var i = 0; i < Math.min(tips.length, 6); i++) {
          var t = tips[i];
          var when = _metTipFmtWhen(t.completed_at || t.created_at);
          var amount = fmt(t.amount_cents);
          var statusColor = t.status === 'completed' ? '#0c7a3c'
                          : t.status === 'pending' ? '#8a5a0a'
                          : '#9b2823';
          var statusLabel = t.status === 'completed' ? 'received'
                          : t.status === 'pending' ? 'pending'
                          : 'failed';
          var planSnippet = (t.review_plan || '').slice(0, 50);
          if ((t.review_plan || '').length > 50) planSnippet += '\u2026';

          html += '<div style="display:grid;grid-template-columns:auto 1fr auto;gap:14px;padding:10px 0;border-bottom:1px solid rgba(15,17,22,0.06);align-items:center;">'
            +   '<div style="font-size:15px;font-weight:600;color:#0E1116;">' + amount + '</div>'
            +   '<div>'
            +     '<div style="font-size:12px;color:rgba(15,17,22,0.85);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">' + escapeHtml(planSnippet || '(review)') + '</div>'
            +     '<div style="font-size:10.5px;color:rgba(15,17,22,0.55);margin-top:1px;">' + escapeHtml(when) + ' &middot; ' + escapeHtml(t.customer_email || 'anonymous') + '</div>'
            +   '</div>'
            +   '<div style="font-size:10px;font-weight:600;color:' + statusColor + ';text-transform:uppercase;letter-spacing:0.05em;">' + statusLabel + '</div>'
            + '</div>';
        }
        recentEl.innerHTML = html;
      })
      .catch(function() {});
  }

  function _metTipFmtWhen(ms) {
    if (!ms) return '\u2014';
    if (ms < 1e12) ms = ms * 1000;
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '\u2014';
    var now = new Date();
    var sameDay = d.getFullYear() === now.getFullYear()
               && d.getMonth() === now.getMonth()
               && d.getDate() === now.getDate();
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    var t = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    if (sameDay) return t + ' today';
    var diffDays = Math.floor((now - d) / 86400000);
    if (diffDays === 1) return 'Yesterday ' + t;
    if (diffDays < 7) return ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'][d.getDay()] + ' ' + t;
    return (d.getMonth() + 1) + '/' + d.getDate() + ' ' + t;
  }

  function setupMetTipEarnings() {
    if (window._metTipsWired) return;
    window._metTipsWired = true;
    // Legacy id retained for compatibility, but the new ID is met-earn-refresh
    var refreshBtn = $('met-earn-refresh') || $('met-tips-refresh');
    if (refreshBtn) refreshBtn.addEventListener('click', loadMetTipEarnings);
  }


  // ════════════════════════════════════════════════════════════════════
  // Met welcome card — first-login orientation
  // ════════════════════════════════════════════════════════════════════
  // Fetches /api/v1/me/profile. If met_onboarded_at is null, show the
  // welcome card. Dismiss button POSTs to /met-onboarding-dismiss and
  // hides the card. Once dismissed, never shows again.

  function loadMetWelcomeCard() {
    var card = $('met-welcome-card');
    if (!card) return;
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/me/profile', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (!res || !res.ok || !res.profile) return;
        // Only show for Mets who haven't dismissed yet
        if (res.profile.met_onboarded_at) return;
        // Personalize with first name
        var name = (res.profile.name || '').trim();
        if (name) {
          var firstName = name.split(/\s+/)[0];
          var nameEl = $('met-welcome-name');
          if (nameEl) nameEl.textContent = firstName;
        }
        card.hidden = false;
      })
      .catch(function() { /* silent — card just doesn't show */ });
  }

  function dismissMetWelcomeCard() {
    var card = $('met-welcome-card');
    if (!card) return;
    // Hide immediately for snappy UX; if the backend call fails, the
    // card will re-appear on next page load — acceptable trade-off
    card.hidden = true;
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/me/met-onboarding-dismiss', {
      method: 'POST',
      credentials: 'include'
    }).catch(function() {
      // If the dismiss fails, the card returns on next workspace open.
      // Not worth surfacing an error — they'll just dismiss again.
    });
  }

  function setupMetWelcomeCard() {
    if (window._metWelcomeWired) return;
    window._metWelcomeWired = true;
    var dismissBtn = $('met-welcome-dismiss');
    if (dismissBtn) dismissBtn.addEventListener('click', dismissMetWelcomeCard);
  }


  // ════════════════════════════════════════════════════════════════════
  // Schedule popup — shared modal for Daily Brief / Pro Brief / Crew post
  // ════════════════════════════════════════════════════════════════════
  // Usage:
  //   openSchedulePopup({
  //     type: 'daily_brief',
  //     getContent: function() { return { body, mode, ... }; },
  //     getAudience: function() { return { counties: [...] }; },
  //     onScheduled: function(scheduledMsg) { /* refresh UI */ }
  //   });
  //
  // Result: when Met clicks "Schedule Send," posts to
  // /api/v1/me/scheduled-messages and calls onScheduled.
  var _SCHEDULE_CTX = null;

  function _schedulePopulateDropdowns() {
    var hourSel = $('schedule-hour');
    var minSel = $('schedule-minute');
    var tzSel = $('schedule-tz');
    if (hourSel && hourSel.options.length === 0) {
      for (var h = 1; h <= 12; h++) {
        var opt = document.createElement('option');
        opt.value = h;
        opt.textContent = h;
        hourSel.appendChild(opt);
      }
    }
    if (minSel && minSel.options.length === 0) {
      // Every 5 minutes
      for (var m = 0; m < 60; m += 5) {
        var opt2 = document.createElement('option');
        opt2.value = m;
        opt2.textContent = (m < 10 ? '0' : '') + m;
        minSel.appendChild(opt2);
      }
    }
    // Default timezone — guess from browser
    if (tzSel) {
      try {
        var browserTz = Intl.DateTimeFormat().resolvedOptions().timeZone;
        var mapping = {
          'America/Indiana/Indianapolis': 'America/New_York',
          'America/Indianapolis': 'America/New_York',
          'America/Detroit': 'America/New_York',
          'America/Phoenix': 'America/Denver',
          'America/Boise': 'America/Denver',
        };
        var tzGuess = mapping[browserTz] || browserTz;
        for (var i = 0; i < tzSel.options.length; i++) {
          if (tzSel.options[i].value === tzGuess) {
            tzSel.selectedIndex = i;
            break;
          }
        }
      } catch (e) { /* fall back to default option */ }
    }
  }

  function _scheduleSetDefaults() {
    // Default date: today. Default time: 1 hour from now, rounded to next 5 min.
    var dateEl = $('schedule-date');
    var hourEl = $('schedule-hour');
    var minEl = $('schedule-minute');
    var ampmEl = $('schedule-ampm');
    if (!dateEl || !hourEl || !minEl || !ampmEl) return;

    var d = new Date();
    d.setMinutes(d.getMinutes() + 60);
    // Round minutes UP to next 5
    var roundUp = Math.ceil(d.getMinutes() / 5) * 5;
    d.setMinutes(roundUp);

    // YYYY-MM-DD for date input
    var yyyy = d.getFullYear();
    var mm = ('0' + (d.getMonth() + 1)).slice(-2);
    var dd = ('0' + d.getDate()).slice(-2);
    dateEl.value = yyyy + '-' + mm + '-' + dd;

    // 12-hour clock
    var h = d.getHours();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12;
    if (h === 0) h = 12;
    hourEl.value = h;
    minEl.value = d.getMinutes() % 60 === 0 ? 0 : d.getMinutes();
    ampmEl.value = ampm;
  }

  function _scheduleComputeFireMs() {
    // Returns { ok: bool, ms?: number, tz?: string, error?: string, label?: string }
    var dateEl = $('schedule-date');
    var hourEl = $('schedule-hour');
    var minEl = $('schedule-minute');
    var ampmEl = $('schedule-ampm');
    var tzEl = $('schedule-tz');
    if (!dateEl || !hourEl || !minEl || !ampmEl || !tzEl) {
      return { ok: false, error: 'Missing form fields.' };
    }
    var dateStr = dateEl.value;
    if (!dateStr) return { ok: false, error: 'Pick a date.' };

    var h = parseInt(hourEl.value, 10);
    var m = parseInt(minEl.value, 10);
    var ampm = ampmEl.value;
    var tz = tzEl.value;

    if (ampm === 'PM' && h !== 12) h += 12;
    if (ampm === 'AM' && h === 12) h = 0;

    // Build a Date in the chosen timezone — JS doesn't have native
    // construction in a non-local TZ, so we use a trick: format a Date
    // built from "local" inputs into the target TZ, compute offset,
    // and apply.
    var parts = dateStr.split('-');
    var y = parseInt(parts[0], 10);
    var mo = parseInt(parts[1], 10) - 1;
    var d = parseInt(parts[2], 10);

    // Construct as if those YMD/HMS values are in UTC, then figure out
    // what the offset is for the chosen TZ at that wall-clock moment.
    var asUtc = Date.UTC(y, mo, d, h, m, 0);
    // Get how the chosen TZ formats that UTC instant
    var formatter = new Intl.DateTimeFormat('en-US', {
      timeZone: tz,
      year: 'numeric', month: '2-digit', day: '2-digit',
      hour: '2-digit', minute: '2-digit', hour12: false
    });
    var fmtParts = formatter.formatToParts(new Date(asUtc));
    var lookup = {};
    for (var i = 0; i < fmtParts.length; i++) {
      lookup[fmtParts[i].type] = fmtParts[i].value;
    }
    // Wall clock the TZ shows for our UTC instant
    var tzWallMs = Date.UTC(
      parseInt(lookup.year, 10),
      parseInt(lookup.month, 10) - 1,
      parseInt(lookup.day, 10),
      parseInt(lookup.hour, 10) === 24 ? 0 : parseInt(lookup.hour, 10),
      parseInt(lookup.minute, 10),
      0
    );
    // Offset between what we WANT (asUtc as wall clock in target TZ) and
    // what the TZ actually shows for that UTC instant
    var offset = asUtc - tzWallMs;
    var fireMs = asUtc + offset;

    if (fireMs < Date.now() + 60_000) {
      return { ok: false, error: 'Pick a time at least a minute in the future.' };
    }

    // Human label like "Friday, May 16 at 6:30 AM CT"
    var labelFmt = new Intl.DateTimeFormat('en-US', {
      timeZone: tz,
      weekday: 'long', month: 'long', day: 'numeric',
      hour: 'numeric', minute: '2-digit', hour12: true,
      timeZoneName: 'short'
    });
    var label = labelFmt.format(new Date(fireMs));
    return { ok: true, ms: fireMs, tz: tz, label: label };
  }

  function _scheduleUpdatePreview() {
    var preview = $('schedule-preview-text');
    if (!preview) return;
    var res = _scheduleComputeFireMs();
    if (res.ok) {
      preview.textContent = 'Will send: ' + res.label;
      preview.parentElement.style.borderColor = 'rgba(46,79,184,0.30)';
      preview.parentElement.style.background = 'rgba(46,79,184,0.05)';
    } else {
      preview.textContent = res.error || 'Pick a date and time above.';
    }
    // F-10: Pro brief staleness warning when >6hrs out
    var warnEl = $('schedule-stale-warning');
    if (warnEl) {
      var isProBrief = _SCHEDULE_CTX && _SCHEDULE_CTX.type === 'pro_brief';
      var hoursOut = res.ok ? ((res.ms - Date.now()) / (1000 * 60 * 60)) : 0;
      warnEl.hidden = !(isProBrief && hoursOut > 6);
    }
  }

  function openSchedulePopup(ctx) {
    _SCHEDULE_CTX = ctx;
    var modal = $('schedule-popup');
    if (!modal) return;
    _schedulePopulateDropdowns();
    _scheduleSetDefaults();
    var errEl = $('schedule-error');
    if (errEl) errEl.hidden = true;
    _scheduleUpdatePreview();
    modal.hidden = false;
  }

  function closeSchedulePopup() {
    var modal = $('schedule-popup');
    if (modal) modal.hidden = true;
    _SCHEDULE_CTX = null;
  }

  function setupSchedulePopup() {
    if (window._schedulePopupWired) return;
    window._schedulePopupWired = true;

    var closeBtn = $('schedule-popup-close');
    var scrim = $('schedule-popup-scrim');
    var cancelBtn = $('schedule-popup-cancel');
    var saveBtn = $('schedule-popup-save');
    if (closeBtn) closeBtn.addEventListener('click', closeSchedulePopup);
    if (scrim) scrim.addEventListener('click', closeSchedulePopup);
    if (cancelBtn) cancelBtn.addEventListener('click', closeSchedulePopup);

    // Live preview on any field change
    ['schedule-date','schedule-hour','schedule-minute','schedule-ampm','schedule-tz'].forEach(function(id) {
      var el = $(id);
      if (el) el.addEventListener('change', _scheduleUpdatePreview);
    });

    if (saveBtn) saveBtn.addEventListener('click', function() {
      if (!_SCHEDULE_CTX) return;
      var res = _scheduleComputeFireMs();
      var errEl = $('schedule-error');
      if (!res.ok) {
        if (errEl) {
          errEl.textContent = res.error || 'Could not parse time.';
          errEl.hidden = false;
        }
        return;
      }
      var content = {};
      var audience = null;
      try {
        if (typeof _SCHEDULE_CTX.getContent === 'function') content = _SCHEDULE_CTX.getContent() || {};
        if (typeof _SCHEDULE_CTX.getAudience === 'function') audience = _SCHEDULE_CTX.getAudience() || null;
      } catch (e) {
        if (errEl) { errEl.textContent = 'Could not build content: ' + e.message; errEl.hidden = false; }
        return;
      }

      // Basic validation — daily brief & crew post need body, pro brief needs draft_id
      if (_SCHEDULE_CTX.type === 'daily_brief' || _SCHEDULE_CTX.type === 'crew_post') {
        if (!content.body || !content.body.trim()) {
          if (errEl) { errEl.textContent = 'Write something to send first.'; errEl.hidden = false; }
          return;
        }
      }
      if (_SCHEDULE_CTX.type === 'pro_brief' && !content.draft_id) {
        if (errEl) { errEl.textContent = 'No Pro brief draft selected.'; errEl.hidden = false; }
        return;
      }

      saveBtn.disabled = true;
      saveBtn.textContent = 'Scheduling…';

      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/me/scheduled-messages', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify({
          type: _SCHEDULE_CTX.type,
          scheduled_for_ms: res.ms,
          scheduled_tz: res.tz,
          content_payload: content,
          target_audience: audience
        })
      })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(resp) {
          saveBtn.disabled = false;
          saveBtn.textContent = 'Schedule Send';
          if (!resp.body || !resp.body.ok) {
            if (errEl) {
              errEl.textContent = (resp.body && (resp.body.message || resp.body.error)) || 'Could not schedule.';
              errEl.hidden = false;
            }
            return;
          }
          // Success
          var onScheduledCb = _SCHEDULE_CTX && _SCHEDULE_CTX.onScheduled;
          closeSchedulePopup();
          if (typeof onScheduledCb === 'function') {
            try { onScheduledCb({ id: resp.body.id, scheduled_for_ms: resp.body.scheduled_for_ms, label: res.label }); } catch (e) {}
          } else {
            window.alert('Scheduled for ' + res.label);
          }
        })
        .catch(function() {
          saveBtn.disabled = false;
          saveBtn.textContent = 'Schedule Send';
          if (errEl) { errEl.textContent = 'Network error.'; errEl.hidden = false; }
        });
    });
  }


  // ════════════════════════════════════════════════════════════════════
  // Scheduled messages — list in Met History tab + edit/cancel
  // ════════════════════════════════════════════════════════════════════

  function loadMetScheduledMessages() {
    var listEl = $('met-scheduled-list');
    if (!listEl) return;
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/me/scheduled-messages?status=pending', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          listEl.innerHTML = '<div style="padding:20px 16px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Could not load scheduled messages.</div>';
          return;
        }
        renderMetScheduledList(res.body.items || []);
      })
      .catch(function() {
        listEl.innerHTML = '<div style="padding:20px 16px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Network error.</div>';
      });
  }

  function renderMetScheduledList(items) {
    var listEl = $('met-scheduled-list');
    if (!listEl) return;
    if (items.length === 0) {
      listEl.innerHTML =
        '<div style="padding:24px 16px;text-align:center;color:rgba(15,17,22,0.55);font-size:13px;line-height:1.6;">'
        + '<div style="font-weight:600;color:#0E1116;margin-bottom:4px;">No scheduled messages</div>'
        + 'When you schedule a brief or post, it appears here so you can edit or cancel before it fires.'
        + '</div>';
      return;
    }
    var typeLabel = {
      'daily_brief': 'Daily Brief',
      'pro_brief': 'Pro Brief',
      'crew_post': 'Crew Post'
    };
    var html = '';
    for (var i = 0; i < items.length; i++) {
      var item = items[i];
      var content = item.content_payload || {};
      var bodyText = content.body || content.final_body || content.headline || '';
      var preview = bodyText.slice(0, 140);
      if (bodyText.length > 140) preview += '\u2026';
      var label = '';
      try {
        var d = new Date(item.scheduled_for_ms);
        var fmt = new Intl.DateTimeFormat('en-US', {
          timeZone: item.scheduled_tz || undefined,
          weekday: 'short', month: 'short', day: 'numeric',
          hour: 'numeric', minute: '2-digit', hour12: true,
          timeZoneName: 'short'
        });
        label = fmt.format(d);
      } catch (e) {
        label = new Date(item.scheduled_for_ms).toLocaleString();
      }

      html += '<div style="display:grid;grid-template-columns:auto 1fr auto;gap:14px;padding:14px 0;border-bottom:1px solid rgba(15,17,22,0.06);align-items:center;">'
        + '<div style="font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:#2E4FB8;background:rgba(46,79,184,0.08);padding:4px 10px;border-radius:5px;white-space:nowrap;">' + escapeHtml(typeLabel[item.type] || item.type) + '</div>'
        + '<div>'
        +   '<div style="font-size:13.5px;color:#0E1116;font-weight:500;">' + escapeHtml(preview || '(no body)') + '</div>'
        +   '<div style="font-size:11.5px;color:rgba(15,17,22,0.55);margin-top:3px;">Sends ' + escapeHtml(label) + '</div>'
        + '</div>'
        + '<div style="display:flex;gap:8px;">'
        +   '<button type="button" data-sched-cancel="' + item.id + '" style="padding:6px 12px;background:transparent;border:1px solid rgba(194,52,43,0.25);color:#9b2823;border-radius:6px;font-size:12px;cursor:pointer;">Cancel</button>'
        + '</div>'
        + '</div>';
    }
    listEl.innerHTML = html;

    // Wire cancel buttons
    var cancelBtns = listEl.querySelectorAll('[data-sched-cancel]');
    for (var j = 0; j < cancelBtns.length; j++) {
      cancelBtns[j].addEventListener('click', function(ev) {
        var id = parseInt(ev.currentTarget.getAttribute('data-sched-cancel'), 10);
        if (!id) return;
        if (!window.confirm('Cancel this scheduled message? It will not send.')) return;
        cancelMetScheduledMessage(id);
      });
    }
  }

  function cancelMetScheduledMessage(id) {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/me/scheduled-messages/' + id, {
      method: 'DELETE',
      credentials: 'include'
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (!res || !res.ok) {
          window.alert('Could not cancel: ' + ((res && (res.message || res.error)) || 'unknown error'));
          return;
        }
        loadMetScheduledMessages();
      })
      .catch(function() {
        window.alert('Network error.');
      });
  }

  function setupMetScheduledMessages() {
    if (window._metScheduledWired) return;
    window._metScheduledWired = true;
    var refreshBtn = $('met-scheduled-refresh');
    if (refreshBtn) refreshBtn.addEventListener('click', loadMetScheduledMessages);
  }


  // ════════════════════════════════════════════════════════════════════
  // My Subscribers (F-1) — full subscriber roster with operational context
  // ════════════════════════════════════════════════════════════════════
  var _MY_SUBS_ALL = [];
  var TIER_LABELS = {
    'pro_enterprise': 'Pro Enterprise',
    'pro_multi': 'Pro Multi',
    'pro_single': 'Pro Single',
    'hobbyist': 'Hobbyist',
    'free': 'Free',
  };
  var TIER_COLORS = {
    'pro_enterprise': { bg: '#1e3a8a', fg: '#fff' },
    'pro_multi':      { bg: '#2E4FB8', fg: '#fff' },
    'pro_single':     { bg: 'rgba(46,79,184,0.12)', fg: '#2E4FB8' },
    'hobbyist':       { bg: 'rgba(15,17,22,0.08)', fg: 'rgba(15,17,22,0.75)' },
    'free':           { bg: 'rgba(15,17,22,0.04)', fg: 'rgba(15,17,22,0.55)' },
  };

  function loadMySubscribers() {
    var listEl = $('met-subs-list');
    if (!listEl) return;
    listEl.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Loading&hellip;</div>';
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/met/my-subscribers', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          listEl.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">'
            + (res.status === 403 ? 'Met access required.' : 'Could not load subscribers.')
            + '</div>';
          return;
        }
        _MY_SUBS_ALL = res.body.subscribers || [];
        // Update tab count
        var countEl = $('met-subscribers-count');
        if (countEl) {
          countEl.hidden = (_MY_SUBS_ALL.length === 0);
          countEl.textContent = _MY_SUBS_ALL.length;
        }
        renderMySubscribers();
      })
      .catch(function() {
        listEl.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Network error.</div>';
      });
  }

  function renderMySubscribers() {
    var listEl = $('met-subs-list');
    if (!listEl) return;

    var q = (($('met-subs-search') && $('met-subs-search').value) || '').toLowerCase().trim();
    var tierFilter = ($('met-subs-tier-filter') && $('met-subs-tier-filter').value) || 'all';
    var channelFilter = ($('met-subs-channel-filter') && $('met-subs-channel-filter').value) || 'all';

    var filtered = _MY_SUBS_ALL.filter(function(s) {
      if (q) {
        var hay = [
          s.name || '', s.email || '',
          s.loc_label || '', s.loc_address || '',
          s.loc_county || '', TIER_LABELS[s.tier] || s.tier || ''
        ].join(' ').toLowerCase();
        if (hay.indexOf(q) === -1) return false;
      }
      if (tierFilter !== 'all' && s.tier !== tierFilter) return false;
      if (channelFilter !== 'all') {
        var channels = ((s.brief_prefs && s.brief_prefs.channels) || '').toLowerCase();
        if (channelFilter === 'sms' && channels.indexOf('sms') === -1) return false;
        if (channelFilter === 'email' && channels.indexOf('email') === -1) return false;
        if (channelFilter === 'both' && (channels.indexOf('sms') === -1 || channels.indexOf('email') === -1)) return false;
      }
      return true;
    });

    if (filtered.length === 0) {
      var emptyMsg = _MY_SUBS_ALL.length === 0
        ? 'No subscribers yet. They\u2019ll show here as people sign up.'
        : 'No matches for that filter.';
      listEl.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.55);font-size:13px;">' + emptyMsg + '</div>';
      return;
    }

    var html = '';
    for (var i = 0; i < filtered.length; i++) {
      var s = filtered[i];
      var tierLabel = TIER_LABELS[s.tier] || s.tier || 'free';
      var tierColors = TIER_COLORS[s.tier] || TIER_COLORS.free;
      var displayName = s.name || s.email || '(no name)';
      var locBit = s.loc_label || s.loc_address || 'no location';
      var county = s.loc_county ? (' &middot; ' + escapeHtml(s.loc_county) + ' County') : '';
      var phoneBit = s.phone ? escapeHtml(s.phone) : '<span style="color:rgba(15,17,22,0.40);">no phone</span>';

      // Brief prefs summary
      var prefs = s.brief_prefs || {};
      var briefSummary = [];
      if (prefs.morning_enabled) {
        var win = (prefs.morning_window_start || '') + ' \u2013 ' + (prefs.morning_window_end || '');
        briefSummary.push('Morning ' + win);
      }
      if (prefs.evening_enabled) briefSummary.push('Evening');
      if (briefSummary.length === 0) briefSummary.push('No briefs');
      var channelsLine = (prefs.channels || 'sms,email').split(',').map(function(c) {
        return c.trim().toUpperCase();
      }).join(' \u00b7 ');

      // Thresholds
      var thresholds = s.thresholds || [];
      var thresholdHtml = '';
      if (thresholds.length > 0) {
        thresholdHtml = '<div style="font-size:11.5px;color:rgba(15,17,22,0.55);margin-top:6px;">'
          + '<strong style="color:rgba(15,17,22,0.75);">Alerts:</strong> ';
        var parts = [];
        for (var j = 0; j < thresholds.length; j++) {
          var t = thresholds[j];
          parts.push(escapeHtml(t.metric + ' ' + t.comparator + ' ' + t.threshold_value + (t.unit || '')));
        }
        thresholdHtml += parts.join(', ') + '</div>';
      }

      // Action buttons
      var isPro = (s.tier === 'pro_single' || s.tier === 'pro_multi' || s.tier === 'pro_enterprise');
      var actions = '';
      if (isPro) {
        actions += '<button type="button" data-sub-message="' + s.user_id + '" '
          + 'style="padding:6px 12px;background:#2E4FB8;border:none;color:#fff;border-radius:6px;font-size:12px;cursor:pointer;">Message</button>';
      }

      html += '<div style="padding:16px 0;border-bottom:1px solid rgba(15,17,22,0.06);">'
        + '<div style="display:grid;grid-template-columns:1fr auto;gap:14px;align-items:start;">'
        +   '<div style="min-width:0;">'
        +     '<div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap;">'
        +       '<div style="font-size:14.5px;font-weight:600;color:#0E1116;">' + escapeHtml(displayName) + '</div>'
        +       '<span style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;padding:3px 8px;border-radius:4px;background:' + tierColors.bg + ';color:' + tierColors.fg + ';">' + escapeHtml(tierLabel) + '</span>'
        +     '</div>'
        +     '<div style="font-size:12px;color:rgba(15,17,22,0.60);margin-top:3px;">'
        +       escapeHtml(s.email || '') + ' &middot; ' + phoneBit
        +     '</div>'
        +     '<div style="font-size:12px;color:rgba(15,17,22,0.70);margin-top:6px;">'
        +       '<strong style="color:rgba(15,17,22,0.85);">' + escapeHtml(locBit) + '</strong>' + county
        +     '</div>'
        +     '<div style="font-size:11.5px;color:rgba(15,17,22,0.55);margin-top:6px;">'
        +       briefSummary.join(' &middot; ') + ' &middot; <span style="font-family:monospace;letter-spacing:0.5px;">' + channelsLine + '</span>'
        +     '</div>'
        +     thresholdHtml
        +   '</div>'
        +   '<div style="display:flex;gap:8px;align-items:center;">' + actions + '</div>'
        + '</div>'
        + '</div>';
    }
    listEl.innerHTML = html;

    // Wire action buttons
    var msgBtns = listEl.querySelectorAll('[data-sub-message]');
    for (var k = 0; k < msgBtns.length; k++) {
      msgBtns[k].addEventListener('click', function(ev) {
        var uid = parseInt(ev.currentTarget.getAttribute('data-sub-message'), 10);
        if (!uid) return;
        // Reuse the Met-initiated Pro Thread flow
        if (typeof startMetNewThread === 'function') {
          startMetNewThread(uid);
        }
      });
    }
  }

  function setupMySubscribers() {
    if (window._mySubsWired) return;
    window._mySubsWired = true;
    var refreshBtn = $('met-subs-refresh');
    if (refreshBtn) refreshBtn.addEventListener('click', loadMySubscribers);

    var searchEl = $('met-subs-search');
    var tierEl = $('met-subs-tier-filter');
    var channelEl = $('met-subs-channel-filter');
    if (searchEl) searchEl.addEventListener('input', renderMySubscribers);
    if (tierEl) tierEl.addEventListener('change', renderMySubscribers);
    if (channelEl) channelEl.addEventListener('change', renderMySubscribers);
  }


  // ════════════════════════════════════════════════════════════════════
  // Create Mission Template modal (F-X2)
  // ════════════════════════════════════════════════════════════════════
  function openCreateTemplateModal() {
    var modal = $('create-template-modal');
    if (!modal) return;
    // Reset form
    ['create-tmpl-name', 'create-tmpl-eyebrow', 'create-tmpl-headline', 'create-tmpl-prompt']
      .forEach(function(id) { var el = $(id); if (el) el.value = ''; });
    var routineRadio = document.querySelector('input[name="create-tmpl-mode"][value="routine"]');
    if (routineRadio) routineRadio.checked = true;
    var errEl = $('create-tmpl-error');
    if (errEl) errEl.hidden = true;
    modal.hidden = false;
    setTimeout(function() { var n = $('create-tmpl-name'); if (n) n.focus(); }, 50);
  }

  function closeCreateTemplateModal() {
    var modal = $('create-template-modal');
    if (modal) modal.hidden = true;
  }

  function saveCreateTemplate() {
    var name = ($('create-tmpl-name').value || '').trim();
    var eyebrow = ($('create-tmpl-eyebrow').value || '').trim();
    var headline = ($('create-tmpl-headline').value || '').trim();
    var prompt = ($('create-tmpl-prompt').value || '').trim();
    var modeEl = document.querySelector('input[name="create-tmpl-mode"]:checked');
    var mode = modeEl ? modeEl.value : 'routine';
    var errEl = $('create-tmpl-error');

    if (!name) {
      if (errEl) { errEl.textContent = 'Template name is required.'; errEl.hidden = false; }
      return;
    }
    if (!headline) {
      if (errEl) { errEl.textContent = 'Button headline is required.'; errEl.hidden = false; }
      return;
    }
    if (!prompt) {
      if (errEl) { errEl.textContent = 'SMS prompt is required.'; errEl.hidden = false; }
      return;
    }

    var saveBtn = $('create-tmpl-save');
    if (saveBtn) { saveBtn.disabled = true; saveBtn.textContent = 'Creating…'; }

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/met/mission-templates', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({
        template_name: name,
        eyebrow: eyebrow || null,
        btn_headline: headline,
        prompt: prompt,
        mode: mode
      })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.textContent = 'Create template'; }
        if (!res || !res.ok) {
          if (errEl) {
            errEl.textContent = (res && (res.message || res.error)) || 'Could not create.';
            errEl.hidden = false;
          }
          return;
        }
        closeCreateTemplateModal();
        if (res.auto_approved) {
          window.alert('"' + name + '" is ready to use. Find it in the mission template list.');
        } else {
          window.alert(
            '"' + name + '" was submitted for approval. ' +
            'Severe templates need a second Met or admin to approve before they can fire. ' +
            'You can use it once it\'s approved.'
          );
        }
        // Refresh mission template UI if it's currently rendered
        if (typeof renderMissionTemplates === 'function') {
          try { renderMissionTemplates(); } catch (e) {}
        }
      })
      .catch(function() {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.textContent = 'Create template'; }
        if (errEl) { errEl.textContent = 'Network error.'; errEl.hidden = false; }
      });
  }

  function setupCreateTemplateModal() {
    if (window._createTmplWired) return;
    window._createTmplWired = true;
    var close = $('create-tmpl-close');
    var scrim = $('create-tmpl-scrim');
    var cancel = $('create-tmpl-cancel');
    var save = $('create-tmpl-save');
    if (close) close.addEventListener('click', closeCreateTemplateModal);
    if (scrim) scrim.addEventListener('click', closeCreateTemplateModal);
    if (cancel) cancel.addEventListener('click', closeCreateTemplateModal);
    if (save) save.addEventListener('click', saveCreateTemplate);
  }


  // ════════════════════════════════════════════════════════════════════
  // Coverage scheduler (Phase 11, May 17) — Met "My Schedule" tab
  // ════════════════════════════════════════════════════════════════════

  var DAY_NAMES = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
  var DAY_NAMES_FULL = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];

  // Heartbeat — pings backend every 60s while Met workspace is open so
  // admin can see who's actually around right now.
  var _heartbeatTimer = null;
  function startMetHeartbeat() {
    if (_heartbeatTimer) return;
    function ping() {
      try {
        var BACKEND = 'https://wv-valet-backend.onrender.com';
        fetch(BACKEND + '/api/v1/met/heartbeat', {
          method: 'POST',
          credentials: 'include'
        }).catch(function() { /* silent */ });
      } catch (e) {}
    }
    ping();
    _heartbeatTimer = setInterval(ping, 60 * 1000);
  }

  function loadMetProfile() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var nameInput = $('met-profile-name');
    var phoneInput = $('met-profile-phone');
    if (!nameInput || !phoneInput) return;
    fetch(BACKEND + '/api/v1/me/profile', { credentials: 'include' })
      .then(function(r) { return r.json(); })
      .then(function(res) {
        if (!res || !res.ok) return;
        nameInput.value = res.profile && res.profile.name ? res.profile.name : '';
        phoneInput.value = res.profile && res.profile.phone ? res.profile.phone : '';
      })
      .catch(function() { /* silent */ });
  }

  function saveMetProfile() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var nameInput = $('met-profile-name');
    var phoneInput = $('met-profile-phone');
    var status = $('met-profile-status');
    var saveBtn = $('met-profile-save-btn');
    if (!nameInput || !phoneInput) return;
    var name = (nameInput.value || '').trim();
    var phone = (phoneInput.value || '').trim();
    if (!phone) {
      if (status) {
        status.textContent = 'Phone is required for Rosie to text you.';
        status.style.color = 'rgba(194,52,43,0.85)';
      }
      return;
    }
    if (saveBtn) { saveBtn.disabled = true; saveBtn.style.opacity = '0.5'; }
    if (status) {
      status.textContent = 'Saving...';
      status.style.color = 'rgba(15,17,22,0.55)';
    }
    fetch(BACKEND + '/api/v1/me/profile', {
      method: 'PATCH',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: name, phone: phone })
    })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.style.opacity = '1'; }
        if (res.body && res.body.ok) {
          if (status) {
            status.textContent = 'Saved. Rosie will text you using this number.';
            status.style.color = 'rgba(18,161,80,0.85)';
          }
          // Refresh value display in case backend normalized phone
          if (res.body.profile && res.body.profile.phone) {
            phoneInput.value = res.body.profile.phone;
          }
        } else {
          var err = (res.body && res.body.error) || ('error ' + res.status);
          if (status) {
            status.textContent = 'Save failed: ' + err;
            status.style.color = 'rgba(194,52,43,0.85)';
          }
        }
      })
      .catch(function(err) {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.style.opacity = '1'; }
        if (status) {
          status.textContent = 'Network error — try again.';
          status.style.color = 'rgba(194,52,43,0.85)';
        }
      });
  }

  // Wire profile save button + load on schedule panel open
  (function() {
    var btn = $('met-profile-save-btn');
    if (btn) btn.addEventListener('click', saveMetProfile);
  })();

  function loadMetSchedule() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var grid = $('met-schedule-recurring-grid');
    var list = $('met-schedule-upcoming-list');
    if (grid) grid.innerHTML = '<div style="font-size: 13px; color: rgba(15,17,22,0.55); padding: 16px;">Loading your schedule…</div>';

    fetch(BACKEND + '/api/v1/met/my-schedule', { credentials: 'include' })
      .then(function(r) { return r.json(); })
      .then(function(res) {
        if (!res || !res.ok) {
          if (grid) grid.innerHTML = '<div style="font-size: 13px; color: rgba(194,52,43,0.85); padding: 16px;">Could not load schedule.</div>';
          return;
        }
        _renderMetScheduleGrid(res.recurring_shifts || []);
        _renderMetUpcomingList(res.recurring_shifts || [], res.overrides || []);
      })
      .catch(function() {
        if (grid) grid.innerHTML = '<div style="font-size: 13px; color: rgba(194,52,43,0.85); padding: 16px;">Network error loading schedule.</div>';
      });

    // Phase 2: also load today's tasks
    loadMetTasks();
    // Phase B (May 17): load storm shelter activations
    loadMetStormShelters();
  }

  // ════════════════════════════════════════════════════════════════════
  // Storm Shelter activations (Met-pay event, $25 per activation)
  // ════════════════════════════════════════════════════════════════════

  function loadMetStormShelters() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var list = $('met-shelter-list');
    if (!list) return;

    fetch(BACKEND + '/api/v1/met/storm-shelter/active', { credentials: 'include' })
      .then(function(r) { return r.json(); })
      .then(function(res) {
        if (!res || !res.ok) {
          list.innerHTML = '<div style="font-size: 12.5px; color: rgba(194,52,43,0.85); padding: 12px;">Could not load activations.</div>';
          return;
        }
        _renderMetStormShelters(res.activations || []);
      })
      .catch(function() {
        list.innerHTML = '<div style="font-size: 12.5px; color: rgba(194,52,43,0.85); padding: 12px;">Network error.</div>';
      });
  }

  function _renderMetStormShelters(activations) {
    var list = $('met-shelter-list');
    if (!list) return;
    if (!activations || activations.length === 0) {
      list.innerHTML = '<div style="font-size: 12.5px; color: rgba(15,17,22,0.55); padding: 12px; background: rgba(15,17,22,0.02); border-radius: 6px;">No activations open or recent. Open one when a tornado / severe T-storm warning impacts your subscribers.</div>';
      return;
    }
    var html = '<div style="display: flex; flex-direction: column; gap: 8px;">';
    for (var i = 0; i < activations.length; i++) {
      var a = activations[i];
      var bg, border, badge, action;
      if (a.is_open) {
        bg = 'rgba(194,52,43,0.05)';
        border = 'rgba(194,52,43,0.30)';
        badge = '<span style="background: #C2342B; color: #fff; padding: 3px 9px; border-radius: 4px; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em;">⚡ OPEN</span>';
        action = '<button type="button" data-shelter-close="' + a.id + '" style="background: #0E1116; border: 0; color: #fff; padding: 7px 14px; font-size: 12px; font-weight: 600; border-radius: 6px; cursor: pointer;">Close activation</button>';
      } else {
        bg = 'rgba(18,161,80,0.04)';
        border = 'rgba(15,17,22,0.08)';
        var reasonLabel = (a.close_reason === 'warning_expired') ? 'auto-closed (warning expired)' :
                          (a.close_reason === 'admin_closed') ? 'closed by admin' : 'closed manually';
        badge = '<span style="background: rgba(18,161,80,0.18); color: #0c7a3c; padding: 3px 9px; border-radius: 4px; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em;">✓ Closed</span>';
        action = '<div style="font-size: 11.5px; color: rgba(15,17,22,0.55);">' + reasonLabel + '</div>';
      }

      var openedDate = new Date(a.opened_at_ms);
      var openedLabel = openedDate.toLocaleString('en-US', {
        month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit'
      });
      var payout = '$' + (a.payout_cents / 100).toFixed(2);

      html += '<div style="background: ' + bg + '; border: 1px solid ' + border + '; border-radius: 8px; padding: 12px 14px;">';
      html += '<div style="display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap;">';
      html += '<div style="flex: 1; min-width: 0;">';
      html += '<div style="display: flex; align-items: center; gap: 8px; margin-bottom: 5px; flex-wrap: wrap;">';
      html += badge;
      html += '<span style="font-size: 11px; color: rgba(15,17,22,0.55);">Opened ' + escapeHtml(openedLabel) + '</span>';
      html += '<span style="font-size: 11px; color: #0c7a3c; font-weight: 600;">' + payout + '</span>';
      html += '</div>';
      html += '<div style="font-size: 14px; font-weight: 600; color: #0E1116;">' + escapeHtml(a.region_label || 'Region') + '</div>';
      if (a.nws_event) {
        html += '<div style="font-size: 11.5px; color: rgba(15,17,22,0.55); margin-top: 2px;">NWS event: ' + escapeHtml(a.nws_event) + '</div>';
      }
      if (a.affected_count) {
        html += '<div style="font-size: 11.5px; color: rgba(15,17,22,0.55); margin-top: 2px;">' + a.affected_count + ' subscriber(s) affected</div>';
      }
      html += '</div>';
      html += '<div style="flex-shrink: 0;">' + action + '</div>';
      html += '</div>';
      html += '</div>';
    }
    html += '</div>';
    list.innerHTML = html;

    // Wire close buttons
    list.querySelectorAll('[data-shelter-close]').forEach(function(btn) {
      btn.addEventListener('click', function() {
        var id = btn.getAttribute('data-shelter-close');
        if (!window.confirm('Close this Storm Shelter activation? You will earn the $25 bonus.')) return;
        btn.disabled = true;
        btn.textContent = 'Closing…';
        var BACKEND = 'https://wv-valet-backend.onrender.com';
        fetch(BACKEND + '/api/v1/met/storm-shelter/' + id + '/close', {
          method: 'POST',
          credentials: 'include'
        })
          .then(function(r) { return r.json(); })
          .then(function(res) {
            if (res && res.ok) {
              loadMetStormShelters();
            } else {
              btn.disabled = false;
              btn.textContent = 'Close activation';
              window.alert((res && (res.message || res.error)) || 'Could not close.');
            }
          })
          .catch(function() {
            btn.disabled = false;
            btn.textContent = 'Close activation';
            window.alert('Network error.');
          });
      });
    });
  }

  function _setupStormShelterOpenButton() {
    var btn = $('met-shelter-open-btn');
    if (!btn || btn._wired) return;
    btn._wired = true;

    btn.addEventListener('click', function() {
      // Simple prompt-based v1 — modal can come in v2.
      var region = window.prompt(
        'OPEN STORM SHELTER ACTIVATION\n\n'
        + 'Enter the region affected (e.g. "Central Kansas", "Boone County, IN").\n'
        + 'You will earn $25 once you close this activation (or when the NWS warning expires).',
        ''
      );
      if (!region || !region.trim()) return;

      var nwsEvent = window.prompt(
        'Optional: enter the NWS event name (e.g. "Tornado Warning").\n'
        + 'Leave blank if not tied to a specific NWS event — activation will auto-close after 12 hours as a sanity cap.',
        ''
      );
      if (nwsEvent === null) return;  // cancel

      var body = { region_label: region.trim() };
      if (nwsEvent.trim()) body.nws_event = nwsEvent.trim();

      var BACKEND = 'https://wv-valet-backend.onrender.com';
      btn.disabled = true;
      var originalText = btn.textContent;
      btn.textContent = 'Opening…';
      fetch(BACKEND + '/api/v1/met/storm-shelter/open', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify(body)
      })
        .then(function(r) { return r.json(); })
        .then(function(res) {
          btn.disabled = false;
          btn.textContent = originalText;
          if (!res || !res.ok) {
            if (res && res.error === 'already-open') {
              window.alert('You already have an open activation for this region.');
            } else {
              window.alert((res && (res.message || res.error)) || 'Could not open activation.');
            }
            return;
          }
          loadMetStormShelters();
        })
        .catch(function() {
          btn.disabled = false;
          btn.textContent = originalText;
          window.alert('Network error.');
        });
    });
  }


  // Phase 2 (May 17): Load + render the Met's current daily brief tasks
  // with "I'm on it" button. Polls every 60 seconds when Schedule tab is
  // open so countdowns and escalation badges stay current.
  var _metTasksTimer = null;

  function loadMetTasks() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var list = $('met-tasks-list');
    if (!list) return;

    fetch(BACKEND + '/api/v1/met/my-tasks', { credentials: 'include' })
      .then(function(r) { return r.json(); })
      .then(function(res) {
        if (!res || !res.ok) {
          list.innerHTML = '<div style="font-size: 13px; color: rgba(194,52,43,0.85); padding: 16px;">Could not load tasks.</div>';
          return;
        }
        _renderMetTasks(res.tasks || []);
      })
      .catch(function() {
        list.innerHTML = '<div style="font-size: 13px; color: rgba(194,52,43,0.85); padding: 16px;">Network error.</div>';
      });

    // Start the polling timer if not already running
    if (!_metTasksTimer) {
      _metTasksTimer = setInterval(function() {
        // Only refresh if the Schedule tab is currently active
        var panel = document.querySelector('[data-met-panel="schedule"]');
        if (panel && panel.classList.contains('is-active')) {
          loadMetTasks();
        }
      }, 60 * 1000);
    }
  }

  function _renderMetTasks(tasks) {
    var list = $('met-tasks-list');
    if (!list) return;
    if (!tasks || tasks.length === 0) {
      list.innerHTML = '<div style="font-size: 13px; color: rgba(15,17,22,0.55); padding: 20px; text-align: center; background: rgba(18,161,80,0.05); border: 1px solid rgba(18,161,80,0.15); border-radius: 8px;">✓ No brief tasks due in the next two days. Nice work.</div>';
      return;
    }
    var now = Date.now();
    var html = '<div style="display: flex; flex-direction: column; gap: 10px;">';
    for (var i = 0; i < tasks.length; i++) {
      var t = tasks[i];
      var status = t.status;
      var dueMs = t.due_at_ms;
      var timeToDue = dueMs - now;
      var isOverdue = (timeToDue < 0 && status !== 'sent');
      var isSent = (status === 'sent');
      var isInProgress = (status === 'in_progress');

      // Background + accent color by state
      var bg = '#fff';
      var border = 'rgba(15,17,22,0.08)';
      var statusLabel = '';
      var statusColor = '';
      if (isSent) {
        bg = 'rgba(18,161,80,0.06)';
        border = 'rgba(18,161,80,0.25)';
        statusLabel = '✓ Sent';
        statusColor = '#0c7a3c';
      } else if (isOverdue) {
        bg = 'rgba(194,52,43,0.06)';
        border = 'rgba(194,52,43,0.30)';
        statusLabel = 'OVERDUE';
        statusColor = '#9b2823';
      } else if (isInProgress) {
        bg = 'rgba(46,79,184,0.06)';
        border = 'rgba(46,79,184,0.25)';
        statusLabel = 'In progress';
        statusColor = '#2E4FB8';
      } else if (timeToDue < 30 * 60 * 1000) {
        // Less than 30 min — yellow warning
        bg = 'rgba(245,165,36,0.06)';
        border = 'rgba(245,165,36,0.30)';
        statusLabel = 'Due soon';
        statusColor = '#B8770A';
      } else {
        statusLabel = 'Pending';
        statusColor = 'rgba(15,17,22,0.55)';
      }

      // Format due time
      var dueDate = new Date(dueMs);
      var dueLabel = dueDate.toLocaleString('en-US', {
        weekday: 'short', month: 'short', day: 'numeric',
        hour: 'numeric', minute: '2-digit'
      });

      // Time-to-deadline label
      var ttdLabel = '';
      if (isSent) {
        // Show send time
        var sentDate = t.sent_at_ms ? new Date(t.sent_at_ms) : null;
        ttdLabel = sentDate ? ('Sent ' + sentDate.toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit' })) : '';
      } else if (isOverdue) {
        var overdueMin = Math.abs(Math.round(timeToDue / 60000));
        ttdLabel = overdueMin + ' min overdue';
      } else {
        var minsLeft = Math.round(timeToDue / 60000);
        if (minsLeft < 60) ttdLabel = 'Due in ' + minsLeft + ' min';
        else {
          var hrsLeft = Math.floor(minsLeft / 60);
          var remMin = minsLeft % 60;
          ttdLabel = 'Due in ' + hrsLeft + 'h ' + (remMin > 0 ? remMin + 'm' : '');
        }
      }

      html += '<div data-task-id="' + t.id + '" style="background: ' + bg + '; border: 1px solid ' + border + '; border-radius: 10px; padding: 14px 16px; display: flex; align-items: center; gap: 14px;">';
      html += '<div style="flex: 1; min-width: 0;">';
      html += '<div style="display: flex; align-items: center; gap: 8px; margin-bottom: 4px;">';
      html += '<span style="font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: ' + statusColor + ';">' + statusLabel + '</span>';
      html += '<span style="font-size: 11px; color: rgba(15,17,22,0.45);">' + escapeHtml(t.tier || '') + '</span>';
      html += '</div>';
      html += '<div style="font-size: 14.5px; font-weight: 600; color: #0E1116;">' + escapeHtml(t.subscriber_name || t.subscriber_email || 'Subscriber') + '</div>';
      html += '<div style="font-size: 11.5px; color: rgba(15,17,22,0.55); margin-top: 3px;">' + escapeHtml(dueLabel) + ' &middot; ' + escapeHtml(ttdLabel) + '</div>';
      html += '</div>';

      // Action buttons
      html += '<div style="display: flex; gap: 8px; flex-shrink: 0;">';
      if (!isSent) {
        if (!t.started_at_ms) {
          html += '<button type="button" data-task-start="' + t.id + '" style="background: #2E4FB8; border: 0; color: #fff; font-size: 12px; font-weight: 600; padding: 8px 14px; border-radius: 6px; cursor: pointer;">I\'m on it</button>';
        }
        html += '<button type="button" data-task-open="' + t.id + '" data-task-subscriber="' + t.subscriber_user_id + '" style="background: transparent; border: 1px solid rgba(15,17,22,0.15); color: rgba(15,17,22,0.85); font-size: 12px; font-weight: 600; padding: 8px 14px; border-radius: 6px; cursor: pointer;">Open brief</button>';
      } else {
        html += '<div style="color: #0c7a3c; font-size: 12px; font-weight: 600;">✓ Done</div>';
      }
      html += '</div>';
      html += '</div>';
    }
    html += '</div>';
    list.innerHTML = html;

    // Wire "I'm on it" buttons
    list.querySelectorAll('[data-task-start]').forEach(function(btn) {
      btn.addEventListener('click', function() {
        var taskId = btn.getAttribute('data-task-start');
        btn.disabled = true;
        btn.textContent = 'Starting…';
        var BACKEND = 'https://wv-valet-backend.onrender.com';
        fetch(BACKEND + '/api/v1/met/tasks/' + taskId + '/start', {
          method: 'POST',
          credentials: 'include'
        })
          .then(function(r) { return r.json(); })
          .then(function(res) {
            if (res && res.ok) {
              loadMetTasks();
            } else {
              btn.disabled = false;
              btn.textContent = "I'm on it";
              window.alert((res && (res.message || res.error)) || 'Could not start task.');
            }
          })
          .catch(function() {
            btn.disabled = false;
            btn.textContent = "I'm on it";
            window.alert('Network error.');
          });
      });
    });

    // Wire "Open brief" buttons — switch to Pro Briefs tab and try
    // to find a matching draft for this subscriber so the Met lands
    // directly on the right editor instead of having to search.
    list.querySelectorAll('[data-task-open]').forEach(function(btn) {
      btn.addEventListener('click', function() {
        var subscriberId = parseInt(btn.getAttribute('data-task-subscriber'), 10);
        // Switch to Pro Briefs tab — its load function refreshes the list
        var proBriefsTab = document.querySelector('[data-met-tab="pro-briefs"]');
        if (proBriefsTab) proBriefsTab.click();

        // After the tab activates and loadMetProBriefs runs, try to
        // open the draft for this subscriber. Need to wait for the
        // fetch to complete; we use a polling approach with a cap so
        // we don't spin forever if no draft exists.
        if (subscriberId && typeof openProBriefEditor === 'function') {
          var attempts = 0;
          var poll = setInterval(function() {
            attempts++;
            // Find pro brief draft for this subscriber that's not yet sent
            var match = null;
            if (Array.isArray(window.MET_PRO_BRIEFS)) {
              for (var i = 0; i < window.MET_PRO_BRIEFS.length; i++) {
                var d = window.MET_PRO_BRIEFS[i];
                if (d.user_id === subscriberId && d.status !== 'sent') {
                  match = d;
                  break;
                }
              }
            }
            if (match) {
              clearInterval(poll);
              openProBriefEditor(match.id);
            } else if (attempts >= 10) {
              // 10 × 200ms = 2 seconds. Give up and let the Met find
              // the draft manually. No alert — they're already on the
              // right tab; an alert would be more annoying than helpful.
              clearInterval(poll);
            }
          }, 200);
        }
      });
    });
  }


  function _renderMetScheduleGrid(shifts) {
    var grid = $('met-schedule-recurring-grid');
    if (!grid) return;
    // Group shifts by day_of_week
    var byDay = [[], [], [], [], [], [], []];
    for (var i = 0; i < shifts.length; i++) {
      byDay[shifts[i].day_of_week].push(shifts[i]);
    }
    var html = '<div style="display: grid; grid-template-columns: repeat(7, 1fr); gap: 8px; min-width: 700px;">';
    for (var d = 0; d < 7; d++) {
      html += '<div style="background: rgba(15,17,22,0.02); border: 1px solid rgba(15,17,22,0.06); border-radius: 8px; padding: 10px; min-height: 110px;">';
      html += '<div style="font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: rgba(15,17,22,0.55); margin-bottom: 8px;">' + DAY_NAMES[d] + '</div>';
      if (byDay[d].length === 0) {
        html += '<div style="font-size: 12px; color: rgba(15,17,22,0.40); font-style: italic;">No shifts</div>';
      } else {
        for (var s = 0; s < byDay[d].length; s++) {
          var shift = byDay[d][s];
          var label = '';
          if (shift.scope_kind === 'review_pool') {
            label = '⚙ Review Pool';
          } else if (shift.scope_kind === 'subscriber_set') {
            label = '👥 ' + (shift.set_owner_name || 'Met') + "'s subscribers";
          } else {
            label = '👤 ' + (shift.subscriber_name || 'subscriber');
          }
          html += '<div style="display: flex; align-items: center; justify-content: space-between; background: #fff; border: 1px solid rgba(46,79,184,0.20); border-radius: 6px; padding: 6px 8px; margin-bottom: 4px; font-size: 12px;">'
            +   '<span style="flex: 1; min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">' + escapeHtml(label) + '</span>'
            +   '<button type="button" data-delete-shift="' + shift.id + '" style="background: transparent; border: 0; color: rgba(194,52,43,0.75); padding: 2px 4px; cursor: pointer; font-size: 12px; line-height: 1;" title="Delete shift">×</button>'
            + '</div>';
        }
      }
      html += '</div>';
    }
    html += '</div>';
    grid.innerHTML = html;

    // Wire delete buttons
    grid.querySelectorAll('[data-delete-shift]').forEach(function(btn) {
      btn.addEventListener('click', function() {
        var id = parseInt(btn.getAttribute('data-delete-shift'), 10);
        if (!window.confirm('Delete this recurring shift?')) return;
        var BACKEND = 'https://wv-valet-backend.onrender.com';
        fetch(BACKEND + '/api/v1/met/recurring-shifts/' + id, {
          method: 'DELETE',
          credentials: 'include'
        })
          .then(function(r) { return r.json(); })
          .then(function(res) {
            if (res && res.ok) loadMetSchedule();
          });
      });
    });
  }

  function _renderMetUpcomingList(shifts, overrides) {
    var list = $('met-schedule-upcoming-list');
    if (!list) return;
    // Build 14-day view from today
    var now = new Date();
    var html = '<div style="display: flex; flex-direction: column; gap: 6px;">';
    for (var i = 0; i < 14; i++) {
      var d = new Date(now.getTime() + i * 86400000);
      var dow = d.getDay();
      var dateStr = d.toISOString().slice(0, 10);
      var label = d.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });
      // What scopes do I have this day?
      var mine = shifts.filter(function(s) { return s.day_of_week === dow; });
      var todayOverrides = overrides.filter(function(o) { return o.shift_date === dateStr; });

      // Was anything dropped or claimed?
      var dropCount = todayOverrides.filter(function(o) { return o.override_kind === 'drop'; }).length;
      var claimCount = todayOverrides.filter(function(o) { return o.override_kind === 'claim'; }).length;

      var statusBadge = '';
      if (i === 0) statusBadge = '<span style="background: rgba(46,79,184,0.12); color: #2E4FB8; padding: 2px 7px; border-radius: 4px; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em;">Today</span>';
      else if (i === 1) statusBadge = '<span style="background: rgba(15,17,22,0.05); color: rgba(15,17,22,0.65); padding: 2px 7px; border-radius: 4px; font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em;">Tomorrow</span>';

      html += '<div style="display: flex; align-items: center; gap: 12px; padding: 10px 14px; background: ' + (i === 0 ? 'rgba(46,79,184,0.04)' : '#fff') + '; border: 1px solid rgba(15,17,22,0.08); border-radius: 8px;">';
      html += '<div style="min-width: 130px; font-size: 13px; font-weight: 600; color: #0E1116;">' + label + ' ' + statusBadge + '</div>';
      html += '<div style="flex: 1; font-size: 12.5px; color: rgba(15,17,22,0.65);">';
      if (mine.length === 0 && claimCount === 0) {
        html += '<span style="color: rgba(15,17,22,0.45); font-style: italic;">No shifts</span>';
      } else {
        var labels = [];
        mine.forEach(function(s) {
          if (s.scope_kind === 'review_pool') labels.push('Review Pool');
          else if (s.scope_kind === 'subscriber_set') labels.push((s.set_owner_name || 'Met') + "'s subs");
          else labels.push(s.subscriber_name || 'subscriber');
        });
        html += escapeHtml(labels.join(' · '));
        if (dropCount > 0) html += ' <span style="color: rgba(194,52,43,0.85); font-size: 11px;">(' + dropCount + ' dropped)</span>';
      }
      html += '</div>';
      html += '</div>';
    }
    html += '</div>';
    list.innerHTML = html;
  }

  function setupMetSchedule() {
    if (window._metScheduleWired) return;
    window._metScheduleWired = true;

    // Storm Shelter open-button wiring (Phase B, May 17)
    _setupStormShelterOpenButton();

    // Add-shift modal wiring
    var addBtn = $('met-schedule-add-shift-btn');
    var modal = $('met-add-shift-modal');
    var closeBtn = $('met-add-shift-close');
    var scrim = $('met-add-shift-scrim');
    var cancelBtn = $('met-add-shift-cancel');
    var saveBtn = $('met-add-shift-save');
    var setOwnerWrap = $('met-add-shift-set-owner-wrap');
    var subWrap = $('met-add-shift-subscriber-wrap');
    var setOwnerSel = $('met-add-shift-set-owner');
    var subSel = $('met-add-shift-subscriber');
    var errEl = $('met-add-shift-error');

    function openAddShift() {
      if (!modal) return;
      // Reset
      var radios = document.querySelectorAll('input[name="met-add-shift-scope"]');
      radios.forEach(function(r) { r.checked = (r.value === 'review_pool'); });
      if (setOwnerWrap) setOwnerWrap.hidden = true;
      if (subWrap) subWrap.hidden = true;
      if (errEl) errEl.hidden = true;
      // Populate dropdowns
      _loadMetsListForDropdown();
      _loadSubscribersForDropdown();
      modal.hidden = false;
    }
    function closeAddShift() {
      if (modal) modal.hidden = true;
    }

    function _loadMetsListForDropdown() {
      if (!setOwnerSel) return;
      // For non-admin Mets we still want the list — use the /met/pro-subscribers
      // endpoint to derive distinct primary Met names via subscribers list.
      // Simpler: just fetch the admin endpoint; if not admin, use a fallback.
      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/admin/mets-list', { credentials: 'include' })
        .then(function(r) { return r.json(); })
        .then(function(res) {
          var opts = '<option value="">Select a Met…</option>';
          if (res && res.ok && res.mets) {
            res.mets.forEach(function(m) {
              opts += '<option value="' + m.id + '">' + escapeHtml(m.name || m.email) + '</option>';
            });
          }
          setOwnerSel.innerHTML = opts;
        })
        .catch(function() { /* silent */ });
    }

    function _loadSubscribersForDropdown() {
      if (!subSel) return;
      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/met/pro-subscribers', { credentials: 'include' })
        .then(function(r) { return r.json(); })
        .then(function(res) {
          var opts = '<option value="">Select a subscriber…</option>';
          if (res && res.subscribers) {
            res.subscribers.forEach(function(s) {
              var label = (s.name || s.email) + ' (' + (s.tier || '?') + ')';
              opts += '<option value="' + s.user_id + '">' + escapeHtml(label) + '</option>';
            });
          }
          subSel.innerHTML = opts;
        })
        .catch(function() { /* silent */ });
    }

    if (addBtn) addBtn.addEventListener('click', openAddShift);
    if (closeBtn) closeBtn.addEventListener('click', closeAddShift);
    if (scrim) scrim.addEventListener('click', closeAddShift);
    if (cancelBtn) cancelBtn.addEventListener('click', closeAddShift);

    // Toggle field visibility based on scope
    document.querySelectorAll('input[name="met-add-shift-scope"]').forEach(function(r) {
      r.addEventListener('change', function() {
        var scope = r.value;
        if (setOwnerWrap) setOwnerWrap.hidden = (scope !== 'subscriber_set');
        if (subWrap) subWrap.hidden = (scope !== 'subscriber');
      });
    });

    if (saveBtn) {
      saveBtn.addEventListener('click', function() {
        var dayEl = $('met-add-shift-day');
        var scopeEl = document.querySelector('input[name="met-add-shift-scope"]:checked');
        var day = parseInt(dayEl.value, 10);
        var scope = scopeEl ? scopeEl.value : 'review_pool';
        var body = { day_of_week: day, scope_kind: scope };
        if (scope === 'subscriber_set') {
          var soVal = setOwnerSel ? setOwnerSel.value : '';
          if (!soVal) {
            if (errEl) { errEl.textContent = 'Please pick a Met whose subscribers you are covering.'; errEl.hidden = false; }
            return;
          }
          body.set_owner_met_id = parseInt(soVal, 10);
        } else if (scope === 'subscriber') {
          var subVal = subSel ? subSel.value : '';
          if (!subVal) {
            if (errEl) { errEl.textContent = 'Please pick a subscriber.'; errEl.hidden = false; }
            return;
          }
          body.subscriber_user_id = parseInt(subVal, 10);
        }

        saveBtn.disabled = true;
        saveBtn.textContent = 'Saving…';
        var BACKEND = 'https://wv-valet-backend.onrender.com';
        fetch(BACKEND + '/api/v1/met/recurring-shifts', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify(body)
        })
          .then(function(r) { return r.json(); })
          .then(function(res) {
            saveBtn.disabled = false;
            saveBtn.textContent = 'Add shift';
            if (!res || !res.ok) {
              if (errEl) { errEl.textContent = (res && (res.message || res.error)) || 'Could not save.'; errEl.hidden = false; }
              return;
            }
            closeAddShift();
            loadMetSchedule();
          })
          .catch(function() {
            saveBtn.disabled = false;
            saveBtn.textContent = 'Add shift';
            if (errEl) { errEl.textContent = 'Network error.'; errEl.hidden = false; }
          });
      });
    }
  }


  var MET_HISTORY_MOCK = [
    { customer: 'Patel Construction',     plan: 'Concrete pour 7 AM',         when: 'Today 11:42',     verdict: 'clear',   amount: '$12.32', helpful: 'up' },
    { customer: 'Henderson Wedding',      plan: 'Outdoor reception 6 PM',     when: 'Today 10:18',     verdict: 'caution', amount: '$12.32', helpful: 'up' },
    { customer: 'Mid-South Asphalt',      plan: 'Paving crew morning',        when: 'Today 08:55',     verdict: 'clear',   amount: '$12.32', helpful: 'pending' },
    { customer: 'Carmichael Tree Service',plan: 'Tree removal afternoon',     when: 'Yesterday 14:14', verdict: 'risk',    amount: '$12.32', helpful: 'up' },
    { customer: 'Fishers Outdoor Wedding',plan: 'Garden ceremony 4 PM',       when: 'Yesterday 11:02', verdict: 'clear',   amount: '$12.32', helpful: 'up' },
    { customer: 'Boone County Roofing',   plan: 'Roofing tear-off 7 AM',      when: 'Yesterday 09:15', verdict: 'caution', amount: '$12.32', helpful: 'down' },
    { customer: 'Lebanon Public Works',   plan: 'Storm drain inspection',     when: 'Thu 15:33',       verdict: 'clear',   amount: '$12.32', helpful: 'up' },
    { customer: 'Westfield Athletics',    plan: 'Football practice 4 PM',     when: 'Thu 11:08',       verdict: 'caution', amount: '$12.32', helpful: 'up' },
    { customer: 'Carmel Concrete',        plan: 'Foundation pour 8 AM',       when: 'Wed 13:47',       verdict: 'clear',   amount: '$12.32', helpful: 'up' },
    { customer: 'Indy Outdoor Films',     plan: 'Drone shoot golden hour',    when: 'Wed 10:22',       verdict: 'caution', amount: '$12.32', helpful: 'up' }
  ];

  var ADMIN_QUEUE_MOCK = [
    { tier: 'pro', tierLabel: 'Pro', plan: 'Concrete pour 9 AM Sat \u2014 Bell Concrete', met: 'unclaimed', status: 'pending' },
    { tier: 'single', tierLabel: 'Single', plan: 'Outdoor wedding 4 PM \u2014 R. Greene', met: 'unclaimed', status: 'pending' },
    { tier: 'day', tierLabel: 'Day Pass', plan: 'Roofing job tomorrow \u2014 Westfield', met: 'unclaimed', status: 'pending' },
    { tier: 'single', tierLabel: 'Single', plan: 'Bike ride 5 PM \u2014 J. Holloway', met: 'M. Reynolds', status: 'claimed' },
    { tier: 'single', tierLabel: 'Single', plan: 'Concrete pour 7 AM \u2014 Patel', met: 'M. Reynolds', status: 'delivered' },
    { tier: 'single', tierLabel: 'Single', plan: 'Garden ceremony \u2014 Henderson', met: 'M. Torres', status: 'delivered' }
  ];

  var ADMIN_ACTIVITY_MOCK = [
    { time: '12:08', kind: 'payment',  text: '<strong>$19</strong> single review <span class="admin-activity-meta">\u2014 Bell Concrete</span>' },
    { time: '11:42', kind: 'brief',    text: 'Brief delivered <span class="admin-activity-meta">by M. Reynolds \u2014 Patel Construction</span>' },
    { time: '11:38', kind: 'signup',   text: 'New Crew member <span class="admin-activity-meta">@kc_thomas \u2014 Lebanon, IN</span>' },
    { time: '11:21', kind: 'report',   text: 'Storm report verified <span class="admin-activity-meta">\u00d73 \u2014 Indianapolis</span>' },
    { time: '11:14', kind: 'payment',  text: '<strong>$49</strong> day pass <span class="admin-activity-meta">\u2014 Westfield Roofing</span>' },
    { time: '10:55', kind: 'brief',    text: 'Brief delivered <span class="admin-activity-meta">by M. Reynolds \u2014 Mid-South Asphalt</span>' },
    { time: '10:18', kind: 'brief',    text: 'Brief delivered <span class="admin-activity-meta">by M. Torres \u2014 Henderson Wedding</span>' },
    { time: '09:47', kind: 'signup',   text: 'New Crew member <span class="admin-activity-meta">@bell_concrete \u2014 Lebanon, IN</span>' }
  ];

  function urgencyLabel(minutesAgo) {
    var sla = 30;
    var remaining = sla - minutesAgo;
    if (remaining <= 5) return { text: remaining + ' min left', urgent: true };
    if (remaining <= 0) return { text: 'OVER SLA', urgent: true };
    return { text: minutesAgo + ' min ago', urgent: false };
  }

  // Render the mock review queue into the Met view's queue list. Each
  // review is a card with tier, plan, area, AI suggestion, and a
  // "Claim review" button that pops open the composer with the customer
  // pre-filled.
  function renderMetQueue() {
    var list = $('met-queue-list');
    if (!list) return;
    var html = '';
    // Real backend-fetched requests only. Mock fallback removed — first-time
    // Mets see a clean empty state instead of demo data.
    var combined = MET_REAL_QUEUE;
    var countEl = $('met-queue-count');
    if (countEl) {
      countEl.textContent = combined.length;
      countEl.hidden = combined.length === 0;
    }
    if (combined.length === 0) {
      list.innerHTML = '<div style="padding:40px 20px;text-align:center;color:rgba(15,17,22,0.55);font-size:13.5px;line-height:1.6;">'
        + '<div style="font-weight:500;color:#0E1116;margin-bottom:6px;">No reviews to claim</div>'
        + 'When a customer pays $19 for a verification, their request lands here. You\u2019ll get an SMS notification too.'
        + '</div>';
      return;
    }
    for (var i = 0; i < combined.length; i++) {
      var rev = combined[i];
      var urgency = urgencyLabel(rev.paidMinutesAgo);
      var verdictLabel = rev.aiVerdict.charAt(0).toUpperCase() + rev.aiVerdict.slice(1);
      var realMarker = rev.isReal
        ? '<span class="ws-queue-real-badge" title="Real paid customer">\u2022 LIVE</span>'
        : '';
      html += '<div class="ws-queue-row' + (rev.isReal ? ' is-real' : '') + '" data-review-id="' + rev.id + '">'
        +   '<span class="ws-queue-tier tier-' + rev.tier + '">' + escapeHtml(rev.tierLabel) + realMarker + '</span>'
        +   '<div class="ws-queue-plan">'
        +     '<p class="ws-queue-plan-headline">' + escapeHtml(rev.plan) + '</p>'
        +     '<p class="ws-queue-plan-meta">' + escapeHtml(rev.customerName) + ' \u00b7 ' + escapeHtml(rev.area) + '</p>'
        +   '</div>'
        +   '<div class="ws-queue-ai">'
        +     '<span>AI:</span>'
        +     '<span class="ws-queue-ai-pill verdict-' + rev.aiVerdict + '">' + verdictLabel + '</span>'
        +     '<span style="color:rgba(15,17,22,0.40)">'
        +       confidenceBand(rev.aiConfidence).label + ' \u00b7 ' + rev.aiConfidence + '%'
        +     '</span>'
        +   '</div>'
        +   '<span class="ws-queue-age">' + escapeHtml(urgency.text) + '</span>'
        +   '<div class="ws-queue-actions">'
        +     '<button type="button" class="ws-queue-skip-btn" data-skip-review="' + rev.id + '">Skip</button>'
        +     '<button type="button" class="ws-queue-claim-btn" data-claim-review="' + rev.id + '">Claim</button>'
        +   '</div>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  function renderMetHistory() {
    var list = $('met-history-list');
    if (!list) return;

    // Show loading state immediately so old mock content doesn't flash
    list.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Loading\u2026</div>';

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/me/met-history', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          list.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">'
            + (res.status === 403 ? 'You must be a Met to view history.' : 'Could not load history.')
            + '</div>';
          return;
        }
        var reviews = res.body.reviews || [];
        if (reviews.length === 0) {
          list.innerHTML = '<div style="padding:40px 20px;text-align:center;color:rgba(15,17,22,0.60);font-size:13.5px;line-height:1.6;">'
            + '<div style="font-weight:600;color:#0E1116;margin-bottom:6px;">No reviews yet</div>'
            + 'When you complete a $19 verification, it shows here with verdict, customer, and earnings.'
            + '</div>';
          return;
        }
        var html = '';
        for (var i = 0; i < reviews.length; i++) {
          var h = reviews[i];
          var verdictClass = h.verdict_class || 'clear';
          var verdictLabel = verdictClass.charAt(0).toUpperCase() + verdictClass.slice(1);
          var whenStr = _metTipFmtWhen(h.completed_at);
          var amount = '$' + ((h.amount_cents || 0) / 100).toFixed(2);
          var customerLabel = h.customer_email || h.customer_phone || '(anonymous)';

          var msgBtn = '<button type="button" class="ws-history-message-btn" '
                     + 'data-message-customer="' + escapeHtml(customerLabel) + '" '
                     + 'data-message-plan="' + escapeHtml(h.plan_text || '') + '" '
                     + 'data-message-when="' + escapeHtml(whenStr) + '" '
                     + 'data-message-request-id="' + h.request_id + '" '
                     + 'data-message-phone="' + escapeHtml(h.customer_phone || '') + '">'
                     + '<svg viewBox="0 0 24 24" width="11" height="11" fill="none" '
                     + 'stroke="currentColor" stroke-width="2.4" stroke-linecap="round" '
                     + 'stroke-linejoin="round" aria-hidden="true">'
                     + '<path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/>'
                     + '</svg>'
                     + 'Message again'
                     + '</button>';
          html += '<div class="ws-history-row">'
            +   '<span class="ws-history-when">' + escapeHtml(whenStr) + '</span>'
            +   '<span class="ws-history-customer">' + escapeHtml(customerLabel) + '</span>'
            +   '<span class="ws-history-plan">' + escapeHtml(h.plan_text || '\u2014') + '</span>'
            +   '<span class="ws-history-verdict verdict-' + verdictClass + '">' + verdictLabel + '</span>'
            +   '<span class="ws-history-helpful is-pending">\u2014</span>'
            +   '<span class="ws-history-amount">' + amount + '</span>'
            +   '<span class="ws-history-action">' + msgBtn + '</span>'
            + '</div>';
        }
        list.innerHTML = html;
      })
      .catch(function() {
        list.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(15,17,22,0.50);font-size:13px;">Network error.</div>';
      });
  }

  // ── Message Again modal (Phase 7) ───────────────────────────────
  // Opens from a "Message again" button in the Met history table.
  // Lets the Met send a follow-up SMS to the original customer.
  function setupMetMessageAgain() {
    if (window._metMessageAgainWired) return;
    window._metMessageAgainWired = true;

    var modal     = $('met-message-modal');
    var scrim     = $('met-message-modal-scrim');
    var closeBtn  = $('met-message-modal-close');
    var cancelBtn = $('met-message-modal-cancel');
    var sendBtn   = $('met-message-modal-send');
    var bodyEl    = $('met-message-body');
    var countEl   = $('met-message-count-num');
    var customerEl = $('met-message-context-customer');
    var planEl    = $('met-message-context-plan');
    var whenEl    = $('met-message-context-when');

    if (!modal || !sendBtn || !bodyEl) return;

    var modalState = {
      requestId: null,
      customerPhone: null,
      customerLabel: null
    };

    function openModal(btn) {
      modalState.requestId    = btn.getAttribute('data-message-request-id') || null;
      modalState.customerPhone = btn.getAttribute('data-message-phone') || null;
      modalState.customerLabel = btn.getAttribute('data-message-customer') || null;
      if (customerEl) customerEl.textContent = modalState.customerLabel || 'Customer';
      if (planEl) planEl.textContent = btn.getAttribute('data-message-plan') || '\u2014';
      if (whenEl) whenEl.textContent = btn.getAttribute('data-message-when') || '\u2014';
      bodyEl.value = '';
      if (countEl) countEl.textContent = '0';
      sendBtn.disabled = true;
      modal.hidden = false;
      setTimeout(function() { bodyEl.focus(); }, 60);
    }

    function closeModal() {
      modal.hidden = true;
      bodyEl.value = '';
      sendBtn.disabled = true;
    }

    // Delegated click handler — buttons get added to history rows dynamically
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-message-customer]');
      if (!btn) return;
      ev.preventDefault();
      openModal(btn);
    });

    if (closeBtn) closeBtn.addEventListener('click', closeModal);
    if (cancelBtn) cancelBtn.addEventListener('click', closeModal);
    if (scrim) scrim.addEventListener('click', closeModal);

    bodyEl.addEventListener('input', function() {
      var n = bodyEl.value.trim().length;
      if (countEl) countEl.textContent = String(bodyEl.value.length);
      sendBtn.disabled = (n === 0);
    });

    sendBtn.addEventListener('click', function() {
      var text = bodyEl.value.trim();
      if (!text) return;
      sendBtn.disabled = true;
      sendBtn.textContent = 'Sending\u2026';

      var payload = {
        body: text,
        customer_label: modalState.customerLabel
      };
      if (modalState.requestId) {
        var n = parseInt(modalState.requestId, 10);
        if (!isNaN(n)) payload.request_id = n;
      }
      if (modalState.customerPhone) payload.customer_phone = modalState.customerPhone;

      fetch('https://wv-valet-backend.onrender.com/api/v1/met/messages', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify(payload)
      })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (res.body && res.body.ok) {
          var status = res.body.delivery_status;
          var msg;
          if (status === 'sent')        msg = 'Message sent to ' + (modalState.customerLabel || 'customer') + '.';
          else if (status === 'stubbed') msg = 'Logged (no phone on file \u2014 demo mode).';
          else if (status === 'failed')  msg = 'Twilio reported a delivery error. Check the dashboard.';
          else                           msg = 'Message recorded.';
          if (typeof showTipToast === 'function') showTipToast(msg);
          closeModal();
        } else {
          var err = (res.body && res.body.error) || ('http-' + res.status);
          alert('Couldn\u2019t send message: ' + err);
          sendBtn.disabled = false;
          sendBtn.textContent = 'Send message';
        }
      })
      .catch(function(err) {
        console.warn('[met-message] send error:', err);
        alert('Network error \u2014 message not sent. Please try again.');
        sendBtn.disabled = false;
        sendBtn.textContent = 'Send message';
      });
    });
  }

  // ════════════════════════════════════════════════════════════════════
  // Mission Tab (Phase 8 — May 14)
  // ════════════════════════════════════════════════════════════════════
  //
  // Catalog of mission templates a Met can deploy to Crew. Each template
  // has display data (icon, name, category) and a default prompt the Met
  // can edit before firing.
  //
  // Persistence: in-memory only for this chunk. The actual fire-mission
  // call is stubbed (records to MISSION_DEPLOYMENTS array, fires success
  // toast). Backend persistence + real Crew SMS notification is a future
  // chunk, mirroring the pattern from Phase 6 / 7.
  //
  // Note: "deployment counts" in the catalog (3 fired today, 7 fired today,
  // etc.) are mock numbers showing the UX. When backend persistence ships,
  // these will become real aggregates from a mission_deployments table.

  var MISSION_TEMPLATES = [
    {
      id: 'flag-check',
      initials: 'FC',
      name: 'Flag check (wind)',
      category: 'Sensor gap',
      cadence: 'routine',
      responseTarget: '15 second response',
      defaultPrompt: 'Quick wind check — is the flag at your location standing out, fluttering, or limp? Reply with one word.',
      iconGradient: 'linear-gradient(135deg, #4169E1, #2E4FCC)',
      severe: false,
      firedToday: 3
    },
    {
      id: 'visibility-check',
      initials: 'VC',
      name: 'Visibility check',
      category: 'Sensor gap',
      cadence: 'routine',
      responseTarget: '30 second response',
      defaultPrompt: 'Visibility check — can you see the horizon clearly, or is there haze/fog/precip in the air?',
      iconGradient: 'linear-gradient(135deg, #B5A0FF, #7E5BEF)',
      severe: false,
      firedToday: 7
    },
    {
      id: 'venue-check',
      initials: 'PV',
      name: 'Pre-event venue check',
      category: 'Decision-support',
      cadence: 'manual deploy',
      responseTarget: '2 minute response',
      defaultPrompt: 'Pre-event check — describe current conditions at the venue: wind, sky cover, anything notable.',
      iconGradient: 'linear-gradient(135deg, #FFB084, #E55B3A)',
      severe: false,
      firedToday: 0
    },
    {
      id: 'severe-recon',
      initials: 'SR',
      name: 'Severe weather recon',
      category: 'Decision-support',
      cadence: 'severe mode',
      responseTarget: 'requires admin approval to deploy',
      defaultPrompt: 'Severe weather recon — confirm cell location, intensity, and any hail, gust front, or rotation. Stay safe.',
      iconGradient: 'linear-gradient(135deg, #FF7676, #D43838)',
      severe: true,
      firedToday: 2,
      firedDetail: 'fired this month'
    }
  ];

  // In-memory ledger of fired missions for the current session.
  // When backend persistence ships, this becomes a fetch from a
  // mission_deployments table.
  if (!window.MISSION_DEPLOYMENTS) window.MISSION_DEPLOYMENTS = [];

  function renderMissionTemplates() {
    var list = $('mission-template-list');
    if (!list) return;
    var html = '';
    for (var i = 0; i < MISSION_TEMPLATES.length; i++) {
      var m = MISSION_TEMPLATES[i];
      var cls = 'mission-template-card' + (m.severe ? ' is-severe' : '');
      var detail = m.firedDetail || 'fired today';
      var metaText = escapeHtml(m.category) + ' \u00b7 ' + escapeHtml(m.cadence) + ' \u00b7 ';
      if (m.severe) {
        metaText += '<strong>severe mode</strong> \u00b7 ' + escapeHtml(m.responseTarget);
      } else {
        metaText += escapeHtml(m.responseTarget);
      }
      html += '<div class="' + cls + '">'
        +   '<div class="mission-template-icon" style="background: ' + m.iconGradient + ';">'
        +     escapeHtml(m.initials)
        +   '</div>'
        +   '<div class="mission-template-info">'
        +     '<p class="mission-template-name">' + escapeHtml(m.name) + '</p>'
        +     '<p class="mission-template-meta">' + metaText + '</p>'
        +   '</div>'
        +   '<span class="mission-template-count">' + m.firedToday + ' ' + detail + '</span>'
        +   '<button type="button" class="mission-template-deploy" data-deploy-mission="' + m.id + '">Deploy \u2192</button>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  function getMissionTemplate(id) {
    for (var i = 0; i < MISSION_TEMPLATES.length; i++) {
      if (MISSION_TEMPLATES[i].id === id) return MISSION_TEMPLATES[i];
    }
    return null;
  }

  // ─── Mission deployments — backend list rendering (Phase 10 Item #4) ──
  // Fetches the Met's recent deployments and renders them in the "Last 5
  // deployments" tile on the Missions tab. Called on Mission tab entry
  // and after each new deploy (so the new row appears immediately).
  function _missionFiredAtLabel(ms) {
    if (!ms) return '\u2014';
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '\u2014';
    var now = new Date();
    var today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
    var that = new Date(d.getFullYear(), d.getMonth(), d.getDate());
    var diffDays = Math.round((today - that) / (1000 * 60 * 60 * 24));
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    var timeStr = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    if (diffDays === 0) return timeStr + ' today';
    if (diffDays === 1) return 'Yesterday ' + timeStr;
    if (diffDays < 7) return ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'][d.getDay()] + ' ' + timeStr;
    return (d.getMonth() + 1) + '/' + d.getDate() + ' ' + timeStr;
  }

  function _missionStatusBadge(dep) {
    // Returns the right side "Cited" column content based on status + counts
    if (dep.status === 'pending-approval') {
      return '<span style="background: rgba(255, 184, 0, 0.15); color: #FFCC4D; padding: 3px 8px; border-radius: 4px; font-size: 11px; font-weight: 600;">Pending</span>';
    }
    if (dep.status === 'cancelled') {
      return '<span style="background: rgba(15, 17, 22, 0.20); color: rgba(255,255,255,0.55); padding: 3px 8px; border-radius: 4px; font-size: 11px; font-weight: 600;">Cancelled</span>';
    }
    var cited = dep.crew_cited || 0;
    if (cited > 0) {
      return '<span style="background: rgba(65, 200, 130, 0.15); color: #5DECA8; padding: 3px 8px; border-radius: 4px; font-size: 11px; font-weight: 600;">' + cited + ' cited</span>';
    }
    return '<span style="background: rgba(255, 184, 0, 0.15); color: #FFCC4D; padding: 3px 8px; border-radius: 4px; font-size: 11px; font-weight: 600;">0 cited</span>';
  }

  function renderMetDeployments() {
    var container = $('met-deployments-list');
    if (!container) return;

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/missions/deployments', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          // 401 / 403 — show neutral empty state (user might not be signed in
          // as a Met yet, e.g. demo via dev menu).
          container.innerHTML = '<div style="padding: 18px 0; font-size: 12px; '
            + 'color: rgba(255,255,255,0.55); text-align: center;">'
            + 'No deployments yet \u2014 deploy your first mission above.</div>';
          return;
        }
        var deployments = res.body.deployments || [];
        if (deployments.length === 0) {
          container.innerHTML = '<div style="padding: 18px 0; font-size: 12px; '
            + 'color: rgba(255,255,255,0.55); text-align: center;">'
            + 'No deployments yet \u2014 deploy your first mission above.</div>';
          return;
        }
        // Render up to 5 most recent
        var rows = deployments.slice(0, 5);
        var html = '';
        for (var i = 0; i < rows.length; i++) {
          var d = rows[i];
          var firedLabel = _missionFiredAtLabel(d.fired_at);
          var responded = (d.crew_responded || 0) + ' of ' + (d.audience_estimate || 0);
          var border = (i < rows.length - 1)
            ? 'border-bottom: 1px solid rgba(255,255,255,0.06);'
            : '';
          html += '<div style="display: grid; grid-template-columns: 1fr 1.5fr 1fr 1fr auto; '
            + 'gap: 16px; padding: 12px 0; ' + border
            + ' font-size: 12px; color: rgba(255,255,255,0.85); align-items: center;">'
            +   '<div>' + escapeHtml(d.template_name) + '</div>'
            +   '<div>' + escapeHtml((d.polygon_label || 'Coverage area') + ' \u00b7 ' + (d.audience_estimate || 0) + ' Crew') + '</div>'
            +   '<div style="color: rgba(255,255,255,0.55);">' + escapeHtml(firedLabel) + '</div>'
            +   '<div>' + escapeHtml(responded) + '</div>'
            +   '<div>' + _missionStatusBadge(d) + '</div>'
            + '</div>';
        }
        container.innerHTML = html;
      })
      .catch(function(err) {
        container.innerHTML = '<div style="padding: 18px 0; font-size: 12px; '
          + 'color: rgba(255,255,255,0.55); text-align: center;">'
          + 'Couldn\u2019t load deployments.</div>';
        console.warn('[deployments] load failed:', err);
      });
  }

  function setupMissionDeploy() {
    if (window._missionDeployWired) return;
    window._missionDeployWired = true;

    var modal     = $('mission-deploy-modal');
    var scrim     = $('mission-deploy-scrim');
    var closeBtn  = $('mission-deploy-close');
    var cancelBtn = $('mission-deploy-cancel');
    var fireBtn   = $('mission-deploy-fire');
    var promptEl  = $('mission-deploy-prompt');
    var countEl   = $('mission-deploy-prompt-count');
    var summaryName = $('mission-deploy-summary-name');
    var summaryMeta = $('mission-deploy-summary-meta');
    var summaryIcon = $('mission-deploy-summary-icon');
    var warningEl   = $('mission-deploy-warning');
    var audienceEl  = $('mission-deploy-audience-value');
    var polygonHidden = $('mission-deploy-polygon-geojson');

    if (!modal || !fireBtn || !promptEl) return;

    var modalState = { templateId: null };

    function openModal(templateId) {
      var t = getMissionTemplate(templateId);
      if (!t) return;
      modalState.templateId = templateId;

      if (summaryIcon) {
        summaryIcon.textContent = t.initials;
        summaryIcon.style.background = t.iconGradient;
      }
      if (summaryName) summaryName.textContent = t.name;
      if (summaryMeta) {
        summaryMeta.textContent = t.category + ' \u00b7 ' + t.cadence + ' \u00b7 ' + t.responseTarget;
      }
      if (warningEl) warningEl.hidden = !t.severe;
      promptEl.value = t.defaultPrompt;
      if (countEl) countEl.textContent = String(t.defaultPrompt.length);
      fireBtn.disabled = (t.defaultPrompt.trim().length === 0);
      fireBtn.textContent = t.severe ? 'Submit for approval' : 'Fire mission';

      // Reset polygon
      if (polygonHidden) polygonHidden.value = '';
      var status = $('mission-deploy-polygon-status');
      if (status) {
        status.textContent = 'No target area drawn \u00b7 will send to all Crew in coverage.';
        status.classList.remove('is-active');
      }
      if (audienceEl) audienceEl.textContent = '\u2014 Crew in coverage area';

      modal.hidden = false;
      // Init the polygon map on first open (idempotent)
      setTimeout(function() {
        if (typeof initPolygonMap === 'function') {
          initPolygonMap({
            containerId:    'mission-deploy-polygon-map',
            loadingSelector:'[data-polygon-loading="mission"]',
            hiddenInputId:  'mission-deploy-polygon-geojson',
            statusElId:     'mission-deploy-polygon-status',
            initialCenter:  [39.8, -86.2],
            initialZoom:    7
          });
        }
        promptEl.focus();
      }, 80);
    }

    function closeModal() {
      modal.hidden = true;
      modalState.templateId = null;
      fireBtn.textContent = 'Fire mission';
      fireBtn.disabled = true;
    }

    // Delegated click for any Deploy button on a mission card
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-deploy-mission]');
      if (!btn) return;
      ev.preventDefault();
      var id = btn.getAttribute('data-deploy-mission');
      openModal(id);
    });

    if (closeBtn) closeBtn.addEventListener('click', closeModal);
    if (cancelBtn) cancelBtn.addEventListener('click', closeModal);
    if (scrim) scrim.addEventListener('click', closeModal);

    promptEl.addEventListener('input', function() {
      var n = promptEl.value.length;
      if (countEl) countEl.textContent = String(n);
      fireBtn.disabled = (promptEl.value.trim().length === 0);
    });

    // When a polygon is drawn, update the audience-estimate text.
    // We don't have real Crew lat/lng data on the frontend, so the
    // estimate is a friendly placeholder. When backend persistence
    // arrives, this will become a real Crew-in-polygon query.
    if (polygonHidden) {
      // The polygon module updates the hidden input directly. Watch for
      // changes by polling — simpler than wiring a MutationObserver and
      // good enough for human-pace interactions.
      var lastValue = polygonHidden.value;
      setInterval(function() {
        if (modal.hidden) return;
        if (polygonHidden.value === lastValue) return;
        lastValue = polygonHidden.value;
        if (lastValue && audienceEl) {
          audienceEl.textContent = 'Estimated 3-6 Crew inside the target area';
        } else if (audienceEl) {
          audienceEl.textContent = '\u2014 Crew in coverage area';
        }
      }, 600);
    }

    fireBtn.addEventListener('click', function() {
      var t = getMissionTemplate(modalState.templateId);
      if (!t) return;
      var prompt = promptEl.value.trim();
      if (!prompt) return;

      fireBtn.disabled = true;
      fireBtn.textContent = t.severe ? 'Submitting\u2026' : 'Firing\u2026';

      // Phase 10 Item #4: POST to backend instead of in-memory.
      // The polygon hidden input holds GeoJSON; pass through verbatim.
      // audience_estimate is parsed from the estimate text rendered in
      // the modal — quick regex grab for the number; defaults to 0.
      var audienceText = audienceEl ? audienceEl.textContent : '';
      var audienceMatch = audienceText.match(/(\d+)/);
      var audienceEstimate = audienceMatch ? parseInt(audienceMatch[1], 10) : 0;

      // Polygon label: best-effort from polygon presence + coverage area.
      // Without a reverse-geocoder, we just say "Targeted area" vs "Coverage area".
      var polygonValue = polygonHidden ? polygonHidden.value : '';
      var polygonLabel = polygonValue ? 'Targeted area' : 'Coverage area';

      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/missions/deployments', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify({
          template_id: t.id,
          template_name: t.name,
          prompt: prompt,
          polygon_geojson: polygonValue || null,
          polygon_label: polygonLabel,
          is_severe: !!t.severe,
          audience_estimate: audienceEstimate
        })
      })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          if (res.body && res.body.ok) {
            // Bump the template's fired-today count (UI feedback)
            t.firedToday = (t.firedToday || 0) + 1;
            renderMissionTemplates();

            // Success state + refresh deployment list
            var toastMsg;
            if (t.severe) {
              toastMsg = 'Submitted for admin approval \u2014 you\u2019ll be notified when it fires.';
            } else if (polygonValue) {
              toastMsg = 'Mission fired to Crew inside your target area.';
            } else {
              toastMsg = 'Mission fired to all Crew in your coverage area.';
            }
            if (typeof showTipToast === 'function') showTipToast(toastMsg);

            // Refresh the deployments list to show the new row
            if (typeof renderMetDeployments === 'function') renderMetDeployments();
            closeModal();
          } else {
            fireBtn.disabled = false;
            fireBtn.textContent = t.severe ? 'Submit for approval' : 'Fire mission';
            var err = (res.body && res.body.error) ? res.body.error : 'unknown';
            if (typeof showTipToast === 'function') {
              showTipToast('Couldn\u2019t fire mission (' + err + '). Try again.');
            }
          }
        })
        .catch(function() {
          fireBtn.disabled = false;
          fireBtn.textContent = t.severe ? 'Submit for approval' : 'Fire mission';
          if (typeof showTipToast === 'function') {
            showTipToast('Network error. Try again.');
          }
        });
    });

    // Browse catalog placeholder — just toast for now
    var browseBtn = $('mission-catalog-browse-btn');
    if (browseBtn) {
      browseBtn.addEventListener('click', function() {
        if (typeof showTipToast === 'function') {
          showTipToast('Full catalog browser coming soon \u2014 these 4 templates are the most-used.');
        }
      });
    }
  }


  function renderAdminQueue() {
    // Queue is now hardcoded in the command-center HTML (cmd-queue-list).
    // When we wire real data from the backend later, this function will
    // populate it dynamically. For now: no-op so initial render doesn't
    // throw when the old element ID is missing.
    return;
  }

  function renderAdminActivity() {
    // Activity feed lives at #cmd-activity-list in the command-center
    // markup. Populate from the same mock data we used in the old view
    // but render with the dark-theme cmd-activity-row classes.
    var list = $('cmd-activity-list');
    if (!list) return;
    var ICONS = {
      payment: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>',
      signup:  '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 11h-6"/><path d="M19 8v6"/></svg>',
      brief:   '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><polyline points="9 13 11 15 15 11"/></svg>',
      report:  '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>'
    };
    var html = '';
    for (var i = 0; i < ADMIN_ACTIVITY_MOCK.length; i++) {
      var a = ADMIN_ACTIVITY_MOCK[i];
      html += '<div class="cmd-activity-row">'
        +   '<span class="cmd-activity-time">' + a.time + '</span>'
        +   '<span class="cmd-activity-icon kind-' + a.kind + '">' + (ICONS[a.kind] || '') + '</span>'
        +   '<span class="cmd-activity-text">' + a.text + '</span>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  /**
   * Render the 24-hour shift coverage bars. Each bar represents one hour;
   * its color encodes how many Mets are scheduled (3 green, 2 blue,
   * 1 yellow, 0 red). The mock data here intentionally shows a thin
   * patch overnight so the warning callout below feels grounded.
   */
  function renderCmdCoverage() {
    var bars = $('cmd-coverage-bars');
    if (!bars) return;
    // Mock shift staffing: index 0 = current hour, 23 = +24h
    // Three Mets day shift, two Mets evening, one Met overnight.
    var staffing = [3, 3, 3, 3, 3, 3,    // next 6h: full coverage
                    2, 2, 2, 2, 2, 2,    // 6-12h: two on
                    1, 1, 1, 0,          // 12-16h: thin overnight, one zero hour
                    1, 1, 2, 2, 3, 3, 3, 3]; // 16-24h: ramp back up
    var html = '';
    for (var i = 0; i < 24; i++) {
      html += '<div class="cmd-coverage-cell staff-' + staffing[i] + '"></div>';
    }
    bars.innerHTML = html;
  }

  /**
   * Render the search activity heatmap. A 12x4 = 48-cell grid where
   * intensity reflects search volume by region. Hardcoded mock pattern
   * for now; when we wire real data, this will pull from the backend.
   * The pattern roughly mimics central-Indiana clustering with a few
   * outlying hotspots.
   */
  function renderCmdHeatmap() {
    var container = $('cmd-heatmap');
    if (!container) return;
    // Pattern: numbers 0-4 representing heat intensity per cell
    var pattern = [
      0,0,1,1,2,2,3,2,1,1,0,0,
      0,1,2,3,4,4,3,2,1,1,0,0,
      0,1,2,3,3,2,2,1,2,1,0,0,
      0,0,1,1,2,1,1,2,1,0,0,0
    ];
    var html = '';
    for (var i = 0; i < pattern.length; i++) {
      var heat = pattern[i];
      var cls = heat === 0 ? '' : ' heat-' + heat;
      html += '<div class="cmd-heatmap-cell' + cls + '"></div>';
    }
    container.innerHTML = html;
  }

  /**
   * Wire the Force Alert button. Currently a confirmation modal stub
   * that simulates the alert flow. When Twilio is wired in, this will
   * fire SMS to every active Met's phone with the alert payload.
   */
  function setupCmdForceAlert() {
    var btn = $('cmd-force-alert');
    if (!btn) return;
    btn.addEventListener('click', function() {
      var ok = window.confirm(
        'Force Alert\n\n' +
        'This will SMS every active Met with high priority. ' +
        'Use only for severe weather the system has not flagged.\n\n' +
        'Are you sure?\n\n' +
        '(In the deployed version, this fires Twilio. ' +
        'Right now it\u2019s a stub for demonstration.)'
      );
      if (!ok) return;
      // Visual feedback — flash the button to confirm the action
      var originalBg = btn.style.background;
      btn.style.background = 'linear-gradient(135deg, #16C77E, #5DECA8)';
      btn.querySelector('span').textContent = 'Sent to 2 Mets';
      setTimeout(function() {
        btn.style.background = originalBg;
        btn.querySelector('span').textContent = 'Force Alert';
      }, 2400);
    });
  }

  /**
   * Set the personalized greeting at the top of the Admin view.
   *
   * Two adaptive parts:
   *   1. The greeting itself ("Good morning/afternoon/evening, Michael.")
   *      shifts with the local time of day.
   *   2. The subtitle gives a quick orientation for the moment:
   *      weekend mode points at the weekend so far; weekday morning
   *      points at the day ahead; late-night points at quietness.
   *
   * This isn't deeply data-driven yet — it's a humanizing touch. When
   * we wire real metrics into the panels below, we can make the
   * subtitle reference actual numbers (e.g. "Two reviews waiting since
   * 3 AM"). For now: warmth, not analytics.
   */
  function setAdminWelcome() {
    // The command-center header has three meta items: Watch (e.g.
    // "Saturday afternoon"), Local time (e.g. "14:47 ET"), and the
    // Live indicator. We update Watch and Local on each entry so the
    // values match the moment the operator actually opened the view.
    var watchEl = $('cmd-header-watch');
    var timeEl = $('cmd-header-time');
    var titleEl = $('cmd-header-title');

    // Personalize title with the real user's first name if available.
    if (titleEl) {
      try {
        var u = window.__wvAuth && window.__wvAuth.getCurrentUser && window.__wvAuth.getCurrentUser();
        if (u && u.name) {
          var firstName = String(u.name).trim().split(/\s+/)[0];
          if (firstName) titleEl.textContent = 'Operator: ' + firstName;
        } else if (u && u.email) {
          titleEl.textContent = 'Operator: ' + String(u.email).split('@')[0];
        }
      } catch (e) {}
    }

    if (!watchEl && !timeEl) return;

    var now = new Date();
    var hour = now.getHours();
    var dayOfWeek = now.getDay();
    var dayNames = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];

    // "Watch" is a friendly label for the current operating window —
    // the kind of phrasing a Met would use, not a calendar timestamp.
    var partOfDay;
    if (hour < 5)        partOfDay = 'overnight';
    else if (hour < 12)  partOfDay = 'morning';
    else if (hour < 17)  partOfDay = 'afternoon';
    else if (hour < 21)  partOfDay = 'evening';
    else                 partOfDay = 'late evening';
    if (watchEl) watchEl.textContent = dayNames[dayOfWeek] + ' ' + partOfDay;

    // Local time, formatted with leading zeros and a TZ stub. The TZ
    // here is hardcoded to ET because that's where Michael is and where
    // the business operates; once we have a real settings layer we'll
    // pull this from a stored preference.
    if (timeEl) {
      var hh = String(hour).padStart(2, '0');
      var mm = String(now.getMinutes()).padStart(2, '0');
      timeEl.textContent = hh + ':' + mm + ' ET';
    }
  }

  // Open the Met composer with a specific review pre-filled. In the new
  // workspace layout, the composer slides in as a full-width tile but
  // doesn't hide the team panel or queue — peripheral awareness stays
  // intact while the Met is writing. The context panel on the right
  // shows the customer's plan, AI suggestion, weather data, and rule
  // reasons all at once so the Met doesn't have to scroll between
  // concerns while composing.
  function openMetComposer(reviewId) {
    var rev = null;
    // Item #2: check real queue first, then mock
    for (var k = 0; k < MET_REAL_QUEUE.length; k++) {
      if (MET_REAL_QUEUE[k].id === reviewId) { rev = MET_REAL_QUEUE[k]; break; }
    }
    if (!rev) {
      for (var i = 0; i < MET_QUEUE_MOCK.length; i++) {
        if (MET_QUEUE_MOCK[i].id === reviewId) { rev = MET_QUEUE_MOCK[i]; break; }
      }
    }
    if (!rev) return;

    // ── Header strip: which customer's review we're working on ──
    var customerEl = $('met-composer-customer');
    if (customerEl) customerEl.textContent = rev.plan;

    // ── Pre-set the verdict to match the AI's suggestion ────────
    // This is a starting point, not a constraint; the Met can change it.
    // Scope the selector to the composer specifically — the Daily Brief
    // tab also has data-verdict buttons that we don't want to touch.
    var composerEl = $('met-composer');
    if (composerEl) {
      var verdictBtns = composerEl.querySelectorAll('[data-verdict]');
      for (var v = 0; v < verdictBtns.length; v++) {
        verdictBtns[v].classList.toggle('is-active',
          verdictBtns[v].getAttribute('data-verdict') === rev.aiVerdict);
      }
    }

    // ── Pre-fill the textareas with example brief content ───────
    // Each example is tailored to the specific review. Mets see the
    // shape of a finished brief immediately — they'll often clear and
    // write their own, but the example keeps them from staring at
    // blank textareas.
    var reasonEl = $('met-reason');
    var watchEl = $('met-watch');
    var bottomEl = $('met-bottom-line');
    var altEl = $('met-alternate');
    if (reasonEl) reasonEl.value = rev.exampleReason || '';
    if (watchEl)  watchEl.value  = rev.exampleWatch || '';
    if (bottomEl) bottomEl.value = rev.exampleBottomLine || '';
    if (altEl)    altEl.value    = rev.exampleAlternate || '';

    // ── Reset confidence slider to AI's starting point ──────────
    var conf = $('met-confidence-input');
    var confVal = $('met-confidence-value');
    if (conf && confVal && rev.aiConfidence) {
      conf.value = rev.aiConfidence;
      var band = confidenceBand(rev.aiConfidence);
      confVal.textContent = band.label + ' \u00b7 ' + rev.aiConfidence + '%';
    }

    // ── Populate the context panel (right column) ──────────────
    // This is the part that makes the composer feel like a real working
    // tool. The Met sees the customer's plan, the AI's verdict and
    // paragraph, the weather data, and the rule reasons all at once.
    populateMetContextPanel(rev);

    // ── Reset the timer and start ticking ──────────────────────
    startComposerTimer();

    // Show the composer
    var composer = $('met-composer');
    if (composer) {
      composer.hidden = false;
      composer.dataset.reviewId = reviewId;
      // Item #2: if this is a real (backend-fetched) request, also stash
      // the claim_token so the submit handler can POST the verdict to
      // the real meteorologist endpoint. Mock rows leave this empty
      // and submit becomes a toast-only flow.
      composer.dataset.claimToken = rev.claimToken || '';
      composer.dataset.isReal = rev.isReal ? '1' : '';
      // Scroll to put the composer in view
      composer.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }
  }

  /**
   * Populate the context panel on the right side of the composer with
   * the customer's plan, the AI's suggestion, the weather data, and
   * the rule-engine reasons. Some of this comes from the review object;
   * weather data is synthesized from the AI's reasoning text in the
   * mock data (we don't yet have structured weather snapshots stored
   * per review — when we wire backend, that gets fixed).
   */
  function populateMetContextPanel(rev) {
    // Customer plan + meta
    var planEl = $('ws-context-customer-plan');
    var metaEl = $('ws-context-customer-meta');
    if (planEl) planEl.textContent = '\u201C' + rev.plan + '\u201D';
    if (metaEl) metaEl.textContent = rev.customerName + ' \u00b7 ' + rev.area;

    // AI verdict + paragraph
    var verdictEl = $('ws-context-ai-verdict');
    var confEl = $('ws-context-ai-confidence');
    var paraEl = $('ws-context-ai-paragraph');
    if (verdictEl) {
      verdictEl.classList.remove('verdict-clear', 'verdict-caution', 'verdict-risk');
      verdictEl.classList.add('verdict-' + rev.aiVerdict);
      verdictEl.textContent = rev.aiVerdict.charAt(0).toUpperCase() + rev.aiVerdict.slice(1);
    }
    if (confEl) confEl.textContent = rev.aiConfidence + '% confidence';
    if (paraEl) paraEl.textContent = rev.aiReason || '';

    // Weather data — for the prototype, we use representative numbers
    // tied to each review's verdict. When real reviews come in via the
    // backend, the actual weather snapshot at that review's time will
    // populate these cells.
    var weather = mockWeatherForReview(rev);
    var setText = function(id, val) { var el = $(id); if (el) el.textContent = val; };
    setText('ws-context-temp', weather.temp);
    setText('ws-context-wind', weather.wind);
    setText('ws-context-precip', weather.precip);
    setText('ws-context-sky', weather.sky);

    // Rule reasons — list of plain-English explanations from the rules
    // engine of why this verdict was suggested. For now we synthesize
    // from the aiReason field; real reviews will store the actual
    // reasons array from the rule engine.
    var reasonsEl = $('ws-context-reasons');
    if (reasonsEl) {
      var reasons = mockRuleReasonsForReview(rev);
      var html = '';
      for (var i = 0; i < reasons.length; i++) {
        html += '<li>' + escapeHtml(reasons[i]) + '</li>';
      }
      reasonsEl.innerHTML = html;
    }
  }

  // Mock weather snapshot per review. In production, these come from the
  // backend with the actual forecast data the rules engine and AI saw.
  function mockWeatherForReview(rev) {
    var defaults = {
      'rev-1': { temp: '62\u00b0F', wind: '8 mph SW',  precip: '5%',  sky: 'Mostly Clear' },
      'rev-2': { temp: '76\u00b0F', wind: '6\u20139 mph SE', precip: '5%',  sky: 'Mostly Sunny' },
      'rev-3': { temp: '71\u00b0F', wind: '14 mph S',  precip: '60%', sky: 'Frontal Cells' },
      'rev-4': { temp: '78\u00b0F', wind: '18\u201322 mph NW', precip: '10%', sky: 'Partly Cloudy' }
    };
    return defaults[rev.id] || { temp: '\u2014', wind: '\u2014', precip: '\u2014', sky: '\u2014' };
  }

  // Mock rule reasons per review. Mirrors what the rule engine would
  // emit on the customer side. Real reviews will carry the actual
  // reasons array; this is just a stand-in until backend wiring.
  function mockRuleReasonsForReview(rev) {
    var defaults = {
      'rev-1': [
        'Wind gusts up to 24 mph after 11 AM',
        'Surface temperature 58\u00b0F at pour time \u2014 marginal for rapid set',
        'No precipitation in the work window'
      ],
      'rev-2': [
        'Light SE winds 6\u20139 mph',
        'Mostly sunny, low humidity',
        'No precipitation in the ceremony window'
      ],
      'rev-3': [
        'Frontal passage estimated 11:00\u201313:00',
        'Possible thunderstorm development',
        'High shear environment supports severe weather'
      ],
      'rev-4': [
        'Wind shifts NW after 16:00, gusts 18\u201322 mph',
        'Direction unfavorable for west-bound legs',
        'No precipitation through 20:00'
      ]
    };
    return defaults[rev.id] || ['(rule reasons would appear here)'];
  }

  /**
   * Soft timer for the composer. Updates the "Claimed just now" text in
   * the strip every 30 seconds while the composer is open. Not a
   * countdown pressure — just awareness of pacing.
   */
  var _composerTimerInterval = null;
  var _composerStartedAt = null;
  function startComposerTimer() {
    _composerStartedAt = Date.now();
    if (_composerTimerInterval) clearInterval(_composerTimerInterval);
    var update = function() {
      var el = $('ws-composer-timer-text');
      if (!el) return;
      var elapsedSec = Math.floor((Date.now() - _composerStartedAt) / 1000);
      if (elapsedSec < 30) el.textContent = 'Claimed just now';
      else if (elapsedSec < 60) el.textContent = 'Claimed under a minute ago';
      else if (elapsedSec < 120) el.textContent = 'Claimed about a minute ago';
      else {
        var min = Math.floor(elapsedSec / 60);
        el.textContent = 'Claimed ' + min + ' minute' + (min > 1 ? 's' : '') + ' ago';
      }
    };
    update();
    _composerTimerInterval = setInterval(update, 30000);
  }
  /**
   * Set the personalized greeting at the top of the Met workspace.
   * Time-of-day-aware, like the Admin command center watch label.
   * Currently hardcoded to "M. Reynolds" — when we wire authentication,
   * this will pull from the logged-in Met's profile.
   */
  function setMetWelcome() {
    var titleEl = $('met-ws-title');
    if (!titleEl) return;
    // Pull the real signed-in user's name from the auth module, falling
    // back to a generic greeting if no one's signed in (e.g. dev menu).
    var displayName = '';
    try {
      var u = window.__wvAuth && window.__wvAuth.getCurrentUser && window.__wvAuth.getCurrentUser();
      if (u && u.name && String(u.name).trim()) {
        displayName = String(u.name).trim();
      } else if (u && u.email) {
        // Fall back to the part before "@" if name is unset
        displayName = String(u.email).split('@')[0];
      }
    } catch (e) {}
    // Final fallback for unauthed/dev contexts — generic, no fake person.
    if (!displayName) displayName = 'Meteorologist';

    var hour = new Date().getHours();
    var greeting;
    if (hour < 5)        greeting = 'Up late, ' + displayName;
    else if (hour < 12)  greeting = 'Good morning, ' + displayName;
    else if (hour < 17)  greeting = 'Welcome back, ' + displayName;
    else if (hour < 21)  greeting = 'Good evening, ' + displayName;
    else                 greeting = 'Working late, ' + displayName;
    titleEl.textContent = greeting;
  }

  /**
   * Set the Crew Workspace "Operator:" title to the signed-in user.
   *
   * Mirrors setMetWelcome but for the Crew workspace screen. The title
   * was previously hardcoded as "Operator: K.C. Thomas" which would
   * show up to any Met team member opening the Crew workspace tonight.
   * We update it on entry using the Render-authed user's first name.
   *
   * Fallback chain: Render auth user.name → email prefix → "Operator"
   */
  function setCrewWorkspaceWelcome() {
    var titleEl = $('crew-ws-title');
    if (!titleEl) return;
    var displayName = '';
    try {
      var u = window.__wvAuth && window.__wvAuth.getCurrentUser && window.__wvAuth.getCurrentUser();
      if (u && u.name && String(u.name).trim()) {
        displayName = String(u.name).trim();
      } else if (u && u.email) {
        displayName = String(u.email).split('@')[0];
      }
    } catch (e) {}
    if (!displayName) {
      // No signed-in user — show neutral fallback rather than a fake name.
      titleEl.textContent = 'Valet Crew Workspace';
      return;
    }
    titleEl.textContent = 'Operator: ' + displayName;
  }

  /**
   * Set the Crew Workspace live-strip — the thin band above the header.
   *
   * Fills the date dynamically (no hardcoded "May 13") and picks an
   * appropriate tagline for the day's posture. Watchers count is
   * currently hardcoded; when the backend tracks live Live-Feed
   * subscribers per region, that wires here.
   */
  function setCrewLiveStrip() {
    var dateEl = $('crew-ws-episode-date');
    if (dateEl) {
      var now = new Date();
      var weekdays = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];
      var months   = ['January','February','March','April','May','June','July','August','September','October','November','December'];
      dateEl.textContent = weekdays[now.getDay()] + ', ' + months[now.getMonth()] + ' ' + now.getDate();
    }
    // Tagline can vary by shift posture later; for now a single calm line.
    // Severe mode swaps it via the existing setCrewState() severe path.
  }

  /**
   * Auto-detect Met shift status.
   *
   * Per the user's product call: no manual "clock in / clock out"
   * toggle. Mets aren't paid hourly — they're paid per review. Shift
   * status is just a presence indicator that helps the team know who's
   * around. So:
   *
   *   - Workspace open + user activity in last 15 min  → "On shift"
   *   - Workspace open + idle for 15+ min              → "Idle"
   *   - Window closed                                  → off (nothing
   *                                                       to render)
   *
   * Listens to mousemove, keydown, scroll, and click anywhere on the
   * page. Resets the idle timer on each. When the timer fires, the
   * status flips to "Idle"; the next user action flips it back.
   */
  var _metLastActivity = Date.now();
  var _metIdleTimerInterval = null;
  var IDLE_THRESHOLD_MS = 15 * 60 * 1000; // 15 minutes

  function updateMetShiftStatus() {
    var dotEl = $('met-ws-shift-dot');
    var labelEl = $('met-ws-shift-status');
    if (!dotEl || !labelEl) return;
    var idleFor = Date.now() - _metLastActivity;
    var isIdle = idleFor >= IDLE_THRESHOLD_MS;
    if (isIdle) {
      dotEl.classList.remove('is-off');
      dotEl.classList.add('is-idle');
      labelEl.textContent = 'Idle';
    } else {
      dotEl.classList.remove('is-off', 'is-idle');
      labelEl.textContent = 'On shift';
    }
  }

  function setupMetShiftDetection() {
    var bumpActivity = function() {
      _metLastActivity = Date.now();
      updateMetShiftStatus();
    };
    // Listen for any user activity. Use passive listeners to avoid
    // touching scroll performance.
    document.addEventListener('mousemove', bumpActivity, { passive: true });
    document.addEventListener('keydown',   bumpActivity, { passive: true });
    document.addEventListener('scroll',    bumpActivity, { passive: true, capture: true });
    document.addEventListener('click',     bumpActivity, { passive: true });
    document.addEventListener('touchstart',bumpActivity, { passive: true });
    // Tick every 30 seconds to catch the transition to idle even if
    // there's no incoming event to drive it.
    if (_metIdleTimerInterval) clearInterval(_metIdleTimerInterval);
    _metIdleTimerInterval = setInterval(updateMetShiftStatus, 30000);
    // Initialize as on-shift
    bumpActivity();
  }

  // ════════════════════════════════════════════════════════════════════
  // METEOROLOGIST WORKSPACE v3 — Pass 2
  // Daily Brief: full 50-state county multi-select, severe alert mode,
  // audience preview that updates as counties are added/removed.
  // ════════════════════════════════════════════════════════════════════

  // The full US counties dataset, sourced from the US Census Bureau via
  // the us-counties npm package (GeoJSON parsed at build time). Includes
  // 50 states + DC, 3,136 counties total. Names match Census short-form
  // (e.g. "Aleutians East" not "Aleutians East Borough"); long-form is
  // generated programmatically when needed.
  // Inserted via build-time include:
  var US_COUNTIES = {"AL":{"name":"Alabama","counties":["Autauga","Baldwin","Barbour","Bibb","Blount","Bullock","Butler","Calhoun","Chambers","Cherokee","Chilton","Choctaw","Clarke","Clay","Cleburne","Coffee","Colbert","Conecuh","Coosa","Covington","Crenshaw","Cullman","Dale","Dallas","DeKalb","Elmore","Escambia","Etowah","Fayette","Franklin","Geneva","Greene","Hale","Henry","Houston","Jackson","Jefferson","Lamar","Lauderdale","Lawrence","Lee","Limestone","Lowndes","Macon","Madison","Marengo","Marion","Marshall","Mobile","Monroe","Montgomery","Morgan","Perry","Pickens","Pike","Randolph","Russell","Shelby","St. Clair","Sumter","Talladega","Tallapoosa","Tuscaloosa","Walker","Washington","Wilcox","Winston"]},"AK":{"name":"Alaska","counties":["Aleutians East","Aleutians West","Anchorage","Bethel","Bristol Bay","Denali","Dillingham","Fairbanks North Star","Haines","Hoonah-Angoon","Juneau","Kenai Peninsula","Ketchikan Gateway","Kodiak Island","Lake and Peninsula","Matanuska-Susitna","Nome","North Slope","Northwest Arctic","Petersburg","Prince of Wales-Hyder","Sitka","Skagway","Southeast Fairbanks","Valdez-Cordova","Wade Hampton","Wrangell","Yakutat","Yukon-Koyukuk"]},"AZ":{"name":"Arizona","counties":["Apache","Cochise","Coconino","Gila","Graham","Greenlee","La Paz","Maricopa","Mohave","Navajo","Pima","Pinal","Santa Cruz","Yavapai","Yuma"]},"AR":{"name":"Arkansas","counties":["Arkansas","Ashley","Baxter","Benton","Boone","Bradley","Calhoun","Carroll","Chicot","Clark","Clay","Cleburne","Cleveland","Columbia","Conway","Craighead","Crawford","Crittenden","Cross","Dallas","Desha","Drew","Faulkner","Franklin","Fulton","Garland","Grant","Greene","Hempstead","Hot Spring","Howard","Independence","Izard","Jackson","Jefferson","Johnson","Lafayette","Lawrence","Lee","Lincoln","Little River","Logan","Lonoke","Madison","Marion","Miller","Mississippi","Monroe","Montgomery","Nevada","Newton","Ouachita","Perry","Phillips","Pike","Poinsett","Polk","Pope","Prairie","Pulaski","Randolph","Saline","Scott","Searcy","Sebastian","Sevier","Sharp","St. Francis","Stone","Union","Van Buren","Washington","White","Woodruff","Yell"]},"CA":{"name":"California","counties":["Alameda","Alpine","Amador","Butte","Calaveras","Colusa","Contra Costa","Del Norte","El Dorado","Fresno","Glenn","Humboldt","Imperial","Inyo","Kern","Kings","Lake","Lassen","Los Angeles","Madera","Marin","Mariposa","Mendocino","Merced","Modoc","Mono","Monterey","Napa","Nevada","Orange","Placer","Plumas","Riverside","Sacramento","San Benito","San Bernardino","San Diego","San Francisco","San Joaquin","San Luis Obispo","San Mateo","Santa Barbara","Santa Clara","Santa Cruz","Shasta","Sierra","Siskiyou","Solano","Sonoma","Stanislaus","Sutter","Tehama","Trinity","Tulare","Tuolumne","Ventura","Yolo","Yuba"]},"CO":{"name":"Colorado","counties":["Adams","Alamosa","Arapahoe","Archuleta","Baca","Bent","Boulder","Broomfield","Chaffee","Cheyenne","Clear Creek","Conejos","Costilla","Crowley","Custer","Delta","Denver","Dolores","Douglas","Eagle","El Paso","Elbert","Fremont","Garfield","Gilpin","Grand","Gunnison","Hinsdale","Huerfano","Jackson","Jefferson","Kiowa","Kit Carson","La Plata","Lake","Larimer","Las Animas","Lincoln","Logan","Mesa","Mineral","Moffat","Montezuma","Montrose","Morgan","Otero","Ouray","Park","Phillips","Pitkin","Prowers","Pueblo","Rio Blanco","Rio Grande","Routt","Saguache","San Juan","San Miguel","Sedgwick","Summit","Teller","Washington","Weld","Yuma"]},"CT":{"name":"Connecticut","counties":["Fairfield","Hartford","Litchfield","Middlesex","New Haven","New London","Tolland","Windham"]},"DE":{"name":"Delaware","counties":["Kent","New Castle","Sussex"]},"DC":{"name":"District of Columbia","counties":["District of Columbia"]},"FL":{"name":"Florida","counties":["Alachua","Baker","Bay","Bradford","Brevard","Broward","Calhoun","Charlotte","Citrus","Clay","Collier","Columbia","DeSoto","Dixie","Duval","Escambia","Flagler","Franklin","Gadsden","Gilchrist","Glades","Gulf","Hamilton","Hardee","Hendry","Hernando","Highlands","Hillsborough","Holmes","Indian River","Jackson","Jefferson","Lafayette","Lake","Lee","Leon","Levy","Liberty","Madison","Manatee","Marion","Martin","Miami-Dade","Monroe","Nassau","Okaloosa","Okeechobee","Orange","Osceola","Palm Beach","Pasco","Pinellas","Polk","Putnam","Santa Rosa","Sarasota","Seminole","St. Johns","St. Lucie","Sumter","Suwannee","Taylor","Union","Volusia","Wakulla","Walton","Washington"]},"GA":{"name":"Georgia","counties":["Appling","Atkinson","Bacon","Baker","Baldwin","Banks","Barrow","Bartow","Ben Hill","Berrien","Bibb","Bleckley","Brantley","Brooks","Bryan","Bulloch","Burke","Butts","Calhoun","Camden","Candler","Carroll","Catoosa","Charlton","Chatham","Chattahoochee","Chattooga","Cherokee","Clarke","Clay","Clayton","Clinch","Cobb","Coffee","Colquitt","Columbia","Cook","Coweta","Crawford","Crisp","Dade","Dawson","DeKalb","Decatur","Dodge","Dooly","Dougherty","Douglas","Early","Echols","Effingham","Elbert","Emanuel","Evans","Fannin","Fayette","Floyd","Forsyth","Franklin","Fulton","Gilmer","Glascock","Glynn","Gordon","Grady","Greene","Gwinnett","Habersham","Hall","Hancock","Haralson","Harris","Hart","Heard","Henry","Houston","Irwin","Jackson","Jasper","Jeff Davis","Jefferson","Jenkins","Johnson","Jones","Lamar","Lanier","Laurens","Lee","Liberty","Lincoln","Long","Lowndes","Lumpkin","Macon","Madison","Marion","McDuffie","McIntosh","Meriwether","Miller","Mitchell","Monroe","Montgomery","Morgan","Murray","Muscogee","Newton","Oconee","Oglethorpe","Paulding","Peach","Pickens","Pierce","Pike","Polk","Pulaski","Putnam","Quitman","Rabun","Randolph","Richmond","Rockdale","Schley","Screven","Seminole","Spalding","Stephens","Stewart","Sumter","Talbot","Taliaferro","Tattnall","Taylor","Telfair","Terrell","Thomas","Tift","Toombs","Towns","Treutlen","Troup","Turner","Twiggs","Union","Upson","Walker","Walton","Ware","Warren","Washington","Wayne","Webster","Wheeler","White","Whitfield","Wilcox","Wilkes","Wilkinson","Worth"]},"HI":{"name":"Hawaii","counties":["Hawaii","Honolulu","Kalawao","Kauai","Maui"]},"ID":{"name":"Idaho","counties":["Ada","Adams","Bannock","Bear Lake","Benewah","Bingham","Blaine","Boise","Bonner","Bonneville","Boundary","Butte","Camas","Canyon","Caribou","Cassia","Clark","Clearwater","Custer","Elmore","Franklin","Fremont","Gem","Gooding","Idaho","Jefferson","Jerome","Kootenai","Latah","Lemhi","Lewis","Lincoln","Madison","Minidoka","Nez Perce","Oneida","Owyhee","Payette","Power","Shoshone","Teton","Twin Falls","Valley","Washington"]},"IL":{"name":"Illinois","counties":["Adams","Alexander","Bond","Boone","Brown","Bureau","Calhoun","Carroll","Cass","Champaign","Christian","Clark","Clay","Clinton","Coles","Cook","Crawford","Cumberland","De Witt","DeKalb","Douglas","DuPage","Edgar","Edwards","Effingham","Fayette","Ford","Franklin","Fulton","Gallatin","Greene","Grundy","Hamilton","Hancock","Hardin","Henderson","Henry","Iroquois","Jackson","Jasper","Jefferson","Jersey","Jo Daviess","Johnson","Kane","Kankakee","Kendall","Knox","LaSalle","Lake","Lawrence","Lee","Livingston","Logan","Macon","Macoupin","Madison","Marion","Marshall","Mason","Massac","McDonough","McHenry","McLean","Menard","Mercer","Monroe","Montgomery","Morgan","Moultrie","Ogle","Peoria","Perry","Piatt","Pike","Pope","Pulaski","Putnam","Randolph","Richland","Rock Island","Saline","Sangamon","Schuyler","Scott","Shelby","St. Clair","Stark","Stephenson","Tazewell","Union","Vermilion","Wabash","Warren","Washington","Wayne","White","Whiteside","Will","Williamson","Winnebago","Woodford"]},"IN":{"name":"Indiana","counties":["Adams","Allen","Bartholomew","Benton","Blackford","Boone","Brown","Carroll","Cass","Clark","Clay","Clinton","Crawford","Daviess","DeKalb","Dearborn","Decatur","Delaware","Dubois","Elkhart","Fayette","Floyd","Fountain","Franklin","Fulton","Gibson","Grant","Greene","Hamilton","Hancock","Harrison","Hendricks","Henry","Howard","Huntington","Jackson","Jasper","Jay","Jefferson","Jennings","Johnson","Knox","Kosciusko","LaGrange","LaPorte","Lake","Lawrence","Madison","Marion","Marshall","Martin","Miami","Monroe","Montgomery","Morgan","Newton","Noble","Ohio","Orange","Owen","Parke","Perry","Pike","Porter","Posey","Pulaski","Putnam","Randolph","Ripley","Rush","Scott","Shelby","Spencer","St. Joseph","Starke","Steuben","Sullivan","Switzerland","Tippecanoe","Tipton","Union","Vanderburgh","Vermillion","Vigo","Wabash","Warren","Warrick","Washington","Wayne","Wells","White","Whitley"]},"IA":{"name":"Iowa","counties":["Adair","Adams","Allamakee","Appanoose","Audubon","Benton","Black Hawk","Boone","Bremer","Buchanan","Buena Vista","Butler","Calhoun","Carroll","Cass","Cedar","Cerro Gordo","Cherokee","Chickasaw","Clarke","Clay","Clayton","Clinton","Crawford","Dallas","Davis","Decatur","Delaware","Des Moines","Dickinson","Dubuque","Emmet","Fayette","Floyd","Franklin","Fremont","Greene","Grundy","Guthrie","Hamilton","Hancock","Hardin","Harrison","Henry","Howard","Humboldt","Ida","Iowa","Jackson","Jasper","Jefferson","Johnson","Jones","Keokuk","Kossuth","Lee","Linn","Louisa","Lucas","Lyon","Madison","Mahaska","Marion","Marshall","Mills","Mitchell","Monona","Monroe","Montgomery","Muscatine","O'Brien","Osceola","Page","Palo Alto","Plymouth","Pocahontas","Polk","Pottawattamie","Poweshiek","Ringgold","Sac","Scott","Shelby","Sioux","Story","Tama","Taylor","Union","Van Buren","Wapello","Warren","Washington","Wayne","Webster","Winnebago","Winneshiek","Woodbury","Worth","Wright"]},"KS":{"name":"Kansas","counties":["Allen","Anderson","Atchison","Barber","Barton","Bourbon","Brown","Butler","Chase","Chautauqua","Cherokee","Cheyenne","Clark","Clay","Cloud","Coffey","Comanche","Cowley","Crawford","Decatur","Dickinson","Doniphan","Douglas","Edwards","Elk","Ellis","Ellsworth","Finney","Ford","Franklin","Geary","Gove","Graham","Grant","Gray","Greeley","Greenwood","Hamilton","Harper","Harvey","Haskell","Hodgeman","Jackson","Jefferson","Jewell","Johnson","Kearny","Kingman","Kiowa","Labette","Lane","Leavenworth","Lincoln","Linn","Logan","Lyon","Marion","Marshall","McPherson","Meade","Miami","Mitchell","Montgomery","Morris","Morton","Nemaha","Neosho","Ness","Norton","Osage","Osborne","Ottawa","Pawnee","Phillips","Pottawatomie","Pratt","Rawlins","Reno","Republic","Rice","Riley","Rooks","Rush","Russell","Saline","Scott","Sedgwick","Seward","Shawnee","Sheridan","Sherman","Smith","Stafford","Stanton","Stevens","Sumner","Thomas","Trego","Wabaunsee","Wallace","Washington","Wichita","Wilson","Woodson","Wyandotte"]},"KY":{"name":"Kentucky","counties":["Adair","Allen","Anderson","Ballard","Barren","Bath","Bell","Boone","Bourbon","Boyd","Boyle","Bracken","Breathitt","Breckinridge","Bullitt","Butler","Caldwell","Calloway","Campbell","Carlisle","Carroll","Carter","Casey","Christian","Clark","Clay","Clinton","Crittenden","Cumberland","Daviess","Edmonson","Elliott","Estill","Fayette","Fleming","Floyd","Franklin","Fulton","Gallatin","Garrard","Grant","Graves","Grayson","Green","Greenup","Hancock","Hardin","Harlan","Harrison","Hart","Henderson","Henry","Hickman","Hopkins","Jackson","Jefferson","Jessamine","Johnson","Kenton","Knott","Knox","Larue","Laurel","Lawrence","Lee","Leslie","Letcher","Lewis","Lincoln","Livingston","Logan","Lyon","Madison","Magoffin","Marion","Marshall","Martin","Mason","McCracken","McCreary","McLean","Meade","Menifee","Mercer","Metcalfe","Monroe","Montgomery","Morgan","Muhlenberg","Nelson","Nicholas","Ohio","Oldham","Owen","Owsley","Pendleton","Perry","Pike","Powell","Pulaski","Robertson","Rockcastle","Rowan","Russell","Scott","Shelby","Simpson","Spencer","Taylor","Todd","Trigg","Trimble","Union","Warren","Washington","Wayne","Webster","Whitley","Wolfe","Woodford"]},"LA":{"name":"Louisiana","counties":["Acadia","Allen","Ascension","Assumption","Avoyelles","Beauregard","Bienville","Bossier","Caddo","Calcasieu","Caldwell","Cameron","Catahoula","Claiborne","Concordia","De Soto","East Baton Rouge","East Carroll","East Feliciana","Evangeline","Franklin","Grant","Iberia","Iberville","Jackson","Jefferson","Jefferson Davis","La Salle","Lafayette","Lafourche","Lincoln","Livingston","Madison","Morehouse","Natchitoches","Orleans","Ouachita","Plaquemines","Pointe Coupee","Rapides","Red River","Richland","Sabine","St. Bernard","St. Charles","St. Helena","St. James","St. John the Baptist","St. Landry","St. Martin","St. Mary","St. Tammany","Tangipahoa","Tensas","Terrebonne","Union","Vermilion","Vernon","Washington","Webster","West Baton Rouge","West Carroll","West Feliciana","Winn"]},"ME":{"name":"Maine","counties":["Androscoggin","Aroostook","Cumberland","Franklin","Hancock","Kennebec","Knox","Lincoln","Oxford","Penobscot","Piscataquis","Sagadahoc","Somerset","Waldo","Washington","York"]},"MD":{"name":"Maryland","counties":["Allegany","Anne Arundel","Baltimore","Calvert","Caroline","Carroll","Cecil","Charles","Dorchester","Frederick","Garrett","Harford","Howard","Kent","Montgomery","Prince George's","Queen Anne's","Somerset","St. Mary's","Talbot","Washington","Wicomico","Worcester"]},"MA":{"name":"Massachusetts","counties":["Barnstable","Berkshire","Bristol","Dukes","Essex","Franklin","Hampden","Hampshire","Middlesex","Nantucket","Norfolk","Plymouth","Suffolk","Worcester"]},"MI":{"name":"Michigan","counties":["Alcona","Alger","Allegan","Alpena","Antrim","Arenac","Baraga","Barry","Bay","Benzie","Berrien","Branch","Calhoun","Cass","Charlevoix","Cheboygan","Chippewa","Clare","Clinton","Crawford","Delta","Dickinson","Eaton","Emmet","Genesee","Gladwin","Gogebic","Grand Traverse","Gratiot","Hillsdale","Houghton","Huron","Ingham","Ionia","Iosco","Iron","Isabella","Jackson","Kalamazoo","Kalkaska","Kent","Keweenaw","Lake","Lapeer","Leelanau","Lenawee","Livingston","Luce","Mackinac","Macomb","Manistee","Marquette","Mason","Mecosta","Menominee","Midland","Missaukee","Monroe","Montcalm","Montmorency","Muskegon","Newaygo","Oakland","Oceana","Ogemaw","Ontonagon","Osceola","Oscoda","Otsego","Ottawa","Presque Isle","Roscommon","Saginaw","Sanilac","Schoolcraft","Shiawassee","St. Clair","St. Joseph","Tuscola","Van Buren","Washtenaw","Wayne","Wexford"]},"MN":{"name":"Minnesota","counties":["Aitkin","Anoka","Becker","Beltrami","Benton","Big Stone","Blue Earth","Brown","Carlton","Carver","Cass","Chippewa","Chisago","Clay","Clearwater","Cook","Cottonwood","Crow Wing","Dakota","Dodge","Douglas","Faribault","Fillmore","Freeborn","Goodhue","Grant","Hennepin","Houston","Hubbard","Isanti","Itasca","Jackson","Kanabec","Kandiyohi","Kittson","Koochiching","Lac qui Parle","Lake","Lake of the Woods","Le Sueur","Lincoln","Lyon","Mahnomen","Marshall","Martin","McLeod","Meeker","Mille Lacs","Morrison","Mower","Murray","Nicollet","Nobles","Norman","Olmsted","Otter Tail","Pennington","Pine","Pipestone","Polk","Pope","Ramsey","Red Lake","Redwood","Renville","Rice","Rock","Roseau","Scott","Sherburne","Sibley","St. Louis","Stearns","Steele","Stevens","Swift","Todd","Traverse","Wabasha","Wadena","Waseca","Washington","Watonwan","Wilkin","Winona","Wright","Yellow Medicine"]},"MS":{"name":"Mississippi","counties":["Adams","Alcorn","Amite","Attala","Benton","Bolivar","Calhoun","Carroll","Chickasaw","Choctaw","Claiborne","Clarke","Clay","Coahoma","Copiah","Covington","DeSoto","Forrest","Franklin","George","Greene","Grenada","Hancock","Harrison","Hinds","Holmes","Humphreys","Issaquena","Itawamba","Jackson","Jasper","Jefferson","Jefferson Davis","Jones","Kemper","Lafayette","Lamar","Lauderdale","Lawrence","Leake","Lee","Leflore","Lincoln","Lowndes","Madison","Marion","Marshall","Monroe","Montgomery","Neshoba","Newton","Noxubee","Oktibbeha","Panola","Pearl River","Perry","Pike","Pontotoc","Prentiss","Quitman","Rankin","Scott","Sharkey","Simpson","Smith","Stone","Sunflower","Tallahatchie","Tate","Tippah","Tishomingo","Tunica","Union","Walthall","Warren","Washington","Wayne","Webster","Wilkinson","Winston","Yalobusha","Yazoo"]},"MO":{"name":"Missouri","counties":["Adair","Andrew","Atchison","Audrain","Barry","Barton","Bates","Benton","Bollinger","Boone","Buchanan","Butler","Caldwell","Callaway","Camden","Cape Girardeau","Carroll","Carter","Cass","Cedar","Chariton","Christian","Clark","Clay","Clinton","Cole","Cooper","Crawford","Dade","Dallas","Daviess","DeKalb","Dent","Douglas","Dunklin","Franklin","Gasconade","Gentry","Greene","Grundy","Harrison","Henry","Hickory","Holt","Howard","Howell","Iron","Jackson","Jasper","Jefferson","Johnson","Knox","Laclede","Lafayette","Lawrence","Lewis","Lincoln","Linn","Livingston","Macon","Madison","Maries","Marion","McDonald","Mercer","Miller","Mississippi","Moniteau","Monroe","Montgomery","Morgan","New Madrid","Newton","Nodaway","Oregon","Osage","Ozark","Pemiscot","Perry","Pettis","Phelps","Pike","Platte","Polk","Pulaski","Putnam","Ralls","Randolph","Ray","Reynolds","Ripley","Saline","Schuyler","Scotland","Scott","Shannon","Shelby","St. Charles","St. Clair","St. Francois","St. Louis","Ste. Genevieve","Stoddard","Stone","Sullivan","Taney","Texas","Vernon","Warren","Washington","Wayne","Webster","Worth","Wright"]},"MT":{"name":"Montana","counties":["Beaverhead","Big Horn","Blaine","Broadwater","Carbon","Carter","Cascade","Chouteau","Custer","Daniels","Dawson","Deer Lodge","Fallon","Fergus","Flathead","Gallatin","Garfield","Glacier","Golden Valley","Granite","Hill","Jefferson","Judith Basin","Lake","Lewis and Clark","Liberty","Lincoln","Madison","McCone","Meagher","Mineral","Missoula","Musselshell","Park","Petroleum","Phillips","Pondera","Powder River","Powell","Prairie","Ravalli","Richland","Roosevelt","Rosebud","Sanders","Sheridan","Silver Bow","Stillwater","Sweet Grass","Teton","Toole","Treasure","Valley","Wheatland","Wibaux","Yellowstone"]},"NE":{"name":"Nebraska","counties":["Adams","Antelope","Arthur","Banner","Blaine","Boone","Box Butte","Boyd","Brown","Buffalo","Burt","Butler","Cass","Cedar","Chase","Cherry","Cheyenne","Clay","Colfax","Cuming","Custer","Dakota","Dawes","Dawson","Deuel","Dixon","Dodge","Douglas","Dundy","Fillmore","Franklin","Frontier","Furnas","Gage","Garden","Garfield","Gosper","Grant","Greeley","Hall","Hamilton","Harlan","Hayes","Hitchcock","Holt","Hooker","Howard","Jefferson","Johnson","Kearney","Keith","Keya Paha","Kimball","Knox","Lancaster","Lincoln","Logan","Loup","Madison","McPherson","Merrick","Morrill","Nance","Nemaha","Nuckolls","Otoe","Pawnee","Perkins","Phelps","Pierce","Platte","Polk","Red Willow","Richardson","Rock","Saline","Sarpy","Saunders","Scotts Bluff","Seward","Sheridan","Sherman","Sioux","Stanton","Thayer","Thomas","Thurston","Valley","Washington","Wayne","Webster","Wheeler","York"]},"NV":{"name":"Nevada","counties":["Carson City","Churchill","Clark","Douglas","Elko","Esmeralda","Eureka","Humboldt","Lander","Lincoln","Lyon","Mineral","Nye","Pershing","Storey","Washoe","White Pine"]},"NH":{"name":"New Hampshire","counties":["Belknap","Carroll","Cheshire","Coos","Grafton","Hillsborough","Merrimack","Rockingham","Strafford","Sullivan"]},"NJ":{"name":"New Jersey","counties":["Atlantic","Bergen","Burlington","Camden","Cape May","Cumberland","Essex","Gloucester","Hudson","Hunterdon","Mercer","Middlesex","Monmouth","Morris","Ocean","Passaic","Salem","Somerset","Sussex","Union","Warren"]},"NM":{"name":"New Mexico","counties":["Bernalillo","Catron","Chaves","Cibola","Colfax","Curry","De Baca","Do\ufffda Ana","Eddy","Grant","Guadalupe","Harding","Hidalgo","Lea","Lincoln","Los Alamos","Luna","McKinley","Mora","Otero","Quay","Rio Arriba","Roosevelt","San Juan","San Miguel","Sandoval","Santa Fe","Sierra","Socorro","Taos","Torrance","Union","Valencia"]},"NY":{"name":"New York","counties":["Albany","Allegany","Bronx","Broome","Cattaraugus","Cayuga","Chautauqua","Chemung","Chenango","Clinton","Columbia","Cortland","Delaware","Dutchess","Erie","Essex","Franklin","Fulton","Genesee","Greene","Hamilton","Herkimer","Jefferson","Kings","Lewis","Livingston","Madison","Monroe","Montgomery","Nassau","New York","Niagara","Oneida","Onondaga","Ontario","Orange","Orleans","Oswego","Otsego","Putnam","Queens","Rensselaer","Richmond","Rockland","Saratoga","Schenectady","Schoharie","Schuyler","Seneca","St. Lawrence","Steuben","Suffolk","Sullivan","Tioga","Tompkins","Ulster","Warren","Washington","Wayne","Westchester","Wyoming","Yates"]},"NC":{"name":"North Carolina","counties":["Alamance","Alexander","Alleghany","Anson","Ashe","Avery","Beaufort","Bertie","Bladen","Brunswick","Buncombe","Burke","Cabarrus","Caldwell","Camden","Carteret","Caswell","Catawba","Chatham","Cherokee","Chowan","Clay","Cleveland","Columbus","Craven","Cumberland","Currituck","Dare","Davidson","Davie","Duplin","Durham","Edgecombe","Forsyth","Franklin","Gaston","Gates","Graham","Granville","Greene","Guilford","Halifax","Harnett","Haywood","Henderson","Hertford","Hoke","Hyde","Iredell","Jackson","Johnston","Jones","Lee","Lenoir","Lincoln","Macon","Madison","Martin","McDowell","Mecklenburg","Mitchell","Montgomery","Moore","Nash","New Hanover","Northampton","Onslow","Orange","Pamlico","Pasquotank","Pender","Perquimans","Person","Pitt","Polk","Randolph","Richmond","Robeson","Rockingham","Rowan","Rutherford","Sampson","Scotland","Stanly","Stokes","Surry","Swain","Transylvania","Tyrrell","Union","Vance","Wake","Warren","Washington","Watauga","Wayne","Wilkes","Wilson","Yadkin","Yancey"]},"ND":{"name":"North Dakota","counties":["Adams","Barnes","Benson","Billings","Bottineau","Bowman","Burke","Burleigh","Cass","Cavalier","Dickey","Divide","Dunn","Eddy","Emmons","Foster","Golden Valley","Grand Forks","Grant","Griggs","Hettinger","Kidder","LaMoure","Logan","McHenry","McIntosh","McKenzie","McLean","Mercer","Morton","Mountrail","Nelson","Oliver","Pembina","Pierce","Ramsey","Ransom","Renville","Richland","Rolette","Sargent","Sheridan","Sioux","Slope","Stark","Steele","Stutsman","Towner","Traill","Walsh","Ward","Wells","Williams"]},"OH":{"name":"Ohio","counties":["Adams","Allen","Ashland","Ashtabula","Athens","Auglaize","Belmont","Brown","Butler","Carroll","Champaign","Clark","Clermont","Clinton","Columbiana","Coshocton","Crawford","Cuyahoga","Darke","Defiance","Delaware","Erie","Fairfield","Fayette","Franklin","Fulton","Gallia","Geauga","Greene","Guernsey","Hamilton","Hancock","Hardin","Harrison","Henry","Highland","Hocking","Holmes","Huron","Jackson","Jefferson","Knox","Lake","Lawrence","Licking","Logan","Lorain","Lucas","Madison","Mahoning","Marion","Medina","Meigs","Mercer","Miami","Monroe","Montgomery","Morgan","Morrow","Muskingum","Noble","Ottawa","Paulding","Perry","Pickaway","Pike","Portage","Preble","Putnam","Richland","Ross","Sandusky","Scioto","Seneca","Shelby","Stark","Summit","Trumbull","Tuscarawas","Union","Van Wert","Vinton","Warren","Washington","Wayne","Williams","Wood","Wyandot"]},"OK":{"name":"Oklahoma","counties":["Adair","Alfalfa","Atoka","Beaver","Beckham","Blaine","Bryan","Caddo","Canadian","Carter","Cherokee","Choctaw","Cimarron","Cleveland","Coal","Comanche","Cotton","Craig","Creek","Custer","Delaware","Dewey","Ellis","Garfield","Garvin","Grady","Grant","Greer","Harmon","Harper","Haskell","Hughes","Jackson","Jefferson","Johnston","Kay","Kingfisher","Kiowa","Latimer","Le Flore","Lincoln","Logan","Love","Major","Marshall","Mayes","McClain","McCurtain","McIntosh","Murray","Muskogee","Noble","Nowata","Okfuskee","Oklahoma","Okmulgee","Osage","Ottawa","Pawnee","Payne","Pittsburg","Pontotoc","Pottawatomie","Pushmataha","Roger Mills","Rogers","Seminole","Sequoyah","Stephens","Texas","Tillman","Tulsa","Wagoner","Washington","Washita","Woods","Woodward"]},"OR":{"name":"Oregon","counties":["Baker","Benton","Clackamas","Clatsop","Columbia","Coos","Crook","Curry","Deschutes","Douglas","Gilliam","Grant","Harney","Hood River","Jackson","Jefferson","Josephine","Klamath","Lake","Lane","Lincoln","Linn","Malheur","Marion","Morrow","Multnomah","Polk","Sherman","Tillamook","Umatilla","Union","Wallowa","Wasco","Washington","Wheeler","Yamhill"]},"PA":{"name":"Pennsylvania","counties":["Adams","Allegheny","Armstrong","Beaver","Bedford","Berks","Blair","Bradford","Bucks","Butler","Cambria","Cameron","Carbon","Centre","Chester","Clarion","Clearfield","Clinton","Columbia","Crawford","Cumberland","Dauphin","Delaware","Elk","Erie","Fayette","Forest","Franklin","Fulton","Greene","Huntingdon","Indiana","Jefferson","Juniata","Lackawanna","Lancaster","Lawrence","Lebanon","Lehigh","Luzerne","Lycoming","McKean","Mercer","Mifflin","Monroe","Montgomery","Montour","Northampton","Northumberland","Perry","Philadelphia","Pike","Potter","Schuylkill","Snyder","Somerset","Sullivan","Susquehanna","Tioga","Union","Venango","Warren","Washington","Wayne","Westmoreland","Wyoming","York"]},"RI":{"name":"Rhode Island","counties":["Bristol","Kent","Newport","Providence","Washington"]},"SC":{"name":"South Carolina","counties":["Abbeville","Aiken","Allendale","Anderson","Bamberg","Barnwell","Beaufort","Berkeley","Calhoun","Charleston","Cherokee","Chester","Chesterfield","Clarendon","Colleton","Darlington","Dillon","Dorchester","Edgefield","Fairfield","Florence","Georgetown","Greenville","Greenwood","Hampton","Horry","Jasper","Kershaw","Lancaster","Laurens","Lee","Lexington","Marion","Marlboro","McCormick","Newberry","Oconee","Orangeburg","Pickens","Richland","Saluda","Spartanburg","Sumter","Union","Williamsburg","York"]},"SD":{"name":"South Dakota","counties":["Aurora","Beadle","Bennett","Bon Homme","Brookings","Brown","Brule","Buffalo","Butte","Campbell","Charles Mix","Clark","Clay","Codington","Corson","Custer","Davison","Day","Deuel","Dewey","Douglas","Edmunds","Fall River","Faulk","Grant","Gregory","Haakon","Hamlin","Hand","Hanson","Harding","Hughes","Hutchinson","Hyde","Jackson","Jerauld","Jones","Kingsbury","Lake","Lawrence","Lincoln","Lyman","Marshall","McCook","McPherson","Meade","Mellette","Miner","Minnehaha","Moody","Pennington","Perkins","Potter","Roberts","Sanborn","Shannon","Spink","Stanley","Sully","Todd","Tripp","Turner","Union","Walworth","Yankton","Ziebach"]},"TN":{"name":"Tennessee","counties":["Anderson","Bedford","Benton","Bledsoe","Blount","Bradley","Campbell","Cannon","Carroll","Carter","Cheatham","Chester","Claiborne","Clay","Cocke","Coffee","Crockett","Cumberland","Davidson","DeKalb","Decatur","Dickson","Dyer","Fayette","Fentress","Franklin","Gibson","Giles","Grainger","Greene","Grundy","Hamblen","Hamilton","Hancock","Hardeman","Hardin","Hawkins","Haywood","Henderson","Henry","Hickman","Houston","Humphreys","Jackson","Jefferson","Johnson","Knox","Lake","Lauderdale","Lawrence","Lewis","Lincoln","Loudon","Macon","Madison","Marion","Marshall","Maury","McMinn","McNairy","Meigs","Monroe","Montgomery","Moore","Morgan","Obion","Overton","Perry","Pickett","Polk","Putnam","Rhea","Roane","Robertson","Rutherford","Scott","Sequatchie","Sevier","Shelby","Smith","Stewart","Sullivan","Sumner","Tipton","Trousdale","Unicoi","Union","Van Buren","Warren","Washington","Wayne","Weakley","White","Williamson","Wilson"]},"TX":{"name":"Texas","counties":["Anderson","Andrews","Angelina","Aransas","Archer","Armstrong","Atascosa","Austin","Bailey","Bandera","Bastrop","Baylor","Bee","Bell","Bexar","Blanco","Borden","Bosque","Bowie","Brazoria","Brazos","Brewster","Briscoe","Brooks","Brown","Burleson","Burnet","Caldwell","Calhoun","Callahan","Cameron","Camp","Carson","Cass","Castro","Chambers","Cherokee","Childress","Clay","Cochran","Coke","Coleman","Collin","Collingsworth","Colorado","Comal","Comanche","Concho","Cooke","Coryell","Cottle","Crane","Crockett","Crosby","Culberson","Dallam","Dallas","Dawson","DeWitt","Deaf Smith","Delta","Denton","Dickens","Dimmit","Donley","Duval","Eastland","Ector","Edwards","El Paso","Ellis","Erath","Falls","Fannin","Fayette","Fisher","Floyd","Foard","Fort Bend","Franklin","Freestone","Frio","Gaines","Galveston","Garza","Gillespie","Glasscock","Goliad","Gonzales","Gray","Grayson","Gregg","Grimes","Guadalupe","Hale","Hall","Hamilton","Hansford","Hardeman","Hardin","Harris","Harrison","Hartley","Haskell","Hays","Hemphill","Henderson","Hidalgo","Hill","Hockley","Hood","Hopkins","Houston","Howard","Hudspeth","Hunt","Hutchinson","Irion","Jack","Jackson","Jasper","Jeff Davis","Jefferson","Jim Hogg","Jim Wells","Johnson","Jones","Karnes","Kaufman","Kendall","Kenedy","Kent","Kerr","Kimble","King","Kinney","Kleberg","Knox","La Salle","Lamar","Lamb","Lampasas","Lavaca","Lee","Leon","Liberty","Limestone","Lipscomb","Live Oak","Llano","Loving","Lubbock","Lynn","Madison","Marion","Martin","Mason","Matagorda","Maverick","McCulloch","McLennan","McMullen","Medina","Menard","Midland","Milam","Mills","Mitchell","Montague","Montgomery","Moore","Morris","Motley","Nacogdoches","Navarro","Newton","Nolan","Nueces","Ochiltree","Oldham","Orange","Palo Pinto","Panola","Parker","Parmer","Pecos","Polk","Potter","Presidio","Rains","Randall","Reagan","Real","Red River","Reeves","Refugio","Roberts","Robertson","Rockwall","Runnels","Rusk","Sabine","San Augustine","San Jacinto","San Patricio","San Saba","Schleicher","Scurry","Shackelford","Shelby","Sherman","Smith","Somervell","Starr","Stephens","Sterling","Stonewall","Sutton","Swisher","Tarrant","Taylor","Terrell","Terry","Throckmorton","Titus","Tom Green","Travis","Trinity","Tyler","Upshur","Upton","Uvalde","Val Verde","Van Zandt","Victoria","Walker","Waller","Ward","Washington","Webb","Wharton","Wheeler","Wichita","Wilbarger","Willacy","Williamson","Wilson","Winkler","Wise","Wood","Yoakum","Young","Zapata","Zavala"]},"UT":{"name":"Utah","counties":["Beaver","Box Elder","Cache","Carbon","Daggett","Davis","Duchesne","Emery","Garfield","Grand","Iron","Juab","Kane","Millard","Morgan","Piute","Rich","Salt Lake","San Juan","Sanpete","Sevier","Summit","Tooele","Uintah","Utah","Wasatch","Washington","Wayne","Weber"]},"VT":{"name":"Vermont","counties":["Addison","Bennington","Caledonia","Chittenden","Essex","Franklin","Grand Isle","Lamoille","Orange","Orleans","Rutland","Washington","Windham","Windsor"]},"VA":{"name":"Virginia","counties":["Accomack","Albemarle","Alexandria","Alleghany","Amelia","Amherst","Appomattox","Arlington","Augusta","Bath","Bedford","Bland","Botetourt","Bristol","Brunswick","Buchanan","Buckingham","Buena Vista","Campbell","Caroline","Carroll","Charles City","Charlotte","Charlottesville","Chesapeake","Chesterfield","Clarke","Colonial Heights","Covington","Craig","Culpeper","Cumberland","Danville","Dickenson","Dinwiddie","Emporia","Essex","Fairfax","Falls Church","Fauquier","Floyd","Fluvanna","Franklin","Frederick","Fredericksburg","Galax","Giles","Gloucester","Goochland","Grayson","Greene","Greensville","Halifax","Hampton","Hanover","Harrisonburg","Henrico","Henry","Highland","Hopewell","Isle of Wight","James City","King George","King William","King and Queen","Lancaster","Lee","Lexington","Loudoun","Louisa","Lunenburg","Lynchburg","Madison","Manassas","Manassas Park","Martinsville","Mathews","Mecklenburg","Middlesex","Montgomery","Nelson","New Kent","Newport News","Norfolk","Northampton","Northumberland","Norton","Nottoway","Orange","Page","Patrick","Petersburg","Pittsylvania","Poquoson","Portsmouth","Powhatan","Prince Edward","Prince George","Prince William","Pulaski","Radford","Rappahannock","Richmond","Roanoke","Rockbridge","Rockingham","Russell","Salem","Scott","Shenandoah","Smyth","Southampton","Spotsylvania","Stafford","Staunton","Suffolk","Surry","Sussex","Tazewell","Virginia Beach","Warren","Washington","Waynesboro","Westmoreland","Williamsburg","Winchester","Wise","Wythe","York"]},"WA":{"name":"Washington","counties":["Adams","Asotin","Benton","Chelan","Clallam","Clark","Columbia","Cowlitz","Douglas","Ferry","Franklin","Garfield","Grant","Grays Harbor","Island","Jefferson","King","Kitsap","Kittitas","Klickitat","Lewis","Lincoln","Mason","Okanogan","Pacific","Pend Oreille","Pierce","San Juan","Skagit","Skamania","Snohomish","Spokane","Stevens","Thurston","Wahkiakum","Walla Walla","Whatcom","Whitman","Yakima"]},"WV":{"name":"West Virginia","counties":["Barbour","Berkeley","Boone","Braxton","Brooke","Cabell","Calhoun","Clay","Doddridge","Fayette","Gilmer","Grant","Greenbrier","Hampshire","Hancock","Hardy","Harrison","Jackson","Jefferson","Kanawha","Lewis","Lincoln","Logan","Marion","Marshall","Mason","McDowell","Mercer","Mineral","Mingo","Monongalia","Monroe","Morgan","Nicholas","Ohio","Pendleton","Pleasants","Pocahontas","Preston","Putnam","Raleigh","Randolph","Ritchie","Roane","Summers","Taylor","Tucker","Tyler","Upshur","Wayne","Webster","Wetzel","Wirt","Wood","Wyoming"]},"WI":{"name":"Wisconsin","counties":["Adams","Ashland","Barron","Bayfield","Brown","Buffalo","Burnett","Calumet","Chippewa","Clark","Columbia","Crawford","Dane","Dodge","Door","Douglas","Dunn","Eau Claire","Florence","Fond du Lac","Forest","Grant","Green","Green Lake","Iowa","Iron","Jackson","Jefferson","Juneau","Kenosha","Kewaunee","La Crosse","Lafayette","Langlade","Lincoln","Manitowoc","Marathon","Marinette","Marquette","Menominee","Milwaukee","Monroe","Oconto","Oneida","Outagamie","Ozaukee","Pepin","Pierce","Polk","Portage","Price","Racine","Richland","Rock","Rusk","Sauk","Sawyer","Shawano","Sheboygan","St. Croix","Taylor","Trempealeau","Vernon","Vilas","Walworth","Washburn","Washington","Waukesha","Waupaca","Waushara","Winnebago","Wood"]},"WY":{"name":"Wyoming","counties":["Albany","Big Horn","Campbell","Carbon","Converse","Crook","Fremont","Goshen","Hot Springs","Johnson","Laramie","Lincoln","Natrona","Niobrara","Park","Platte","Sheridan","Sublette","Sweetwater","Teton","Uinta","Washakie","Weston"]}};

  /**
   * Estimated subscriber count per county. Generated deterministically
   * from the county+state name so the demo behaves consistently — same
   * county always shows the same count. Real production replaces this
   * with a live count from the subscribers table.
   *
   * Distribution roughly mirrors county-population variance:
   *   - Famous urban counties (Los Angeles, Cook, Harris, etc.) → 60-200
   *   - Mid-size urban → 20-60
   *   - Rural → 3-20
   *
   * The hash is simple: sum char codes mod 200 + base. Not random;
   * deterministic. Good enough to make the audience preview feel like
   * real data without tying ourselves to any specific table.
   */
  function _wvCountyHash(state, county) {
    var s = state + '|' + county;
    var h = 0;
    for (var i = 0; i < s.length; i++) {
      h = ((h << 5) - h) + s.charCodeAt(i);
      h |= 0;
    }
    return Math.abs(h);
  }
  // Known urban counties get higher density; everything else uses the hash
  var URBAN_COUNTIES = {
    // Format: STATE|County → boost factor
    'CA|Los Angeles': 220, 'CA|San Diego': 140, 'CA|Orange': 130,
    'CA|San Francisco': 110, 'CA|Santa Clara': 120, 'CA|Alameda': 100,
    'IL|Cook': 180, 'TX|Harris': 200, 'TX|Dallas': 150, 'TX|Bexar': 130,
    'TX|Travis': 120, 'AZ|Maricopa': 170, 'NY|Kings': 160, 'NY|Queens': 150,
    'NY|New York': 150, 'NY|Bronx': 130, 'FL|Miami-Dade': 170,
    'FL|Broward': 140, 'FL|Orange': 130, 'WA|King': 140, 'GA|Fulton': 140,
    'PA|Philadelphia': 150, 'OH|Cuyahoga': 110, 'CO|Denver': 100,
    'IN|Marion': 90, 'IN|Hamilton': 60, 'IN|Lake': 70,
    'NC|Mecklenburg': 110, 'TN|Davidson': 100, 'TN|Shelby': 110,
    'MA|Suffolk': 110, 'MA|Middlesex': 130, 'MN|Hennepin': 110,
    'MI|Wayne': 130, 'MO|St. Louis': 100, 'NV|Clark': 130,
    'OR|Multnomah': 100
  };
  function estimateSubscribersInCounty(state, county) {
    var key = state + '|' + county;
    if (URBAN_COUNTIES[key]) return URBAN_COUNTIES[key];
    var h = _wvCountyHash(state, county);
    // Most counties land 5-35 subscribers; some get 35-80
    var base = (h % 30) + 5;
    var bump = (h % 7 === 0) ? (h % 50) : 0;
    return base + bump;
  }

  // ── Daily Brief state ───────────────────────────────────────
  // Selected counties: array of {state: 'IN', county: 'Boone'}
  var _wvSelectedCounties = [];
  var _wvCurrentState = 'IN'; // default to Indiana since that's home base
  var _wvBriefMode = 'routine'; // or 'severe'

  /**
   * Initialize the Daily Brief county selector. Populates the state
   * dropdown, wires search filtering, click-to-toggle, select-all, and
   * the audience preview.
   */
  function setupBriefCountySelector() {
    var stateEl = $('met-county-state');
    if (!stateEl) return; // not on the brief tab yet

    // Populate the state dropdown — sorted alphabetically by full name
    var states = Object.keys(US_COUNTIES);
    states.sort(function(a, b) {
      return US_COUNTIES[a].name.localeCompare(US_COUNTIES[b].name);
    });
    var html = '';
    for (var i = 0; i < states.length; i++) {
      var abbr = states[i];
      var info = US_COUNTIES[abbr];
      var selected = (abbr === _wvCurrentState) ? ' selected' : '';
      html += '<option value="' + abbr + '"' + selected + '>'
        + escapeHtml(info.name) + ' (' + info.counties.length + ')</option>';
    }
    stateEl.innerHTML = html;

    // State change → re-render county list for that state
    stateEl.addEventListener('change', function() {
      _wvCurrentState = stateEl.value;
      var search = $('met-county-search');
      if (search) search.value = '';
      renderCountyList();
    });

    // Search filter → re-render with the filter applied
    var searchEl = $('met-county-search');
    if (searchEl) {
      searchEl.addEventListener('input', function() {
        renderCountyList();
      });
    }

    // Select-all-in-state button
    var selectAllBtn = $('met-county-select-all');
    if (selectAllBtn) {
      selectAllBtn.addEventListener('click', function() {
        var counties = US_COUNTIES[_wvCurrentState].counties;
        for (var i = 0; i < counties.length; i++) {
          addSelectedCounty(_wvCurrentState, counties[i]);
        }
        renderCountyList();
        renderSelectedChips();
        renderAudiencePreview();
      });
    }

    // Clear-all button — wipe everything regardless of state
    var clearBtn = $('met-county-clear');
    if (clearBtn) {
      clearBtn.addEventListener('click', function() {
        _wvSelectedCounties = [];
        renderCountyList();
        renderSelectedChips();
        renderAudiencePreview();
      });
    }

    // Click on a county item or its checkbox toggles selection.
    // Delegated to the list container so we don't re-attach on each render.
    var listEl = $('met-county-list');
    if (listEl) {
      listEl.addEventListener('click', function(ev) {
        var item = ev.target.closest('.ws-county-item');
        if (!item) return;
        var county = item.getAttribute('data-county');
        if (!county) return;
        // If the checkbox itself was clicked, the browser already toggled
        // its checked state; otherwise we simulate the toggle. Either
        // way, we end up reflecting the new state in our model.
        var cb = item.querySelector('input[type="checkbox"]');
        if (ev.target !== cb) {
          // Clicked on the row, not the checkbox — toggle manually
          ev.preventDefault();
        }
        if (isCountySelected(_wvCurrentState, county)) {
          removeSelectedCounty(_wvCurrentState, county);
        } else {
          addSelectedCounty(_wvCurrentState, county);
        }
        renderCountyList();
        renderSelectedChips();
        renderAudiencePreview();
      });
    }

    // Chips bar — click an X to remove a county
    var chipsEl = $('met-county-chips');
    if (chipsEl) {
      chipsEl.addEventListener('click', function(ev) {
        var x = ev.target.closest('.ws-county-chip-x');
        if (!x) return;
        var st = x.getAttribute('data-state');
        var county = x.getAttribute('data-county');
        removeSelectedCounty(st, county);
        renderCountyList();
        renderSelectedChips();
        renderAudiencePreview();
      });
    }

    // Initial render
    renderCountyList();
    renderSelectedChips();
    renderAudiencePreview();
  }

  function isCountySelected(state, county) {
    for (var i = 0; i < _wvSelectedCounties.length; i++) {
      if (_wvSelectedCounties[i].state === state && _wvSelectedCounties[i].county === county) {
        return true;
      }
    }
    return false;
  }
  function addSelectedCounty(state, county) {
    if (isCountySelected(state, county)) return;
    _wvSelectedCounties.push({state: state, county: county});
  }
  function removeSelectedCounty(state, county) {
    for (var i = 0; i < _wvSelectedCounties.length; i++) {
      if (_wvSelectedCounties[i].state === state && _wvSelectedCounties[i].county === county) {
        _wvSelectedCounties.splice(i, 1);
        return;
      }
    }
  }

  function renderCountyList() {
    var listEl = $('met-county-list');
    if (!listEl) return;
    var counties = US_COUNTIES[_wvCurrentState].counties;
    var searchEl = $('met-county-search');
    var query = (searchEl && searchEl.value) ? searchEl.value.trim().toLowerCase() : '';
    var html = '';
    var visible = 0;
    for (var i = 0; i < counties.length; i++) {
      var c = counties[i];
      if (query && c.toLowerCase().indexOf(query) === -1) continue;
      visible++;
      var selected = isCountySelected(_wvCurrentState, c);
      html += '<label class="ws-county-item' + (selected ? ' is-selected' : '')
        + '" data-county="' + escapeHtml(c) + '">'
        +   '<input type="checkbox"' + (selected ? ' checked' : '') + '>'
        +   '<span class="ws-county-item-label">' + escapeHtml(c) + '</span>'
        + '</label>';
    }
    if (visible === 0) {
      html = '<div class="ws-county-empty">No counties match \u201C' + escapeHtml(query) + '\u201D in '
        + escapeHtml(US_COUNTIES[_wvCurrentState].name) + '.</div>';
    }
    listEl.innerHTML = html;
  }

  function renderSelectedChips() {
    var chipsEl = $('met-county-chips');
    if (!chipsEl) return;
    if (_wvSelectedCounties.length === 0) {
      chipsEl.innerHTML = '<span class="ws-county-chips-empty">No counties selected yet \u2014 pick a state, then check counties above.</span>';
      return;
    }
    // Sort chips by state then county for stable display
    var sorted = _wvSelectedCounties.slice().sort(function(a, b) {
      if (a.state !== b.state) return a.state.localeCompare(b.state);
      return a.county.localeCompare(b.county);
    });
    var html = '';
    for (var i = 0; i < sorted.length; i++) {
      var sel = sorted[i];
      html += '<span class="ws-county-chip">'
        +   '<span class="ws-county-chip-state">' + sel.state + '</span>'
        +   escapeHtml(sel.county)
        +   '<button type="button" class="ws-county-chip-x" '
        +     'data-state="' + sel.state + '" data-county="' + escapeHtml(sel.county) + '" '
        +     'aria-label="Remove ' + escapeHtml(sel.county) + ' County">\u00d7</button>'
        + '</span>';
    }
    chipsEl.innerHTML = html;
  }

  function renderAudiencePreview() {
    var headlineEl = $('brief-audience-headline');
    var detailEl = $('brief-audience-detail');
    if (!headlineEl || !detailEl) return;

    if (_wvSelectedCounties.length === 0) {
      headlineEl.innerHTML = 'Select counties to see your audience.';
      detailEl.textContent = 'Subscriber counts are estimated from county population and current WeatherValet penetration.';
      return;
    }

    // Sum up subscribers and tally states
    var totalSubs = 0;
    var stateSet = {};
    for (var i = 0; i < _wvSelectedCounties.length; i++) {
      var sel = _wvSelectedCounties[i];
      totalSubs += estimateSubscribersInCounty(sel.state, sel.county);
      stateSet[sel.state] = (stateSet[sel.state] || 0) + 1;
    }
    var stateCount = Object.keys(stateSet).length;
    var countyCount = _wvSelectedCounties.length;

    var modeWord = (_wvBriefMode === 'severe') ? 'will receive this severe alert' : 'will receive this brief';
    var subPlural = (totalSubs === 1) ? 'subscriber' : 'subscribers';
    var countyPlural = (countyCount === 1) ? 'county' : 'counties';
    var statePlural = (stateCount === 1) ? 'state' : 'states';

    headlineEl.innerHTML =
      '<span class="ws-audience-headline-num">~' + totalSubs.toLocaleString() + '</span> '
      + escapeHtml(subPlural) + ' in ' + countyCount + ' ' + escapeHtml(countyPlural)
      + (stateCount > 1 ? ' across ' + stateCount + ' ' + escapeHtml(statePlural) : '')
      + ' ' + escapeHtml(modeWord) + '.';

    if (_wvBriefMode === 'severe') {
      detailEl.textContent = 'Severe alerts deliver via SMS + in-app banner immediately. Make sure the targeting is correct before publishing.';
    } else {
      detailEl.textContent = 'Routine briefs deliver via in-app notification at the next subscriber check-in. Read rate trends 88\u201394% within first 6 hours.';
    }
  }

  /**
   * Switch the Daily Brief between Routine and Severe Alert mode. Both
   * the visual treatment (red-tinted strip, audience preview, submit
   * button) and copy (eyebrow text, button label, audience phrasing)
   * change. The composer stays useful for both — same fields, same
   * county selection — but the framing communicates urgency.
   */
  function setupBriefModeSwitcher() {
    var btns = document.querySelectorAll('[data-brief-mode]');
    if (!btns.length) return;
    for (var i = 0; i < btns.length; i++) {
      btns[i].addEventListener('click', function(ev) {
        var mode = ev.currentTarget.getAttribute('data-brief-mode');
        applyBriefMode(mode);
      });
    }
  }
  function applyBriefMode(mode) {
    _wvBriefMode = mode;
    // Toggle button active states
    var btns = document.querySelectorAll('[data-brief-mode]');
    for (var i = 0; i < btns.length; i++) {
      btns[i].classList.toggle('is-active',
        btns[i].getAttribute('data-brief-mode') === mode);
    }
    // Toggle composer-shell severe class — drives the red visual treatment
    var shell = $('brief-composer-shell');
    if (shell) shell.classList.toggle('is-severe-mode', mode === 'severe');
    // Update strip eyebrow + headline + status
    var eyebrow = $('brief-mode-eyebrow');
    var headline = $('brief-mode-headline');
    var status = $('brief-status-text');
    var summaryLabel = $('brief-summary-label');
    var verdictLabel = $('brief-verdict-label');
    var timeLabel = $('brief-time-label');
    var submit = $('brief-submit-btn');
    if (mode === 'severe') {
      if (eyebrow)  eyebrow.textContent = 'Severe alert \u00b7 ' + new Date().toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' });
      if (headline) headline.textContent = 'Severe Weather Alert';
      if (status)   status.textContent = 'Draft \u00b7 not yet sent';
      if (summaryLabel) summaryLabel.textContent = 'Alert message \u2014 visible to subscribers';
      if (verdictLabel) verdictLabel.textContent = 'Alert severity';
      if (timeLabel)    timeLabel.textContent = 'Alert window of concern';
      if (submit) {
        submit.textContent = 'Send severe alert';
        submit.classList.add('is-severe');
      }
    } else {
      if (eyebrow)  eyebrow.textContent = 'Today\u2019s brief \u00b7 ' + new Date().toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' });
      if (headline) headline.textContent = 'Daily Brief';
      if (status)   status.textContent = 'Draft \u00b7 not yet published';
      if (summaryLabel) summaryLabel.textContent = 'Summary \u2014 visible to subscribers';
      if (verdictLabel) verdictLabel.textContent = 'Headline verdict for the day';
      if (timeLabel)    timeLabel.textContent = 'Time windows of concern';
      if (submit) {
        submit.textContent = 'Publish daily brief';
        submit.classList.remove('is-severe');
      }
    }
    renderAudiencePreview();
  }

  // ════════════════════════════════════════════════════════════════════
  // VALET CREW WORKSPACE — PASS 2
  // Mission-first operations zone with collapsed mission buttons,
  // citation badges, Second-this-report mechanic, real-time
  // Met-citation toast notifications.
  // ════════════════════════════════════════════════════════════════════
  //
  // Design principle (from Michael's brief, multiple turns):
  //   - Mets do NOT trigger missions; a rules engine does.
  //   - Missions are tucked behind a button by default. Tap to deploy.
  //   - Severe missions auto-expand and dominate the screen.
  //   - Reward = real attribution + a public citation from a Met,
  //     not points or badges.
  //   - Other Crew can "Second" a teammate's report (not "like").
  //   - When a Met cites a report, the Crew member gets a real-time
  //     toast notification (in production: also a Twilio SMS).

  // Mission templates. The mission engine in production picks one
  // based on Crew member geofence + conditions. For the prototype,
  // we render two routine missions plus a hidden severe one.
  var CREW_MISSIONS = {
    'mesonet-flag': {
      mode: 'routine',
      icon: 'mesonet',
      eyebrow: 'Mission \u00b7 Mesonet Calibration',
      btnHeadline: 'Calibrate Lebanon HS flag',
      status: 'Auto-assigned 12 min ago \u00b7 expires in 4h',
      headline: 'Station Calibration: Lebanon High School flag',
      context: 'You\u2019re 0.4 miles from the nearest unmonitored wind point. The closest official station is 11 miles away, which means our forecast for Lebanon is running on extrapolation. A 30-second visual check from you fills that gap.',
      purposeStrong: 'Why this matters.',
      purpose: 'M. Reynolds is forecasting an outdoor wedding in Lebanon at 4 PM. Surface wind is the variable they have the least confidence in. Your reading goes straight into their 6 PM brief.',
      question: 'Look at the flag at Lebanon High School. How is it behaving?',
      answers: [
        { id: 'limp',       icon: '|',  label: 'Limp',       detail: 'no movement, hanging straight' },
        { id: 'fluttering', icon: '~',  label: 'Fluttering', detail: 'moving but partial droop' },
        { id: 'extended',   icon: '\u27F6', label: 'Extended',   detail: 'fully horizontal, snapping' }
      ],
      missionId: 4582,
      completeHeadline: 'You\u2019re on the record.',
      completeDetail: 'The Meteorologist working your county has your reading on screen. If it shapes their next call, you\u2019ll see your name on the brief.'
    },
    'visibility': {
      mode: 'routine',
      icon: 'visibility',
      eyebrow: 'Mission \u00b7 Visibility Check',
      btnHeadline: 'Check Sugar Creek valley visibility',
      status: 'Auto-assigned 4 min ago \u00b7 morning rush window',
      headline: 'Visibility Grid: Lebanon valley check',
      context: 'HRRR shows fog density spiking in the Sugar Creek valley, but our visibility data is averaged across the county. Your specific location at this time of morning is the only ground truth we can get.',
      purposeStrong: 'Why this matters.',
      purpose: 'A school district 6 miles south is deciding right now whether to delay buses. Your reading is one of the inputs they\u2019ll see.',
      question: 'Step outside or look out a window. Can you see clearly?',
      answers: [
        { id: 'clear',  icon: '\u2600',  label: 'Clear',         detail: '10+ miles, no haze' },
        { id: 'hazy',   icon: '\u2592',  label: 'Hazy',          detail: '1-3 miles, soft visibility' },
        { id: 'fog',    icon: '\u2591',  label: 'Dense fog',     detail: 'cannot see 3 houses away' }
      ],
      missionId: 4584,
      completeHeadline: 'That could help someone\u2019s morning.',
      completeDetail: 'Your reading is in the Meteorologist\u2019s queue. If it informs a commuter alert or a school-district decision, you\u2019ll get a notification with the details.'
    },
    'storm-recon': {
      mode: 'severe',
      icon: 'storm',
      eyebrow: 'PRIORITY \u00b7 Ground Truth Recon',
      btnHeadline: 'STORM 18 MIN OUT \u2014 Ground Truth Recon',
      status: 'STORM 18 MIN OUT \u00b7 stay indoors \u00b7 do not pursue',
      headline: 'Eyes on the cloud base, Westfield rotation track',
      context: 'A supercell is tracking E-NE at 32 mph. NWS has a Tornado Warning for Boone County. Your location is 8 miles from the projected rotation track. Mets and the rules engine need a visual on the cloud base from your angle \u2014 NWS spotters can\u2019t cover everywhere.',
      purposeStrong: 'Stay safe first.',
      purpose: 'Photograph from your safest indoor location. A window, a covered porch, a doorway. Do not leave shelter to get a better angle. If sirens activate or you hear thunder continuously, stop the mission and shelter immediately.',
      question: 'Take 4 quick photos: north, south, east, west.',
      answers: [
        { id: 'north', icon: 'N', label: 'North',  detail: 'tap to capture' },
        { id: 'south', icon: 'S', label: 'South',  detail: 'tap to capture' },
        { id: 'east',  icon: 'E', label: 'East',   detail: 'tap to capture' },
        { id: 'west',  icon: 'W', label: 'West',   detail: 'tap to capture' }
      ],
      missionId: 4591,
      completeHeadline: 'The Meteorologists have eyes on you.',
      completeDetail: 'Your photos are in the severe-weather queue. Stay in shelter. You\u2019ll get a notification the moment the cell clears your area.'
    }
  };

  // Currently available missions (in production, populated by the rules
  // engine; for prototype, just a list of mission keys).
  var CREW_AVAILABLE_MISSIONS = ['mesonet-flag', 'visibility'];

  // Receipts data — what the Crew member's recent work did. Real
  // attribution from real Mets. Citation field is what makes a
  // receipt a Verifiable Flex.
  var CREW_RECEIPTS = [
    {
      id: 4471,
      headline: 'Your fog visibility report from this morning was used by <strong>M. Reynolds</strong> in the 6 AM brief for Boone County.',
      meta: 'Today \u00b7 06:14 \u00b7 Mission #4471',
      citation: { metInitials: 'MR', metName: 'M. Reynolds', verb: 'cited' },
      seconds: 3,
      seconded: false,
      status: 'verified'
    },
    {
      id: 4423,
      headline: 'Your hail photo from Friday afternoon was attached to <strong>Chris Sramek\u2019s</strong> storm-verification report and forwarded to 3 roofing leads.',
      meta: 'Fri 16:22 \u00b7 Mission #4423 \u00b7 Compensable lead',
      citation: { metInitials: 'CS', metName: 'Chris Sramek', verb: 'cited' },
      seconds: 7,
      seconded: false,
      status: 'verified'
    },
    {
      id: 4318,
      headline: 'Your wind reading from Lebanon High School (Wednesday) became part of the calibration dataset for the Boone County micro-climate model.',
      meta: 'Wed 14:08 \u00b7 Mission #4318 \u00b7 Archived to model',
      citation: null,
      seconds: 1,
      seconded: false,
      status: 'verified'
    },
    {
      id: 4287,
      headline: 'Your damage-survey report on Tuesday is being reviewed for inclusion in the business-intelligence feed.',
      meta: 'Tue 18:44 \u00b7 Mission #4287',
      citation: null,
      seconds: 0,
      seconded: false,
      status: 'pending'
    }
  ];

  /**
   * Render the available-missions list as collapsed buttons.
   * Each button shows mission eyebrow + headline + "DEPLOY" cue.
   * Tap → expand the mission body inline.
   */
  function renderCrewMissionList() {
    var list = $('crew-mission-list');
    var countText = $('crew-mission-count-text');
    if (!list) return;
    if (CREW_AVAILABLE_MISSIONS.length === 0) {
      list.innerHTML = '<div class="crew-mission-empty">'
        + '<strong>No callouts right now.</strong> '
        + 'The Meteorologist is watching. When something needs eyes on the ground, '
        + 'your phone will buzz.'
        + '</div>';
      if (countText) countText.textContent = 'no callouts right now';
      return;
    }
    if (countText) {
      var n = CREW_AVAILABLE_MISSIONS.length;
      countText.textContent = n + ' mission' + (n > 1 ? 's' : '') + ' live \u00b7 tap to take one';
    }
    var html = '';
    for (var i = 0; i < CREW_AVAILABLE_MISSIONS.length; i++) {
      var key = CREW_AVAILABLE_MISSIONS[i];
      var m = CREW_MISSIONS[key];
      if (!m) continue;
      var sevClass = (m.mode === 'severe') ? ' is-severe' : '';
      var iconSvg = crewMissionIconSvg(m.icon);
      html += '<button type="button" class="crew-mission-btn' + sevClass + '" data-mission-deploy="' + key + '">'
        +   '<div class="crew-mission-btn-icon">' + iconSvg + '</div>'
        +   '<div class="crew-mission-btn-info">'
        +     '<div class="crew-mission-btn-eyebrow">'
        +       '<span class="crew-mission-btn-eyebrow-dot"></span>'
        +       '<span>' + escapeHtml(m.eyebrow) + '</span>'
        +     '</div>'
        +     '<p class="crew-mission-btn-headline">' + escapeHtml(m.btnHeadline) + '</p>'
        +   '</div>'
        +   '<span class="crew-mission-btn-deploy">'
        +     'Deploy'
        +     '<svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        +       '<polyline points="9 18 15 12 9 6"/>'
        +     '</svg>'
        +   '</span>'
        + '</button>';
    }
    list.innerHTML = html;
  }

  /**
   * Inline SVG icons for the mission button. Return the icon SVG
   * string for the given icon key. Keeps the markup self-contained.
   */
  function crewMissionIconSvg(key) {
    if (key === 'storm') {
      return '<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'
        + '<path d="M19 16.9A5 5 0 0 0 18 7h-1.26a8 8 0 1 0-11.62 9"/>'
        + '<polyline points="13 11 9 17 15 17 11 23"/>'
        + '</svg>';
    }
    if (key === 'visibility') {
      return '<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'
        + '<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>'
        + '<circle cx="12" cy="12" r="3"/>'
        + '</svg>';
    }
    // Default: mesonet calibration (a flag)
    return '<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'
      + '<path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z"/>'
      + '<line x1="4" y1="22" x2="4" y2="15"/>'
      + '</svg>';
  }

  /**
   * Deploy a mission — expand the active mission card with the full
   * briefing for the given mission key. Scrolls into view. Severe
   * missions also flip the screen-level severe class so the ambient
   * gradient and operator dot turn red.
   */
  function deployCrewMission(missionKey) {
    var mission = CREW_MISSIONS[missionKey];
    if (!mission) return;
    var card = $('crew-mission-card');
    if (!card) return;

    // Toggle severe state
    var screen = $('screen-crew-ws');
    if (screen) {
      screen.classList.toggle('is-severe', mission.mode === 'severe');
      screen.setAttribute('data-crew-state', mission.mode);
    }
    card.classList.toggle('is-severe', mission.mode === 'severe');

    // Build answer grid
    var actionsCols = 'repeat(' + mission.answers.length + ', 1fr)';
    var actionsHtml = '';
    for (var i = 0; i < mission.answers.length; i++) {
      var a = mission.answers[i];
      actionsHtml += '<button type="button" class="crew-mission-tap" data-mission-answer="'
        + escapeHtml(a.id) + '">'
        +   '<span class="crew-mission-tap-icon">' + escapeHtml(a.icon) + '</span>'
        +   '<span class="crew-mission-tap-label">' + escapeHtml(a.label) + '</span>'
        +   '<span class="crew-mission-tap-detail">' + escapeHtml(a.detail) + '</span>'
        + '</button>';
    }

    // Severe missions don't show the optional "Add photo" button
    // alongside the cardinal capture targets — the targets ARE the
    // photo workflow.
    var captureBtnHtml = '';
    if (mission.mode !== 'severe') {
      captureBtnHtml = '<button type="button" class="crew-mission-capture" id="crew-mission-capture-btn">'
        + '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'
        +   '<path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/>'
        +   '<circle cx="12" cy="13" r="4"/>'
        + '</svg>'
        + '<span>Add photo</span>'
        + '</button>';
    }

    card.innerHTML =
        '<div class="crew-mission-strip">'
      +   '<div class="crew-mission-eyebrow">'
      +     '<span class="crew-mission-eyebrow-dot"></span>'
      +     '<span>' + escapeHtml(mission.eyebrow) + '</span>'
      +   '</div>'
      +   '<div class="crew-mission-status">' + escapeHtml(mission.status) + '</div>'
      + '</div>'
      + '<div class="crew-mission-body">'
      +   '<h2 class="crew-mission-headline">' + escapeHtml(mission.headline) + '</h2>'
      +   '<p class="crew-mission-context">' + escapeHtml(mission.context) + '</p>'
      +   '<div class="crew-mission-purpose">'
      +     '<svg class="crew-mission-purpose-icon" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">'
      +       '<circle cx="12" cy="12" r="10"/>'
      +       '<line x1="12" y1="16" x2="12" y2="12"/>'
      +       '<line x1="12" y1="8" x2="12.01" y2="8"/>'
      +     '</svg>'
      +     '<p class="crew-mission-purpose-text"><strong>' + escapeHtml(mission.purposeStrong)
      +       '</strong> ' + escapeHtml(mission.purpose) + '</p>'
      +   '</div>'
      +   '<p class="crew-tile-label" style="margin-bottom: 10px;">'
      +     '<span>' + escapeHtml(mission.question) + '</span>'
      +   '</p>'
      +   '<div class="crew-mission-actions" style="grid-template-columns: ' + actionsCols + '">'
      +     actionsHtml
      +   '</div>'
      +   '<div class="crew-mission-secondary">'
      +     '<button type="button" class="crew-mission-decline" id="crew-mission-decline-btn">Stand down</button>'
      +     captureBtnHtml
      +   '</div>'
      + '</div>';

    card.dataset.missionKey = missionKey;
    var completeCard = $('crew-mission-complete-card');
    if (completeCard) completeCard.hidden = true;
    card.hidden = false;
    // Scroll the deployed mission into view smoothly
    card.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }

  /**
   * Show the mission complete confirmation and remove the mission
   * from the available list.
   */
  function showCrewMissionComplete(missionKey, answerLabel) {
    var mission = CREW_MISSIONS[missionKey];
    if (!mission) return;
    var card = $('crew-mission-card');
    var complete = $('crew-mission-complete-card');
    if (!card || !complete) return;

    var headlineEl = $('crew-complete-headline');
    var detailEl = $('crew-complete-detail');
    var receiptEl = $('crew-complete-receipt');
    var timeEl = $('crew-complete-time');

    if (headlineEl) headlineEl.textContent = mission.completeHeadline;
    if (detailEl)   detailEl.textContent = mission.completeDetail;
    if (timeEl) {
      var now = new Date();
      var hh = String(now.getHours()).padStart(2, '0');
      var mm = String(now.getMinutes()).padStart(2, '0');
      timeEl.textContent = hh + ':' + mm;
    }
    if (receiptEl) {
      receiptEl.innerHTML = 'Mission #' + mission.missionId + ' \u00b7 Auto-assigned \u00b7 Submitted at <span id="crew-complete-time">'
        + (timeEl ? timeEl.textContent : '\u2014') + '</span> \u00b7 Answer: <strong>'
        + escapeHtml(answerLabel) + '</strong>';
    }
    card.hidden = true;
    complete.hidden = false;

    // Remember which mission this confessional belongs to, so when the
    // Crew member saves a note we can attach it to the right report.
    complete.setAttribute('data-pending-mission', missionKey);
    complete.setAttribute('data-pending-answer', answerLabel);

    // Reset and reveal the confessional input. The "saved" confirmation
    // is hidden until they actually save a note.
    var confessional = $('crew-confessional');
    var confessionalInput = $('crew-confessional-input');
    var confessionalQuote = $('crew-confessional-quote');
    var confessionalCountNum = $('crew-confessional-count-num');
    var confessionalCount = document.querySelector('.crew-confessional-count');
    var confessionalSaved = $('crew-confessional-saved');
    if (confessional) confessional.hidden = false;
    if (confessionalInput) confessionalInput.value = '';
    if (confessionalQuote) confessionalQuote.checked = false;
    if (confessionalCountNum) confessionalCountNum.textContent = '0';
    if (confessionalCount) confessionalCount.classList.remove('is-near-limit');
    if (confessionalSaved) confessionalSaved.hidden = true;

    // Remove the completed mission from available list
    var idx = CREW_AVAILABLE_MISSIONS.indexOf(missionKey);
    if (idx !== -1) CREW_AVAILABLE_MISSIONS.splice(idx, 1);
    renderCrewMissionList();

    // If this was a severe mission, return the screen to routine state
    var screen = $('screen-crew-ws');
    if (screen && mission.mode === 'severe') {
      screen.classList.remove('is-severe');
      screen.setAttribute('data-crew-state', 'routine');
    }

    complete.scrollIntoView({ behavior: 'smooth', block: 'center' });
  }

  /**
   * Wire the post-submit confessional input on the mission-complete card.
   * Idempotent — only wires listeners once. Save button stores the note
   * in CREW_CONFESSIONALS (the field notebook source of truth); Skip
   * just dismisses the input. Both end the confessional moment.
   */
  function setupCrewConfessional() {
    if (window._crewConfessionalWired) return;
    window._crewConfessionalWired = true;

    var input = $('crew-confessional-input');
    var quote = $('crew-confessional-quote');
    var countNum = $('crew-confessional-count-num');
    var countWrap = document.querySelector('.crew-confessional-count');
    var skipBtn = $('crew-confessional-skip');
    var saveBtn = $('crew-confessional-save');
    var savedEl = $('crew-confessional-saved');
    var confessional = $('crew-confessional');
    var complete = $('crew-mission-complete-card');

    if (input && countNum) {
      input.addEventListener('input', function() {
        var n = input.value.length;
        countNum.textContent = String(n);
        if (countWrap) countWrap.classList.toggle('is-near-limit', n >= 550);
      });
    }

    function endMoment(showSavedChip) {
      if (confessional) confessional.hidden = true;
      if (savedEl) savedEl.hidden = !showSavedChip;
    }

    if (skipBtn) {
      skipBtn.addEventListener('click', function() {
        endMoment(false);
      });
    }

    if (saveBtn) {
      saveBtn.addEventListener('click', function() {
        var text = (input && input.value || '').trim();
        if (!text) {
          // No text → treat as skip (don't store an empty note)
          endMoment(false);
          return;
        }
        var missionKey = complete ? complete.getAttribute('data-pending-mission') : null;
        var answerLabel = complete ? complete.getAttribute('data-pending-answer') : null;
        var mission = missionKey ? CREW_MISSIONS[missionKey] : null;

        // Optimistic UI — build the entry, add to in-memory list, re-render
        // immediately so the save feels instant. Then fire the POST. If the
        // POST fails (network, auth), the localStorage write below keeps it
        // available locally; on next reload the backend GET will reconcile.
        if (!window.CREW_CONFESSIONALS) window.CREW_CONFESSIONALS = [];
        var clientId = 'c-' + Date.now().toString(36);
        var entry = {
          id: clientId,
          text: text,
          quoteable: !!(quote && quote.checked),
          timestamp: new Date().toISOString(),
          missionId: mission ? mission.missionId : null,
          missionLabel: mission ? mission.eyebrow : null,
          answerLabel: answerLabel || null
        };
        window.CREW_CONFESSIONALS.unshift(entry);

        // Cache locally so a reload doesn't lose this entry before the
        // backend GET re-syncs. If the POST below fails, this is the only
        // record of the entry.
        try {
          localStorage.setItem('wv_crew_confessionals_v1',
                               JSON.stringify(window.CREW_CONFESSIONALS));
        } catch (e) {}

        if (typeof renderCrewNotebook === 'function') renderCrewNotebook();
        endMoment(true);

        // Phase 4b — persist to the backend. Use the same id the client
        // generated so we don't end up with two records (one local, one
        // server) for the same entry.
        fetch('https://wv-valet-backend.onrender.com/api/v1/confessionals', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({
            id: clientId,
            text: text,
            quoteable: entry.quoteable,
            missionId: entry.missionId,
            missionLabel: entry.missionLabel,
            answerLabel: entry.answerLabel
          })
        })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          if (!res.body || !res.body.ok) {
            console.warn('[confessionals] save failed:', res.status, res.body);
            // Don't roll back the UI — the entry is in localStorage and
            // will sync next time the user is online + signed in.
          }
        })
        .catch(function(err) {
          // Network error — entry stays in localStorage only for now.
          console.warn('[confessionals] save network error:', err);
        });
      });
    }
  }

  /**
   * Load the current user's confessionals from the backend, falling back
   * to localStorage if the request fails (offline, not signed in, etc).
   *
   * Phase 4b: the backend is now the source of truth. localStorage stays
   * as a one-write-back cache so the UI still has something to show on
   * page reload before the network request finishes — and so an offline
   * Crew member doesn't see an empty notebook.
   *
   * The renderer (renderCrewNotebook) reads from window.CREW_CONFESSIONALS,
   * which we populate from either source. Caller doesn't need to know.
   */
  function loadCrewConfessionals() {
    // First, fill from localStorage so the UI renders something instantly.
    // This is a stale cache but it's better than a flash of empty state.
    try {
      var raw = localStorage.getItem('wv_crew_confessionals_v1');
      if (raw) window.CREW_CONFESSIONALS = JSON.parse(raw);
    } catch (e) {
      window.CREW_CONFESSIONALS = [];
    }
    if (!window.CREW_CONFESSIONALS) window.CREW_CONFESSIONALS = [];

    // Then fetch from backend. On success, replace the in-memory array
    // and refresh the localStorage cache. On failure (401, network),
    // leave the cache in place — Crew can still see what they wrote
    // even if the server is unreachable.
    fetch('https://wv-valet-backend.onrender.com/api/v1/confessionals', {
      credentials: 'include'
    })
    .then(function(r) {
      if (!r.ok) throw new Error('http-' + r.status);
      return r.json();
    })
    .then(function(j) {
      if (j && j.ok && Array.isArray(j.confessionals)) {
        window.CREW_CONFESSIONALS = j.confessionals;
        try {
          localStorage.setItem('wv_crew_confessionals_v1',
                               JSON.stringify(j.confessionals));
        } catch (e) {}
        if (typeof renderCrewNotebook === 'function') renderCrewNotebook();
      }
    })
    .catch(function(err) {
      // Quiet failure — localStorage cache stays. Most common reason:
      // user isn't signed in yet, which is normal on first page load.
      console.log('[confessionals] load failed (using cache):', err.message);
    });
  }

  /**
   * Render the Crew's private field notebook — the list of confessionals
   * they've saved. Most recent first. Shows the text, the report it was
   * attached to, and the quoteable flag. Editable for 1 hour after save
   * (not yet implemented — Phase 4).
   */
  function renderCrewNotebook() {
    // The notebook moved from Workspace to Profile in Chunk 5d.
    // Target ID is now profile-notebook-list with light-mode classes.
    // Existing call sites (boot init, post-confessional-save in
    // setupCrewConfessional) keep working since the function name
    // and signature are unchanged.
    var section = $('profile-notebook');
    var list = $('profile-notebook-list');
    if (!list || !section) return;
    var entries = window.CREW_CONFESSIONALS || [];
    // Show the section whether it has entries or not — the empty state
    // is part of the Profile's reflective vocabulary ("here's where
    // your writing will live"). If you'd rather hide it on empty, flip
    // the line below to `section.hidden = entries.length === 0`.
    section.hidden = false;
    if (entries.length === 0) {
      list.innerHTML = '<div class="profile-notebook-empty">'
        + 'Your notebook is empty. After you submit a report, you\u2019ll be invited '
        + 'to add a note &mdash; what stood out, what the numbers won\u2019t capture. '
        + 'Notes you save here go to the Meteorologists with your reading.'
        + '</div>';
      return;
    }
    var html = '';
    for (var i = 0; i < entries.length; i++) {
      var e = entries[i];
      var d = new Date(e.timestamp);
      var dateStr = d.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' })
        + ' \u00b7 ' + d.toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit' });
      html += '<div class="profile-notebook-entry">'
        +   '<div class="profile-notebook-entry-meta">'
        +     '<span class="profile-notebook-entry-date">' + escapeHtml(dateStr) + '</span>'
        +     (e.missionLabel
              ? '<span class="profile-notebook-entry-mission">' + escapeHtml(e.missionLabel) + '</span>'
              : '')
        +     (e.quoteable
              ? '<span class="profile-notebook-entry-flag" title="WeatherValet has permission to quote this note">Quoteable</span>'
              : '<span class="profile-notebook-entry-flag is-private">Private</span>')
        +   '</div>'
        +   '<p class="profile-notebook-entry-text">' + escapeHtml(e.text) + '</p>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  // ── Profile · Your recent reports (Chunk 5d) ────────────────────────
  // A vertically scrollable list of the signed-in Crew member's own
  // most recent reports. Last 10. Compact rows with type-color stripe,
  // clamped text, and meta line (time · city · verifies). Tappable —
  // each row opens that report in the detail view.
  function renderProfileRecent(user) {
    var section = $('profile-recent');
    var list = $('profile-recent-list');
    var sub = $('profile-recent-sub');
    if (!section || !list) return;

    if (typeof CREW_REPORTS === 'undefined' || !user) {
      section.hidden = true;
      return;
    }

    // Filter to this user's reports, sort newest-first, take last 10
    var mine = [];
    for (var i = 0; i < CREW_REPORTS.length; i++) {
      if (reportBelongsTo(CREW_REPORTS[i], user)) {
        mine.push(CREW_REPORTS[i]);
      }
    }
    mine.sort(function(a, b) {
      var aT = a.submittedAt ? new Date(a.submittedAt).getTime()
                              : Date.now() - (a.timeMinutes || 999) * 60000;
      var bT = b.submittedAt ? new Date(b.submittedAt).getTime()
                              : Date.now() - (b.timeMinutes || 999) * 60000;
      return bT - aT;
    });
    var top = mine.slice(0, 10);

    // Empty state — keep the section visible with a friendly nudge.
    if (top.length === 0) {
      section.hidden = false;
      if (sub) sub.textContent = '';
      list.innerHTML = '<li class="profile-recent-empty">'
        + 'You haven\u2019t submitted a report yet. When you do, the last 10 will land here.'
        + '</li>';
      return;
    }

    section.hidden = false;
    if (sub) sub.textContent = 'Last ' + top.length + ' \u00b7 newest first';

    var html = '';
    for (var j = 0; j < top.length; j++) {
      var r = top[j];
      var text = (r.text || '').trim();
      var ageMin;
      if (r.submittedAt) {
        ageMin = (Date.now() - new Date(r.submittedAt).getTime()) / 60000;
      } else if (typeof r.timeMinutes === 'number') {
        ageMin = r.timeMinutes;
      } else {
        ageMin = 0;
      }
      var timeLabel = (typeof formatAgeLabel === 'function')
        ? formatAgeLabel(ageMin)
        : (Math.floor(ageMin) + ' min ago');
      var verifyHtml = '';
      if ((r.verified || 0) > 0) {
        verifyHtml = '<span class="profile-recent-meta-sep">\u00b7</span>'
          + '<span class="profile-recent-verifies">'
          + '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round">'
          + '<polyline points="20 6 9 17 4 12"/></svg>'
          + r.verified + ' verified'
          + '</span>';
      }
      html += '<li class="profile-recent-item" data-type="' + escapeHtml(r.type || '') + '" '
        +   'data-profile-report="' + escapeHtml(r.id) + '">'
        +   '<span class="profile-recent-type" aria-hidden="true"></span>'
        +   '<div class="profile-recent-body">'
        +     '<p class="profile-recent-text">' + escapeHtml(text) + '</p>'
        +     '<div class="profile-recent-meta">'
        +       '<span>' + escapeHtml(timeLabel) + '</span>'
        +       (r.city
              ? '<span class="profile-recent-meta-sep">\u00b7</span><span>' + escapeHtml(r.city) + '</span>'
              : '')
        +       verifyHtml
        +     '</div>'
        +   '</div>'
        + '</li>';
    }
    list.innerHTML = html;
  }



  /**
   * Render the contribution receipts panel. Each receipt has the
   * core text, a citation badge if a Met cited it, and a
   * Second-this-report button with current count. The Second
   * mechanic is the social layer pointed at useful behavior:
   * "I'm two miles south and confirm 3-inch hail" instead of "like."
   */
  function renderCrewReceipts() {
    var listEl = $('crew-receipts-list');
    if (!listEl) return;
    var html = '';
    for (var i = 0; i < CREW_RECEIPTS.length; i++) {
      var r = CREW_RECEIPTS[i];
      var iconCls = (r.status === 'pending') ? ' is-pending' : '';
      var iconSvg;
      if (r.status === 'pending') {
        iconSvg = '<svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
          + '<circle cx="12" cy="12" r="10"/>'
          + '<polyline points="12 6 12 12 16 14"/>'
          + '</svg>';
      } else {
        iconSvg = '<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
          + '<polyline points="20 6 9 17 4 12"/>'
          + '</svg>';
      }

      var citationHtml = '';
      if (r.citation) {
        citationHtml = '<div class="crew-receipt-citation">'
          + '<svg class="crew-receipt-citation-icon" viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
          +   '<polyline points="20 6 9 17 4 12"/>'
          + '</svg>'
          + '<span>Cited by ' + escapeHtml(r.citation.metName) + '</span>'
          + '</div>';
      }

      var secondedClass = r.seconded ? ' is-seconded' : '';
      var secondedLabel = r.seconded ? 'Seconded' : 'Second this';
      var secondsLabel = r.seconds + ' ' + (r.seconds === 1 ? 'second' : 'seconds');

      html += '<div class="crew-receipt" data-receipt-id="' + r.id + '">'
        +   '<div class="crew-receipt-icon' + iconCls + '">' + iconSvg + '</div>'
        +   '<div class="crew-receipt-body">'
        +     '<p class="crew-receipt-headline">' + r.headline + '</p>'
        +     '<p class="crew-receipt-meta">' + escapeHtml(r.meta)
        +       (r.status === 'pending' ? ' \u00b7 Pending verification' : ' \u00b7 Verified') + '</p>'
        +     citationHtml
        +     '<div class="crew-receipt-actions">'
        +       '<button type="button" class="crew-second-btn' + secondedClass + '" data-second-receipt="' + r.id + '">'
        +         '<svg class="crew-second-btn-icon" viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
        +           '<polyline points="20 6 9 17 4 12"/>'
        +           '<polyline points="13 6 4 15 8 19"/>'
        +         '</svg>'
        +         '<span>' + secondedLabel + '</span>'
        +       '</button>'
        +       '<span class="crew-second-count" data-seconds-count="' + r.id + '">' + secondsLabel + '</span>'
        +     '</div>'
        +   '</div>'
        + '</div>';
    }
    listEl.innerHTML = html;
  }

  /**
   * Trigger the Met-citation toast. In production: the backend fires
   * this when a Met saves a brief that incorporates a Crew member's
   * report, AND a Twilio SMS goes to the Crew member's phone with
   * the same message. The toast handles the in-app case.
   *
   * For prototype, the toast is exposed as window._crewCiteMe() so
   * testers can fire it on demand.
   */
  function triggerCrewCitationToast(opts) {
    opts = opts || {};
    var toast = $('crew-citation-toast');
    if (!toast) return;
    var headlineEl = $('crew-citation-toast-headline');
    var detailEl = $('crew-citation-toast-detail');
    var metaEl = $('crew-citation-toast-meta');
    if (headlineEl) {
      headlineEl.innerHTML = '<strong>' + escapeHtml(opts.metName || 'M. Reynolds')
        + '</strong> just used your ' + escapeHtml(opts.what || 'fog report') + '.';
    }
    if (detailEl) {
      detailEl.textContent = opts.detail
        || 'Incorporated into the 6 AM brief for Boone County. Your reading helped 8 morning commuters make a safer decision.';
    }
    if (metaEl) {
      metaEl.textContent = 'Mission #' + (opts.missionId || 4471) + ' \u00b7 tap to view in your contribution log';
    }
    toast.classList.add('is-visible');
    // Auto-dismiss after 7 seconds
    if (window._citationToastTimer) clearTimeout(window._citationToastTimer);
    window._citationToastTimer = setTimeout(function() {
      toast.classList.remove('is-visible');
    }, 7000);
  }


  // ════════════════════════════════════════════════════════════════════
  // STORM SHELTER v2 — three-tier severe weather surface
  //
  // Tiers:
  //   'watch'     → amber, conditions favorable
  //   'warning'   → red, storm imminent or occurring
  //   'all-clear' → green, risk has passed (auto-fade after 8 min)
  //   false / null → dismiss entirely
  //
  // Activation in production:
  //   A backend job polls NWS Watch/Warning feeds + SPC categorical risk
  //   for each subscriber's saved location. When the tier changes, push
  //   to active customer sessions via WebSocket (or short-poll). The
  //   client picks up the new tier and calls the same toggle function.
  //
  // Met override:
  //   When a Met regionally edits the active shelter content, the
  //   override locks for 60 minutes by default. During the lock,
  //   the auto-refresh timer is paused (visible indicator). A Met
  //   can extend the lock or release it. After the lock expires,
  //   auto-refresh resumes and the shelter content reverts to NWS
  //   data on the next poll cycle.
  //
  // Console hooks for testing:
  //   _stormShelterToggle('watch')      → amber
  //   _stormShelterToggle('warning')    → red
  //   _stormShelterToggle('all-clear')  → green with flags
  //   _stormShelterToggle()             → dismiss
  //   _stormShelterMetOverride()        → toggle the override pill
  // ════════════════════════════════════════════════════════════════════

  function setupStormShelter() {
    var shelter = document.getElementById('storm-shelter');
    var toggleBtn = document.getElementById('storm-shelter-toggle-btn');
    var ackBtn = document.getElementById('storm-shelter-ack-btn');
    var ackWrap = document.getElementById('storm-shelter-ack-wrap');
    if (!shelter) return;

    // Tier-specific copy. Easy to tune without editing HTML.
    // tierName goes in the colored bar; tierMeta goes after the separator.
    var TIER_COPY = {
      watch: {
        tierName: 'Tornado Watch',
        tierMeta: 'Boone County, IN \u00b7 until 9:00 PM',
        headline: 'Severe weather is possible. We\u2019re watching it.',
        sub: 'Conditions favor severe storms in your area later today. Stay aware. We\u2019ll alert you if it escalates.'
      },
      warning: {
        tierName: 'Tornado Warning',
        tierMeta: 'Boone County, IN \u00b7 until 7:45 PM',
        headline: 'Take shelter now. We\u2019re with you.',
        sub: 'A confirmed rotation is moving toward your area. Get to your safe room. We\u2019re tracking this for you in real time.'
      },
      'all-clear': {
        tierName: 'All Clear',
        tierMeta: 'Boone County, IN \u00b7 issued 8:18 PM',
        headline: 'You\u2019re clear. The risk has passed.',
        sub: 'Severe weather has moved out of your area. No additional risk in tonight\u2019s forecast.'
      }
    };

    function applyTier(tier) {
      shelter.setAttribute('data-tier', tier);
      var copy = TIER_COPY[tier];
      if (copy) {
        var n = document.getElementById('storm-shelter-tier-name');
        var m = document.getElementById('storm-shelter-tier-meta');
        var h = document.getElementById('storm-shelter-headline');
        var s = document.getElementById('storm-shelter-sub');
        if (n) n.textContent = copy.tierName;
        if (m) m.innerHTML = copy.tierMeta;
        if (h) h.textContent = copy.headline;
        if (s) s.textContent = copy.sub;
      }
      // All-Clear: hide the phase tabs and content; show flags + ack button
      var phases = shelter.querySelector('.storm-shelter-phases');
      var contents = shelter.querySelectorAll('.storm-shelter-phase-content');
      var inAllClear = (tier === 'all-clear');
      if (phases) phases.style.display = inAllClear ? 'none' : '';
      for (var i = 0; i < contents.length; i++) {
        contents[i].style.display = inAllClear ? 'none' : '';
      }
      if (ackWrap) ackWrap.style.display = inAllClear ? 'flex' : 'none';
    }

    // Toggle button: open/close the shelter body
    if (toggleBtn) {
      toggleBtn.addEventListener('click', function() {
        var nowOpen = !shelter.classList.contains('is-open');
        shelter.classList.toggle('is-open', nowOpen);
        toggleBtn.setAttribute('aria-expanded', nowOpen ? 'true' : 'false');
      });
    }

    // Phase tabs (Before / During / After)
    var tabs = shelter.querySelectorAll('[data-shelter-phase]');
    var contents = shelter.querySelectorAll('[data-shelter-phase-content]');
    for (var i = 0; i < tabs.length; i++) {
      tabs[i].addEventListener('click', function(ev) {
        var phase = ev.currentTarget.getAttribute('data-shelter-phase');
        for (var j = 0; j < tabs.length; j++) {
          tabs[j].classList.toggle('is-active',
            tabs[j].getAttribute('data-shelter-phase') === phase);
        }
        for (var k = 0; k < contents.length; k++) {
          contents[k].classList.toggle('is-active',
            contents[k].getAttribute('data-shelter-phase-content') === phase);
        }
      });
    }

    // Damage check buttons
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-damage]');
      if (!btn) return;
      ev.preventDefault();
      var siblings = document.querySelectorAll('[data-damage]');
      for (var i = 0; i < siblings.length; i++) siblings[i].classList.remove('is-selected');
      btn.classList.add('is-selected');
      if (typeof showTipToast === 'function') {
        var damageLevel = btn.getAttribute('data-damage');
        var msg = damageLevel === 'none' ? 'Glad you\u2019re safe \u2014 noted, thank you'
               :  damageLevel === 'minor' ? 'Noted \u2014 logged for the impact assessment'
               : 'Noted \u2014 we\u2019ve flagged this and M. Reynolds will follow up';
        showTipToast(msg);
      }
    });

    // Acknowledge button (All Clear) — dismiss the shelter entirely
    if (ackBtn) {
      ackBtn.addEventListener('click', function() {
        shelter.classList.remove('is-active', 'is-open');
        if (typeof showTipToast === 'function') {
          showTipToast('Welcome back \u2014 stay weather-aware');
        }
      });
    }

    // Console hooks. Tier can be 'watch', 'warning', 'all-clear', or
    // false/undefined to dismiss.
    window._stormShelterToggle = function(tier) {
      if (tier === false || tier === null) {
        shelter.classList.remove('is-active', 'is-open');
        return;
      }
      // Backwards compat: bare `true` activates as warning
      if (tier === true || typeof tier === 'undefined') {
        // If already active, dismiss (toggle behavior)
        if (shelter.classList.contains('is-active')) {
          shelter.classList.remove('is-active', 'is-open');
          return;
        }
        tier = 'warning';
      }
      if (!TIER_COPY[tier]) {
        console.warn('Unknown shelter tier:', tier, '\u2014 valid: watch, warning, all-clear, false');
        return;
      }
      applyTier(tier);
      shelter.classList.add('is-active');
      // Compressed by default — the title bar shows but the body stays
      // closed so the search bar stays above the fold. User clicks the
      // colored bar to expand. (For All Clear, we DO auto-open since
      // the celebration-flags + acknowledge button is the whole point.)
      if (tier === 'all-clear') {
        shelter.classList.add('is-open');
        if (toggleBtn) toggleBtn.setAttribute('aria-expanded', 'true');
      } else {
        shelter.classList.remove('is-open');
        if (toggleBtn) toggleBtn.setAttribute('aria-expanded', 'false');
      }
    };

    // Met override toggle — flips the visible "Met-edited · auto-refresh
    // paused" pill. In production, would also pause the auto-refresh
    // timer for 60 minutes by default.
    window._stormShelterMetOverride = function(on) {
      var pill = document.getElementById('storm-shelter-override-pill');
      if (!pill) return;
      if (typeof on === 'undefined') {
        pill.classList.toggle('is-visible');
      } else {
        pill.classList.toggle('is-visible', !!on);
      }
    };

    // ──────────────────────────────────────────────────────────────────
    // NWS auto-trigger (Phase 10 — Storm Shelter real trigger)
    // ──────────────────────────────────────────────────────────────────
    // Polls /api/v1/me/severe-weather-status every 60 seconds. When the
    // backend reports a tier change (e.g. 'clear' → 'warning'), the
    // Storm Shelter activates automatically.
    //
    // Respects user dismissal: if the user closes the shelter (Ack
    // button or _stormShelterToggle(false)), we remember the NWS alert
    // they dismissed for. We don't re-open until either the alert
    // escalates (warning > watch) or a new alert fires.
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var _lastNwsTier = 'clear';      // server-reported tier last poll
    var _dismissedKey = null;        // string identifying alert user dismissed
    var _nwsPollTimer = null;

    function _nwsAlertKey(data) {
      // Build an identifier for "the specific alert" — event + expires.
      // If a new alert fires with the same event but different expiry,
      // it's a new alert. If the same alert is still active, same key.
      if (!data || !data.event) return null;
      return (data.event || '') + '|' + (data.expires_at || '');
    }

    function _applyNwsTier(data) {
      var tier = (data && data.status) || 'clear';
      var key = _nwsAlertKey(data);

      // If user dismissed this exact alert AND server tier didn't escalate,
      // don't re-show. Escalation = warning when previously watch.
      if (_dismissedKey && _dismissedKey === key) {
        return;
      }

      if (tier === 'warning' || tier === 'watch') {
        // Update the meta line with real area + expiry
        var copy = TIER_COPY[tier];
        if (copy) {
          var areaLabel = data.area_desc || data.location_label || '';
          var expiryStr = '';
          if (data.expires_at) {
            try {
              var d = new Date(data.expires_at);
              var h = d.getHours();
              var m = d.getMinutes();
              var ampm = h >= 12 ? 'PM' : 'AM';
              h = h % 12; if (h === 0) h = 12;
              expiryStr = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
            } catch (e) {}
          }
          if (areaLabel && expiryStr) {
            copy.tierMeta = areaLabel + ' \u00b7 until ' + expiryStr;
          } else if (areaLabel) {
            copy.tierMeta = areaLabel;
          }
        }
        applyTier(tier);
        shelter.classList.add('is-active');
        // Warning gets attention — auto-open. Watch stays collapsed.
        if (tier === 'warning') {
          shelter.classList.add('is-open');
          if (toggleBtn) toggleBtn.setAttribute('aria-expanded', 'true');
        }
      } else if (tier === 'clear' && _lastNwsTier !== 'clear' && _lastNwsTier !== 'no-location') {
        // Tier dropped from warning/watch back to clear → show All Clear
        applyTier('all-clear');
        shelter.classList.add('is-active', 'is-open');
        if (toggleBtn) toggleBtn.setAttribute('aria-expanded', 'true');
        // The All Clear's Ack button (existing handler) will fully close it
      }
      // Reset dismissal tracker when alert clears
      if (tier === 'clear') _dismissedKey = null;
      _lastNwsTier = tier;
    }

    function _pollNwsStatus() {
      fetch(BACKEND + '/api/v1/me/severe-weather-status', {
        credentials: 'include'
      })
        .then(function(r) { return r.json().then(function(j) { return j; }); })
        .then(function(res) {
          if (!res || !res.ok) return;
          // 'no-location' = anonymous user or no primary saved location;
          // don't auto-trigger.
          if (res.status === 'no-location') return;
          _applyNwsTier(res);
        })
        .catch(function() { /* silent — transient network blips */ });
    }

    // Track manual dismissal: if user clicks the Ack button while a
    // server-driven alert is active, remember it so we don't re-open
    // on the next poll for the same alert.
    if (ackBtn) {
      ackBtn.addEventListener('click', function() {
        if (_lastNwsTier === 'warning' || _lastNwsTier === 'watch') {
          // Reconstruct the key from the current tier meta — best-effort
          _dismissedKey = (TIER_COPY[_lastNwsTier] && TIER_COPY[_lastNwsTier].tierMeta) || _lastNwsTier;
        }
      });
    }

    // Kick off polling. Initial poll runs after a small delay to let
    // auth restore complete. Then every 60s.
    function startNwsPolling() {
      if (_nwsPollTimer) return;
      setTimeout(_pollNwsStatus, 5000);  // first poll after 5s
      _nwsPollTimer = setInterval(_pollNwsStatus, 60000);
    }
    startNwsPolling();

    // Expose for testing
    window._stormShelterPollNow = _pollNwsStatus;
  }


  /**
   * Setup. Wires the mission-deploy buttons, mission answer taps,
   * decline, daily check-in, Second buttons, and the citation
   * toast close.
   */
  var _crewSetupDone = false;
  function setupCrewWorkspace() {
    // Always re-render the lists so the latest data shows up when
    // the Crew view is reopened. (Setup of click handlers is
    // gated by the _crewSetupDone flag.)
    renderCrewMissionList();
    renderCrewReceipts();

    if (_crewSetupDone) return;
    _crewSetupDone = true;

    // Mission deploy — tap a mission button to expand the briefing
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-mission-deploy]');
      if (!btn) return;
      ev.preventDefault();
      var key = btn.getAttribute('data-mission-deploy');
      deployCrewMission(key);
    });

    // Mission answer tap — submit the reading and show complete card
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-mission-answer]');
      if (!btn) return;
      ev.preventDefault();
      var card = $('crew-mission-card');
      if (!card) return;
      var missionKey = card.dataset.missionKey;
      if (!missionKey) return;
      var answerId = btn.getAttribute('data-mission-answer');
      var mission = CREW_MISSIONS[missionKey];
      var answerLabel = answerId;
      if (mission) {
        for (var i = 0; i < mission.answers.length; i++) {
          if (mission.answers[i].id === answerId) {
            answerLabel = mission.answers[i].label;
            break;
          }
        }
      }
      showCrewMissionComplete(missionKey, answerLabel);
    });

    // Daily check-in — visual selection only for prototype.
    // Tapping "Other" opens the sub-grid of 10 spotter conditions
    // instead of being treated as a final answer; selecting any of
    // the four primary conditions closes the sub-grid.
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-checkin]');
      if (!btn) return;
      ev.preventDefault();
      var siblings = document.querySelectorAll('[data-checkin]');
      for (var i = 0; i < siblings.length; i++) {
        siblings[i].classList.remove('is-selected');
      }
      btn.classList.add('is-selected');
      var other = $('crew-checkin-other');
      var key = btn.getAttribute('data-checkin');
      if (key === 'other') {
        // Toggle expansion; don't fire the streak toast yet, the
        // Crew member needs to pick a specific sub-condition.
        if (other) other.classList.toggle('is-open');
        return;
      }
      // Any of the four primary conditions: close the sub-grid and
      // clear any sub-selections that had been made.
      if (other) {
        other.classList.remove('is-open');
        var subs = other.querySelectorAll('[data-checkin-other]');
        for (var j = 0; j < subs.length; j++) subs[j].classList.remove('is-selected');
      }
      if (typeof showTipToast === 'function') {
        showTipToast('Check-in logged \u2014 streak continues');
      }
    });

    // Sub-option tap inside the Other expansion. Treats the chosen
    // sub-condition as the actual check-in and confirms.
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-checkin-other]');
      if (!btn) return;
      ev.preventDefault();
      var siblings = document.querySelectorAll('[data-checkin-other]');
      for (var i = 0; i < siblings.length; i++) {
        siblings[i].classList.remove('is-selected');
      }
      btn.classList.add('is-selected');
      var label = btn.querySelector('.crew-checkin-other-label');
      var labelText = label ? label.textContent : 'condition';
      if (typeof showTipToast === 'function') {
        showTipToast('Reported: ' + labelText + ' \u2014 streak continues');
      }
    });

    // Decline / stand-down button — collapse the mission card back
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('#crew-mission-decline-btn');
      if (!btn) return;
      ev.preventDefault();
      var card = $('crew-mission-card');
      if (card) card.hidden = true;
      var screen = $('screen-crew-ws');
      if (screen) {
        screen.classList.remove('is-severe');
        screen.setAttribute('data-crew-state', 'routine');
      }
      if (typeof showTipToast === 'function') {
        showTipToast('Stood down \u2014 the system will find another Crew member');
      }
    });

    // Photo capture stub
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('#crew-mission-capture-btn');
      if (!btn) return;
      ev.preventDefault();
      if (typeof showTipToast === 'function') {
        showTipToast('Photo capture coming \u2014 wired in next pass');
      }
    });

    // Second-this-report — confirms a teammate's report
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-second-receipt]');
      if (!btn || btn.classList.contains('is-seconded')) return;
      ev.preventDefault();
      var receiptId = parseInt(btn.getAttribute('data-second-receipt'), 10);
      // Update the data
      for (var i = 0; i < CREW_RECEIPTS.length; i++) {
        if (CREW_RECEIPTS[i].id === receiptId) {
          CREW_RECEIPTS[i].seconded = true;
          CREW_RECEIPTS[i].seconds += 1;
          // Update the DOM directly (lighter than re-render)
          btn.classList.add('is-seconded');
          var label = btn.querySelector('span');
          if (label) label.textContent = 'Seconded';
          var countEl = document.querySelector('[data-seconds-count="' + receiptId + '"]');
          if (countEl) {
            var n = CREW_RECEIPTS[i].seconds;
            countEl.textContent = n + ' ' + (n === 1 ? 'second' : 'seconds');
          }
          if (typeof showTipToast === 'function') {
            showTipToast('Seconded \u2014 the original reporter will see it');
          }
          break;
        }
      }
    });

    // Citation toast close
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('#crew-citation-toast-close');
      if (!btn) return;
      ev.preventDefault();
      var toast = $('crew-citation-toast');
      if (toast) toast.classList.remove('is-visible');
      if (window._citationToastTimer) clearTimeout(window._citationToastTimer);
    });

    // Click on the toast itself (other than close) — just dismisses
    document.addEventListener('click', function(ev) {
      var toast = ev.target.closest('#crew-citation-toast');
      if (!toast) return;
      if (ev.target.closest('#crew-citation-toast-close')) return;
      // tap to view the contribution log — for now, just dismiss
      toast.classList.remove('is-visible');
      if (window._citationToastTimer) clearTimeout(window._citationToastTimer);
      // Scroll to receipts
      var receipts = $('crew-receipts-tile');
      if (receipts) receipts.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });

    // Console hooks for prototype demo:
    //   _crewLoadMission('storm-recon')   → preview severe mode
    //   _crewCiteMe()                      → simulate a Met using your report
    window._crewLoadMission = function(key) {
      // Add it to available list if not already there, then deploy
      if (CREW_AVAILABLE_MISSIONS.indexOf(key) === -1) {
        CREW_AVAILABLE_MISSIONS.push(key);
        renderCrewMissionList();
      }
      deployCrewMission(key);
    };
    window._crewCiteMe = function(opts) {
      triggerCrewCitationToast(opts);
    };

    // Auto-deploy any severe missions that are in the available list
    // (the rules-engine analogue for the prototype: severe missions
    // don't wait for a tap, they take over).
    for (var i = 0; i < CREW_AVAILABLE_MISSIONS.length; i++) {
      var key = CREW_AVAILABLE_MISSIONS[i];
      var m = CREW_MISSIONS[key];
      if (m && m.mode === 'severe') {
        deployCrewMission(key);
        break;
      }
    }
  }

  function stopComposerTimer() {
    if (_composerTimerInterval) {
      clearInterval(_composerTimerInterval);
      _composerTimerInterval = null;
    }
  }

  /**
   * Hide the composer and (optionally) remove the claimed review from
   * the queue. Called by the Submit button (removeFromQueue=true) and
   * the Cancel button (removeFromQueue=false). The team panel and
   * queue stay visible during composing in the new workspace, so we
   * don't need to un-hide them here.
   */
  function closeMetComposer(removeFromQueue) {
    var composer = $('met-composer');
    if (!composer) return;
    var reviewId = composer.dataset.reviewId;
    var isReal = composer.dataset.isReal === '1';
    composer.hidden = true;
    stopComposerTimer();
    if (removeFromQueue && reviewId) {
      if (isReal) {
        // Item #2: real request was submitted — remove from MET_REAL_QUEUE.
        // The backend already moved its status to 'completed' so a future
        // refresh from the server won't bring it back.
        for (var i = 0; i < MET_REAL_QUEUE.length; i++) {
          if (MET_REAL_QUEUE[i].id === reviewId) {
            MET_REAL_QUEUE.splice(i, 1);
            break;
          }
        }
      } else {
        for (var j = 0; j < MET_QUEUE_MOCK.length; j++) {
          if (MET_QUEUE_MOCK[j].id === reviewId) {
            MET_QUEUE_MOCK.splice(j, 1);
            break;
          }
        }
      }
      renderMetQueue();
    }
  }

  // ── Atmospheric parallax ────────────────────────────────────────────
  // Subtle scroll-driven motion on the ambient orbs. Each orb shifts
  // at a different rate (15-30% of scroll speed) so the depth layers
  // feel parallactic — closer orbs move more, deeper ones less.
  // This is the "atmospheric depth" feeling the visual identity rests
  // on. Uses requestAnimationFrame for smoothness and only runs on
  // customer-facing screens (workspace screens hide orbs entirely).
  function setupAtmosphericParallax() {
    var blue = document.querySelector('.orb-blue');
    var deep = document.querySelector('.orb-deep');
    var amber = document.querySelector('.orb-amber');
    if (!blue || !deep || !amber) return;

    // Respect reduced-motion preference — accessibility-first.
    var reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (reducedMotion) return;

    var rafPending = false;
    var lastScroll = 0;

    function apply() {
      rafPending = false;
      var y = lastScroll;
      // Different rates create parallax: blue closest (most motion),
      // amber middle, deep furthest (least motion). Multipliers are
      // intentionally small — this is atmosphere, not animation.
      blue.style.transform = 'translate3d(' + (y * -0.04) + 'px, ' + (y * -0.18) + 'px, 0)';
      amber.style.transform = 'translate3d(' + (y * 0.03) + 'px, ' + (y * 0.12) + 'px, 0)';
      deep.style.transform = 'translate3d(' + (y * 0.02) + 'px, ' + (y * -0.08) + 'px, 0)';
    }

    function onScroll() {
      lastScroll = window.scrollY || window.pageYOffset || 0;
      if (!rafPending) {
        rafPending = true;
        window.requestAnimationFrame(apply);
      }
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    // Apply once on init to handle any initial scroll position
    apply();
  }

  // ── Email capture modal ────────────────────────────────────────────
  // For the May 24 launch, real subscription / Crew signup isn't yet
  // wired (Stripe + Twilio still pending). Any element with the
  // [data-email-capture] attribute triggers this modal instead. The
  // attribute value selects the variant: "subscriber" (default) or
  // "crew" — copy and accent color adapt.
  //
  // Submission posts to the Flask backend at /waitlist; backend
  // captures email + name + ZIP + variant into a simple table.
  // Real subscription/Crew signup goes live in June/July.
  var EMAIL_CAPTURE_COPY = {
    subscriber: {
      eyebrow: 'Subscription waitlist',
      title: 'Get notified when subscriptions open.',
      body: 'WeatherValet subscriptions launch in June. Drop your email and we\u2019ll let you know the moment they\u2019re live \u2014 with a launch discount for early signups.',
      submitText: 'Join the subscription waitlist',
      successBody: 'We\u2019ll be in touch when subscriptions open. In the meantime, the free walk-up search is always here.'
    },
    crew: {
      eyebrow: 'Valet Crew waitlist',
      title: 'We\u2019re building the Valet Crew.',
      body: 'The Valet Crew launches this summer. Drop your info and we\u2019ll let you know when membership opens. Early Crew members get founding-member status and priority for paid missions.',
      submitText: 'Join the Crew waitlist',
      successBody: 'You\u2019re on the list. We\u2019ll be in touch when Crew membership opens. Until then, the live feed is read-only.'
    }
  };

  function setupEmailCapture() {
    var modal = document.getElementById('email-capture-modal');
    if (!modal) return;
    var closeBtn = document.getElementById('email-capture-close');
    var form = document.getElementById('email-capture-form');
    var successBlock = document.getElementById('email-capture-success');
    var successCloseBtn = document.getElementById('email-capture-success-close');
    var eyebrow = document.getElementById('email-capture-eyebrow');
    var eyebrowText = document.getElementById('email-capture-eyebrow-text');
    var titleEl = document.getElementById('email-capture-title');
    var bodyEl = document.getElementById('email-capture-body');
    var submitTextEl = document.getElementById('email-capture-submit-text');
    var successBodyEl = document.getElementById('email-capture-success-body');

    function openModal(variant) {
      var copy = EMAIL_CAPTURE_COPY[variant] || EMAIL_CAPTURE_COPY.subscriber;
      eyebrow.classList.toggle('is-crew', variant === 'crew');
      eyebrowText.textContent = copy.eyebrow;
      titleEl.textContent = copy.title;
      bodyEl.textContent = copy.body;
      submitTextEl.textContent = copy.submitText;
      successBodyEl.textContent = copy.successBody;
      // Reset form/success states
      if (form) form.style.display = '';
      if (successBlock) successBlock.setAttribute('hidden', '');
      // Store variant on the modal for the submit handler
      modal.setAttribute('data-active-variant', variant);
      modal.removeAttribute('hidden');
      // Focus first field for accessibility
      setTimeout(function() {
        var firstInput = document.getElementById('email-capture-name');
        if (firstInput) firstInput.focus();
      }, 100);
    }

    function closeModal() {
      modal.setAttribute('hidden', '');
    }

    // Trigger from any [data-email-capture] element
    document.addEventListener('click', function(ev) {
      var trigger = ev.target.closest('[data-email-capture]');
      if (trigger) {
        ev.preventDefault();
        ev.stopPropagation();
        var variant = trigger.getAttribute('data-email-capture') || 'subscriber';
        openModal(variant);
      }
    });

    // Phase 10 C2 — Subscribe buttons on the pricing page.
    // Any element with [data-subscribe-tier] kicks off Stripe Checkout
    // for the named tier. Backend creates the session; we redirect.
    document.addEventListener('click', function(ev) {
      var trigger = ev.target.closest('[data-subscribe-tier]');
      if (!trigger) return;
      ev.preventDefault();
      ev.stopPropagation();
      var tier = trigger.getAttribute('data-subscribe-tier');
      if (!tier) return;

      var BACKEND = 'https://wv-valet-backend.onrender.com';
      var originalLabel = trigger.innerHTML;
      trigger.disabled = true;
      trigger.textContent = 'Loading\u2026';

      fetch(BACKEND + '/api/v1/subscribe', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify({ tier: tier })
      })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          if (res.body && res.body.ok && res.body.url) {
            window.location.href = res.body.url;
          } else {
            trigger.disabled = false;
            trigger.innerHTML = originalLabel;
            var msg = (res.body && res.body.message) ||
                      'Couldn\u2019t start checkout. Please try again.';
            window.alert(msg);
          }
        })
        .catch(function() {
          trigger.disabled = false;
          trigger.innerHTML = originalLabel;
          window.alert('Network error. Please try again.');
        });
    });

    if (closeBtn) closeBtn.addEventListener('click', closeModal);
    if (successCloseBtn) successCloseBtn.addEventListener('click', closeModal);

    // Click on scrim (outside the modal card) closes it
    modal.addEventListener('click', function(ev) {
      if (ev.target === modal) closeModal();
    });

    // Escape key closes it
    document.addEventListener('keydown', function(ev) {
      if (ev.key === 'Escape' && !modal.hasAttribute('hidden')) closeModal();
    });

    // Submit — POSTs to the backend's /api/v1/email-capture endpoint,
    // which forwards the email to Mailchimp with a source-specific tag.
    // If the backend call fails (network, Render cold start, Mailchimp
    // down), we still show success to the user and stash the email in
    // localStorage as a fallback so we can recover it later.
    if (form) {
      form.addEventListener('submit', function(ev) {
        ev.preventDefault();
        var name = document.getElementById('email-capture-name').value.trim();
        var email = document.getElementById('email-capture-email').value.trim();
        var zip = document.getElementById('email-capture-zip').value.trim();
        var variant = modal.getAttribute('data-active-variant') || 'subscriber';

        // Map the modal variant to a Mailchimp tag. The frontend uses
        // simple variant labels ("subscriber", "crew"); Mailchimp uses
        // more specific tags. For the modal we map all subscriber-variant
        // captures to "general-interest" since the modal doesn't currently
        // know which CTA opened it. (If we later want pricing-tier-specific
        // tagging, we'd extend openModal() to accept a more specific tag.)
        var sourceTag = (variant === 'crew') ? 'crew-applicant' : 'general-interest';

        // Always stash in localStorage as a fallback — even if the backend
        // call succeeds. This gives us a recovery path if Mailchimp is
        // ever out of sync or if we need to audit captures later.
        try {
          var stash = JSON.parse(localStorage.getItem('wv-email-captures') || '[]');
          stash.push({
            variant: variant,
            name: name,
            email: email,
            zip: zip,
            sourceTag: sourceTag,
            ts: Date.now()
          });
          localStorage.setItem('wv-email-captures', JSON.stringify(stash));
        } catch (e) {
          // localStorage can fail in private browsing — don't break the flow
        }

        // POST to backend. Show success regardless of backend outcome —
        // user shouldn't see a broken experience if our infrastructure
        // has a hiccup. Backend errors get logged to console for debugging.
        var apiBase = (typeof window !== 'undefined' && window.WV_API_BASE) || '';
        if (apiBase) {
          fetch(apiBase + '/api/v1/email-capture', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email: email, source: sourceTag })
          })
          .then(function(res) {
            return res.json().catch(function() { return null; });
          })
          .then(function(data) {
            if (window.console && console.log) {
              console.log('[email-capture] backend response:', data);
            }
          })
          .catch(function(err) {
            // Network error or backend down. Email is in localStorage;
            // we can recover it. User sees success regardless.
            if (window.console && console.warn) {
              console.warn('[email-capture] backend failed (email saved locally):', err);
            }
          });
        }

        // Show success state immediately — don't make the user wait for
        // the backend response. The fetch above happens in the background.
        form.style.display = 'none';
        successBlock.removeAttribute('hidden');
      });
    }
  }

  // ── Sign-in modal setup — password-based auth (Phase 4) ──────────
  // User enters email + password → backend validates → session cookie
  // set → routed to workspace (or picker if multiple roles).
  //
  // The magic-link infrastructure is retained for password reset
  // (the "Forgot password?" flow) but is no longer the primary login.
  // Account creation happens via Stripe checkout, not through this
  // modal — the pricing cards on the homepage are the signup path.
  //
  // Backend endpoints used:
  //   POST /api/v1/auth/login               — username/password login (primary)
  //   GET  /api/v1/auth/session             — reads current session (page load)
  //   POST /api/v1/auth/logout              — destroys session
  //   POST /api/v1/auth/request-magic-link  — used for password reset only
  //   GET  /api/v1/auth/verify              — used for password reset only
  //
  // Modal state machine (data-step attribute on .signin-modal):
  //   "email"             — email + password form (default)
  //   "check-email"       — shown after password-reset request (deferred)
  //   "workspace-picker"  — shown after login when user has 2+ roles
  //   "account-pending"   — shown after verify when user has 0 roles
  //   "verify-error"      — shown on /auth/verify failure (expired/used)
  //
  // The admin workspace option is hidden by default; ?dev=1 reveals it.
  function setupSignInModal() {
    var trigger = document.getElementById('signin-pill-trigger');
    var modal = document.getElementById('signin-modal');
    if (!trigger || !modal) return;

    var modalCard = modal.querySelector('.signin-modal');
    var closeBtn = document.getElementById('signin-modal-close');
    var emailForm = document.getElementById('signin-email-form');
    var emailInput = document.getElementById('signin-email-input');
    var emailError = document.getElementById('signin-email-error');
    var submitBtn = document.getElementById('signin-email-submit');
    var submitLabel = submitBtn ? submitBtn.querySelector('.signin-submit-label') : null;
    var submitSpinner = submitBtn ? submitBtn.querySelector('.signin-submit-spinner') : null;
    var checkEmailAddress = document.getElementById('signin-check-email-address');
    var workspaceList = document.getElementById('signin-workspace-list');
    var welcomeName = document.getElementById('signin-welcome-name');

    // Backend base URL — production-deployed at api.weathervalet.ai or
    // wv-valet-backend.onrender.com. Frontend lives at weathervalet.ai.
    // We hard-code the backend URL here since the frontend is a static
    // site without a build-time config step.
    var BACKEND_URL = 'https://wv-valet-backend.onrender.com';

    // Reveal the admin workspace option if we're in dev mode.
    var params = new URLSearchParams(window.location.search);
    if (params.get('dev') === '1') {
      document.body.classList.add('has-dev-mode');
    }

    // Set the modal's current step. Hides all steps, shows the named one.
    function setStep(stepName) {
      if (!modalCard) return;
      modalCard.setAttribute('data-step', stepName);
      var steps = modalCard.querySelectorAll('[data-signin-step]');
      for (var i = 0; i < steps.length; i++) {
        if (steps[i].getAttribute('data-signin-step') === stepName) {
          steps[i].removeAttribute('hidden');
        } else {
          steps[i].setAttribute('hidden', '');
        }
      }
    }

    function openModal(initialStep) {
      modal.removeAttribute('hidden');
      setStep(initialStep || 'email');
      // Focus the email input for keyboard users (only on email step)
      setTimeout(function() {
        if ((initialStep || 'email') === 'email' && emailInput) {
          emailInput.focus();
        }
      }, 100);
    }

    function closeModal() {
      modal.setAttribute('hidden', '');
      // Reset to email step for next open, so users don't see the
      // previous state. UX detail: better that "open sign-in" always
      // shows a fresh email entry.
      setStep('email');
      if (emailInput) {
        emailInput.value = '';
        emailInput.removeAttribute('aria-busy');
      }
      // Clear password field too — security best practice.
      // Variable is declared further down in this function (after
      // emailForm initialization), so check before accessing.
      var pwInput = document.getElementById('signin-password-input');
      if (pwInput) pwInput.value = '';
      hideEmailError();
      setSubmitting(false);
    }

    function showEmailError(msg) {
      if (!emailError) return;
      emailError.textContent = msg;
      emailError.removeAttribute('hidden');
    }
    function hideEmailError() {
      if (!emailError) return;
      emailError.setAttribute('hidden', '');
    }

    function setSubmitting(isBusy) {
      if (!submitBtn) return;
      if (isBusy) {
        submitBtn.disabled = true;
        if (submitLabel) submitLabel.setAttribute('hidden', '');
        if (submitSpinner) submitSpinner.removeAttribute('hidden');
        if (emailInput) emailInput.setAttribute('aria-busy', 'true');
      } else {
        submitBtn.disabled = false;
        if (submitLabel) submitLabel.removeAttribute('hidden');
        if (submitSpinner) submitSpinner.setAttribute('hidden', '');
        if (emailInput) emailInput.removeAttribute('aria-busy');
      }
    }

    // ── Trigger: click the sign-in pill ──
    trigger.addEventListener('click', function(ev) {
      ev.preventDefault();
      openModal('email');
    });

    // ── Close: button, scrim click, escape key ──
    if (closeBtn) closeBtn.addEventListener('click', closeModal);
    modal.addEventListener('click', function(ev) {
      if (ev.target === modal) closeModal();
    });
    document.addEventListener('keydown', function(ev) {
      if (ev.key === 'Escape' && !modal.hasAttribute('hidden')) closeModal();
    });

    // Capture the password input (declared in the form alongside email)
    var passwordInput = document.getElementById('signin-password-input');

    // ── Form submission: POST to /auth/login (password-based auth) ──
    if (emailForm) {
      emailForm.addEventListener('submit', function(ev) {
        ev.preventDefault();
        hideEmailError();

        var email = (emailInput.value || '').trim();
        var password = passwordInput ? (passwordInput.value || '') : '';

        // Basic client-side validation. Real validation happens on the
        // backend; this just catches obvious garbage to give faster feedback.
        if (!email || email.indexOf('@') === -1 || email.indexOf('.') === -1) {
          showEmailError('Please enter a valid email address.');
          return;
        }
        if (!password) {
          showEmailError('Please enter your password.');
          return;
        }

        setSubmitting(true);

        fetch(BACKEND_URL + '/api/v1/auth/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ email: email, password: password })
        })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          setSubmitting(false);

          if (res.status === 200 && res.body && res.body.ok) {
            // Successful login. Backend set the session cookie. Now route
            // based on workspaces, same logic as the magic-link verify path.
            var workspaces = res.body.workspaces || [];
            var user = res.body.user || {};

            // Clear the password field for security (don't leave it in DOM)
            if (passwordInput) passwordInput.value = '';

            // Tell the auth-session module to update the pill (becomes
            // the user-aware variant with avatar and dropdown).
            if (window.__wvAuth && typeof window.__wvAuth.refreshFromLogin === 'function') {
              window.__wvAuth.refreshFromLogin(user, workspaces);
            }

            if (workspaces.length === 0) {
              setStep('account-pending');
            } else if (workspaces.length === 1) {
              // Route to the workspace's prototype screen. The backend's
              // workspace URL is a placeholder; the real navigation goes
              // through the frontend's screen-switching mechanism using
              // the role-to-screen map exposed by setupAuthSession.
              closeModal();
              var screenMap = (window.__wvAuth && window.__wvAuth.WORKSPACE_SCREEN_MAP) || {};
              var screenId = screenMap[workspaces[0].role];
              if (screenId && typeof window.showScreen === 'function') {
                window.showScreen(screenId);
              } else if (screenId) {
                // Fallback: synthetic click on a dummy element with
                // data-show-screen, same pattern the dev menu uses.
                var dummy = document.createElement('button');
                dummy.setAttribute('data-show-screen', screenId);
                dummy.style.display = 'none';
                document.body.appendChild(dummy);
                dummy.click();
                document.body.removeChild(dummy);
              }
            } else {
              renderWorkspacePicker(workspaces, user.name);
              setStep('workspace-picker');
            }
            return;
          }

          if (res.status === 429) {
            showEmailError('Too many failed attempts. Wait 15 minutes and try again.');
            return;
          }

          // 403 — account has been deactivated (subscription cancelled,
          // banned, etc.). Backend returns error='account-deactivated'.
          if (res.status === 403 && res.body && res.body.error === 'account-deactivated') {
            showEmailError('This account has been deactivated. If this is a mistake, please contact support.');
            return;
          }

          // 400 or 401 — generic error to prevent enumeration
          // (backend uses same response for "no such user" vs "wrong password")
          showEmailError('Email or password is incorrect. Please try again.');
        })
        .catch(function(err) {
          // Network error — backend unreachable.
          setSubmitting(false);
          showEmailError('Could not reach the server. Please check your connection and try again.');
          console.error('[signin] login failed:', err);
        });
      });
    }

    // ── "Forgot password?" link — opens the reset-request step
    // ── where the user enters their email and gets a reset link
    // ── delivered by Resend. The full flow:
    // ──   1. User clicks "Forgot password?" → reset-request step shows
    // ──   2. User submits email → POST /api/v1/auth/request-magic-link
    // ──      with intent=password-reset
    // ──   3. Backend sends reset email via Resend
    // ──   4. User clicks link in email → lands back here with
    // ──      ?auth=verify&token=...&intent=password-reset in URL
    // ──   5. Page-load detector calls /api/v1/auth/verify → signs in
    // ──   6. Frontend shows set-password step
    // ──   7. User submits new password → POST /api/v1/auth/set-password
    // ──   8. User is routed to their workspace
    modal.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-signin-action="forgot-password"]');
      if (!btn) return;
      ev.preventDefault();
      setStep('reset-request');
      // Carry over the email if the user typed one on the sign-in step
      var resetInput = document.getElementById('signin-reset-email-input');
      if (resetInput && emailInput && emailInput.value) {
        resetInput.value = emailInput.value;
      }
      setTimeout(function() {
        if (resetInput) resetInput.focus();
      }, 80);
    });

    // ── Reset-request form: POST to request-magic-link with intent=password-reset
    var resetForm = document.getElementById('signin-reset-form');
    if (resetForm) {
      resetForm.addEventListener('submit', function(ev) {
        ev.preventDefault();
        var resetEmailInput = document.getElementById('signin-reset-email-input');
        var resetErr = document.getElementById('signin-reset-error');
        var resetBtn = document.getElementById('signin-reset-submit');
        var resetLabel = resetBtn ? resetBtn.querySelector('.signin-submit-label') : null;
        var resetSpinner = resetBtn ? resetBtn.querySelector('.signin-submit-spinner') : null;
        if (resetErr) resetErr.setAttribute('hidden', '');

        var email = resetEmailInput ? (resetEmailInput.value || '').trim() : '';
        if (!email || email.indexOf('@') === -1 || email.indexOf('.') === -1) {
          if (resetErr) {
            resetErr.textContent = 'Please enter a valid email address.';
            resetErr.removeAttribute('hidden');
          }
          return;
        }

        // Busy state
        if (resetBtn) resetBtn.disabled = true;
        if (resetLabel) resetLabel.setAttribute('hidden', '');
        if (resetSpinner) resetSpinner.removeAttribute('hidden');

        fetch(BACKEND_URL + '/api/v1/auth/request-magic-link', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ email: email, intent: 'password-reset' })
        })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          // Reset busy state
          if (resetBtn) resetBtn.disabled = false;
          if (resetLabel) resetLabel.removeAttribute('hidden');
          if (resetSpinner) resetSpinner.setAttribute('hidden', '');

          if (res.status === 200 && res.body && res.body.ok) {
            // Show the existing "check your email" step. Populate the
            // email-address span if it exists.
            var addrEl = document.getElementById('signin-check-email-address');
            if (addrEl) addrEl.textContent = email;
            // Update copy for the password-reset intent — the same step
            // is reused for both magic-link sign-in and password-reset,
            // but the language needs to differ. The body element doesn't
            // have an ID, so we grab it via its parent.
            try {
              var titleEl = document.querySelector('[data-signin-step="check-email"] .signin-modal-title');
              var bodyEl = document.querySelector('[data-signin-step="check-email"] .signin-modal-body');
              if (titleEl) titleEl.textContent = 'Check your email';
              if (bodyEl) {
                bodyEl.innerHTML = 'We sent a password-reset link to <strong id="signin-check-email-address">'
                                 + email.replace(/[&<>"']/g, function(c){
                                     return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];
                                   })
                                 + '</strong>. Click the link in that email to set a new password. '
                                 + 'The link expires in <strong>15 minutes</strong>.';
              }
            } catch (e) {}
            setStep('check-email');
          } else if (res.status === 429) {
            if (resetErr) {
              resetErr.textContent = 'Too many requests. Try again in a few minutes.';
              resetErr.removeAttribute('hidden');
            }
          } else {
            if (resetErr) {
              resetErr.textContent = 'Something went wrong. Please try again.';
              resetErr.removeAttribute('hidden');
            }
          }
        })
        .catch(function() {
          if (resetBtn) resetBtn.disabled = false;
          if (resetLabel) resetLabel.removeAttribute('hidden');
          if (resetSpinner) resetSpinner.setAttribute('hidden', '');
          if (resetErr) {
            resetErr.textContent = 'Couldn\u2019t reach the server. Check your connection.';
            resetErr.removeAttribute('hidden');
          }
        });
      });
    }

    // ── Set-password form: POST to set-password (after successful verify)
    var setpwForm = document.getElementById('signin-setpw-form');
    if (setpwForm) {
      setpwForm.addEventListener('submit', function(ev) {
        ev.preventDefault();
        var pwInput = document.getElementById('signin-setpw-input');
        var pwConfirm = document.getElementById('signin-setpw-confirm');
        var setpwErr = document.getElementById('signin-setpw-error');
        var setpwBtn = document.getElementById('signin-setpw-submit');
        var setpwLabel = setpwBtn ? setpwBtn.querySelector('.signin-submit-label') : null;
        var setpwSpinner = setpwBtn ? setpwBtn.querySelector('.signin-submit-spinner') : null;
        if (setpwErr) setpwErr.setAttribute('hidden', '');

        var pw = pwInput ? (pwInput.value || '') : '';
        var pwc = pwConfirm ? (pwConfirm.value || '') : '';

        if (pw.length < 8) {
          if (setpwErr) {
            setpwErr.textContent = 'Password must be at least 8 characters.';
            setpwErr.removeAttribute('hidden');
          }
          return;
        }
        if (pw !== pwc) {
          if (setpwErr) {
            setpwErr.textContent = 'Passwords don\u2019t match.';
            setpwErr.removeAttribute('hidden');
          }
          return;
        }

        if (setpwBtn) setpwBtn.disabled = true;
        if (setpwLabel) setpwLabel.setAttribute('hidden', '');
        if (setpwSpinner) setpwSpinner.removeAttribute('hidden');

        fetch(BACKEND_URL + '/api/v1/auth/set-password', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ new_password: pw })
        })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          if (setpwBtn) setpwBtn.disabled = false;
          if (setpwLabel) setpwLabel.removeAttribute('hidden');
          if (setpwSpinner) setpwSpinner.setAttribute('hidden', '');

          if (res.status === 200 && res.body && res.body.ok) {
            // Password set. Clear inputs for security.
            if (pwInput) pwInput.value = '';
            if (pwConfirm) pwConfirm.value = '';

            // The user is already signed in (verify created the session).
            // Route them to their workspace using the same logic the
            // sign-in flow uses.
            var pendingWorkspaces = window.__wvPendingWorkspaces || [];
            var pendingUser = window.__wvPendingUser || null;
            if (pendingUser && window.__wvAuth && typeof window.__wvAuth.refreshFromLogin === 'function') {
              try { window.__wvAuth.refreshFromLogin(pendingUser, pendingWorkspaces); } catch (e) {}
            }
            window.__wvPendingWorkspaces = null;
            window.__wvPendingUser = null;

            // Route based on workspaces
            if (pendingWorkspaces.length === 1) {
              var screenMap = (window.__wvAuth && window.__wvAuth.WORKSPACE_SCREEN_MAP) || {};
              var screenId = screenMap[pendingWorkspaces[0].role];
              closeModal();
              if (screenId && typeof window.showScreen === 'function') {
                window.showScreen(screenId);
              }
            } else if (pendingWorkspaces.length >= 2) {
              renderWorkspacePicker(pendingWorkspaces, pendingUser ? pendingUser.name : '');
              setStep('workspace-picker');
            } else {
              closeModal();
            }
          } else if (res.status === 400 && res.body && res.body.error === 'password-too-short') {
            if (setpwErr) {
              setpwErr.textContent = 'Password must be at least 8 characters.';
              setpwErr.removeAttribute('hidden');
            }
          } else if (res.status === 401) {
            if (setpwErr) {
              setpwErr.textContent = 'Your session expired. Request a new reset link and try again.';
              setpwErr.removeAttribute('hidden');
            }
          } else {
            if (setpwErr) {
              setpwErr.textContent = 'Something went wrong. Please try again.';
              setpwErr.removeAttribute('hidden');
            }
          }
        })
        .catch(function() {
          if (setpwBtn) setpwBtn.disabled = false;
          if (setpwLabel) setpwLabel.removeAttribute('hidden');
          if (setpwSpinner) setpwSpinner.setAttribute('hidden', '');
          if (setpwErr) {
            setpwErr.textContent = 'Couldn\u2019t reach the server. Check your connection.';
            setpwErr.removeAttribute('hidden');
          }
        });
      });
    }

    // ── "Try a different email" / "Request a new link" → go back to email step ──
    modal.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-signin-action="back-to-email"]');
      if (!btn) return;
      ev.preventDefault();
      setStep('email');
      if (emailInput) emailInput.focus();
    });

    // ── Workspace picker: click a workspace → navigate to it ──
    // The workspace buttons are rendered dynamically by renderWorkspacePicker()
    // after a successful login. Each button has data-signin-workspace-role
    // identifying which workspace to route to. We use the role-to-screen
    // map (exposed by setupAuthSession on window.__wvAuth) to switch to
    // the corresponding prototype screen.
    modal.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-signin-workspace-role]');
      if (!btn) return;
      ev.preventDefault();
      var role = btn.getAttribute('data-signin-workspace-role');
      var screenMap = (window.__wvAuth && window.__wvAuth.WORKSPACE_SCREEN_MAP) || {};
      var screenId = screenMap[role];
      if (!screenId) return;
      closeModal();
      if (typeof window.showScreen === 'function') {
        window.showScreen(screenId);
      } else {
        // Fallback: synthetic click pattern
        var dummy = document.createElement('button');
        dummy.setAttribute('data-show-screen', screenId);
        dummy.style.display = 'none';
        document.body.appendChild(dummy);
        dummy.click();
        document.body.removeChild(dummy);
      }
    });

    // ── Render workspace picker from backend's workspaces array ──
    // Called after /auth/verify or /auth/session returns 2+ workspaces.
    function renderWorkspacePicker(workspaces, userName) {
      if (!workspaceList) return;

      // Welcome message uses the user's name if available, else just generic.
      if (welcomeName) {
        welcomeName.textContent = userName ? (', ' + userName.split(' ')[0]) : '';
      }

      // Workspace icons (reused from the v1 hard-coded buttons)
      var ICONS = {
        subscriber: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>',
        crew: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>',
        met: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>',
        admin: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>',
      };
      var DETAILS = {
        subscriber: 'Your forecasts, plans, and account',
        crew: 'Active missions and recent receipts',
        met: 'Daily brief composer · review queue · missions',
        admin: 'Command center, subscribers, Mets, missions',
      };

      var html = '';
      for (var i = 0; i < workspaces.length; i++) {
        var ws = workspaces[i];
        var iconHtml = ICONS[ws.role] || '';
        var detailText = DETAILS[ws.role] || '';
        html += '<button type="button" class="signin-workspace-btn" data-signin-workspace-role="' + ws.role + '">'
              + '<span class="signin-workspace-icon is-' + ws.role + '">' + iconHtml + '</span>'
              + '<div class="signin-workspace-info">'
              + '<p class="signin-workspace-name">' + ws.label + '</p>'
              + '<p class="signin-workspace-detail">' + detailText + '</p>'
              + '</div>'
              + '<svg class="signin-workspace-arrow" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>'
              + '</button>';
      }
      workspaceList.innerHTML = html;
    }

    // ── Magic-link verify handler ──
    // Runs on page load. If the URL is /?auth=verify&token=..., call the
    // backend to consume the token. The response sets the session cookie
    // and tells us where to route the user.
    //
    // Using query params (?auth=verify&token=...) instead of a path
    // (/auth/verify?token=...) because the static site doesn't have
    // rewrite infrastructure to map arbitrary paths to index.html. The
    // query-param approach works on any URL the static site serves,
    // including the root path which always loads index.html.
    function checkForMagicLinkVerify() {
      var search = new URLSearchParams(window.location.search);
      var authAction = search.get('auth');
      var token = search.get('token');
      // Chunk: password-reset flow. The intent is round-tripped from
      // the original /request-magic-link call so the frontend knows
      // whether to route the user straight to their workspace (sign-in)
      // or stop on the set-password step (password-reset).
      var intent = search.get('intent') || 'sign-in';

      // Only run when the URL signals an auth-verify intent with a token.
      if (authAction !== 'verify' || !token) return;

      // Show modal in a "verifying" state. We reuse the check-email
      // step's centered layout temporarily — feels right to show
      // progress, not just a blank page.
      openModal('check-email');
      if (checkEmailAddress) checkEmailAddress.textContent = 'Verifying your link…';

      fetch(BACKEND_URL + '/api/v1/auth/verify?token=' + encodeURIComponent(token), {
        method: 'GET',
        credentials: 'include'
      })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        // Clean the token from the URL regardless of outcome — we don't
        // want the magic link sitting in browser history.
        window.history.replaceState({}, '', '/');

        if (res.status === 200 && res.body && res.body.ok) {
          // Success — session cookie was set by the backend. Now route
          // based on intent + workspace count.
          var workspaces = res.body.workspaces || [];
          var user = res.body.user || {};

          if (intent === 'password-reset' || intent === 'new-account') {
            // Password-reset and new-account paths both end on the
            // set-password step. The user is signed in (verify created the
            // session) but we want them to set a password before they
            // continue. Stash the user+workspaces for the set-password
            // handler to consume after they submit.
            //
            // The difference between the two intents is purely cosmetic:
            // the email subject and copy differ ("reset your password" vs
            // "welcome — set your password"), but the in-app flow is the
            // same: type a password, sign in, route to workspace.
            window.__wvPendingUser = user;
            window.__wvPendingWorkspaces = workspaces;
            setStep('set-password');
            setTimeout(function() {
              var pwInput = document.getElementById('signin-setpw-input');
              if (pwInput) pwInput.focus();
            }, 80);
            return;
          }

          // Standard sign-in path: tell the auth-session module to
          // update the pill and route the user to their workspace.
          if (window.__wvAuth && typeof window.__wvAuth.refreshFromLogin === 'function') {
            window.__wvAuth.refreshFromLogin(user, workspaces);
          }

          if (workspaces.length === 0) {
            setStep('account-pending');
          } else if (workspaces.length === 1) {
            // Single workspace — route to the prototype screen via the
            // role-to-screen map (same as the password-login path).
            closeModal();
            var screenMap = (window.__wvAuth && window.__wvAuth.WORKSPACE_SCREEN_MAP) || {};
            var screenId = screenMap[workspaces[0].role];
            if (screenId && typeof window.showScreen === 'function') {
              window.showScreen(screenId);
            } else if (screenId) {
              var dummy = document.createElement('button');
              dummy.setAttribute('data-show-screen', screenId);
              dummy.style.display = 'none';
              document.body.appendChild(dummy);
              dummy.click();
              document.body.removeChild(dummy);
            }
          } else {
            // Multiple workspaces — show picker.
            renderWorkspacePicker(workspaces, user.name);
            setStep('workspace-picker');
          }
        } else if (res.status === 410) {
          // Token expired or already used.
          var errorTitle = document.getElementById('signin-verify-error-title');
          var errorBody = document.getElementById('signin-verify-error-body');
          if (res.body.error === 'already-used') {
            if (errorTitle) errorTitle.textContent = 'Link already used';
            if (errorBody) errorBody.textContent = 'This sign-in link has already been used. For your security, each link can only be used once. Request a new one.';
          } else {
            if (errorTitle) errorTitle.textContent = 'Link expired';
            if (errorBody) errorBody.textContent = 'This sign-in link is no longer valid. Links expire 15 minutes after they are sent. Request a new one.';
          }
          setStep('verify-error');
        } else if (res.status === 403 && res.body && res.body.error === 'account-deactivated') {
          // Account was deactivated (subscription cancelled, banned, etc.).
          // The magic link still verified successfully, but we won't let
          // them in. Same UI surface as expired-link, different copy.
          var errorTitleD = document.getElementById('signin-verify-error-title');
          var errorBodyD = document.getElementById('signin-verify-error-body');
          if (errorTitleD) errorTitleD.textContent = 'Account deactivated';
          if (errorBodyD) errorBodyD.textContent = 'This account has been deactivated. If this is a mistake, please contact support.';
          setStep('verify-error');
        } else {
          // 401 invalid-token or any other error
          setStep('verify-error');
        }
      })
      .catch(function(err) {
        window.history.replaceState({}, '', '/');
        setStep('verify-error');
        console.error('[signin] verify failed:', err);
      });
    }

    // Run the verify handler on page load
    checkForMagicLinkVerify();
  }

  // ── Session restoration + logout (Phase 4 Chunk 2) ─────────────────
  // Runs on every page load. Calls GET /api/v1/auth/session to check if
  // a session cookie is valid. If so, transforms the "Sign in" pill into
  // a logged-in dropdown showing the user's email and a sign-out option.
  //
  // The pill at the top-right has two modes:
  //   - Not logged in: brand-blue pill saying "Sign in" — opens the modal
  //   - Logged in: white pill with user's initial + email — opens the menu
  //
  // The dropdown menu is a separate element (#signin-pill-menu) that
  // overlays the page. It's positioned via CSS to appear below the pill.
  function setupAuthSession() {
    var pill = document.getElementById('signin-pill-trigger');
    var pillContent = document.getElementById('signin-pill-content');
    var menu = document.getElementById('signin-pill-menu');
    var menuName = document.getElementById('signin-pill-menu-name');
    var menuRoles = document.getElementById('signin-pill-menu-roles');
    var signoutBtn = document.getElementById('signin-pill-signout');

    if (!pill || !pillContent || !menu || !menuName || !menuRoles || !signoutBtn) {
      // Page doesn't have the auth UI elements (probably a different
      // screen). Bail silently.
      return;
    }

    var BACKEND_URL = 'https://wv-valet-backend.onrender.com';

    // Workspace mapping: backend returns roles, we map to prototype screens.
    // Backend's _roles_to_workspaces returns URL placeholders like '/portal'
    // but those aren't real routes — the frontend is a single-page app
    // with internal screen IDs. This map is what actually navigates.
    var WORKSPACE_SCREEN_MAP = {
      subscriber: 'screen-subscriber-portal',
      crew: 'screen-crew-ws',
      met: 'screen-met',
      admin: 'screen-admin'
    };

    // Track whether the user is currently authed (drives pill behavior).
    var currentUser = null;
    var currentWorkspaces = [];

    // ── Pill renderers ──
    function renderPillSignedOut() {
      pill.classList.remove('is-authed');
      pillContent.innerHTML = '<span>Sign in</span>';
    }

    function renderPillSignedIn(user) {
      pill.classList.add('is-authed');
      // First initial as avatar — uppercase first letter of name or email
      var initial = '?';
      if (user.name) {
        initial = user.name.charAt(0).toUpperCase();
      } else if (user.email) {
        initial = user.email.charAt(0).toUpperCase();
      }
      // Short display: name if available, else email local part. Capitalize
      // the first letter so "michael" renders as "Michael" — matches the
      // convention every well-designed product uses (Stripe, Linear, Notion).
      // We only capitalize the first char; the rest stays as the user typed
      // it, so "alex.lee" stays "Alex.lee" (not aggressively title-cased).
      var displayName = user.name || (user.email || '').split('@')[0];
      if (displayName.length > 0) {
        displayName = displayName.charAt(0).toUpperCase() + displayName.slice(1);
      }
      // Truncate long names to keep pill compact
      if (displayName.length > 20) displayName = displayName.substring(0, 18) + '…';
      pillContent.innerHTML = '<span class="signin-pill-avatar">' + initial + '</span><span>' + displayName + '</span>';
    }

    function renderMenu(user, workspaces) {
      menuName.textContent = user.email || 'unknown';
      if (workspaces.length === 0) {
        menuRoles.textContent = 'Account pending — no roles yet';
      } else {
        // List roles, capitalized via CSS
        menuRoles.textContent = workspaces.map(function(w) { return w.label; }).join(' \u00b7 ');
      }

      // Workspace nav links — one per role the user has. Clicking takes
      // them to that workspace's screen. (Added for Phase 4 / team panel.)
      var wsContainer = document.getElementById('signin-pill-menu-workspaces');
      if (wsContainer) {
        if (!workspaces.length) {
          wsContainer.innerHTML = '';
        } else {
          var html = '';
          for (var i = 0; i < workspaces.length; i++) {
            var w = workspaces[i];
            var screenId = WORKSPACE_SCREEN_MAP[w.role] || '';
            if (!screenId) continue;
            // Icon: simple monogram from the label (first 2 letters)
            var iconMono = (w.label || w.role || '?').slice(0, 2).toUpperCase();
            html += '<button type="button" class="signin-pill-menu-item" '
                  + 'data-workspace-screen="' + screenId + '" '
                  + 'data-workspace-role="' + w.role + '">'
                  +   '<span class="signin-pill-menu-workspace-icon">' + iconMono + '</span>'
                  +   '<span>' + (w.label || w.role) + '</span>'
                  + '</button>';
          }
          wsContainer.innerHTML = html;
          // Wire each button's click → navigate to that screen
          var btns = wsContainer.querySelectorAll('[data-workspace-screen]');
          btns.forEach(function(btn) {
            btn.addEventListener('click', function() {
              var screenId = btn.getAttribute('data-workspace-screen');
              hideMenu();
              if (screenId && typeof window.showScreen === 'function') {
                window.showScreen(screenId);
              } else if (screenId) {
                // Fallback: trigger via the existing data-show-screen handler
                var dummy = document.createElement('button');
                dummy.setAttribute('data-show-screen', screenId);
                dummy.style.display = 'none';
                document.body.appendChild(dummy);
                dummy.click();
                document.body.removeChild(dummy);
              }
            });
          });
        }
      }
    }

    function showMenu() { menu.removeAttribute('hidden'); }
    function hideMenu() { menu.setAttribute('hidden', ''); }

    // ── Session check on page load ──
    function checkSession() {
      fetch(BACKEND_URL + '/api/v1/auth/session', {
        method: 'GET',
        credentials: 'include'
      })
      .then(function(r) {
        if (r.status === 401) {
          // Not logged in — keep the default Sign in pill, no-op
          return null;
        }
        return r.json().then(function(j) { return { status: r.status, body: j }; });
      })
      .then(function(res) {
        if (!res || !res.body || !res.body.ok) {
          renderPillSignedOut();
          return;
        }
        currentUser = res.body.user || {};
        currentWorkspaces = res.body.workspaces || [];
        renderPillSignedIn(currentUser);
        renderMenu(currentUser, currentWorkspaces);
        // Personalize workspace welcome headers now that we have the user.
        try { if (typeof setMetWelcome === 'function') setMetWelcome(); } catch (e) {}
        try { if (typeof setAdminWelcome === 'function') setAdminWelcome(); } catch (e) {}
        try { if (typeof setCrewWorkspaceWelcome === 'function') setCrewWorkspaceWelcome(); } catch (e) {}
        // Phase 5b: pull the user's verify history from the backend.
        // Replaces what used to come from localStorage only.
        try { if (typeof loadVerifiesFromBackend === 'function') loadVerifiesFromBackend(); } catch (e) {}
      })
      .catch(function(err) {
        // Network error or unexpected response — silently keep default pill
        console.warn('[auth-session] check failed:', err);
        renderPillSignedOut();
      });
    }

    // ── Pill click: open modal (signed out) or menu (signed in) ──
    // Note: the existing modal trigger handler is set up by setupSignInModal().
    // We need to PREVENT that handler from running when the user is authed
    // — instead, show the dropdown menu.
    pill.addEventListener('click', function(ev) {
      if (pill.classList.contains('is-authed')) {
        // Stop the modal from opening
        ev.stopImmediatePropagation();
        ev.preventDefault();
        if (menu.hasAttribute('hidden')) {
          showMenu();
        } else {
          hideMenu();
        }
      }
      // If not authed, let the modal trigger handler from setupSignInModal run
    }, true);  // useCapture = true so this runs BEFORE the modal handler

    // ── Sign-out click ──
    signoutBtn.addEventListener('click', function(ev) {
      ev.preventDefault();
      // Disable the button to prevent double-clicks
      signoutBtn.disabled = true;
      signoutBtn.style.opacity = '0.5';

      fetch(BACKEND_URL + '/api/v1/auth/logout', {
        method: 'POST',
        credentials: 'include'
      })
      .then(function() {
        // Whether the request succeeded or failed, we clear our local state.
        // Logout is idempotent server-side; if it failed we still want the
        // user to see the signed-out state in the UI.
        currentUser = null;
        currentWorkspaces = [];
        hideMenu();
        renderPillSignedOut();
        // Reload to clear any cached UI state (e.g., if user was viewing
        // a workspace screen, they should land back on the homepage).
        window.location.reload();
      })
      .catch(function(err) {
        console.error('[auth-session] logout failed:', err);
        // Even on network failure, hide menu and show signed-out state
        currentUser = null;
        currentWorkspaces = [];
        hideMenu();
        renderPillSignedOut();
        signoutBtn.disabled = false;
        signoutBtn.style.opacity = '';
      });
    });

    // ── Close menu when clicking outside ──
    document.addEventListener('click', function(ev) {
      if (menu.hasAttribute('hidden')) return;
      if (ev.target.closest('#signin-pill-menu')) return;
      if (ev.target.closest('#signin-pill-trigger')) return;
      hideMenu();
    });

    // ── Close menu on Escape ──
    document.addEventListener('keydown', function(ev) {
      if (ev.key === 'Escape' && !menu.hasAttribute('hidden')) {
        hideMenu();
      }
    });

    // Expose a workspace-routing helper so the modal's renderWorkspacePicker
    // can use the SCREEN map instead of window.location.href. We attach it
    // to a global namespace so the modal code can find it.
    window.__wvAuth = {
      WORKSPACE_SCREEN_MAP: WORKSPACE_SCREEN_MAP,
      // Read the currently signed-in user (or null). Allows other code
      // (workspace welcome headers, name displays) to personalize without
      // re-fetching /auth/session every render.
      getCurrentUser: function() { return currentUser; },
      // After successful login, the modal calls this to refresh the pill
      refreshFromLogin: function(user, workspaces) {
        currentUser = user;
        currentWorkspaces = workspaces || [];
        renderPillSignedIn(currentUser);
        renderMenu(currentUser, currentWorkspaces);
        // Re-render any welcome strings that depend on the user.
        try { if (typeof setMetWelcome === 'function') setMetWelcome(); } catch (e) {}
        try { if (typeof setAdminWelcome === 'function') setAdminWelcome(); } catch (e) {}
        try { if (typeof setCrewWorkspaceWelcome === 'function') setCrewWorkspaceWelcome(); } catch (e) {}
        // Phase 5b: pull verify history fresh on sign-in.
        try { if (typeof loadVerifiesFromBackend === 'function') loadVerifiesFromBackend(); } catch (e) {}
      }
    };

    // Kick off the session check
    checkSession();
  }

  // ── Rotating headline word ──────────────────────────────────────────
  // Cycles the word inside "Precise weather for your exact ___." every
  // few seconds. Words slide up and fade in/out. Order is shuffled on
  // each page load so returning visitors don't see the same sequence.
  //
  // The pool of words spans different use cases: contractors (jobsite),
  // hosts (event, party), parents (peace of mind), commuters (commute),
  // vacationers (vacation, weekend getaway), and the catch-all (plans).
  // The seed word "plans" is what's in the HTML, so we start the cycle
  // by transitioning AWAY from it after a brief first-show delay.
  function setupRotatingHeadline() {
    var el = document.getElementById('rotating-word');
    if (!el) return;

    var WORDS = [
      'plans',
      'jobsite',
      'event',
      'party',
      'travels',
      'vacation',
      'activity',
      'commute',
      'peace of mind',
      'knowledge',
      'weekend getaway'
    ];

    // Fisher-Yates shuffle so each page load has a fresh order.
    // We keep "plans" (the SEO/copy-key word) somewhere in the cycle
    // but not necessarily first. The HTML already shows "plans" at
    // page load; the cycle then transitions to the shuffled order.
    function shuffle(arr) {
      var copy = arr.slice();
      for (var i = copy.length - 1; i > 0; i--) {
        var j = Math.floor(Math.random() * (i + 1));
        var tmp = copy[i]; copy[i] = copy[j]; copy[j] = tmp;
      }
      return copy;
    }

    var queue = shuffle(WORDS);
    var index = 0;

    // Timing knobs:
    //   VISIBLE_MS — how long each word is shown before starting to leave
    //   FADE_OUT_MS — duration of the leave animation (matches CSS)
    //   FADE_IN_MS — duration of the enter animation (matches CSS)
    // Total cycle is VISIBLE_MS + FADE_OUT_MS + FADE_IN_MS = ~3.9 sec.
    var VISIBLE_MS = 3200;
    var FADE_OUT_MS = 280;
    var FADE_IN_MS = 420;

    var nextTimer = null;
    var paused = false;

    // Move to the next word: animate the current one OUT, swap text,
    // animate the new one IN. The animations are mutually exclusive
    // via class swaps — the leaving animation finishes before we add
    // the entering class.
    function rotateOnce() {
      if (paused) return;
      el.classList.remove('is-entering');
      el.classList.add('is-leaving');

      // After the leave animation completes, swap the text and
      // animate the new word in.
      setTimeout(function() {
        // Pick the next word; loop back to the start of the queue
        // when we run out. Note we don't skip the word currently
        // shown — over many cycles it'll show again naturally.
        index = (index + 1) % queue.length;
        // If the next word is the same as the current displayed text
        // (can happen after first cycle when 'plans' comes around if
        // the seed was also 'plans'), skip to the one after.
        if (queue[index] === el.textContent) {
          index = (index + 1) % queue.length;
        }
        el.textContent = queue[index];
        el.classList.remove('is-leaving');
        el.classList.add('is-entering');
        // Schedule the next rotation after the entering animation
        // completes plus the visible time.
        nextTimer = setTimeout(rotateOnce, FADE_IN_MS + VISIBLE_MS);
      }, FADE_OUT_MS);
    }

    // Pause rotation when the tab isn't visible. Saves CPU and avoids
    // a jarring "swap moment" when the user comes back. We do this
    // via the visibilitychange event, which is the modern way to
    // detect tab focus changes.
    document.addEventListener('visibilitychange', function() {
      if (document.hidden) {
        paused = true;
        if (nextTimer) { clearTimeout(nextTimer); nextTimer = null; }
      } else if (paused) {
        paused = false;
        // Restart the cycle after a brief delay so the user has a
        // moment to orient before things start moving.
        nextTimer = setTimeout(rotateOnce, 800);
      }
    });

    // Pause when a modal is open (sign-in modal, email-capture modal).
    // We watch the body for the relevant scrim elements being visible
    // and pause/resume accordingly. Cheaper than a MutationObserver:
    // we check on the same visibilitychange event and on each rotation.
    function modalIsOpen() {
      var signin = document.getElementById('signin-modal');
      var email = document.querySelector('.email-capture-scrim');
      return (signin && !signin.hasAttribute('hidden'))
          || (email && !email.hasAttribute('hidden'));
    }

    // Wrap rotateOnce to check for modal open before running. If a
    // modal is open, we skip this cycle and try again later. Using
    // the same VISIBLE_MS interval keeps the cycle pace consistent.
    var originalRotate = rotateOnce;
    rotateOnce = function() {
      if (modalIsOpen()) {
        nextTimer = setTimeout(rotateOnce, VISIBLE_MS);
        return;
      }
      originalRotate();
    };

    // Kick off the first rotation after the initial visible time.
    // The seed word "plans" stays for VISIBLE_MS before we start
    // rotating to the shuffled queue.
    nextTimer = setTimeout(rotateOnce, VISIBLE_MS);
  }

  // ── Admin: Team management ─────────────────────────────────────────
  // Manages Met / Crew / Admin accounts in the Command Center's Team tab.
  // Phase 1 (this code): all data lives in-memory (window.TEAM_MEMBERS).
  // Phase 2 (after Render access returns): swap the four stub functions
  // (apiListMembers, apiAddMember, apiUpdateMember, apiDeactivateMember)
  // for real fetch() calls to /admin/users endpoints. The UI never has
  // to change — the contract is fixed.

  // ════════════════════════════════════════════════════════════════════
  // Subscriber portal — real data wiring (Phase 10 — May 14)
  // ════════════════════════════════════════════════════════════════════
  //
  // The portal HTML at #screen-subscriber-portal has hardcoded mock
  // values. This function replaces them with real data from the backend.
  // Fires on entry to the portal screen (via showScreen lazy-load).
  // Idempotent — re-entry just re-fetches.

  // Helper: format ms-since-epoch as "Month D, YYYY"
  function _portalFormatDate(ms) {
    if (!ms) return '\u2014';
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '\u2014';
    var months = ['January','February','March','April','May','June',
                  'July','August','September','October','November','December'];
    return months[d.getMonth()] + ' ' + d.getDate() + ', ' + d.getFullYear();
  }

  // Helper: format relative day label for history (Today/Yesterday/Wed/etc.)
  function _portalRelativeDay(ms) {
    if (!ms) return '\u2014';
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '\u2014';
    var now = new Date();
    var today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
    var that  = new Date(d.getFullYear(), d.getMonth(), d.getDate());
    var diffDays = Math.round((today - that) / (1000 * 60 * 60 * 24));
    if (diffDays === 0) return 'Today';
    if (diffDays === 1) return 'Yesterday';
    if (diffDays < 7) {
      return ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'][d.getDay()];
    }
    return _portalFormatDate(ms);
  }

  // Helper: format a time label "5:32 AM"
  function _portalFormatTime(ms) {
    if (!ms) return '\u2014';
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '\u2014';
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    return h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
  }

  // ════════════════════════════════════════════════════════════════════
  // Crew home base (Phase 10 Item #6) — load + edit
  // ════════════════════════════════════════════════════════════════════
  // Surfaces the crew_home_lat/lng/label/active fields on Crew Profile.
  // Edit modal mirrors the subscriber saved-location pattern: address
  // lookup via /api/v1/geocode, draggable Leaflet pin.
  //
  // Visibility: the card is hidden by default. It shows only after
  // GET /api/v1/me/crew-location returns ok=true (which means the user
  // has the crew role). If the user isn't crew (or isn't signed in),
  // the endpoint returns 403/401, the card stays hidden, no noise.

  var _crewHomeBase = null;  // most recent backend state
  var _crewHomeBaseMap = null;  // Leaflet instance
  var _crewHomeBaseMarker = null;
  var _crewHomeBaseMapInitFor = null;  // sentinel: have we created the map yet?

  function loadCrewHomeBase() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var card = $('crew-homebase-card');
    if (!card) return;

    fetch(BACKEND + '/api/v1/me/crew-location', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          // Not a Crew member, not signed in, or backend error. Keep
          // the card hidden — nothing to surface.
          card.hidden = true;
          return;
        }
        card.hidden = false;
        _crewHomeBase = res.body.crew_location || {};
        renderCrewHomeBase();
      })
      .catch(function() {
        card.hidden = true;
      });
  }

  function renderCrewHomeBase() {
    var loc = _crewHomeBase || {};
    var placeholderEl = $('crew-homebase-placeholder');
    var dataEl = $('crew-homebase-data');
    var labelEl = $('crew-homebase-label-text');
    var coordsEl = $('crew-homebase-coords');
    var activeEl = $('crew-homebase-active');

    var hasCoords = (typeof loc.lat === 'number' && typeof loc.lng === 'number');
    if (placeholderEl) placeholderEl.hidden = hasCoords;
    if (dataEl) dataEl.hidden = !hasCoords;

    if (hasCoords) {
      if (labelEl) labelEl.textContent = loc.label || 'Unlabeled location';
      if (coordsEl) coordsEl.textContent = loc.lat.toFixed(4) + ', ' + loc.lng.toFixed(4);
    }
    if (activeEl) activeEl.checked = !!loc.active;
  }

  function openCrewHomeBaseModal() {
    var modal = $('crew-homebase-modal');
    if (!modal) return;
    modal.hidden = false;
    modal.classList.add('is-open');

    // Pre-fill label + address if we have them
    var labelInput = $('crew-homebase-label-input');
    var addressInput = $('crew-homebase-address-input');
    if (labelInput) labelInput.value = (_crewHomeBase && _crewHomeBase.label) || '';
    if (addressInput) addressInput.value = '';

    // Initialize map (idempotent — Leaflet handles re-init OK if we
    // tear down properly between opens)
    setTimeout(function() { _initCrewHomeBaseMap(); }, 50);
  }

  function closeCrewHomeBaseModal() {
    var modal = $('crew-homebase-modal');
    if (!modal) return;
    modal.classList.remove('is-open');
    modal.hidden = true;
    // Don't destroy the map — Leaflet's re-init is cheap and tearing
    // it down on every close was adding 200ms of flicker. We just
    // reset the marker on next open.
  }

  function _initCrewHomeBaseMap() {
    var container = $('crew-homebase-modal-map');
    if (!container) return;
    if (!window.L) {
      // Leaflet not loaded yet. Defer 200ms and try once more.
      setTimeout(_initCrewHomeBaseMap, 200);
      return;
    }

    var startLat = (_crewHomeBase && _crewHomeBase.lat) || 39.8;
    var startLng = (_crewHomeBase && _crewHomeBase.lng) || -86.15;
    var startZoom = (_crewHomeBase && _crewHomeBase.lat) ? 13 : 8;

    if (!_crewHomeBaseMap || _crewHomeBaseMapInitFor !== container) {
      // First init (or re-init if container changed)
      if (_crewHomeBaseMap) {
        try { _crewHomeBaseMap.remove(); } catch (e) {}
      }
      _crewHomeBaseMap = L.map(container, {
        zoomControl: true,
        scrollWheelZoom: true,
      }).setView([startLat, startLng], startZoom);

      L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', {
        attribution: '\u00a9 OpenStreetMap, \u00a9 CARTO',
        maxZoom: 19,
      }).addTo(_crewHomeBaseMap);

      _crewHomeBaseMapInitFor = container;
    } else {
      _crewHomeBaseMap.setView([startLat, startLng], startZoom);
    }

    // Drop or move the marker
    if (_crewHomeBaseMarker) {
      _crewHomeBaseMarker.setLatLng([startLat, startLng]);
    } else {
      _crewHomeBaseMarker = L.marker([startLat, startLng], { draggable: true }).addTo(_crewHomeBaseMap);
    }

    // Force a redraw — Leaflet sometimes needs invalidateSize when the
    // container was hidden when the map was instantiated
    setTimeout(function() {
      try { _crewHomeBaseMap.invalidateSize(); } catch (e) {}
    }, 100);
  }

  function _lookupCrewHomeBaseAddress() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var addressInput = $('crew-homebase-address-input');
    var addr = addressInput ? addressInput.value.trim() : '';
    if (!addr) return;
    fetch(BACKEND + '/api/v1/geocode?q=' + encodeURIComponent(addr), { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (!res || !res.ok || !res.results || !res.results.length) {
          window.alert('Couldn\u2019t find that address. Try a more specific search like "Lebanon, IN" or a full street address.');
          return;
        }
        var hit = res.results[0];
        if (_crewHomeBaseMap && _crewHomeBaseMarker) {
          _crewHomeBaseMap.setView([hit.lat, hit.lng], 14);
          _crewHomeBaseMarker.setLatLng([hit.lat, hit.lng]);
        }
        // Auto-fill label if it's empty
        var labelInput = $('crew-homebase-label-input');
        if (labelInput && !labelInput.value.trim() && hit.label) {
          labelInput.value = hit.label;
        }
      })
      .catch(function() {
        window.alert('Address lookup failed. Try again.');
      });
  }

  function _saveCrewHomeBase() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var labelInput = $('crew-homebase-label-input');
    var saveBtn = $('crew-homebase-modal-save');
    if (!_crewHomeBaseMarker) {
      window.alert('Drop a pin on the map first (use the address lookup above).');
      return;
    }
    var pos = _crewHomeBaseMarker.getLatLng();
    var label = labelInput ? labelInput.value.trim() : '';

    saveBtn.disabled = true;
    saveBtn.textContent = 'Saving\u2026';

    fetch(BACKEND + '/api/v1/me/crew-location', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({
        lat: pos.lat,
        lng: pos.lng,
        label: label
      })
    })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        saveBtn.disabled = false;
        saveBtn.textContent = 'Save';
        if (res.body && res.body.ok) {
          _crewHomeBase = res.body.crew_location;
          renderCrewHomeBase();
          closeCrewHomeBaseModal();
          if (typeof showTipToast === 'function') {
            showTipToast('Home base saved');
          }
        } else {
          window.alert('Couldn\u2019t save. ' + (res.body && res.body.error ? '(' + res.body.error + ')' : ''));
        }
      })
      .catch(function() {
        saveBtn.disabled = false;
        saveBtn.textContent = 'Save';
        window.alert('Network error. Try again.');
      });
  }

  function _toggleCrewActive(active) {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/me/crew-location', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ active: !!active })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (res && res.ok) {
          _crewHomeBase = res.crew_location;
          if (typeof showTipToast === 'function') {
            showTipToast(active
              ? 'You\u2019re available for missions.'
              : 'Holding your pages \u2014 you won\u2019t get mission SMS until you flip this back on.');
          }
        }
      });
  }

  function setupCrewHomeBaseControls() {
    if (window._crewHomeBaseControlsWired) return;
    window._crewHomeBaseControlsWired = true;

    var editBtn = $('crew-homebase-edit');
    if (editBtn) editBtn.addEventListener('click', openCrewHomeBaseModal);

    var closeBtn = $('crew-homebase-modal-close');
    if (closeBtn) closeBtn.addEventListener('click', closeCrewHomeBaseModal);
    var cancelBtn = $('crew-homebase-modal-cancel');
    if (cancelBtn) cancelBtn.addEventListener('click', closeCrewHomeBaseModal);
    var scrim = $('crew-homebase-modal-scrim');
    if (scrim) scrim.addEventListener('click', closeCrewHomeBaseModal);

    var lookupBtn = $('crew-homebase-lookup-btn');
    if (lookupBtn) lookupBtn.addEventListener('click', _lookupCrewHomeBaseAddress);

    var addressInput = $('crew-homebase-address-input');
    if (addressInput) {
      addressInput.addEventListener('keydown', function(ev) {
        if (ev.key === 'Enter') {
          ev.preventDefault();
          _lookupCrewHomeBaseAddress();
        }
      });
    }

    var saveBtn = $('crew-homebase-modal-save');
    if (saveBtn) saveBtn.addEventListener('click', _saveCrewHomeBase);

    var activeToggle = $('crew-homebase-active');
    if (activeToggle) {
      activeToggle.addEventListener('change', function() {
        _toggleCrewActive(activeToggle.checked);
      });
    }
  }


  // ════════════════════════════════════════════════════════════════════
  // Mission response page (Phase 10 Item #5)
  // ════════════════════════════════════════════════════════════════════
  // When the user lands on /?mission=<token>, this fetches the mission
  // context and renders the response form. On submit, POSTs the response.

  function _missionResponseGetToken() {
    try {
      var sp = new URLSearchParams(window.location.search);
      return sp.get('mission');
    } catch (e) {
      return null;
    }
  }

  function checkForMissionResponse() {
    var token = _missionResponseGetToken();
    if (!token) return false;  // No token = not a mission response URL

    // Show the mission response screen, hide everything else
    showScreen('screen-mission-response');

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/missions/respond/' + encodeURIComponent(token))
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        var loadingEl = $('mission-response-loading');
        var errorEl = $('mission-response-error');
        var formEl = $('mission-response-form');
        if (loadingEl) loadingEl.hidden = true;
        if (!res.body || !res.body.ok) {
          if (errorEl) errorEl.hidden = false;
          var errorTextEl = $('mission-response-error-text');
          if (errorTextEl && res.body && res.body.error === 'not-found') {
            errorTextEl.textContent = 'This mission link is invalid or has expired.';
          }
          return;
        }
        var mission = res.body.mission || {};
        if (formEl) formEl.hidden = false;
        var fromEl = $('mission-response-from');
        var templateEl = $('mission-response-template');
        var promptEl = $('mission-response-prompt');
        var textareaEl = $('mission-response-textarea');
        if (fromEl) fromEl.textContent = mission.fired_by_name || 'your meteorologist';
        if (templateEl) templateEl.textContent = mission.template_name || 'Mission';
        if (promptEl) promptEl.textContent = mission.prompt || '';
        if (textareaEl && mission.already_responded) {
          textareaEl.value = mission.previous_response || '';
          // Soft acknowledgement that they already submitted
          var note = document.createElement('p');
          note.style.cssText = 'font-size: 12px; color: rgba(15,17,22,0.55); margin: 8px 0 0;';
          note.textContent = 'You\u2019ve already responded \u2014 editing will overwrite your previous answer.';
          textareaEl.parentNode.appendChild(note);
        }
      })
      .catch(function() {
        var loadingEl = $('mission-response-loading');
        var errorEl = $('mission-response-error');
        if (loadingEl) loadingEl.hidden = true;
        if (errorEl) errorEl.hidden = false;
      });

    return true;  // Token was present, we handled it
  }

  function setupMissionResponseSubmit() {
    if (window._missionResponseWired) return;
    window._missionResponseWired = true;

    var submitBtn = $('mission-response-submit');
    if (!submitBtn) return;

    submitBtn.addEventListener('click', function() {
      var token = _missionResponseGetToken();
      var textareaEl = $('mission-response-textarea');
      var text = textareaEl ? textareaEl.value.trim() : '';
      if (!token || !text) {
        window.alert('Please type your observation before submitting.');
        return;
      }
      submitBtn.disabled = true;
      submitBtn.textContent = 'Submitting\u2026';

      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/missions/respond/' + encodeURIComponent(token), {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ response_text: text, overwrite: true })
      })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          if (res.body && res.body.ok) {
            var formEl = $('mission-response-form');
            var successEl = $('mission-response-success');
            if (formEl) formEl.hidden = true;
            if (successEl) successEl.hidden = false;
          } else {
            submitBtn.disabled = false;
            submitBtn.textContent = 'Submit observation';
            window.alert('Couldn\u2019t submit. ' + ((res.body && res.body.error) || ''));
          }
        })
        .catch(function() {
          submitBtn.disabled = false;
          submitBtn.textContent = 'Submit observation';
          window.alert('Network error. Try again.');
        });
    });
  }


  // ════════════════════════════════════════════════════════════════════
  // NWS Met severe-alert page (Phase 10 Item #7 Chunk 7B)
  // ════════════════════════════════════════════════════════════════════
  // Triggered when URL has ?nws-page=<token>. Met taps SMS link, page
  // loads alert + affected subscribers, Met confirms (sends message to
  // subscribers) or dismisses (false alarm).

  function _nwsPageGetToken() {
    try {
      var sp = new URLSearchParams(window.location.search);
      return sp.get('nws-page');
    } catch (e) {
      return null;
    }
  }

  function _nwsFmtExpiry(ms) {
    if (!ms) return 'unknown';
    var d = new Date(ms);
    if (isNaN(d.getTime())) return 'unknown';
    var now = new Date();
    var mins = Math.round((ms - now.getTime()) / 60000);
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    var timeStr = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    if (mins < 0) return 'expired ' + Math.abs(mins) + ' min ago';
    if (mins < 60) return timeStr + ' (' + mins + ' min)';
    return timeStr;
  }

  function checkForNwsPage() {
    var token = _nwsPageGetToken();
    if (!token) return false;

    showScreen('screen-nws-page');

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/nws/page/' + encodeURIComponent(token), {
      credentials: 'include'
    })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        var loadingEl = $('nws-page-loading');
        var errorEl = $('nws-page-error');
        var reviewEl = $('nws-page-review');
        var handledEl = $('nws-page-handled');
        if (loadingEl) loadingEl.hidden = true;

        if (!res.body || !res.body.ok) {
          if (errorEl) errorEl.hidden = false;
          var errText = $('nws-page-error-text');
          if (errText && res.body && res.body.error === 'not-found') {
            errText.textContent = 'This page link is invalid or has expired.';
          }
          return;
        }

        var page = res.body.page || {};
        var subs = res.body.subscribers || [];

        // If already handled (confirmed or dismissed), show the
        // already-handled state with appropriate copy
        if (page.status === 'confirmed' || page.status === 'dismissed') {
          if (handledEl) handledEl.hidden = false;
          var t = $('nws-page-handled-title');
          var s = $('nws-page-handled-sub');
          if (page.status === 'confirmed') {
            if (t) t.textContent = 'Already confirmed';
            if (s) s.textContent = (page.reviewed_by_name || 'A meteorologist')
              + ' confirmed this alert. ' + (page.subscribers_notified || 0)
              + ' subscribers were notified.';
          } else {
            if (t) t.textContent = 'Already dismissed';
            if (s) s.textContent = (page.reviewed_by_name || 'A meteorologist')
              + ' dismissed this alert. Subscribers were not notified.';
          }
          return;
        }

        // Pending review — render the review surface
        if (reviewEl) reviewEl.hidden = false;
        var eventEl = $('nws-page-event');
        var severityEl = $('nws-page-severity');
        var expiresEl = $('nws-page-expires');
        var headlineEl = $('nws-page-headline');
        var areaEl = $('nws-page-area');
        var descEl = $('nws-page-description');
        var instrEl = $('nws-page-instruction');
        var countEl = $('nws-page-sub-count');
        var listEl = $('nws-page-sub-list');
        var metNameEl = $('nws-page-met-name');

        if (eventEl) eventEl.textContent = page.event || 'NWS Alert';
        if (severityEl) severityEl.textContent = page.severity || 'unknown';
        if (expiresEl) expiresEl.textContent = _nwsFmtExpiry(page.expires_at);
        if (headlineEl) headlineEl.textContent = page.headline || '(no headline provided)';
        if (areaEl) areaEl.textContent = page.area_desc || '(no area description)';
        if (descEl) descEl.textContent = page.description || '(no description)';
        if (instrEl) instrEl.textContent = page.instruction || '(no instructions provided)';
        if (countEl) countEl.textContent = subs.length;

        // Pre-fill Met name if signed in via session
        try {
          var sess = window.WVSession || null;
          if (metNameEl && sess && sess.name) metNameEl.value = sess.name;
        } catch (e) {}

        if (listEl) {
          if (subs.length === 0) {
            listEl.innerHTML = '<div style="padding:14px;text-align:center;color:rgba(15,17,22,0.55);font-size:12.5px;">No subscribers currently affected.</div>';
          } else {
            var html = '';
            for (var i = 0; i < subs.length; i++) {
              var s = subs[i];
              var tierLabel = s.tier === 'pro_multi' ? 'Pro Multi'
                            : s.tier === 'pro_enterprise' ? 'Pro Enterprise'
                            : 'Pro Single';
              html += '<div style="padding:10px 14px;border-bottom:1px solid rgba(15,17,22,0.06);display:flex;align-items:center;justify-content:space-between;gap:10px;">'
                +   '<div>'
                +     '<div style="font-size:13px;font-weight:500;color:#0E1116;">' + escapeHtml(s.name || s.email || 'Subscriber') + '</div>'
                +     '<div style="font-size:11px;color:rgba(15,17,22,0.50);margin-top:1px;">' + escapeHtml(s.location || '\u2014') + '</div>'
                +   '</div>'
                +   '<span style="background:rgba(65,105,225,0.10);color:#2E4FB8;padding:2px 8px;border-radius:3px;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;">' + escapeHtml(tierLabel) + '</span>'
                + '</div>';
            }
            // Remove last border
            listEl.innerHTML = html.replace(/border-bottom:[^;]+;(?=[^<]*<\/div>\s*$)/, '');
          }
        }
      })
      .catch(function() {
        var loadingEl = $('nws-page-loading');
        var errorEl = $('nws-page-error');
        if (loadingEl) loadingEl.hidden = true;
        if (errorEl) errorEl.hidden = false;
      });

    return true;
  }

  function setupNwsPageActions() {
    if (window._nwsPageActionsWired) return;
    window._nwsPageActionsWired = true;

    var BACKEND = 'https://wv-valet-backend.onrender.com';

    // Confirm — send the Met's message to all affected subscribers
    var confirmBtn = $('nws-page-confirm');
    if (confirmBtn) {
      confirmBtn.addEventListener('click', function() {
        var token = _nwsPageGetToken();
        var messageEl = $('nws-page-message');
        var metNameEl = $('nws-page-met-name');
        var message = messageEl ? messageEl.value.trim() : '';
        var metName = metNameEl ? metNameEl.value.trim() : '';

        if (!message) {
          window.alert('Please write a message to send to subscribers.');
          return;
        }
        if (!window.confirm('Send this alert to all affected subscribers?')) return;

        confirmBtn.disabled = true;
        confirmBtn.textContent = 'Sending\u2026';

        fetch(BACKEND + '/api/v1/nws/page/' + encodeURIComponent(token) + '/confirm', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ message: message, met_name: metName || undefined })
        })
          .then(function(r) { return r.json().then(function(j) { return j; }); })
          .then(function(res) {
            if (res && res.ok) {
              var reviewEl = $('nws-page-review');
              var successEl = $('nws-page-success');
              var countEl = $('nws-page-success-count');
              if (reviewEl) reviewEl.hidden = true;
              if (successEl) successEl.hidden = false;
              if (countEl) countEl.textContent = res.subscribers_notified || 0;
            } else {
              confirmBtn.disabled = false;
              confirmBtn.textContent = 'Send alert to subscribers';
              window.alert('Couldn\u2019t send. ' + ((res && (res.message || res.error)) || ''));
            }
          })
          .catch(function() {
            confirmBtn.disabled = false;
            confirmBtn.textContent = 'Send alert to subscribers';
            window.alert('Network error. Try again.');
          });
      });
    }

    // Dismiss — mark as false alarm
    var dismissBtn = $('nws-page-dismiss');
    if (dismissBtn) {
      dismissBtn.addEventListener('click', function() {
        var token = _nwsPageGetToken();
        var reason = window.prompt('Why are you dismissing this alert? (optional)');
        if (reason === null) return;  // user cancelled prompt

        dismissBtn.disabled = true;
        dismissBtn.textContent = 'Dismissing\u2026';

        fetch(BACKEND + '/api/v1/nws/page/' + encodeURIComponent(token) + '/dismiss', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({ reason: reason })
        })
          .then(function(r) { return r.json().then(function(j) { return j; }); })
          .then(function(res) {
            if (res && res.ok) {
              var reviewEl = $('nws-page-review');
              var dismissedEl = $('nws-page-dismissed');
              if (reviewEl) reviewEl.hidden = true;
              if (dismissedEl) dismissedEl.hidden = false;
            } else {
              dismissBtn.disabled = false;
              dismissBtn.textContent = 'Dismiss as false alarm';
              window.alert('Couldn\u2019t dismiss. ' + ((res && res.error) || ''));
            }
          })
          .catch(function() {
            dismissBtn.disabled = false;
            dismissBtn.textContent = 'Dismiss as false alarm';
            window.alert('Network error. Try again.');
          });
      });
    }
  }


  // ════════════════════════════════════════════════════════════════════
  // Pro Threads — subscriber side (PT-B)
  // ════════════════════════════════════════════════════════════════════
  //
  // Called from loadSubscriberPortal after we know the user's tier.
  // Pro tier (pro_single|pro_multi|pro_enterprise) → conversation view.
  // Hobbyist/free → upgrade teaser.

  var _PRO_THREAD_TIER = null;
  var _PRO_THREAD_WIRED = false;

  function setupProThreadForTier(plan) {
    _PRO_THREAD_TIER = plan;
    var section = $('portal-pro-thread-section');
    var conversation = $('portal-pro-thread-conversation');
    var teaser = $('portal-pro-thread-teaser');
    if (!section) return;

    var isPro = (plan === 'pro_single' || plan === 'pro_multi' || plan === 'pro_enterprise');
    section.hidden = false;

    if (isPro) {
      if (conversation) conversation.hidden = false;
      if (teaser) teaser.hidden = true;
      // Wire one-time event handlers
      if (!_PRO_THREAD_WIRED) {
        _PRO_THREAD_WIRED = true;
        var sendBtn = $('portal-pro-thread-send');
        if (sendBtn) sendBtn.addEventListener('click', sendProThreadMessage);
        var refreshBtn = $('portal-pro-thread-refresh');
        if (refreshBtn) refreshBtn.addEventListener('click', loadProThread);
        var input = $('portal-pro-thread-input');
        // Enter to send (Shift+Enter for newline)
        if (input) {
          input.addEventListener('keydown', function(ev) {
            if (ev.key === 'Enter' && !ev.shiftKey) {
              ev.preventDefault();
              sendProThreadMessage();
            }
          });
        }
      }
      loadProThread();
    } else {
      if (conversation) conversation.hidden = true;
      if (teaser) teaser.hidden = false;
      // Wire upgrade button (one-time)
      if (!_PRO_THREAD_WIRED) {
        _PRO_THREAD_WIRED = true;
        var upgradeBtn = $('portal-pro-thread-upgrade');
        if (upgradeBtn) {
          upgradeBtn.addEventListener('click', function() {
            // Send them to the subscribe screen
            if (typeof showScreen === 'function') {
              showScreen('screen-subscribe');
            }
          });
        }
      }
    }
  }

  function _proThreadFmtTime(ms) {
    if (!ms) return '';
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '';
    var now = new Date();
    var sameDay = d.getFullYear() === now.getFullYear()
               && d.getMonth() === now.getMonth()
               && d.getDate() === now.getDate();
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    var t = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    if (sameDay) return t;
    var diffDays = Math.floor((now - d) / 86400000);
    if (diffDays === 1) return 'Yesterday ' + t;
    if (diffDays < 7) return ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'][d.getDay()] + ' ' + t;
    return (d.getMonth() + 1) + '/' + d.getDate() + ' ' + t;
  }

  function loadProThread() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var listEl = $('portal-pro-thread-messages');
    if (!listEl) return;
    fetch(BACKEND + '/api/v1/me/thread', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          // 403 pro-tier-required — silently leave the section hidden
          if (res.status === 403) {
            var section = $('portal-pro-thread-section');
            if (section) section.hidden = true;
          }
          return;
        }
        renderProThread(res.body.messages || [], res.body.thread || {});

        // Mark as read if there were any unread messages — the user is
        // looking at the thread right now, so it's read.
        var unread = (res.body.thread && res.body.thread.unread_for_subscriber) || 0;
        if (unread > 0) {
          fetch(BACKEND + '/api/v1/me/thread/mark-read', {
            method: 'POST',
            credentials: 'include'
          }).catch(function() {});
        }
      })
      .catch(function() {
        if (listEl) listEl.innerHTML =
          '<div style="text-align:center;padding:20px 0;color:rgba(15,17,22,0.50);font-size:13px;">'
          + 'Could not load messages. Try refreshing.</div>';
      });
  }

  function renderProThread(messages, thread) {
    var listEl = $('portal-pro-thread-messages');
    var unreadBadge = $('portal-pro-thread-unread');
    if (!listEl) return;

    // Update unread badge based on backend value (before mark-read clears it)
    if (unreadBadge) {
      var unread = (thread && thread.unread_for_subscriber) || 0;
      unreadBadge.hidden = unread === 0;
      unreadBadge.textContent = unread;
    }

    if (messages.length === 0) {
      listEl.innerHTML =
        '<div style="text-align:center;padding:40px 20px;color:rgba(15,17,22,0.55);font-size:13px;line-height:1.55;">'
        + '<div style="font-weight:500;color:#0E1116;margin-bottom:6px;">Start the conversation</div>'
        + 'Ask about a forecast, flag a concern, request a custom briefing &mdash; whatever helps you operate. '
        + 'Our meteorologist team responds within 30 minutes during business hours.'
        + '</div>';
      return;
    }

    var html = '';
    for (var i = 0; i < messages.length; i++) {
      var m = messages[i];
      var isMine = m.sender_role === 'subscriber';
      // Date separator if this message is a different day from the previous
      if (i > 0) {
        var prevDate = new Date(messages[i-1].created_at);
        var curDate = new Date(m.created_at);
        if (prevDate.getFullYear() !== curDate.getFullYear()
            || prevDate.getMonth() !== curDate.getMonth()
            || prevDate.getDate() !== curDate.getDate()) {
          html += '<div style="text-align:center;font-size:11px;color:rgba(15,17,22,0.45);margin:8px 0 4px;font-weight:600;">'
            + escapeHtml(_proThreadDateLabel(curDate))
            + '</div>';
        }
      }

      var alignSide = isMine ? 'flex-end' : 'flex-start';
      var bubbleBg = isMine ? '#2E4FB8' : 'rgba(15,17,22,0.05)';
      var bubbleColor = isMine ? '#fff' : '#0E1116';
      var senderLabel = isMine ? '' : (m.sender_name || 'Meteorologist');

      html += '<div style="display:flex;flex-direction:column;align-items:' + alignSide + ';max-width:100%;">'
        + (senderLabel
            ? '<div style="font-size:10px;font-weight:700;color:rgba(15,17,22,0.55);text-transform:uppercase;letter-spacing:0.05em;margin-bottom:3px;">' + escapeHtml(senderLabel) + '</div>'
            : '')
        + '<div style="background:' + bubbleBg + ';color:' + bubbleColor + ';padding:9px 14px;border-radius:14px;'
        +   'max-width:80%;font-size:13.5px;line-height:1.5;white-space:pre-wrap;word-wrap:break-word;">'
        +   escapeHtml(m.body)
        + '</div>'
        + '<div style="font-size:10px;color:rgba(15,17,22,0.45);margin-top:3px;">' + escapeHtml(_proThreadFmtTime(m.created_at)) + '</div>'
        + '</div>';
    }
    listEl.innerHTML = html;
    // Scroll to bottom (most recent message)
    listEl.scrollTop = listEl.scrollHeight;
  }

  function _proThreadDateLabel(d) {
    var now = new Date();
    var sameDay = d.getFullYear() === now.getFullYear()
               && d.getMonth() === now.getMonth()
               && d.getDate() === now.getDate();
    if (sameDay) return 'Today';
    var diffDays = Math.floor((now - d) / 86400000);
    if (diffDays === 1) return 'Yesterday';
    if (diffDays < 7) return ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'][d.getDay()];
    var months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    return months[d.getMonth()] + ' ' + d.getDate();
  }

  function sendProThreadMessage() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var input = $('portal-pro-thread-input');
    var sendBtn = $('portal-pro-thread-send');
    if (!input || !sendBtn) return;
    var body = input.value.trim();
    if (!body) return;

    sendBtn.disabled = true;
    sendBtn.textContent = 'Sending\u2026';

    fetch(BACKEND + '/api/v1/me/thread/messages', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ body: body })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        sendBtn.disabled = false;
        sendBtn.textContent = 'Send';
        if (res && res.ok) {
          input.value = '';
          loadProThread();
        } else {
          window.alert('Could not send. ' + ((res && (res.message || res.error)) || ''));
        }
      })
      .catch(function() {
        sendBtn.disabled = false;
        sendBtn.textContent = 'Send';
        window.alert('Network error. Try again.');
      });
  }


  // ════════════════════════════════════════════════════════════════════
  // Customer review + tip page (Phase 10 — Met tips, TIP-B)
  // ════════════════════════════════════════════════════════════════════
  // URL: /?review=<token> (optionally &tip=thanks after Checkout return)
  // Customer arrives via SMS link after their $19 review delivers.

  function _custReviewGetToken() {
    try {
      return new URLSearchParams(window.location.search).get('review');
    } catch (e) {
      return null;
    }
  }

  function _custTipReturnedFromCheckout() {
    try {
      return new URLSearchParams(window.location.search).get('tip') === 'thanks';
    } catch (e) {
      return false;
    }
  }

  function _custFmtCompletedAt(ms) {
    if (!ms) return '';
    // verification_requests.completed_at is stored in SECONDS (now_ts()),
    // but other tables use ms. Normalize: if value looks like a small
    // number (< year-2001 in ms), it's seconds; multiply.
    if (ms < 1e12) ms = ms * 1000;
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '';
    var now = new Date();
    var sameDay = d.getFullYear() === now.getFullYear()
               && d.getMonth() === now.getMonth()
               && d.getDate() === now.getDate();
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    var t = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    if (sameDay) return ' \u00b7 ' + t + ' today';
    return ' \u00b7 ' + (d.getMonth() + 1) + '/' + d.getDate() + ' at ' + t;
  }

  function checkForCustomerReview() {
    var token = _custReviewGetToken();
    if (!token) return false;

    showScreen('screen-customer-review');
    loadCustomerReview(token);
    return true;
  }

  function loadCustomerReview(token) {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/reviews/' + encodeURIComponent(token), {
      credentials: 'include'
    })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        var loadingEl = $('cust-review-loading');
        var errorEl = $('cust-review-error');
        var mainEl = $('cust-review-main');
        if (loadingEl) loadingEl.hidden = true;

        if (!res.body || !res.body.ok) {
          if (errorEl) errorEl.hidden = false;
          return;
        }
        var rv = res.body.review || {};
        if (mainEl) mainEl.hidden = false;

        // Plan
        var planEl = $('cust-review-plan');
        var metaEl = $('cust-review-meta');
        if (planEl) planEl.textContent = rv.plan_text || 'Your review';
        if (metaEl) {
          var metaBits = [];
          if (rv.plan_location) metaBits.push(rv.plan_location);
          if (rv.plan_window) metaBits.push(rv.plan_window);
          metaEl.textContent = metaBits.join(' \u00b7 ') || '\u2014';
        }

        // Verdict
        var verdictEl = $('cust-review-verdict');
        if (verdictEl) verdictEl.textContent = rv.verdict || '(no verdict)';

        // Notes (Met's "why")
        if (rv.notes && rv.notes.trim()) {
          var notesWrap = $('cust-review-notes-wrap');
          var notesEl = $('cust-review-notes');
          if (notesWrap) notesWrap.hidden = false;
          if (notesEl) notesEl.textContent = rv.notes;
        }

        // Met name
        var metName = rv.met_name || 'your meteorologist';
        var metNameEl = $('cust-review-met-name');
        if (metNameEl) metNameEl.textContent = metName;
        var completedEl = $('cust-review-completed-at');
        if (completedEl) completedEl.textContent = _custFmtCompletedAt(rv.completed_at);

        // Tip section — only show if Met attributable
        if (rv.tip_eligible) {
          var tipSection = $('cust-tip-section');
          if (tipSection) tipSection.hidden = false;
          var tipMetNameEl = $('cust-tip-met-name');
          if (tipMetNameEl) tipMetNameEl.textContent = metName;
        }

        // Were we redirected back from Stripe Checkout success?
        if (_custTipReturnedFromCheckout()) {
          _custShowTipThanks(metName);
        }
      })
      .catch(function() {
        var loadingEl = $('cust-review-loading');
        var errorEl = $('cust-review-error');
        if (loadingEl) loadingEl.hidden = true;
        if (errorEl) errorEl.hidden = false;
      });
  }

  function _custShowTipThanks(metName) {
    var mainEl = $('cust-review-main');
    var processEl = $('cust-tip-processing');
    var thanksEl = $('cust-tip-thanks');
    if (mainEl) mainEl.hidden = true;
    if (processEl) processEl.hidden = true;
    if (thanksEl) thanksEl.hidden = false;
    var nameEl = $('cust-tip-thanks-met-name');
    if (nameEl && metName) nameEl.textContent = metName;
  }

  function _custSubmitTip(amountCents) {
    var token = _custReviewGetToken();
    if (!token) return;
    if (!amountCents || amountCents < 100 || amountCents > 10000) {
      window.alert('Tips must be between $1 and $100.');
      return;
    }

    var mainEl = $('cust-review-main');
    var processEl = $('cust-tip-processing');
    if (mainEl) mainEl.hidden = true;
    if (processEl) processEl.hidden = false;

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/reviews/' + encodeURIComponent(token) + '/tip', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ amount_cents: amountCents })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (!res || !res.ok) {
          if (processEl) processEl.hidden = true;
          if (mainEl) mainEl.hidden = false;
          window.alert('Could not process tip. ' + ((res && (res.message || res.error)) || ''));
          return;
        }
        if (res.status === 'completed') {
          // Subscriber off-session charge — instant success
          var metNameEl = $('cust-review-met-name');
          var metName = metNameEl ? metNameEl.textContent : 'your meteorologist';
          _custShowTipThanks(metName);
        } else if (res.status === 'pending' && res.checkout_url) {
          // Anon — redirect to Stripe Checkout
          window.location.href = res.checkout_url;
        }
      })
      .catch(function() {
        if (processEl) processEl.hidden = true;
        if (mainEl) mainEl.hidden = false;
        window.alert('Network error. Try again.');
      });
  }

  function setupCustomerReviewActions() {
    if (window._custReviewWired) return;
    window._custReviewWired = true;

    // Amount buttons
    var amountsContainer = $('cust-tip-amounts');
    if (amountsContainer) {
      amountsContainer.addEventListener('click', function(ev) {
        var btn = ev.target.closest('[data-tip-amount]');
        if (!btn) return;
        var amount = parseInt(btn.getAttribute('data-tip-amount'), 10);
        _custSubmitTip(amount);
      });
    }

    // Custom amount toggle
    var customToggle = $('cust-tip-custom-toggle');
    var customWrap = $('cust-tip-custom-wrap');
    var customInput = $('cust-tip-custom-input');
    if (customToggle) {
      customToggle.addEventListener('click', function() {
        if (customWrap) {
          customWrap.hidden = !customWrap.hidden;
          if (!customWrap.hidden && customInput) customInput.focus();
        }
      });
    }

    // Custom amount submit
    var customSubmit = $('cust-tip-custom-submit');
    if (customSubmit) {
      customSubmit.addEventListener('click', function() {
        var dollars = customInput ? parseFloat(customInput.value) : 0;
        if (!dollars || dollars < 1 || dollars > 100) {
          window.alert('Please enter an amount between $1 and $100.');
          return;
        }
        _custSubmitTip(Math.round(dollars * 100));
      });
    }
    // Enter in custom input submits
    if (customInput) {
      customInput.addEventListener('keydown', function(ev) {
        if (ev.key === 'Enter') {
          ev.preventDefault();
          if (customSubmit) customSubmit.click();
        }
      });
    }

    // Back to review (from thanks state)
    var backBtn = $('cust-tip-back-to-review');
    if (backBtn) {
      backBtn.addEventListener('click', function() {
        var thanksEl = $('cust-tip-thanks');
        var mainEl = $('cust-review-main');
        if (thanksEl) thanksEl.hidden = true;
        if (mainEl) mainEl.hidden = false;
        // Clean up &tip=thanks from URL so refresh doesn't re-trigger thanks
        try {
          var url = new URL(window.location.href);
          url.searchParams.delete('tip');
          window.history.replaceState({}, '', url.toString());
        } catch (e) {}
      });
    }
  }


  function loadSubscriberPortal() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';

    // ─── Identity card + billing card ─────────────────────────────────
    fetch(BACKEND + '/api/v1/me/subscription', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) return;
        var d = res.body;
        var planEl = $('portal-plan-name');
        if (planEl) planEl.textContent = d.plan_display;
        var planDisplayEl = $('portal-plan-display');
        if (planDisplayEl) planDisplayEl.textContent = 'WeatherValet \u2014 ' + d.plan_display;
        var renewsEl = $('portal-renews');
        if (renewsEl) {
          renewsEl.textContent = d.next_billing_at
            ? _portalFormatDate(d.next_billing_at)
            : '\u2014';
        }
        var sinceEl = $('portal-since');
        if (sinceEl) sinceEl.textContent = _portalFormatDate(d.member_since);

        // Starter Month banner (sales funnel) — show if currently in starter window
        var banner = $('portal-starter-banner');
        if (banner) {
          if (d.is_starter_month && d.starter_renews_at) {
            var renewsDate = _portalFormatDate(d.starter_renews_at);
            var dateEl = $('portal-starter-renews-date');
            if (dateEl) dateEl.textContent = renewsDate;
            banner.hidden = false;
          } else {
            banner.hidden = true;
          }
        }

        // Welcome name from auth
        try {
          var u = window.__wvAuth && window.__wvAuth.getCurrentUser && window.__wvAuth.getCurrentUser();
          var nameEl = $('portal-user-name');
          if (nameEl) {
            if (u && u.name) {
              var firstName = String(u.name).trim().split(/\s+/)[0];
              nameEl.textContent = 'Welcome back, ' + firstName;
            } else {
              nameEl.textContent = 'Welcome back';
            }
          }
        } catch (e) {}

        // Billing card values
        var billingCard = document.querySelector('.portal-billing-card');
        if (billingCard) {
          var values = billingCard.querySelectorAll('.portal-billing-value');
          if (values[0]) values[0].textContent = 'WeatherValet \u2014 ' + d.plan_display;
          if (values[1]) values[1].textContent = d.price_display;
          if (values[2]) {
            values[2].textContent = d.next_billing_at
              ? _portalFormatDate(d.next_billing_at)
              : '\u2014';
          }
          // Payment method placeholder until Stripe integration (Chunk C)
          if (values[3]) values[3].textContent = '\u2014 (set on signup)';
        }

        // Pro Threads (PT-B): show the section, branch on tier
        if (typeof setupProThreadForTier === 'function') {
          try { setupProThreadForTier(d.plan || ''); } catch (e) { console.warn('pro thread setup failed', e); }
        }
      })
      .catch(function(err) { console.warn('[portal] subscription load failed:', err); });

    // ─── Saved locations ──────────────────────────────────────────────
    fetch(BACKEND + '/api/v1/me/saved-locations', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) return;
        var locs = res.body.locations || [];
        var locEl = $('portal-user-location');
        if (locEl) {
          if (locs.length === 0) {
            locEl.textContent = 'No saved location yet';
          } else {
            var primary = locs.find(function(l) { return l.is_primary; }) || locs[0];
            locEl.textContent = primary.address_text || primary.label;
          }
        }
        var locCard = document.querySelector('.portal-location-card');
        if (locCard) {
          var nameRow = locCard.querySelector('.portal-location-name');
          var coordsRow = locCard.querySelector('.portal-location-coords');
          if (locs.length === 0) {
            if (nameRow) nameRow.textContent = 'No saved location';
            if (coordsRow) coordsRow.textContent = 'Tap Edit to add one';
          } else {
            var primary = locs.find(function(l) { return l.is_primary; }) || locs[0];
            if (nameRow) nameRow.textContent = primary.label;
            if (coordsRow) {
              var coords = primary.lat.toFixed(4) + '\u00b0N, '
                         + Math.abs(primary.lng).toFixed(4) + '\u00b0W';
              coordsRow.textContent = primary.county
                ? coords + ' \u00b7 ' + primary.county
                : coords;
            }
          }
        }
      })
      .catch(function(err) { console.warn('[portal] locations load failed:', err); });

    // ─── Brief preferences ────────────────────────────────────────────
    fetch(BACKEND + '/api/v1/me/brief-preferences', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) return;
        var p = res.body.preferences;
        var grid = document.querySelector('.portal-prefs-grid');
        if (!grid) return;
        var prefs = grid.querySelectorAll('.portal-pref');
        if (prefs[0]) {
          var mv = prefs[0].querySelector('.portal-pref-value');
          var md = prefs[0].querySelector('.portal-pref-detail');
          if (mv) {
            mv.textContent = p.morning_enabled
              ? p.morning_window_start + '\u2013' + p.morning_window_end
              : 'Off';
            mv.classList.toggle('portal-pref-muted', !p.morning_enabled);
          }
          if (md) {
            md.textContent = p.morning_enabled
              ? 'Customer-local \u00b7 daily'
              : 'Tap edit to enable';
          }
        }
        if (prefs[1]) {
          var ev = prefs[1].querySelector('.portal-pref-value');
          var ed = prefs[1].querySelector('.portal-pref-detail');
          if (ev) {
            if (p.evening_enabled && p.evening_window_start && p.evening_window_end) {
              ev.textContent = p.evening_window_start + '\u2013' + p.evening_window_end;
              ev.classList.remove('portal-pref-muted');
              if (ed) ed.textContent = 'Customer-local \u00b7 daily';
            } else {
              ev.textContent = 'Off';
              ev.classList.add('portal-pref-muted');
              if (ed) ed.textContent = 'Tap edit to enable';
            }
          }
        }
        if (prefs[2]) {
          var qv = prefs[2].querySelector('.portal-pref-value');
          if (qv) qv.textContent = p.quiet_start + ' \u2013 ' + p.quiet_end;
        }
        if (prefs[3]) {
          var cc = prefs[3].querySelector('.portal-pref-channels');
          if (cc) {
            var chips = cc.querySelectorAll('.portal-channel-chip');
            chips.forEach(function(chip) {
              var label = (chip.textContent || '').trim().toLowerCase();
              chip.classList.toggle('is-active', p.channels.indexOf(label) !== -1);
            });
          }
        }
      })
      .catch(function(err) { console.warn('[portal] preferences load failed:', err); });

    // ─── Threshold alerts ─────────────────────────────────────────────
    fetch(BACKEND + '/api/v1/me/threshold-alerts', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) return;
        var alerts = res.body.alerts || [];
        var container = document.querySelector('.portal-thresholds');
        if (!container) return;
        if (alerts.length === 0) {
          container.innerHTML = '<div style="padding: 24px 16px; text-align: center; '
            + 'color: rgba(15,17,22,0.55); font-size: 13px;">'
            + 'No threshold alerts yet. Tap <strong>+ Add alert</strong> to create one.'
            + '</div>';
          return;
        }
        var COMP = { 'gt':'exceeds','gte':'reaches','lt':'drops below','lte':'falls to','eq':'equals' };
        var METRIC = { 'wind':'Wind','temp_low':'Temperature','temp_high':'Temperature',
                       'precip_chance':'Precipitation chance','humidity':'Humidity' };
        var html = '';
        alerts.forEach(function(a) {
          var name = (METRIC[a.metric] || a.metric) + ' ' + (COMP[a.comparator] || a.comparator) + ' '
                   + a.threshold_value + (a.units === 'pct' ? '%' :
                     (a.units === 'F' ? '\u00b0F' : (a.units === 'C' ? '\u00b0C' : ' ' + a.units)));
          var channelText = a.channels.map(function(c) {
            return c.charAt(0).toUpperCase() + c.slice(1);
          }).join(' + ');
          var statusText = a.enabled ? 'active' : 'paused';
          html += '<div class="portal-threshold">'
            +   '<div class="portal-threshold-icon">'
            +     '<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">'
            +     '<circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>'
            +   '</div>'
            +   '<div class="portal-threshold-text">'
            +     '<div class="portal-threshold-name">' + escapeHtml(name) + '</div>'
            +     '<div class="portal-threshold-detail">Notify via ' + escapeHtml(channelText) + ' \u00b7 ' + statusText + '</div>'
            +   '</div>'
            +   '<button type="button" class="portal-threshold-action" data-threshold-id="' + a.id + '">Edit</button>'
            + '</div>';
        });
        container.innerHTML = html;
      })
      .catch(function(err) { console.warn('[portal] alerts load failed:', err); });

    // ─── Brief history ────────────────────────────────────────────────
    fetch(BACKEND + '/api/v1/me/brief-history', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) return;
        var history = res.body.history || [];
        var container = document.querySelector('.portal-history');
        if (!container) return;
        if (history.length === 0) {
          container.innerHTML = '<div style="padding: 28px 16px; text-align: center; '
            + 'color: rgba(15,17,22,0.55); font-size: 13px;">'
            + 'No briefs delivered yet. Your first daily brief will appear here.'
            + '</div>';
          return;
        }
        var html = '';
        history.slice(0, 5).forEach(function(h) {
          var typeLabel = h.brief_type === 'morning' ? 'Morning brief'
                        : h.brief_type === 'evening' ? 'Evening brief'
                        : h.brief_type === 'severe'  ? 'Severe alert'
                        : 'Threshold alert';
          var signedText = h.is_met_touched && h.met_name
            ? 'Met-touched by ' + h.met_name + ' \u00b7 tap to read'
            : 'AI brief \u00b7 tap to read';
          var verdictHtml = h.verdict
            ? '<span class="portal-history-verdict verdict-' + h.verdict + '">'
              + h.verdict.charAt(0).toUpperCase() + h.verdict.slice(1) + '</span>'
            : '';
          html += '<div class="portal-history-item">'
            +   '<div class="portal-history-date">'
            +     '<div class="portal-history-day">' + escapeHtml(_portalRelativeDay(h.delivered_at)) + '</div>'
            +     '<div class="portal-history-time">' + escapeHtml(_portalFormatTime(h.delivered_at)) + '</div>'
            +   '</div>'
            +   '<div class="portal-history-content">'
            +     '<div class="portal-history-meta">'
            +       '<span class="portal-history-type">' + escapeHtml(typeLabel) + '</span>'
            +       verdictHtml
            +     '</div>'
            +     '<div class="portal-history-snippet">' + escapeHtml(h.snippet || '\u2014') + '</div>'
            +     '<div class="portal-history-signed">' + escapeHtml(signedText) + '</div>'
            +   '</div>'
            + '</div>';
        });
        container.innerHTML = html;
      })
      .catch(function(err) { console.warn('[portal] history load failed:', err); });
  }


  // ════════════════════════════════════════════════════════════════════
  // Subscriber portal — edit modals + write API (Phase 10 Chunk B1)
  // ════════════════════════════════════════════════════════════════════
  //
  // Wires the two edit modals (brief prefs + threshold alert) into the
  // portal. Buttons that open modals:
  //   - "Edit" on Brief delivery section → opens brief-prefs-modal
  //   - "+ Add alert" on Threshold alerts → opens threshold-alert-modal in add mode
  //   - "Edit" on each threshold row    → opens threshold-alert-modal in edit mode
  // On save: PATCH/POST to backend, then reload the portal to show fresh data.

  function setupPortalEditing() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';

    // ─── Brief preferences modal ──────────────────────────────────────
    var prefsModal = $('brief-prefs-modal');
    var prefsScrim = $('brief-prefs-scrim');
    var prefsClose = $('brief-prefs-close');
    var prefsCancel = $('prefs-cancel');
    var prefsSave = $('prefs-save');
    var prefsError = $('prefs-error');

    function openPrefsModal() {
      if (!prefsModal) return;
      // Fetch current prefs and populate the form before showing.
      fetch(BACKEND + '/api/v1/me/brief-preferences', { credentials: 'include' })
        .then(function(r) { return r.json().then(function(j) { return j; }); })
        .then(function(res) {
          if (!res || !res.ok) return;
          var p = res.preferences;
          var morningEn = $('prefs-morning-enabled');
          var morningStart = $('prefs-morning-start');
          var morningEnd = $('prefs-morning-end');
          var eveningEn = $('prefs-evening-enabled');
          var eveningStart = $('prefs-evening-start');
          var eveningEnd = $('prefs-evening-end');
          var quietStart = $('prefs-quiet-start');
          var quietEnd = $('prefs-quiet-end');
          var chSms = $('prefs-channel-sms');
          var chEmail = $('prefs-channel-email');
          var chPush = $('prefs-channel-push');
          if (morningEn) morningEn.checked = !!p.morning_enabled;
          if (morningStart) morningStart.value = p.morning_window_start || '05:30';
          if (morningEnd) morningEnd.value = p.morning_window_end || '07:00';
          if (eveningEn) eveningEn.checked = !!p.evening_enabled;
          if (eveningStart) eveningStart.value = p.evening_window_start || '17:30';
          if (eveningEnd) eveningEnd.value = p.evening_window_end || '18:30';
          if (quietStart) quietStart.value = p.quiet_start || '21:00';
          if (quietEnd) quietEnd.value = p.quiet_end || '05:00';
          var channels = p.channels || [];
          if (chSms) chSms.checked = channels.indexOf('sms') !== -1;
          if (chEmail) chEmail.checked = channels.indexOf('email') !== -1;
          if (chPush) chPush.checked = channels.indexOf('push') !== -1;
        })
        .catch(function() {});

      if (prefsError) { prefsError.hidden = true; prefsError.textContent = ''; }
      prefsModal.hidden = false;
    }
    function closePrefsModal() {
      if (prefsModal) prefsModal.hidden = true;
    }
    if (prefsScrim) prefsScrim.addEventListener('click', closePrefsModal);
    if (prefsClose) prefsClose.addEventListener('click', closePrefsModal);
    if (prefsCancel) prefsCancel.addEventListener('click', closePrefsModal);

    if (prefsSave) {
      prefsSave.addEventListener('click', function() {
        if (prefsError) { prefsError.hidden = true; prefsError.textContent = ''; }
        var body = {
          morning_enabled:      $('prefs-morning-enabled').checked,
          morning_window_start: $('prefs-morning-start').value,
          morning_window_end:   $('prefs-morning-end').value,
          evening_enabled:      $('prefs-evening-enabled').checked,
          evening_window_start: $('prefs-evening-start').value || null,
          evening_window_end:   $('prefs-evening-end').value || null,
          quiet_start:          $('prefs-quiet-start').value,
          quiet_end:            $('prefs-quiet-end').value,
          channels: ['sms','email','push'].filter(function(c) {
            var el = $('prefs-channel-' + c);
            return el && el.checked;
          })
        };
        if (body.channels.length === 0) {
          if (prefsError) {
            prefsError.textContent = 'Pick at least one notification channel.';
            prefsError.hidden = false;
          }
          return;
        }
        prefsSave.disabled = true;
        prefsSave.textContent = 'Saving\u2026';
        fetch(BACKEND + '/api/v1/me/brief-preferences', {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify(body)
        })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          prefsSave.disabled = false;
          prefsSave.textContent = 'Save changes';
          if (res.body && res.body.ok) {
            closePrefsModal();
            // Reload the portal data so the change is visible immediately.
            if (typeof loadSubscriberPortal === 'function') loadSubscriberPortal();
          } else {
            if (prefsError) {
              prefsError.textContent = 'Could not save. ' + (res.body && res.body.error ? '(' + res.body.error + ')' : '');
              prefsError.hidden = false;
            }
          }
        })
        .catch(function() {
          prefsSave.disabled = false;
          prefsSave.textContent = 'Save changes';
          if (prefsError) {
            prefsError.textContent = 'Network error. Try again.';
            prefsError.hidden = false;
          }
        });
      });
    }

    // ─── Threshold alert modal ────────────────────────────────────────
    var thresholdModal = $('threshold-alert-modal');
    var thresholdScrim = $('threshold-alert-scrim');
    var thresholdClose = $('threshold-alert-close');
    var thresholdCancel = $('threshold-cancel');
    var thresholdSave = $('threshold-save');
    var thresholdDelete = $('threshold-delete');
    var thresholdError = $('threshold-error');
    var thresholdTitle = $('threshold-alert-title');
    var thresholdEnabledSection = $('threshold-enabled-section');

    var thresholdEditingId = null; // null = add mode, number = edit mode

    function openThresholdModal(mode, alertData) {
      if (!thresholdModal) return;
      thresholdEditingId = (mode === 'edit' && alertData) ? alertData.id : null;

      // Title + button labels + delete button visibility
      if (thresholdTitle) {
        thresholdTitle.textContent = (mode === 'edit') ? 'Edit threshold alert' : 'Add threshold alert';
      }
      if (thresholdSave) {
        thresholdSave.textContent = (mode === 'edit') ? 'Save changes' : 'Add alert';
      }
      if (thresholdDelete) {
        thresholdDelete.hidden = (mode !== 'edit');
      }
      if (thresholdEnabledSection) {
        // Hide enabled toggle in add mode — new alerts are always active.
        thresholdEnabledSection.style.display = (mode === 'edit') ? '' : 'none';
      }

      // Populate fields
      var metric = $('threshold-metric');
      var comparator = $('threshold-comparator');
      var value = $('threshold-value');
      var units = $('threshold-units');
      var chSms = $('threshold-channel-sms');
      var chEmail = $('threshold-channel-email');
      var chPush = $('threshold-channel-push');
      var enabled = $('threshold-enabled');

      if (mode === 'edit' && alertData) {
        if (metric) metric.value = alertData.metric;
        if (comparator) comparator.value = alertData.comparator;
        if (value) value.value = alertData.threshold_value;
        if (units) units.value = alertData.units;
        var channels = alertData.channels || [];
        if (chSms) chSms.checked = channels.indexOf('sms') !== -1;
        if (chEmail) chEmail.checked = channels.indexOf('email') !== -1;
        if (chPush) chPush.checked = channels.indexOf('push') !== -1;
        if (enabled) enabled.checked = !!alertData.enabled;
      } else {
        // Add mode — sensible defaults
        if (metric) metric.value = 'wind';
        if (comparator) comparator.value = 'gt';
        if (value) value.value = '';
        if (units) units.value = 'mph';
        if (chSms) chSms.checked = true;
        if (chEmail) chEmail.checked = true;
        if (chPush) chPush.checked = false;
        if (enabled) enabled.checked = true;
      }

      if (thresholdError) { thresholdError.hidden = true; thresholdError.textContent = ''; }
      thresholdModal.hidden = false;
      // Focus the value input — it's the field most likely to need typing.
      setTimeout(function() { if (value) value.focus(); }, 60);
    }
    function closeThresholdModal() {
      if (thresholdModal) thresholdModal.hidden = true;
      thresholdEditingId = null;
    }
    if (thresholdScrim) thresholdScrim.addEventListener('click', closeThresholdModal);
    if (thresholdClose) thresholdClose.addEventListener('click', closeThresholdModal);
    if (thresholdCancel) thresholdCancel.addEventListener('click', closeThresholdModal);

    if (thresholdSave) {
      thresholdSave.addEventListener('click', function() {
        if (thresholdError) { thresholdError.hidden = true; thresholdError.textContent = ''; }
        var valueEl = $('threshold-value');
        if (!valueEl || valueEl.value === '' || isNaN(parseFloat(valueEl.value))) {
          if (thresholdError) {
            thresholdError.textContent = 'Enter a number for the threshold.';
            thresholdError.hidden = false;
          }
          return;
        }
        var body = {
          metric:          $('threshold-metric').value,
          comparator:      $('threshold-comparator').value,
          threshold_value: parseFloat(valueEl.value),
          units:           $('threshold-units').value,
          channels: ['sms','email','push'].filter(function(c) {
            var el = $('threshold-channel-' + c);
            return el && el.checked;
          })
        };
        if (body.channels.length === 0) {
          if (thresholdError) {
            thresholdError.textContent = 'Pick at least one notification channel.';
            thresholdError.hidden = false;
          }
          return;
        }

        var isEdit = thresholdEditingId !== null;
        if (isEdit) {
          var enEl = $('threshold-enabled');
          if (enEl) body.enabled = enEl.checked;
        }

        var url = isEdit
          ? BACKEND + '/api/v1/me/threshold-alerts/' + thresholdEditingId
          : BACKEND + '/api/v1/me/threshold-alerts';
        var method = isEdit ? 'PATCH' : 'POST';

        thresholdSave.disabled = true;
        thresholdSave.textContent = isEdit ? 'Saving\u2026' : 'Adding\u2026';
        fetch(url, {
          method: method,
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify(body)
        })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          thresholdSave.disabled = false;
          thresholdSave.textContent = isEdit ? 'Save changes' : 'Add alert';
          if (res.body && res.body.ok) {
            closeThresholdModal();
            if (typeof loadSubscriberPortal === 'function') loadSubscriberPortal();
          } else {
            if (thresholdError) {
              thresholdError.textContent = 'Could not save. ' + (res.body && res.body.error ? '(' + res.body.error + ')' : '');
              thresholdError.hidden = false;
            }
          }
        })
        .catch(function() {
          thresholdSave.disabled = false;
          thresholdSave.textContent = isEdit ? 'Save changes' : 'Add alert';
          if (thresholdError) {
            thresholdError.textContent = 'Network error. Try again.';
            thresholdError.hidden = false;
          }
        });
      });
    }

    if (thresholdDelete) {
      thresholdDelete.addEventListener('click', function() {
        if (thresholdEditingId === null) return;
        if (!window.confirm('Delete this alert?')) return;
        thresholdDelete.disabled = true;
        fetch(BACKEND + '/api/v1/me/threshold-alerts/' + thresholdEditingId, {
          method: 'DELETE',
          credentials: 'include'
        })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          thresholdDelete.disabled = false;
          if (res.body && res.body.ok) {
            closeThresholdModal();
            if (typeof loadSubscriberPortal === 'function') loadSubscriberPortal();
          } else {
            if (thresholdError) {
              thresholdError.textContent = 'Could not delete.';
              thresholdError.hidden = false;
            }
          }
        })
        .catch(function() {
          thresholdDelete.disabled = false;
          if (thresholdError) {
            thresholdError.textContent = 'Network error.';
            thresholdError.hidden = false;
          }
        });
      });
    }

    // ─── Wire portal Edit / Add buttons to open the modals ───────────
    // Brief delivery's "Edit" button — second .portal-section-edit on the page.
    // Use event delegation on the document so re-renders don't break it.
    document.addEventListener('click', function(ev) {
      var target = ev.target;
      if (!target) return;

      // Brief delivery "Edit" button — match by section heading
      var editBtn = target.closest('.portal-section-edit');
      if (editBtn) {
        var section = editBtn.closest('.portal-section');
        if (section) {
          var titleEl = section.querySelector('.portal-section-title');
          var titleText = titleEl ? titleEl.textContent : '';
          if (/Brief delivery/i.test(titleText)) {
            ev.preventDefault();
            openPrefsModal();
            return;
          }
          if (/Saved location/i.test(titleText)) {
            ev.preventDefault();
            // Check if user has an existing primary location → edit mode.
            // If not → add mode (open with no preset).
            fetch(BACKEND + '/api/v1/me/saved-locations', { credentials: 'include' })
              .then(function(r) { return r.json().then(function(j) { return j; }); })
              .then(function(res) {
                if (res && res.ok) {
                  var locs = res.locations || [];
                  var primary = locs.find(function(l) { return l.is_primary; }) || locs[0];
                  if (primary) {
                    openSavedLocationModal(primary);
                  } else {
                    openSavedLocationModal();
                  }
                } else {
                  openSavedLocationModal();
                }
              })
              .catch(function() { openSavedLocationModal(); });
            return;
          }
        }
      }

      // Add alert button
      var addBtn = target.closest('.portal-section-add');
      if (addBtn) {
        ev.preventDefault();
        openThresholdModal('add');
        return;
      }

      // Edit button on individual threshold row — has data-threshold-id
      var thresholdEdit = target.closest('.portal-threshold-action[data-threshold-id]');
      if (thresholdEdit) {
        ev.preventDefault();
        var id = parseInt(thresholdEdit.getAttribute('data-threshold-id'), 10);
        // Re-fetch the alert to get its current state, then open in edit mode
        fetch(BACKEND + '/api/v1/me/threshold-alerts', { credentials: 'include' })
          .then(function(r) { return r.json().then(function(j) { return j; }); })
          .then(function(res) {
            if (!res || !res.ok) return;
            var alert = (res.alerts || []).find(function(a) { return a.id === id; });
            if (alert) openThresholdModal('edit', alert);
          })
          .catch(function() {});
        return;
      }

      // Billing actions — all three buttons (Change plan, Cancel,
      // Update payment method) open the same Stripe Customer Portal.
      // Stripe's portal UI surfaces each action; the button label here
      // is a UX hint, not a separate flow.
      var billingBtn = target.closest('[data-billing-action="portal"]');
      if (billingBtn) {
        ev.preventDefault();
        var originalLabel = billingBtn.textContent;
        billingBtn.disabled = true;
        billingBtn.textContent = 'Opening\u2026';
        fetch(BACKEND + '/api/v1/me/stripe-portal-session', {
          method: 'POST',
          credentials: 'include'
        })
          .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
          .then(function(res) {
            if (res.body && res.body.ok && res.body.url) {
              // Redirect to Stripe-hosted portal. User comes back after
              // they're done (return_url on backend points to our home).
              window.location.href = res.body.url;
            } else {
              billingBtn.disabled = false;
              billingBtn.textContent = originalLabel;
              var msg = (res.body && res.body.message) ||
                        'Couldn\u2019t open billing portal. Please try again.';
              window.alert(msg);
            }
          })
          .catch(function() {
            billingBtn.disabled = false;
            billingBtn.textContent = originalLabel;
            window.alert('Network error. Please try again.');
          });
        return;
      }
    });

    // ═══════════════════════════════════════════════════════════════
    // Saved location modal (Phase 10 B2)
    // ═══════════════════════════════════════════════════════════════
    //
    // Uses Leaflet directly (not initPolygonMap, which is for polygons).
    // Single draggable marker; "Look up" calls our backend geocode.
    var savedLocationModal       = $('saved-location-modal');
    var savedLocationScrim       = $('saved-location-scrim');
    var savedLocationClose       = $('saved-location-close');
    var savedLocationCancel      = $('saved-location-cancel');
    var savedLocationSave        = $('saved-location-save');
    var savedLocationDelete      = $('saved-location-delete');
    var savedLocationError       = $('saved-location-error');
    var savedLocationLabel       = $('saved-location-label');
    var savedLocationAddress     = $('saved-location-address');
    var savedLocationLookupBtn   = $('saved-location-lookup-btn');
    var savedLocationCoordsOut   = $('saved-location-coords-readout');
    var savedLocationTitle       = $('saved-location-title');

    var savedLocationState = {
      mode: 'add',              // 'add' or 'edit'
      editingId: null,
      lat: null, lng: null,
      county: null,
      map: null, marker: null
    };

    // Lazy-loaded Leaflet (mirrors WV_FeedMapAPI loader pattern)
    function ensureLeafletForPinDrop(cb) {
      if (window.L && typeof L.map === 'function') return cb();
      if (!document.querySelector('link[data-leaflet]')) {
        var link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
        link.setAttribute('data-leaflet', 'css');
        document.head.appendChild(link);
      }
      if (document.querySelector('script[data-leaflet]')) {
        var existing = document.querySelector('script[data-leaflet]');
        existing.addEventListener('load', cb);
        return;
      }
      var s = document.createElement('script');
      s.src = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';
      s.async = true;
      s.setAttribute('data-leaflet', '1');
      s.onload = cb;
      document.head.appendChild(s);
    }

    function _savedLocationUpdateReadout() {
      if (!savedLocationCoordsOut) return;
      if (savedLocationState.lat == null) {
        savedLocationCoordsOut.textContent = 'No coordinates set yet.';
        return;
      }
      var lat = savedLocationState.lat;
      var lng = savedLocationState.lng;
      var coords = lat.toFixed(4) + '\u00b0N, ' + Math.abs(lng).toFixed(4) + '\u00b0W';
      savedLocationCoordsOut.textContent = savedLocationState.county
        ? coords + ' \u00b7 ' + savedLocationState.county
        : coords;
    }

    function _savedLocationBuildMap(centerLat, centerLng) {
      var container = $('saved-location-map');
      var loadingEl = $('saved-location-map-loading');
      if (!container) return;

      // If map already built, just recenter
      if (savedLocationState.map) {
        savedLocationState.map.setView([centerLat, centerLng], 12);
        if (savedLocationState.marker) {
          savedLocationState.marker.setLatLng([centerLat, centerLng]);
        }
        if (loadingEl) loadingEl.classList.add('is-hidden');
        setTimeout(function() {
          try { savedLocationState.map.invalidateSize(); } catch (e) {}
        }, 60);
        return;
      }

      var map = L.map(container, {
        center: [centerLat, centerLng],
        zoom: 12,
        zoomControl: true,
        attributionControl: false
      });
      L.tileLayer(
        'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}.png',
        { subdomains: 'abcd', maxZoom: 19 }
      ).addTo(map);

      var marker = L.marker([centerLat, centerLng], {
        draggable: true
      }).addTo(map);

      // Drag → update state + readout
      marker.on('dragend', function() {
        var ll = marker.getLatLng();
        savedLocationState.lat = ll.lat;
        savedLocationState.lng = ll.lng;
        // County is no longer accurate after drag; clear it.
        savedLocationState.county = null;
        _savedLocationUpdateReadout();
      });

      // Tap on map → move marker (alternative to dragging)
      map.on('click', function(e) {
        marker.setLatLng(e.latlng);
        savedLocationState.lat = e.latlng.lat;
        savedLocationState.lng = e.latlng.lng;
        savedLocationState.county = null;
        _savedLocationUpdateReadout();
      });

      savedLocationState.map = map;
      savedLocationState.marker = marker;

      if (loadingEl) loadingEl.classList.add('is-hidden');
      setTimeout(function() { try { map.invalidateSize(); } catch (e) {} }, 80);
    }

    function openSavedLocationModal(existingLoc) {
      if (!savedLocationModal) return;

      // Reset state + populate
      savedLocationState.editingId = existingLoc ? existingLoc.id : null;
      savedLocationState.mode = existingLoc ? 'edit' : 'add';
      savedLocationState.lat = existingLoc ? existingLoc.lat : null;
      savedLocationState.lng = existingLoc ? existingLoc.lng : null;
      savedLocationState.county = existingLoc ? existingLoc.county : null;

      if (savedLocationTitle) {
        savedLocationTitle.textContent = existingLoc ? 'Edit saved location' : 'Add saved location';
      }
      if (savedLocationDelete) {
        savedLocationDelete.hidden = !existingLoc;
      }
      if (savedLocationLabel) {
        savedLocationLabel.value = existingLoc ? (existingLoc.label || '') : '';
      }
      if (savedLocationAddress) {
        savedLocationAddress.value = existingLoc ? (existingLoc.address_text || '') : '';
      }
      if (savedLocationError) {
        savedLocationError.hidden = true;
        savedLocationError.textContent = '';
      }

      savedLocationModal.hidden = false;
      _savedLocationUpdateReadout();

      // Build map. Default center = Indianapolis if no existing coords.
      var lat = savedLocationState.lat != null ? savedLocationState.lat : 39.7684;
      var lng = savedLocationState.lng != null ? savedLocationState.lng : -86.1581;
      ensureLeafletForPinDrop(function() {
        // The marker only shows if there ARE coords. If not, we use a
        // soft pattern: map shows centered on default, but no pin until
        // a lookup or click drops one. For simplicity here, we always
        // show the marker — at default coords if user hasn't picked.
        _savedLocationBuildMap(lat, lng);
        // If there's no existing location, treat the default as "not yet set"
        // so the user knows they need to look up or click.
        if (savedLocationState.lat == null) {
          savedLocationState.lat = lat;
          savedLocationState.lng = lng;
          _savedLocationUpdateReadout();
        }
      });
    }
    function closeSavedLocationModal() {
      if (savedLocationModal) savedLocationModal.hidden = true;
      savedLocationState.editingId = null;
    }
    if (savedLocationScrim)  savedLocationScrim.addEventListener('click', closeSavedLocationModal);
    if (savedLocationClose)  savedLocationClose.addEventListener('click', closeSavedLocationModal);
    if (savedLocationCancel) savedLocationCancel.addEventListener('click', closeSavedLocationModal);

    // Look up — geocode + recenter map + drop pin
    if (savedLocationLookupBtn) {
      savedLocationLookupBtn.addEventListener('click', function() {
        var q = savedLocationAddress ? savedLocationAddress.value.trim() : '';
        if (!q) {
          if (savedLocationError) {
            savedLocationError.textContent = 'Type an address to look up.';
            savedLocationError.hidden = false;
          }
          return;
        }
        if (savedLocationError) { savedLocationError.hidden = true; }
        savedLocationLookupBtn.disabled = true;
        savedLocationLookupBtn.textContent = 'Looking up\u2026';

        fetch(BACKEND + '/api/v1/geocode?q=' + encodeURIComponent(q), {
          credentials: 'include'
        })
          .then(function(r) { return r.json().then(function(j) { return j; }); })
          .then(function(res) {
            savedLocationLookupBtn.disabled = false;
            savedLocationLookupBtn.textContent = 'Look up';
            if (res && res.ok && res.result) {
              savedLocationState.lat = res.result.lat;
              savedLocationState.lng = res.result.lng;
              savedLocationState.county = res.result.admin2 || null;
              _savedLocationUpdateReadout();
              ensureLeafletForPinDrop(function() {
                _savedLocationBuildMap(res.result.lat, res.result.lng);
              });
              // Auto-fill label from result if empty
              if (savedLocationLabel && !savedLocationLabel.value.trim()) {
                var labelGuess = res.result.name || q;
                if (res.result.admin1) labelGuess += ', ' + res.result.admin1;
                savedLocationLabel.value = labelGuess;
              }
            } else if (res && res.ok && !res.result) {
              if (savedLocationError) {
                savedLocationError.textContent = 'No match found. Try a more specific address or drag the pin manually.';
                savedLocationError.hidden = false;
              }
            } else {
              if (savedLocationError) {
                savedLocationError.textContent = 'Lookup failed. ' + (res && res.error ? '(' + res.error + ')' : '');
                savedLocationError.hidden = false;
              }
            }
          })
          .catch(function() {
            savedLocationLookupBtn.disabled = false;
            savedLocationLookupBtn.textContent = 'Look up';
            if (savedLocationError) {
              savedLocationError.textContent = 'Network error. Try again.';
              savedLocationError.hidden = false;
            }
          });
      });

      // Enter key in address input → trigger lookup
      if (savedLocationAddress) {
        savedLocationAddress.addEventListener('keydown', function(ev) {
          if (ev.key === 'Enter') {
            ev.preventDefault();
            savedLocationLookupBtn.click();
          }
        });
      }
    }

    // Save — POST or PATCH
    if (savedLocationSave) {
      savedLocationSave.addEventListener('click', function() {
        if (savedLocationError) { savedLocationError.hidden = true; }
        var label = (savedLocationLabel ? savedLocationLabel.value.trim() : '') || '';
        var address = (savedLocationAddress ? savedLocationAddress.value.trim() : '') || '';
        if (!label) {
          if (savedLocationError) {
            savedLocationError.textContent = 'Add a label for this location (e.g. "Home").';
            savedLocationError.hidden = false;
          }
          return;
        }
        if (savedLocationState.lat == null || savedLocationState.lng == null) {
          if (savedLocationError) {
            savedLocationError.textContent = 'Pick a location first (look up an address or drag the pin).';
            savedLocationError.hidden = false;
          }
          return;
        }

        var body = {
          label: label,
          address_text: address || null,
          lat: savedLocationState.lat,
          lng: savedLocationState.lng,
          county: savedLocationState.county || null
        };

        var url, method;
        if (savedLocationState.editingId != null) {
          url = BACKEND + '/api/v1/me/saved-locations/' + savedLocationState.editingId;
          method = 'PATCH';
        } else {
          url = BACKEND + '/api/v1/me/saved-locations';
          method = 'POST';
        }

        savedLocationSave.disabled = true;
        savedLocationSave.textContent = 'Saving\u2026';
        fetch(url, {
          method: method,
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify(body)
        })
          .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
          .then(function(res) {
            savedLocationSave.disabled = false;
            savedLocationSave.textContent = 'Save location';
            if (res.body && res.body.ok) {
              closeSavedLocationModal();
              if (typeof loadSubscriberPortal === 'function') loadSubscriberPortal();
            } else {
              if (savedLocationError) {
                savedLocationError.textContent = 'Could not save. ' + (res.body && res.body.error ? '(' + res.body.error + ')' : '');
                savedLocationError.hidden = false;
              }
            }
          })
          .catch(function() {
            savedLocationSave.disabled = false;
            savedLocationSave.textContent = 'Save location';
            if (savedLocationError) {
              savedLocationError.textContent = 'Network error.';
              savedLocationError.hidden = false;
            }
          });
      });
    }

    // Delete (edit mode only)
    if (savedLocationDelete) {
      savedLocationDelete.addEventListener('click', function() {
        if (savedLocationState.editingId == null) return;
        if (!window.confirm('Delete this saved location?')) return;
        savedLocationDelete.disabled = true;
        fetch(BACKEND + '/api/v1/me/saved-locations/' + savedLocationState.editingId, {
          method: 'DELETE',
          credentials: 'include'
        })
          .then(function(r) { return r.json().then(function(j) { return j; }); })
          .then(function(res) {
            savedLocationDelete.disabled = false;
            if (res && res.ok) {
              closeSavedLocationModal();
              if (typeof loadSubscriberPortal === 'function') loadSubscriberPortal();
            } else {
              if (savedLocationError) {
                savedLocationError.textContent = 'Could not delete.';
                savedLocationError.hidden = false;
              }
            }
          })
          .catch(function() {
            savedLocationDelete.disabled = false;
            if (savedLocationError) {
              savedLocationError.textContent = 'Network error.';
              savedLocationError.hidden = false;
            }
          });
      });
    }

    // ─── Brief delivery time modal (Phase 11, May 17) ─────────────
    // Subscriber sets when they want their daily brief delivered (in
    // their local timezone). The assigned Met sees this deadline in
    // their workspace.
    var deliveryModal = $('portal-brief-delivery-modal');
    var deliveryEditBtn = $('portal-brief-delivery-edit');
    var deliveryClose = $('portal-brief-delivery-close');
    var deliveryScrim = $('portal-brief-delivery-scrim');
    var deliveryCancel = $('portal-brief-delivery-cancel');
    var deliverySave = $('portal-brief-delivery-save');
    var deliveryDisplay = $('portal-brief-delivery-display');
    var deliveryTz = $('portal-brief-delivery-tz');
    var deliveryError = $('portal-brief-delivery-error');

    function _formatTime12h(hhmm) {
      if (!hhmm || hhmm.length !== 5) return hhmm || '';
      var h = parseInt(hhmm.slice(0, 2), 10);
      var m = hhmm.slice(3, 5);
      var period = h >= 12 ? 'PM' : 'AM';
      var h12 = h % 12 || 12;
      return h12 + ':' + m + ' ' + period;
    }

    function loadDeliveryPref() {
      if (!deliveryDisplay) return;
      fetch(BACKEND + '/api/v1/me/brief-delivery-time', { credentials: 'include' })
        .then(function(r) { return r.json(); })
        .then(function(res) {
          if (!res || !res.ok) return;
          if (deliveryDisplay) deliveryDisplay.textContent = _formatTime12h(res.delivery_time || '07:00');
          if (deliveryTz) deliveryTz.textContent = (res.delivery_timezone || 'America/New_York') + ' · your local time';
        })
        .catch(function() {});
    }

    function openDeliveryModal() {
      if (!deliveryModal) return;
      // Pre-populate with current value
      fetch(BACKEND + '/api/v1/me/brief-delivery-time', { credentials: 'include' })
        .then(function(r) { return r.json(); })
        .then(function(res) {
          var timeEl = $('portal-brief-delivery-time');
          var tzEl = $('portal-brief-delivery-timezone');
          if (timeEl && res && res.delivery_time) timeEl.value = res.delivery_time;
          if (tzEl && res && res.delivery_timezone) tzEl.value = res.delivery_timezone;
        })
        .catch(function() {});
      if (deliveryError) deliveryError.hidden = true;
      deliveryModal.hidden = false;
    }

    function closeDeliveryModal() {
      if (deliveryModal) deliveryModal.hidden = true;
    }

    function saveDeliveryPref() {
      var timeEl = $('portal-brief-delivery-time');
      var tzEl = $('portal-brief-delivery-timezone');
      var newTime = timeEl ? timeEl.value : '';
      var newTz = tzEl ? tzEl.value : 'America/New_York';
      if (!newTime || newTime.length !== 5) {
        if (deliveryError) { deliveryError.textContent = 'Please enter a valid time.'; deliveryError.hidden = false; }
        return;
      }
      if (deliverySave) { deliverySave.disabled = true; deliverySave.textContent = 'Saving…'; }
      fetch(BACKEND + '/api/v1/me/brief-delivery-time', {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify({ delivery_time: newTime, delivery_timezone: newTz })
      })
        .then(function(r) { return r.json(); })
        .then(function(res) {
          if (deliverySave) { deliverySave.disabled = false; deliverySave.textContent = 'Save'; }
          if (!res || !res.ok) {
            if (deliveryError) { deliveryError.textContent = (res && (res.message || res.error)) || 'Save failed.'; deliveryError.hidden = false; }
            return;
          }
          closeDeliveryModal();
          loadDeliveryPref();
        })
        .catch(function() {
          if (deliverySave) { deliverySave.disabled = false; deliverySave.textContent = 'Save'; }
          if (deliveryError) { deliveryError.textContent = 'Network error.'; deliveryError.hidden = false; }
        });
    }

    if (deliveryEditBtn) deliveryEditBtn.addEventListener('click', openDeliveryModal);
    if (deliveryClose) deliveryClose.addEventListener('click', closeDeliveryModal);
    if (deliveryScrim) deliveryScrim.addEventListener('click', closeDeliveryModal);
    if (deliveryCancel) deliveryCancel.addEventListener('click', closeDeliveryModal);
    if (deliverySave) deliverySave.addEventListener('click', saveDeliveryPref);

    // Load current preference on page render
    loadDeliveryPref();
  }


  function setupTeamPanel() {
    // Tab switcher — Dashboard / Team / Operations
    var tabs = document.querySelectorAll('.cmd-tab');
    var panels = document.querySelectorAll('.cmd-tab-panel');
    tabs.forEach(function(tab) {
      tab.addEventListener('click', function() {
        var target = tab.getAttribute('data-cmd-tab');
        tabs.forEach(function(t) { t.classList.toggle('is-active', t === tab); });
        panels.forEach(function(p) {
          p.classList.toggle('is-active', p.getAttribute('data-cmd-panel') === target);
        });
        // Phase 10 Admin Chunk C: lazy-load Operations content on tab switch
        if (target === 'operations' && typeof loadAdminOperations === 'function') {
          try { loadAdminOperations(); } catch (e) { console.warn('admin ops load failed', e); }
        }
      });
    });

    // ─── Real API layer ────────────────────────────────────────────
    // Connects to /api/v1/admin/users on the backend. All four functions
    // return Promises with the same shape the prior stubs returned, so
    // the rest of the panel UI doesn't need to change.
    //
    // Auth: every call uses credentials:'include' to send the session
    // cookie. The backend's @require_role('admin') decorator gates each
    // endpoint, so a non-admin user gets 401/403 and the UI surfaces it.
    var TEAM_API_BASE = 'https://wv-valet-backend.onrender.com';

    // Helper — convert backend errors into stub-shaped {error: string}
    // rejections. Frontend renderers already display .error nicely.
    function teamApiError(res) {
      var code = (res && res.body && res.body.error) || 'unknown-error';
      var humanMap = {
        'missing-name':         'Please provide a name.',
        'invalid-email':        'That email address looks invalid.',
        'invalid-role':         'That role isn\u2019t recognized.',
        'email-already-exists': 'A member with that email already exists.',
        'no-such-user':         'That member no longer exists. Refresh and try again.',
        'not-authenticated':    'Your admin session expired. Sign in again.',
        'forbidden':            'You don\u2019t have permission to do that.',
        'create-failed':        'Couldn\u2019t create the member. Please try again.',
        'update-failed':        'Couldn\u2019t save changes. Please try again.',
        'deactivate-failed':    'Couldn\u2019t deactivate the member. Please try again.'
      };
      return { error: humanMap[code] || ('Something went wrong (' + code + ').') };
    }

    function apiListMembers() {
      return fetch(TEAM_API_BASE + '/api/v1/admin/users', {
        credentials: 'include'
      })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (res.status === 200 && res.body && res.body.ok) {
          return res.body.members || [];
        }
        return Promise.reject(teamApiError(res));
      });
    }

    function apiAddMember(payload) {
      return fetch(TEAM_API_BASE + '/api/v1/admin/users', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify({
          name: payload.name,
          email: payload.email,
          role: payload.role
        })
      })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (res.status === 200 && res.body && res.body.ok) {
          return {
            member: res.body.member,
            temp_password: res.body.temp_password
          };
        }
        return Promise.reject(teamApiError(res));
      });
    }

    function apiUpdateMember(id, payload) {
      // Frontend stores ids as "u_<int>" strings. Backend endpoint takes
      // the bare integer. Strip the prefix.
      var numericId = String(id).replace(/^u_/, '');
      var body = {};
      if (payload.name !== undefined)            body.name = payload.name;
      if (payload.email !== undefined)           body.email = payload.email;
      if (payload.role !== undefined)            body.role = payload.role;
      if (payload.is_active !== undefined)       body.is_active = payload.is_active;
      if (payload.reset_password !== undefined)  body.reset_password = payload.reset_password;

      return fetch(TEAM_API_BASE + '/api/v1/admin/users/' + numericId, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include',
        body: JSON.stringify(body)
      })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (res.status === 200 && res.body && res.body.ok) {
          var result = { member: res.body.member };
          if (res.body.temp_password) result.temp_password = res.body.temp_password;
          return result;
        }
        return Promise.reject(teamApiError(res));
      });
    }

    function apiDeactivateMember(id) {
      var numericId = String(id).replace(/^u_/, '');
      return fetch(TEAM_API_BASE + '/api/v1/admin/users/' + numericId, {
        method: 'DELETE',
        credentials: 'include'
      })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (res.status === 200 && res.body && res.body.ok) {
          // Backend returns just {ok:true} on success; the renderer needs a
          // member object so we synthesize a minimal one with is_active:false.
          // The next list-refresh will get the full row.
          return { member: { id: id, is_active: false } };
        }
        return Promise.reject(teamApiError(res));
      });
    }

    // Temp password generator — kept around because some test paths still
    // call it. Real backend generates server-side and we use what comes
    // back in apiAddMember / apiUpdateMember responses; this is fallback.
    function generateTempPassword() {
      var words = ['storm','river','cedar','quiet','amber','north','calm','flint','dune','marsh','pine','vivid'];
      function pick() { return words[Math.floor(Math.random() * words.length)]; }
      var w1 = pick(), w2 = pick(), w3 = pick();
      while (w2 === w1) w2 = pick();
      while (w3 === w1 || w3 === w2) w3 = pick();
      var digits = Math.floor(Math.random() * 90) + 10;
      return w1 + '-' + w2 + '-' + w3 + '-' + digits;
    }

    // ─── Rendering ──────────────────────────────────────────────────
    function renderTeamList() {
      var list = $('team-list');
      if (!list) return;
      // Keep the header, replace everything after it.
      var header = list.querySelector('.team-list-header');
      list.innerHTML = '';
      if (header) list.appendChild(header);

      apiListMembers().then(function(members) {
        // Cache the latest member list so modals can look up by id
        // without re-fetching. Replaces the old window.TEAM_MEMBERS
        // stub array. Refreshed on every renderTeamList() call.
        window._teamMembersCache = members.slice();

        if (!members.length) {
          var empty = document.createElement('div');
          empty.className = 'team-empty';
          empty.textContent = 'No team members yet. Click "Add member" to invite the first one.';
          list.appendChild(empty);
          return;
        }
        members.forEach(function(m) {
          var row = document.createElement('div');
          row.className = 'team-list-row' + (m.is_active ? '' : ' is-inactive');
          row.setAttribute('data-member-id', m.id);

          var roleLabel = m.role === 'meteorologist' ? 'Met'
                        : m.role === 'admin'         ? 'Admin'
                        : m.role === 'crew'          ? 'Crew'
                        : m.role;

          row.innerHTML =
            '<div class="team-list-name">' + escapeHtml(m.name) + '</div>' +
            '<div class="team-list-email">' + escapeHtml(m.email) + '</div>' +
            '<div><span class="team-list-role role-' + escapeHtml(m.role) + '">' + roleLabel + '</span></div>' +
            '<div class="team-list-meta">' + escapeHtml(m.last_login || '—') + '</div>' +
            '<div class="team-list-status ' + (m.is_active ? 'is-active' : 'is-inactive') + '">' +
              (m.is_active ? 'Active' : 'Inactive') +
            '</div>' +
            '<div class="team-list-actions">' +
              '<button type="button" class="team-action-btn" data-team-edit="' + escapeHtml(m.id) + '">Edit</button>' +
              (m.is_active
                ? '<button type="button" class="team-action-btn is-danger" data-team-deactivate="' + escapeHtml(m.id) + '">Deactivate</button>'
                : '<button type="button" class="team-action-btn" data-team-reactivate="' + escapeHtml(m.id) + '">Reactivate</button>'
              ) +
            '</div>';
          list.appendChild(row);
        });
      });
    }

    // ─── Add / Edit modal ───────────────────────────────────────────
    // Used for both Add (when state.editingId is null) and Edit (when set).
    // After save, the temp-password card is shown for Add and for password
    // resets; after that the modal closes on its own when the user clicks
    // Close.
    var modalState = {
      editingId: null,
      showingTempPassword: false
    };

    function openAddModal() {
      modalState.editingId = null;
      modalState.showingTempPassword = false;
      $('team-modal-title').textContent = 'Add member';
      $('team-modal-sub').textContent = "They'll get a temporary password and be required to change it on first login.";
      $('team-modal-name').value = '';
      $('team-modal-email').value = '';
      $('team-modal-role').value = 'meteorologist';
      $('team-modal-error').hidden = true;
      $('team-temp-pw').hidden = true;
      $('team-modal-submit').textContent = 'Save';
      $('team-modal-submit').disabled = false;
      // Add flow — hide the reset password row (only relevant when editing)
      var resetRow = $('team-modal-reset-row');
      if (resetRow) resetRow.hidden = true;
      // Re-show form fields in case they were hidden by a previous Add flow
      var fields = document.querySelectorAll('#team-modal-scrim .team-modal-field');
      fields.forEach(function(f) { f.style.display = ''; });
      $('team-modal-scrim').hidden = false;
      $('team-modal-name').focus();
    }

    function _findCachedMember(id) {
      var cache = window._teamMembersCache || [];
      for (var i = 0; i < cache.length; i++) {
        if (cache[i].id === id) return cache[i];
      }
      return null;
    }

    function openEditModal(id) {
      var member = _findCachedMember(id);
      if (!member) return;
      modalState.editingId = id;
      modalState.showingTempPassword = false;
      $('team-modal-title').textContent = 'Edit member';
      $('team-modal-sub').textContent = "Update details, or reset their password to issue a new temp one.";
      $('team-modal-name').value = member.name;
      $('team-modal-email').value = member.email;
      $('team-modal-role').value = member.role;
      $('team-modal-error').hidden = true;
      $('team-temp-pw').hidden = true;
      $('team-modal-submit').textContent = 'Save changes';
      $('team-modal-submit').disabled = false;
      // Edit flow — show the reset password row
      var resetRow = $('team-modal-reset-row');
      if (resetRow) resetRow.hidden = false;
      var fields = document.querySelectorAll('#team-modal-scrim .team-modal-field');
      fields.forEach(function(f) { f.style.display = ''; });
      $('team-modal-scrim').hidden = false;
    }

    function closeModal() {
      $('team-modal-scrim').hidden = true;
      modalState.editingId = null;
      modalState.showingTempPassword = false;
      // Reset the temp-password reveal state for next open
      var revealed = $('team-temp-pw-revealed');
      var revealBtn = $('team-temp-pw-reveal-btn');
      var revealWrap = $('team-temp-pw-reveal-wrap');
      if (revealed) revealed.hidden = true;
      if (revealWrap) revealWrap.hidden = false;
      if (revealBtn) revealBtn.textContent = 'Show temp password (backup)';
      var resetRow = $('team-modal-reset-row');
      if (resetRow) resetRow.hidden = true;
    }

    function showTempPassword(password) {
      // Store the password as a data attribute so the reveal button can show it
      $('team-temp-pw-value').textContent = password;
      $('team-temp-pw').hidden = false;
      // Reset reveal state — hide the password block, show the reveal button
      var revealed = $('team-temp-pw-revealed');
      var revealBtn = $('team-temp-pw-reveal-btn');
      var revealWrap = $('team-temp-pw-reveal-wrap');
      if (revealed) revealed.hidden = true;
      if (revealWrap) revealWrap.hidden = false;
      if (revealBtn) revealBtn.textContent = 'Show temp password (backup)';
      // Hide form fields, swap submit button to "Done"
      var fields = document.querySelectorAll('#team-modal-scrim .team-modal-field');
      fields.forEach(function(f) { f.style.display = 'none'; });
      // Hide the reset row in this success state
      var resetRow = $('team-modal-reset-row');
      if (resetRow) resetRow.hidden = true;
      $('team-modal-sub').textContent = '';
      $('team-modal-submit').textContent = 'Done';
      $('team-modal-submit').disabled = false;
      modalState.showingTempPassword = true;
    }

    function submitModal() {
      // If we're showing the temp password, the submit button acts as "Done"
      if (modalState.showingTempPassword) {
        closeModal();
        return;
      }
      var name = $('team-modal-name').value.trim();
      var email = $('team-modal-email').value.trim();
      var role = $('team-modal-role').value;
      var errorEl = $('team-modal-error');

      // Validation
      if (!name) {
        errorEl.textContent = 'Please enter a name.';
        errorEl.hidden = false;
        return;
      }
      if (!email || email.indexOf('@') === -1) {
        errorEl.textContent = 'Please enter a valid email.';
        errorEl.hidden = false;
        return;
      }

      $('team-modal-submit').disabled = true;
      errorEl.hidden = true;

      if (modalState.editingId) {
        apiUpdateMember(modalState.editingId, { name: name, email: email, role: role })
          .then(function() {
            renderTeamList();
            closeModal();
          })
          .catch(function(err) {
            errorEl.textContent = (err && err.error) || 'Something went wrong. Try again.';
            errorEl.hidden = false;
            $('team-modal-submit').disabled = false;
          });
      } else {
        apiAddMember({ name: name, email: email, role: role })
          .then(function(result) {
            renderTeamList();
            showTempPassword(result.temp_password);
          })
          .catch(function(err) {
            errorEl.textContent = (err && err.error) || 'Something went wrong. Try again.';
            errorEl.hidden = false;
            $('team-modal-submit').disabled = false;
          });
      }
    }

    // ─── Confirm deactivate modal ───────────────────────────────────
    var pendingDeactivateId = null;

    function openDeactivateConfirm(id) {
      var member = _findCachedMember(id);
      if (!member) return;
      pendingDeactivateId = id;
      $('team-confirm-title').textContent = 'Deactivate ' + member.name + '?';
      $('team-confirm-error').hidden = true;
      $('team-confirm-scrim').hidden = false;
    }

    function closeDeactivateConfirm() {
      $('team-confirm-scrim').hidden = true;
      pendingDeactivateId = null;
    }

    function confirmDeactivate() {
      if (!pendingDeactivateId) return;
      $('team-confirm-submit').disabled = true;
      apiDeactivateMember(pendingDeactivateId)
        .then(function() {
          renderTeamList();
          closeDeactivateConfirm();
          $('team-confirm-submit').disabled = false;
        })
        .catch(function(err) {
          $('team-confirm-error').textContent = (err && err.error) || 'Something went wrong. Try again.';
          $('team-confirm-error').hidden = false;
          $('team-confirm-submit').disabled = false;
        });
    }

    function reactivateMember(id) {
      // Real: PATCH /admin/users/:id with { is_active: true } so the
      // user can sign in again. Sessions remain killed from deactivation,
      // so they'll need to re-auth.
      apiUpdateMember(id, { is_active: true })
        .then(function() { renderTeamList(); })
        .catch(function(err) {
          alert((err && err.error) || 'Couldn\u2019t reactivate that member.');
        });
    }

    // ─── Wiring ─────────────────────────────────────────────────────
    var addBtn = $('team-add-btn');
    if (addBtn) addBtn.addEventListener('click', openAddModal);

    var modalCancel = $('team-modal-cancel');
    if (modalCancel) modalCancel.addEventListener('click', closeModal);

    var modalSubmit = $('team-modal-submit');
    if (modalSubmit) modalSubmit.addEventListener('click', submitModal);

    // Reveal temp password (backup) button — hidden behind a click so admin
    // doesn't see the password unless they specifically need it (e.g. the
    // welcome email didn't arrive).
    var revealBtn = $('team-temp-pw-reveal-btn');
    if (revealBtn) {
      revealBtn.addEventListener('click', function() {
        var revealed = $('team-temp-pw-revealed');
        var revealWrap = $('team-temp-pw-reveal-wrap');
        if (revealed) revealed.hidden = false;
        if (revealWrap) revealWrap.hidden = true;
      });
    }

    // Reset password button (Edit modal only). Calls the existing
    // PATCH /api/v1/admin/users/<id> with action=reset_password, which
    // generates a new temp + emails it to the user. We surface success
    // by showing the temp-password block (hidden by default).
    var resetBtn = $('team-modal-reset-btn');
    if (resetBtn) {
      resetBtn.addEventListener('click', function() {
        if (!modalState.editingId) return;
        if (!window.confirm("Reset this member's password? A new temp password will be emailed to them.")) return;
        resetBtn.disabled = true;
        resetBtn.textContent = 'Sending…';
        // Reuse the existing apiUpdateMember helper — it strips the
        // "u_" prefix from the cached id and handles error shapes.
        apiUpdateMember(modalState.editingId, { reset_password: true })
          .then(function(result) {
            resetBtn.disabled = false;
            resetBtn.textContent = 'Reset password & email a new temp';
            // result.temp_password is the new temp (if backend returned one)
            showTempPassword(result.temp_password || '');
          })
          .catch(function(err) {
            resetBtn.disabled = false;
            resetBtn.textContent = 'Reset password & email a new temp';
            console.error('[reset-password] failed:', err);
            var msg = (err && err.error) || 'Reset failed.';
            window.alert('Could not reset password: ' + msg);
          });
      });
    }

    var confirmCancel = $('team-confirm-cancel');
    if (confirmCancel) confirmCancel.addEventListener('click', closeDeactivateConfirm);

    var confirmSubmit = $('team-confirm-submit');
    if (confirmSubmit) confirmSubmit.addEventListener('click', confirmDeactivate);

    // Close modal on scrim click (outside the modal box)
    var modalScrim = $('team-modal-scrim');
    if (modalScrim) {
      modalScrim.addEventListener('click', function(e) {
        if (e.target === modalScrim) closeModal();
      });
    }
    var confirmScrim = $('team-confirm-scrim');
    if (confirmScrim) {
      confirmScrim.addEventListener('click', function(e) {
        if (e.target === confirmScrim) closeDeactivateConfirm();
      });
    }

    // Delegated row-action handler — Edit / Deactivate / Reactivate
    var list = $('team-list');
    if (list) {
      list.addEventListener('click', function(e) {
        var editBtn = e.target.closest('[data-team-edit]');
        if (editBtn) {
          openEditModal(editBtn.getAttribute('data-team-edit'));
          return;
        }
        var deactivateBtn = e.target.closest('[data-team-deactivate]');
        if (deactivateBtn) {
          openDeactivateConfirm(deactivateBtn.getAttribute('data-team-deactivate'));
          return;
        }
        var reactivateBtn = e.target.closest('[data-team-reactivate]');
        if (reactivateBtn) {
          reactivateMember(reactivateBtn.getAttribute('data-team-reactivate'));
          return;
        }
      });
    }

    // Initial render
    renderTeamList();
  }


  // ════════════════════════════════════════════════════════════════════
  // Admin Operations panel (Phase 10 Admin Chunk C)
  // ════════════════════════════════════════════════════════════════════
  // Four sections: Crew applications, Users, Payments, Audit log.
  // Each loads from its backend endpoint. Refresh buttons re-fetch.
  // Action buttons (Approve, Reject, Deactivate, Refund) call the
  // corresponding endpoint and re-render.

  function loadAdminOperations() {
    loadAdminCrewApplications();
    loadAdminUsers();
    loadAdminPayments();
    loadAdminPayroll();
    loadAdminCommissions();
    loadAdminAuditLog();
  }


  // ── Admin Payroll dashboard ──────────────────────────────────────────
  // Loads /api/v1/admin/payroll/<year>/<month> and renders per-Met totals.
  // Month selector defaults to last month (the typical payroll workflow:
  // on the 1st, you look back at last month to enter numbers).
  function _adminPopulatePayrollMonths() {
    var sel = $('admin-payroll-month');
    if (!sel) return;
    if (sel.options.length > 0) return;  // already populated
    // List last 12 months. Default = "last completed month" (prev month).
    var now = new Date();
    var months = ['January','February','March','April','May','June',
                  'July','August','September','October','November','December'];
    for (var i = 0; i < 12; i++) {
      var d = new Date(now.getFullYear(), now.getMonth() - i, 1);
      var opt = document.createElement('option');
      opt.value = d.getFullYear() + '-' + (d.getMonth() + 1);
      opt.textContent = months[d.getMonth()] + ' ' + d.getFullYear();
      if (i === 1) opt.selected = true;  // default to prev month
      sel.appendChild(opt);
    }
  }

  function loadAdminPayroll() {
    _adminPopulatePayrollMonths();
    var sel = $('admin-payroll-month');
    var content = $('admin-payroll-content');
    if (!sel || !content) return;
    var parts = (sel.value || '').split('-');
    if (parts.length !== 2) return;
    var year = parseInt(parts[0], 10);
    var month = parseInt(parts[1], 10);
    if (!year || !month) return;

    content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>';

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/admin/payroll/' + year + '/' + month, { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">'
            + (res.status === 403 ? 'Admin only.' : 'Could not load payroll.')
            + '</div>';
          return;
        }
        renderAdminPayroll(res.body);
      })
      .catch(function() {
        content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Network error.</div>';
      });
  }

  function renderAdminPayroll(data) {
    var content = $('admin-payroll-content');
    if (!content) return;
    var mets = data.mets || [];
    var house = data.house || {};

    var fmt = function(c) { return '$' + ((c || 0) / 100).toFixed(2); };

    var html = '';

    // Per-Met breakdown table
    if (mets.length === 0) {
      html += '<div style="padding:30px 20px;text-align:center;color:rgba(255,255,255,0.55);font-size:13px;">No Mets in the system yet.</div>';
    } else {
      html += '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr 1fr;gap:14px;padding:10px 0;border-bottom:1px solid rgba(255,255,255,0.10);font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.55);">'
        + '<div>Met</div>'
        + '<div style="text-align:right;">$19 reviews</div>'
        + '<div style="text-align:right;">Brief share</div>'
        + '<div style="text-align:right;">Tips</div>'
        + '<div style="text-align:right;">Total</div>'
        + '</div>';
      for (var i = 0; i < mets.length; i++) {
        var m = mets[i];
        html += '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr 1fr;gap:14px;padding:12px 0;border-bottom:1px solid rgba(255,255,255,0.05);font-size:13px;color:#fff;align-items:center;">'
          + '<div>'
          +   '<div style="font-weight:500;">' + escapeHtml(m.name || '\u2014') + '</div>'
          +   '<div style="font-size:11px;color:rgba(255,255,255,0.50);margin-top:2px;">' + escapeHtml(m.email || '') + '</div>'
          + '</div>'
          + '<div style="text-align:right;">'
          +   '<div>' + fmt(m.review_cents) + '</div>'
          +   '<div style="font-size:10.5px;color:rgba(255,255,255,0.45);">' + m.review_count + ' review' + (m.review_count === 1 ? '' : 's') + '</div>'
          + '</div>'
          + '<div style="text-align:right;">'
          +   '<div>' + fmt(m.brief_cents) + '</div>'
          +   '<div style="font-size:10.5px;color:rgba(255,255,255,0.45);">' + m.brief_count + ' brief' + (m.brief_count === 1 ? '' : 's') + '</div>'
          + '</div>'
          + '<div style="text-align:right;">'
          +   '<div>' + fmt(m.tip_cents) + '</div>'
          +   '<div style="font-size:10.5px;color:rgba(255,255,255,0.45);">' + m.tip_count + ' tip' + (m.tip_count === 1 ? '' : 's') + '</div>'
          + '</div>'
          + '<div style="text-align:right;font-weight:600;color:#5DECA8;">' + fmt(m.total_cents) + '</div>'
          + '</div>';
      }
    }

    // House revenue
    html += '<div style="margin-top:18px;padding:14px 16px;background:rgba(255,255,255,0.03);border-radius:8px;font-size:12px;color:rgba(255,255,255,0.65);">'
      + '<div style="font-weight:700;text-transform:uppercase;letter-spacing:0.06em;font-size:10.5px;color:rgba(255,255,255,0.45);margin-bottom:8px;">House (non-Met revenue)</div>'
      + '<div style="display:flex;justify-content:space-between;padding:3px 0;">'
      +   '<span>Hobbyist subscriptions</span>'
      +   '<span style="color:#fff;">' + fmt(house.hobbyist_revenue_cents) + '</span>'
      + '</div>'
      + '<div style="display:flex;justify-content:space-between;padding:3px 0;">'
      +   '<span>Unattributed Pro days <span style="opacity:0.6;">(brief not sent)</span></span>'
      +   '<span style="color:#fff;">' + fmt(house.unattributed_pro_cents) + '</span>'
      + '</div>'
      + '<div style="display:flex;justify-content:space-between;padding:6px 0 0;margin-top:6px;border-top:1px solid rgba(255,255,255,0.08);font-weight:600;color:#fff;">'
      +   '<span>House total</span>'
      +   '<span>' + fmt(house.total_cents) + '</span>'
      + '</div>'
      + '</div>';

    content.innerHTML = html;
  }


  // ── Admin Sales Commissions tile (Starter Month funnel) ──────────────
  function _adminPopulateCommMonths() {
    var sel = $('admin-comm-month');
    if (!sel) return;
    if (sel.options.length > 0) return;
    var now = new Date();
    var months = ['January','February','March','April','May','June',
                  'July','August','September','October','November','December'];
    for (var i = 0; i < 12; i++) {
      var d = new Date(now.getFullYear(), now.getMonth() - i, 1);
      var opt = document.createElement('option');
      opt.value = d.getFullYear() + '-' + (d.getMonth() + 1);
      opt.textContent = months[d.getMonth()] + ' ' + d.getFullYear();
      if (i === 0) opt.selected = true;  // default to THIS month (live tracking)
      sel.appendChild(opt);
    }
  }

  // ════════════════════════════════════════════════════════════════════
  // Admin Coverage Dashboard (Phase 11, May 17)
  // ════════════════════════════════════════════════════════════════════

  var ADMIN_COVERAGE_DATA = null;  // cached for click handlers

  function loadAdminCoverage() {
    var content = $('admin-coverage-content');
    var liveEl = $('admin-coverage-live-count');
    if (!content) return;
    content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading…</div>';

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/admin/coverage-dashboard', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">'
            + (res.status === 403 ? 'Admin only.' : 'Could not load coverage.')
            + '</div>';
          return;
        }
        ADMIN_COVERAGE_DATA = res.body;
        if (liveEl) {
          var n = (res.body.live_mets || []).length;
          liveEl.textContent = n + ' Met' + (n === 1 ? '' : 's') + ' online now';
        }
        _renderAdminCoverage(res.body);
      })
      .catch(function() {
        content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(194,52,43,0.85);font-size:13px;">Network error.</div>';
      });
  }

  function _renderAdminCoverage(data) {
    var content = $('admin-coverage-content');
    if (!content) return;
    var days = data.days || [];
    var subs = data.subscriber_coverage || [];
    var pool = data.review_pool_coverage || [];

    // Day header labels (Mon Jan 5 style)
    var html = '<table style="width:100%; border-collapse:collapse; font-size:12px; min-width:800px;">';
    html += '<thead><tr style="border-bottom:1px solid rgba(255,255,255,0.10);">';
    html += '<th style="text-align:left;padding:8px 10px;color:rgba(255,255,255,0.55);font-weight:600;text-transform:uppercase;font-size:10px;letter-spacing:0.06em;">Subscriber</th>';
    for (var d = 0; d < days.length; d++) {
      var date = new Date(days[d].date + 'T12:00:00');
      var lbl = date.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });
      html += '<th style="text-align:left;padding:8px 6px;color:rgba(255,255,255,0.55);font-weight:600;text-transform:uppercase;font-size:10px;letter-spacing:0.04em;">' + lbl + '</th>';
    }
    html += '</tr></thead><tbody>';

    // Review Pool row (highlighted)
    html += '<tr style="border-bottom:1px solid rgba(255,255,255,0.06); background:rgba(46,79,184,0.06);">';
    html += '<td style="padding:8px 10px;color:#fff;font-weight:600;">⚙ Review Pool</td>';
    for (var pi = 0; pi < pool.length; pi++) {
      var p = pool[pi];
      var bg = p.is_gap ? 'rgba(194,52,43,0.40)' : 'rgba(18,161,80,0.22)';
      var fg = '#fff';
      var label = p.is_gap ? 'GAP' : ((p.covering_mets || []).map(function(m) { return m.name || '?'; }).join(', '));
      html += '<td style="padding:6px;"><div style="background:' + bg + ';color:' + fg + ';padding:5px 7px;border-radius:4px;font-size:11px;">' + escapeHtml(label) + '</div></td>';
    }
    html += '</tr>';

    if (subs.length === 0) {
      html += '<tr><td colspan="' + (days.length + 1) + '" style="padding:20px 10px;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">No Pro subscribers yet.</td></tr>';
    } else {
      for (var si = 0; si < subs.length; si++) {
        var sub = subs[si];
        html += '<tr style="border-bottom:1px solid rgba(255,255,255,0.06);">';
        html += '<td style="padding:8px 10px;">';
        html += '<button type="button" data-coverage-sub="' + sub.subscriber_id + '" style="background:transparent;border:0;color:#fff;font-weight:600;cursor:pointer;padding:0;text-align:left;font-size:12.5px;text-decoration:underline;text-decoration-color:rgba(255,255,255,0.25);text-underline-offset:3px;">' + escapeHtml(sub.subscriber_name || sub.subscriber_email || ('#' + sub.subscriber_id)) + '</button>';
        html += '<div style="font-size:10.5px;color:rgba(255,255,255,0.45);margin-top:2px;">' + escapeHtml(sub.tier || '') + ' · brief ' + escapeHtml(sub.daily_brief_time || '07:00') + '</div>';
        html += '<div style="font-size:10.5px;color:rgba(255,255,255,0.45);">Primary: ' + escapeHtml(sub.primary_met_name || '—') + '</div>';
        html += '</td>';
        for (var di = 0; di < sub.days.length; di++) {
          var dd = sub.days[di];
          // Three-state coloring:
          //   red GAP — nobody assigned
          //   yellow soft gap — primary Met is assigned but has no
          //     recurring shift this day (no one explicitly took it)
          //   green — actual recurring shift or override claim
          var bgC, lbl2, viaNote;
          if (dd.is_gap) {
            bgC = 'rgba(194,52,43,0.40)';
            lbl2 = 'GAP';
            viaNote = '';
          } else if (dd.is_soft_gap) {
            bgC = 'rgba(245,165,36,0.30)';
            lbl2 = (dd.assigned_met_name || 'Primary') + ' (no shift)';
            viaNote = '<div style="font-size:9px;color:rgba(255,255,255,0.65);margin-top:1px;">primary, not scheduled</div>';
          } else {
            bgC = 'rgba(18,161,80,0.22)';
            lbl2 = dd.assigned_met_name || 'assigned';
            viaNote = dd.via ? ('<div style="font-size:9px;color:rgba(255,255,255,0.55);margin-top:1px;">' + dd.via + '</div>') : '';
          }
          html += '<td style="padding:6px;"><div style="background:' + bgC + ';color:#fff;padding:5px 7px;border-radius:4px;font-size:11px;">' + escapeHtml(lbl2) + viaNote + '</div></td>';
        }
        html += '</tr>';
      }
    }
    html += '</tbody></table>';

    // Live Mets list below the table
    html += '<div style="margin-top:18px;padding-top:14px;border-top:1px solid rgba(255,255,255,0.08);">';
    html += '<div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.55);margin-bottom:6px;">Online right now</div>';
    if ((data.live_mets || []).length === 0) {
      html += '<div style="font-size:12px;color:rgba(255,255,255,0.45);">No Mets online.</div>';
    } else {
      var live = data.live_mets.map(function(m) {
        var ago = Math.max(1, Math.round((Date.now() - m.last_seen_at) / 60000));
        return '<span style="display:inline-block;background:rgba(18,161,80,0.18);color:#7ed8a0;padding:3px 9px;border-radius:12px;font-size:11px;margin-right:6px;margin-bottom:4px;">● ' + escapeHtml(m.name || '?') + ' (' + ago + 'm)</span>';
      });
      html += '<div>' + live.join('') + '</div>';
    }
    html += '</div>';

    content.innerHTML = html;

    // Wire subscriber-click → open assignment modal
    content.querySelectorAll('[data-coverage-sub]').forEach(function(btn) {
      btn.addEventListener('click', function() {
        var subId = parseInt(btn.getAttribute('data-coverage-sub'), 10);
        var sub = ADMIN_COVERAGE_DATA && ADMIN_COVERAGE_DATA.subscriber_coverage
          ? ADMIN_COVERAGE_DATA.subscriber_coverage.find(function(s) { return s.subscriber_id === subId; })
          : null;
        if (!sub) return;
        _openAdminAssignmentPrompt(sub);
      });
    });
  }

  function _openAdminAssignmentPrompt(sub) {
    // Simple prompt-based assignment for v1. (Modal can come in v2.)
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/admin/mets-list', { credentials: 'include' })
      .then(function(r) { return r.json(); })
      .then(function(res) {
        if (!res || !res.ok) {
          window.alert('Could not load Met list.');
          return;
        }
        var mets = res.mets || [];
        var metList = mets.map(function(m) { return m.id + ': ' + (m.name || m.email); }).join('\n');
        var currentPrimary = sub.primary_met_id || '';
        var newPrimary = window.prompt(
          'Set primary Met for ' + (sub.subscriber_name || sub.subscriber_email) + '\n\n'
          + 'Available Mets:\n' + metList + '\n\n'
          + 'Enter the ID number for primary Met (or leave blank to unassign):',
          currentPrimary
        );
        if (newPrimary === null) return;  // cancelled
        var newPrimaryId = newPrimary.trim() ? parseInt(newPrimary.trim(), 10) : null;
        if (newPrimary.trim() && !newPrimaryId) {
          window.alert('Invalid ID.');
          return;
        }
        // Now backup
        var currentBackup = sub.backup_met_id || '';
        var newBackup = window.prompt(
          'Set BACKUP Met (or leave blank for none):',
          currentBackup
        );
        if (newBackup === null) return;
        var newBackupId = newBackup.trim() ? parseInt(newBackup.trim(), 10) : null;
        if (newBackup.trim() && !newBackupId) {
          window.alert('Invalid ID for backup.');
          return;
        }

        fetch(BACKEND + '/api/v1/admin/subscriber-coverage/' + sub.subscriber_id, {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          credentials: 'include',
          body: JSON.stringify({
            primary_met_id: newPrimaryId,
            backup_met_id: newBackupId
          })
        })
          .then(function(r) { return r.json(); })
          .then(function(res) {
            if (res && res.ok) {
              window.alert('Updated.');
              loadAdminCoverage();
            } else {
              window.alert('Update failed.');
            }
          })
          .catch(function() { window.alert('Network error.'); });
      });
  }


  function loadAdminCommissions() {
    _adminPopulateCommMonths();
    var sel = $('admin-comm-month');
    var content = $('admin-comm-content');
    if (!sel || !content) return;
    var parts = (sel.value || '').split('-');
    if (parts.length !== 2) return;
    var year = parseInt(parts[0], 10);
    var month = parseInt(parts[1], 10);
    if (!year || !month) return;

    content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Loading&hellip;</div>';

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/admin/commissions/' + year + '/' + month, { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">'
            + (res.status === 403 ? 'Admin only.' : 'Could not load commissions.')
            + '</div>';
          return;
        }
        renderAdminCommissions(res.body);
      })
      .catch(function() {
        content.innerHTML = '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">Network error.</div>';
      });
  }

  function renderAdminCommissions(data) {
    var content = $('admin-comm-content');
    if (!content) return;
    var reps = data.reps || [];
    var fmt = function(c) { return '$' + ((c || 0) / 100).toFixed(2); };

    if (reps.length === 0) {
      content.innerHTML = '<div style="padding:30px 20px;text-align:center;color:rgba(255,255,255,0.55);font-size:13px;line-height:1.6;">'
        + '<div style="font-weight:600;color:#fff;margin-bottom:6px;">No sales reps yet</div>'
        + 'Click <strong>+ Add Rep</strong> above to create your first sales rep. Then share <code style="background:rgba(255,255,255,0.08);padding:2px 5px;border-radius:3px;">weathervalet.ai/starter?rep=their-slug</code> with prospects.'
        + '</div>';
      return;
    }

    // Compute period totals
    var grandTotalCents = 0;
    var grandCustomerCount = 0;
    for (var k = 0; k < reps.length; k++) {
      grandTotalCents += reps[k].commission_cents || 0;
      grandCustomerCount += reps[k].customer_count || 0;
    }

    var html = '';

    // Summary row
    html += '<div style="display:flex;gap:24px;padding:12px 16px;background:rgba(255,255,255,0.03);border-radius:8px;margin-bottom:18px;">'
      + '<div><div style="font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.55);">' + escapeHtml(data.month) + '</div>'
      +     '<div style="font-size:18px;font-weight:600;color:#fff;margin-top:2px;">' + fmt(grandTotalCents) + ' total</div></div>'
      + '<div style="border-left:1px solid rgba(255,255,255,0.08);padding-left:24px;">'
      +     '<div style="font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.55);">Active customers</div>'
      +     '<div style="font-size:18px;font-weight:600;color:#fff;margin-top:2px;">' + grandCustomerCount + '</div></div>'
      + '</div>';

    // Per-rep breakdown
    html += '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr;gap:14px;padding:10px 0;border-bottom:1px solid rgba(255,255,255,0.10);font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.55);">'
      + '<div>Rep</div>'
      + '<div style="text-align:right;">Customers</div>'
      + '<div style="text-align:right;">Commission</div>'
      + '<div></div>'
      + '</div>';

    for (var i = 0; i < reps.length; i++) {
      var r = reps[i];
      html += '<div style="border-bottom:1px solid rgba(255,255,255,0.05);">'
        + '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr;gap:14px;padding:12px 0;align-items:center;">'
        +   '<div>'
        +     '<div style="font-size:13.5px;color:#fff;font-weight:500;">' + escapeHtml(r.name || '\u2014') + (r.is_active ? '' : ' <span style="font-size:10px;color:rgba(255,255,255,0.45);font-weight:400;">(inactive)</span>') + '</div>'
        +     '<div style="font-size:11px;color:rgba(255,255,255,0.50);margin-top:1px;">/starter?rep=<strong style="color:rgba(255,255,255,0.75);">' + escapeHtml(r.slug) + '</strong></div>'
        +   '</div>'
        +   '<div style="text-align:right;color:#fff;">' + r.customer_count + '</div>'
        +   '<div style="text-align:right;color:#5DECA8;font-weight:600;">' + fmt(r.commission_cents) + '</div>'
        +   '<div style="text-align:right;">'
        +     (r.customers && r.customers.length > 0
          ? '<button type="button" data-comm-expand="' + escapeHtml(r.slug) + '" style="background:transparent;border:1px solid rgba(255,255,255,0.15);color:rgba(255,255,255,0.65);padding:4px 10px;border-radius:5px;font-size:11px;cursor:pointer;">Details</button>'
          : '')
        +   '</div>'
        + '</div>';

      // Expandable per-customer detail (hidden by default)
      if (r.customers && r.customers.length > 0) {
        html += '<div id="comm-detail-' + escapeHtml(r.slug) + '" hidden style="background:rgba(0,0,0,0.18);padding:10px 16px;margin:0 -16px 12px;border-radius:6px;">';
        html += '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr 1fr;gap:10px;padding:6px 0;border-bottom:1px solid rgba(255,255,255,0.08);font-size:9.5px;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:rgba(255,255,255,0.50);">'
          + '<div>Customer</div>'
          + '<div>Tier</div>'
          + '<div style="text-align:right;">Month #</div>'
          + '<div style="text-align:right;">Paid</div>'
          + '<div style="text-align:right;">Commission</div>'
          + '</div>';
        for (var j = 0; j < r.customers.length; j++) {
          var c = r.customers[j];
          var cName = c.name || c.email || '\u2014';
          html += '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr 1fr;gap:10px;padding:6px 0;border-bottom:1px solid rgba(255,255,255,0.04);font-size:11.5px;color:rgba(255,255,255,0.85);">'
            + '<div>' + escapeHtml(cName) + (c.starter_used && c.month_index === 0 ? ' <span style="font-size:9px;color:#FFCC4D;">STARTER</span>' : '') + '</div>'
            + '<div>' + escapeHtml(c.tier || '\u2014') + '</div>'
            + '<div style="text-align:right;">Month ' + c.month_index + '</div>'
            + '<div style="text-align:right;">' + fmt(c.month_cents) + '</div>'
            + '<div style="text-align:right;color:#5DECA8;">' + fmt(c.commission_cents) + '</div>'
            + '</div>';
        }
        html += '</div>';
      }
      html += '</div>';
    }

    content.innerHTML = html;

    // Wire expand buttons
    var expandBtns = content.querySelectorAll('[data-comm-expand]');
    for (var x = 0; x < expandBtns.length; x++) {
      expandBtns[x].addEventListener('click', function(ev) {
        var slug = ev.currentTarget.getAttribute('data-comm-expand');
        var detail = document.getElementById('comm-detail-' + slug);
        if (detail) {
          detail.hidden = !detail.hidden;
          ev.currentTarget.textContent = detail.hidden ? 'Details' : 'Hide';
        }
      });
    }
  }


  // ── Add Sales Rep modal ──────────────────────────────────────────────
  function openAdminAddRepModal() {
    var modal = $('admin-add-rep-modal');
    if (!modal) return;
    $('admin-rep-slug').value = '';
    $('admin-rep-name').value = '';
    $('admin-rep-email').value = '';
    $('admin-rep-phone').value = '';
    var errEl = $('admin-rep-error');
    if (errEl) errEl.hidden = true;
    modal.hidden = false;
    setTimeout(function() { var s = $('admin-rep-slug'); if (s) s.focus(); }, 50);
  }

  function closeAdminAddRepModal() {
    var modal = $('admin-add-rep-modal');
    if (modal) modal.hidden = true;
  }

  function saveAdminNewRep() {
    var slug = ($('admin-rep-slug').value || '').toLowerCase().trim();
    slug = slug.split('').filter(function(c) { return /[a-z0-9_]/.test(c); }).join('').slice(0, 40);
    var name = ($('admin-rep-name').value || '').trim();
    var email = ($('admin-rep-email').value || '').trim();
    var phone = ($('admin-rep-phone').value || '').trim();
    var errEl = $('admin-rep-error');

    if (!slug) {
      if (errEl) { errEl.textContent = 'Slug is required.'; errEl.hidden = false; }
      return;
    }
    if (!name) {
      if (errEl) { errEl.textContent = 'Name is required.'; errEl.hidden = false; }
      return;
    }

    var saveBtn = $('admin-rep-save');
    if (saveBtn) { saveBtn.disabled = true; saveBtn.textContent = 'Saving…'; }

    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/admin/sales-reps', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ slug: slug, name: name, email: email || null, phone: phone || null })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.textContent = 'Add rep'; }
        if (!res || !res.ok) {
          if (errEl) {
            errEl.textContent = (res && (res.message || res.error)) || 'Could not save.';
            errEl.hidden = false;
          }
          return;
        }
        closeAdminAddRepModal();
        loadAdminCommissions();
      })
      .catch(function() {
        if (saveBtn) { saveBtn.disabled = false; saveBtn.textContent = 'Add rep'; }
        if (errEl) { errEl.textContent = 'Network error.'; errEl.hidden = false; }
      });
  }


  function _adminFmtTs(ms) {
    // Accepts either ms or seconds; admin timestamps are ms (we use
    // int(time.time() * 1000) consistently for Phase 10 tables).
    if (!ms) return '\u2014';
    if (ms < 1e12) ms = ms * 1000;  // seconds → ms
    var d = new Date(ms);
    if (isNaN(d.getTime())) return '\u2014';
    var now = new Date();
    var sameDay = d.getFullYear() === now.getFullYear()
               && d.getMonth() === now.getMonth()
               && d.getDate() === now.getDate();
    var h = d.getHours();
    var m = d.getMinutes();
    var ampm = h >= 12 ? 'PM' : 'AM';
    h = h % 12; if (h === 0) h = 12;
    var t = h + ':' + (m < 10 ? '0' : '') + m + ' ' + ampm;
    if (sameDay) return t + ' today';
    var diffDays = Math.floor((now - d) / (1000 * 60 * 60 * 24));
    if (diffDays === 1) return 'Yesterday ' + t;
    if (diffDays < 7) return ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'][d.getDay()] + ' ' + t;
    return (d.getMonth() + 1) + '/' + d.getDate() + '/' + (d.getFullYear() % 100) + ' ' + t;
  }

  function _adminEmpty(message) {
    return '<div style="padding:20px 0;text-align:center;color:rgba(255,255,255,0.45);font-size:13px;">'
         + escapeHtml(message) + '</div>';
  }

  // ─── Crew applications ─────────────────────────────────────────────
  var _adminCrewApps = [];

  function loadAdminCrewApplications() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var list = $('admin-crew-list');
    if (!list) return;
    fetch(BACKEND + '/api/v1/admin/crew/applications?status=pending', {
      credentials: 'include'
    })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          var msg = res.status === 403 ? 'You must be an admin to view this.' : 'Could not load.';
          list.innerHTML = _adminEmpty(msg);
          return;
        }
        _adminCrewApps = res.body.applications || [];
        var countEl = $('admin-crew-count');
        if (countEl) countEl.textContent = _adminCrewApps.length + ' pending';
        renderAdminCrewApplications();
      })
      .catch(function() { list.innerHTML = _adminEmpty('Network error.'); });
  }

  function renderAdminCrewApplications() {
    var list = $('admin-crew-list');
    if (!list) return;
    if (_adminCrewApps.length === 0) {
      list.innerHTML = _adminEmpty('No pending applications. Crew sign-ups will appear here.');
      return;
    }
    var html = '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr auto;gap:14px;padding:8px 0;border-bottom:1px solid rgba(255,255,255,0.08);font-size:10px;color:rgba(255,255,255,0.45);text-transform:uppercase;letter-spacing:0.08em;font-weight:600;">'
      +   '<div>Name &middot; email</div><div>County</div><div>Interests</div><div>Submitted</div><div></div>'
      + '</div>';
    for (var i = 0; i < _adminCrewApps.length; i++) {
      var a = _adminCrewApps[i];
      var interests = a.mission_interests ? a.mission_interests.replace(/,/g, ', ') : '\u2014';
      html += '<div style="display:grid;grid-template-columns:1.5fr 1fr 1fr 1fr auto;gap:14px;padding:12px 0;border-bottom:1px solid rgba(255,255,255,0.05);font-size:12.5px;color:rgba(255,255,255,0.85);align-items:center;">'
        +   '<div><div style="font-weight:500;color:#fff;">' + escapeHtml(a.name) + '</div>'
        +     '<div style="font-size:11px;color:rgba(255,255,255,0.50);margin-top:2px;">' + escapeHtml(a.email) + (a.phone ? ' &middot; ' + escapeHtml(a.phone) : '') + '</div></div>'
        +   '<div>' + escapeHtml(a.county || '\u2014') + '</div>'
        +   '<div style="font-size:11.5px;">' + escapeHtml(interests) + '</div>'
        +   '<div style="color:rgba(255,255,255,0.55);font-size:11.5px;">' + escapeHtml(_adminFmtTs(a.created_at)) + '</div>'
        +   '<div style="display:flex;gap:6px;">'
        +     '<button type="button" data-crew-approve="' + a.id + '"'
        +       ' style="background:rgba(65,200,130,0.18);color:#5DECA8;border:1px solid rgba(65,200,130,0.35);padding:5px 12px;border-radius:5px;font-size:11.5px;cursor:pointer;">Approve</button>'
        +     '<button type="button" data-crew-reject="' + a.id + '"'
        +       ' style="background:rgba(255,255,255,0.04);color:rgba(255,255,255,0.65);border:1px solid rgba(255,255,255,0.12);padding:5px 12px;border-radius:5px;font-size:11.5px;cursor:pointer;">Reject</button>'
        +   '</div>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  function _adminCrewApprove(appId) {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    if (!window.confirm('Approve this Crew application? They\u2019ll get a welcome email to set their password.')) return;
    fetch(BACKEND + '/api/v1/admin/crew/applications/' + appId + '/approve', {
      method: 'POST',
      credentials: 'include'
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (res && res.ok) {
          showTipToast('Approved \u2014 welcome email sent.');
          loadAdminCrewApplications();
          loadAdminAuditLog();
        } else {
          window.alert('Couldn\u2019t approve. ' + ((res && res.error) || ''));
        }
      })
      .catch(function() { window.alert('Network error.'); });
  }

  function _adminCrewOpenReject(appId) {
    var app = null;
    for (var i = 0; i < _adminCrewApps.length; i++) {
      if (_adminCrewApps[i].id === appId) { app = _adminCrewApps[i]; break; }
    }
    if (!app) return;
    var scrim = $('admin-reject-scrim');
    var applicantInput = $('admin-reject-applicant');
    var reasonInput = $('admin-reject-reason');
    var submitBtn = $('admin-reject-submit');
    var errEl = $('admin-reject-error');
    if (applicantInput) applicantInput.value = app.name + ' (' + app.email + ')';
    if (reasonInput) reasonInput.value = '';
    if (errEl) errEl.hidden = true;
    if (submitBtn) submitBtn.dataset.appId = String(appId);
    if (scrim) scrim.hidden = false;
  }

  function _adminCrewSubmitReject() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var submitBtn = $('admin-reject-submit');
    var reasonInput = $('admin-reject-reason');
    var scrim = $('admin-reject-scrim');
    var errEl = $('admin-reject-error');
    var appId = submitBtn ? submitBtn.dataset.appId : null;
    if (!appId) return;
    var reason = reasonInput ? reasonInput.value.trim() : '';

    submitBtn.disabled = true;
    submitBtn.textContent = 'Rejecting\u2026';

    fetch(BACKEND + '/api/v1/admin/crew/applications/' + appId + '/reject', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ reason: reason })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Reject application';
        if (res && res.ok) {
          if (scrim) scrim.hidden = true;
          showTipToast('Application rejected.');
          loadAdminCrewApplications();
          loadAdminAuditLog();
        } else if (errEl) {
          errEl.textContent = 'Could not reject. ' + ((res && res.error) || '');
          errEl.hidden = false;
        }
      })
      .catch(function() {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Reject application';
        if (errEl) { errEl.textContent = 'Network error.'; errEl.hidden = false; }
      });
  }

  // ─── Users ─────────────────────────────────────────────────────────
  var _adminUsers = [];

  function loadAdminUsers() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var list = $('admin-users-list');
    if (!list) return;
    fetch(BACKEND + '/api/v1/admin/users', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          var msg = res.status === 403 ? 'You must be an admin to view this.' : 'Could not load.';
          list.innerHTML = _adminEmpty(msg);
          return;
        }
        // The existing endpoint returns "members" not "users"
        _adminUsers = res.body.members || res.body.users || [];
        renderAdminUsers();
      })
      .catch(function() { list.innerHTML = _adminEmpty('Network error.'); });
  }

  function renderAdminUsers() {
    var list = $('admin-users-list');
    if (!list) return;

    // Client-side filter from search + role dropdown
    var qEl = $('admin-users-search');
    var roleEl = $('admin-users-role-filter');
    var q = qEl ? qEl.value.trim().toLowerCase() : '';
    var roleFilter = roleEl ? roleEl.value : '';

    var filtered = _adminUsers.filter(function(u) {
      if (roleFilter && !(u.roles || []).includes(roleFilter)) return false;
      if (q) {
        var hay = ((u.email || '') + ' ' + (u.name || '')).toLowerCase();
        if (hay.indexOf(q) === -1) return false;
      }
      return true;
    });

    if (filtered.length === 0) {
      list.innerHTML = _adminEmpty('No users match.');
      return;
    }

    var html = '<div style="display:grid;grid-template-columns:2fr 1fr 1fr 1fr auto;gap:14px;padding:8px 0;border-bottom:1px solid rgba(255,255,255,0.08);font-size:10px;color:rgba(255,255,255,0.45);text-transform:uppercase;letter-spacing:0.08em;font-weight:600;">'
      +   '<div>User</div><div>Roles</div><div>Joined</div><div>Last login</div><div></div>'
      + '</div>';
    for (var i = 0; i < filtered.length; i++) {
      var u = filtered[i];
      var roleBadges = (u.roles || []).map(function(r) {
        return '<span style="background:rgba(65,105,225,0.15);color:#9BB5F0;padding:1px 7px;border-radius:3px;font-size:10px;font-weight:600;margin-right:3px;">' + escapeHtml(r) + '</span>';
      }).join('');
      var inactive = u.is_active === false;
      var rowStyle = inactive
        ? 'opacity:0.45;display:grid;grid-template-columns:2fr 1fr 1fr 1fr auto;gap:14px;padding:12px 0;border-bottom:1px solid rgba(255,255,255,0.05);font-size:12.5px;color:rgba(255,255,255,0.75);align-items:center;'
        : 'display:grid;grid-template-columns:2fr 1fr 1fr 1fr auto;gap:14px;padding:12px 0;border-bottom:1px solid rgba(255,255,255,0.05);font-size:12.5px;color:rgba(255,255,255,0.85);align-items:center;';
      var statusBtn = inactive
        ? '<button type="button" data-user-reactivate="' + u.id + '" style="background:rgba(65,200,130,0.18);color:#5DECA8;border:1px solid rgba(65,200,130,0.35);padding:5px 12px;border-radius:5px;font-size:11.5px;cursor:pointer;">Reactivate</button>'
        : '<button type="button" data-user-deactivate="' + u.id + '" style="background:rgba(194,52,43,0.12);color:#E89890;border:1px solid rgba(194,52,43,0.30);padding:5px 12px;border-radius:5px;font-size:11.5px;cursor:pointer;">Deactivate</button>';
      html += '<div style="' + rowStyle + '">'
        +   '<div><div style="font-weight:500;color:#fff;">' + escapeHtml(u.name || '(no name)') + (inactive ? ' <span style="color:rgba(194,52,43,0.85);font-size:10px;">DEACTIVATED</span>' : '') + '</div>'
        +     '<div style="font-size:11px;color:rgba(255,255,255,0.50);margin-top:2px;">' + escapeHtml(u.email) + '</div></div>'
        +   '<div>' + (roleBadges || '<span style="color:rgba(255,255,255,0.35);">no role</span>') + '</div>'
        +   '<div style="color:rgba(255,255,255,0.60);font-size:11.5px;">' + escapeHtml(_adminFmtTs(u.created_at)) + '</div>'
        +   '<div style="color:rgba(255,255,255,0.60);font-size:11.5px;">' + escapeHtml(u.last_login_at ? _adminFmtTs(u.last_login_at) : 'never') + '</div>'
        +   '<div>' + statusBtn + '</div>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  function _adminUserDeactivate(userId) {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    if (!window.confirm('Deactivate this user? They will be unable to sign in until reactivated.')) return;
    fetch(BACKEND + '/api/v1/admin/users/' + userId, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ is_active: false })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (res && res.ok) {
          showTipToast('User deactivated.');
          loadAdminUsers();
          loadAdminAuditLog();
        } else {
          window.alert('Could not deactivate. ' + ((res && res.error) || ''));
        }
      })
      .catch(function() { window.alert('Network error.'); });
  }

  function _adminUserReactivate(userId) {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    fetch(BACKEND + '/api/v1/admin/users/' + userId, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ is_active: true })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        if (res && res.ok) {
          showTipToast('User reactivated.');
          loadAdminUsers();
          loadAdminAuditLog();
        } else {
          window.alert('Could not reactivate. ' + ((res && res.error) || ''));
        }
      })
      .catch(function() { window.alert('Network error.'); });
  }

  // ─── Payments ──────────────────────────────────────────────────────
  var _adminPayments = [];

  function loadAdminPayments() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var list = $('admin-payments-list');
    if (!list) return;
    fetch(BACKEND + '/api/v1/admin/payments', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          var msg = res.status === 403 ? 'You must be an admin to view this.' : 'Could not load.';
          list.innerHTML = _adminEmpty(msg);
          return;
        }
        _adminPayments = res.body.payments || [];
        renderAdminPayments();
      })
      .catch(function() { list.innerHTML = _adminEmpty('Network error.'); });
  }

  function renderAdminPayments() {
    var list = $('admin-payments-list');
    if (!list) return;
    if (_adminPayments.length === 0) {
      list.innerHTML = _adminEmpty('No $19 reviews yet.');
      return;
    }
    var html = '<div style="display:grid;grid-template-columns:1.6fr 1fr 0.6fr 1fr 1fr auto;gap:14px;padding:8px 0;border-bottom:1px solid rgba(255,255,255,0.08);font-size:10px;color:rgba(255,255,255,0.45);text-transform:uppercase;letter-spacing:0.08em;font-weight:600;">'
      +   '<div>Plan</div><div>Customer</div><div>Amount</div><div>Status</div><div>When</div><div></div>'
      + '</div>';
    for (var i = 0; i < _adminPayments.length; i++) {
      var p = _adminPayments[i];
      var amount = '$' + ((p.price_cents || 1900) / 100).toFixed(2);
      var statusColor = p.status === 'completed' ? '#5DECA8'
                       : p.status === 'refunded' ? '#E89890'
                       : p.status === 'claimed' ? '#FFCC4D'
                       : '#9BB5F0';
      var refundBtn = (p.status === 'refunded' || !p.stripe_payment_id)
        ? '<span style="color:rgba(255,255,255,0.30);font-size:11px;">' + (p.status === 'refunded' ? 'refunded' : '\u2014') + '</span>'
        : '<button type="button" data-payment-refund="' + p.request_id + '"'
          + ' style="background:rgba(194,52,43,0.12);color:#E89890;border:1px solid rgba(194,52,43,0.30);padding:5px 12px;border-radius:5px;font-size:11.5px;cursor:pointer;">Refund</button>';
      html += '<div style="display:grid;grid-template-columns:1.6fr 1fr 0.6fr 1fr 1fr auto;gap:14px;padding:12px 0;border-bottom:1px solid rgba(255,255,255,0.05);font-size:12.5px;color:rgba(255,255,255,0.85);align-items:center;">'
        +   '<div><div style="font-weight:500;color:#fff;">' + escapeHtml(p.plan_text || '(no plan)') + '</div>'
        +     '<div style="font-size:11px;color:rgba(255,255,255,0.50);margin-top:2px;">' + escapeHtml(p.plan_location || '\u2014') + '</div></div>'
        +   '<div style="font-size:11.5px;">' + escapeHtml(p.customer_email || p.customer_phone || '\u2014') + '</div>'
        +   '<div>' + amount + '</div>'
        +   '<div><span style="color:' + statusColor + ';font-weight:600;font-size:11.5px;">' + escapeHtml(p.status) + '</span></div>'
        +   '<div style="color:rgba(255,255,255,0.55);font-size:11.5px;">' + escapeHtml(_adminFmtTs(p.created_at)) + '</div>'
        +   '<div>' + refundBtn + '</div>'
        + '</div>';
    }
    list.innerHTML = html;
  }

  function _adminPaymentOpenRefund(requestId) {
    var p = null;
    for (var i = 0; i < _adminPayments.length; i++) {
      if (_adminPayments[i].request_id === requestId) { p = _adminPayments[i]; break; }
    }
    if (!p) return;
    var scrim = $('admin-refund-scrim');
    var custInput = $('admin-refund-customer');
    var amtInput = $('admin-refund-amount');
    var reasonInput = $('admin-refund-reason');
    var submitBtn = $('admin-refund-submit');
    var errEl = $('admin-refund-error');
    if (custInput) custInput.value = p.customer_email || p.customer_phone || '(no contact)';
    if (amtInput) amtInput.value = '$' + ((p.price_cents || 1900) / 100).toFixed(2) + ' (full refund)';
    if (reasonInput) reasonInput.value = '';
    if (errEl) errEl.hidden = true;
    if (submitBtn) submitBtn.dataset.requestId = String(requestId);
    if (scrim) scrim.hidden = false;
  }

  function _adminPaymentSubmitRefund() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var submitBtn = $('admin-refund-submit');
    var reasonInput = $('admin-refund-reason');
    var scrim = $('admin-refund-scrim');
    var errEl = $('admin-refund-error');
    var requestId = submitBtn ? parseInt(submitBtn.dataset.requestId, 10) : null;
    if (!requestId) return;
    var reason = reasonInput ? reasonInput.value.trim() : '';

    submitBtn.disabled = true;
    submitBtn.textContent = 'Refunding\u2026';

    fetch(BACKEND + '/api/v1/admin/payments/refund', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ request_id: requestId, reason: reason })
    })
      .then(function(r) { return r.json().then(function(j) { return j; }); })
      .then(function(res) {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Refund';
        if (res && res.ok) {
          if (scrim) scrim.hidden = true;
          showTipToast('Refunded \u2014 customer notified by SMS.');
          loadAdminPayments();
          loadAdminAuditLog();
        } else if (errEl) {
          errEl.textContent = 'Refund failed. ' + ((res && (res.message || res.error)) || '');
          errEl.hidden = false;
        }
      })
      .catch(function() {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Refund';
        if (errEl) { errEl.textContent = 'Network error.'; errEl.hidden = false; }
      });
  }

  // ─── Audit log ─────────────────────────────────────────────────────
  function loadAdminAuditLog() {
    var BACKEND = 'https://wv-valet-backend.onrender.com';
    var list = $('admin-audit-list');
    if (!list) return;
    fetch(BACKEND + '/api/v1/admin/audit-log?limit=30', { credentials: 'include' })
      .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
      .then(function(res) {
        if (!res.body || !res.body.ok) {
          var msg = res.status === 403 ? 'You must be an admin to view this.' : 'Could not load.';
          list.innerHTML = _adminEmpty(msg);
          return;
        }
        var entries = res.body.entries || [];
        if (entries.length === 0) {
          list.innerHTML = _adminEmpty('No admin actions logged yet.');
          return;
        }
        var html = '';
        for (var i = 0; i < entries.length; i++) {
          var e = entries[i];
          var detailsBits = [];
          if (e.details) {
            for (var k in e.details) {
              if (e.details.hasOwnProperty(k) && e.details[k] != null && k !== '_unparsed') {
                detailsBits.push(escapeHtml(k) + '=' + escapeHtml(String(e.details[k])));
              }
            }
          }
          var detailsStr = detailsBits.length ? detailsBits.join(' &middot; ') : '';
          html += '<div style="display:grid;grid-template-columns:1fr 1.5fr 2.5fr 1fr;gap:14px;padding:10px 0;border-bottom:1px solid rgba(255,255,255,0.05);font-size:12px;color:rgba(255,255,255,0.80);align-items:start;">'
            +   '<div style="font-family:JetBrains Mono,monospace;color:#9BB5F0;font-size:11px;">' + escapeHtml(e.action) + '</div>'
            +   '<div>' + escapeHtml(e.actor_name || ('user #' + e.actor_user_id)) + '</div>'
            +   '<div style="color:rgba(255,255,255,0.60);font-size:11.5px;">' + detailsStr + '</div>'
            +   '<div style="color:rgba(255,255,255,0.50);font-size:11.5px;">' + escapeHtml(_adminFmtTs(e.created_at)) + '</div>'
            + '</div>';
        }
        list.innerHTML = html;
      })
      .catch(function() { list.innerHTML = _adminEmpty('Network error.'); });
  }

  // ─── Wire up event delegation + refresh buttons ─────────────────────
  function setupAdminOperations() {
    if (window._adminOperationsWired) return;
    window._adminOperationsWired = true;

    // Refresh buttons
    var crewRefresh = $('admin-crew-refresh');
    if (crewRefresh) crewRefresh.addEventListener('click', loadAdminCrewApplications);
    var usersRefresh = $('admin-users-refresh');
    if (usersRefresh) usersRefresh.addEventListener('click', loadAdminUsers);
    var paymentsRefresh = $('admin-payments-refresh');
    if (paymentsRefresh) paymentsRefresh.addEventListener('click', loadAdminPayments);
    var auditRefresh = $('admin-audit-refresh');
    if (auditRefresh) auditRefresh.addEventListener('click', loadAdminAuditLog);

    // Payroll
    var payrollRefresh = $('admin-payroll-refresh');
    if (payrollRefresh) payrollRefresh.addEventListener('click', loadAdminPayroll);
    var payrollMonth = $('admin-payroll-month');
    if (payrollMonth) payrollMonth.addEventListener('change', loadAdminPayroll);

    // Sales Commissions
    var commRefresh = $('admin-comm-refresh');
    if (commRefresh) commRefresh.addEventListener('click', loadAdminCommissions);
    var commMonth = $('admin-comm-month');
    if (commMonth) commMonth.addEventListener('change', loadAdminCommissions);
    var commAddRep = $('admin-comm-add-rep');
    if (commAddRep) commAddRep.addEventListener('click', openAdminAddRepModal);

    // Phase 11 — Coverage dashboard wiring
    var coverageRefresh = $('admin-coverage-refresh');
    if (coverageRefresh) coverageRefresh.addEventListener('click', loadAdminCoverage);
    // Auto-load on first setup
    try { loadAdminCoverage(); } catch (e) { console.warn('coverage load failed', e); }

    // Add Rep modal wiring
    var repClose = $('admin-add-rep-close');
    var repScrim = $('admin-add-rep-scrim');
    var repCancel = $('admin-rep-cancel');
    var repSave = $('admin-rep-save');
    if (repClose) repClose.addEventListener('click', closeAdminAddRepModal);
    if (repScrim) repScrim.addEventListener('click', closeAdminAddRepModal);
    if (repCancel) repCancel.addEventListener('click', closeAdminAddRepModal);
    if (repSave) repSave.addEventListener('click', saveAdminNewRep);

    // Users filter — re-render client-side on change
    var qInput = $('admin-users-search');
    if (qInput) qInput.addEventListener('input', renderAdminUsers);
    var roleSel = $('admin-users-role-filter');
    if (roleSel) roleSel.addEventListener('change', renderAdminUsers);

    // Event delegation for action buttons + modal interactions
    document.addEventListener('click', function(ev) {
      var t = ev.target;
      var btn;

      btn = t.closest('[data-crew-approve]');
      if (btn) { _adminCrewApprove(parseInt(btn.getAttribute('data-crew-approve'), 10)); return; }

      btn = t.closest('[data-crew-reject]');
      if (btn) { _adminCrewOpenReject(parseInt(btn.getAttribute('data-crew-reject'), 10)); return; }

      btn = t.closest('[data-user-deactivate]');
      if (btn) { _adminUserDeactivate(parseInt(btn.getAttribute('data-user-deactivate'), 10)); return; }

      btn = t.closest('[data-user-reactivate]');
      if (btn) { _adminUserReactivate(parseInt(btn.getAttribute('data-user-reactivate'), 10)); return; }

      btn = t.closest('[data-payment-refund]');
      if (btn) { _adminPaymentOpenRefund(parseInt(btn.getAttribute('data-payment-refund'), 10)); return; }
    });

    // Refund modal wiring
    var refundCancel = $('admin-refund-cancel');
    if (refundCancel) refundCancel.addEventListener('click', function() {
      var scrim = $('admin-refund-scrim');
      if (scrim) scrim.hidden = true;
    });
    var refundSubmit = $('admin-refund-submit');
    if (refundSubmit) refundSubmit.addEventListener('click', _adminPaymentSubmitRefund);

    // Reject modal wiring
    var rejectCancel = $('admin-reject-cancel');
    if (rejectCancel) rejectCancel.addEventListener('click', function() {
      var scrim = $('admin-reject-scrim');
      if (scrim) scrim.hidden = true;
    });
    var rejectSubmit = $('admin-reject-submit');
    if (rejectSubmit) rejectSubmit.addEventListener('click', _adminCrewSubmitReject);
  }


  // ── Dev menu setup — gated on ?dev=1 in URL ──────────────────────────
  function setupDevMenu() {
    var params = new URLSearchParams(window.location.search);
    var devMode = params.get('dev') === '1';

    // Dev-menu UI bits — only shown to testers using ?dev=1. Real
    // authed users don't see the dev chip, but they still need the
    // workspace setup below (tab handlers, exit buttons, renders).
    var menu = $('dev-menu');
    if (devMode && menu) {
      menu.style.display = 'block';

      // Auto-open the menu on initial load so testers immediately see the
      // three view options.
      setTimeout(function() {
        menu.classList.add('is-open');
        var trig = $('dev-menu-trigger');
        if (trig) trig.setAttribute('aria-expanded', 'true');
        setTimeout(function() {
          menu.classList.remove('is-open');
          if (trig) trig.setAttribute('aria-expanded', 'false');
        }, 4000);
      }, 600);

      var trigger = $('dev-menu-trigger');
      if (trigger) {
        trigger.addEventListener('click', function(ev) {
          ev.stopPropagation();
          menu.classList.toggle('is-open');
          menu.classList.add('has-been-opened');
          trigger.setAttribute('aria-expanded',
            menu.classList.contains('is-open') ? 'true' : 'false');
        });
      }

      // Click outside the menu closes it
      document.addEventListener('click', function(ev) {
        if (!menu.contains(ev.target)) {
          menu.classList.remove('is-open');
          if (trigger) trigger.setAttribute('aria-expanded', 'false');
        }
      });
    }

    // View switcher
    var options = document.querySelectorAll('[data-dev-view]');
    for (var i = 0; i < options.length; i++) {
      options[i].addEventListener('click', function(ev) {
        var view = ev.currentTarget.getAttribute('data-dev-view');
        switchDevView(view);
      });
    }

    // Exit-to-search button — present in the Met and Crew workspaces.
    // For real users we route to the homepage; for dev-menu testers we
    // call switchDevView so the dev menu's pretend-session state stays
    // consistent. (Note: showScreen is in the same IIFE scope, no
    // window.* needed.)
    document.addEventListener('click', function(ev) {
      var exitBtn = ev.target.closest('[data-ws-exit]');
      if (!exitBtn) return;
      ev.preventDefault();
      var params = new URLSearchParams(window.location.search);
      if (params.get('dev') === '1') {
        switchDevView('customer');
      } else {
        showScreen('screen-welcome');
      }
    });

    // Storm Shelter test buttons in the dev menu — one per tier. Routes
    // to the customer view first so the shelter is visible (it sits at
    // the top of the welcome screen), then activates the chosen tier.
    // The "Dismiss" button removes the shelter entirely.
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-shelter-tier]');
      if (!btn) return;
      ev.preventDefault();
      var tier = btn.getAttribute('data-shelter-tier');
      switchDevView('customer');
      setTimeout(function() {
        if (typeof window._stormShelterToggle !== 'function') return;
        if (tier === 'dismiss') {
          window._stormShelterToggle(false);
        } else {
          window._stormShelterToggle(tier);
        }
        // Update active-state on the dev menu buttons so the user can
        // see which tier is currently active.
        var allTierBtns = document.querySelectorAll('[data-shelter-tier]');
        for (var i = 0; i < allTierBtns.length; i++) {
          allTierBtns[i].classList.toggle('is-active',
            allTierBtns[i].getAttribute('data-shelter-tier') === tier && tier !== 'dismiss');
        }
      }, 80);
    });

    // Initial render of mock data so the views are populated as soon as
    // a tester switches to them.
    renderMetQueue();
    renderMetHistory();
    renderAdminQueue();
    renderAdminActivity();
    renderCmdCoverage();
    renderCmdHeatmap();
    setupCmdForceAlert();
    setAdminWelcome();
    setMetWelcome();
    setupMetShiftDetection();
    setCrewLiveStrip();
    loadCrewConfessionals();
    setupCrewConfessional();
    renderCrewNotebook();
    renderCrewMissionList();
    setupBriefCountySelector();
    setupBriefModeSwitcher();
    setupMetMessageAgain();
    renderMissionTemplates();
    setupMissionDeploy();
    setCrewWorkspaceWelcome();
    setupPortalEditing();
    if (typeof setupMetScheduledMessages === 'function') {
      try { setupMetScheduledMessages(); } catch (e) {}
    }
    if (typeof setupMySubscribers === 'function') {
      try { setupMySubscribers(); } catch (e) {}
    }
    if (typeof setupCreateTemplateModal === 'function') {
      try { setupCreateTemplateModal(); } catch (e) {}
    }

    // ── Phase 10 Met scheduling: schedule popup + Daily Brief button ──
    setupSchedulePopup();
    var briefScheduleBtn = $('brief-schedule-btn');
    if (briefScheduleBtn && !briefScheduleBtn._wvWired) {
      briefScheduleBtn._wvWired = true;
      briefScheduleBtn.addEventListener('click', function() {
        openSchedulePopup({
          type: 'daily_brief',
          getContent: function() {
            // Collect the visible composer fields into a payload
            var bodyEl = $('brief-summary');
            var timeEl = $('brief-time-windows');
            var headlineEl = $('brief-mode-headline');
            var verdictBtn = document.querySelector('#brief-verdict-grid .is-selected');
            return {
              body: bodyEl ? bodyEl.value : '',
              time_windows: timeEl ? timeEl.value : '',
              headline: headlineEl ? headlineEl.textContent : 'Daily Brief',
              verdict: verdictBtn ? verdictBtn.getAttribute('data-verdict') : null,
              mode: document.querySelector('.ws-brief-mode-btn.is-active')
                    ? document.querySelector('.ws-brief-mode-btn.is-active').getAttribute('data-brief-mode')
                    : 'routine'
            };
          },
          getAudience: function() {
            // Pull selected counties from the multi-select.
            var counties = [];
            var checked = document.querySelectorAll('#met-county-list input[type="checkbox"]:checked');
            for (var i = 0; i < checked.length; i++) {
              counties.push(checked[i].value);
            }
            var stateEl = $('met-county-state');
            return {
              state: stateEl ? stateEl.value : null,
              counties: counties
            };
          },
          onScheduled: function(info) {
            window.alert('Scheduled for ' + (info.label || 'a future time') + '.\n\nYou can view and edit it in the History tab under "Scheduled."');
            if (typeof loadMetScheduledMessages === 'function') {
              try { loadMetScheduledMessages(); } catch (e) {}
            }
          }
        });
      });
    }

    // Met view tab switcher.
    // The new workspace layout uses CSS grid for the queue panel (so its
    // children can span columns). When a tab activates, we set its panel
    // to the right display value: `contents` for the queue (lets grid
    // children participate in the parent grid) and `block` for the
    // others. Inactive panels get `display: none`.
    var metTabs = document.querySelectorAll('[data-met-tab]');
    for (var t = 0; t < metTabs.length; t++) {
      metTabs[t].addEventListener('click', function(ev) {
        var tabName = ev.currentTarget.getAttribute('data-met-tab');
        var allTabs = document.querySelectorAll('[data-met-tab]');
        var allPanels = document.querySelectorAll('[data-met-panel]');
        for (var x = 0; x < allTabs.length; x++) {
          var match = allTabs[x].getAttribute('data-met-tab') === tabName;
          allTabs[x].classList.toggle('is-active', match);
          allTabs[x].setAttribute('aria-selected', match ? 'true' : 'false');
        }
        for (var p = 0; p < allPanels.length; p++) {
          var pName = allPanels[p].getAttribute('data-met-panel');
          var isActive = (pName === tabName);
          allPanels[p].classList.toggle('is-active', isActive);
          // All panels use display: contents when active so their tile
          // children participate directly in the parent's 12-column grid.
          // Without this, panels would block-layout the tiles into a
          // narrow column on the left edge of the grid.
          allPanels[p].style.display = isActive ? 'contents' : 'none';
        }

        // Lazy-load the map when its tab is first clicked. The map is
        // heavy (Leaflet library + county boundary data), so we don't
        // pay that cost for users who never open it.
        if (tabName === 'map') loadMetMap();
        // Item #4 — refresh mission deployments when Missions tab opens
        // so the user sees fresh data without needing to leave/re-enter
        // the whole workspace.
        if (tabName === 'missions' && typeof renderMetDeployments === 'function') {
          try { renderMetDeployments(); } catch (e) {}
        }
        // Item #3 Chunk 3B-2 — refresh Pro briefs when that tab opens
        if (tabName === 'pro-briefs' && typeof loadMetProBriefs === 'function') {
          try { loadMetProBriefs(); } catch (e) {}
        }
        // Pro Threads PT-C — load Messages list when that tab opens
        if (tabName === 'messages' && typeof loadMetThreadsList === 'function') {
          try { loadMetThreadsList(); } catch (e) {}
        }
        // Met tips (TIP-B) — refresh earnings when History tab opens
        if (tabName === 'history' && typeof loadMetTipEarnings === 'function') {
          try { loadMetTipEarnings(); } catch (e) {}
        }
        // Phase 10 scheduling — load scheduled-messages list on History
        if (tabName === 'history' && typeof loadMetScheduledMessages === 'function') {
          try { loadMetScheduledMessages(); } catch (e) {}
        }
        // F-1: My Subscribers — load roster on tab activation
        if (tabName === 'subscribers' && typeof loadMySubscribers === 'function') {
          try { loadMySubscribers(); } catch (e) {}
        }
        // Phase 11 — Schedule tab (May 17): load schedule + set up modal
        if (tabName === 'schedule') {
          try {
            if (typeof setupMetSchedule === 'function') setupMetSchedule();
            if (typeof loadMetSchedule === 'function') loadMetSchedule();
            if (typeof loadMetProfile === 'function') loadMetProfile();
          } catch (e) { console.warn('schedule init failed', e); }
        }
      });
    }

    // ── Met map lazy loader ─────────────────────────────────────────
    // Loads Leaflet from CDN, then our met-map.js, then initializes.
    // Idempotent — safe to call repeatedly (e.g. user switches away
    // from Map tab and back). State machine:
    //   undefined (initial) → 'loading' → 'ready' | 'error'
    function loadMetMap() {
      if (window._metMapState === 'ready' || window._metMapState === 'loading') return;
      window._metMapState = 'loading';

      // Step 1: load Leaflet's CSS (required for the map controls + tile
      // positioning to render correctly).
      if (!document.querySelector('link[data-leaflet]')) {
        var link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
        link.setAttribute('data-leaflet', 'css');
        document.head.appendChild(link);
      }

      // Step 2: load Leaflet's JS library.
      function loadScript(src, onload) {
        var s = document.createElement('script');
        s.src = src;
        s.async = true;
        s.onload = onload;
        s.onerror = function() {
          window._metMapState = 'error';
          var loadingEl = document.getElementById('met-map-loading');
          if (loadingEl) {
            loadingEl.innerHTML = '<span style="color:#C2342B;">Couldn\'t load the map. Check your connection and try again.</span>';
          }
        };
        document.head.appendChild(s);
      }

      loadScript('https://unpkg.com/leaflet@1.9.4/dist/leaflet.js', function() {
        // Step 3: load our map setup script. It expects `L` (Leaflet)
        // to be defined globally already.
        loadScript('met-map.js', function() {
          if (typeof window.initMetMap === 'function') {
            window.initMetMap();
            window._metMapState = 'ready';
            wireAlertsBar();
          }
        });
      });
    }

    // ── Crew map lazy loader (Chunk 6b) ────────────────────────────────
    // Mirrors loadMetMap. Loads Leaflet CSS+JS from CDN (shared with Met
    // map — same <link> works for both), then loads crew-map.js, then
    // calls window.initCrewMap(). Idempotent.
    function loadCrewMap() {
      if (window._crewMapState === 'ready' || window._crewMapState === 'loading') return;
      window._crewMapState = 'loading';

      // Step 1: shared Leaflet CSS link (same one Met map uses)
      if (!document.querySelector('link[data-leaflet]')) {
        var link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
        link.setAttribute('data-leaflet', 'css');
        document.head.appendChild(link);
      }

      function showLoadError(message) {
        window._crewMapState = 'error';
        var loadingEl = document.getElementById('crew-map-loading');
        if (loadingEl) {
          loadingEl.innerHTML = '<span style="color:#C2342B;text-align:center;padding:20px;line-height:1.5;">'
            + (message || 'Couldn\'t load the map. Check your connection and try again.')
            + '</span>';
        }
      }

      function loadScript(src, onload) {
        var s = document.createElement('script');
        s.src = src;
        s.async = true;
        s.onload = onload;
        s.onerror = function() {
          showLoadError('Couldn\'t load <code>' + src + '</code>. Check the file exists at that path.');
        };
        document.head.appendChild(s);
      }

      // Safety timeout — if init doesn't fire ready state within 15s,
      // surface a helpful message instead of silent loading.
      var watchdog = setTimeout(function() {
        if (window._crewMapState !== 'ready') {
          showLoadError('Map didn\'t finish loading. Open browser console for details.');
        }
      }, 15000);

      function loadCrewMapScript() {
        loadScript('crew-map.js', function() {
          if (typeof window.initCrewMap !== 'function') {
            clearTimeout(watchdog);
            showLoadError('crew-map.js loaded but initCrewMap is not defined.');
            return;
          }
          try {
            window.initCrewMap();
            window._crewMapState = 'ready';
            clearTimeout(watchdog);
          } catch (err) {
            clearTimeout(watchdog);
            console.error('initCrewMap threw:', err);
            showLoadError('Map error: ' + (err && err.message ? err.message : err));
          }
        });
      }

      if (window.L) {
        // Leaflet already on the page (Met map loaded it earlier)
        loadCrewMapScript();
      } else {
        loadScript('https://unpkg.com/leaflet@1.9.4/dist/leaflet.js', loadCrewMapScript);
      }
    }

    // ── Wire the NWS alerts bar ─────────────────────────────────────
    // Once the map and met-map.js are loaded, hook up the three toggle
    // checkboxes, the manual refresh button, and the live counts/
    // timestamp updates. We do this once per page-load (idempotent flag).
    function wireAlertsBar() {
      if (window._metAlertsBarWired) return;
      window._metAlertsBarWired = true;

      // Toggle checkboxes — clicking the pill toggles the checkbox and
      // calls into met-map.js to show/hide the corresponding layer.
      var toggles = document.querySelectorAll('.met-map-toggle');
      for (var i = 0; i < toggles.length; i++) {
        toggles[i].addEventListener('change', function(ev) {
          var label = ev.currentTarget;
          var category = label.getAttribute('data-alert-category');
          var checkbox = label.querySelector('input[type="checkbox"]');
          var visible = checkbox.checked;
          label.classList.toggle('is-off', !visible);
          if (typeof window.setMetMapAlertVisibility === 'function') {
            window.setMetMapAlertVisibility(category, visible);
          }
        });
      }

      // Manual refresh button — re-fetches alerts now and resets the
      // 5-minute auto-refresh timer. Spins the icon briefly so the
      // user sees that something happened.
      var refreshBtn = document.getElementById('met-map-refresh-btn');
      if (refreshBtn) {
        refreshBtn.addEventListener('click', function() {
          refreshBtn.classList.remove('is-refreshing');
          // Force a reflow so the animation restarts cleanly on repeat clicks
          void refreshBtn.offsetWidth;
          refreshBtn.classList.add('is-refreshing');
          if (typeof window.refreshMetMapAlerts === 'function') {
            window.refreshMetMapAlerts();
          }
        });
      }

      // Radar toggle — wires the checkbox to met-map.js
      var radarCheckbox = document.getElementById('met-map-radar-checkbox');
      if (radarCheckbox) {
        // Restore saved state to keep UI in sync
        try {
          if (localStorage.getItem('wv_met_map_radar_on') === '1') {
            radarCheckbox.checked = true;
          }
        } catch (e) {}
        radarCheckbox.addEventListener('change', function() {
          var on = radarCheckbox.checked;
          if (typeof window.setMetMapRadar === 'function') {
            window.setMetMapRadar(on);
          }
        });
      }

      // ── Per-event-type filter dropdowns ────────────────────────────
      // Each category button has a caret that opens a dropdown listing
      // every event type currently active in that category. Mets can
      // uncheck event types they don't want to see (e.g. hide Red Flag
      // Warnings while severe weather is the focus).
      var EVENT_FILTER_CATEGORY_LABELS = {
        warning: 'Filter warnings',
        watch: 'Filter watches',
        advisory: 'Filter advisories'
      };

      var filterDropdown = document.getElementById('met-event-filter-dropdown');
      var filterHeader = document.getElementById('met-event-filter-header');
      var filterList = document.getElementById('met-event-filter-list');
      var openFilterCategory = null;

      function _renderEventFilter(category) {
        if (!filterList) return;
        var eventsObj = (window._metEventTypesByCategory && window._metEventTypesByCategory[category]) || {};
        var keys = Object.keys(eventsObj).sort(function(a, b) {
          return eventsObj[b].count - eventsObj[a].count;
        });
        if (filterHeader) {
          filterHeader.textContent = EVENT_FILTER_CATEGORY_LABELS[category] || 'Filter';
        }
        if (keys.length === 0) {
          filterList.innerHTML = '<div style="padding: 14px; font-size: 12.5px; color: rgba(15,17,22,0.55); text-align: center;">No ' + category + 's are active right now.</div>';
          return;
        }
        var html = '';
        for (var i = 0; i < keys.length; i++) {
          var key = keys[i];
          var info = eventsObj[key];
          var isVisible = !info.hidden;
          var displayName = info.event || key;
          html += '<label data-evt-key="' + escapeHtml(key) + '" style="display: flex; align-items: center; gap: 10px; padding: 8px 14px; cursor: pointer; font-size: 13px; color: #0E1116;">'
            + '<input type="checkbox" ' + (isVisible ? 'checked' : '') + ' style="margin: 0;">'
            + '<span style="flex: 1;">' + escapeHtml(displayName) + '</span>'
            + '<span style="font-size: 11px; font-weight: 600; color: rgba(15,17,22,0.50); background: rgba(15,17,22,0.05); padding: 2px 7px; border-radius: 4px;">' + info.count + '</span>'
            + '</label>';
        }
        filterList.innerHTML = html;
        // Hover state
        var labels = filterList.querySelectorAll('label');
        for (var j = 0; j < labels.length; j++) {
          (function(label) {
            label.addEventListener('mouseenter', function() { label.style.background = 'rgba(15,17,22,0.04)'; });
            label.addEventListener('mouseleave', function() { label.style.background = ''; });
            var input = label.querySelector('input');
            if (input) {
              input.addEventListener('change', function() {
                var key = label.getAttribute('data-evt-key');
                if (typeof window.setMetMapEventVisibility === 'function') {
                  window.setMetMapEventVisibility(key, input.checked);
                }
                // Update the local copy so we don't re-render incorrectly
                if (window._metEventTypesByCategory && window._metEventTypesByCategory[category] && window._metEventTypesByCategory[category][key]) {
                  window._metEventTypesByCategory[category][key].hidden = !input.checked;
                }
              });
            }
          })(labels[j]);
        }
      }

      function _positionFilterDropdown(caretBtn) {
        if (!filterDropdown || !caretBtn) return;
        var rect = caretBtn.getBoundingClientRect();
        // Anchor below the caret
        filterDropdown.style.top = (rect.bottom + window.scrollY + 4) + 'px';
        // Align to caret's left, but don't overflow the right edge
        var leftPos = rect.left + window.scrollX;
        var dropdownWidth = 280;  // approx
        var maxLeft = window.innerWidth - dropdownWidth - 12;
        if (leftPos > maxLeft) leftPos = maxLeft;
        if (leftPos < 8) leftPos = 8;
        filterDropdown.style.left = leftPos + 'px';
      }

      function _openFilterDropdown(category, caretBtn) {
        if (!filterDropdown) return;
        openFilterCategory = category;
        _renderEventFilter(category);
        _positionFilterDropdown(caretBtn);
        filterDropdown.hidden = false;
      }

      function _closeFilterDropdown() {
        if (filterDropdown) filterDropdown.hidden = true;
        openFilterCategory = null;
      }

      // Wire each caret button
      var carets = document.querySelectorAll('[data-filter-toggle]');
      for (var k = 0; k < carets.length; k++) {
        (function(caret) {
          caret.addEventListener('click', function(ev) {
            ev.stopPropagation();
            var cat = caret.getAttribute('data-filter-toggle');
            if (openFilterCategory === cat && !filterDropdown.hidden) {
              _closeFilterDropdown();
            } else {
              _openFilterDropdown(cat, caret);
            }
          });
        })(carets[k]);
      }

      // Close dropdown when clicking outside
      document.addEventListener('click', function(ev) {
        if (!filterDropdown || filterDropdown.hidden) return;
        if (filterDropdown.contains(ev.target)) return;
        // If they clicked a caret, the caret handler already toggled
        if (ev.target.closest && ev.target.closest('[data-filter-toggle]')) return;
        _closeFilterDropdown();
      });

      // Listen for the custom event from met-map.js when alerts are
      // (re)fetched. Update the count badges and the "Updated 14:23"
      // timestamp in the corner.
      window.addEventListener('metMapAlertsUpdated', function(ev) {
        var counts = (ev.detail && ev.detail.counts) || {};
        var warnEl = document.getElementById('met-alert-count-warning');
        var watchEl = document.getElementById('met-alert-count-watch');
        var advEl = document.getElementById('met-alert-count-advisory');
        if (warnEl) warnEl.textContent = formatCount(counts.warning);
        if (watchEl) watchEl.textContent = formatCount(counts.watch);
        if (advEl) advEl.textContent = formatCount(counts.advisory);

        var updatedEl = document.getElementById('met-alert-updated');
        if (updatedEl) {
          var t = ev.detail.fetchedAt || new Date();
          var hh = String(t.getHours()).padStart(2, '0');
          var mm = String(t.getMinutes()).padStart(2, '0');
          updatedEl.textContent = 'Updated ' + hh + ':' + mm;
        }

        // Render zone-based alerts list (alerts without polygon geometry)
        var zoneAlerts = (ev.detail && ev.detail.zoneAlerts) || [];
        var detailsEl = document.getElementById('met-zone-alerts-details');
        var countEl = document.getElementById('met-zone-alerts-count');
        var listEl = document.getElementById('met-zone-alerts-list');
        if (detailsEl && listEl && countEl) {
          if (zoneAlerts.length === 0) {
            detailsEl.hidden = true;
            listEl.innerHTML = '';
          } else {
            detailsEl.hidden = false;
            countEl.textContent = zoneAlerts.length;

            // NWS event color palette (same as map polygons)
            var EVENT_COLORS = {
              warning: '#C2342B',
              watch: '#F5A524',
              advisory: '#4169E1'
            };

            var html = '';
            for (var i = 0; i < zoneAlerts.length; i++) {
              var za = zoneAlerts[i];
              var dotColor = EVENT_COLORS[za.category] || '#4169E1';
              var expiresStr = '';
              if (za.expires) {
                try {
                  var exp = new Date(za.expires);
                  expiresStr = exp.toLocaleString('en-US', {
                    weekday: 'short', month: 'short', day: 'numeric',
                    hour: 'numeric', minute: '2-digit'
                  });
                } catch (e) {}
              }
              // Sanitize area (can be very long for county lists)
              var areaShort = (za.area || '').length > 200
                ? (za.area.slice(0, 200) + '…')
                : (za.area || '');

              html += '<div style="display: flex; gap: 10px; padding: 8px 0; border-bottom: 1px solid rgba(15,17,22,0.04); align-items: flex-start;">'
                + '<span style="width: 6px; height: 6px; border-radius: 50%; background: ' + dotColor + '; flex-shrink: 0; margin-top: 6px;"></span>'
                + '<div style="flex: 1; min-width: 0; font-size: 11.5px; line-height: 1.45;">'
                +   '<div style="font-weight: 600; color: #0E1116;">' + escapeHtml(za.event) + '</div>'
                +   (areaShort ? '<div style="color: rgba(15,17,22,0.60); margin-top: 2px;">' + escapeHtml(areaShort) + '</div>' : '')
                +   (expiresStr ? '<div style="color: rgba(15,17,22,0.45); margin-top: 2px; font-size: 10.5px;">expires ' + escapeHtml(expiresStr) + '</div>' : '')
                + '</div>'
                + '</div>';
            }
            listEl.innerHTML = html;
          }
        }
      });

      window.addEventListener('metMapAlertsError', function() {
        var updatedEl = document.getElementById('met-alert-updated');
        if (updatedEl) {
          updatedEl.textContent = 'Couldn\'t reach NWS';
          updatedEl.style.color = '#C2342B';
          setTimeout(function() { updatedEl.style.color = ''; }, 3000);
        }
      });

      function formatCount(n) {
        if (n === 0 || n === undefined || n === null) return '0';
        if (n > 99) return '99+';
        return String(n);
      }
    }

    // Recognition button — soft visual confirmation when a Met sends a
    // high-five to a teammate. In the prototype this is a stub; in
    // production it'll fire a real notification (in-app + optional
    // SMS) to the recipient. The point is the affordance, not the
    // mechanism.
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('.ws-recognize-btn');
      if (!btn || btn.classList.contains('is-sent')) return;
      ev.preventDefault();
      var target = btn.getAttribute('data-recognize-target') || 'teammate';
      btn.classList.add('is-sent');
      var label = btn.querySelector('span:last-child');
      if (label) label.textContent = 'Sent';
      var firstName = target.charAt(0).toUpperCase() + target.slice(1);
      if (typeof showTipToast === 'function') {
        showTipToast('Sent to ' + firstName + ' \u2014 they\u2019ll see it on next sign-in');
      }
    });

    // Met queue card actions — claim/skip
    document.addEventListener('click', function(ev) {
      var claim = ev.target.closest('[data-claim-review]');
      if (claim) {
        ev.preventDefault();
        openMetComposer(claim.getAttribute('data-claim-review'));
        return;
      }
      var skip = ev.target.closest('[data-skip-review]');
      if (skip) {
        ev.preventDefault();
        showTipToast('Skipped \u2014 returned to queue for another Met');
        return;
      }
    });

    // Verdict button toggle. Scope to the group the clicked button is
    // in (closest .ws-verdict-grid or .met-verdict-grid) so we don't
    // accidentally clear active state on a different button group.
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-verdict]');
      if (!btn) return;
      ev.preventDefault();
      var group = btn.closest('.ws-verdict-grid, .met-verdict-grid');
      var siblings = group ? group.querySelectorAll('[data-verdict]') : document.querySelectorAll('[data-verdict]');
      for (var i = 0; i < siblings.length; i++) siblings[i].classList.remove('is-active');
      btn.classList.add('is-active');
    });
    // Same for daily-brief verdict
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-daily-verdict]');
      if (!btn) return;
      ev.preventDefault();
      var group = btn.closest('.ws-verdict-grid, .met-verdict-grid');
      var siblings = group ? group.querySelectorAll('[data-daily-verdict]') : document.querySelectorAll('[data-daily-verdict]');
      for (var i = 0; i < siblings.length; i++) siblings[i].classList.remove('is-active');
      btn.classList.add('is-active');
    });

    // Confidence slider live value — shows the band label + percentage
    // so the Met sees in real time what posture the customer will read.
    var conf = $('met-confidence-input');
    var confVal = $('met-confidence-value');
    if (conf && confVal) {
      conf.addEventListener('input', function() {
        var band = confidenceBand(conf.value);
        confVal.textContent = band.label + ' \u00b7 ' + conf.value + '%';
      });
    }

    // Brief Type + Delivery wiring — the load-bearing piece of the
    // composer workflow. Forecast type carries delivery semantics:
    // pick Morning Brief → delivers in the customer's morning window;
    // pick Severe Alert → bypasses everything. The override panel is
    // for the rare exception. Default state: Met composes, glances at
    // "will deliver at X," hits send, walks away.
    //
    // The DELIVERY_RULES table below encodes the five forecast types
    // and their default behavior. Computed strings are placeholder-
    // realistic for the prototype; production reads from customer
    // record (timezone, brief windows, quiet hours).
    var DELIVERY_RULES = {
      'morning': {
        when:  'Will deliver in <strong>Marcus\u2019s morning brief window</strong>',
        meta:  '5:30\u20137:00 AM Friday',
        why:   'Customer chose this window during onboarding &middot; system holds until then'
      },
      'evening': {
        when:  'Will deliver in <strong>Marcus\u2019s evening brief window</strong>',
        meta:  '5:00\u20137:00 PM today',
        why:   'Customer chose this window during onboarding &middot; system holds until then'
      },
      'paid-review': {
        when:  'Will deliver <strong>immediately upon send</strong>',
        meta:  'Marcus is waiting',
        why:   'Paid reviews go straight to the customer &middot; they\u2019re actively expecting an answer'
      },
      'thread-reply': {
        when:  'Will deliver <strong>immediately upon send</strong>',
        meta:  'Async reply',
        why:   'Thread replies send right away &middot; if it\u2019s after 10 PM, system will ask whether to hold until morning'
      },
      'severe': {
        when:  'Will deliver <strong>immediately upon send</strong>',
        meta:  'bypasses quiet hours',
        why:   'Severe weather alerts bypass everything &middot; this is what the customer paid for'
      }
    };

    var OVERRIDE_RULES = {
      'asap-quiet': {
        when:  'Will deliver <strong>as soon as possible</strong>',
        meta:  'override active',
        why:   'Sends now, but still respects quiet hours &middot; useful when you\u2019re running late on a brief'
      },
      'now-bypass': {
        when:  'Will deliver <strong>immediately, bypassing quiet hours</strong>',
        meta:  'override active',
        why:   'Use only for severe weather or paid reviews flagged urgent &middot; will wake the customer if it\u2019s the middle of the night'
      }
    };

    var deliveryWhen = $('met-delivery-when');
    var deliveryWhy  = $('met-delivery-why');

    // Compute and write the delivery line from the current selections.
    function updateDeliveryLine() {
      if (!deliveryWhen || !deliveryWhy) return;
      var typeBtn = document.querySelector('.ws-brief-type-btn.is-active');
      var type = typeBtn ? typeBtn.getAttribute('data-brief-type') : 'morning';
      var overrideRadio = document.querySelector('input[name="met-delivery-override"]:checked');
      var override = overrideRadio ? overrideRadio.value : 'default';

      var rule;
      if (override !== 'default' && OVERRIDE_RULES[override]) {
        rule = OVERRIDE_RULES[override];
      } else {
        rule = DELIVERY_RULES[type] || DELIVERY_RULES['morning'];
      }
      deliveryWhen.innerHTML = rule.when +
        ' <span class="ws-delivery-relative">' + rule.meta + '</span>';
      deliveryWhy.innerHTML = rule.why;
    }

    // Brief Type buttons — click to pick the forecast type.
    var typeBtns = document.querySelectorAll('.ws-brief-type-btn');
    for (var tb = 0; tb < typeBtns.length; tb++) {
      typeBtns[tb].addEventListener('click', function() {
        for (var i = 0; i < typeBtns.length; i++) {
          typeBtns[i].classList.remove('is-active');
        }
        this.classList.add('is-active');

        // Reset any override back to default whenever the Met picks
        // a new type — the type's own default is almost always what
        // they want. Override is for the rare exception.
        var defaultOverride = document.querySelector('input[name="met-delivery-override"][value="default"]');
        if (defaultOverride) defaultOverride.checked = true;

        updateDeliveryLine();
      });
    }

    // Override toggle — expand/collapse the panel.
    var overrideToggle = $('met-delivery-override-toggle');
    var overridePanel  = $('met-delivery-override-panel');
    if (overrideToggle && overridePanel) {
      overrideToggle.addEventListener('click', function() {
        var nowOpen = overridePanel.hasAttribute('hidden');
        if (nowOpen) {
          overridePanel.removeAttribute('hidden');
          overrideToggle.setAttribute('aria-expanded', 'true');
        } else {
          overridePanel.setAttribute('hidden', '');
          overrideToggle.setAttribute('aria-expanded', 'false');
        }
      });
    }

    // Override radios — update delivery line when chosen.
    var overrideRadios = document.querySelectorAll('input[name="met-delivery-override"]');
    for (var ovr = 0; ovr < overrideRadios.length; ovr++) {
      overrideRadios[ovr].addEventListener('change', updateDeliveryLine);
    }

    // Initialize on composer-open so the line is correct before the
    // Met touches anything.
    updateDeliveryLine();

    // Composer submit + cancel
    var submitBtn = $('met-submit-btn');
    if (submitBtn) {
      submitBtn.addEventListener('click', function() {
        var composer = $('met-composer');
        if (!composer) return;
        var isReal = composer.dataset.isReal === '1';
        var claimToken = composer.dataset.claimToken;

        // For real requests, POST the verdict to the backend before
        // closing the composer. Backend will SMS the customer their
        // verdict and mark the request completed.
        if (isReal && claimToken) {
          // Read the verdict from the active button + assemble notes
          // from the four textareas.
          var activeVerdictBtn = composer.querySelector('[data-verdict].is-active');
          var verdict = activeVerdictBtn ? activeVerdictBtn.getAttribute('data-verdict') : 'caution';
          var reason = ($('met-reason') || {}).value || '';
          var watch = ($('met-watch') || {}).value || '';
          var bottom = ($('met-bottom-line') || {}).value || '';
          var alt = ($('met-alternate') || {}).value || '';
          // Compose the notes block sent to the customer. The /complete
          // endpoint accepts free-form notes; we shape it as a readable
          // brief with the standard section labels.
          var notesParts = [];
          if (reason.trim()) notesParts.push('REASON\n' + reason.trim());
          if (watch.trim())  notesParts.push('WATCH FOR\n' + watch.trim());
          if (bottom.trim()) notesParts.push('BOTTOM LINE\n' + bottom.trim());
          if (alt.trim())    notesParts.push('ALTERNATE\n' + alt.trim());
          var notes = notesParts.join('\n\n');

          submitBtn.disabled = true;
          var originalLabel = submitBtn.textContent;
          submitBtn.textContent = 'Submitting\u2026';

          var BACKEND = 'https://wv-valet-backend.onrender.com';
          fetch(BACKEND + '/meteorologist/' + encodeURIComponent(claimToken) + '/complete', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
              'Accept': 'application/json'
            },
            credentials: 'include',
            body: JSON.stringify({ verdict: verdict, notes: notes })
          })
            .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
            .then(function(res) {
              submitBtn.disabled = false;
              submitBtn.textContent = originalLabel;
              if (res.body && res.body.ok) {
                showTipToast('Brief sent \u2014 customer notified by SMS');
                closeMetComposer(true);
                // Refetch the queue to make sure it's in sync with backend
                try { if (typeof loadMetQueueFromBackend === 'function') loadMetQueueFromBackend(); } catch (e) {}
              } else {
                showTipToast('Couldn\u2019t submit. ' + (res.body && res.body.error ? '(' + res.body.error + ')' : ''));
              }
            })
            .catch(function() {
              submitBtn.disabled = false;
              submitBtn.textContent = originalLabel;
              showTipToast('Network error. Try again.');
            });
          return;
        }

        // Mock-only path — original toast-and-close behavior
        showTipToast('Review submitted \u2014 customer notified');
        closeMetComposer(true);
      });
    }
    var cancelBtn = $('met-cancel-btn');
    if (cancelBtn) {
      cancelBtn.addEventListener('click', function() {
        showTipToast('Released back to queue');
        closeMetComposer(false);
      });
    }
  }

  // Switch the visible screen based on the dev-menu role selection.
  // "customer" returns to whatever screen the user was last on (or
  // welcome by default). "met" and "admin" route to the simulated
  // portals. Updates the active state of the dev menu options.
  var lastCustomerScreen = 'screen-welcome';
  function switchDevView(view) {
    // Track the customer-side screen so "Customer" returns the user where they were
    var currentActive = document.querySelector('.screen.active');
    if (currentActive && currentActive.id !== 'screen-met'
                     && currentActive.id !== 'screen-admin'
                     && currentActive.id !== 'screen-crew-ws') {
      lastCustomerScreen = currentActive.id;
    }

    if (view === 'customer') {
      showScreen(lastCustomerScreen);
    } else if (view === 'met') {
      showScreen('screen-met');
      // Refresh the greeting so it matches the time of day at the
      // moment the Met opens the workspace.
      setMetWelcome();
    } else if (view === 'crew-ws') {
      showScreen('screen-crew-ws');
      setupCrewWorkspace();
    } else if (view === 'admin') {
      showScreen('screen-admin');
      // Refresh the greeting so it reflects the time of day at the
      // moment the user actually opens the Admin view, not whatever
      // time it was when the page first loaded.
      setAdminWelcome();
    }

    // Toggle body class so global Crew UI (Quick Report sheet, FAB) hides
    // when the prototype is showing a non-customer view.
    document.body.classList.remove('dev-view-met', 'dev-view-admin', 'dev-view-crew-ws');
    if (view === 'met') document.body.classList.add('dev-view-met');
    if (view === 'admin') document.body.classList.add('dev-view-admin');
    if (view === 'crew-ws') document.body.classList.add('dev-view-crew-ws');

    // Update active state + label on the dev menu
    var options = document.querySelectorAll('[data-dev-view]');
    for (var i = 0; i < options.length; i++) {
      options[i].classList.toggle('is-active',
        options[i].getAttribute('data-dev-view') === view);
    }
    var label = $('dev-menu-current-label');
    if (label) {
      var labelMap = { customer: 'Customer', met: 'Meteorologist', admin: 'Admin', 'crew-ws': 'Valet Crew' };
      label.textContent = labelMap[view] || 'Customer';
    }
    var menu = $('dev-menu');
    if (menu) menu.classList.remove('is-open');
  }

  // ── Wire up event listeners ──────────────────────────────────────────────
  function init() {
    // ── Phase 10 Item #5 — Crew mission response page ─────────────────
    // If the URL has ?mission=<token>, this is a Crew member tapping
    // their SMS link. Show the mission response page and skip the rest
    // of the normal page init. The mission response page is its own
    // self-contained screen — no need to set up the rest of the app.
    if (typeof checkForMissionResponse === 'function' && checkForMissionResponse()) {
      // Wire the submit button; everything else can stay un-initialized.
      if (typeof setupMissionResponseSubmit === 'function') {
        setupMissionResponseSubmit();
      }
      return;
    }

    // ── Phase 10 Item #7 — NWS Met severe-alert page ───────────────────
    // If URL has ?nws-page=<token>, this is the on-duty Met tapping their
    // SMS link. Show the alert review page and skip normal init.
    if (typeof checkForNwsPage === 'function' && checkForNwsPage()) {
      if (typeof setupNwsPageActions === 'function') {
        setupNwsPageActions();
      }
      return;
    }

    // ── Phase 10 Met tips (TIP-B) — customer review/tip page ──────────
    // If URL has ?review=<token>, this is the $19 customer arriving from
    // their SMS link to view the review and optionally tip the Met.
    if (typeof checkForCustomerReview === 'function' && checkForCustomerReview()) {
      if (typeof setupCustomerReviewActions === 'function') {
        setupCustomerReviewActions();
      }
      return;
    }

    // ── Phase 10 marketing pages — legal pages (MARK-A) ───────────────
    // ?legal=privacy or ?legal=terms shows the corresponding policy page.
    // Static content, no API calls. Just routing.
    try {
      var legalParam = new URLSearchParams(window.location.search).get('legal');
      if (legalParam === 'privacy') {
        showScreen('screen-legal-privacy');
        return;
      }
      if (legalParam === 'terms') {
        showScreen('screen-legal-terms');
        return;
      }
      // ── MARK-B: FAQ + About pages ────────────────────────────────────
      // ?page=faq or ?page=about. Same pattern.
      var pageParam = new URLSearchParams(window.location.search).get('page');
      if (pageParam === 'faq') {
        showScreen('screen-faq');
        return;
      }
      if (pageParam === 'about') {
        showScreen('screen-about');
        return;
      }
      // ── Starter Month landing page (sales funnel) ─────────────────────
      // Routes that show the /starter page:
      //   - /starter  (clean URL — requires Render rewrite to /index.html)
      //   - /?page=starter  (fallback path)
      // Both accept ?rep=brian to attribute the lead to a sales rep.
      // Capture the rep slug in sessionStorage so it survives the
      // Stripe Checkout roundtrip.
      var isStarter = (window.location.pathname === '/starter') || (pageParam === 'starter');
      if (isStarter) {
        try {
          var repParam = new URLSearchParams(window.location.search).get('rep');
          if (repParam) {
            // Sanitize: alphanumeric + underscore, max 40 chars
            var cleanRep = (repParam.toLowerCase().match(/[a-z0-9_]+/g) || []).join('').slice(0, 40);
            if (cleanRep) {
              window.sessionStorage.setItem('wv_starter_rep', cleanRep);
              // Show "Referred by X" line on the page
              var repLine = document.getElementById('starter-rep-line');
              var repName = document.getElementById('starter-rep-name');
              if (repLine && repName) {
                repName.textContent = cleanRep.charAt(0).toUpperCase() + cleanRep.slice(1);
                repLine.hidden = false;
              }
            }
          }
        } catch (e) {}
        // Wire the CTA button
        var ctaBtn = document.getElementById('starter-cta-btn');
        if (ctaBtn && !ctaBtn._wvWired) {
          ctaBtn._wvWired = true;
          ctaBtn.addEventListener('click', function() {
            ctaBtn.disabled = true;
            ctaBtn.textContent = 'Starting checkout…';
            var rep = null;
            try { rep = window.sessionStorage.getItem('wv_starter_rep'); } catch (e) {}
            var BACKEND = 'https://wv-valet-backend.onrender.com';
            fetch(BACKEND + '/api/v1/subscribe', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              credentials: 'include',
              body: JSON.stringify({
                tier: 'pro_single',
                starter: true,
                rep: rep || null
              })
            })
              .then(function(r) { return r.json().then(function(j) { return j; }); })
              .then(function(res) {
                if (res && res.ok && res.url) {
                  window.location.href = res.url;
                } else {
                  ctaBtn.disabled = false;
                  ctaBtn.textContent = 'Start my $99 month';
                  window.alert((res && (res.message || res.error)) || 'Could not start checkout. Try again.');
                }
              })
              .catch(function() {
                ctaBtn.disabled = false;
                ctaBtn.textContent = 'Start my $99 month';
                window.alert('Network error. Try again.');
              });
          });
        }
        showScreen('screen-starter');
        return;
      }
    } catch (e) {}

    // ── Phase 10 Item #6 — Crew home base controls ────────────────────
    if (typeof setupCrewHomeBaseControls === 'function') {
      try { setupCrewHomeBaseControls(); } catch (e) { console.warn('crew homebase setup failed', e); }
    }

    // ── Phase 10 Admin Tools — Crew register form real submission ─────
    // Replaces the old data-email-capture="crew" waitlist stub. Wires
    // the button to POST /api/v1/crew/apply. On success, shows a clean
    // confirmation overlay so the user knows they're in the queue.
    document.addEventListener('click', function(ev) {
      var btn = ev.target.closest('[data-crew-register-submit]');
      if (!btn) return;
      ev.preventDefault();
      ev.stopPropagation();

      var form = $('crew-register-form');
      if (!form) return;
      var fd = new FormData(form);

      // Gather mission interests from checkboxes
      var interests = [];
      var checkboxes = form.querySelectorAll('input[type="checkbox"][name^="mission-"]');
      for (var i = 0; i < checkboxes.length; i++) {
        if (checkboxes[i].checked) {
          interests.push(checkboxes[i].name.replace('mission-', ''));
        }
      }

      // Validate required fields client-side for a snappy error
      var name = (fd.get('name') || '').toString().trim();
      var email = (fd.get('email') || '').toString().trim();
      var agree = form.querySelector('input[name="agree"]');
      if (!name) {
        window.alert('Please enter your name.');
        return;
      }
      if (!email) {
        window.alert('Please enter your email.');
        return;
      }
      if (!agree || !agree.checked) {
        window.alert('Please agree to the Valet Crew Code of Conduct.');
        return;
      }

      var notifyVal = 'sms';
      var notifyRadio = form.querySelector('input[name="notify"]:checked');
      if (notifyRadio) notifyVal = notifyRadio.value;

      var payload = {
        name: name,
        handle: (fd.get('handle') || '').toString().trim(),
        email: email,
        phone: (fd.get('phone') || '').toString().trim(),
        county: (fd.get('county') || '').toString().trim(),
        mission_interests: interests,
        hours: (fd.get('hours') || 'all').toString(),
        notify: notifyVal
      };

      var originalLabel = btn.textContent;
      btn.disabled = true;
      btn.textContent = 'Submitting\u2026';

      var BACKEND = 'https://wv-valet-backend.onrender.com';
      fetch(BACKEND + '/api/v1/crew/apply', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      })
        .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
        .then(function(res) {
          btn.disabled = false;
          btn.textContent = originalLabel;
          if (res.body && res.body.ok) {
            // Show success state — replace form area with a thank-you
            var section = form.closest('section') || form.parentNode;
            if (section) {
              section.innerHTML =
                '<div style="text-align:center;padding:60px 20px;">'
                + '<div style="width:64px;height:64px;background:rgba(18,161,80,0.12);'
                + 'border-radius:50%;display:inline-flex;align-items:center;justify-content:center;margin-bottom:20px;">'
                + '<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#0c7a3c" '
                + 'stroke-width="3" stroke-linecap="round" stroke-linejoin="round">'
                + '<polyline points="20 6 9 17 4 12"/></svg></div>'
                + '<h2 style="font-size:22px;margin:0 0 8px;color:#0E1116;">You&rsquo;re on the list.</h2>'
                + '<p style="font-size:14px;color:rgba(15,17,22,0.65);max-width:420px;margin:0 auto;line-height:1.55;">'
                + 'We\u2019ll review your application and reach out within 48 hours. '
                + 'Watch your inbox at <strong>' + escapeHtml(email) + '</strong> for next steps.</p>'
                + '</div>';
            }
            window.scrollTo(0, 0);
          } else {
            var errMsg = (res.body && res.body.message) ||
              (res.body && res.body.error === 'already-approved'
                ? 'You\u2019re already a Crew member \u2014 sign in to access your workspace.'
                : 'Couldn\u2019t submit. Please try again.');
            window.alert(errMsg);
          }
        })
        .catch(function() {
          btn.disabled = false;
          btn.textContent = originalLabel;
          window.alert('Network error. Please try again.');
        });
    });

    // ── Phase 10 C2 — handle Stripe Checkout return URLs ───────────────
    // When a customer completes Stripe Checkout, Stripe redirects them
    // back to FRONTEND_BASE_URL/?subscribed=1&tier=<tier>&session=<id>.
    // Show a celebration toast and clean up the URL so a reload doesn't
    // re-trigger. Cancellation returns ?subscribe_cancelled=1.
    try {
      var spStripe = new URLSearchParams(window.location.search);
      if (spStripe.get('subscribed') === '1') {
        var tierReturned = spStripe.get('tier') || '';
        var tierLabel = tierReturned === 'pro_multi' ? 'Pro Multi'
                      : tierReturned === 'pro_single' ? 'Pro Single'
                      : tierReturned === 'hobbyist' ? 'Hobbyist'
                      : 'WeatherValet';
        // Lightweight toast — a fixed banner top-center for 6 seconds.
        var toast = document.createElement('div');
        toast.style.cssText = 'position:fixed;top:16px;left:50%;transform:translateX(-50%);'
          + 'z-index:9999;background:#0E1116;color:#fff;padding:14px 22px;border-radius:8px;'
          + 'font-family:Inter,system-ui,sans-serif;font-size:14px;font-weight:500;'
          + 'box-shadow:0 8px 24px rgba(15,17,22,0.25);max-width:90vw;text-align:center;'
          + 'opacity:0;transition:opacity 200ms ease;';
        toast.innerHTML = '\u2713 Welcome to WeatherValet ' + tierLabel
          + '. Check your email to set your password.';
        document.body.appendChild(toast);
        setTimeout(function() { toast.style.opacity = '1'; }, 50);
        setTimeout(function() {
          toast.style.opacity = '0';
          setTimeout(function() { toast.remove(); }, 240);
        }, 6500);
        // Clean the URL so refresh doesn't re-toast
        try { window.history.replaceState({}, '', '/'); } catch (e) {}
      } else if (spStripe.get('subscribe_cancelled') === '1') {
        // Soft no-op — could show a "no charge made" toast. For now,
        // just clean the URL silently.
        try { window.history.replaceState({}, '', '/'); } catch (e) {}
      }
    } catch (e) {}

    // ── Restore persisted state at boot ────────────────────────────────
    // Order matters: load reports first so the in-memory CREW_REPORTS
    // reflects past mutations and any user-submitted reports BEFORE auth
    // restoration triggers the first render of dependent UI.
    loadReportsState();
    setupAgingTick();
    setupBroadcastTicker();
    // Met posts (Chunk 6c-1): load persisted posts + wire composer + modal.
    try {
      loadMetPosts();
      // Chunk 6c-2: migrate old posts to have a status field, render
      // the scheduled queue (if any), start the promotion timer.
      migrateMetPostsForScheduling();
      renderMetPostQueue();
      startMetPostPromotionTimer();
      setupMetPostComposer();
      setupMetPostModal();
      // Chunk D — polygon-drawing map in the Daily Brief tab
      try { setupBriefPolygonMap(); } catch (e) { console.warn('brief polygon map setup failed', e); }
    } catch (e) { console.warn('Met post setup failed', e); }
    setupOnAirRail();
    setupProfileRecent();
    setupUpNextFooter();
    setupNwsFilterControls();
    setupNwsOverlayClicks();
    setupCrewMapFilterPills();
    // Initial render of the tonight-story tagline so the strip isn't
    // blank before the first renderFeed() runs (which would otherwise
    // be the first thing to populate it).
    try {
      if (typeof renderTonightStory === 'function') renderTonightStory();
    } catch (e) {}
    auth.restore();
    setupDevMenu();
    setupTeamPanel();
    if (typeof setupAdminOperations === 'function') {
      try { setupAdminOperations(); } catch (e) { console.warn('admin ops setup failed', e); }
    }
    if (typeof setupProBriefControls === 'function') {
      try { setupProBriefControls(); } catch (e) { console.warn('pro briefs setup failed', e); }
    }
    // F-X3: Daily Brief Polish button (same shared helper, different IDs)
    if (typeof wirePolishButton === 'function') {
      try {
        wirePolishButton({
          btnId: 'daily-brief-polish-btn',
          textareaId: 'brief-summary',
          panelId: 'daily-brief-polish-panel',
          statusId: 'daily-brief-polish-status',
          listId: 'daily-brief-polish-list',
          closeId: 'daily-brief-polish-close'
        });
      } catch (e) { console.warn('daily brief polish setup failed', e); }
    }
    if (typeof setupMetMessages === 'function') {
      try { setupMetMessages(); } catch (e) { console.warn('met messages setup failed', e); }
    }
    if (typeof setupMetTipEarnings === 'function') {
      try { setupMetTipEarnings(); } catch (e) { console.warn('met tips setup failed', e); }
    }
    if (typeof setupMetWelcomeCard === 'function') {
      try { setupMetWelcomeCard(); } catch (e) { console.warn('met welcome setup failed', e); }
    }
    // F-X2: "+ Create new mission template" — opens modal that POSTs to backend
    var newTmplBtn = $('met-new-template-btn');
    if (newTmplBtn && !newTmplBtn._wvWired) {
      newTmplBtn._wvWired = true;
      newTmplBtn.addEventListener('click', openCreateTemplateModal);
    }
    setupStormShelter();
    setupAtmosphericParallax();
    setupEmailCapture();
    setupSignInModal();
    setupAuthSession();
    setupRotatingHeadline();

    var form = $('search-form');
    var input = $('search-input');
    var submitBtn = document.querySelector('.search-submit');

    // ════════════════════════════════════════════════════════════════════
    // BULLETPROOF SUBMIT WIRING
    // We wire the submission through FOUR independent paths so that any
    // browser quirk (mobile Safari autofill panels, autocomplete dropdowns,
    // keyboard dismissal, transform-related click target weirdness) cannot
    // silently swallow the user's intent. The fireSubmit() function is
    // idempotent — if multiple paths fire on a single user action, only
    // the first one triggers a navigation thanks to the lock flag.
    // ════════════════════════════════════════════════════════════════════
    var submitLock = false;
    function fireSubmit(eventOrigin) {
      if (submitLock) return;
      submitLock = true;
      // Release the lock after the navigation animation completes
      setTimeout(function() { submitLock = false; }, 3000);
      var v = input ? input.value : '';
      handleSubmit(v);
    }

    // Path 1: form submit event (covers Enter key + most button clicks)
    if (form) {
      form.addEventListener('submit', function(e) {
        e.preventDefault();
        fireSubmit('form-submit');
      });
    }

    // Path 2: explicit click on the submit button (covers cases where the
    // SVG inside the button captures the click and submit doesn't bubble)
    if (submitBtn) {
      submitBtn.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        fireSubmit('button-click');
      });
    }

    // Path 3: pointerdown on the button (covers iOS Safari delays where
    // the click event is suppressed because the keyboard is dismissing)
    if (submitBtn) {
      submitBtn.addEventListener('pointerdown', function(e) {
        // Don't interfere with the natural click — only catch missed clicks
        // by setting a short fallback. If the click event fires within 600ms,
        // the lock prevents double-fire. If not, this guarantees a submit.
        setTimeout(function() { fireSubmit('pointerdown-fallback'); }, 80);
      });
    }

    // Path 4: explicit Enter key on the input (some mobile keyboards send
    // Enter without firing the form's submit event)
    if (input) {
      input.addEventListener('keydown', function(e) {
        if (e.key === 'Enter' || e.keyCode === 13) {
          e.preventDefault();
          fireSubmit('input-enter');
        }
      });
      // Also dismiss any browser-native dropdown that might be intercepting clicks
      input.addEventListener('blur', function() {
        // No-op — just being explicit about blur events
      });
    }

    // ── Search dropdown — focus opens, blur closes (with delay) ──
    var searchWrap = $('search-wrap');
    if (input && searchWrap) {
      input.addEventListener('focus', function() {
        searchWrap.classList.add('is-focused');
      });
      input.addEventListener('blur', function() {
        // Delay the close so a chip click can complete before we hide the dropdown
        setTimeout(function() {
          // Don't close if focus moved to something inside the wrap (e.g., a chip)
          if (!searchWrap.contains(document.activeElement)) {
            searchWrap.classList.remove('is-focused');
          }
        }, 180);
      });
      // Click outside the wrap also closes it
      document.addEventListener('click', function(e) {
        if (!searchWrap.contains(e.target)) {
          searchWrap.classList.remove('is-focused');
        }
      });
    }

    // ── Example chips ──
    // Use mousedown instead of click to fire BEFORE the input's blur event.
    // Otherwise the dropdown can hide before the click resolves (especially on touch).
    var chips = document.querySelectorAll('.chip');
    for (var i = 0; i < chips.length; i++) {
      (function(chip) {
        function handleChip(e) {
          if (e) e.preventDefault();
          var text = chip.getAttribute('data-chip') || chip.textContent;
          if (input) input.value = text.trim();
          if (searchWrap) searchWrap.classList.remove('is-focused');
          fireSubmit('chip-click');
        }
        chip.addEventListener('mousedown', handleChip);
        chip.addEventListener('touchstart', handleChip, { passive: false });
        // Click as a final fallback (e.g., keyboard activation)
        chip.addEventListener('click', handleChip);
      })(chips[i]);
    }

    var backBtn = $('back-button');
    if (backBtn) {
      backBtn.addEventListener('click', handleBack);
    }

    // ── Modal wiring ──
    var modalCloseX = $('modal-close-x');
    var modalCancel = $('modal-cancel');
    var modalDone = $('modal-done');
    var modalConfirm = $('modal-confirm');
    var modalScrim = $('upsell-modal');

    if (modalCloseX) modalCloseX.addEventListener('click', closeUpsellModal);
    if (modalCancel) modalCancel.addEventListener('click', closeUpsellModal);
    if (modalDone) modalDone.addEventListener('click', closeUpsellModal);
    if (modalConfirm) modalConfirm.addEventListener('click', handleConfirmVerification);

    // Click the dark scrim background to close (but not the card itself)
    if (modalScrim) {
      modalScrim.addEventListener('click', function(e) {
        if (e.target === modalScrim) closeUpsellModal();
      });
    }

    // Pricing tier selector
    var pricingOptions = document.querySelectorAll('.pricing-option');
    for (var p = 0; p < pricingOptions.length; p++) {
      (function(opt) {
        opt.addEventListener('click', function() {
          var all = document.querySelectorAll('.pricing-option');
          for (var k = 0; k < all.length; k++) all[k].classList.remove('is-selected');
          opt.classList.add('is-selected');
        });
      })(pricingOptions[p]);
    }

    // Escape key closes modal
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && modalScrim && modalScrim.classList.contains('active')) {
        closeUpsellModal();
      }
    });

    // ── Valet Crew sub-app wiring ─────────────────────────────────────────
    // The Crew screens (Profile / Live Feed / Quick Report sheet) are
    // secondary surfaces. Entry: tap "Join the Crew" on the welcome page.
    // Navigation: data-show-screen / data-back-to attributes drive showScreen.
    // Quick Report: data-open-report opens the bottom sheet from anywhere.

    // Welcome-page entry point — signed-out users land on the Live Feed
    // (browseable demo surface). Signed-in users land on their Profile
    // (the member card with their tokens, badges, stats).
    var joinCrewBtn = document.querySelector('.join-crew');
    if (joinCrewBtn) {
      joinCrewBtn.addEventListener('click', function() {
        showScreen(auth.isSignedIn() ? 'screen-crew-profile' : 'screen-crew-feed');
      });
    }

    // Generic data-show-screen / data-back-to navigation
    document.addEventListener('click', function(ev) {
      var showTarget = ev.target.closest('[data-show-screen]');
      if (showTarget) {
        ev.preventDefault();
        showScreen(showTarget.getAttribute('data-show-screen'));
        return;
      }
      var backTarget = ev.target.closest('[data-back-to]');
      if (backTarget) {
        ev.preventDefault();
        showScreen(backTarget.getAttribute('data-back-to'));
        return;
      }

      // ── Verify button on the Detail screen ────────────────────────────
      // Order matters: check verify/tip BEFORE the generic data-report-id
      // handler, because those buttons live INSIDE elements that may also
      // carry data-report-id ancestors. The verify/tip handlers swallow
      // the click; ancestors don't see it.
      var verifyBtn = ev.target.closest('[data-verify-id]');
      if (verifyBtn) {
        ev.preventDefault();
        ev.stopPropagation();
        handleVerify(verifyBtn);
        return;
      }
      var unverifyBtn = ev.target.closest('[data-unverify-id]');
      if (unverifyBtn) {
        ev.preventDefault();
        ev.stopPropagation();
        handleUnverify(unverifyBtn);
        return;
      }
      var refreshBtn = ev.target.closest('[data-refresh-id]');
      if (refreshBtn) {
        ev.preventDefault();
        ev.stopPropagation();
        handleRefresh(refreshBtn);
        return;
      }
      var tipBtn = ev.target.closest('[data-tip-id]');
      if (tipBtn) {
        ev.preventDefault();
        ev.stopPropagation();
        handleTip(tipBtn);
        return;
      }

      // ── Open the Detail screen for a tapped pulse or story card ───────
      // data-report-id can sit on either a .feed-pulse or a <li class="story">.
      // Both use the same handler — both lead to the same Detail view.
      var reportTarget = ev.target.closest('[data-report-id]');
      if (reportTarget) {
        ev.preventDefault();
        openReportDetail(reportTarget.getAttribute('data-report-id'));
        return;
      }

      var openReport = ev.target.closest('[data-open-report]');
      if (openReport) {
        ev.preventDefault();
        // For May 24 launch: signed-out users can't actually submit
        // reports (real Crew registration is deferred to July). Route
        // them to the Crew waitlist email-capture instead. A signed-in
        // Crew member (post-July, once registration is wired) will
        // pass this gate and open the report sheet normally.
        if (!auth.isSignedIn()) {
          var modal = document.getElementById('email-capture-modal');
          if (modal) {
            // Synthesize a click on a hidden trigger by dispatching
            // the same event flow used elsewhere — set variant on
            // the modal, show it.
            var fakeTrigger = document.createElement('span');
            fakeTrigger.setAttribute('data-email-capture', 'crew');
            document.body.appendChild(fakeTrigger);
            fakeTrigger.click();
            document.body.removeChild(fakeTrigger);
            return;
          }
        }
        openReportSheet();
        return;
      }
    });

    // ── Verify flow ────────────────────────────────────────────────────
    function handleVerify(btn) {
      var reportId = btn.getAttribute('data-verify-id');
      var report = getReportById(reportId);
      if (!report) return;
      // Guard rails — never duplicate, never on own report. (When signed
      // out, "own report" is a non-concept since there's no current user;
      // the auth gate fires first and the rest of the rails apply after.)
      if (isMyReport(report)) return;
      if (verifiedByMe.has(reportId)) return;

      // Auth gate: signed-out users see the modal. After they tap Continue,
      // this same callback fires with auth.isSignedIn() === true and the
      // verification proceeds.
      requireAuth('verify', function() {
        // Mutate state
        report.verified = (report.verified || 0) + 1;
        verifiedByMe.add(reportId);
        verifiedByMeAt[reportId] = Date.now();  // for 5-min un-verify window
        tokens.earn(2);  // auto-persists tokens
        // Bump verifications count for the user's profile stats
        var s = auth.currentState();
        if (s) {
          s.verificationsCount = (s.verificationsCount || 0) + 1;
          s.verifiedByMeAt = verifiedByMeAt;  // persist timestamps
        }
        // Persist the verifiedByMe Set + report.verified change
        auth.persist();
        persistReports();
        // Phase 5b — also persist to the backend (fire and forget)
        try { backendVerifyReport(reportId); } catch (e) {}
        afterStateChange();

        // Re-render the side feed list so the verify button on the
        // tapped card flips to "You verified" without a page reload.
        try { renderFeed(); } catch (e) {}

        // Visual feedback in the count number — bump scale
        var countEl = $('detail-verify-count-num');
        if (countEl) {
          countEl.textContent = report.verified;
          countEl.classList.add('bump');
          setTimeout(function() { countEl.classList.remove('bump'); }, 320);
        }

        // +2 token burst inside the button
        var burst = document.createElement('span');
        burst.className = 'verify-burst';
        burst.textContent = '+2';
        btn.appendChild(burst);
        setTimeout(function() {
          if (burst.parentNode) burst.parentNode.removeChild(burst);
        }, 1200);

        // Re-render the section so the button moves to "Verified ✓" state.
        setTimeout(function() {
          renderReportDetail(reportId);
        }, 700);

        showTipToast('+2 tokens · Thanks for verifying');
      });
    }

    // ── Un-verify flow ─────────────────────────────────────────────────
    // 5-minute window for catching mistaps. After the window closes
    // the Undo button no longer renders. Reverses everything the
    // verify did: count goes down, set entry is cleared, tokens
    // returned, stat decremented.
    function handleUnverify(btn) {
      var reportId = btn.getAttribute('data-unverify-id');
      var report = getReportById(reportId);
      if (!report) return;
      if (!canUnverify(reportId)) return;  // window expired

      report.verified = Math.max(0, (report.verified || 0) - 1);
      verifiedByMe['delete'](reportId);
      delete verifiedByMeAt[reportId];

      // Return the +2 tokens. Stat goes down by 1.
      tokens.earn(-2);
      var s = auth.currentState();
      if (s) {
        s.verificationsCount = Math.max(0, (s.verificationsCount || 0) - 1);
        s.verifiedByMeAt = verifiedByMeAt;
      }
      auth.persist();
      persistReports();
      // Phase 5b — also delete from backend (fire and forget)
      try { backendUnverifyReport(reportId); } catch (e) {}
      afterStateChange();

      try { renderFeed(); } catch (e) {}
      showTipToast('Verification undone');
    }

    // ── "Things are different here" flow ───────────────────────────────
    // One-tap counter-report from a stale report card. We don't expect
    // the Crew member to think about it — we infer the most useful
    // counter-observation type from the original:
    //   stale storm/hail → submit "clearing" (the storm passed)
    //   stale rain/snow/sleet/freezing → submit "clearing"
    //   stale wind/fog → submit "clear" (conditions normalized)
    //   stale sunny/cloudy/clear → no counter needed (these aren't
    //     events; user should submit a fresh report normally)
    // The new report goes through submitQuickReport(), so it gets
    // proper attribution, location, token reward, and field-notebook
    // note (skipped — fast path).
    function handleRefresh(btn) {
      var reportId = btn.getAttribute('data-refresh-id');
      var originalType = btn.getAttribute('data-refresh-type');
      if (!reportId || !originalType) return;
      // Map original → counter-type
      var counterMap = {
        storm: 'clearing', hail: 'clearing',
        rain: 'clearing', snow: 'clearing', sleet: 'clearing',
        freezing: 'clearing', wind: 'clear', fog: 'clear'
      };
      var counterType = counterMap[originalType];
      if (!counterType) {
        // No sensible counter — invite the user to submit a fresh report.
        openReportSheet();
        return;
      }
      requireAuth('report', function() {
        // Fast-path: directly call submitQuickReport with the counter type.
        // This handles tokens, persistence, and re-render automatically.
        submitQuickReport(counterType, null);
        showTipToast('Submitted a fresh observation \u00b7 thanks for the update');
      });
    }

    // ── Tip flow ────────────────────────────────────────────────────────
    function handleTip(btn) {
      var reportId = btn.getAttribute('data-tip-id');
      var level = btn.getAttribute('data-tip-level');
      var cost = parseInt(btn.getAttribute('data-tip-cost') || '0', 10);
      var report = getReportById(reportId);
      if (!report) return;
      // Guard rails
      if (isMyReport(report)) return;
      if (hasTipped(reportId, level)) return;
      if (!tokens.canAfford(cost)) {
        showTipToast('Not enough tokens — keep contributing!');
        return;
      }

      // Auth gate: signed-out users can't spend tokens.
      requireAuth('tip', function() {
        // Mutate state
        tokens.spend(cost);  // auto-persists tokens
        markTipped(reportId, level);
        report.tips = report.tips || { nod: 0, coffee: 0, handshake: 0 };
        report.tips[level] = (report.tips[level] || 0) + 1;
        // Persist the tipsByMe Set + report.tips change
        auth.persist();
        persistReports();
        afterStateChange();

        // -N token burst inside the tip button
        var burst = document.createElement('span');
        burst.className = 'tip-burst';
        burst.textContent = '-' + cost;
        btn.appendChild(burst);
        setTimeout(function() {
          if (burst.parentNode) burst.parentNode.removeChild(burst);
        }, 1200);

        // Re-render after the burst plays
        setTimeout(function() {
          renderReportDetail(reportId);
        }, 700);

        var labels = { nod: 'The Nod', coffee: 'Bought a coffee', handshake: 'The Handshake' };
        var label = labels[level] || 'Tipped';
        showTipToast(label + ' · ' + cost + ' token' + (cost === 1 ? '' : 's') + ' sent');
      });
    }

    // ── Profile token chip stays in sync ───────────────────────────────
    // The chip on the member card is one subscriber; verify/tip/submit
    // all flow through tokens.earn / tokens.spend, which notifies every
    // subscriber. Format the balance with a comma for readability.
    var tokensChipValue = $('tokens-chip-value');
    if (tokensChipValue) {
      tokens.subscribe(function(balance) {
        tokensChipValue.dataset.tokens = String(balance);
        tokensChipValue.textContent = balance.toLocaleString('en-US');
      });
    }

    // ── Auth gate wiring ───────────────────────────────────────────────
    // Single delegation handler for every auth-related interaction:
    //   data-auth-trigger="profile" → open the auth modal (Profile signed-out CTA)
    //   data-auth-action="signout"  → end the session
    //   data-auth-action="close"    → dismiss the gate without signing in
    //   data-auth-tab="signin|signup" → switch tab inside the modal
    //   data-onboard-action="..."   → onboarding flow controls
    document.addEventListener('click', function(ev) {
      var trigger = ev.target.closest('[data-auth-trigger]');
      if (trigger) {
        ev.preventDefault();
        ev.stopPropagation();
        openAuthGate(trigger.getAttribute('data-auth-trigger'));
        return;
      }
      var tab = ev.target.closest('[data-auth-tab]');
      if (tab) {
        ev.preventDefault();
        ev.stopPropagation();
        switchAuthTab(tab.getAttribute('data-auth-tab'));
        return;
      }
      var action = ev.target.closest('[data-auth-action]');
      if (action) {
        ev.preventDefault();
        ev.stopPropagation();
        var actionType = action.getAttribute('data-auth-action');
        if (actionType === 'signout') {
          auth.signOut();
          showScreen('screen-welcome');
        } else if (actionType === 'close') {
          closeAuthGate();
        }
        return;
      }
      var onboard = ev.target.closest('[data-onboard-action]');
      if (onboard) {
        ev.preventDefault();
        ev.stopPropagation();
        handleOnboardAction(onboard.getAttribute('data-onboard-action'));
        return;
      }
    });

    // Tab switcher inside the auth modal
    function switchAuthTab(tabName) {
      var tabs = document.querySelectorAll('.auth-tab');
      var panels = document.querySelectorAll('.auth-panel');
      for (var i = 0; i < tabs.length; i++) {
        var isActive = tabs[i].getAttribute('data-auth-tab') === tabName;
        tabs[i].classList.toggle('is-active', isActive);
        tabs[i].setAttribute('aria-selected', isActive ? 'true' : 'false');
      }
      for (var j = 0; j < panels.length; j++) {
        panels[j].classList.toggle('is-active',
          panels[j].id === 'auth-' + tabName + '-form');
      }
      // Hide any error from the other panel
      var allErrors = document.querySelectorAll('.auth-error');
      for (var k = 0; k < allErrors.length; k++) allErrors[k].hidden = true;
    }

    // Sign-in form submit
    var signinForm = $('auth-signin-form');
    if (signinForm) {
      signinForm.addEventListener('submit', function(ev) {
        ev.preventDefault();
        var email = signinForm.elements['email'].value;
        var password = signinForm.elements['password'].value;
        var errEl = $('auth-signin-error');
        var result = auth.signInWithCredentials(email, password);
        if (!result.ok) {
          if (errEl) {
            errEl.textContent = result.error;
            errEl.hidden = false;
          }
          return;
        }
        if (errEl) errEl.hidden = true;
        // Run pending action (verify/tip/report) if there was one
        var pending = pendingAuthAction;
        pendingAuthAction = null;
        closeAuthGate();
        signinForm.reset();
        if (typeof pending === 'function') setTimeout(pending, 320);
      });
    }

    // Sign-up form submit
    var signupForm = $('auth-signup-form');
    if (signupForm) {
      signupForm.addEventListener('submit', function(ev) {
        ev.preventDefault();
        // _signupGeoCoords is set by the geolocation button when the user
        // taps "Use my location" — null if they typed the city manually.
        var coords = window._signupGeoCoords || null;
        var input = {
          name: signupForm.elements['name'].value,
          email: signupForm.elements['email'].value,
          handle: signupForm.elements['handle'].value,
          city: signupForm.elements['city'].value,
          password: signupForm.elements['password'].value,
          lat: coords ? coords.lat : null,
          lng: coords ? coords.lng : null,
          locationSource: coords ? 'geolocation' : 'typed'
        };
        var errEl = $('auth-signup-error');
        var result = auth.signUp(input);
        if (!result.ok) {
          if (errEl) {
            errEl.textContent = result.error;
            errEl.hidden = false;
          }
          return;
        }
        if (errEl) errEl.hidden = true;
        // Clear the cached coords once consumed
        window._signupGeoCoords = null;
        var pending = pendingAuthAction;
        pendingAuthAction = null;
        closeAuthGate();
        signupForm.reset();
        // First-time-signup gets the onboarding flow before any pending action
        setTimeout(function() {
          startOnboarding(function() {
            if (typeof pending === 'function') pending();
          });
        }, 360);
      });
    }

    // ── "Use my location" geolocation button ────────────────────────────
    // Asks the browser for the user's current coordinates, then reverse-
    // geocodes them via Open-Meteo's /v1/reverse endpoint. Populates the
    // city input AND stashes the precise lat/lng on window._signupGeoCoords
    // so the form submit handler can persist them on the user record.
    // If geolocation is denied or fails, the user can still type a city
    // manually — the button is a shortcut, not a requirement.
    var geoBtn = $('auth-signup-geo-btn');
    if (geoBtn) {
      geoBtn.addEventListener('click', function() {
        if (!navigator.geolocation) {
          if (typeof showTipToast === 'function') {
            showTipToast('Your browser doesn\u2019t support location \u2014 type your city instead');
          }
          return;
        }
        var label = $('auth-signup-geo-label');
        var cityInput = $('auth-signup-city');
        if (!label || !cityInput) return;

        // UI: show loading state
        geoBtn.classList.add('is-loading');
        geoBtn.disabled = true;
        label.textContent = 'Locating\u2026';

        navigator.geolocation.getCurrentPosition(
          // Success
          function(pos) {
            var lat = pos.coords.latitude;
            var lng = pos.coords.longitude;
            // Reverse-geocode via Open-Meteo
            var url = 'https://geocoding-api.open-meteo.com/v1/reverse?latitude='
                    + encodeURIComponent(lat.toFixed(4))
                    + '&longitude=' + encodeURIComponent(lng.toFixed(4))
                    + '&count=1&language=en&format=json';
            fetch(url)
              .then(function(r) { return r.ok ? r.json() : null; })
              .then(function(data) {
                var place = data && data.results && data.results[0];
                if (place) {
                  // Build a clean "City, ST" label
                  var stateAbbrev = place.admin1 || '';
                  // Map common state names to abbreviations for tighter display
                  var stateAbbrevs = {
                    'Alabama':'AL','Alaska':'AK','Arizona':'AZ','Arkansas':'AR',
                    'California':'CA','Colorado':'CO','Connecticut':'CT','Delaware':'DE',
                    'Florida':'FL','Georgia':'GA','Hawaii':'HI','Idaho':'ID',
                    'Illinois':'IL','Indiana':'IN','Iowa':'IA','Kansas':'KS',
                    'Kentucky':'KY','Louisiana':'LA','Maine':'ME','Maryland':'MD',
                    'Massachusetts':'MA','Michigan':'MI','Minnesota':'MN','Mississippi':'MS',
                    'Missouri':'MO','Montana':'MT','Nebraska':'NE','Nevada':'NV',
                    'New Hampshire':'NH','New Jersey':'NJ','New Mexico':'NM','New York':'NY',
                    'North Carolina':'NC','North Dakota':'ND','Ohio':'OH','Oklahoma':'OK',
                    'Oregon':'OR','Pennsylvania':'PA','Rhode Island':'RI','South Carolina':'SC',
                    'South Dakota':'SD','Tennessee':'TN','Texas':'TX','Utah':'UT',
                    'Vermont':'VT','Virginia':'VA','Washington':'WA','West Virginia':'WV',
                    'Wisconsin':'WI','Wyoming':'WY','District of Columbia':'DC'
                  };
                  var abbr = stateAbbrevs[stateAbbrev] || stateAbbrev;
                  cityInput.value = place.name + (abbr ? ', ' + abbr : '');
                  window._signupGeoCoords = { lat: lat, lng: lng };
                  if (typeof showTipToast === 'function') {
                    showTipToast('Located \u2014 ' + cityInput.value);
                  }
                } else {
                  // Reverse-geocode found nothing; still pass the raw coords
                  cityInput.value = lat.toFixed(2) + ', ' + lng.toFixed(2);
                  window._signupGeoCoords = { lat: lat, lng: lng };
                }
              })
              .catch(function() {
                // Network failure — keep coords, let user type city
                cityInput.value = lat.toFixed(2) + ', ' + lng.toFixed(2);
                window._signupGeoCoords = { lat: lat, lng: lng };
              })
              .then(function() {
                // Always reset button state
                geoBtn.classList.remove('is-loading');
                geoBtn.disabled = false;
                label.textContent = 'Use my location';
              });
          },
          // Error / denied
          function(err) {
            geoBtn.classList.remove('is-loading');
            geoBtn.disabled = false;
            label.textContent = 'Use my location';
            var msg = (err.code === 1)
              ? 'Location permission denied \u2014 type your city instead'
              : 'Couldn\u2019t get your location \u2014 type your city instead';
            if (typeof showTipToast === 'function') showTipToast(msg);
          },
          { enableHighAccuracy: false, timeout: 10000, maximumAge: 60000 }
        );
      });
    }

    // Auth scrim click → close the gate (treat as cancel)
    var authScrim = $('auth-gate-scrim');
    if (authScrim) {
      authScrim.addEventListener('click', closeAuthGate);
    }

    // Subscribe to auth state — toggle body class for CSS-driven Profile state
    auth.subscribe(function(user) {
      if (user) {
        document.body.classList.add('is-signed-in');
        applySignedInProfile(user);
      } else {
        document.body.classList.remove('is-signed-in');
        // Clear session-only state so a different user signing in next
        // doesn't see the previous user's NEW ribbons.
        sessionRecentBadges = new Set();
      }
      // Refresh the token chip even on signout so it stops showing a
      // stale balance from a previous session.
      tokens.refresh();
    });

    // ── Live Feed zoom controls + initial render ──────────────────────
    document.addEventListener('click', function(ev) {
      var zoomBtn = ev.target.closest('[data-zoom-action]');
      if (!zoomBtn || zoomBtn.disabled) return;
      ev.preventDefault();
      ev.stopPropagation();
      stepZoom(zoomBtn.getAttribute('data-zoom-action'));
    });

    // Render the feed initially so the pulses + stories are visible on
    // first load. Re-render whenever the user navigates back to the feed
    // (e.g. from Detail → back to Feed) so any zoom changes they made
    // before navigating away are reflected.
    renderFeed();
    document.addEventListener('click', function(ev) {
      // After a click that lands on the Live Feed screen, re-render.
      // setTimeout 0 lets the showScreen call complete first.
      var navTarget = ev.target.closest('[data-show-screen="screen-crew-feed"], [data-back-to="screen-crew-feed"]');
      if (navTarget) {
        setTimeout(function() {
          renderFeed();
          maybeShowCrewWelcome();
        }, 0);
      }
    });

    // First-time Crew welcome card. Shown the first time someone lands on
    // the Live Feed; dismissed-state persists in localStorage so they
    // don't see it again on this device. Reviewers reported the original
    // landing felt abrupt — this card answers "what is this and what can
    // I do here" before they have to figure it out from the map.
    var CREW_WELCOME_KEY = 'wv_v1_crew_welcome_dismissed';
    function maybeShowCrewWelcome() {
      var card = $('crew-welcome-card');
      if (!card) return;
      var dismissed = false;
      try { dismissed = localStorage.getItem(CREW_WELCOME_KEY) === '1'; } catch (e) {}
      if (!dismissed) card.hidden = false;
    }
    function dismissCrewWelcome() {
      var card = $('crew-welcome-card');
      if (card) card.hidden = true;
      try { localStorage.setItem(CREW_WELCOME_KEY, '1'); } catch (e) {}
    }
    var welcomeDismiss = $('crew-welcome-dismiss');
    var welcomeCta = $('crew-welcome-cta');
    if (welcomeDismiss) welcomeDismiss.addEventListener('click', dismissCrewWelcome);
    if (welcomeCta) welcomeCta.addEventListener('click', dismissCrewWelcome);
    // Also try showing immediately in case the user is already on the feed
    // when this code runs (e.g., bookmarked the feed URL with hash).
    maybeShowCrewWelcome();

    // Report sheet — open / close / submit
    var reportSheet = $('report-sheet');
    var reportScrim = $('report-scrim');
    var reportPick = $('report-pick-state');
    var reportThanks = $('report-thanks-state');
    var reportPhoto = $('report-photo-state');

    // Photo capture state — held in module scope so the file-input handler,
    // type-chip handler, and submit handler all share one set of values.
    var pickedPhotoDataUrl = null;   // compressed JPEG data URL or null
    var pickedPhotoType = null;      // selected weather type ('storm', 'rain', etc.)

    function showReportState(stateName) {
      // stateName: 'pick' | 'photo' | 'thanks'
      if (reportPick)   reportPick.hidden   = (stateName !== 'pick');
      if (reportPhoto)  reportPhoto.hidden  = (stateName !== 'photo');
      if (reportThanks) reportThanks.hidden = (stateName !== 'thanks');
      // Photo state has two sub-steps; reset to picker every time we open
      if (stateName === 'photo') {
        var pickerStep = $('photo-picker-step');
        var classifyStep = $('photo-classify-step');
        if (pickerStep) pickerStep.hidden = false;
        if (classifyStep) classifyStep.hidden = true;
        pickedPhotoDataUrl = null;
        pickedPhotoType = null;
        var submitBtn = $('photo-submit');
        if (submitBtn) submitBtn.disabled = true;
        var chips = document.querySelectorAll('.photo-type-chip');
        for (var i = 0; i < chips.length; i++) {
          chips[i].classList.remove('is-active');
        }
        var input = $('photo-input');
        if (input) input.value = '';  // allow re-picking same file
      }
    }

    function openReportSheet() {
      if (!reportSheet || !reportScrim) return;
      // Reset to "pick" state every time we open
      showReportState('pick');
      reportScrim.classList.add('active');
      reportSheet.classList.add('active');
    }
    function closeReportSheet() {
      if (!reportSheet || !reportScrim) return;
      reportSheet.classList.remove('active');
      reportScrim.classList.remove('active');
      // Reset the optional confessional note so the next time the sheet
      // opens we start fresh. (Same reason we reset photo state.)
      var noteDetails = $('report-note-details');
      var noteInput = $('report-note-input');
      var noteQuote = $('report-note-quote');
      var noteCount = $('report-note-count-num');
      if (noteDetails) noteDetails.removeAttribute('open');
      if (noteInput) noteInput.value = '';
      if (noteQuote) noteQuote.checked = false;
      if (noteCount) noteCount.textContent = '0';
      var noteCountWrap = document.querySelector('.report-note-count');
      if (noteCountWrap) noteCountWrap.classList.remove('is-near-limit');
    }
    if (reportScrim) {
      reportScrim.addEventListener('click', closeReportSheet);
    }

    // Char counter for the confessional textarea. Stays muted until the
    // user is within 50 chars of the limit, then warns in amber.
    var noteInputEl = $('report-note-input');
    if (noteInputEl) {
      noteInputEl.addEventListener('input', function() {
        var n = noteInputEl.value.length;
        var countEl = $('report-note-count-num');
        var wrapEl = document.querySelector('.report-note-count');
        if (countEl) countEl.textContent = String(n);
        if (wrapEl) wrapEl.classList.toggle('is-near-limit', n >= 550);
      });
    }

    /**
     * Compress an image File to a data URL suitable for localStorage.
     * Downscales to max 800px on the longest side, re-encodes as JPEG
     * at 0.7 quality. A 4 MB phone photo typically lands at 80–150 KB.
     * Returns a Promise that resolves to the data URL.
     */
    function compressImageFile(file) {
      return new Promise(function(resolve, reject) {
        var reader = new FileReader();
        reader.onload = function() {
          var img = new Image();
          img.onload = function() {
            var MAX = 800;
            var w = img.width, h = img.height;
            if (w > h && w > MAX) { h = Math.round(h * (MAX / w)); w = MAX; }
            else if (h >= w && h > MAX) { w = Math.round(w * (MAX / h)); h = MAX; }
            var canvas = document.createElement('canvas');
            canvas.width = w; canvas.height = h;
            var ctx = canvas.getContext('2d');
            ctx.drawImage(img, 0, 0, w, h);
            try {
              var dataUrl = canvas.toDataURL('image/jpeg', 0.7);
              resolve(dataUrl);
            } catch (e) { reject(e); }
          };
          img.onerror = function() { reject(new Error('Image decode failed')); };
          img.src = reader.result;
        };
        reader.onerror = function() { reject(reader.error); };
        reader.readAsDataURL(file);
      });
    }

    /**
     * Submit a Quick Report. Centralized so both the four-button text path
     * and the photo path go through one code path. `photoDataUrl` is null
     * for text-only reports.
     */
    function submitQuickReport(reportType, photoDataUrl) {
      // Increment the user's clarity streak + reports count and persist
      var s = auth.currentState();
      if (s) {
        s.streak = (s.streak || 0) + 1;
        s.reportsCount = (s.reportsCount || 0) + 1;
        // Tiny bump in protected hours per report (mock formula — real
        // backend would calculate this from window-coverage)
        s.protectedHours = (s.protectedHours || 0) + 2;
      }
      var streakEl = $('report-thanks-streak');
      if (streakEl) {
        streakEl.textContent = 'Clarity streak: ' + (s ? s.streak : 1);
      }

      var typeText = {
        storm:    'Storm building here. Sharing what I\u2019m seeing.',
        hail:     'Just saw hail here. Reporting from the ground.',
        wind:     'Picking up wind here \u2014 noticeable gusts.',
        rain:     'Rain just started here.',
        snow:     'Snow falling here right now.',
        sleet:    'Sleet here \u2014 ice pellets mixed with rain.',
        freezing: 'Freezing rain here. Surfaces icing over.',
        fog:      'Heavy fog here. Visibility cut down.',
        sunny:    'Clear skies and full sun where I am.',
        cloudy:   'Overcast here. No precip yet.',
        humid:    'Heavy humidity here \u2014 sticky and warm.',
        clearing: 'Weather is letting up here. Things are calming down.',
        clear:    'Conditions clearing out where I am.'
      }[reportType] || 'Crew report.';
      // Override copy when a photo is attached — different voice
      if (photoDataUrl) {
        typeText = {
          storm:    'Storm rolling in \u2014 see attached.',
          hail:     'Hail on the ground \u2014 photo attached.',
          wind:     'Wind picking up \u2014 photo attached.',
          rain:     'Rain here now \u2014 photo attached.',
          snow:     'Snow falling \u2014 photo attached.',
          sleet:    'Sleet on the ground \u2014 photo attached.',
          freezing: 'Freezing rain \u2014 photo attached.',
          fog:      'Fog here \u2014 photo attached.',
          sunny:    'Sunny here \u2014 photo attached.',
          cloudy:   'Overcast skies \u2014 photo attached.',
          humid:    'Humid conditions \u2014 photo attached.',
          clearing: 'Sky clearing up \u2014 photo attached.',
          clear:    'Sky clearing up \u2014 photo attached.'
        }[reportType] || 'Photo report from the ground.';
      }

      var user = auth.currentUser();
      // Try to pull the state code from "City, ST" format. If the user's
      // signup city doesn't have a state suffix, leave it null — the Crew
      // evidence matcher falls back to city-name matching in that case.
      var userStateMatch = (user.city || '').match(/,\s*([A-Z]{2})\b/);
      var userState = userStateMatch ? userStateMatch[1] : null;
      // Pull the optional confessional note from the textarea (if filled)
      // and the "quote me on social" flag. Both are stored on the report
      // so the Met sees the note, and the social-publish toggle gates any
      // external use later.
      var noteInput = $('report-note-input');
      var quoteCheckbox = $('report-note-quote');
      var noteText = (noteInput && noteInput.value || '').trim();
      var noteQuoteable = !!(quoteCheckbox && quoteCheckbox.checked);

      var newReport = {
        id: 'r-user-' + Date.now().toString(36),
        type: reportType,
        author: '@' + (user.handle || 'crew'),
        authorFull: user.name,
        authorId: user.id,
        city: user.city || 'Boone County, IN',
        state: userState || 'IN',  // fallback assumes founding-territory tester
        county: 'Boone County',
        timeAgo: 'just now',
        timeMinutes: 0,
        submittedAt: new Date().toISOString(),
        text: typeText,
        note: noteText || null,
        noteQuoteable: noteQuoteable,
        noteTimestamp: noteText ? new Date().toISOString() : null,
        verified: 0,
        tips: { nod: 0, coffee: 0, handshake: 0 },
        streak: s ? s.streak : 1,
        hasPhoto: !!photoDataUrl,
        photoDataUrl: photoDataUrl || null,
        photoStyle: photoDataUrl ? reportType : null,  // for fallback rendering
        userSubmitted: true,
        zoomTier: 'city',
        mapPosCity:   { top: '40%', left: '54%' },
        mapPosCounty: { top: '50%', left: '46%' },
        mapPosRegion: { top: '34%', left: '26%' },
        mapPosState:  { top: '38%', left: '34%' }
      };
      newReport.mapPos = newReport.mapPosRegion;
      CREW_REPORTS.unshift(newReport);
      if (s) s.myReports = (s.myReports || []).concat([newReport.id]);
      persistReports();

      // If they wrote a note in the Quick Report sheet, log it to the
      // field notebook too. Same persistence path as mission confessionals.
      if (noteText) {
        if (!window.CREW_CONFESSIONALS) window.CREW_CONFESSIONALS = [];
        window.CREW_CONFESSIONALS.unshift({
          id: 'c-' + Date.now().toString(36),
          text: noteText,
          quoteable: noteQuoteable,
          timestamp: new Date().toISOString(),
          missionId: null,
          missionLabel: 'Quick Report \u00b7 ' + reportType,
          answerLabel: null
        });
        try {
          localStorage.setItem('wv_crew_confessionals_v1', JSON.stringify(window.CREW_CONFESSIONALS));
        } catch (e) {}
        if (typeof renderCrewNotebook === 'function') renderCrewNotebook();
      }

      auth.persist();
      afterStateChange();

      tokens.earn(10);
      var tokensEl = $('tokens-chip-value');
      if (tokensEl) {
        tokensEl.classList.add('bump');
        setTimeout(function() { tokensEl.classList.remove('bump'); }, 320);
      }
      var burst = $('tokens-burst');
      if (burst && burst.parentNode) {
        var clone = burst.cloneNode(true);
        burst.parentNode.replaceChild(clone, burst);
      }

      showReportState('thanks');
      setTimeout(function() {
        try { renderFeed(); } catch (e) {}
      }, 100);
      setTimeout(closeReportSheet, 1800);
    }

    // Tap a category in the four-button grid → either submit immediately
    // (text categories) or open the photo state (photo category).
    document.addEventListener('click', function(ev) {
      var opt = ev.target.closest('[data-report]');
      if (!opt) return;
      ev.preventDefault();

      // Auth gate — same as before
      if (!auth.isSignedIn()) {
        closeReportSheet();
        setTimeout(function() {
          requireAuth('report', function() {
            openReportSheet();
          });
        }, 280);
        return;
      }

      var reportType = opt.getAttribute('data-report');

      // Photo category opens the photo capture state instead of submitting
      if (reportType === 'photo') {
        showReportState('photo');
        return;
      }

      // Other categories submit immediately as text-only reports
      submitQuickReport(reportType, null);
    });

    // ── Photo state wiring ─────────────────────────────────────────────
    var photoInput = $('photo-input');
    if (photoInput) {
      photoInput.addEventListener('change', function(ev) {
        var file = ev.target.files && ev.target.files[0];
        if (!file) return;
        compressImageFile(file)
          .then(function(dataUrl) {
            pickedPhotoDataUrl = dataUrl;
            var preview = $('photo-preview');
            if (preview) preview.src = dataUrl;
            // Swap to the classify sub-step
            var pickerStep = $('photo-picker-step');
            var classifyStep = $('photo-classify-step');
            if (pickerStep) pickerStep.hidden = true;
            if (classifyStep) classifyStep.hidden = false;
          })
          .catch(function(err) {
            // Quiet failure — log and reset the input. A real product would
            // surface a friendly error toast here.
            console.error('Photo compression failed:', err);
            ev.target.value = '';
          });
      });
    }

    // Type chip selection — single-select, enables submit when picked
    document.addEventListener('click', function(ev) {
      var chip = ev.target.closest('.photo-type-chip');
      if (!chip) return;
      ev.preventDefault();
      var chips = document.querySelectorAll('.photo-type-chip');
      for (var i = 0; i < chips.length; i++) chips[i].classList.remove('is-active');
      chip.classList.add('is-active');
      pickedPhotoType = chip.getAttribute('data-photo-type');
      var submitBtn = $('photo-submit');
      if (submitBtn) submitBtn.disabled = false;
    });

    // Retake — clear the picked image and go back to picker
    var retakeBtn = $('photo-retake');
    if (retakeBtn) {
      retakeBtn.addEventListener('click', function(ev) {
        ev.preventDefault();
        pickedPhotoDataUrl = null;
        pickedPhotoType = null;
        var pickerStep = $('photo-picker-step');
        var classifyStep = $('photo-classify-step');
        if (pickerStep) pickerStep.hidden = false;
        if (classifyStep) classifyStep.hidden = true;
        var input = $('photo-input');
        if (input) input.value = '';
        var chips = document.querySelectorAll('.photo-type-chip');
        for (var j = 0; j < chips.length; j++) chips[j].classList.remove('is-active');
        var submitBtn = $('photo-submit');
        if (submitBtn) submitBtn.disabled = true;
      });
    }

    // Photo submit — call the central submission helper with the data URL
    var photoSubmitBtn = $('photo-submit');
    if (photoSubmitBtn) {
      photoSubmitBtn.addEventListener('click', function(ev) {
        ev.preventDefault();
        if (!pickedPhotoDataUrl || !pickedPhotoType) return;
        submitQuickReport(pickedPhotoType, pickedPhotoDataUrl);
      });
    }
    // Esc key closes the sheet (desktop convenience)
    document.addEventListener('keydown', function(ev) {
      if (ev.key === 'Escape' && reportSheet && reportSheet.classList.contains('active')) {
        closeReportSheet();
      }
    });

    // Update the welcome page phone clock to the current time
    var now = new Date();
    var hr = now.getHours() % 12 || 12;
    var mn = now.getMinutes();
    var nowStr = hr + ':' + (mn < 10 ? '0' : '') + mn;
    var welcomeClock = $('welcome-phone-clock');
    var welcomeBar = $('welcome-phone-time');
    var welcomeDate = $('welcome-phone-date');
    if (welcomeClock) welcomeClock.textContent = nowStr;
    if (welcomeBar) welcomeBar.textContent = nowStr;
    if (welcomeDate) welcomeDate.textContent = now.toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' });
  }

  // ════════════════════════════════════════════════════════════════════════
  // LIVE VALET BACKEND INTEGRATION (v2.5)
  // ════════════════════════════════════════════════════════════════════════
  //
  // Wires the existing upsell modal to the Flask backend at /api/v1/...
  //
  // Two operating modes:
  //
  //   1. LIVE MODE — when window.WV_API_BASE points at a running backend.
  //      Real Stripe checkout, real Twilio SMS, real meteorologist queue.
  //      Set this on the page BEFORE this script runs, e.g.:
  //          window.WV_API_BASE = 'https://api.weathervalet.ai';
  //
  //   2. DEMO MODE — automatic fallback when the backend is unreachable.
  //      Skips Stripe entirely. Walks through the full flow visually:
  //      pricing → contact → simulated payment → standby with countdown
  //      → simulated meteorologist verdict appears after a few seconds.
  //      You can demo the product on a laptop with no backend running.
  //
  // The mode is decided by a quick health probe at upsell time, not at page
  // load — this way the page works even if the backend goes down between
  // the user opening the page and clicking the upsell.

  var WV_API_BASE = (typeof window !== 'undefined' && window.WV_API_BASE) || '';
  var WV_DEMO_MODE = false;
  var WV_CURRENT_REQUEST_ID = null;
  var WV_POLL_TIMER = null;
  var WV_DEMO_TIMER = null;

  // Capture the latest ticket + reading on every render so the upsell knows
  // which plan it's selling a verification of. Wrap renderTicket.
  var _wvLastTicket = null;
  var _wvLastReading = null;
  var _origRenderTicket = renderTicket;
  renderTicket = function(ticket, reading) {
    _wvLastTicket = ticket;
    _wvLastReading = reading;
    return _origRenderTicket.apply(this, arguments);
  };

  function wvProbeBackend() {
    if (!WV_API_BASE) return Promise.resolve(false);
    return new Promise(function(resolve) {
      var done = false;
      var settle = function(ok) { if (!done) { done = true; resolve(ok); } };
      setTimeout(function() { settle(false); }, 1500);
      fetch(WV_API_BASE + '/healthz', { method: 'GET' })
        .then(function(r) { settle(r.ok); })
        .catch(function() { settle(false); });
    });
  }

  // Override the placeholder confirm handler with the real flow. This
  // reassignment must happen BEFORE init() runs, because init() reads
  // handleConfirmVerification once when wiring the click listener.
  handleConfirmVerification = function() {
    var phoneInput = $('sms-phone');
    var phoneVal = phoneInput ? phoneInput.value.trim() : '';
    var display = $('standby-phone-display');
    if (display) display.textContent = phoneVal || 'your phone';

    if (!_wvLastTicket) {
      WV_DEMO_MODE = true;
      showModalStep('standby');
      wvStartDemoCountdown(phoneVal);
      setTimeout(wvShowModeIndicator, 200);
      return;
    }

    showModalStep('standby');
    wvShowStandbyMessage('Connecting to secure checkout\u2026');

    wvProbeBackend().then(function(ok) {
      if (!ok) {
        WV_DEMO_MODE = true;
        wvShowDemoBanner();
        wvShowModeIndicator();
        wvStartDemoCountdown(phoneVal);
        return;
      }
      WV_DEMO_MODE = false;
      wvShowModeIndicator();
      var tier = wvSelectedTier() || 'single';
      fetch(WV_API_BASE + '/api/v1/verification/checkout', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
          tier: tier,
          plan_text: _wvLastTicket.query || _wvLastTicket.industryLabel || '',
          plan_industry: _wvLastTicket.industry || 'general',
          plan_location: _wvLastTicket.area || _wvLastTicket.city || '',
          plan_window: _wvLastTicket.scheduledShort || '',
          ai_status_key: (_wvLastReading && _wvLastReading.statusKey) || 'caution',
          customer_phone: phoneVal,
          customer_email: ''
        })
      }).then(function(r) {
        return r.json().then(function(body) {
          if (!r.ok) throw new Error(body.error || ('HTTP ' + r.status));
          return body;
        });
      }).then(function(data) {
        if (data.fake === true) {
          WV_CURRENT_REQUEST_ID = data.request_id;
          wvShowStandbyMessage('Your meteorologist has been notified. Reviewing your plan\u2026');
          wvStartStatusPolling(data.request_id);
          return;
        }
        if (data.checkout_url) {
          window.location.href = data.checkout_url;
          return;
        }
        throw new Error('No checkout URL returned');
      }).catch(function(err) {
        console.warn('[wv] live checkout failed, falling back to demo:', err.message);
        WV_DEMO_MODE = true;
        wvShowDemoBanner();
        wvShowModeIndicator();
        wvStartDemoCountdown(phoneVal);
      });
    });
  };

  function wvSelectedTier() {
    var selected = document.querySelector('[data-tier-selected]');
    if (selected) return selected.getAttribute('data-tier-selected');
    return 'single';
  }

  function wvShowStandbyMessage(text) {
    var msg = document.getElementById('wv-standby-message');
    if (msg) { msg.textContent = text; return; }
    var standbyStep = document.getElementById('modal-step-standby');
    if (!standbyStep) return;
    var existing = standbyStep.querySelector('.standby-body, .standby-text, p');
    if (existing) {
      var n = document.createElement('p');
      n.id = 'wv-standby-message';
      n.style.cssText = 'margin-top:12px;font-size:13px;color:rgba(15,17,22,0.65);font-family:Inter,system-ui,sans-serif';
      n.textContent = text;
      existing.parentNode.insertBefore(n, existing.nextSibling);
    } else {
      var n2 = document.createElement('p');
      n2.id = 'wv-standby-message';
      n2.style.cssText = 'margin-top:12px;font-size:13px;color:rgba(15,17,22,0.65);font-family:Inter,system-ui,sans-serif';
      n2.textContent = text;
      standbyStep.appendChild(n2);
    }
  }

  function wvShowDemoBanner() {
    if (document.getElementById('wv-demo-banner')) return;
    var standbyStep = document.getElementById('modal-step-standby');
    if (!standbyStep) return;
    var b = document.createElement('div');
    b.id = 'wv-demo-banner';
    b.style.cssText = ''
      + 'margin: 12px 0; padding: 10px 12px;'
      + 'background: rgba(232, 164, 0, 0.08);'
      + 'border-left: 3px solid #E8A400;'
      + 'border-radius: 8px;'
      + 'font-size: 12px; line-height: 1.5;'
      + 'color: rgba(15,17,22,0.75);'
      + 'font-family: Inter, system-ui, sans-serif;';
    b.innerHTML = ''
      + '<strong style="color:#8a5a0a">Demo mode</strong> &mdash; '
      + 'no backend connected, so we\'re simulating the meteorologist response. '
      + 'In production, you\'d be on Stripe\'s checkout page right now, then receive '
      + 'a real SMS, then see the actual verdict here.';
    standbyStep.insertBefore(b, standbyStep.firstChild);
  }

  function wvStartDemoCountdown(phoneVal) {
    if (WV_DEMO_TIMER) clearTimeout(WV_DEMO_TIMER);
    var sequence = [
      { delay: 1500, msg: 'Payment received. Your meteorologist has been notified.' },
      { delay: 4000, msg: 'Your meteorologist is reviewing your plan\u2026' },
      { delay: 9000, done: true }
    ];
    sequence.forEach(function(step) {
      WV_DEMO_TIMER = setTimeout(function() {
        if (step.done) wvRenderDemoVerdict();
        else wvShowStandbyMessage(step.msg);
      }, step.delay);
    });
  }

  function wvRenderDemoVerdict() {
    var ticket = _wvLastTicket || {};
    var reading = _wvLastReading || { statusKey: 'caution' };
    var verdict, notes;
    if (reading.statusKey === 'clear') {
      verdict = 'Looks good for your ' + (ticket.industryLabel || 'plan') + '. Confirmed clear conditions through your window. Trust the Valet.';
      notes = 'High pressure ridge holds. No model spread of concern.';
    } else if (reading.statusKey === 'risk') {
      verdict = 'I\'d re-plan this one. ' + (reading.reasons && reading.reasons[0] ? reading.reasons[0] + '. ' : '') + 'Better window opens up tomorrow morning if you can shift.';
      notes = 'Multiple model agreement on storm timing. Not a marginal call.';
    } else {
      verdict = 'Your window is workable but watch ' +
        (reading.reasons && reading.reasons[0] && reading.reasons[0].toLowerCase().indexOf('wind') !== -1 ? 'the wind' : 'conditions') +
        ' through the afternoon. ' +
        (ticket.industryLabel === 'pour' ? 'Have covers staged just in case.' : 'Have a backup plan ready.');
      notes = 'HRRR vs RAP disagree on timing of front; went with HRRR (better skill in this region).';
    }
    wvShowVerdict(verdict, notes, true);
  }

  function wvStartStatusPolling(requestId) {
    var tries = 0;
    function tick() {
      tries++;
      fetch(WV_API_BASE + '/api/v1/verification/status?rid=' + encodeURIComponent(requestId))
        .then(function(r) { return r.json(); })
        .then(function(data) {
          if (data.error) { wvShowStandbyMessage('Status unavailable: ' + data.error); return; }
          var copy = {
            pending: 'Waiting for payment to clear\u2026',
            paid: 'Payment cleared. Your meteorologist has been notified.',
            claimed: 'Your meteorologist is on it. Brief coming any moment.',
            completed: 'Your meteorologist\'s call is in.',
            expired: 'This request expired. Please start a new one.'
          };
          wvShowStandbyMessage(copy[data.status] || data.status);
          if (data.status === 'completed') { wvShowVerdict(data.verdict, data.notes, false); return; }
          if (data.status === 'expired' || tries > 90) return;
          WV_POLL_TIMER = setTimeout(tick, 4000);
        })
        .catch(function() {
          if (tries < 5) WV_POLL_TIMER = setTimeout(tick, 6000);
          else wvShowStandbyMessage('Connection lost \u2014 try refreshing the page.');
        });
    }
    tick();
  }

  function wvShowVerdict(verdict, notes, isDemo) {
    var standbyStep = document.getElementById('modal-step-standby');
    if (!standbyStep) { alert('Verdict: ' + verdict); return; }
    if (WV_DEMO_TIMER) clearTimeout(WV_DEMO_TIMER);
    if (WV_POLL_TIMER) clearTimeout(WV_POLL_TIMER);

    var verdictHtml = ''
      + (isDemo ? '<div style="margin:0 0 12px;padding:8px 10px;background:rgba(232,164,0,0.08);border-left:3px solid #E8A400;border-radius:6px;font-size:11px;color:#8a5a0a;font-family:JetBrains Mono,monospace;letter-spacing:1px;text-transform:uppercase;font-weight:700">Demo verdict &mdash; synthesized for preview</div>' : '')
      + '<div style="display:flex;align-items:center;gap:10px;margin-bottom:14px">'
      +   '<div style="width:36px;height:36px;border-radius:999px;background:rgba(65,105,225,0.10);display:flex;align-items:center;justify-content:center">'
      +     '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="#4169E1" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 4 6v6c0 5 3.5 9.5 8 10 4.5-.5 8-5 8-10V6l-8-4z"/><path d="m9 12 2 2 4-4"/></svg>'
      +   '</div>'
      +   '<div>'
      +     '<div style="font-family:JetBrains Mono,monospace;font-size:10px;font-weight:700;letter-spacing:1.8px;text-transform:uppercase;color:#4169E1">WeatherValet Meteorologist Review</div>'
      +     '<div style="font-family:Fraunces,Georgia,serif;font-size:18px;color:#0E1116;margin-top:2px;font-weight:600">Your review is ready.</div>'
      +   '</div>'
      + '</div>'
      + '<div style="padding:14px 16px;background:rgba(65,105,225,0.05);border-left:3px solid #4169E1;border-radius:0 10px 10px 0;font-family:Fraunces,Georgia,serif;font-size:14.5px;line-height:1.55;color:#0E1116;white-space:pre-wrap">' + escapeHtml(verdict || '') + '</div>';
    if (notes) {
      verdictHtml += ''
        + '<details style="margin-top:14px;font-size:12px;color:rgba(15,17,22,0.55)">'
        +   '<summary style="cursor:pointer;font-weight:600">Meteorologist notes</summary>'
        +   '<p style="margin-top:8px;white-space:pre-wrap">' + escapeHtml(notes) + '</p>'
        + '</details>';
    }
    verdictHtml += ''
      + '<button type="button" onclick="document.getElementById(\'upsell-modal\').classList.remove(\'active\')" '
      + 'style="margin-top:18px;width:100%;background:#4169E1;color:#fff;padding:12px 16px;border:none;border-radius:10px;font-family:Inter,system-ui,sans-serif;font-weight:600;font-size:14px;cursor:pointer">'
      + 'Got it &mdash; close</button>';

    standbyStep.innerHTML = verdictHtml;
  }

  function wvShowModeIndicator() {
    var existing = document.getElementById('wv-mode-indicator');
    if (existing) existing.remove();
    var el = document.createElement('div');
    el.id = 'wv-mode-indicator';
    el.style.cssText = ''
      + 'position:fixed;bottom:16px;right:16px;z-index:9999;'
      + 'padding:6px 10px;border-radius:999px;'
      + 'font-family:JetBrains Mono,monospace;font-size:10px;font-weight:700;'
      + 'letter-spacing:1.4px;text-transform:uppercase;'
      + 'background:' + (WV_DEMO_MODE ? 'rgba(232,164,0,0.95)' : 'rgba(18,161,80,0.95)') + ';'
      + 'color:#fff;'
      + 'box-shadow:0 4px 12px rgba(15,17,22,0.18);'
      + 'pointer-events:none;';
    el.textContent = WV_DEMO_MODE ? 'demo mode' : 'live \u00b7 ' + (WV_API_BASE.replace(/^https?:\/\//, '').slice(0, 24));
    document.body.appendChild(el);
  }

  window.WVTest = {
    demo: function() {
      _wvLastTicket = _wvLastTicket || { industryLabel: 'pour', query: 'demo plan', industry: 'concrete' };
      _wvLastReading = _wvLastReading || { statusKey: 'caution', reasons: ['Watch the wind'] };
      WV_DEMO_MODE = true;
      var modal = document.getElementById('upsell-modal');
      if (modal) modal.classList.add('active');
      showModalStep('standby');
      wvShowDemoBanner();
      wvShowModeIndicator();
      wvStartDemoCountdown('+15555550100');
    },
    setApiBase: function(url) { WV_API_BASE = url; console.log('[wv] API base set to', url); }
  };

  // ── Boot ─────────────────────────────────────────────────────────────────
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
</script>

<!-- ──────────────────────────────────────────────────────────────
     Rosie — AI assistant for the Met team (Phase 1: web chat)
     Floating side panel, opens via "Ask Rosie" button in workspace.
     Lives outside any specific panel so it overlays the whole UI.
     ────────────────────────────────────────────────────────────── -->
<div id="rosie-panel" style="position:fixed; top:0; right:-440px; width:420px; max-width:92vw; height:100vh; background:#fff; border-left:1px solid rgba(15,17,22,0.08); box-shadow:-8px 0 32px rgba(15,17,22,0.10); z-index:9999; display:flex; flex-direction:column; transition:right 0.3s ease;">
  <!-- Header -->
  <div style="padding:14px 18px; border-bottom:1px solid rgba(15,17,22,0.06); display:flex; align-items:center; justify-content:space-between; background:linear-gradient(135deg, #fef5f7, #fff);">
    <div style="display:flex; align-items:center; gap:10px;">
      <div style="width:34px; height:34px; border-radius:50%; background:linear-gradient(135deg, #f5708e, #c23450); display:flex; align-items:center; justify-content:center; font-size:18px; box-shadow:0 2px 8px rgba(194,52,80,0.25);">🌹</div>
      <div>
        <div style="font-weight:600; color:#0f1116; font-size:14px; letter-spacing:-0.01em;">Rosie</div>
        <div style="font-size:11px; color:rgba(15,17,22,0.55);">your team assistant</div>
      </div>
    </div>
    <button type="button" id="rosie-close-btn" aria-label="Close Rosie"
            style="background:transparent; border:0; cursor:pointer; padding:4px; color:rgba(15,17,22,0.45); font-size:20px; line-height:1;">×</button>
  </div>

  <!-- Message list -->
  <div id="rosie-messages" style="flex:1; overflow-y:auto; padding:18px; display:flex; flex-direction:column; gap:14px; background:#fafafa;">
    <!-- Welcome message (replaced if history exists) -->
    <div class="rosie-msg rosie-msg-assistant" style="align-self:flex-start; max-width:85%;">
      <div style="background:#fff; border:1px solid rgba(15,17,22,0.06); border-radius:12px 12px 12px 4px; padding:10px 14px; font-size:13px; line-height:1.45; color:#0f1116;">
        Hey — I'm Rosie, your team assistant. I can answer questions about your schedule, earnings, brief tasks, and how-to docs. I can also schedule reminders and draft requests for Michael.
<br><br>
What can I help with?
      </div>
      <div style="font-size:10px; color:rgba(15,17,22,0.45); margin-top:4px; padding-left:4px;">— Rosie</div>
    </div>
  </div>

  <!-- Input -->
  <div style="border-top:1px solid rgba(15,17,22,0.06); padding:12px 14px; background:#fff;">
    <form id="rosie-form" style="display:flex; gap:8px; align-items:flex-end;">
      <textarea id="rosie-input" rows="2" placeholder="Ask Rosie..." maxlength="4000"
        style="flex:1; border:1px solid rgba(15,17,22,0.12); border-radius:8px; padding:8px 10px; font-size:13px; font-family:inherit; resize:none; outline:none; line-height:1.4;"></textarea>
      <button type="submit" id="rosie-send-btn"
        style="background:#c23450; color:#fff; border:0; border-radius:8px; padding:10px 14px; font-size:13px; font-weight:600; cursor:pointer; white-space:nowrap;">Send</button>
    </form>
    <div style="font-size:10px; color:rgba(15,17,22,0.40); margin-top:6px; text-align:center;">Rosie may make mistakes — verify important info.</div>
  </div>
</div>

<script>
(function() {
  var BACKEND = 'https://wv-valet-backend.onrender.com';
  var panel = document.getElementById('rosie-panel');
  var openBtn = document.getElementById('rosie-open-btn');
  var closeBtn = document.getElementById('rosie-close-btn');
  var msgList = document.getElementById('rosie-messages');
  var input = document.getElementById('rosie-input');
  var form = document.getElementById('rosie-form');
  var sendBtn = document.getElementById('rosie-send-btn');
  var loadedHistory = false;

  function escapeHtml(s) {
    var d = document.createElement('div');
    d.textContent = s || '';
    return d.innerHTML;
  }

  function openPanel() {
    panel.style.right = '0';
    if (!loadedHistory) {
      loadHistory();
      loadedHistory = true;
    }
    setTimeout(function() { input.focus(); }, 320);
  }

  function closePanel() {
    panel.style.right = '-440px';
  }

  function appendMessage(role, content) {
    var wrap = document.createElement('div');
    wrap.className = 'rosie-msg rosie-msg-' + role;
    wrap.style.cssText = 'align-self:' + (role === 'user' ? 'flex-end' : 'flex-start') + '; max-width:85%;';
    var bubble = document.createElement('div');
    if (role === 'user') {
      bubble.style.cssText = 'background:#c23450; color:#fff; border-radius:12px 12px 4px 12px; padding:10px 14px; font-size:13px; line-height:1.45;';
    } else {
      bubble.style.cssText = 'background:#fff; border:1px solid rgba(15,17,22,0.06); border-radius:12px 12px 12px 4px; padding:10px 14px; font-size:13px; line-height:1.45; color:#0f1116; white-space:pre-wrap;';
    }
    bubble.textContent = content;
    wrap.appendChild(bubble);
    if (role === 'assistant') {
      var sig = document.createElement('div');
      sig.style.cssText = 'font-size:10px; color:rgba(15,17,22,0.45); margin-top:4px; padding-left:4px;';
      // Don't add — Rosie suffix if the message already ends with it
      sig.textContent = /[—-]\s*Rosie\s*$/.test(content) ? '' : '— Rosie';
      wrap.appendChild(sig);
    }
    msgList.appendChild(wrap);
    msgList.scrollTop = msgList.scrollHeight;
  }

  function appendTyping() {
    var wrap = document.createElement('div');
    wrap.id = 'rosie-typing';
    wrap.style.cssText = 'align-self:flex-start; max-width:85%;';
    wrap.innerHTML = '<div style="background:#fff; border:1px solid rgba(15,17,22,0.06); border-radius:12px 12px 12px 4px; padding:10px 14px; font-size:13px; color:rgba(15,17,22,0.55);">Rosie is thinking…</div>';
    msgList.appendChild(wrap);
    msgList.scrollTop = msgList.scrollHeight;
  }

  function removeTyping() {
    var t = document.getElementById('rosie-typing');
    if (t) t.parentNode.removeChild(t);
  }

  function loadHistory() {
    fetch(BACKEND + '/api/v1/rosie/history', { credentials: 'include' })
      .then(function(r) { return r.json(); })
      .then(function(j) {
        if (!j || !j.ok || !j.messages || !j.messages.length) return;
        // Clear welcome message and replace with history
        msgList.innerHTML = '';
        for (var i = 0; i < j.messages.length; i++) {
          appendMessage(j.messages[i].role, j.messages[i].content);
        }
      })
      .catch(function(err) { console.warn('[rosie] history load failed', err); });
  }

  function sendMessage(text) {
    if (!text || sendBtn.disabled) return;
    appendMessage('user', text);
    input.value = '';
    sendBtn.disabled = true;
    sendBtn.style.opacity = '0.5';
    appendTyping();
    fetch(BACKEND + '/api/v1/rosie/chat', {
      method: 'POST',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ message: text })
    })
    .then(function(r) { return r.json().then(function(j) { return { status: r.status, body: j }; }); })
    .then(function(res) {
      removeTyping();
      sendBtn.disabled = false;
      sendBtn.style.opacity = '1';
      if (res.body && res.body.ok && res.body.reply) {
        appendMessage('assistant', res.body.reply);
      } else if (res.status === 401) {
        appendMessage('assistant', "I can't see who you are — try logging in again.");
      } else if (res.status === 403) {
        appendMessage('assistant', "I'm only available to the Met team right now.");
      } else {
        var err = (res.body && res.body.error) || ('error ' + res.status);
        appendMessage('assistant', "Something went wrong: " + err + ". Try again in a moment.");
      }
    })
    .catch(function(err) {
      removeTyping();
      sendBtn.disabled = false;
      sendBtn.style.opacity = '1';
      appendMessage('assistant', "Couldn't reach my brain. Check your internet, then try again.");
      console.error('[rosie] send failed', err);
    });
  }

  if (openBtn) openBtn.addEventListener('click', openPanel);
  if (closeBtn) closeBtn.addEventListener('click', closePanel);
  if (form) {
    form.addEventListener('submit', function(e) {
      e.preventDefault();
      sendMessage(input.value.trim());
    });
  }
  if (input) {
    input.addEventListener('keydown', function(e) {
      // Cmd/Ctrl+Enter sends, plain Enter creates newline
      if (e.key === 'Enter' && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        sendMessage(input.value.trim());
      }
    });
  }
})();
</script>

</body>
</html>
