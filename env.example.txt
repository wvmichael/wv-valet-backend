# WeatherValet · Live Valet Verification — environment template
# ===============================================================
#
# Copy to .env and fill in real values. NEVER commit the filled file to git.
# Add `.env` to your .gitignore.

# ── Stripe ──
# Get test keys at https://dashboard.stripe.com/test/apikeys
# Test keys begin with sk_test_ and pk_test_; switch to sk_live_ / pk_live_
# AFTER passing Stripe's account activation review.
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
# Webhook signing secret. Get at Dashboard → Developers → Webhooks → your
# endpoint → "Signing secret". Begins with whsec_
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME

# ── Twilio ──
# Get credentials at https://console.twilio.com
TWILIO_ACCOUNT_SID=AC_REPLACE_ME
TWILIO_AUTH_TOKEN=REPLACE_ME
# A Twilio phone number you've provisioned, in E.164 format.
TWILIO_FROM_NUMBER=+15555550100

# ── Meteorologist on duty ──
# For v1, single phone. The on-duty meteorologist gets every notification.
# Add routing logic when you have more than one.
METEOROLOGIST_PHONE=+15555550101
METEOROLOGIST_NAME=Timmy

# ── URLs ──
# How Stripe reaches us for webhooks (must be HTTPS in production).
# For local dev: use `stripe listen --forward-to localhost:8080/webhooks/stripe`
# or ngrok.
PUBLIC_BASE_URL=https://api.weathervalet.ai

# Where customers came from — used for redirect-back URLs.
FRONTEND_BASE_URL=https://weathervalet.ai

# ── Optional ──
# Admin dashboard auth. If unset, /admin/queue is open. Always set in prod.
WV_ADMIN_USER=admin
WV_ADMIN_PASS=REPLACE_ME

# Where SQLite lives. Override for tests / multi-tenant deployments.
WV_DB_PATH=wv_valet.db

# ── Gemini AI (powers the friendly "what this means for you" paragraph) ──
# Get a free API key at https://aistudio.google.com/app/apikey
# (Sign in with Google, click "Create API key", copy the value.)
# The free tier allows ~15 requests/minute — plenty for prototype testing.
GEMINI_API_KEY=

# Which Gemini model to use. gemini-1.5-flash-latest is fast and cheap.
# If you want higher-quality output, try gemini-1.5-pro-latest (slower,
# uses more of the free quota per call).
GEMINI_MODEL=gemini-1.5-flash-latest

# ── CORS — which frontend origins can call this backend ──
# Comma-separated. Includes localhost by default for dev. Add your
# Netlify URL once you deploy the frontend.
WV_ALLOWED_ORIGINS=https://weathervalet.ai,https://www.weathervalet.ai,http://localhost:8000,http://localhost:8080

# DEV-ONLY: skip Stripe entirely, mark requests paid immediately.
# NEVER set this in production — it would let anyone get free verifications.
# WV_ALLOW_FAKE_PAYMENT=1